---
phase: 13-nyquist-coverage-uplift
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-openai-surface/03-VALIDATION.md
  - .planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-03-ESCALATIONS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md
  - internal/adapter/openai/*_test.go
  - internal/adapter/openai/testdata/**
autonomous: true
requirements:
  - NYQ-03
tags:
  - nyquist
  - validation
  - openai

must_haves:
  truths:
    - "Phase 03 VALIDATION.md flips from nyquist_compliant: false to nyquist_compliant: true."
    - "Per-Task Verification Map contains one row for every task ID across the 4 Phase-03 plans (15 task surface)."
    - "Every map row has automated <verify>, Wave 0 fixture, or manual-only rationale."
    - "All 6 Validation Sign-Off boxes ticked for Phase 03."
    - "Any new behavioral tests pass under `go test -race ./internal/adapter/openai/...`."
    - "Read-only implementation rule held: zero production-source edits attributable to this plan."
  artifacts:
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt"
      provides: "Per-task gap classification for Phase 03"
    - path: ".planning/phases/03-openai-surface/03-VALIDATION.md"
      provides: "Filled per-task map, flipped frontmatter, ticked sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md"
      provides: "What shipped, gap counts, ESCALATEs"
  key_links:
    - from: "03-VALIDATION.md per-task map rows"
      to: "03-{01..04}-PLAN.md task IDs"
      via: "row-per-task enumeration"
      pattern: "03-(01|02|03|04)-\\d+"
---

<objective>
Lift Phase 03 (OpenAI Surface) VALIDATION.md to the post-08.1 standard. Adapter-pattern phase (4 plans, 15 tasks) sharing the canonical engine with Phase 02; expect significant `auto` classification since `internal/adapter/openai/` already has substantial unit + integration coverage. Per NYQ-03: fill the per-task map, document Wave 0 fixtures, tick the 6 sign-off boxes, flip `nyquist_compliant: false → true`.

Output: Updated `03-VALIDATION.md`, possibly new `*_test.go` files under `internal/adapter/openai/`, and a `13-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
@.planning/phases/13-nyquist-coverage-uplift/13-BASELINE.txt
@.planning/phases/03-openai-surface/03-VALIDATION.md
@.planning/phases/03-openai-surface/03-01-PLAN.md
@.planning/phases/03-openai-surface/03-02-PLAN.md
@.planning/phases/03-openai-surface/03-03-PLAN.md
@.planning/phases/03-openai-surface/03-04-PLAN.md
@.planning/phases/03-openai-surface/03-01-SUMMARY.md
@.planning/phases/03-openai-surface/03-02-SUMMARY.md
@.planning/phases/03-openai-surface/03-03-SUMMARY.md
@.planning/phases/03-openai-surface/03-04-SUMMARY.md
</context>

<scope_guard>
This plan MUST NOT edit `internal/adapter/openai/*.go` files that are not `_test.go`, or any other production source. ESCALATE bugs via `13-03-ESCALATIONS.txt` instead of patching.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap list for Phase 03</name>
  <read_first>
    - .planning/phases/03-openai-surface/03-VALIDATION.md
    - .planning/phases/03-openai-surface/03-01-PLAN.md
    - .planning/phases/03-openai-surface/03-02-PLAN.md
    - .planning/phases/03-openai-surface/03-03-PLAN.md
    - .planning/phases/03-openai-surface/03-04-PLAN.md
    - .planning/phases/03-openai-surface/03-{01,02,03,04}-SUMMARY.md
    - internal/adapter/openai/ (read-only — existing test patterns)
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt</files>
  <action>Enumerate every task ID across the 4 Phase-03 PLAN.md files (15 task surface per BASELINE). Classify each task as `auto` / `wave0` / `manual-only` / `escalate-candidate`. Adapter-pattern phase, so expect most tasks to resolve to `auto` against existing `internal/adapter/openai/*_test.go` — verify against the actual file list. Write per-row format from plan 13-01.</action>
  <acceptance_criteria>
    - `.planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt` exists, row count = 15.
    - Each row carries a classification token.
    - `git diff` outside `.planning/phases/13-*/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt &amp;&amp; [ "$(grep -vc '^#' .planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt)" -ge "15" ]</automated>
  </verify>
  <done>Gap list committed with 15 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn gsd-nyquist-auditor against the gap list</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt
    - .planning/phases/03-openai-surface/03-VALIDATION.md
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
    - internal/adapter/openai/ (read-only — surface + existing tests)
  </read_first>
  <files>internal/adapter/openai/*_test.go, internal/adapter/openai/testdata/**, .planning/phases/03-openai-surface/03-VALIDATION.md, .planning/phases/13-nyquist-coverage-uplift/13-03-ESCALATIONS.txt</files>
  <action>Spawn `gsd-nyquist-auditor` against the gap list. Contract: production `internal/adapter/openai/*.go` (NOT `_test.go`) is read-only — bugs ESCALATE to `13-03-ESCALATIONS.txt`. Read-write surfaces: `internal/adapter/openai/*_test.go`, `internal/adapter/openai/testdata/`, `03-VALIDATION.md`. New behavioral tests must follow adapter-package conventions (request-shape decode → canonical → response-shape encode round-trips against golden JSON fixtures). Run `go test -race ./internal/adapter/openai/... -timeout 60s` after auditor returns.</action>
  <acceptance_criteria>
    - `gsd-nyquist-auditor` returned with structured-return format.
    - `go test -race ./internal/adapter/openai/... -timeout 60s` exits 0.
    - 03-VALIDATION.md per-task map fully populated.
    - `git diff --name-only -- internal/adapter/openai/ | grep -v _test.go | grep -E '\.go$'` is empty.
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/adapter/openai/... -timeout 60s &amp;&amp; ! git diff --name-only -- internal/adapter/openai/ | grep -v _test.go | grep -E '\.go$'</automated>
  </verify>
  <done>Per-task map populated; openai-adapter tests green under `-race`.</done>
</task>

<task type="auto">
  <name>Task 3: Flip frontmatter + sign-off ticks</name>
  <read_first>
    - .planning/phases/03-openai-surface/03-VALIDATION.md (after Task 2)
    - .planning/phases/13-nyquist-coverage-uplift/13-03-ESCALATIONS.txt (if exists)
  </read_first>
  <files>.planning/phases/03-openai-surface/03-VALIDATION.md</files>
  <action>Verify the 6 sign-off constraints; flip `nyquist_compliant: false → true`; tick all 6 boxes; set `**Approval:** approved 2026-06-07`.</action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" …` returns 1; `false` returns 0.
    - 6 sign-off boxes ticked.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'nyquist_compliant: true' .planning/phases/03-openai-surface/03-VALIDATION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'nyquist_compliant: false' .planning/phases/03-openai-surface/03-VALIDATION.md)" = "0" ] &amp;&amp; [ "$(grep -c '^- \[x\]' .planning/phases/03-openai-surface/03-VALIDATION.md)" -ge "6" ]</automated>
  </verify>
  <done>Frontmatter flipped; sign-off ticked.</done>
</task>

<task type="auto">
  <name>Task 4: Write SUMMARY.md documenting Phase 03 uplift</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-03-GAPS.txt
    - .planning/phases/13-nyquist-coverage-uplift/13-03-ESCALATIONS.txt (if exists)
    - .planning/phases/03-openai-surface/03-VALIDATION.md
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md</files>
  <action>Mirror the 13-01 SUMMARY structure: NYQ-03 target, gap counts before/after, BLOCKER/WARNING/ESCALATE counts, new test files, "no production source edited" with git-diff evidence.</action>
  <acceptance_criteria>
    - Summary file exists with cited fields.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md &amp;&amp; grep -qE 'BLOCKER|ESCALATE|production source' .planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md</automated>
  </verify>
  <done>Summary committed.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/adapter/openai/... -timeout 60s` exits 0.
- `grep "nyquist_compliant: true" .planning/phases/03-openai-surface/03-VALIDATION.md` matches once.
</verification>

<success_criteria>
- NYQ-03 satisfied. Zero production-source edits under `internal/adapter/openai/`.
</success_criteria>

<output>
Create `.planning/phases/13-nyquist-coverage-uplift/13-03-SUMMARY.md` when done.
</output>
