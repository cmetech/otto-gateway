---
phase: 13-nyquist-coverage-uplift
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-tool-call-path/06-VALIDATION.md
  - .planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-05-ESCALATIONS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md
  - internal/engine/*_test.go
  - internal/engine/testdata/**
  - internal/jsonformat/*_test.go
  - internal/jsonformat/testdata/**
autonomous: true
requirements:
  - NYQ-06
tags:
  - nyquist
  - validation
  - tool-call

must_haves:
  truths:
    - "Phase 06 VALIDATION.md flips from nyquist_compliant: false to nyquist_compliant: true."
    - "Per-Task Verification Map contains one row for every task ID across the 5 Phase-06 plans (23 task surface)."
    - "Every map row has automated <verify>, Wave 0 fixture, or manual-only rationale."
    - "All 6 Validation Sign-Off boxes ticked for Phase 06."
    - "Any new behavioral tests pass under `go test -race ./internal/engine/... ./internal/jsonformat/...`."
    - "Read-only implementation rule held: zero production-source edits attributable to this plan."
  artifacts:
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt"
      provides: "Per-task gap classification for Phase 06"
    - path: ".planning/phases/06-tool-call-path/06-VALIDATION.md"
      provides: "Filled per-task map, flipped frontmatter, ticked sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md"
      provides: "What shipped, gap counts, ESCALATEs"
  key_links:
    - from: "06-VALIDATION.md per-task map rows"
      to: "06-{01..05}-PLAN.md task IDs"
      via: "row-per-task enumeration"
      pattern: "06-(01|02|03|04|05)-\\d+"
---

<objective>
Lift Phase 06 (Tool-Call Path) VALIDATION.md to the post-08.1 standard. Per-surface tool-call rendering + `coerceToolCall` (5 plans, 23 tasks). The canonical engine's tool-call surface plus three adapter encodings — covers `internal/engine/`, `internal/jsonformat/`, and adapter-side render paths. Per NYQ-06: fill the per-task map, document Wave 0 fixtures, tick the 6 sign-off boxes, flip `nyquist_compliant: false → true`.

Output: Updated `06-VALIDATION.md`, possibly new `*_test.go` files under `internal/engine/` and `internal/jsonformat/` (tool-call encoding lives there), and a `13-05-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
@.planning/phases/13-nyquist-coverage-uplift/13-BASELINE.txt
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-01-PLAN.md
@.planning/phases/06-tool-call-path/06-02-PLAN.md
@.planning/phases/06-tool-call-path/06-03-PLAN.md
@.planning/phases/06-tool-call-path/06-04-PLAN.md
@.planning/phases/06-tool-call-path/06-05-PLAN.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md
@.planning/phases/06-tool-call-path/06-02-SUMMARY.md
@.planning/phases/06-tool-call-path/06-03-SUMMARY.md
@.planning/phases/06-tool-call-path/06-04-SUMMARY.md
@.planning/phases/06-tool-call-path/06-05-SUMMARY.md
</context>

<scope_guard>
This plan MUST NOT edit production `.go` files under `internal/engine/`, `internal/jsonformat/`, or any adapter package. **Important:** the per-surface tool-call render code lives in `internal/adapter/{ollama,openai,anthropic}/` — those production files are owned by plans 13-02 (no, 13-02 owns admin), 13-03 (openai), 13-04 (ollama). For tests that exercise adapter tool-call rendering, write the tests in the package's `_test.go` file — and coordinate via the file_ownership rule: this plan's tests are restricted to `internal/engine/*_test.go` and `internal/jsonformat/*_test.go`. Adapter-side tool-call tests are owned by 13-03 (openai) and 13-04 (ollama); 3.1 anthropic is already compliant. If a Phase-06 PLAN.md task is adapter-only, mark it as `auto` referencing the adapter package's existing tests OR coordinate via the gap list (note "tests live in adapter pkg, written by adapter-uplift plan") without modifying adapter files in THIS plan. ESCALATE bugs via `13-05-ESCALATIONS.txt`.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap list for Phase 06</name>
  <read_first>
    - .planning/phases/06-tool-call-path/06-VALIDATION.md
    - .planning/phases/06-tool-call-path/06-{01..05}-PLAN.md
    - .planning/phases/06-tool-call-path/06-{01..05}-SUMMARY.md
    - .planning/phases/06-tool-call-path/06-HUMAN-UAT.md
    - internal/engine/ (read-only — coerceToolCall surface)
    - internal/jsonformat/ (read-only — tool-call encoding)
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt</files>
  <action>Enumerate every task ID across the 5 Phase-06 PLAN.md files (23 task surface per BASELINE). Classify each. For tasks whose tests would naturally live in an adapter package, note in the row whether existing adapter-package tests already cover the behavior (most do — `internal/adapter/{ollama,openai,anthropic}/*_test.go` from v1.5); those resolve to `auto` referencing the existing file. Only `escalate-candidate` rows that map to `internal/engine/` or `internal/jsonformat/` will receive new tests in THIS plan. Write per-row format.</action>
  <acceptance_criteria>
    - Gap list exists with 23 rows.
    - `git diff` outside `.planning/phases/13-*/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt &amp;&amp; [ "$(grep -vc '^#' .planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt)" -ge "23" ]</automated>
  </verify>
  <done>Gap list committed with 23 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn gsd-nyquist-auditor against the gap list</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt
    - .planning/phases/06-tool-call-path/06-VALIDATION.md
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
    - internal/engine/ (read-only)
    - internal/jsonformat/ (read-only)
  </read_first>
  <files>internal/engine/*_test.go, internal/engine/testdata/**, internal/jsonformat/*_test.go, internal/jsonformat/testdata/**, .planning/phases/06-tool-call-path/06-VALIDATION.md, .planning/phases/13-nyquist-coverage-uplift/13-05-ESCALATIONS.txt</files>
  <action>Spawn `gsd-nyquist-auditor` against the gap list. Contract: production `internal/engine/*.go` and `internal/jsonformat/*.go` (NOT `_test.go`) are read-only — bugs ESCALATE to `13-05-ESCALATIONS.txt`. **Adapter package files are off-limits to this plan** (owned by 13-03/13-04) — for adapter-classified gaps, the auditor's row in 06-VALIDATION.md should reference the existing adapter test file/test name; new adapter-side tests are NOT written here. Read-write surfaces for this plan: `internal/engine/*_test.go`, `internal/jsonformat/*_test.go`, their `testdata/`, and `06-VALIDATION.md`. Run `go test -race ./internal/engine/... ./internal/jsonformat/... -timeout 60s` after auditor returns.</action>
  <acceptance_criteria>
    - `gsd-nyquist-auditor` returned with structured-return format.
    - `go test -race ./internal/engine/... ./internal/jsonformat/... -timeout 60s` exits 0.
    - 06-VALIDATION.md per-task map fully populated.
    - `git diff --name-only -- internal/engine/ internal/jsonformat/ internal/adapter/ | grep -v _test.go | grep -E '\.go$'` is empty.
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/engine/... ./internal/jsonformat/... -timeout 60s &amp;&amp; ! git diff --name-only -- internal/engine/ internal/jsonformat/ internal/adapter/ | grep -v _test.go | grep -E '\.go$'</automated>
  </verify>
  <done>Per-task map populated; engine + jsonformat tests green under `-race`; no adapter-package writes from this plan.</done>
</task>

<task type="auto">
  <name>Task 3: Flip frontmatter + sign-off ticks</name>
  <read_first>
    - .planning/phases/06-tool-call-path/06-VALIDATION.md (after Task 2)
    - .planning/phases/13-nyquist-coverage-uplift/13-05-ESCALATIONS.txt (if exists)
  </read_first>
  <files>.planning/phases/06-tool-call-path/06-VALIDATION.md</files>
  <action>Verify the 6 sign-off constraints; flip frontmatter; tick all 6 boxes; set Approval date.</action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" …` = 1; `false` = 0.
    - 6 sign-off boxes ticked.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'nyquist_compliant: true' .planning/phases/06-tool-call-path/06-VALIDATION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'nyquist_compliant: false' .planning/phases/06-tool-call-path/06-VALIDATION.md)" = "0" ] &amp;&amp; [ "$(grep -c '^- \[x\]' .planning/phases/06-tool-call-path/06-VALIDATION.md)" -ge "6" ]</automated>
  </verify>
  <done>Frontmatter flipped; sign-off ticked.</done>
</task>

<task type="auto">
  <name>Task 4: Write SUMMARY.md documenting Phase 06 uplift</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-05-GAPS.txt
    - .planning/phases/13-nyquist-coverage-uplift/13-05-ESCALATIONS.txt (if exists)
    - .planning/phases/06-tool-call-path/06-VALIDATION.md
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md</files>
  <action>Mirror the 13-01 SUMMARY structure: NYQ-06 target, gap counts before/after, BLOCKER/WARNING/ESCALATE counts, new test files (engine + jsonformat only), explicit note that adapter-side rows reference existing adapter tests rather than introducing new ones here, "no production source edited" with git-diff evidence.</action>
  <acceptance_criteria>
    - Summary file exists with cited fields.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md &amp;&amp; grep -qE 'BLOCKER|ESCALATE|production source' .planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md</automated>
  </verify>
  <done>Summary committed.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/engine/... ./internal/jsonformat/... -timeout 60s` exits 0.
- `grep "nyquist_compliant: true" .planning/phases/06-tool-call-path/06-VALIDATION.md` matches once.
- This plan does not write to any `internal/adapter/*` file.
</verification>

<success_criteria>
- NYQ-06 satisfied. Zero production-source edits anywhere. File-ownership boundary with 13-03 and 13-04 held.
</success_criteria>

<output>
Create `.planning/phases/13-nyquist-coverage-uplift/13-05-SUMMARY.md` when done.
</output>
