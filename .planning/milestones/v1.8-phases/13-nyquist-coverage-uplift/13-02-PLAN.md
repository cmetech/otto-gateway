---
phase: 13-nyquist-coverage-uplift
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
  - .planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-02-ESCALATIONS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md
  - internal/admin/*_test.go
  - internal/admin/testdata/**
autonomous: true
requirements:
  - NYQ-06.1
tags:
  - nyquist
  - validation
  - admin-ui

must_haves:
  truths:
    - "Phase 06.1 VALIDATION.md flips from nyquist_compliant: false to nyquist_compliant: true."
    - "Per-Task Verification Map contains one row for every task ID across the 4 Phase-06.1 plans (14 task surface)."
    - "Every map row has automated <verify>, Wave 0 fixture, or manual-only rationale (UI-shaped phase — expect more manual-only rows than backend phases)."
    - "All 6 Validation Sign-Off boxes ticked for Phase 06.1."
    - "Any new behavioral tests pass under `go test -race ./internal/admin/...`."
    - "Read-only implementation rule held: zero production-source edits attributable to this plan."
  artifacts:
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt"
      provides: "Per-task gap classification for Phase 06.1"
    - path: ".planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md"
      provides: "Filled per-task map, flipped frontmatter, ticked sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md"
      provides: "What shipped, BLOCKER/WARNING/ESCALATE counts, files changed"
  key_links:
    - from: "06.1-VALIDATION.md per-task map rows"
      to: "06.1-{01..04}-PLAN.md task IDs"
      via: "row-per-task enumeration"
      pattern: "06.1-(01|02|03|04)-\\d+"
---

<objective>
Lift Phase 06.1 (Admin Observability UI) VALIDATION.md to the post-08.1 standard. UI-shaped phase (4 plans, 14 tasks); manual-only rationales likely common because UI behaviors often require visual confirmation. Per NYQ-06.1: fill the per-task map, document Wave 0 fixtures, justify manual-only items, tick the 6 sign-off boxes, flip `nyquist_compliant: false → true`.

Purpose: One of six parallel plans in Phase 13. Each `_test.go` file lives under `internal/admin/`; no overlap with other plans' file ownership.

Output: Updated `06.1-VALIDATION.md`, possibly new `*_test.go` files under `internal/admin/`, and a `13-02-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
@.planning/phases/13-nyquist-coverage-uplift/13-BASELINE.txt
@.planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
@.planning/phases/06.1-admin-observability-ui/06.1-01-PLAN.md
@.planning/phases/06.1-admin-observability-ui/06.1-02-PLAN.md
@.planning/phases/06.1-admin-observability-ui/06.1-03-PLAN.md
@.planning/phases/06.1-admin-observability-ui/06.1-04-PLAN.md
@.planning/phases/06.1-admin-observability-ui/06.1-01-SUMMARY.md
@.planning/phases/06.1-admin-observability-ui/06.1-02-SUMMARY.md
@.planning/phases/06.1-admin-observability-ui/06.1-03-SUMMARY.md
@.planning/phases/06.1-admin-observability-ui/06.1-04-SUMMARY.md
</context>

<scope_guard>
v1.8 is validation-doc + new test files only. This plan MUST NOT edit `internal/admin/*.go` files that are not `_test.go`, refactor existing tests, add linters or hooks, or modify the GSD VALIDATION.md template. UI behaviors that genuinely cannot be automated land in the Manual-Only Verifications section with written rationale — not as "fix the UI" tasks.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap list for Phase 06.1</name>
  <read_first>
    - .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
    - .planning/phases/06.1-admin-observability-ui/06.1-01-PLAN.md
    - .planning/phases/06.1-admin-observability-ui/06.1-02-PLAN.md
    - .planning/phases/06.1-admin-observability-ui/06.1-03-PLAN.md
    - .planning/phases/06.1-admin-observability-ui/06.1-04-PLAN.md
    - .planning/phases/06.1-admin-observability-ui/06.1-01-SUMMARY.md
    - .planning/phases/06.1-admin-observability-ui/06.1-02-SUMMARY.md
    - .planning/phases/06.1-admin-observability-ui/06.1-03-SUMMARY.md
    - .planning/phases/06.1-admin-observability-ui/06.1-04-SUMMARY.md
    - .planning/phases/06.1-admin-observability-ui/06.1-HUMAN-UAT.md (manual-only catalog)
    - internal/admin/handlers_test.go (read-only — existing patterns)
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt</files>
  <action>Enumerate every task ID across all 4 Phase-06.1 PLAN.md files (14 task surface per BASELINE). For each task classify the current state as `auto` / `wave0` / `manual-only` / `escalate-candidate`. UI tasks (visual layout, theme toggle, palette tweaks) overwhelmingly resolve to `manual-only` — that is correct, not a gap. Backend handler tasks (aboutHandler / docsHandler / snapshot fields) should resolve to `auto` against existing `internal/admin/*_test.go`. Write the gap list per row format from plan 13-01.</action>
  <acceptance_criteria>
    - `.planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt` exists with one row per task.
    - Total row count = 14 (matches BASELINE).
    - Each row carries a classification token in {auto, wave0, manual-only, escalate-candidate}.
    - `git diff` outside `.planning/phases/13-*/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt &amp;&amp; [ "$(grep -vc '^#' .planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt)" -ge "14" ]</automated>
  </verify>
  <done>Gap list committed; 14 rows present; classifications assigned.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn gsd-nyquist-auditor against the gap list</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt
    - .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
    - internal/admin/handlers_test.go (read-only)
    - internal/admin/assets.go (read-only — to understand handler surface)
  </read_first>
  <files>internal/admin/*_test.go, internal/admin/testdata/**, .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md, .planning/phases/13-nyquist-coverage-uplift/13-02-ESCALATIONS.txt</files>
  <action>Spawn `gsd-nyquist-auditor` against the gap list with the read-only-implementation contract: production `internal/admin/*.go` files (NOT `_test.go`) are read-only — bugs ESCALATE to `13-02-ESCALATIONS.txt`. Read-write surfaces: `internal/admin/*_test.go`, `internal/admin/testdata/`, `06.1-VALIDATION.md`. For each `escalate-candidate` gap that targets backend behavior (handler output shape, template field rendering, route registration), write a behavioral test mirroring `internal/admin/handlers_test.go` conventions. For UI-visual gaps that cannot be automated (palette correctness, theme toggle icon swap, table column widths), move the row to the Manual-Only Verifications section with a written rationale citing the specific HUMAN-UAT step. Run `go test -race ./internal/admin/... -timeout 60s` after auditor returns; must pass.</action>
  <acceptance_criteria>
    - `gsd-nyquist-auditor` returned with structured-return format.
    - `go test -race ./internal/admin/... -timeout 60s` exits 0.
    - 06.1-VALIDATION.md Per-Task Verification Map has one row per task; no blank "Test Type" / "Automated Command" cell outside manual-only rows.
    - Each manual-only row carries a Test Instructions paragraph (no blank "Test Instructions" cell).
    - `git diff --name-only -- internal/admin/ | grep -v _test.go | grep -E '\.go$'` is empty.
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/admin/... -timeout 60s &amp;&amp; ! git diff --name-only -- internal/admin/ | grep -v _test.go | grep -E '\.go$'</automated>
  </verify>
  <done>Per-task map populated; new admin-package tests pass under `-race`; manual-only rows carry rationale + instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Flip frontmatter + sign-off ticks</name>
  <read_first>
    - .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md (after Task 2)
    - .planning/phases/13-nyquist-coverage-uplift/13-02-ESCALATIONS.txt (if exists)
  </read_first>
  <files>.planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md</files>
  <action>Verify the 6 sign-off constraints. Special care on (2) sampling continuity: with a UI phase, multiple consecutive manual-only tasks are NOT acceptable — re-check Task 2's output to ensure no 3 consecutive PLAN-order tasks are all manual-only without an automated anchor (e.g. a handler-output test that exercises the same code path). If a stretch of 3+ consecutive manual-only tasks exists, either find an automatable proxy (template-render output assertion, JSON shape assertion against snapshot) or document the cluster as an explicit caveat under sign-off. Flip `nyquist_compliant: false → true`. Tick all 6 boxes. Set `**Approval:** approved 2026-06-07`.</action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md` returns 1.
    - `grep -c "nyquist_compliant: false" .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md` returns 0.
    - 6 sign-off boxes ticked (`grep -c '^- \[x\]' …VALIDATION.md` ≥ 6).
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'nyquist_compliant: true' .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'nyquist_compliant: false' .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md)" = "0" ] &amp;&amp; [ "$(grep -c '^- \[x\]' .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md)" -ge "6" ]</automated>
  </verify>
  <done>Frontmatter flipped; 6 boxes ticked; manual-only clustering audited.</done>
</task>

<task type="auto">
  <name>Task 4: Write SUMMARY.md documenting Phase 06.1 uplift</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-02-GAPS.txt
    - .planning/phases/13-nyquist-coverage-uplift/13-02-ESCALATIONS.txt (if exists)
    - .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md</files>
  <action>Write `13-02-SUMMARY.md` mirroring the 13-01 SUMMARY structure: target REQ-ID (NYQ-06.1), gap counts before/after, BLOCKER/WARNING/ESCALATE counts, new `*_test.go` files added, manual-only cluster audit result, explicit "no production source edited" claim with git-diff evidence.</action>
  <acceptance_criteria>
    - `.planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md` exists with the cited fields.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md &amp;&amp; grep -qE 'BLOCKER|ESCALATE|production source' .planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md</automated>
  </verify>
  <done>Summary committed for milestone audit.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/admin/... -timeout 60s` exits 0.
- `grep "nyquist_compliant: true" .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md` matches once.
- `git diff` against this plan's commits shows no production-source edits under `internal/admin/`.
</verification>

<success_criteria>
- NYQ-06.1 satisfied. UI-visual gaps documented as manual-only with full rationale + instructions. No production source edited.
</success_criteria>

<output>
Create `.planning/phases/13-nyquist-coverage-uplift/13-02-SUMMARY.md` when done.
</output>
