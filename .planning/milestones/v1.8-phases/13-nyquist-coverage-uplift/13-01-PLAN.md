---
phase: 13-nyquist-coverage-uplift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
  - .planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-01-ESCALATIONS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md
  - internal/plugin/pii/*_test.go
  - internal/plugin/pii/testdata/**
autonomous: true
requirements:
  - NYQ-08.4
tags:
  - nyquist
  - validation
  - pii

must_haves:
  truths:
    - "Phase 08.4 VALIDATION.md flips from nyquist_compliant: false to nyquist_compliant: true."
    - "Per-Task Verification Map contains one row for every task ID present in 08.4-01-PLAN.md (5 task surface)."
    - "Every map row has either an automated <verify> command, a Wave 0 fixture reference, or an explicit manual-only rationale."
    - "All 6 Validation Sign-Off boxes ticked for Phase 08.4."
    - "Any new behavioral tests pass under `go test -race ./internal/plugin/pii/...`."
    - "Read-only implementation rule held: zero production-source edits attributable to this plan."
  artifacts:
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt"
      provides: "Per-task gap classification for Phase 08.4"
    - path: ".planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md"
      provides: "Filled per-task map, flipped frontmatter, ticked sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md"
      provides: "What shipped, BLOCKER/WARNING/ESCALATE counts, files changed"
  key_links:
    - from: "08.4-VALIDATION.md per-task map rows"
      to: "08.4-01-PLAN.md task IDs"
      via: "row-per-task enumeration"
      pattern: "08.4-01-(R|G|F|H)"
---

<objective>
Lift Phase 08.4 (US Address PII Coverage) VALIDATION.md to the post-08.1 standard. Smallest target (1 plan, 5 tasks); recent phase with tighter discipline, so gaps should be lightweight. Per NYQ-08.4: fill the per-task verification map, document Wave 0 fixtures, justify manual-only items, tick the 6 sign-off boxes, flip `nyquist_compliant: false → true`.

Purpose: One of six parallel plans in Phase 13 that collectively flip the milestone-wide Nyquist compliance ratio from 7/13 to 13/13. This is the warm-up — smallest surface, recent authoring, expected to land quickly.

Output: Updated `08.4-VALIDATION.md`, possibly new `*_test.go` files under `internal/plugin/pii/`, and a `13-01-SUMMARY.md` documenting what shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
@.planning/phases/13-nyquist-coverage-uplift/13-BASELINE.txt
@.planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
@.planning/phases/08.4-us-address-pii-coverage/08.4-01-PLAN.md
@.planning/phases/08.4-us-address-pii-coverage/08.4-01-SUMMARY.md
</context>

<scope_guard>
v1.8 is validation-doc work + new test files only. This plan MUST NOT:
- Edit any `internal/**/*.go` file that is NOT `_test.go`
- Refactor existing tests beyond what filling a gap demands
- Add new linters, hooks, framework changes
- Change the GSD VALIDATION.md template at `~/.claude/get-shit-done/templates/VALIDATION.md`

If `git diff` outside `*_test.go`, `*VALIDATION.md`, `testdata/`, and `.planning/phases/13-*/` exceeds zero lines, the plan is out of scope — ESCALATE instead.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap list for Phase 08.4</name>
  <read_first>
    - .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
    - .planning/phases/08.4-us-address-pii-coverage/08.4-01-PLAN.md
    - .planning/phases/08.4-us-address-pii-coverage/08.4-01-SUMMARY.md
    - .planning/phases/08.4-us-address-pii-coverage/08.4-HUMAN-UAT.md (manual-only rationales)
    - internal/plugin/pii/recognizers_test.go (read-only — existing test patterns to mirror)
    - internal/plugin/pii/pii_test.go (read-only — existing integration test patterns)
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt</files>
  <action>Enumerate every task ID from 08.4-01-PLAN.md (Tasks R, G, F, H — 5 task surface per BASELINE; confirm by reading the PLAN). For each task, classify its current state in 08.4-VALIDATION.md as one of: `auto` (automated `<verify>` command already exists and runs in &lt;60s); `wave0` (covered by Wave 0 fixture — test infra that was assumed but not declared as automated); `manual-only` (no programmatic verify possible, requires written rationale); `escalate-candidate` (gap can only be closed by a real behavioral test that does not yet exist). Write each row as: `<task_id> | <plan> | <current_state> | <classification> | <proposed_action>`. Do NOT modify any production `.go` file while doing this — the gap list is read-only analysis. Commit `13-01-GAPS.txt`.</action>
  <acceptance_criteria>
    - File `.planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt` exists.
    - Row count equals task count in 08.4-01-PLAN.md (verify with `grep -c "&lt;name&gt;Task " .planning/phases/08.4-us-address-pii-coverage/08.4-01-PLAN.md`).
    - Each row carries a classification token in {auto, wave0, manual-only, escalate-candidate}.
    - `git diff` outside `.planning/phases/13-*/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt && awk '{print $NF}' .planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt | grep -v '^#' | grep -Eq '^(auto|wave0|manual-only|escalate-candidate)$'</automated>
  </verify>
  <done>Gap list committed; every Phase-08.4 task accounted for; classification token present on each row.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn gsd-nyquist-auditor against the gap list</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt
    - .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
    - internal/plugin/pii/recognizers.go (read-only — to understand the surface under test)
    - internal/plugin/pii/recognizers_test.go (read-only — existing test patterns)
  </read_first>
  <files>internal/plugin/pii/*_test.go, internal/plugin/pii/testdata/**, .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md, .planning/phases/13-nyquist-coverage-uplift/13-01-ESCALATIONS.txt</files>
  <action>Spawn `gsd-nyquist-auditor` with the gap list as `<gaps>` and the following contract: (1) **Read-only implementation:** production source files (`internal/plugin/pii/*.go` NOT ending `_test.go`) MUST NOT be modified — any bug found must ESCALATE via a row written to `.planning/phases/13-nyquist-coverage-uplift/13-01-ESCALATIONS.txt`, never a silent patch. (2) **Read-write surfaces:** `internal/plugin/pii/*_test.go`, fixtures under `internal/plugin/pii/testdata/`, and `08.4-VALIDATION.md` itself. (3) For each `escalate-candidate` gap, the agent writes a new behavioral test under `internal/plugin/pii/` that follows the package's existing test conventions (mirror `TestUSPhoneRecognizer_CapturedSpan` FindString-based span assertion pattern); each test must be capable of failing (adversarial stance). (4) After tests are added, the agent updates 08.4-VALIDATION.md's Per-Task Verification Map with one row per task ID — the `Automated Command` column carries `go test -race -run &lt;TestName&gt; ./internal/plugin/pii/...` for `auto`/`escalate-candidate` rows, references the Wave 0 fixture for `wave0` rows, and quotes the operator-test rationale for `manual-only` rows. The agent's return MUST be one of `GAPS FILLED` / `PARTIAL` / `ESCALATE` per its structured-returns contract. Record the BLOCKER and WARNING counts in `13-01-ESCALATIONS.txt` (one row per escalate) — this file is per-plan, so no cross-plan write conflict exists. Run `go test -race ./internal/plugin/pii/... -timeout 60s` after the agent returns; must pass.</action>
  <acceptance_criteria>
    - `gsd-nyquist-auditor` returned with `GAPS FILLED`, `PARTIAL`, or `ESCALATE`.
    - `go test -race ./internal/plugin/pii/... -timeout 60s` exits 0.
    - 08.4-VALIDATION.md Per-Task Verification Map has one row per task in 08.4-01-PLAN.md with no blank "Test Type" or "Automated Command" cell outside the manual-only column.
    - `git diff -- internal/plugin/pii/ | grep -E '^\+\+\+ b/internal/plugin/pii/[^/]+\.go$' | grep -v '_test.go'` returns nothing (no production-source edits).
    - If `13-01-ESCALATIONS.txt` exists, every row cites the file/line of the suspect implementation behavior.
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/plugin/pii/... -timeout 60s &amp;&amp; ! git diff --name-only -- internal/plugin/pii/ | grep -v _test.go | grep -E '\.go$'</automated>
  </verify>
  <done>Per-task verification map fully populated; new tests pass under `-race`; ESCALATIONS recorded (if any) without inline production fixes.</done>
</task>

<task type="auto">
  <name>Task 3: Flip frontmatter + sign-off ticks</name>
  <read_first>
    - .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md (after Task 2)
    - .planning/phases/13-nyquist-coverage-uplift/13-01-ESCALATIONS.txt (if exists)
  </read_first>
  <files>.planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md</files>
  <action>Manually verify the 6 Validation Sign-Off constraints against the post-Task-2 state: (1) every task has an `<automated>` verify or Wave 0 dependency; (2) sampling continuity — no 3 consecutive tasks without automated verify (08.4 has only 5 tasks; if Task R+G+F all have automated, F+H+(none) is fine); (3) Wave 0 covers all MISSING references; (4) no watch-mode flags appear anywhere in the map's Automated Command column; (5) feedback latency &lt; 10 s (quick run) / &lt; 90 s (`make ci`); (6) flip `nyquist_compliant: false` → `nyquist_compliant: true` in the frontmatter. Tick all 6 checkboxes under `## Validation Sign-Off`. Set `**Approval:** approved 2026-06-07`.</action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md` returns 1.
    - `grep -c "nyquist_compliant: false" .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md` returns 0.
    - All 6 sign-off lines start with `- [x]` (verify with `grep -c '^- \[x\]' .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md` ≥ 6).
    - No watch-mode flag (`--watch`, `-w` as a watch flag) appears in the per-task map.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'nyquist_compliant: true' .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'nyquist_compliant: false' .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md)" = "0" ] &amp;&amp; [ "$(grep -c '^- \[x\]' .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md)" -ge "6" ]</automated>
  </verify>
  <done>Frontmatter flipped, all 6 sign-off boxes ticked, approval date set.</done>
</task>

<task type="auto">
  <name>Task 4: Write SUMMARY.md documenting Phase 08.4 uplift</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-01-GAPS.txt
    - .planning/phases/13-nyquist-coverage-uplift/13-01-ESCALATIONS.txt (if exists)
    - .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md</files>
  <action>Write `13-01-SUMMARY.md` documenting: target REQ-ID (NYQ-08.4), gap counts before/after, BLOCKER count, WARNING count, ESCALATE count (with cross-reference to `13-01-ESCALATIONS.txt` if any rows exist), Wave 0 fixtures noted, manual-only rationales preserved, new `*_test.go` files added (full paths), and explicit confirmation that no production source was edited (`git diff` evidence). Use the standard SUMMARY template structure.</action>
  <acceptance_criteria>
    - File exists at `.planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md`.
    - File cites BLOCKER count, WARNING count, ESCALATE count, and new test-file paths.
    - File contains an explicit "no production source edited" claim with git-diff evidence.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md &amp;&amp; grep -qE 'BLOCKER|ESCALATE|production source' .planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md</automated>
  </verify>
  <done>Summary committed; cross-references to escalations and tests preserved for milestone audit.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/plugin/pii/... -timeout 60s` exits 0 on green.
- `grep "nyquist_compliant: true" .planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md` matches once.
- `git diff main...HEAD -- ':!*_test.go' ':!*VALIDATION.md' ':!testdata/' ':!.planning/'` is empty for this plan's commits.
</verification>

<success_criteria>
- NYQ-08.4 satisfied: Phase 08.4 VALIDATION.md `nyquist_compliant: true`, all 6 sign-off boxes ticked, every PLAN task represented in the verification map.
- Zero production-source edits attributable to plan 13-01.
- Any ESCALATEs documented in `13-01-ESCALATIONS.txt` for downstream phase scoping (NOT fixed inline).
</success_criteria>

<output>
Create `.planning/phases/13-nyquist-coverage-uplift/13-01-SUMMARY.md` when done.
</output>
