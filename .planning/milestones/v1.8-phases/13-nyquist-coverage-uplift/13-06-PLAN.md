---
phase: 13-nyquist-coverage-uplift
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md
  - .planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-06-ESCALATIONS.txt
  - .planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md
  - internal/plugin/*_test.go
  - internal/plugin/testdata/**
  - internal/plugin/hooks/*_test.go
  - internal/plugin/hooks/testdata/**
autonomous: true
requirements:
  - NYQ-08
tags:
  - nyquist
  - validation
  - plugin

must_haves:
  truths:
    - "Phase 08 VALIDATION.md flips from nyquist_compliant: false to nyquist_compliant: true."
    - "Per-Task Verification Map contains one row for every task ID across the 5 Phase-08 plans (31 task surface — LARGEST target)."
    - "Every map row has automated <verify>, Wave 0 fixture, or manual-only rationale."
    - "All 6 Validation Sign-Off boxes ticked for Phase 08."
    - "Any new behavioral tests pass under `go test -race ./internal/plugin/...`."
    - "Read-only implementation rule held: zero production-source edits attributable to this plan."
  artifacts:
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt"
      provides: "Per-task gap classification for Phase 08"
    - path: ".planning/phases/08-plugin-hook-chain/08-VALIDATION.md"
      provides: "Filled per-task map, flipped frontmatter, ticked sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md"
      provides: "What shipped, gap counts, ESCALATEs"
  key_links:
    - from: "08-VALIDATION.md per-task map rows"
      to: "08-{01..05}-PLAN.md task IDs"
      via: "row-per-task enumeration"
      pattern: "08-(01|02|03|04|05)-\\d+"
---

<objective>
Lift Phase 08 (Plugin Hook Chain) VALIDATION.md to the post-08.1 standard. **Largest target** — 5 plans, 31 tasks; most surface in the milestone. Covers `PreHook`/`PostHook` chain, RequestID/Auth/Logging hooks, the PII redaction hook, the auth-bypass posture for list-mode stubs, and the `/health/hooks` view. Per NYQ-08: fill the per-task map, document Wave 0 fixtures, tick the 6 sign-off boxes, flip `nyquist_compliant: false → true`.

Note: Phase 08 has two compliant sibling phases (08.1, 08.3) — their post-08.1-standard VALIDATION.md docs can be used as exemplars when authoring rows for 08's map. Phase 08.4 is being uplifted in parallel by plan 13-01; the PII-related rows in 08-VALIDATION.md MUST NOT duplicate work owned by 13-01 (they reference each other, but each plan owns its own VALIDATION.md).

Output: Updated `08-VALIDATION.md`, possibly new `*_test.go` files under `internal/plugin/` and `internal/plugin/hooks/`, and a `13-06-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
@$HOME/.claude/agents/gsd-nyquist-auditor.md
@.planning/phases/13-nyquist-coverage-uplift/13-BASELINE.txt
@.planning/phases/08-plugin-hook-chain/08-VALIDATION.md
@.planning/phases/08-plugin-hook-chain/08-01-PLAN.md
@.planning/phases/08-plugin-hook-chain/08-02-PLAN.md
@.planning/phases/08-plugin-hook-chain/08-03-PLAN.md
@.planning/phases/08-plugin-hook-chain/08-04-PLAN.md
@.planning/phases/08-plugin-hook-chain/08-05-PLAN.md
@.planning/phases/08-plugin-hook-chain/08-01-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-02-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-03-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-04-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-05-SUMMARY.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-VALIDATION.md
</context>

<scope_guard>
This plan MUST NOT edit production `.go` files under `internal/plugin/`, `internal/plugin/hooks/`, or anywhere else. The PII sub-package `internal/plugin/pii/*_test.go` is owned by plan 13-01 — this plan must NOT write tests there. If a Phase-08 task touches PII behavior, its row in 08-VALIDATION.md references existing `internal/plugin/pii/*_test.go` files (read-only here) rather than authoring new tests. Watch carefully for adversarial-stance violations on the largest surface — 31 tasks is the most opportunity for "test passes trivially" softness. ESCALATE bugs via `13-06-ESCALATIONS.txt`.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate gap list for Phase 08</name>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md
    - .planning/phases/08-plugin-hook-chain/08-{01..05}-PLAN.md
    - .planning/phases/08-plugin-hook-chain/08-{01..05}-SUMMARY.md
    - .planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-VALIDATION.md (exemplar — post-08.1 standard)
    - internal/plugin/ (read-only — surface map)
    - internal/plugin/hooks/ (read-only)
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt</files>
  <action>Enumerate every task ID across the 5 Phase-08 PLAN.md files (31 task surface per BASELINE). Classify each. PII-related rows reference `internal/plugin/pii/*_test.go` (owned by 13-01); other rows resolve to tests under `internal/plugin/` (chain, registry) or `internal/plugin/hooks/` (RequestID, Auth, Logging). Auth-bypass posture rows for Ollama list-mode stubs may resolve to `manual-only` with rationale citing the durable annotation in `docs/operating.md`. Write per-row format.</action>
  <acceptance_criteria>
    - Gap list exists with 31 rows.
    - `git diff` outside `.planning/phases/13-*/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt &amp;&amp; [ "$(grep -vc '^#' .planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt)" -ge "31" ]</automated>
  </verify>
  <done>Gap list committed with 31 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn gsd-nyquist-auditor against the gap list</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md
    - .planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-VALIDATION.md (exemplar)
    - $HOME/.claude/agents/gsd-nyquist-auditor.md
    - internal/plugin/ (read-only)
    - internal/plugin/hooks/ (read-only)
  </read_first>
  <files>internal/plugin/*_test.go, internal/plugin/testdata/**, internal/plugin/hooks/*_test.go, internal/plugin/hooks/testdata/**, .planning/phases/08-plugin-hook-chain/08-VALIDATION.md, .planning/phases/13-nyquist-coverage-uplift/13-06-ESCALATIONS.txt</files>
  <action>Spawn `gsd-nyquist-auditor` against the gap list. Contract: production `internal/plugin/*.go` and `internal/plugin/hooks/*.go` (NOT `_test.go`) are read-only — bugs ESCALATE to `13-06-ESCALATIONS.txt`. **PII sub-package is off-limits to this plan** (owned by 13-01) — for PII-classified gaps, the auditor's row references the existing `internal/plugin/pii/` test file/test name; no new files written under `pii/`. Adversarial-stance reminder: 31 tasks invite shortcuts; on this plan especially, every new test must be capable of failing (assert a specific shape/value that a broken impl would violate, not just "no error"). Run `go test -race ./internal/plugin/... -timeout 90s` after auditor returns.</action>
  <acceptance_criteria>
    - `gsd-nyquist-auditor` returned with structured-return format.
    - `go test -race ./internal/plugin/... -timeout 90s` exits 0.
    - 08-VALIDATION.md per-task map fully populated (31 rows).
    - `git diff --name-only -- internal/plugin/ | grep -v _test.go | grep -E '\.go$'` is empty.
    - `git diff --name-only -- internal/plugin/pii/` reports no new files (PII boundary held).
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/plugin/... -timeout 90s &amp;&amp; ! git diff --name-only -- internal/plugin/ | grep -v _test.go | grep -E '\.go$'</automated>
  </verify>
  <done>Per-task map populated; plugin tests green under `-race`; PII boundary with 13-01 held.</done>
</task>

<task type="auto">
  <name>Task 3: Flip frontmatter + sign-off ticks</name>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md (after Task 2)
    - .planning/phases/13-nyquist-coverage-uplift/13-06-ESCALATIONS.txt (if exists)
  </read_first>
  <files>.planning/phases/08-plugin-hook-chain/08-VALIDATION.md</files>
  <action>Verify the 6 sign-off constraints carefully against 31 rows: with this many tasks, the (2) sampling-continuity check is the most likely failure point — re-walk the PLAN-order sequence and confirm no 3-consecutive-manual-only stretch. Flip frontmatter; tick all 6 boxes; set Approval date.</action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" …` = 1; `false` = 0.
    - 6 sign-off boxes ticked.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'nyquist_compliant: true' .planning/phases/08-plugin-hook-chain/08-VALIDATION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'nyquist_compliant: false' .planning/phases/08-plugin-hook-chain/08-VALIDATION.md)" = "0" ] &amp;&amp; [ "$(grep -c '^- \[x\]' .planning/phases/08-plugin-hook-chain/08-VALIDATION.md)" -ge "6" ]</automated>
  </verify>
  <done>Frontmatter flipped; sign-off ticked; sampling-continuity audited on 31-row surface.</done>
</task>

<task type="auto">
  <name>Task 4: Write SUMMARY.md documenting Phase 08 uplift</name>
  <read_first>
    - .planning/phases/13-nyquist-coverage-uplift/13-06-GAPS.txt
    - .planning/phases/13-nyquist-coverage-uplift/13-06-ESCALATIONS.txt (if exists)
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md
  </read_first>
  <files>.planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md</files>
  <action>Mirror the 13-01 SUMMARY structure: NYQ-08 target, gap counts before/after (31 surface), BLOCKER/WARNING/ESCALATE counts, new test files (NOT under `internal/plugin/pii/`), explicit "PII boundary with plan 13-01 held" claim, "no production source edited" with git-diff evidence.</action>
  <acceptance_criteria>
    - Summary file exists with cited fields.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md &amp;&amp; grep -qE 'BLOCKER|ESCALATE|production source' .planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md</automated>
  </verify>
  <done>Summary committed; largest-target uplift recorded for milestone audit.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/plugin/... -timeout 90s` exits 0.
- `grep "nyquist_compliant: true" .planning/phases/08-plugin-hook-chain/08-VALIDATION.md` matches once.
- This plan adds no new files under `internal/plugin/pii/`.
</verification>

<success_criteria>
- NYQ-08 satisfied. Zero production-source edits anywhere. PII boundary with 13-01 held. Sampling continuity verified across 31-row surface.
</success_criteria>

<output>
Create `.planning/phases/13-nyquist-coverage-uplift/13-06-SUMMARY.md` when done.
</output>
