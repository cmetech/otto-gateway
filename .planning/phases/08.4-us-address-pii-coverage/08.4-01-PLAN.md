---
phase: 08.4
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/plugin/pii/recognizers.go
  - internal/plugin/pii/recognizers_test.go
  - internal/plugin/pii/pii_test.go
  - internal/config/config.go
  - scripts/test-pii.ps1
  - scripts/test-pii.sh
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - PII-01
tags:
  - pii
  - recognizers
  - regex
  - address
  - tdd

must_haves:
  truths:
    - "PII-01 — `1111 Main Street, Austin, TX 27584` round-trips byte-for-byte through PII_REDACTION_MODE=encrypt across /v1/messages, /v1/chat/completions, /api/chat (SC1, SC2, SC3, SC6)."
    - "PII-01 — Three new regex recognizers (USAddress, USState, USZIP) compile at package init from stdlib regexp (RE2 dialect) — no new dependencies, no cgo (SC1-3)."
    - "PII-01 — USAddress requires a street-suffix vocabulary (St/Street/Ave/Avenue/Blvd/Boulevard/Rd/Road/Dr/Drive/Ln/Lane/Way/Pl/Place/Ct/Court/Pkwy/Parkway/Cir/Circle/Ter/Terrace/Sq/Square/Hwy/Highway); bare `<number> <words>` without a known suffix does NOT match (AP-1, SC3)."
    - "PII-01 — USState is context-anchored by `(?:,\\s+|^|\\n)` lead and `(?:\\b|\\s+\\d{5}|\\.|,)` trail; English words `OR`, `IN`, `OK`, `HI`, `ME`, `ID` mid-sentence do NOT match (AP-2, SC2)."
    - "PII-01 — validateUSZIPRange rejects all-same-digit codes (00000, 11111, 22222, 99999) and inspects only the 5-digit base when ZIP+4 is present (AP-3, SC1)."
    - "PII-01 — Registration order in the Recognizers slice is USAddress → USState → USZIP so the first-recognizer-wins overlap arbiter at pii.go:227-233 favors the largest span (AP-4, SC4)."
    - "PII-01 — With PII_NER_ENABLED=true, NER PERSON candidates on `Main Street` / `Pennsylvania Avenue` are dropped by the overlap arbiter at pii.go:258-277 because the USAddress regex span covers those bytes — `summary.Counts[\"PERSON\"]` is 0 on the address fixture (SC4)."
    - "PII-01 — Three new TestU*Recognizer_CapturedSpan tests use exact-string equality on r.Pattern.FindString (NOT `!= \"\"`) per AP-5 / TestUSPhoneRecognizer_CapturedSpan gold standard (SC5)."
    - "PII-01 — TestRecognizers_RegistryShape wantNames slice grows 13 → 16 entries in registration order; len(Recognizers) assertion remains exact (Pitfall 5)."
    - "PII-01 — internal/config/config.go piiAllowedEntities map gains exactly three keys (`USAddress`, `USState`, `USZIP`); operator setting `PII_ENABLED_ENTITIES=USAddress` no longer fails boot validation (Pitfall 4, SC6)."
    - "PII-01 — REQUIREMENTS.md gains a new `### PII — Recognizer coverage` section BEFORE `## v2 Requirements` (line 105), owning PII-01; Traceability table includes the PII-01 row; v1 count footer flips 62 → 63 (SC8)."
    - "PII-01 — scripts/test-pii.ps1 + scripts/test-pii.sh both extend their fixture with `Site address: 1111 Main Street, Austin, TX 27584` and add three needles (`1111 Main Street`, `TX`, `27584`) — PS1/SH parity maintained in ONE commit to avoid f7ccd40-class drift (SC6)."
    - "PII-01 — `make ci` is green (gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`); `goleak.VerifyTestMain` continues clean (SC7)."
    - "PII-01 — Operator HUMAN-UAT proves on a Windows box AND a POSIX box that `test-pii.ps1 pii` / `test-pii.sh pii` returns exit 0 with `0 check(s) failed` on the v1.10.0 binary (SC6 acceptance)."
  artifacts:
    - path: internal/plugin/pii/recognizers.go
      provides: "Three new package-init regex literals (usAddressRe, usStateRe, usZIPRe), one validator (validateUSZIPRange), three Recognizer{} entries appended to the Recognizers slice in order USAddress → USState → USZIP."
      contains: "usAddressRe"
    - path: internal/plugin/pii/recognizers_test.go
      provides: "Three new TestU*Recognizer_CapturedSpan tests using exact-string equality on FindString; reject-invalid tests for AP-1/AP-2/AP-3; TestUSZIPRecognizer_ValidatorRejectsAllSameDigit; TestRecognizers_RegistryShape wantNames bumped 13 → 16."
      contains: "TestUSAddressRecognizer_CapturedSpan"
    - path: internal/plugin/pii/pii_test.go
      provides: "TestPIIRedactionHook_USAddressFullCoverage — NER-enabled end-to-end on `Send a tech to 1111 Main Street, Austin, TX 27584 ASAP.` asserting summary.Counts[USAddress]==1, USState==1, USZIP==1, LOCATION==1 (Austin), PERSON==0 (Main Street suppressed by overlap arbiter)."
      contains: "TestPIIRedactionHook_USAddressFullCoverage"
    - path: internal/config/config.go
      provides: "piiAllowedEntities map gains USAddress, USState, USZIP keys inserted between SITE and the PERSON/LOCATION NER block; validatePIIEntities consumes the map unchanged."
      contains: "\"USAddress\":"
    - path: scripts/test-pii.ps1
      provides: "$script:PIIPrompt gains `Site address: 1111 Main Street, Austin, TX 27584` line in the customer profile block; $script:PIIExpected appends three needles ('1111 Main Street', 'TX', '27584'). ASCII-only invariant from f7ccd40 (no em-dashes, no smart quotes, no non-ASCII bytes); UTF-8 BOM is NOT present and MUST NOT be re-introduced (file currently reports `ASCII text` from `file(1)`; PowerShell 5.1 reads pure ASCII without BOM)."
      contains: "1111 Main Street"
    - path: scripts/test-pii.sh
      provides: "PII_PROMPT gains the same `Site address: ...` segment; new PII_EXPECT_ADDRESS / PII_EXPECT_STATE / PII_EXPECT_ZIP vars added; iteration loop in pii_probe extended to assert each. POSIX-sibling parity with PS1 in the same commit."
      contains: "PII_EXPECT_ADDRESS"
    - path: .planning/REQUIREMENTS.md
      provides: "New `### PII — Recognizer coverage` section inserted BEFORE `## v2 Requirements` (currently line 105) owning PII-01; Traceability table gains `| PII-01 | Phase 08.4 | Complete |` row; v1 count footer flips `62 total` → `63 total` with footnote extension."
      contains: "PII-01"
  key_links:
    - from: internal/plugin/pii/recognizers.go Recognizers slice
      to: internal/plugin/pii/pii.go collectRegexSpans (lines 227-233)
      via: "first-recognizer-wins overlap arbitration consumes the slice in declaration order — USAddress must be registered before USState and USZIP"
      pattern: "Name: \"USAddress\""
    - from: internal/plugin/pii/recognizers.go usAddressRe
      to: internal/plugin/pii/pii.go acceptNERSpans (lines 258-277)
      via: "regex span covers `Main Street` bytes → NER PERSON candidate dropped by `acceptNERSpans` overlap check at pii.go:277"
      pattern: "St|Street|Ave|Avenue"
    - from: internal/plugin/pii/recognizers.go validateUSZIPRange
      to: internal/plugin/pii/recognizers.go Recognizers slice USZIP entry
      via: "Validate field on the {Name: \"USZIP\", ...} struct literal"
      pattern: "Validate: validateUSZIPRange"
    - from: internal/config/config.go piiAllowedEntities map
      to: internal/config/config.go validatePIIEntities (lines 755-769)
      via: "validator is data-driven off the map; three new map keys make PII_ENABLED_ENTITIES=USAddress,USState,USZIP boot-valid without validator code change"
      pattern: "\"USAddress\""
    - from: scripts/test-pii.ps1 $script:PIIExpected
      to: scripts/test-pii.sh PII_EXPECT_*
      via: "PS1/SH parity — both fixtures extended in the same commit; the iteration loops in each assert all needles round-trip plaintext"
      pattern: "1111 Main Street"
---

## Phase Goal

**As an** OTTO Gateway operator running PII_REDACTION_MODE=encrypt in front of kiro-cli, **I want to** send prompts that contain US street addresses, state codes, and ZIP codes — including the canonical fixture `1111 Main Street, Austin, TX 27584` — **so that** every address component is tokenized end-to-end, round-trips byte-for-byte through encrypt/decrypt, and the prose-v2 NER no longer mis-classifies `Main Street`, `Pennsylvania Avenue`, or `Apple Park` as PERSON.

<objective>
Add US-address coverage to the PII redactor with three regex recognizers — `USAddress`, `USState`, `USZIP` — registered before the NER stage so the existing overlap arbiter at `internal/plugin/pii/pii.go:258-277` ("Skip NER candidates that overlap any regex span") silences the 2026-06-04 splunk-box probe's PERSON false positives on street names. Implements PII-01 (newly introduced in this phase) per the goal-backward truths derived from ROADMAP Phase 8.4 SC1–SC8.

**Fix shape** (mechanically additive — no engine, hook-chain, or config-loading changes):

1. **Three regex literals** in `internal/plugin/pii/recognizers.go`'s existing `var (...)` block (after `siteRe` at line 143-145):
   - `usZIPRe`: `\b\d{5}(?:-\d{4})?\b` + `validateUSZIPRange` rejects all-same-digit codes.
   - `usStateRe`: `(?:,\s+|^|\n)(?:AL|AK|…|VI)(?:\b|\s+\d{5}|\.|,)` — 56-code alternation (50 states + DC + 5 territories) with comma/newline lead anchor + ZIP/punct trail anchor. Mitigates AP-2 (English words like `OR`/`IN`/`OK` matching mid-sentence).
   - `usAddressRe`: `\b\d{1,6}\s+[A-Z][A-Za-z]*(?:\s+[A-Z][A-Za-z]*)*\s+(?:St|Street|Ave|Avenue|Blvd|Boulevard|Rd|Road|Dr|Drive|Ln|Lane|Way|Pl|Place|Ct|Court|Pkwy|Parkway|Cir|Circle|Ter|Terrace|Sq|Square|Hwy|Highway)\b\.?` — house number + TitleCase street name + 16-form suffix vocabulary. Mitigates AP-1 (bare `<num> <word>` matching room numbers / phone fragments).
2. **One validator function** `validateUSZIPRange` (string → bool) — mirrors `validateSSNRange` shape; rejects all-same-digit base.
3. **Three Recognizer{} entries** appended to the `Recognizers = []Recognizer{...}` slice at `recognizers.go:213-255` in order `USAddress → USState → USZIP` (largest span first, AP-4 mitigation).
4. **Three keys** added to `piiAllowedEntities` in `internal/config/config.go` between `"SITE": {},` and the `// NER-emitted entity names` comment block — keeps regex-grouping invariant intact.
5. **Three new TestU*Recognizer_CapturedSpan tests + reject-invalid tests + TestUSZIPRecognizer_ValidatorRejectsAllSameDigit** in `recognizers_test.go` — all using **exact-string equality** on `r.Pattern.FindString` (AP-5 mitigation; the v1.9.5–8.7 USPhone fidelity bugs hid behind `FindString != ""` assertions). `TestRecognizers_RegistryShape` `wantNames` slice grows 13 → 16 entries in registration order.
6. **One new integration test** `TestPIIRedactionHook_USAddressFullCoverage` in `pii_test.go` — drives a full `*PIIRedactionHook` with `NER: NewNEREngine()` against `Send a tech to 1111 Main Street, Austin, TX 27584 ASAP.`, asserts `summary.Counts[USAddress]==1, USState==1, USZIP==1, LOCATION==1 (Austin), PERSON==0` (the load-bearing claim — PERSON false positive on `Main Street` IS silenced by overlap arbitration).
7. **REQUIREMENTS.md** gains `### PII — Recognizer coverage` section + PII-01 entry + Traceability row + v1 count `62 total` → `63 total` (per RESEARCH §A5 recommendation a).
8. **scripts/test-pii.ps1 + scripts/test-pii.sh** extended in lockstep — one new fixture line (`Site address: 1111 Main Street, Austin, TX 27584`) + three needles — in the SAME commit per PATTERNS.md §Shared "PS1/SH parity gate".

**Why TDD:** the PERSON false positive IS the assertion. The RED test (`TestPIIRedactionHook_USAddressFullCoverage`) drives the full hook with NER enabled against the address fixture and asserts `summary.Counts["PERSON"] == 0`. Against the current code (no USAddress regex → prose's PERSON candidate for `Main Street` survives `acceptNERSpans` arbitration → PERSON count is 1, not 0), the test MUST fail — falsification surface that locks in the regression catch. Additionally the three captured-span tests fail with compile error (`undefined: usAddressRe`, `usStateRe`, `usZIPRe`) before the recognizer literals are added.

**Purpose:** Close PII-01 (a new requirement introduced in this phase — does not exist in REQUIREMENTS.md yet). Eliminate the partial-address coverage + PERSON false-positive class observed in the v1.9.7 prose-v2 NER 2026-06-04 splunk-box probe.

**Output:** A single atomic vertical slice landed across three RED/GREEN/REFACTOR commits + a separate operator HUMAN-UAT checkpoint (commit-less) covering Windows + POSIX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.4-us-address-pii-coverage/08.4-RESEARCH.md
@.planning/phases/08.4-us-address-pii-coverage/08.4-PATTERNS.md
@.planning/phases/08.4-us-address-pii-coverage/08.4-VALIDATION.md
@internal/plugin/pii/recognizers.go
@internal/plugin/pii/recognizers_test.go
@internal/plugin/pii/pii.go
@internal/plugin/pii/pii_test.go
@internal/plugin/pii/spans.go
@internal/plugin/pii/luhn.go
@internal/config/config.go
@scripts/test-pii.ps1
@scripts/test-pii.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/chat, /v1/messages, /v1/chat/completions | Untrusted prompt text reaches `PIIRedactionHook.Before` where the new recognizers scan it. RE2 dialect is non-backtracking — pathological inputs are bounded O(n). |
| recognizer regex → `collectRegexSpans` | New regex spans participate in the existing first-recognizer-wins overlap arbiter; no new arbitration code paths introduced. |
| recognizer regex → NER overlap arbiter (`acceptNERSpans` at `pii.go:258-277`) | USAddress regex span overrides NER PERSON candidates that overlap — the load-bearing security property that closes the splunk-box probe false positive. |
| encrypt-mode token → `decryptTokenRe` (`pii.go:339`) | Existing regex `\[PII:([A-Za-z0-9]+):([A-Za-z0-9_-]+)\]` already accepts alphanumeric entity-name group; `USAddress` / `USState` / `USZIP` all pass — no regex change required (Pitfall 6). |
| operator env `PII_ENABLED_ENTITIES=USAddress,...` → `validatePIIEntities` | Closed-set whitelist gate at `config.go:755-769`; three new map keys make the boot validation accept the new names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.4-01 | Tampering (false positive) | `usAddressRe` on shipping-log / ticket text containing `<digits> <name> <suffix>` shape (e.g., `42 Customer Street tickets opened today`) | medium | mitigate | (a) Street-suffix vocabulary already filters most non-address text — bare `<num> <name>` without a known suffix word does NOT match; (b) operators with sensitive log volume can exclude via `PII_ENABLED_ENTITIES` allowlist (omit `USAddress`); (c) `PII_REDACTION_MODE=replace` lets non-PII matches round-trip as `[USAddress_1]` token without data loss — encrypt mode still round-trips byte-for-byte. Trade-off documented in PII-01 requirement text. |
| T-08.4-02 | Tampering (false positive) | `usStateRe` on common English two-letter words (`OR`, `IN`, `OK`, `HI`, `ME`, `ID`, `AS`) | medium | mitigate | (a) Lead anchor `(?:,\s+|^|\n)` requires comma+space OR line start before the two-letter code; English mid-sentence words don't match; (b) Reject-invalid test `TestUSStateRecognizer_RejectsEnglishWords` exercises all five English-word collision cases per PATTERNS.md §B2 — RED-phase gate before GREEN can land. |
| T-08.4-03 | Tampering (false positive) | `usZIPRe` on phone area codes / PINs / order numbers / 5-digit IDs | medium | mitigate | (a) `validateUSZIPRange` rejects all-same-digit codes (00000, 11111, 99999); (b) Accept the residual trade-off — same shape as the existing IPv4 recognizer which matches version numbers like `1.2.3.4` — false positive becomes a token round-trip (operator-trust model); (c) Operators can exclude via `PII_ENABLED_ENTITIES` allowlist. Documented in PII-01 requirement text. |
| T-08.4-04 | Cross-recognizer span overlap | Adjacent USAddress + USState + USZIP spans on `1111 Main Street, Austin, TX 27584` | low | accept | Existing `mergeSpansGreedy` (`spans.go:37`) handles non-overlapping spans correctly per RESEARCH §A8. Integration test `TestPIIRedactionHook_USAddressFullCoverage` asserts all three counts hit exactly 1 — three distinct tokens emit. Pitfall 7 documents the trailing `, ` consumption on USState — acceptable (comma falls outside encrypted blob). |
| T-08.4-05 | PERSON false positive on landmark names | `Apple Park`, `Liberty Park` (not street addresses — no suffix) | low | accept (out of scope) | Out-of-scope per ROADMAP Phase 8.4 lock ("PERSON recognizer tuning. Tuning the model itself is a separate concern"). Landmarks without a street suffix won't be caught by `usAddressRe`; the NER PERSON false positive on them still fires when NER is enabled. Documented for future phase. |

No `high`-severity threats. ASVS L1 baseline preserved (input validation at hook boundary; closed-set allowlist on env var; no new auth surface). Plan proceeds without security-gate block.

## Package Legitimacy Audit

Not applicable. Phase 8.4 introduces no new packages. The `Recognizers` slice grows from 13 to 16 entries; all backed by `regexp` from the Go stdlib. Slopcheck gate is a no-op. (See RESEARCH.md §Package Legitimacy Audit.)
</threat_model>

<tasks>

<task type="tdd" tdd="true">
  <name>Task R: RED — span tests + integration test that MUST fail against current code</name>
  <gate>red</gate>
  <files>internal/plugin/pii/recognizers_test.go, internal/plugin/pii/pii_test.go</files>
  <read_first>
    - @internal/plugin/pii/recognizers_test.go (lines 30-50: regexAndValidate helper + findRecognizer; lines 135-161: TestSSNRecognizer_ReservedRangeValidator gold standard for paired regex+validator; lines 233-271: TestUSPhoneRecognizer_CapturedSpan — THE gold-standard captured-span test, AP-5 reference; lines 273-296: TestUSPhoneRecognizer_RejectsInvalidShapes pattern for reject-invalid; lines 487-508: TestRecognizers_RegistryShape — count 13 to flip)
    - @internal/plugin/pii/pii_test.go (lines 51: userMessage helper; lines 71-76: withCtxSummary helper; lines 904-973: TestPIIRedactionHook_NEREncryptRoundTrip — closest analog for the integration test, mirror the construction + Before + summary.Counts assertion shape; lines 294-297: existing summary.Counts assertion example)
    - @internal/plugin/pii/pii.go (lines 227-233: collectRegexSpans first-recognizer-wins overlap; lines 258-277: acceptNERSpans the load-bearing overlap-arbiter contract)
    - @internal/plugin/pii/spans.go (line 37: mergeSpansGreedy final merge step)
    - @internal/plugin/pii/ner.go (NewNEREngine API — the integration test uses this)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-PATTERNS.md (§B1 captured-span gold standard / §B2 reject-invalid patterns / §B3 paired regex+validator / §C1 integration test pattern)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-RESEARCH.md (§Code Examples lines 239-260 for USAddress capture shape; lines 550-619 for reject-invalid cases; lines 622-669 for integration test shape; §Pitfall 7 for the USState lead-anchor consumption note)
    - Phase 8.3 gold-standard analog commits: `5bbfe03` (RED — failing test added first) + `f47dc8c` (GREEN — minimal pass) — the structural template for this task's RED commit shape.
  </read_first>
  <behavior>
    - TestUSAddressRecognizer_CapturedSpan: 7+ subtests, each with `wantCapture` field. Cases: `"Visit 1111 Main Street today."` → `"1111 Main Street"`; `"Visit 1600 Pennsylvania Ave today."` → `"1600 Pennsylvania Ave"`; `"Visit 1600 Pennsylvania Avenue today."` → `"1600 Pennsylvania Avenue"`; `"Visit 350 Fifth Avenue today."` → `"350 Fifth Avenue"`; `"Visit 1 Apple Park Way today."` → `"1 Apple Park Way"`; `"Mail to 42 Elm St."` → `"42 Elm St"` (trailing period absorbed by `\.?`); `"Office at 100 Beacon Hill Pkwy."` → `"100 Beacon Hill Pkwy"`. Assertion: `if got := r.Pattern.FindString(c.input); got != c.wantCapture { t.Errorf(...) }`. Exact-string equality, AP-5.
    - TestUSAddressRecognizer_RejectsNonAddressShapes: subtests for `"Visit 1111 Main today."` (no suffix), `"Visit Main Street today."` (no number), `"Visit 1111 Main Mall today."` (unknown suffix), `"Visit 1111 main street today."` (lowercase), `"Visit 1111 Main\nStreet today."` (newline inside). Assertion: `if got := r.Pattern.FindString(c.input); got != "" { t.Errorf("USAddress should NOT have matched %q but captured %q", c.input, got) }`.
    - TestUSStateRecognizer_CapturedSpan: subtests for `"He lives at 100 Oak Ave, Austin, TX 27584."` → wantCapture `", TX "` (lead-anchor + trail-anchor consumption per Pitfall 7); `"He lives at 100 Oak Ave, Boston, MA 02101."` → `", MA "`; `"He lives at 100 Oak Ave, Honolulu, HI 96701."` → `", HI "`; `"He lives in CA."` → `" CA."` (single-space + period trail) or document the exact span the regex produces.
    - TestUSStateRecognizer_RejectsEnglishWords: subtests for `"Should we use Plan A OR Plan B?"`, `"She lives IN that house."`, `"Hi there, how are you?"`, `"OK, that works for me."`, `"Send ME the report."`, `"Can you ID this issue?"`, `"TX is a state code but lacks context here."` (bare leading TX, no comma). Assertion: must be `r.Pattern.FindString(c.input) == ""` for each.
    - TestUSZIPRecognizer_CapturedSpan: subtests for `"ZIP 27584"` → wantCapture `"27584"`; `"ZIP 20500-1234"` → `"20500-1234"`; `"ZIP 02101"` → `"02101"`. Exact-string equality.
    - TestUSZIPRecognizer_ValidatorRejectsAllSameDigit: paired regex+validator pattern (use the existing `regexAndValidate` helper at recognizers_test.go:35-44). Cases: `{"27584", true, true}`, `{"20500-1234", true, true}`, `{"00000", true, false}`, `{"11111", true, false}`, `{"99999", true, false}`, `{"55555", true, false}`, `{"1234", false, false}` (4 digits — regex shape rejects), `{"12345-6789", true, true}` (passes validator + regex).
    - TestPIIRedactionHook_USAddressFullCoverage: integration test in pii_test.go (NOT a subtest of recognizers_test.go — it exercises the full hook). Construct `hook := &PIIRedactionHook{Recognizers: Recognizers, Enabled: true, Mode: "replace", NER: NewNEREngine()}`. Build `req := &canonical.ChatRequest{Messages: []canonical.Message{userMessage("Send a tech to 1111 Main Street, Austin, TX 27584 ASAP.")}}`. Acquire `ctx, summary := withCtxSummary(t)`. Call `hook.Before(ctx, req)`. Assert `summary.Counts["USAddress"] == 1`, `summary.Counts["USState"] == 1`, `summary.Counts["USZIP"] == 1`, `summary.Counts["LOCATION"] == 1` (Austin from NER), and CRITICALLY `summary.Counts["PERSON"] == 0` (Main Street suppressed by USAddress overlap). Also assert `!strings.Contains(req.Messages[0].Content[0].Text, "1111 Main Street")` to prove plaintext street was redacted.
    - DO NOT yet add the usAddressRe / usStateRe / usZIPRe regex literals to recognizers.go — this task is RED only. The recognizers_test.go tests MUST fail with compile errors (`undefined: usAddressRe`) because `findRecognizer(t, "USAddress")` will lookup-fail (the Recognizers slice doesn't contain the entry yet) and panic with `recognizer not found`. The integration test MUST fail at runtime because (a) compile fails before runtime if any recognizers_test.go test references a missing symbol — actually findRecognizer lookups by name string so compilation passes, then runtime t.Fatal fires.
    - DO NOT yet update TestRecognizers_RegistryShape — that update lands in Task F (REFACTOR).
  </behavior>
  <action>
    Add to internal/plugin/pii/recognizers_test.go (append after the last test function): TestUSAddressRecognizer_CapturedSpan, TestUSAddressRecognizer_RejectsNonAddressShapes, TestUSStateRecognizer_CapturedSpan, TestUSStateRecognizer_RejectsEnglishWords, TestUSZIPRecognizer_CapturedSpan, TestUSZIPRecognizer_ValidatorRejectsAllSameDigit. Mirror the table-driven `for _, c := range cases { t.Run(c.name, ...) }` shape from TestUSPhoneRecognizer_CapturedSpan. Use `findRecognizer(t, "USAddress" | "USState" | "USZIP")` helper. Use `regexAndValidate(r, c.in)` helper for the USZIP paired test.

    Add to internal/plugin/pii/pii_test.go (append after TestPIIRedactionHook_NEREncryptRoundTrip): TestPIIRedactionHook_USAddressFullCoverage. Use the helpers documented in <read_first> — userMessage, withCtxSummary. Required imports: `strings` (for Contains check) — already imported in pii_test.go.

    Commit message: `test(08.4): add RED span+integration tests for US address recognizers` — atomic RED commit per Phase 8.3 analog `5bbfe03`. The commit body should note: "These tests FAIL against current code: (a) findRecognizer(t, \"USAddress\"|\"USState\"|\"USZIP\") panics with `recognizer not found` because the Recognizers slice lacks the three new entries; (b) TestPIIRedactionHook_USAddressFullCoverage's `summary.Counts[\"PERSON\"] == 0` assertion fails because the current code lets the prose-v2 NER's PERSON candidate for `Main Street` survive overlap arbitration. GREEN follows in the next commit."

    Do NOT touch recognizers.go in this commit. Do NOT touch config.go in this commit. Do NOT touch scripts/test-pii.* in this commit. Do NOT touch REQUIREMENTS.md in this commit. The RED commit is test-only.

    PowerShell BOM vigilance: no scripts/test-pii.ps1 edit in this task — the f7ccd40 + executor-rescue pattern (UTF-8 BOM + ASCII-only) applies in Task F when the smoke scripts ARE edited. Flagged here for awareness because the next task involves a similar Recognizers-slice edit and the executor must NOT drift into editing scripts in this task.

    No deferred TEST-01 / TEST-02 references — TEST-01 was reverted in 08.3.2; this plan only introduces PII-01.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test -race -run 'TestUSAddressRecognizer_CapturedSpan|TestUSStateRecognizer_CapturedSpan|TestUSZIPRecognizer_CapturedSpan|TestUSAddressRecognizer_RejectsNonAddressShapes|TestUSStateRecognizer_RejectsEnglishWords|TestUSZIPRecognizer_ValidatorRejectsAllSameDigit|TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s 2>&amp;1 | tee /tmp/08.4-red.log; ! grep -q 'PASS' /tmp/08.4-red.log || echo 'RED FAILED — tests should NOT pass'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "TestUSAddressRecognizer_CapturedSpan\|TestUSStateRecognizer_CapturedSpan\|TestUSZIPRecognizer_CapturedSpan" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers_test.go` returns >= 3.
    - Source: `grep -c "TestPIIRedactionHook_USAddressFullCoverage" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/pii_test.go` returns >= 1.
    - Source: `grep -c "summary.Counts\[\"PERSON\"\] == 0\|summary.Counts\\[\"PERSON\"\\]; got != 0" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/pii_test.go` returns >= 1 (the load-bearing assertion).
    - Source AP-5 enforcement: `grep -v '^[[:space:]]*//' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers_test.go | grep -c 'got != c.wantCapture'` returns >= 3 (one per new captured-span test).
    - Source AP-5 negative: `grep -v '^[[:space:]]*//' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers_test.go | grep -c 'TestUSAddressRecognizer_CapturedSpan.*FindString.*!= ""'` returns 0 (the new tests must NOT use the weak `!= ""` form).
    - Behavior: `go test -race -run 'TestUSAddressRecognizer_CapturedSpan|TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s` exits NON-zero (RED — tests must fail because the regex literals + Recognizers entries do not yet exist).
    - File untouched: `git diff --name-only HEAD -- internal/plugin/pii/recognizers.go internal/config/config.go scripts/test-pii.ps1 scripts/test-pii.sh .planning/REQUIREMENTS.md` returns EMPTY (RED commit is test-only).
    - Commit shape: `git log -1 --pretty=%s` matches `^test(08\.4): add RED .*`.
  </acceptance_criteria>
  <done>
    Seven new test functions added across recognizers_test.go (six) and pii_test.go (one). Running the new tests fails — findRecognizer panics on the three missing names AND the integration test's PERSON==0 assertion fails because USAddress regex doesn't exist yet to suppress NER. No source files touched. Atomic RED commit landed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task G: GREEN — add three regex literals + validator + three Recognizer entries</name>
  <gate>green</gate>
  <files>internal/plugin/pii/recognizers.go</files>
  <read_first>
    - @internal/plugin/pii/recognizers.go (lines 64-78: top-of-var docstring on RE2 limitations + new-recognizer extension path; lines 80-145: existing var (...) block — append after siteRe at 143-145; lines 185-205: validateSSNRange — gold-standard validator shape for validateUSZIPRange; lines 213-255: Recognizers slice — append three entries before the closing `}` at line 255)
    - @internal/plugin/pii/luhn.go (whole file: alternate validator-shape analog — shows closure-adapter idiom that we DON'T need for validateUSZIPRange; documented in PATTERNS §A4 for contrast)
    - @internal/plugin/pii/pii.go (lines 227-233: collectRegexSpans first-recognizer-wins overlap — confirms registration order USAddress → USState → USZIP delivers the AP-4 mitigation)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-PATTERNS.md (§A1 init-time MustCompile / §A2 context-anchored regex / §A3 alternation-with-vocabulary / §A4 validator function / §A5 Recognizers slice registration)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-RESEARCH.md (§Code Examples lines 373-491 — verbatim regex patterns + validator body + slice diff)
    - Phase 8.3 gold-standard analog commit: `f47dc8c` (GREEN — minimum-diff implementation that makes RED pass) — structural template for this task.
  </read_first>
  <action>
    Edit internal/plugin/pii/recognizers.go in three localized regions:

    1. **Append regex literals** to the existing `var (...)` block (currently ending around line 145 after `siteRe`). Add (in this order — USZIP, USState, USAddress — alphabetized within the new group is fine; registration order is controlled separately by the Recognizers slice):

       ```
       usZIPRe = regexp.MustCompile(`\b\d{5}(?:-\d{4})?\b`)

       usStateRe = regexp.MustCompile(
           `(?:,\s+|^|\n)` +
               `(?:AL|AK|AZ|AR|CA|CO|CT|DE|DC|FL|GA|HI|ID|IL|IN|IA|KS|KY|LA|ME|` +
               `MD|MA|MI|MN|MS|MO|MT|NE|NV|NH|NJ|NM|NY|NC|ND|OH|OK|OR|PA|RI|SC|` +
               `SD|TN|TX|UT|VT|VA|WA|WV|WI|WY|AS|GU|MP|PR|VI)` +
               `(?:\b|\s+\d{5}|\.|,)`)

       usAddressRe = regexp.MustCompile(
           `\b\d{1,6}\s+[A-Z][A-Za-z]*(?:\s+[A-Z][A-Za-z]*)*\s+` +
               `(?:St|Street|Ave|Avenue|Blvd|Boulevard|Rd|Road|Dr|Drive|Ln|Lane|` +
               `Way|Pl|Place|Ct|Court|Pkwy|Parkway|Cir|Circle|Ter|Terrace|Sq|` +
               `Square|Hwy|Highway)\b\.?`)
       ```

       Each literal MUST be preceded by a doc comment matching the existing style (see ssnRe / creditCardRe / coordinatesRe / siteRe comments). Use the doc-block text from RESEARCH.md §Code Examples lines 380-432 verbatim where it fits, lightly trimmed for line length.

    2. **Append validator function** `validateUSZIPRange` after `validateSSNRange` (around line 205). Body per RESEARCH.md §Code Examples lines 442-458 — uses `strings.IndexByte` (already imported at recognizers.go:35 per RESEARCH.md §Standard Stack). Function shape:

       ```
       func validateUSZIPRange(s string) bool {
           base := s
           if dash := strings.IndexByte(s, '-'); dash > 0 {
               base = s[:dash]
           }
           if len(base) != 5 {
               return false
           }
           allSame := true
           for i := 1; i < len(base); i++ {
               if base[i] != base[0] {
                   allSame = false
                   break
               }
           }
           return !allSame
       }
       ```

    3. **Append Recognizer entries** to the `Recognizers = []Recognizer{...}` slice (currently ending around line 255 after the SITE entry). Insert BEFORE the closing `}`:

       ```
       // Phase 08.4: US address coverage (USAddress, USState, USZIP).
       // Order: largest span first (USAddress) → context-anchored alphabet
       // alternation (USState) → 5-digit numeric (USZIP). First-recognizer-
       // wins overlap arbitration (pii.go:227-233) gives the largest match
       // priority on the rare overlap case.
       {Name: "USAddress", Pattern: usAddressRe, Validate: nil},
       {Name: "USState", Pattern: usStateRe, Validate: nil},
       {Name: "USZIP", Pattern: usZIPRe, Validate: validateUSZIPRange},
       ```

    DO NOT modify any other file in this commit. The GREEN commit is minimum-diff: ONLY recognizers.go. This is critical — TestRecognizers_RegistryShape will FAIL (count 13 → got 16) after this commit, AND the recognizers_test.go new tests must NOW PASS. The registry-shape failure is expected — Task F (REFACTOR) bumps it.

    Commit message: `feat(08.4): add USAddress, USState, USZIP regex recognizers per PII-01` — atomic GREEN commit per Phase 8.3 analog `f47dc8c`. Body should note: "Three regex literals + validateUSZIPRange + three Recognizer slice entries. Closes the seven RED tests added in previous commit. Registry-shape test (TestRecognizers_RegistryShape) intentionally remains RED until REFACTOR phase bumps wantNames 13 → 16."

    Verify imports: `strings` should already be in recognizers.go's import block per recognizers.go:35 (per PATTERNS.md §A4 line 200) — no new import needed.

    Per D-06 PATTERNS §Shared "init-time MustCompile + source-level guard": the existing `TestRecognizers_CompiledAtPackageInit_NoPerRequestCompile` test at recognizers_test.go:514 greps for `regexp.MustCompile(` calls — three new ones land here and automatically satisfy its assertion. No edit to that test.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test -race -run 'TestUSAddressRecognizer_CapturedSpan|TestUSStateRecognizer_CapturedSpan|TestUSZIPRecognizer_CapturedSpan|TestUSAddressRecognizer_RejectsNonAddressShapes|TestUSStateRecognizer_RejectsEnglishWords|TestUSZIPRecognizer_ValidatorRejectsAllSameDigit|TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "usAddressRe\s*=\s*regexp.MustCompile" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns >= 1.
    - Source: `grep -c "usStateRe\s*=\s*regexp.MustCompile" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns >= 1.
    - Source: `grep -c "usZIPRe\s*=\s*regexp.MustCompile" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns >= 1.
    - Source AP-1 (suffix vocabulary mandatory): `grep -c "Street\|Boulevard\|Avenue" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns >= 1 inside the usAddressRe block.
    - Source AP-2 (context anchor mandatory): `grep -c '(?:,..s+|\^|.n)' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns >= 1 (the usStateRe lead-anchor literal — escape-aware grep).
    - Source AP-3 (validator mandatory): `grep -c "func validateUSZIPRange" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns 1.
    - Source AP-4 (registration order): `grep -nE '"USAddress"|"USState"|"USZIP"' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` shows USAddress first, then USState, then USZIP in line-number order.
    - Source: `grep -c 'Validate: validateUSZIPRange' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers.go` returns 1.
    - Behavior: `go test -race -run 'TestUSAddressRecognizer_CapturedSpan|TestUSStateRecognizer_CapturedSpan|TestUSZIPRecognizer_CapturedSpan|TestUSAddressRecognizer_RejectsNonAddressShapes|TestUSStateRecognizer_RejectsEnglishWords|TestUSZIPRecognizer_ValidatorRejectsAllSameDigit|TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s` exits 0.
    - Behavior negative-control: `go test -race -run 'TestRecognizers_RegistryShape' ./internal/plugin/pii/... -timeout 30s` exits NON-zero (count 16 != want 13 — fixed in Task F).
    - File scope: `git diff --name-only HEAD~1 HEAD -- internal/plugin/pii/` returns ONLY `internal/plugin/pii/recognizers.go`.
    - Commit shape: `git log -1 --pretty=%s` matches `^feat(08\.4): .*USAddress.*USState.*USZIP.*PII-01.*` (or similar — PII-01 must be cited in the commit subject or body per decision-coverage requirement).
  </acceptance_criteria>
  <done>
    Three regex literals + validateUSZIPRange + three Recognizer{} entries land in recognizers.go. The seven RED tests now pass. TestRecognizers_RegistryShape now fails (count mismatch) — by design, fixed in Task F. Atomic GREEN commit landed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task F: REFACTOR — registry-shape, config allowlist, REQUIREMENTS.md, smoke scripts (PS1+SH parity)</name>
  <gate>refactor</gate>
  <files>internal/plugin/pii/recognizers_test.go, internal/config/config.go, .planning/REQUIREMENTS.md, scripts/test-pii.ps1, scripts/test-pii.sh</files>
  <read_first>
    - @internal/plugin/pii/recognizers_test.go (lines 487-508: TestRecognizers_RegistryShape — wantNames slice + len assertion to bump 13 → 16)
    - @internal/config/config.go (lines 715-741: piiAllowedEntities docstring + map; lines 755-769: validatePIIEntities consumer — read-only context, no edit)
    - @.planning/REQUIREMENTS.md (lines 6-104: ## v1 Requirements section; line 75: PLUG-06 entry — closest sibling for shape; line 105: ## v2 Requirements — INSERT new ### PII — Recognizer coverage section BEFORE this line per RESEARCH §A5 rec a; line 196: PLUG-06 Traceability row — model for new row; lines 218-222: count footer + footnote)
    - @scripts/test-pii.ps1 (lines 310-356: $script:PIIPrompt + $script:PIIExpected — extend; line 432: decryptTokenRe `[A-Za-z0-9]+` entity-name group — VERIFIED no change needed per Pitfall 6)
    - @scripts/test-pii.sh (lines 250-260: PII_PROMPT + PII_EXPECT_* — mirror; around line 324: `[A-Za-z0-9]+` decrypt regex — also no change needed; iteration loop in pii_probe — extend with three new vars)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-PATTERNS.md (§B4 registry-shape bump / §D1 piiAllowedEntities edit / §E1+§F1 smoke-script extension + ASCII-only invariant + PS1/SH parity gate / §G1+G2+G3 REQUIREMENTS.md edits)
    - @.planning/phases/08.4-us-address-pii-coverage/08.4-RESEARCH.md (§Code Examples lines 525-541 for TestRecognizers_RegistryShape edit; lines 496-519 for piiAllowedEntities; lines 672-721 for smoke scripts)
    - Project memory note: `feedback-pii-bracket-shape.md` — square brackets only in PowerShell PII fixtures, never angle brackets (kiro-cli/Claude hangs on `<...>` as XML tag).
    - PowerShell encoding commit reference: `f7ccd40` originally added a UTF-8 BOM as belt-and-suspenders, but the BOM was subsequently lost in editor roundtrips and the file now reports `ASCII text` from `file(1)`. The LOAD-BEARING invariant is **ASCII-only content** (no em-dashes, no smart quotes, no non-ASCII bytes) — pure ASCII parses correctly under PowerShell 5.1 regardless of BOM presence. Do NOT re-introduce a UTF-8 BOM in this phase; the executor must keep the file pure-ASCII without BOM.
  </read_first>
  <action>
    Five localized edits, landed in ONE atomic commit (PS1/SH parity gate per PATTERNS.md §Shared):

    **1. internal/plugin/pii/recognizers_test.go** — Update `TestRecognizers_RegistryShape` (around line 489-508). Change the `wantNames` literal from the existing 13-entry list to:

       ```
       wantNames := []string{
           "Email", "IPv4", "IPv6", "SSN", "CreditCard", "USPhone",
           "SIP_URI", "IMEI", "IMSI", "MSISDN", "MAC_ADDRESS",
           "COORDINATES", "SITE",
           // Phase 08.4: US address coverage (PII-01).
           "USAddress", "USState", "USZIP",
       }
       ```

       The `len(Recognizers): got 16, want 13` assertion now passes — the count is implicit (`len(wantNames)` so the literal is the source of truth).

    **2. internal/config/config.go** — Update `piiAllowedEntities` map (lines 720-741). Insert three keys BETWEEN `"SITE": {},` (currently line 733) and the `// NER-emitted entity names (Task 11). ...` comment block (currently line 734):

       ```
       "SITE":        {},
       // Phase 08.4: US address coverage (PII-01). Order matches Recognizers
       // slice in internal/plugin/pii/recognizers.go (USAddress → USState → USZIP).
       "USAddress":   {},
       "USState":     {},
       "USZIP":       {},
       // NER-emitted entity names (Task 11). ...
       "PERSON":   {},
       "LOCATION": {},
       ```

       Validator `validatePIIEntities` at config.go:755-769 is data-driven off the map — NO edit to the validator function.

    **3. .planning/REQUIREMENTS.md** — Three edits:

       (a) Insert new section BEFORE `## v2 Requirements` (line 105). New section text:

       ```
       ### PII — Recognizer coverage

       - [ ] **PII-01**: `PIIRedactionHook` ships US-address coverage as three additional recognizers in the canonical registry: `USAddress` (house number + TitleCase street-name words + 16-form USPS-suffix vocabulary), `USState` (context-anchored two-letter state/DC/territory code — 56 codes total), `USZIP` (5-digit base + optional ZIP+4 extension, all-same-digit codes rejected by `validateUSZIPRange`). Registration order is `USAddress → USState → USZIP` so the first-recognizer-wins overlap arbitration at `internal/plugin/pii/pii.go:227-233` favors the largest atomic span. All three recognizers participate in encrypt-mode round-trip via the existing `[PII:<Entity>:base64url]` token (entity-name group `[A-Za-z0-9]+` already alphanumeric-tolerant — Pitfall 6). With `PII_NER_ENABLED=true`, NER PERSON candidates that overlap a `USAddress` span are dropped at `internal/plugin/pii/pii.go:258-277` — closes the 2026-06-04 splunk-box probe false positives on "Main Street" / "Pennsylvania Avenue" / "Apple Park".

       ```

       (b) Insert into Traceability table after the PLUG-06 row (line 196):

       ```
       | PII-01 | Phase 08.4 | Complete |
       ```

       (c) Update count footer (lines 218 + 222):

       - Line 218: `- v1 requirements: 62 total` → `- v1 requirements: 63 total`
       - Line 222 footnote: extend with `Phase 08.4 adds PII-01 (US address coverage), count now 63.`

    **4. scripts/test-pii.ps1** — Extend `$script:PIIPrompt` (around lines 310-356):

       (a) Insert a new line in the customer-profile block BEFORE the existing `- Email on file:` line:

       ```
       - Site address: 1111 Main Street, Austin, TX 27584
       ```

       (b) Update the LLM-instructions numbered list — extend item #2 to mention the address: `2. States the customer name (John Smith), city (Boston, Massachusetts), AND on-site address (1111 Main Street, Austin, TX 27584) so the on-call engineer knows who is affected AND where to dispatch a tech.`

       (c) Append three needles to `$script:PIIExpected` array:

       ```
       '1111 Main Street',    # USAddress — exact captured span
       'TX',                  # USState — needle on raw code (recognizer match span includes leading ", ")
       '27584'                # USZIP — validator accepts non-all-same-digit
       ```

       (d) ASCII-only invariant per f7ccd40: do NOT introduce smart quotes, em-dashes, or non-ASCII characters. The file currently reports `ASCII text` from `file(1)` and has NO UTF-8 BOM — do NOT introduce a BOM in this phase (pure-ASCII content parses correctly under PowerShell 5.1 without one). Do NOT use `<...>` placeholders anywhere — use `[...]` if a placeholder is added (per project memory `feedback-pii-bracket-shape.md`).

       (e) Decrypt-regex at `test-pii.ps1:432` is `[A-Za-z0-9]+` — already accepts `USAddress`/`USState`/`USZIP`. **No edit needed.**

    **5. scripts/test-pii.sh** — Mirror PS1 changes (PS1/SH parity per PATTERNS §Shared):

       (a) Extend `PII_PROMPT` (around lines 250-260) with the same `\n- Site address: 1111 Main Street, Austin, TX 27584\n` segment in the customer profile block.

       (b) Add three new vars after the existing `PII_EXPECT_*` block:

       ```
       PII_EXPECT_ADDRESS="1111 Main Street"   # USAddress
       PII_EXPECT_STATE="TX"                   # USState
       PII_EXPECT_ZIP="27584"                  # USZIP
       ```

       (c) Locate the iteration loop inside `pii_probe` function (around test-pii.sh:330 — check exact line via grep). Extend the iteration to include the three new vars so each needle is asserted against the decrypted response. Match the existing for-loop / case statement idiom — DO NOT introduce a new idiom.

       (d) Decrypt-regex at `test-pii.sh:324` is `[A-Za-z0-9]+` — already accepts the three new entity names. **No edit needed.**

       (e) ASCII-only invariant: same as PS1. No non-ASCII characters. POSIX bash 3.2+ compatible (no bash 4+ features like associative arrays unless already used in the file).

    **All five edits land in ONE atomic commit.** The PS1/SH parity gate is the load-bearing rule — splitting these across two commits is the f7ccd40-class drift bug. Commit message: `refactor(08.4): registry-shape, config allowlist, REQUIREMENTS.md, smoke scripts for PII-01` — atomic REFACTOR commit per Phase 8.3 cycle shape. Body should note: "Closes Phase 08.4 source delivery: TestRecognizers_RegistryShape bumped 13 → 16, piiAllowedEntities map gains three keys, REQUIREMENTS.md gets new ### PII — Recognizer coverage section + PII-01 entry + Traceability row + v1 count 62 → 63, scripts/test-pii.ps1 + scripts/test-pii.sh fixtures extended in lockstep with the canonical address fixture `1111 Main Street, Austin, TX 27584`. PS1/SH parity preserved in one commit per PATTERNS §Shared. Operator HUMAN-UAT in Task H."

    Run `make ci` locally before commit to confirm gofumpt-diff, govet, build, lint, govulncheck, and `go test -race ./...` all pass. The `make ci` exit-0 is the load-bearing trust gate.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; make ci 2>&amp;1 | tee /tmp/08.4-refactor-ci.log &amp;&amp; grep -v '^#' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers_test.go | grep -c '"USAddress", "USState", "USZIP"' &amp;&amp; grep -v '^#' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/config/config.go | grep -c '"USAddress":' &amp;&amp; grep -v '^[[:space:]]*#' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/.planning/REQUIREMENTS.md | grep -c 'PII-01' &amp;&amp; grep -c '1111 Main Street' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.ps1 &amp;&amp; grep -c '1111 Main Street' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.sh &amp;&amp; (file /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.ps1 | grep -q 'ASCII text' || { echo 'BOM/non-ASCII regression on scripts/test-pii.ps1 — must be pure ASCII'; exit 1; }) &amp;&amp; (! file /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.sh | grep -q 'with BOM' || { echo 'BOM regression on scripts/test-pii.sh — must NOT be (with BOM)'; exit 1; })</automated>
  </verify>
  <acceptance_criteria>
    - Source registry-shape: `grep -v '^[[:space:]]*//' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/plugin/pii/recognizers_test.go | grep -c '"USAddress", "USState", "USZIP"'` returns >= 1 (the new entries appended to wantNames).
    - Source config allowlist: `grep -v '^[[:space:]]*//' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/internal/config/config.go | grep -c '"USAddress":[[:space:]]*{}'` returns 1; same check for `"USState":` and `"USZIP":` each returns 1.
    - Source REQUIREMENTS section: `grep -c '^### PII — Recognizer coverage' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/.planning/REQUIREMENTS.md` returns 1.
    - Source REQUIREMENTS entry: `grep -c 'PII-01.*PIIRedactionHook.*US-address' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/.planning/REQUIREMENTS.md` returns >= 1.
    - Source REQUIREMENTS traceability: `grep -c '| PII-01 | Phase 08\.4 |' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/.planning/REQUIREMENTS.md` returns 1.
    - Source REQUIREMENTS count: `grep -c 'v1 requirements: 63 total' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/.planning/REQUIREMENTS.md` returns 1; `grep -c 'v1 requirements: 62 total' ...` returns 0.
    - Source PS1: `grep -c '1111 Main Street, Austin, TX 27584' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.ps1` returns >= 1.
    - Source SH: `grep -c '1111 Main Street, Austin, TX 27584' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.sh` returns >= 1.
    - Source PS1 needles: `grep -c "'1111 Main Street'" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.ps1` returns >= 1; `grep -c "'27584'" ...` returns >= 1.
    - Source SH needles: `grep -c 'PII_EXPECT_ADDRESS=' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/test-pii.sh` returns 1; same for PII_EXPECT_STATE and PII_EXPECT_ZIP each returns 1.
    - PS1/SH parity gate: `git diff --name-only HEAD~1 HEAD -- scripts/test-pii.ps1 scripts/test-pii.sh | wc -l` returns 2 (both files in the same commit — neither alone).
    - Behavior registry-shape: `go test -race -run 'TestRecognizers_RegistryShape' ./internal/plugin/pii/... -timeout 30s` exits 0.
    - Behavior all-new tests still green: `go test -race -run 'TestUSAddressRecognizer_CapturedSpan|TestUSStateRecognizer_CapturedSpan|TestUSZIPRecognizer_CapturedSpan|TestUSAddressRecognizer_RejectsNonAddressShapes|TestUSStateRecognizer_RejectsEnglishWords|TestUSZIPRecognizer_ValidatorRejectsAllSameDigit|TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s` exits 0.
    - Behavior full PII suite: `go test -race ./internal/plugin/pii/... -timeout 120s` exits 0 (no regressions in existing 13 recognizers or NER pipeline).
    - Behavior config: `go test -race ./internal/config/... -timeout 30s` exits 0 (validatePIIEntities accepts USAddress/USState/USZIP).
    - Trust gate: `make ci` exits 0 (gofumpt-diff clean, vet clean, build clean, golangci-lint clean, govulncheck clean, go test -race ./... clean).
    - Goleak: search the test output for `goleak: no goroutines leaked` markers (`grep -c 'goleak' /tmp/08.4-refactor-ci.log` returns >= 1 OR `make ci` exit-0 implies the testmain goleak guard passed).
    - Commit shape: `git log -1 --pretty=%s` matches `^refactor(08\.4): .*PII-01.*` (or commit body cites PII-01 explicitly per decision-coverage requirement).
    - Commit scope: `git diff --name-only HEAD~1 HEAD` returns exactly five files: `internal/plugin/pii/recognizers_test.go`, `internal/config/config.go`, `.planning/REQUIREMENTS.md`, `scripts/test-pii.ps1`, `scripts/test-pii.sh`.
  </acceptance_criteria>
  <done>
    Registry-shape test bumped 13 → 16, piiAllowedEntities map gains three keys, REQUIREMENTS.md gets the new `### PII — Recognizer coverage` section + PII-01 entry + Traceability row + count footer flip, both smoke scripts get the canonical address fixture extension in lockstep. `make ci` is green. Atomic REFACTOR commit landed touching exactly five files. Source delivery for PII-01 is complete; Task H is the operator HUMAN-UAT gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task H: HUMAN-UAT — operator smoke runs on Windows + POSIX</name>
  <files>(no source edits — operator-only smoke runs against the v1.10.0 binary; the load-bearing gate is the production-OS validation that the dev-box `make ci` cannot prove)</files>
  <action>Operator runs `scripts/test-pii.ps1 pii` on the Windows splunk box AND `scripts/test-pii.sh pii` on a POSIX box against a freshly-built gateway with `PII_REDACTION_MODE=encrypt`, `PII_REDACTION_ENABLED=true`, `PII_NER_ENABLED=true`. Confirms both exit 0 with `0 check(s) failed`, the three new needles (`1111 Main Street`, `TX`, `27584`) appear plaintext in the decrypted response, the gateway `pii.redact.done` summary shows `USAddress:1, USState:1, USZIP:1, LOCATION:>=1 (Austin), PERSON:0` on the address fixture, and a negative-control prompt (`Plan A OR Plan B`, `Hi there, how are you?`) produces `USState:0` (AP-2 mitigation verified in production). See &lt;how-to-verify&gt; for stepwise instructions.</action>
  <verify><human-check>Operator confirms BOTH OS runs print `0 check(s) failed` AND the negative-control USState count is 0 in the `pii.redact.done` summary.</human-check></verify>
  <done>BOTH Windows + POSIX smoke runs return exit 0 with `0 check(s) failed`; operator notes captured in 08.4-01-SUMMARY.md (OS version, gateway commit SHA, `0 check(s) failed` line, negative-control USState count); PII-01 row in REQUIREMENTS.md Traceability flips to Complete.</done>
  <what-built>
    Three new regex PII recognizers (USAddress, USState, USZIP) with validator, integration test, NER-overlap-arbitration claim, config allowlist update, REQUIREMENTS.md PII-01 entry, and matching smoke-test fixture extensions in scripts/test-pii.ps1 + scripts/test-pii.sh. All committed across three atomic commits (RED → GREEN → REFACTOR). `make ci` is green on the dev box; the load-bearing acceptance is byte-for-byte round-trip through `PII_REDACTION_MODE=encrypt` against real `kiro-cli` + Claude on the production OS (Windows splunk box + POSIX dev box) — mirrors Phase 8.3 Task H pattern.
  </what-built>
  <how-to-verify>
    **Windows splunk box** (mirrors Phase 8.3 Task H step shape):

    1. Pull the v1.10.0 / Phase 08.4 branch on the Windows splunk box: `git pull && git log -1 --oneline` confirms the REFACTOR commit is HEAD.
    2. Build the gateway: `make build` (or unzip the v1.10.0 release artifact if using the release path).
    3. Confirm `PII_REDACTION_MODE=encrypt` and `PII_REDACTION_ENABLED=true` and `PII_NER_ENABLED=true` are set (existing operator env; no new env vars).
    4. Restart the gateway: `.\scripts\otto-gw.ps1 restart`.
    5. Confirm operator-published recognizer list: `Invoke-RestMethod http://localhost:11434/health/hooks` shows `USAddress`, `USState`, `USZIP` in the `entities` array under `PIIRedactionHook`.
    6. Run the PII smoke test: `.\scripts\test-pii.ps1 pii`.
    7. Verify the script prints `0 check(s) failed` AND that the new needles (`1111 Main Street`, `TX`, `27584`) appear in the decrypted-response section of the output.
    8. Pull the gateway log (the `pii.redact.done` line); confirm the summary counts show `USAddress: 1, USState: 1, USZIP: 1, LOCATION: >=1 (Austin), PERSON: 0` for the address-line fixture.

    **POSIX dev box (macOS or Linux)**:

    1. From repo root: `make build`.
    2. Start the gateway with the same env: `PII_REDACTION_MODE=encrypt PII_REDACTION_ENABLED=true PII_NER_ENABLED=true ./scripts/otto-gw start`.
    3. Run the POSIX smoke test: `./scripts/test-pii.sh pii`.
    4. Verify `0 check(s) failed` and that the three new needles appear plaintext in the decrypted response.

    **Negative-control check (run on either OS)**:

    5. Send a prompt containing the prose `Plan A OR Plan B` and the prose `Hi there, how are you?` — confirm in the `pii.redact.done` log line that `USState` count is 0 for that request (the context-anchor mitigation is working; AP-2 closed).

    Approve only if BOTH OS runs succeed AND the negative-control check confirms no English-word USState false positive.

    On Windows: ASCII-only is the load-bearing invariant — if the script fails to parse (PowerShell 5.1 chokes on non-ASCII characters), STOP and re-check `scripts/test-pii.ps1` byte shape: run `file scripts/test-pii.ps1` and expect `ASCII text`. Do NOT re-introduce a UTF-8 BOM as a fix — the v1.9.6 work proved that pure-ASCII content parses correctly without one and BOM keeps getting lost in editor roundtrips anyway. The fix path is to strip non-ASCII characters from the content, not to add a BOM.
  </how-to-verify>
  <resume-signal>Type "approved" with both OS run results (Windows + POSIX), OR describe failures (which assertion, exit code, log line text, OS, gateway version, commit SHA) — do NOT approve unless BOTH OS runs print `0 check(s) failed` AND the negative-control USState count is 0.</resume-signal>
</task>

</tasks>

<verification>
## Phase-Level Checks

After all four tasks complete:

1. **Source coverage:**
   - `grep -c "Name: \"USAddress\"\|Name: \"USState\"\|Name: \"USZIP\"" internal/plugin/pii/recognizers.go` returns exactly 3.
   - `grep -c "\"USAddress\":\|\"USState\":\|\"USZIP\":" internal/config/config.go` returns exactly 3.
   - `grep -c "PII-01" .planning/REQUIREMENTS.md` returns >= 2 (the v1 section entry + the Traceability row).

2. **Test coverage:**
   - `go test -race -run 'TestU(SAddress|SState|SZIP)Recognizer' ./internal/plugin/pii/... -timeout 60s` exits 0.
   - `go test -race -run 'TestPIIRedactionHook_USAddressFullCoverage' ./internal/plugin/pii/... -timeout 60s` exits 0.
   - `go test -race -run 'TestRecognizers_RegistryShape' ./internal/plugin/pii/... -timeout 30s` exits 0 with `wantNames` length 16.

3. **Trust gates:**
   - `make ci` exits 0 across gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`.
   - `goleak.VerifyTestMain` continues clean in `internal/plugin/pii/testmain_test.go`.

4. **Operator smoke (HUMAN-UAT in Task H):**
   - Windows `.\scripts\test-pii.ps1 pii` prints `0 check(s) failed` with all three new needles round-tripping.
   - POSIX `./scripts/test-pii.sh pii` prints `0 check(s) failed` with the same needles.
   - `/health/hooks` exposes `USAddress, USState, USZIP` in the PIIRedactionHook entities array (via `SourceAuditNames` consumer at `pii.go:121-130` — automatic).
   - Negative control: a prose-only request without an address shows `USState: 0` and `USAddress: 0` and `USZIP: 0` in `pii.redact.done` summary.

5. **Anti-pattern enforcement:**
   - AP-1 (USAddress without suffix): RED test `TestUSAddressRecognizer_RejectsNonAddressShapes` includes the `no-street-suffix` case and asserts no match.
   - AP-2 (USState without anchor): RED test `TestUSStateRecognizer_RejectsEnglishWords` includes `OR`, `IN`, `OK`, `HI`, `ME`, `ID` cases and asserts no match.
   - AP-3 (USZIP without validator): RED test `TestUSZIPRecognizer_ValidatorRejectsAllSameDigit` includes `00000`, `11111`, `99999` and asserts validator false.
   - AP-4 (registration order): `grep -n 'Name: "USAddress"\|Name: "USState"\|Name: "USZIP"' internal/plugin/pii/recognizers.go` confirms USAddress < USState < USZIP in line number.
   - AP-5 (exact-string equality): RED tests use `got != c.wantCapture` (NOT `got != ""`); confirmed by acceptance criteria grep on Task R.

6. **PS1/SH parity gate:**
   - The REFACTOR commit (Task F) touches BOTH scripts/test-pii.ps1 AND scripts/test-pii.sh in the same commit. Confirmed via `git log --stat -1 HEAD` after Task F lands.

7. **PowerShell encoding vigilance (ASCII-only invariant — NO BOM):**
   - `scripts/test-pii.ps1` currently reports `ASCII text` from `file(1)` (no UTF-8 BOM). The REFACTOR commit MUST preserve that: post-commit `file scripts/test-pii.ps1` MUST report `ASCII text` (NOT `UTF-8 (with BOM) text` or `UTF-8 Unicode text` with non-ASCII content). The load-bearing invariant is ASCII-only content; a BOM is neither required nor desired (v1.9.6 work proved pure-ASCII parses correctly under PowerShell 5.1 without one and BOM keeps getting lost in editor roundtrips).
   - Same invariant for `scripts/test-pii.sh`: `file scripts/test-pii.sh` may report `Unicode text, UTF-8 text` (the existing bash heredoc-encoded `\n` content is fine) but MUST NOT report `(with BOM)`.

## Verifying the Falsification Surface

The load-bearing claim of Phase 8.4 is that `summary.Counts["PERSON"] == 0` on the address fixture — i.e., the USAddress regex span covers `Main Street` and the overlap arbiter at `pii.go:258-277` drops the NER PERSON candidate. To independently verify this is the regression catch:

- Run with the USAddress entry temporarily commented out of the Recognizers slice: `summary.Counts["PERSON"]` should jump to 1 (or more) on the address fixture — proves the suppression is doing real work.
- Restore USAddress entry: count returns to 0.

This is documentation-only — not part of automated CI — but operators / future planners can use this manual experiment to re-validate the falsification.
</verification>

<success_criteria>
- [ ] PII-01 entry exists in `.planning/REQUIREMENTS.md` under new `### PII — Recognizer coverage` section before `## v2 Requirements`.
- [ ] Three regex literals (`usAddressRe`, `usStateRe`, `usZIPRe`) defined at package init via `regexp.MustCompile` in `internal/plugin/pii/recognizers.go`.
- [ ] One validator (`validateUSZIPRange`) defined in `internal/plugin/pii/recognizers.go`.
- [ ] Three `Recognizer{}` entries in the `Recognizers` slice in order `USAddress → USState → USZIP`.
- [ ] Three keys (`USAddress`, `USState`, `USZIP`) in `piiAllowedEntities` map in `internal/config/config.go`.
- [ ] Three `TestU*Recognizer_CapturedSpan` tests in `recognizers_test.go` using exact-string equality (AP-5 mitigation).
- [ ] Three reject-invalid tests covering AP-1, AP-2, AP-3 mitigations.
- [ ] One `TestUSZIPRecognizer_ValidatorRejectsAllSameDigit` test with paired regex+validator assertions.
- [ ] One integration test `TestPIIRedactionHook_USAddressFullCoverage` asserting `summary.Counts[PERSON] == 0` on `1111 Main Street, Austin, TX 27584`.
- [ ] `TestRecognizers_RegistryShape` `wantNames` slice grows 13 → 16 in registration order.
- [ ] `scripts/test-pii.ps1` extends `$script:PIIPrompt` with the address line and `$script:PIIExpected` with three new needles. ASCII-only preserved (no BOM, no em-dashes, no smart quotes); post-commit `file scripts/test-pii.ps1` reports `ASCII text`.
- [ ] `scripts/test-pii.sh` extends `PII_PROMPT` and adds `PII_EXPECT_ADDRESS`, `PII_EXPECT_STATE`, `PII_EXPECT_ZIP` vars + iteration loop.
- [ ] REFACTOR commit touches both scripts atomically (PS1/SH parity).
- [ ] `make ci` exits 0 (all trust gates green).
- [ ] Operator HUMAN-UAT (Task H) approved with Windows AND POSIX runs printing `0 check(s) failed`.
- [ ] `/health/hooks` exposes the three new recognizer names automatically via `SourceAuditNames`.
- [ ] Three atomic commits (RED, GREEN, REFACTOR) landed in sequence per Phase 8.3 cycle shape.
- [ ] PII-01 cited in every plan-level artifact (commit messages, REQUIREMENTS.md, traceability row, plan frontmatter `requirements:` field).
</success_criteria>

<output>
Create `.planning/phases/08.4-us-address-pii-coverage/08.4-01-SUMMARY.md` when done. Mirror the Phase 8.3 SUMMARY shape (commits + diff stats + verification results + HUMAN-UAT operator notes). Include:

- Three commit SHAs (RED, GREEN, REFACTOR).
- Diff stats per file (lines added/removed).
- Test counts (new tests added, existing tests still passing, total package count).
- `make ci` exit code + key gate timings.
- HUMAN-UAT Task H operator approval notes (Windows OS version + commit SHA + `0 check(s) failed` confirmation; POSIX OS + version + same).
- Negative-control USState count from operator log (confirms AP-2 mitigation in production).
- Any drift findings (PS1 BOM regressions, ASCII collisions, recognizers_test.go test-name typos, REQUIREMENTS.md count-footer miscounts).
- Reference back to RESEARCH.md / PATTERNS.md / VALIDATION.md anchors that were load-bearing during execution.
- PII-01 status flip in REQUIREMENTS.md Traceability table (Complete after HUMAN-UAT signs off).
</output>
