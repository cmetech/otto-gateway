---
phase: 08.4
slug: us-address-pii-coverage
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 08.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.23+) with `-race`; `go.uber.org/goleak` for goroutine leak detection; PowerShell 5.1+ for the Windows operator smoke run; bash 3.2+ for the POSIX smoke run |
| **Config file** | `internal/plugin/pii/testmain_test.go` (existing `goleak.VerifyTestMain`) |
| **Quick run command** | `go test -race -run 'TestUSAddressRecognizer\|TestUSStateRecognizer\|TestUSZIPRecognizer' ./internal/plugin/pii/... -timeout 30s` |
| **Full suite command** | `make ci` (gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`) |
| **Estimated runtime** | ~10 s for quick run; ~90 s for `make ci`; ~10 s per operator surface |

---

## Sampling Rate

- **After every task commit:** Run `go test -race ./internal/plugin/pii/...` (covers all 16 recognizers + the new integration test)
- **After every plan wave:** Run `make ci`
- **Before `/gsd-verify-work`:** `make ci` green + `test-pii.ps1 pii` exits 0 on Windows + `test-pii.sh pii` exits 0 on POSIX
- **Max feedback latency:** ~10 s (quick) / ~90 s (full)

---

## Per-Task Verification Map

> Populated by the planner — one row per task with explicit `<automated>` verify command, plus operator-only smoke runs.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.4-XX-XX | XX | 1 | PII-01 | — | (planner to fill) | unit / integration / shell | `go test -race -run <Name> ./internal/plugin/pii/...` OR `pwsh -File scripts/test-pii.ps1 pii` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/plugin/pii/recognizers.go` — add `usZIPRe`, `usStateRe`, `usAddressRe` regex literals + `validateUSZIPRange` validator + three new entries in the `Recognizers` slice (registration order: `USAddress → USState → USZIP`)
- [ ] `internal/plugin/pii/recognizers_test.go` — extend `TestRecognizers_RegistryShape` from 13 → 16 entries; add `TestUSAddressRecognizer_CapturedSpan` / `TestUSStateRecognizer_CapturedSpan` / `TestUSZIPRecognizer_CapturedSpan` following the v1.9.7 `TestUSPhoneRecognizer_CapturedSpan` pattern (FindString-based span assertions, not match-yes/no); add reject-invalid subtests per recognizer
- [ ] New integration test (in `pii_test.go` or new file) — full-address fixture `1111 Main Street, Austin, TX 27584` with `PII_NER_ENABLED=true` override; asserts `USAddress:1, USState:1, USZIP:1, LOCATION:1 (Austin), PERSON:0` (no street-name false positive)
- [ ] `internal/config/config.go` — add `USAddress`, `USState`, `USZIP` to the `piiAllowedEntities` set so `PII_ENABLED_ENTITIES` validation accepts them
- [ ] `.planning/REQUIREMENTS.md` — introduce new `### PII — Recognizer coverage` section starting at `PII-01`: "US address PII (street, state, ZIP) is captured and tokenized in encrypt mode with byte-for-byte round-trip fidelity"; add Traceability row; coverage count 62 → 63
- [ ] `scripts/test-pii.ps1` + `scripts/test-pii.sh` — extend `$script:PIIPrompt` / `PII_PROMPT` with one address line; add three needles to `$script:PIIExpected` / `PII_EXPECT_*` for round-trip assertions

*If none of the above need work: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `test-pii.ps1 pii` exits 0 on the Windows splunk box with the new address fixture; address tokens decrypt byte-for-byte | PII-01 | Operator-only by design; tests gateway against real `kiro-cli` + Claude on production OS | 1. Pull v1.10.0 on the Windows box; `git log -1 --oneline` confirms 2. `make build` or unzip the v1.10.0 release 3. Existing PII_REDACTION_MODE=encrypt env stands 4. `.\scripts\otto-gw.ps1 restart` 5. `.\scripts\test-pii.ps1 pii` 6. Verify `0 check(s) failed` |
| `test-pii.sh pii` exits 0 on macOS / Linux dev box | PII-01 | POSIX sibling of the PowerShell smoke test | 1. From repo root: `make build` 2. Start gateway with same env 3. `./scripts/test-pii.sh pii` 4. Verify `0 check(s) failed` |
| With `PII_NER_ENABLED=true` opt-in, "1111 Main Street" does NOT get tagged PERSON in the redaction summary | PII-01 (overlap arbitration claim) | Requires live worker + log tail; covered indirectly by the integration test but operator-visible confirmation is the load-bearing acceptance | Operator sets `PII_NER_ENABLED=true` for one run; tails `pii.redact.done` summary log; confirms PERSON count is 0 for the street-name segment of the address fixture |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (recognizers.go entries, validator, registry test update, integration test, config allowlist, REQUIREMENTS.md entry, smoke fixture extension)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10 s (quick) / < 90 s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
