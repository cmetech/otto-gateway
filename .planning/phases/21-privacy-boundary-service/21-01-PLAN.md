---
phase: 21-privacy-boundary-service
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - PRIV-04
  - PRIV-05
  - PRIV-06
  - PRIV-07
  - PRIV-08
  - PRIV-09
  - PRIV-10
  - PRIV-11
  - PRIV-12
files_modified:
  - internal/config/
  - internal/privacy/
  - internal/plugin/
  - internal/engine/
  - internal/adapter/ollama/
  - internal/adapter/openai/
  - internal/adapter/anthropic/
  - internal/metrics/
  - internal/admin/
  - cmd/otto-gateway/
  - install/
  - scripts/
  - tests/
  - docs/
  - README.md
  - Makefile
must_haves:
  truths:
    - Standard privacy remains default-on and backward-compatible.
    - Strict privacy can raise but never lower the configured minimum profile.
    - Credentials are one-way and never enter the reversible ledger.
    - Only caller-input mappings may restore originals.
    - Mappings are bounded, scoped, memory-only, parallel-safe, inspectable only through protected triage, and explicitly clearable.
    - Compression precedes privacy and privacy is the final inbound content mutation.
    - Strict input blocks before worker dispatch.
    - Strict output is validated before any headers or body bytes.
    - All Ollama, OpenAI, and Anthropic routes share policy while preserving native wire formats.
    - Ordinary operational surfaces never expose mappings or protected values.
    - Operator tooling has POSIX and PowerShell parity and dashboard configuration remains read-only.
    - The complete conformance, race, security, benchmark, documentation, and release gates pass.
  artifacts:
    - path: internal/privacy/
      provides: Shared classifier, bounded scope store, technical aliases, service, receipts, and lifecycle enforcement
    - path: internal/plugin/pii/
      provides: PIIRedactionHook compatibility facade backed by the privacy service
    - path: tests/privacy/
      provides: Cross-surface privacy conformance and leakage gates
    - path: docs/superpowers/plans/2026-07-31-gateway-privacy-boundary-service.md
      provides: Authoritative 17-task sequence and verification contract
---

<objective>
Execute the approved OTTO Gateway Privacy Boundary Service implementation exactly as specified by the locked design and authoritative 17-task strict-TDD plan.

This GSD plan is a local milestone/phase tracking mirror only. If any wording here is less specific than the approved design or implementation plan, those approved documents control.
</objective>

<execution_context>
@CLAUDE.md
@docs/superpowers/specs/2026-07-31-privacy-boundary-service-design.md
@docs/superpowers/plans/2026-07-31-gateway-privacy-boundary-service.md
@.planning/STATE.md
@.planning/quick/260731-ghu-create-the-strict-tdd-implementation-pla/260731-ghu-SUMMARY.md
</execution_context>

<tdd_contract>
For every behavior change: write the focused failing test; run it and observe the intended RED; implement the smallest correct change; observe GREEN; refactor without behavior change; run the task's broader commands; run `git diff --check`; commit at the exact boundary named by the authoritative plan. An unexpectedly passing test must be corrected before implementation.
</tdd_contract>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Parse and validate the privacy configuration contract</name>
  <authority>Authoritative plan Task 1; follow every listed RED/GREEN/verification step and commit `feat(config): add privacy profile and capacity contract`.</authority>
  <verify>go test ./internal/config -run 'Privacy|PIINEREnabled_Default' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add the shared credential classifier</name>
  <authority>Authoritative plan Task 2; follow every listed step and commit `feat(privacy): add shared credential classifier`.</authority>
  <verify>go test ./internal/privacy -run 'Arbitrate|TransformStrings|SecretClassifier' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add the bounded scoped mapping store</name>
  <authority>Authoritative plan Task 3; follow every listed step and commit `feat(privacy): add bounded scoped mapping store`.</authority>
  <verify>go test -race ./internal/privacy -run 'ScopeStore' -count=20</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Add scoped technical pseudonyms</name>
  <authority>Authoritative plan Task 4; follow every listed step and commit `feat(privacy): add scoped technical pseudonyms`.</authority>
  <verify>go test -race ./internal/privacy ./internal/plugin/pii -run 'Technical|CIDR' -count=10</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: Route standard PII protection through the privacy service</name>
  <authority>Authoritative plan Task 5; preserve PIIRedactionHook and commit `refactor(privacy): route standard PII through service`.</authority>
  <verify>go test ./internal/privacy ./internal/plugin/pii -run 'StandardCompatibility|ServiceStandard|Actions|Context|Receipt|Error' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 6: Enforce the strict inbound boundary</name>
  <authority>Authoritative plan Task 6; follow every listed step and commit `feat(privacy): enforce strict inbound boundary`.</authority>
  <verify>go test ./internal/privacy ./internal/plugin/pii ./internal/engine -run 'ServiceStrict|PrivacyInputBlocked|PrivacyPanic' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 7: Enforce the strict outbound boundary</name>
  <authority>Authoritative plan Task 7; follow every listed step and commit `feat(privacy): enforce strict outbound boundary`.</authority>
  <verify>go test -race ./internal/privacy -run 'ServiceStrict|Receipt_NoValues' -count=50</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 8: Wire the service, hook order, and trace safety</name>
  <authority>Authoritative plan Task 8; follow every listed step and commit `feat(gateway): wire privacy service into hook chain`.</authority>
  <verify>go test ./cmd/otto-gateway ./internal/plugin -run 'PrivacyService|HookOrder|StrictRequiresHook|ChatTracePrivacy' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 9: Enforce the privacy response boundary on Ollama routes</name>
  <authority>Authoritative plan Task 9; follow every listed step and commit `feat(ollama): enforce privacy response boundary`.</authority>
  <verify>go test ./internal/adapter/ollama -run Privacy -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 10: Enforce the privacy response boundary on OpenAI routes</name>
  <authority>Authoritative plan Task 10; follow every listed step and commit `feat(openai): enforce privacy response boundary`.</authority>
  <verify>go test ./internal/adapter/openai -run Privacy -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 11: Enforce the privacy response boundary on Anthropic routes</name>
  <authority>Authoritative plan Task 11; follow every listed step and commit `feat(anthropic): enforce privacy response boundary`.</authority>
  <verify>go test ./internal/adapter/anthropic -run Privacy -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 12: Expose bounded privacy telemetry</name>
  <authority>Authoritative plan Task 12; follow every listed step and commit `feat(metrics): expose bounded privacy telemetry`.</authority>
  <verify>go test ./internal/metrics ./cmd/otto-gateway -run 'Privacy|MetricsNoLeak' -count=1</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 13: Add the protected privacy triage API</name>
  <authority>Authoritative plan Task 13; follow every listed step and commit `feat(admin): add protected privacy triage API`.</authority>
  <verify>go test -race ./internal/admin ./internal/privacy -run 'PrivacyTriage|Clear' -count=10</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 14: Manage secrets and inspect privacy state safely</name>
  <authority>Authoritative plan Task 14; follow every POSIX/PowerShell, CLI, and support-bundle step and commit `feat(cli): manage and inspect privacy state safely`.</authority>
  <verify>bash tests/install/privacy_secrets_posix_test.sh &amp;&amp; bash tests/scripts/test-privacy-cli.sh &amp;&amp; bash tests/scripts/test-support-bundle.sh</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 15: Show read-only privacy status</name>
  <authority>Authoritative plan Task 15; follow every listed step and commit `feat(admin): show read-only privacy status`.</authority>
  <verify>go test ./internal/admin ./cmd/otto-gateway -run 'PrivacySnapshot|PrivacyHealth|PrivacyPage|DashboardPrivacy|AboutPrivacy' -count=1 &amp;&amp; node --test internal/admin/admin_js_test.js</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 16: Publish privacy boundary operations documentation and Grafana assets</name>
  <authority>Authoritative plan Task 16; follow every listed step and commit `docs: publish privacy boundary operations guide`.</authority>
  <verify>python3 scripts/test_gen_grafana_dashboard.py &amp;&amp; python3 scripts/test_privacy_docs.py</verify>
</task>

<task type="tdd" tdd="true">
  <name>Task 17: Add cross-surface conformance and complete the release gate</name>
  <authority>Authoritative plan Task 17; follow every listed conformance, leakage, race, benchmark, cross-platform, and final release command and commit `test(privacy): add cross-surface release gates`.</authority>
  <verify>make test-privacy &amp;&amp; make test-privacy-race</verify>
</task>

</tasks>

<verification>
The exact per-task commands, cross-platform checks, benchmark invocations, leakage scans, full test/race suites, cross-compiles, and final release gate in Task 17 of the authoritative plan are mandatory. Record unavailable platform-only checks explicitly; do not silently omit them.
</verification>

<success_criteria>
- All 17 authoritative task commits exist at their planned atomic boundaries with observed RED/GREEN evidence.
- PRIV-01..12 are satisfied without reopening or weakening locked product decisions.
- Major code-review gates and whole-branch review have no unresolved blocking findings.
- The complete Task 17 release gate passes, with any genuinely unavailable platform-only check documented.
- The implementation worktree is clean and no sibling repository has been changed.
</success_criteria>
