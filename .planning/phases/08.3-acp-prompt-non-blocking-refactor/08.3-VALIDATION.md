---
phase: 08.3
slug: acp-prompt-non-blocking-refactor
status: executing
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-03
---

# Phase 08.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.23+) with `-race`; `go.uber.org/goleak` for goroutine leak detection |
| **Config file** | `internal/acp/testmain_test.go` (existing `goleak.VerifyTestMain`) |
| **Quick run command** | `go test -race -run TestIntegration_Prompt_OverflowsBuffer_DoesNotDeadlock ./internal/acp/...` |
| **Full suite command** | `make ci` (gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`) |
| **Estimated runtime** | ~90 s for `make ci`; ~5 s for the quick run |

---

## Sampling Rate

- **After every task commit:** Run `go test -race ./internal/acp/...`
- **After every plan wave:** Run `make ci`
- **Before `/gsd-verify-work`:** `make ci` must be green; Windows `scripts/test-pii.ps1 pii` must round-trip end-to-end
- **Max feedback latency:** ~10 s (quick) / ~90 s (full)

---

## Per-Task Verification Map

> Populated by the planner — one row per task with explicit `<automated>` verify command, plus the manual-only Windows PII round-trip.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.3-01-R | 01 | 1 | ACP-02, ACP-03 | T-08.3-01 | RED gate — Prompt MUST return within 100ms of c.send acceptance; 128 chunks before response must drain without deadlock; goleak clean | integration (fakeacp) | `go test -race -run TestIntegration_Prompt_OverflowsBuffer_DoesNotDeadlock ./internal/acp/... -timeout 30s` (expected NON-ZERO exit against synchronous Prompt; literal `Prompt did not return within 100ms` in failure output) | ✅ added | ✅ green |
| 08.3-01-G | 01 | 1 | ACP-02, ACP-03 | T-08.3-01, T-08.3-02 | GREEN gate — RED test now passes; awaitPromptResult goroutine owns response wait + finalizes via stream.close; engine.prompt.completed Debug fires with session_id/chunks/stop_reason; backward-compat mixed-variants test still passes | integration + unit | `go test -race ./internal/acp/... -timeout 120s` exits 0 (covers TestIntegration_Prompt_OverflowsBuffer_DoesNotDeadlock + TestIntegration_FakeACP_E2E_MixedVariants + goleak.VerifyTestMain) | ✅ exists | ✅ green |
| 08.3-01-F | 01 | 1 | ACP-02, ACP-03 | T-08.3-01, T-08.3-02, T-08.3-03 | REFACTOR gate — Stream docstring rewritten (no "callers MUST drain" / "Phase 1.1 WR-01"); three whitebox tests cover close-race, ctx-cancel + session/cancel wire emission, engine.prompt.completed field shape; deliberate-leak verification proves goleak catches orphaned goroutine | unit (whitebox) + full ci | `make ci` exits 0 (gofumpt → vet → build → golangci-lint → govulncheck → test -race) | ✅ added | ✅ green |
| 08.3-01-H | 01 | 1 | ACP-02, ACP-03 (regression) | — | Windows operator regression sign-off — scripts/test-pii.ps1 pii round-trips PII_REDACTION_MODE=encrypt through all three surfaces with no STREAM_IDLE_TIMEOUT 500 and no WARN acp: ping failed | manual (Windows operator) | `pwsh -File scripts/test-pii.ps1 pii` on Windows test box with kiro-cli.exe + Phase 8.3 build (see Plan 08.3-01 Task H `<how-to-verify>`) | ✅ exists | ⬜ pending operator |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/acp/integration_test.go` — host for new `TestIntegration_Prompt_OverflowsBuffer_DoesNotDeadlock` (if not already present)
- [ ] `goleak.VerifyTestMain` (existing at `internal/acp/testmain_test.go:18`) — confirm coverage of the new `awaitPromptResult` goroutine; no install needed
- [ ] `internal/acp/fakeacp_test.go` — confirm `promptSeen` / `emitPromptResult` / `emitUpdate` (lines 243-258) support emit-order separation; **no new helper expected** per RESEARCH.md

*If none of the above need work: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Windows v1.9.2 PII-encrypt round-trip succeeds end-to-end through `/v1/messages`, `/v1/chat/completions`, `/api/chat` with no `[PII:Entity:...]` ciphertext leaked and expected plaintext present | ACP-02, ACP-03 (regression) | Requires native `kiro-cli.exe` on Windows + a real `PII_REDACTION_MODE=encrypt` gateway; cannot run under `go test -race` on CI | 1. Build gateway: `make build` 2. Start gateway with `PII_REDACTION_MODE=encrypt` 3. Run `.\scripts\test-pii.ps1 pii` 4. Verify all three "expected plaintext present + no `[PII:Entity:...]` leaked" assertions pass |
| `engine.prompt.sent` log line fires within milliseconds of `c.send(...)` accepting the request (was previously emitted after full response) | (semantics-shift signal) | Requires a live worker + log scraping at debug level; covered indirectly by the overflow test but not asserted as a wall-clock metric | Tail gateway logs at `LOG_LEVEL=debug` during a real prompt; confirm `engine.prompt.sent` precedes `session/update` log lines |
| `engine.prompt.completed` carries `session_id`, `chunks`, `stop_reason` fields | (new log line) | Field-shape grep verification on real worker output | Tail gateway logs; grep for `engine.prompt.completed`; confirm all three fields present and `stop_reason` matches a `canonical.StopReason` string |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (fakeacp / goleak / integration test scaffold)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10 s (quick) / < 90 s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
