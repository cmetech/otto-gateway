---
status: partial
phase: 03.1-anthropic-surface
source: [03.1-VERIFICATION.md, 03.1-04-PLAN.md Task 3]
started: 2026-05-24T16:00:00Z
updated: 2026-05-24T16:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Boot gateway + sanity probes
expected: kiro-cli on PATH and authenticated; `make build && AUTH_TOKEN=test-token-123 KIRO_CMD=kiro-cli ENABLED_SURFACES=ollama,anthropic ./bin/loop24-gateway` starts; `curl -s http://localhost:11434/health | jq` returns 200 with pool stats; `curl -s -X POST http://localhost:11434/v1/messages` (no headers) returns 401.
result: [pending]

### 2. Non-streaming curl probe — x-api-key AND Authorization: Bearer (D-15 dual auth)
expected: `curl -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{"model":"auto","max_tokens":256,"stream":false,"messages":[{"role":"user","content":"say hi"}]}'` → 200 + Anthropic Message JSON with non-empty `content[0].text` + `stop_reason: "end_turn"`. Repeat with `Authorization: Bearer test-token-123` — same shape returned.
result: [pending]
note: Use canonical `Bearer` (capital B) — CR-02 means lowercase `bearer` will silently fall through to x-api-key fallback. Fix tracked separately.

### 3. Streaming curl probe — raw SSE byte inspection against Anthropic spec
expected: `curl -N -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{"model":"auto","max_tokens":256,"stream":true,"messages":[{"role":"user","content":"say hi"}]}'`. Response includes `Content-Type: text/event-stream`. Body shows `event: message_start\ndata: {...}\n\n` then several `event: content_block_*\ndata: {...}\n\n` frames, finishing with `event: message_stop\ndata: {"type":"message_stop"}\n\n`. Visually compare framing to https://docs.anthropic.com/en/api/messages-streaming — no extra whitespace, no missing newlines.
result: [pending]

### 4. loop24-client non-streaming round-trip with ANTHROPIC_BASE_URL (load-bearing ANTH-07 + ANTH-04)
expected: From `~/Projects/repos/local/loop24-client`, set `ANTHROPIC_BASE_URL=http://localhost:11434` + `ANTHROPIC_API_KEY=test-token-123` and invoke `a.messages.create({model:"auto",max_tokens:256,messages:[{role:"user",content:"hi"}]})`. SDK returns a Message object with non-empty `content[0].text`. No SDK schema-validation exception. No parse exception.
result: [pending]
note: THIS IS THE LOAD-BEARING ANTH-07 / ANTH-04 ACCEPTANCE BAR. If CR-01 (tool_use.input omitempty drop) trips during a tool-bearing response, parse exception will surface here.

### 5. loop24-client streaming round-trip with ANTHROPIC_BASE_URL (load-bearing ANTH-07 streaming)
expected: Same env. Invoke `a.messages.stream({...})` and consume: `for await (const event of stream) console.log(event.type); const final = await stream.finalMessage();`. Prints event sequence: message_start → content_block_start → content_block_delta (1+) → content_block_stop → message_delta → message_stop. Final message has non-empty `content[0].text`. No SDK errors.
result: [pending]

### 6. Surface gating cross-check — ENABLED_SURFACES=ollama + ENABLED_SURFACES=anthrpic typo
expected: Stop gateway, restart with `ENABLED_SURFACES=ollama ./bin/loop24-gateway`. `curl -i -X POST http://localhost:11434/v1/messages …` returns 404; `curl -i -X POST http://localhost:11434/api/chat …` works. Stop, restart with `ENABLED_SURFACES=anthrpic` (typo) — gateway EXITS NON-ZERO at boot with a clear error message naming `anthrpic`.
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps

(filled in as steps fail)

## Known code-review defects (independent of UAT — tracked in 03.1-REVIEW.md)

- **CR-01 (BLOCKER)**: `internal/adapter/anthropic/render.go:48` — `Input` field uses `omitempty`, drops `tool_use.input` from wire when nil/empty. Anthropic spec requires `{}`. May trip Test 4 if response contains a tool_use block.
- **CR-02 (BLOCKER)**: `internal/auth/bearer.go:79` — case-sensitive `Bearer ` prefix match violates RFC 7235 §2.1. Lowercase clients silently fall through.
- **CR-03 (WARNING)**: `internal/adapter/anthropic/handlers.go:92-102` — SSE post-header errors logged at Debug; operator visibility issue.
