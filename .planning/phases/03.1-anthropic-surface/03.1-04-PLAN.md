---
phase: 03.1
plan: 04
type: execute
wave: 4
depends_on: ["03.1-03"]
files_modified:
  - cmd/loop24-gateway/main.go
  - cmd/loop24-gateway/main_test.go
  - internal/adapter/anthropic/integration_test.go
autonomous: false
requirements: [ANTH-04, SURF-08]
must_haves:
  truths:
    - "cmd/loop24-gateway/main.go gates the anthropic adapter construction on slices.Contains(cfg.EnabledSurfaces, \"anthropic\")"
    - "Both surfaces wire through server.NewFromConfig with parallel mount blocks"
    - "Real-kiro integration test exercises POST /v1/messages stream:false + stream:true paths through actual kiro-cli subprocess"
    - "Real-kiro integration test sends a follow-up turn whose conversation history contains a redacted_thinking block from the previous response; asserts kiro-cli does not error (verifies D-13 drop-at-wire-decode is safe for kiro-cli)"
    - "Real-kiro streaming integration test asserts strict SSE byte framing: every 'event: <name>\\n' line is followed by exactly one 'data: {...}\\n' line followed by a blank '\\n' line; any deviation fails the test"
    - "Gateway boots with default ENABLED_SURFACES (ollama,anthropic) and serves both /api/chat and /v1/messages"
    - "Setting ENABLED_SURFACES=ollama disables /v1/messages at boot (returns 404)"
    - "Surface gating is covered by an AUTOMATED test (TestNewApp_SurfaceGating) that probes route presence with three env permutations (default, ollama-only, anthropic-only) via httptest seam — not just HUMAN-UAT"
    - "loop24-client with ANTHROPIC_BASE_URL=http://localhost:11434 completes both messages.create({stream:false}) and messages.stream({...}) round-trips against the gateway with zero client changes (HUMAN-UAT)"
  artifacts:
    - path: "cmd/loop24-gateway/main.go"
      provides: "Conditional adapter construction + server.NewFromConfig with both protected routers; exported/testable newApp seam"
      contains: "anthropic.New"
    - path: "cmd/loop24-gateway/main_test.go"
      provides: "TestNewApp_SurfaceGating — automated surface-gating test using httptest server probes"
      contains: "TestNewApp_SurfaceGating"
    - path: "internal/adapter/anthropic/integration_test.go"
      provides: "Real-kiro round-trip integration test for both streaming and non-streaming paths + redacted_thinking follow-up turn + strict SSE framing assertions"
      contains: "TestIntegration_RealKiroCLI"
  key_links:
    - from: "cmd/loop24-gateway/main.go"
      to: "internal/adapter/anthropic.New"
      via: "anthropicAdapter = anthropic.New(anthropic.Config{Logger, Engine: engineForAdapter})"
      pattern: "anthropic.New"
    - from: "cmd/loop24-gateway/main_test.go"
      to: "cmd/loop24-gateway/main.go newApp"
      via: "main_test calls config.Load after t.Setenv then newApp(ctx, cfg, logger); probes /v1/messages and /api/chat via a.srv.ServeHTTP"
      pattern: "TestNewApp_SurfaceGating"
    - from: "internal/adapter/anthropic/integration_test.go"
      to: "real kiro-cli + engine + pool"
      via: "resolveKiroCLI + pool.Warmup + engine.New + anthropic.New"
      pattern: "TestIntegration_RealKiroCLI"
---

<objective>
Final integration plan — wire the Anthropic adapter into `cmd/loop24-gateway/main.go` with `ENABLED_SURFACES` gating, add an AUTOMATED surface-gating test (Task 1) that probes route presence under three env permutations (closes B3 — no longer relies on HUMAN-UAT alone for surface gating verification), add a real-kiro integration test exercising both streaming and non-streaming paths (Task 2) with a `redacted_thinking` follow-up turn verifying D-13 drop-at-wire-decode is safe and with strict SSE byte-framing assertions, and provide a HUMAN-UAT checkpoint (Task 3) to verify loop24-client (GSD Pi CLI) round-trips against the live gateway with `ANTHROPIC_BASE_URL=http://localhost:11434`. This is the Phase 3.1 acceptance gate.

Purpose: All prior plans built the pieces; Plan 04 makes the binary actually serve `/v1/messages` and proves the dominant client (loop24-client + `@anthropic-ai/sdk@^0.90`) works against it. Without this plan, the adapter exists but isn't reachable. Without the automated surface-gating test, regressions in `cmd/main.go` would only surface in HUMAN-UAT.

Output: 1 modified file (cmd/main.go) + 1 new main_test.go + 1 new integration test + 1 HUMAN-UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-anthropic-surface/03.1-CONTEXT.md
@.planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md
@.planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md
@.planning/phases/03.1-anthropic-surface/03.1-VALIDATION.md
@.planning/phases/03.1-anthropic-surface/03.1-01-PLAN.md
@.planning/phases/03.1-anthropic-surface/03.1-02-PLAN.md
@.planning/phases/03.1-anthropic-surface/03.1-03-PLAN.md
@CLAUDE.md
@cmd/loop24-gateway/main.go
@cmd/loop24-gateway/main_test.go
@internal/adapter/ollama/integration_test.go

<interfaces>
<!-- Source-of-truth identifiers from prior plans + LIVE codebase. -->

From cmd/loop24-gateway/main.go (LIVE — Phase 2 + Plan 01 state, verified):
- `newApp(ctx context.Context, cfg config.Config, logger *slog.Logger) (*app, func(), error)` — THREE args, THREE returns. Cleanup is a returned `func()` closure (NOT a method on *app). There is NO `Shutdown` or `Close` method on `*app`.
- `*app` struct fields: `cfg config.Config, logger *slog.Logger, pool *pool.Pool, engine *engine.Engine, srv *server.Server`.
- `cfg.OllamaPathPrefix` already used for mounting.
- Plan 01 added `cfg.EnabledSurfaces []string` and `cfg.AnthropicPathPrefix string` to `config.Config`.

From cmd/loop24-gateway/main_test.go (LIVE — DO NOT MODIFY these existing tests):
- `TestApp_NoKiroCmd_StartsHealthOnly` (lines 18-51): builds a `config.Config` literal with `KiroCmd: ""`, calls `newApp(ctx, cfg, logger)`, asserts `a.pool == nil && a.engine == nil`, probes `/health` via `a.srv.ServeHTTP(w, r)`.
- `TestApp_WarmupBeforeListen` (lines 58-89): proves that `KIRO_CMD` set to a non-ACP binary like `/usr/bin/true` causes `newApp` to return a non-nil error and a nil `*app`. Surface-gating tests MUST NOT trigger this code path.

From internal/config/config.go:
- `config.Load() (Config, error)` reads ALL env vars at call time. To exercise env permutations in tests, `t.Setenv` MUST be called BEFORE `config.Load()`; passing a pre-built Config literal bypasses env entirely.

From internal/server/server.go:
- `*server.Server` already implements `http.Handler` via `func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request)` (line 170). Tests can call `a.srv.ServeHTTP(w, r)` directly OR mount via `httptest.NewServer(a.srv)`. NO new `Handler()` accessor is needed.

From internal/adapter/anthropic (Plans 02-03):
- `anthropic.New(anthropic.Config{Logger, Engine})` returns `*anthropic.Adapter`.
- `*anthropic.Adapter.ProtectedRouter() chi.Router`.

From internal/server/server.go (modified by Plan 01):
- `server.Config` carries `AnthropicPath string` + `AnthropicProtectedRouter chi.Router` parallel to the existing Ollama mount fields.

From internal/adapter/ollama/integration_test.go (TEMPLATE):
- `resolveKiroCLI(t)` — gates on `LOOP24_INTEGRATION=1` and `LOOP24_KIRO_BIN` or `kiro-cli` on PATH (Phase 1.1 D-17 pattern).
- Pool warmup with timeout, engine + adapter construction, real ServeHTTP probe.

loop24-client (HUMAN-UAT):
- Located at `~/Projects/repos/local/loop24-client/` per RESEARCH.md.
- Set `ANTHROPIC_BASE_URL=http://localhost:11434` to redirect Anthropic SDK calls through the gateway.
- Two call paths to verify: `messages.create({stream:false})` and `messages.stream({...})`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire anthropic adapter into cmd/loop24-gateway/main.go with EnabledSurfaces gating + AUTOMATED surface-gating test</name>
  <files>cmd/loop24-gateway/main.go, cmd/loop24-gateway/main_test.go</files>
  <read_first>
    - cmd/loop24-gateway/main.go (full file — focus on `newApp` lines ~99-178 per PATTERNS.md; live signature is `(ctx, cfg, logger) → (*app, func(), error)`)
    - cmd/loop24-gateway/main_test.go (existing `TestApp_NoKiroCmd_StartsHealthOnly` lines 18-51 and `TestApp_WarmupBeforeListen` lines 58-89 — DO NOT MODIFY; mirror their newApp call pattern in the new test)
    - internal/config/config.go (full file — confirm `Load()` reads env at call time, so `t.Setenv` MUST precede `config.Load()`)
    - internal/server/server.go (line 168-172 — confirm `*server.Server` already has `ServeHTTP`; no new accessor needed)
    - internal/adapter/anthropic/adapter.go (Plan 02 — anthropic.New, anthropic.Config shape)
    - internal/server/server.go (Plan 01 — AnthropicPath + AnthropicProtectedRouter Config fields)
    - .planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md §`cmd/loop24-gateway/main.go` (lines ~1516-1592)
    - .planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md §Pattern 4 Surface Gating (lines 570-608)
    - .planning/phases/03.1-anthropic-surface/03.1-CONTEXT.md §code_context Integration Points (lines ~540-582)
    - .planning/phases/03.1-anthropic-surface/03.1-02-PLAN.md Task 3 line 351 (confirms nil-engine Anthropic handler returns 503, not 404 — so a degraded-mode route probe distinguishes "route mounted but engine nil" from "route absent")
  </read_first>
  <behavior>
    - With `ENABLED_SURFACES` unset (default `ollama,anthropic`): gateway starts, both `/api/chat` and `/v1/messages` are mounted and respond (200 with valid auth, 401 without).
    - With `ENABLED_SURFACES=ollama`: gateway starts, `/api/chat` mounted, `/v1/messages` returns 404 (no route).
    - With `ENABLED_SURFACES=anthropic`: gateway starts, `/v1/messages` mounted, `/api/chat` returns 404.
    - With `ENABLED_SURFACES=anthrpic` (typo): gateway EXITS NON-ZERO at startup with a clear error message naming `anthrpic` (D-16 fail-fast).
    - With `ANTHROPIC_PATH_PREFIX=/anthropic/v1` and default `ENABLED_SURFACES`: `/anthropic/v1/messages` mounted, `/v1/messages` returns 404.
    - The anthropic adapter is constructed BEFORE `server.NewFromConfig`; its `ProtectedRouter()` is passed via `server.Config.AnthropicProtectedRouter`. When the adapter is nil (surface disabled), the field is nil and the server.go mount block skips it.
    - Engine handle is SHARED between ollama and anthropic adapters (same `*engine.Engine` instance — both adapters consume the same canonical engine + pool).
    - **AUTOMATED surface-gating test (`TestNewApp_SurfaceGating` — B3):** the test runs the gateway in **degraded mode** (`KIRO_CMD=""`) so pool and engine are nil and warmup is skipped entirely (mirrors `TestApp_NoKiroCmd_StartsHealthOnly`). In degraded mode the adapters still mount their routes; Anthropic's nil-engine guard (Plan 02 Task 3 line 351) returns 503 with an `api_error` envelope, and Ollama's chat handler returns 503 the same way. The test distinguishes "route mounted" (non-404) from "route absent" (404). Three sub-tests via `t.Run`:
      - **Default** (`ENABLED_SURFACES` unset → resolves to `ollama,anthropic`): probe `GET /api/chat` → expect non-404; probe `GET /v1/messages` → expect non-404.
      - **OllamaOnly** (`t.Setenv("ENABLED_SURFACES", "ollama")`): probe `GET /api/chat` → expect non-404; probe `GET /v1/messages` → expect 404.
      - **AnthropicOnly** (`t.Setenv("ENABLED_SURFACES", "anthropic")`): probe `GET /api/chat` → expect 404; probe `GET /v1/messages` → expect non-404.
    - The test MUST NOT modify `TestApp_NoKiroCmd_StartsHealthOnly` or `TestApp_WarmupBeforeListen`; both must continue to pass after this task lands.
  </behavior>
  <action>
    Modify `cmd/loop24-gateway/main.go` in `newApp()` per PATTERNS.md §main.go (lines 1516-1592) and RESEARCH.md §Pattern 4 Surface Gating. Preserve the LIVE signature exactly — do NOT change the three-arg three-return shape, do NOT add `Handler()` or `Shutdown()` or `Close()` methods to `*app`:

    1. Import `loop24-gateway/internal/adapter/anthropic` (Go module path matches the existing `loop24-gateway/internal/adapter/ollama` style).
    2. Import `slices` from the stdlib if not already imported.
    3. After engine construction (existing `a.engine = engine.New(...)`), gate adapter construction on `cfg.EnabledSurfaces`:
       - Replace the existing unconditional `adapter := ollama.New(ollama.Config{...})` with `var ollamaAdapter *ollama.Adapter` and `if slices.Contains(cfg.EnabledSurfaces, "ollama") { ollamaAdapter = ollama.New(...) }`.
       - Add `var anthropicAdapter *anthropic.Adapter` and `if slices.Contains(cfg.EnabledSurfaces, "anthropic") { anthropicAdapter = anthropic.New(anthropic.Config{Logger: logger, Engine: engineForAdapter}) }`. Reuse the SAME `engineForAdapter` variable (the canonical engine handle shared across surfaces). In degraded mode (`a.engine == nil`), `engineForAdapter` stays its zero-value interface (nil) — Plan 02's nil-engine guard handles the resulting 503.
    4. Compose the protected-router accessors with nil-safety:
       - `var ollamaProtectedRouter, anthropicProtectedRouter chi.Router; var ollamaVersionHandler http.HandlerFunc`
       - `if ollamaAdapter != nil { ollamaProtectedRouter = ollamaAdapter.ProtectedRouter(); ollamaVersionHandler = ollamaAdapter.HandleVersion() }`
       - `if anthropicAdapter != nil { anthropicProtectedRouter = anthropicAdapter.ProtectedRouter() }`
    5. Pass both to `server.NewFromConfig`:
       - Existing fields: `OllamaPath: cfg.OllamaPathPrefix, OllamaProtectedRouter: ollamaProtectedRouter, OllamaVersionHandler: ollamaVersionHandler`.
       - NEW fields: `AnthropicPath: cfg.AnthropicPathPrefix, AnthropicProtectedRouter: anthropicProtectedRouter`.
    6. Update structured-log boot message to include `slog.Any("enabled_surfaces", cfg.EnabledSurfaces)` so operators see the surface set at startup.
    7. If both adapters are nil (e.g., `ENABLED_SURFACES=` resolved to empty after validation — should not happen but defensive), still start the server with `/`, `/health`, `/api/version` exempt routes alive. Log a warning. Do NOT exit non-zero (that's `config.Load`'s job for unknown names; an empty enabled list is a separate operator decision).

    **Do NOT introduce a new `Handler()` accessor or `Shutdown()`/`Close()` method on `*app`.** The existing `*server.Server` already implements `http.Handler` via `ServeHTTP` (server.go:170-172); tests probe routes through `a.srv.ServeHTTP(w, r)` or `httptest.NewServer(a.srv)` directly. Cleanup happens via the `func()` closure that `newApp` returns (live signature).

    Create `cmd/loop24-gateway/main_test.go` ADDITIONS (`package main`, same file as existing tests — append `TestNewApp_SurfaceGating` and do NOT touch existing tests):

    `TestNewApp_SurfaceGating(t *testing.T)`:
    - Define a `subtests := []struct{ name, enabledSurfaces string; expectOllamaRoute, expectAnthropicRoute bool }` table:
      - `{"Default", "", true, true}` — empty string sentinel meaning "do not set ENABLED_SURFACES" (env unset → config default).
      - `{"OllamaOnly", "ollama", true, false}`
      - `{"AnthropicOnly", "anthropic", false, true}`
    - For each sub-test, inside `t.Run(tc.name, func(t *testing.T){...})`:
      - `t.Setenv("KIRO_CMD", "")` — explicit degraded mode. Mirrors `TestApp_NoKiroCmd_StartsHealthOnly`. This skips pool construction AND warmup, so `KIRO_CMD=/nonexistent` (which would trigger fatal warmup) is NOT used.
      - `t.Setenv("HTTP_ADDR", ":0")` — defensive; the test never actually listens, but keeps env consistent.
      - `t.Setenv("AUTH_TOKEN", "")` — no-auth mode so route probes are not blocked by 401 before reaching the chi router's 404 handler.
      - If `tc.enabledSurfaces != ""`, call `t.Setenv("ENABLED_SURFACES", tc.enabledSurfaces)`. Otherwise leave the env unset to exercise the default.
      - `cfg, err := config.Load(); if err != nil { t.Fatalf("config.Load: %v", err) }` — MUST be after the `t.Setenv` calls so env changes are observed.
      - `logger := testutil.Logger(t)` (reuse the existing helper used by the live tests).
      - `a, cleanup, err := newApp(context.Background(), cfg, logger)` — matches the live three-return signature exactly.
      - `if err != nil { t.Fatalf("newApp: %v", err) }`
      - `defer cleanup()`
      - For each `(path, mountExpected)` in `{"/api/chat": tc.expectOllamaRoute, "/v1/messages": tc.expectAnthropicRoute}`:
        - Build `req := httptest.NewRequestWithContext(context.Background(), http.MethodGet, path, nil)`.
        - `w := httptest.NewRecorder()`.
        - `a.srv.ServeHTTP(w, req)` — uses the existing `(*server.Server).ServeHTTP` method (server.go:170); NO new accessor required.
        - If `mountExpected`: assert `w.Code != http.StatusNotFound` (any non-404 — 503 from nil-engine guard, 405 from method-not-allowed on GET, etc. — proves the route is mounted). Log the actual status on failure for diagnostics.
        - If `!mountExpected`: assert `w.Code == http.StatusNotFound` (chi router returns 404 when no route matches the path).
    - Use `GET` (not `POST`) for the probes so the test does not need to construct a valid JSON body; the chi router's 404 decision is path-based and route-pattern-based, not method-based for our purposes (a mounted POST-only route still returns 405 for GET, which is non-404 and proves "route mounted").

    Optional bonus (I12 if trivial): add `TestNewApp_LogsEnabledSurfaces(t *testing.T)` that captures the logger output via a custom slog handler and asserts the boot log line includes both surface names when the default is in effect. Skip if it adds non-trivial complexity.
  </action>
  <verify>
    <automated>go build ./cmd/loop24-gateway/ && go test ./cmd/... -race -count=1 -v -run 'TestNewApp_SurfaceGating' 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `cmd/loop24-gateway/main.go` compiles with `anthropic` and `slices` imports; `newApp` retains its LIVE signature `(ctx, cfg, logger) → (*app, func(), error)`.
    - `cmd/loop24-gateway/main_test.go` contains `TestNewApp_SurfaceGating` covering three env permutations via `t.Setenv` → `config.Load` → `newApp` → `a.srv.ServeHTTP` flow.
    - Existing `TestApp_NoKiroCmd_StartsHealthOnly` and `TestApp_WarmupBeforeListen` are unchanged and still pass.
    - `go build ./cmd/loop24-gateway/` produces a working binary.
    - `go test ./cmd/... -race -count=1 -v -run 'TestNewApp_SurfaceGating'` passes all three sub-tests.
    - `go vet ./cmd/loop24-gateway/`, `golangci-lint run`, `go-arch-lint check` all green.
    - Manual smoke: start the gateway with `ENABLED_SURFACES=ollama,anthropic` and curl `GET /health` returns 200; the log line includes both surfaces.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real-kiro integration test for /v1/messages (non-streaming + streaming + redacted_thinking follow-up turn + strict SSE framing)</name>
  <files>internal/adapter/anthropic/integration_test.go</files>
  <read_first>
    - internal/adapter/ollama/integration_test.go (FULL FILE — template for resolveKiroCLI, pool warmup, engine construction)
    - internal/adapter/anthropic/adapter.go (Plan 02 — Adapter, Config shapes)
    - internal/adapter/anthropic/handlers.go (Plan 02 + 03 — handleMessages)
    - internal/adapter/anthropic/wire.go (Plan 02 — redacted_thinking drop at wire-decode; D-13)
    - internal/pool/ (Phase 2 — pool.New, pool.Config, Warmup, Models methods)
    - .planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md §`internal/adapter/anthropic/integration_test.go` (lines ~995-1073)
    - .planning/phases/03.1-anthropic-surface/03.1-VALIDATION.md Per-Task Verification Map row 3.1-04-T2
  </read_first>
  <behavior>
    - Test (real kiro non-streaming): with kiro-cli available + `LOOP24_INTEGRATION=1`, POST `/v1/messages` with `{"model":"auto","max_tokens":256,"messages":[{"role":"user","content":"say hi"}],"stream":false}` returns 200 + Anthropic JSON with non-empty `content[0].text`, `type:"message"`, `role:"assistant"`, `stop_reason` non-nil.
    - Test (real kiro streaming): with the same auth, POST with `stream:true` returns Content-Type `text/event-stream`; reading the body line-by-line yields at least one `event: message_start`, one or more `event: content_block_delta` with `text_delta`, and a final `event: message_stop`.
    - **STRICT SSE BYTE FRAMING (W10):** the streaming test enforces line-by-line that EVERY `event: <name>\n` line is IMMEDIATELY followed by exactly one `data: {...}\n` line, IMMEDIATELY followed by a blank `\n` line. Any deviation (extra space, missing newline, two `data:` lines for one `event:`, blank line missing between frames) fails the test with a clear diff showing the offending line index. The scanner walks the response body and tracks state `expectingEvent → expectingData → expectingBlank → expectingEvent`; any wrong transition is a fail.
    - **REDACTED_THINKING FOLLOW-UP TURN (W5 part 2):** with the same setup, send a SECOND POST after the first non-streaming response succeeds. The second request's `messages` array contains the original user message PLUS the assistant's response (reconstructed with a synthetic `redacted_thinking` content block prepended — e.g., `[{"role":"user","content":"say hi"},{"role":"assistant","content":[{"type":"redacted_thinking","data":"<opaque-blob>"},{"type":"text","text":"<assistant-response>"}]},{"role":"user","content":"say bye"}]`). Assert the second response is 200 + valid Anthropic JSON (kiro-cli did NOT error on the conversation history containing redacted_thinking — verifies D-13 drop-at-wire-decode produces a kiro-cli-safe transcript). Use a clearly-fake opaque blob like `"data":"REDACTED_TEST_BLOB"` since the actual redacted-thinking format is opaque.
    - Test (skip when kiro-cli unavailable): without `LOOP24_INTEGRATION=1` or without kiro-cli on PATH (and no `LOOP24_KIRO_BIN`), the test calls `t.Skip(...)` with a clear reason. No false failures in normal CI.
    - Test (goroutine hygiene): `goleak.VerifyNone(t)` at test end verifies no pool/engine/SSE goroutines leak. Pool is closed via `defer p.Close()`.
  </behavior>
  <action>
    Create `internal/adapter/anthropic/integration_test.go` mirroring `internal/adapter/ollama/integration_test.go` (PATTERNS.md §integration_test.go lines 995-1073). Use whitebox `package anthropic`.

    1. Copy the `resolveKiroCLI(t *testing.T) string` helper VERBATIM from Ollama's integration_test.go (Phase 1.1 D-17 pattern — checks `LOOP24_KIRO_BIN` first, then `LOOP24_INTEGRATION=1` + `kiro-cli` on PATH).

    2. `TestIntegration_RealKiroCLI_NonStreaming(t *testing.T)`:
       - `bin := resolveKiroCLI(t)`.
       - Construct logger via `testutil.Logger(t)`.
       - Construct pool: `p := pool.New(pool.Config{Logger, Size: 1, KiroCmd: bin, KiroArgs: []string{"acp"}, PingInterval: 10*time.Minute})`. `defer p.Close()`.
       - `warmCtx, cancelWarm := context.WithTimeout(ctx, 30*time.Second); defer cancelWarm()`.
       - `if err := p.Warmup(warmCtx); err != nil { t.Skipf("pool.Warmup failed: %v", err) }`.
       - `eng := engine.New(engine.Config{Logger, ACP: p})`.
       - `a := New(Config{Logger, Engine: eng})`.
       - Build `httptest.NewServer(a.ProtectedRouter())` or use `httptest.NewRequest`+`a.ProtectedRouter().ServeHTTP(w, r)` directly.
       - POST `/messages` with `anthropic-version: 2023-06-01`, `Content-Type: application/json`, body `{"model":"auto","max_tokens":128,"stream":false,"messages":[{"role":"user","content":"say the word ping"}]}`.
       - Assert response status 200, content-type application/json, body decodes into a struct with non-empty `Content[0].Text`.
       - Assert `StopReason` is non-nil.
       - Capture the assistant response text into a variable for the redacted_thinking follow-up sub-test.

    3. `TestIntegration_RealKiroCLI_Streaming(t *testing.T)`:
       - Same setup as above.
       - POST `/messages` with `stream:true`.
       - **STRICT FRAMING ASSERTION (W10):** read the response body with `bufio.Scanner` line-by-line. Maintain a state machine over the lines:
         - State `expectingEvent`: line MUST start with `event: ` and have a known event-name suffix (`message_start`, `content_block_start`, `content_block_delta`, `content_block_stop`, `message_delta`, `message_stop`, `ping`, `error`). Transition to `expectingData`.
         - State `expectingData`: line MUST start with `data: ` and the remainder MUST be valid JSON. Transition to `expectingBlank`.
         - State `expectingBlank`: line MUST be empty string. Transition back to `expectingEvent`.
         - Any deviation fails with `t.Fatalf("strict framing violation at line %d: state=%s, got=%q", lineIdx, state, line)`.
       - Accumulate event names into a slice; assert first event is `message_start`; at least one event is `content_block_start` for a text block; at least one event is `content_block_delta` with `delta.type == "text_delta"`; final event is `message_stop`.
       - Separately assert that NO event in the captured sequence is `error`. (Note: the framing state machine treats `error` as a syntactically valid event name to enforce strict envelope shape on it if it ever appears, but a happy-path "say hi" prompt should never produce one — its presence indicates a kiro-cli or adapter regression.)

    4. `TestIntegration_RealKiroCLI_RedactedThinkingFollowUp(t *testing.T)` (W5 part 2):
       - Same setup as NonStreaming.
       - FIRST POST: same as NonStreaming — capture `firstResponseText`.
       - SECOND POST: body `{"model":"auto","max_tokens":128,"stream":false,"messages":[{"role":"user","content":"say hi"},{"role":"assistant","content":[{"type":"redacted_thinking","data":"REDACTED_TEST_BLOB_OPAQUE"},{"type":"text","text":"<firstResponseText>"}]},{"role":"user","content":"say bye"}]}` (interpolate `firstResponseText` into the assistant message).
       - Assert: response status 200 (NOT 500/400 — kiro-cli did NOT error on the redacted_thinking block in history because wire.go drops it at decode per D-13).
       - Assert: body has non-empty `content[0].text` (kiro-cli produced a response to the second user turn).
       - This test is the load-bearing verification for D-13. If it fails, D-13's drop-at-wire-decode policy is wrong and needs revisiting.

    5. Set a per-test timeout via `ctx, cancel := context.WithTimeout(ctx, 60*time.Second); defer cancel()` so a hung kiro-cli doesn't stall CI indefinitely.

    6. Wire `goleak.VerifyNone(t)` in `t.Cleanup`. Pool's Close + engine teardown should be clean.

    For the streaming test, account for kiro-cli's actual output: a real session may emit `agent_message_chunk` (ChunkKindText) chunks plus possibly `agent_thought_chunk` (ChunkKindThought) on some prompts. The test should be tolerant of either-or; just assert that ChunkKindText shows up at least once (text_delta in the SSE) and the sequence closes cleanly with message_stop — while ALSO enforcing the strict line-by-line framing state machine above.
  </action>
  <verify>
    <automated>LOOP24_INTEGRATION=0 go test ./internal/adapter/anthropic/ -race -count=1 -v -run TestIntegration 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `internal/adapter/anthropic/integration_test.go` exists with three test cases: NonStreaming, Streaming (with strict framing), RedactedThinkingFollowUp.
    - With `LOOP24_INTEGRATION=0` (CI default), all three tests SKIP cleanly with a clear reason.
    - With `LOOP24_INTEGRATION=1 + kiro-cli` available, all three tests PASS against real kiro-cli.
    - Streaming test's strict framing state machine catches any byte-level framing deviation.
    - RedactedThinkingFollowUp verifies D-13 drop-at-wire-decode is safe for kiro-cli (W5 part 2).
    - `goleak` reports no leaks at end of suite.
    - The test file imports only `package anthropic` testing-internal symbols + stdlib + internal/canonical + internal/engine + internal/pool + internal/testutil. (Note: integration_test.go is allowed to import internal/engine because it's a test file; `.go-arch-lint.yml` excludes `_test.go` files.)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN-UAT: loop24-client round-trip against live gateway with real kiro-cli</name>
  <files>cmd/loop24-gateway/main.go, internal/adapter/anthropic/, scripts/start.sh, scripts/start.ps1</files>
  <what-built>
    Plans 01-04 deliver:
    - `internal/adapter/anthropic/` — new adapter package serving `POST /v1/messages` (non-streaming + SSE streaming) via the same canonical engine that Phase 2 wired for Ollama.
    - Dual-auth (Authorization: Bearer + x-api-key) middleware support (D-15).
    - `ENABLED_SURFACES` (default `ollama,anthropic`) + `ANTHROPIC_PATH_PREFIX` (default `/v1`) env vars wired through `config.Load` + `server.NewFromConfig` + `cmd/main.go`.
    - Full SSE event sequence emission (`message_start` → content_block_start/delta/stop+ → `message_delta` → `message_stop`) with 15s ping keepalive.
    - Real-kiro integration tests for both streaming and non-streaming paths, plus a redacted_thinking follow-up turn (verifies D-13 drop-at-wire-decode is kiro-cli-safe) and strict SSE byte-framing assertions.
    - AUTOMATED surface-gating test (`TestNewApp_SurfaceGating`) covers route presence under three env permutations.

    The remaining check is whether the dominant client — loop24-client (`~/Projects/repos/local/loop24-client`) using `@anthropic-ai/sdk@^0.90.x` — completes a round-trip through the gateway with `ANTHROPIC_BASE_URL=http://localhost:11434` and zero code changes to loop24-client. This is the ANTH-07 acceptance bar from CONTEXT.md §`<specifics>`.
  </what-built>
  <how-to-verify>
    1. **Boot the gateway:**
       - Verify `kiro-cli` is on PATH and authenticated (run `kiro-cli acp` once interactively to confirm it boots).
       - From `/Users/coreyellis/Projects/repos/local/loop24-gateway`, run: `make build && AUTH_TOKEN=test-token-123 KIRO_CMD=kiro-cli ENABLED_SURFACES=ollama,anthropic ./bin/loop24-gateway` (use the wrapper scripts if preferred — `scripts/start.sh` or `scripts/start.ps1`).
       - In a second terminal, smoke-check: `curl -s http://localhost:11434/health | jq` should return 200 with pool/registry stats. `curl -s -X POST http://localhost:11434/v1/messages` (no headers) should return 401 (auth required).

    2. **Non-streaming curl probe (sanity before invoking loop24-client):**
       - `curl -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{"model":"auto","max_tokens":256,"stream":false,"messages":[{"role":"user","content":"say hi"}]}'`.
       - Expected: 200 OK; body shape `{"id":"msg_01...","type":"message","role":"assistant","model":"auto","content":[{"type":"text","text":"<non-empty>"}],"stop_reason":"end_turn","stop_sequence":null,"usage":{"input_tokens":0,"output_tokens":0}}`.
       - Also try with `Authorization: Bearer test-token-123` instead of `x-api-key` — same response (D-15 dual auth).

    3. **Streaming curl probe (raw SSE byte inspection):**
       - `curl -N -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{"model":"auto","max_tokens":256,"stream":true,"messages":[{"role":"user","content":"say hi"}]}'`.
       - Expected: Response includes `Content-Type: text/event-stream`. Body shows `event: message_start\ndata: {...}\n\n` then several `event: content_block_*\ndata: {...}\n\n` frames, finishing with `event: message_stop\ndata: {"type":"message_stop"}\n\n`.
       - Visually compare framing to the Anthropic spec example at https://docs.anthropic.com/en/api/messages-streaming — there should be no extra whitespace, no missing newlines, and `data:` is always followed by a single space then the JSON.

    4. **loop24-client non-streaming round-trip:**
       - From `/Users/coreyellis/Projects/repos/local/loop24-client`, run a chat invocation via the SDK / CLI that uses Anthropic. Concretely: set the env `ANTHROPIC_BASE_URL=http://localhost:11434` and `ANTHROPIC_API_KEY=test-token-123` and run whatever loop24-client harness exposes for non-streaming Anthropic chats (e.g., `pnpm test -- --testNamePattern=anthropic` if a test exists, or a one-shot script: `pnpm ts-node -e 'import Anthropic from "@anthropic-ai/sdk"; const a = new Anthropic({baseURL: process.env.ANTHROPIC_BASE_URL!, apiKey: process.env.ANTHROPIC_API_KEY!}); a.messages.create({model:"auto",max_tokens:256,messages:[{role:"user",content:"hi"}]}).then(r => console.log(r))'`).
       - Expected: the SDK returns a `Message` object with non-empty `content[0].text`. No exceptions, no schema validation failures.

    5. **loop24-client streaming round-trip:**
       - Same env, but invoke `a.messages.stream({...})` and consume the stream (e.g., `for await (const event of stream) { console.log(event.type); }`). One-shot: `pnpm ts-node -e 'import Anthropic from "@anthropic-ai/sdk"; const a = new Anthropic({baseURL: process.env.ANTHROPIC_BASE_URL!, apiKey: process.env.ANTHROPIC_API_KEY!}); (async () => { const s = a.messages.stream({model:"auto",max_tokens:256,messages:[{role:"user",content:"hi"}]}); for await (const e of s) console.log(e.type); const final = await s.finalMessage(); console.log("FINAL:", final); })()'`.
       - Expected: prints a sequence of event types `message_start` → `content_block_start` → `content_block_delta` (one or more) → `content_block_stop` → `message_delta` → `message_stop`; then the final message object with non-empty content[0].text.

    6. **Surface gating sanity (manual layer — automated coverage in Task 1 already verifies):**
       - Stop the gateway, restart with `ENABLED_SURFACES=ollama ./bin/loop24-gateway`.
       - `curl -i -X POST http://localhost:11434/v1/messages ...` should return 404.
       - `curl -i -X POST http://localhost:11434/api/chat -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'` should still work.
       - Stop, restart with `ENABLED_SURFACES=anthrpic` (typo) — gateway should EXIT NON-ZERO at boot with an error naming `anthrpic` (D-16 fail-fast).

    7. **Report:**
       - Confirm steps 1-6 succeed.
       - If any step fails, capture the exact failure (HTTP status, response body, SDK error message, gateway log line) so the planner can adjust.
  </how-to-verify>
  <action>
    Operator executes the 6 verification blocks defined in the &lt;how-to-verify&gt; section above. This task is a blocking human checkpoint — no Claude actions occur here; the executor pauses for the developer to run the steps and report results. If any step fails, the developer responds with the specific failure(s) which triggers a gap-closure plan via /gsd:plan-phase 03.1 --gaps.
  </action>
  <verify>
    <human-check>Developer runs the 6 verification blocks (boot, non-streaming curl, streaming curl, loop24-client non-streaming, loop24-client streaming, surface gating) and reports pass/fail for each. Steps 4 and 5 (loop24-client SDK round-trips) are the load-bearing ANTH-07 acceptance bar. Step 6 cross-checks the automated TestNewApp_SurfaceGating in Task 1.</human-check>
  </verify>
  <done>
    - All six verification blocks reported as PASS by the developer.
    - loop24-client `messages.create({stream:false})` returns a valid Message object with non-empty content (step 4).
    - loop24-client `messages.stream({...})` consumes the full event sequence without SDK errors (step 5).
    - Surface gating works (step 6): ENABLED_SURFACES=ollama disables /v1/messages; typo causes fail-fast exit.
    - Any failures captured in the response trigger a gap-closure plan rather than blocking phase completion silently.
  </done>
  <resume-signal>
    Reply `approved` if all six verification blocks pass.
    Reply with a description of any failure(s) — naming the specific step number and the observed behavior — to drive a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env → cmd/main.go | `ENABLED_SURFACES`, `ANTHROPIC_PATH_PREFIX`, `AUTH_TOKEN` etc. — typo-tolerant fail-fast via config.Load (D-16). |
| HUMAN-UAT operator → live gateway | Real kiro-cli subprocess + real tokens. Misconfig (auth token mismatch) surfaces in the SDK's APIError. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-WIRE | Architecture drift | cmd/main.go adapter construction | mitigate | `slices.Contains(cfg.EnabledSurfaces, "anthropic")` gate keeps the adapter construction explicit. Empty enabled list logs a warning but doesn't crash. Unknown surface names already rejected by config.Load (Plan 01). AUTOMATED test `TestNewApp_SurfaceGating` (Task 1) verifies route presence under three env permutations — no longer relies on HUMAN-UAT alone. |
| T-3.1-LEAK | Resource leak | integration test pool + SSE | mitigate | `defer p.Close()` + `goleak.VerifyNone(t)` + per-test ctx timeout. If the test hangs, the timeout fires and the goroutines unwind. |
| T-3.1-D13 | Trust boundary | wire.go redacted_thinking drop | mitigate | D-13 drops `redacted_thinking` blocks at wire-decode (Plan 02 Task 2). Plan 04 Task 2's `TestIntegration_RealKiroCLI_RedactedThinkingFollowUp` verifies kiro-cli does not error on a follow-up turn whose conversation history contained a redacted_thinking block — proves the drop policy produces a kiro-cli-safe transcript. |
| T-3.1-FRAME | Tampering | SSE framing drift in real kiro-cli output | mitigate | Plan 04 Task 2's streaming integration test enforces strict line-by-line state machine (event → data → blank → event) against real kiro-cli output. Catches any framing regression that golden tests with scripted chunks might miss. |
| T-3.1-UAT | Verification gap | HUMAN-UAT step | mitigate | Six explicit verification blocks covering boot + curl + dual-auth + non-streaming SDK + streaming SDK + surface gating. The operator must report each step; any failure becomes a gap-closure trigger. Surface gating is ADDITIONALLY covered by automated test in Task 1. |
| T-3.1-SC | Tampering (Supply Chain) | npm/pip/cargo installs | N/A | Phase 3.1 introduces no new third-party deps. The HUMAN-UAT step relies on the user's existing loop24-client install (`~/Projects/repos/local/loop24-client`), which was vetted in Phase 2. |
</threat_model>

<verification>
- `go build ./cmd/loop24-gateway/` produces a binary that starts without errors with default env.
- `go test ./... -race -count=1` exits 0 (integration tests skip without LOOP24_INTEGRATION=1; everything else passes).
- `go test ./cmd/... -race -count=1 -v -run 'TestNewApp_SurfaceGating'` passes all three sub-tests.
- `go vet ./...`, `golangci-lint run`, `go-arch-lint check` all exit 0.
- With `LOOP24_INTEGRATION=1` and kiro-cli available, `TestIntegration_RealKiroCLI_NonStreaming`, `TestIntegration_RealKiroCLI_Streaming` (with strict framing), and `TestIntegration_RealKiroCLI_RedactedThinkingFollowUp` all pass.
- HUMAN-UAT all six steps reported as passing.
</verification>

<success_criteria>
- ANTH-04 closed: dual-auth (Plan 01) + version header (Plan 02) + accept-and-ignore beta (Plan 02) ⟶ verified end-to-end by HUMAN-UAT step 4 (loop24-client sends Bearer; SDK sends anthropic-version: 2023-06-01).
- SURF-08 closed: `/v1/messages` mounted; ENABLED_SURFACES gating verified by AUTOMATED `TestNewApp_SurfaceGating` (Task 1) AND HUMAN-UAT step 6.
- Phase 3.1 acceptance criteria from ROADMAP.md:
  1. Curl probe (step 2) returns Anthropic-compatible JSON.
  2. loop24-client (step 4 + 5) completes both stream:false and stream:true round-trips.
  3. SSE event sequence verified at byte level (step 3) and via SDK (step 5) AND strict line-by-line framing via Task 2 integration test.
  4. tool_use input as object + tool_result inbound — closed in Plan 02; not exercised in HUMAN-UAT but covered by unit tests.
  5. ENABLED_SURFACES default + override + arch-lint boundary — closed in Plans 01 + 02; verified by Task 1 automated test + step 6 manual.
  6. anthropic-version required + dual-auth — closed across plans; verified by steps 2-4.
- D-13 redacted_thinking drop policy verified safe for kiro-cli (Task 2 follow-up turn test).
</success_criteria>

<output>
Create `.planning/phases/03.1-anthropic-surface/03.1-04-SUMMARY.md` when done. Include: HUMAN-UAT report (each of 6 steps with pass/fail + notes), TestNewApp_SurfaceGating result, integration test results (including redacted_thinking follow-up turn outcome), final list of files modified across all 4 plans, full requirements coverage table (ANTH-01..07 + SURF-08 all closed), any deferred items surfaced during UAT for the next phase.
</output>
</content>
</invoke>
