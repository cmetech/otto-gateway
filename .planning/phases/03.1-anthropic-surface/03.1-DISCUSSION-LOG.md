# Phase 3.1: Anthropic Surface - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-23
**Phase:** 3.1-anthropic-surface
**Areas discussed:** SSE mechanics & block boundaries, Auth + ENABLED_SURFACES routing, Thinking chunk routing, Inbound shape normalization

---

## SSE mechanics & block boundaries

### Q1: Streaming path — how the Anthropic adapter consumes the engine for messages.stream()

| Option | Description | Selected |
|--------|-------------|----------|
| Use engine.Run() + range Chunks() directly | Anthropic handler calls engine.Run(ctx, req) and ranges run.Stream().Chunks() itself. engine.Collect unchanged. Matches Phase 2 D-01. Zero engine-package signature churn. | ✓ |
| Add engine.Stream(ctx, req, sink) helper | New cross-cutting abstraction. One phase early — would land before its second consumer (Phase 3/4) needs it. | |
| Pre-build a canonical.StreamEvent type | Most abstraction; biggest blast radius. Speculative. | |
| You decide | Planner picks. | |

**User's choice:** Use engine.Run() + range Chunks() directly
**Notes:** Aligns with the "add when needed" rule and Phase 2 D-01 engine surface intent.

---

### Q2: Block-boundary state machine location

| Option | Description | Selected |
|--------|-------------|----------|
| Adapter-local state machine | Per-request struct tracking blockIndex + currentKind + blockOpen. Canonical chunks stay simple. Matches Bifrost. | ✓ |
| Add BlockIndex+IsFirst to canonical.Chunk | Pushes Anthropic-shaped concerns into canonical; violates D-11 (canonical is wire-agnostic). | |
| Engine emits super-chunks grouped by kind | Defeats streaming benefit (buffering latency); changes Phase 2 Ollama behavior. | |
| You decide | Planner picks. | |

**User's choice:** Adapter-local state machine
**Notes:** Canonical stays untouched; Ollama (Phase 4 NDJSON) never has to track blocks.

---

### Q3: Ping ticker + chunk channel + writer sharing

| Option | Description | Selected |
|--------|-------------|----------|
| Single select-loop in handler goroutine | One goroutine, one writer; select handles chunks, ticker, ctx.Done. Go's "share by communicating" idiom. | ✓ |
| Two goroutines + sync.Mutex around writer | Works but lock contention on hot path; Flush ordering harder to reason about. | |
| Pre-format-and-multiplex through one channel | More machinery; same outcome. | |
| You decide | Planner picks. | |

**User's choice:** Single select-loop in handler goroutine
**Notes:** Testing is trivial via a controlled chunks channel + injected fake ticker.

---

### Q4: Client disconnect — cancel aggression in Phase 3.1

| Option | Description | Selected |
|--------|-------------|----------|
| Rely on ctx propagation, defer explicit session/cancel to Phase 4 | ctx.Done unwinds the handler; engine.Run was called with that ctx so ACPClient.Prompt unwinds too. STRM-04 is Phase 4 scope. | ✓ |
| Explicitly call engine.Cancel(sid) on disconnect now | Adds engine.Cancel method + watchdog goroutine — Phase 4 work one phase early. | |
| Both: ctx propagation + a log line on disconnect | Cheap observability; could fall out for free. | (folded into the recommendation as a noted addition) |
| You decide | Planner picks. | |

**User's choice:** Rely on ctx propagation, defer explicit session/cancel to Phase 4
**Notes:** D-06 notes that a debug log line on ctx.Done SHOULD be added — observability win, costs nothing.

---

## Auth + ENABLED_SURFACES routing

### Q1: Dual-auth header support (x-api-key + Authorization: Bearer)

| Option | Description | Selected |
|--------|-------------|----------|
| Extend auth.Bearer to accept both headers globally | Modify internal/auth/bearer.go: try Authorization: Bearer first, fall back to x-api-key. All three surfaces accept both. One middleware, one mental model. | ✓ |
| Anthropic-specific middleware shim | Pushes auth knowledge into the adapter — adapter should not touch auth (server-level concern in Phase 2 model). | |
| Auth header in canonical.ChatRequest.Metadata | Defers auth into engine hook chain (Phase 8 territory). Over-engineered. | |
| You decide | Planner picks. | |

**User's choice:** Extend auth.Bearer to accept both headers globally
**Notes:** Token set still controlled by single AUTH_TOKEN env var; constant-time-compare unchanged.

---

### Q2: ENABLED_SURFACES edge-case semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Empty = default; unknown = error at boot | Default = ollama,anthropic. Unknown surface name → config.Load returns error; binary exits non-zero. Fail-fast on misconfig. | ✓ |
| Empty = default; unknown = warn-and-skip | Friendlier but masks typos — user deploys with ENABLED_SURFACES=olama and finds out later via 404s. | |
| Empty = disable all; explicit names required | Maximum explicitness. Breaks Node parity (Node had no such env var); requires updating every deploy doc. | |
| You decide | Planner picks. | |

**User's choice:** Empty = default; unknown = error at boot
**Notes:** Aligns with trust-gate philosophy; future Phase 3 extension to add "openai" to the allowed-names set is mechanical.

---

### Q3: Router composition for 2+ adapters under /v1

| Option | Description | Selected |
|--------|-------------|----------|
| Config grows AnthropicProtectedRouter + AnthropicPath fields now | Mirrors Phase 2's OllamaPath/OllamaProtectedRouter. NewFromConfig adds parallel r.Route block. Phase 3.1 owns /v1 solo; Phase 3 deals with co-mount. Surgical. | ✓ |
| Introduce a generic SurfaceMount list now | Future-proofs for 3+ surfaces; refactors working Phase 2 code. Slightly violates "add when needed". | |
| Mount everything via chi.Mux composition outside server.go | main.go grows; inverts Phase 2 wiring model. | |
| You decide | Planner picks. | |

**User's choice:** Config grows AnthropicProtectedRouter + AnthropicPath fields now
**Notes:** Phase 3 inherits the deferred refactor decision when OpenAI co-mounts under /v1.

---

### Q4: anthropic-version header check location

| Option | Description | Selected |
|--------|-------------|----------|
| Per-handler check in adapter/anthropic/handlers.go | First line of handleMessages: if header empty → 400 invalid_request_error. Accepts ANY non-empty value. | ✓ |
| Anthropic-specific middleware on the protected router | DRY across future endpoints (count_tokens etc.). | |
| Whitelist specific versions (2023-06-01 only) | Stricter; rejects future versions our SDK gets bumped to. Self-foot-gun. | |
| You decide | Planner picks. | |

**User's choice:** Per-handler check in adapter/anthropic/handlers.go
**Notes:** If /v1/messages/count_tokens ever lands, lift this into a chi middleware.

---

## Thinking chunk routing

### Q1: engine.Collect treatment of ChunkKindThought

| Option | Description | Selected |
|--------|-------------|----------|
| Stop dropping thoughts; aggregate parallel to text in Collect | Second strings.Builder for thoughts; on stream end, append ContentPart{Kind:ContentKindThinking} after text. Engine stays single source of truth. | ✓ |
| Anthropic adapter uses Run() and aggregates thoughts itself | Bypasses engine's response-assembly abstraction; adapter does engine's job. | |
| Add canonical.ChatResponse.Thinking string field | Duplicates content modeling — we already have ContentKindThinking. Two paths for the same data. | |
| You decide | Planner picks. | |

**User's choice:** Stop dropping thoughts in Collect; aggregate parallel to text
**Notes:** Ollama's render.go can choose to drop the thinking part (default) or fill ollamaChatResponseMessage.Thinking (planner's discretion).

---

### Q2: SSE block-transition rule for kind changes

| Option | Description | Selected |
|--------|-------------|----------|
| Any ChunkKind change opens a new block | Block 0 text, Block 1 thinking, Block 2 text on resume. Matches Anthropic wire model directly. | ✓ |
| Thinking always opens block 0 (before text) | Fragile if kiro-cli interleaves — would need to buffer text until thinking ends. | |
| Thinking buffered and emitted at end | Defeats real-time thinking display in loop24-client. | |
| You decide | Planner picks. | |

**User's choice:** Any ChunkKind change opens a new block
**Notes:** Tool-call chunks (Phase 6) slot in naturally as the next block kind.

---

### Q3: redacted_thinking handling in Phase 3.1

| Option | Description | Selected |
|--------|-------------|----------|
| Never emit redacted_thinking outbound | kiro-cli has no equivalent. Phase 3.1 emits only `{type:thinking,thinking:...}`. Documented known not-implemented variant. | ✓ |
| Map empty ChunkKindThought content to redacted_thinking | Synthetic mapping; not what either side means. | |
| Add ContentKindRedactedThinking to canonical | Speculative — no concrete trigger. | |
| You decide | Planner picks. | |

**User's choice:** Never emit redacted_thinking in Phase 3.1 outbound
**Notes:** Inbound redacted_thinking blocks in history are passed-through or dropped at the wire-decode layer (planner's call).

---

### Q4: Inbound thinking blocks in conversation history

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve as ContentPart{Kind:ContentKindThinking} in canonical | Maps to existing dormant seam (Phase 2 D-09). engine.buildBlocks gains a small extension — either new [Reasoning] bracket OR fold into [Assistant]. | ✓ |
| Drop inbound thinking blocks | Loses multi-turn thinking context. | |
| Pass through verbatim as opaque JSON in Metadata | Max preservation; hard to inspect/debug. | |
| You decide | Planner picks. | |

**User's choice:** Preserve as ContentPart{Kind: ContentKindThinking} in canonical
**Notes:** D-11 — planner picks between new [Reasoning] section vs fold-into-[Assistant] based on what reads cleanest on the ACP wire.

---

## Inbound shape normalization

### Q1: System block normalization (string OR array of blocks)

| Option | Description | Selected |
|--------|-------------|----------|
| String → verbatim; array → join text blocks with \n\n | wire.go checks JSON type. Drops cache_control. req.System stays string-typed. Zero canonical churn. | ✓ |
| Widen canonical.ChatRequest.System to []SystemBlock | Canonical churn for a feature (cache_control) we won't act on for several phases. | |
| Preserve full structure in canonical.ChatRequest.Metadata | Mostly invisible to downstream code. | |
| You decide | Planner picks. | |

**User's choice:** String form → verbatim; array form → join text blocks with \n\n
**Notes:** Phase 7+ may revisit if prompt-caching becomes load-bearing.

---

### Q2: tool_result.content normalization (string OR array of blocks)

| Option | Description | Selected |
|--------|-------------|----------|
| String → verbatim; array → join text blocks, drop images, debug-log | Lossy on images but Phase 3.1's tool-result path is read-only. Phase 6 revisits when tools execute. | ✓ |
| Widen ToolResultPart to []ContentPart | Full fidelity but canonical churn — Phase 6 might want this anyway. | |
| Reject array-form with 400 invalid_request_error | Strict; breaks loop24-client whenever it returns multi-block tool results. | |
| You decide | Planner picks. | |

**User's choice:** String → verbatim; array → join text blocks, drop images, log a warn
**Notes:** Phase 6 may widen ToolResultPart.Content to []ContentPart when tool execution actually lands.

---

### Q3: anthropic-beta + cache_control passthrough semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Accepted-and-ignored; logged at debug; never persisted to canonical | Matches Phase 2 KeepAlive/Options pattern. "Passed through" reads as "doesn't 400". | ✓ |
| Stored in canonical.ChatRequest.Metadata | Forward-design seam for PLUG-V2-04 semantic cache hook; slight per-request overhead. | |
| Whitelist specific beta flags; reject unknowns | Perpetual compatibility lag with Anthropic SDK updates. | |
| You decide | Planner picks. | |

**User's choice:** Accepted-and-ignored; logged at debug; never persisted to canonical
**Notes:** Future hooks (Phase 8+) can lift these into req.Metadata when needed.

---

### Q4: usage block contents when kiro-cli doesn't report tokens

| Option | Description | Selected |
|--------|-------------|----------|
| Honest zeros: input_tokens=0, output_tokens=0 | Documented limitation; loop24-client tolerates zeros. cache_* fields omitted (omitempty). | ✓ |
| Estimate by char count (chars / 4) | Closer to truthful for English; bad for code/Chinese. Hard to revoke later. | |
| Local tiktoken-style count | Real numbers but multi-MB tokenizer table; complicates static-binary story. Phase 7+. | |
| You decide | Planner picks. | |

**User's choice:** Honest zeros — input_tokens=0, output_tokens=0
**Notes:** Easy to upgrade when kiro-cli adds token reporting or when a Phase 7+ tokenizer is acceptable.

---

## Claude's Discretion

Areas where the planner has latitude (no specific user direction; planner picks
based on what reads cleanest):

- Whether Phase 2 Ollama's render.go starts populating
  `ollamaChatResponseMessage.Thinking` (D-02 follow-on) or keeps dropping
  thinking.
- Exact chi sub-router composition for the anthropic protected router.
- 403 mapping between `permission_error` vs `authentication_error` (D-20).
- Whether engine.buildBlocks adds a new `[Reasoning]` bracketed section for
  inbound thinking, or folds thinking text into `[Assistant]` (D-11).
- File-scoped split inside `internal/adapter/anthropic/`.
- ID generation strategy for the `message_start` `id` field.
- Test approach for the SSE handler (httptest + scanner vs fake loop24-client).
- Whether `/v1/messages/count_tokens` returns 404 or a cheap stub.

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section. Notable items:

- Explicit session/cancel on client disconnect (Phase 4 / STRM-04)
- Real token counting in usage (Phase 7+)
- Anthropic prompt-caching activation (Phase 7+)
- redacted_thinking outbound emission (never unless kiro-cli adds equivalent)
- count_tokens endpoint (planner picks 404 vs stub)
- @anthropic-ai/vertex-sdk and Bedrock variants (SURF-V2-03)
- Anthropic streaming tool_use with input_json_delta (Phase 6)
- signature_delta event emission (planner-decided)
- anthropic-beta header lifting into canonical metadata (Phase 8+)
- Generic Surfaces []SurfaceMount refactor of server.Config (Phase 3)
- CORS handling (v2)
