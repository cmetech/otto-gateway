---
phase: 03.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/auth/bearer.go
  - internal/auth/auth_test.go
  - internal/auth/auth_internal_test.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/config/config_internal_test.go
  - internal/server/server.go
  - internal/server/server_test.go
  - internal/engine/collect.go
  - internal/engine/collect_test.go
  - internal/engine/build_acp.go
  - internal/engine/build_acp_test.go
  - internal/adapter/ollama/render.go
  - internal/adapter/ollama/render_test.go
  - internal/adapter/ollama/handlers_test.go
  - .go-arch-lint.yml
autonomous: true
requirements: [ANTH-04, SURF-08]
must_haves:
  truths:
    - "auth.Bearer accepts BOTH Authorization: Bearer AND x-api-key headers; same constant-time-compare; same AUTH_TOKEN env var"
    - "config.Load reads ENABLED_SURFACES (default ollama,anthropic) and rejects unknown surface names (fail-fast at startup)"
    - "config.Load reads ANTHROPIC_PATH_PREFIX (default /v1)"
    - "server.NewFromConfig mounts cfg.AnthropicProtectedRouter under cfg.AnthropicPath with the same auth.Bearer + auth.IPAllowlist + AuthTrustXFF chain as Ollama"
    - "engine.Collect aggregates ChunkKindThought into a ContentPart{Kind:ContentKindThinking} on Message.Content (no longer drops thoughts)"
    - "engine.buildBlocks preserves inbound ContentKindThinking parts (planner picks [Reasoning] section vs fold into [Assistant])"
    - "Phase 2 Ollama tests continue passing after Collect thought aggregation"
    - ".go-arch-lint.yml declares adapter_anthropic component with mayDependOn: [canonical] only (TRST-04)"
  artifacts:
    - path: "internal/auth/bearer.go"
      provides: "Bearer middleware extended with extractToken(r) helper supporting x-api-key fallback"
      contains: "func extractToken"
    - path: "internal/config/config.go"
      provides: "EnabledSurfaces []string + AnthropicPathPrefix string fields + validateEnabledSurfaces"
      contains: "EnabledSurfaces"
    - path: "internal/server/server.go"
      provides: "AnthropicPath + AnthropicProtectedRouter Config fields; parallel r.Route mount block"
      contains: "AnthropicProtectedRouter"
    - path: "internal/engine/collect.go"
      provides: "Thought aggregation into Message.Content ContentKindThinking part (D-02)"
      contains: "ChunkKindThought"
    - path: "internal/engine/build_acp.go"
      provides: "Inbound ContentKindThinking handling per D-11"
      contains: "joinThinkingParts"
    - path: ".go-arch-lint.yml"
      provides: "adapter_anthropic + adapter_ollama + engine + pool components with TRST-04 boundaries"
      contains: "adapter_anthropic"
  key_links:
    - from: "internal/auth/bearer.go"
      to: "x-api-key + Authorization headers"
      via: "extractToken(r) tries Authorization: Bearer first, falls back to x-api-key"
      pattern: "extractToken"
    - from: "internal/config/config.go"
      to: "EnabledSurfaces validation"
      via: "validateEnabledSurfaces appends errors via errors.Join"
      pattern: "validateEnabledSurfaces"
    - from: "internal/server/server.go"
      to: "cfg.AnthropicProtectedRouter"
      via: "s.router.Route(cfg.AnthropicPath, ...) with auth.Bearer + auth.IPAllowlist"
      pattern: "AnthropicPath"
    - from: "internal/engine/collect.go"
      to: "assembleChatResponse"
      via: "thoughtSB.String() passed as new thinking arg"
      pattern: "thoughtSB"
    - from: ".go-arch-lint.yml"
      to: "internal/adapter/anthropic"
      via: "components.adapter_anthropic.in: adapter/anthropic/** + deps.adapter_anthropic.mayDependOn: [canonical]"
      pattern: "adapter_anthropic"
---

<objective>
Foundation work that unblocks the Anthropic adapter: extend auth.Bearer to dual-header (D-15), add ENABLED_SURFACES (D-16) + ANTHROPIC_PATH_PREFIX (D-19) to config, grow server.Config with parallel Anthropic mount fields (D-17), aggregate thought chunks in engine.Collect (D-02), preserve inbound thinking in engine.buildBlocks (D-11), and declare the adapter_anthropic + adapter_ollama + engine + pool TRST-04 boundaries in .go-arch-lint.yml.

Purpose: Plans 02/03/04 build the Anthropic adapter on top of this foundation. Without these changes the adapter cannot mount, cannot read its auth, cannot be selected at startup, cannot produce a thinking content block on the non-streaming path, and cannot enforce the TRST-04 import boundary.

Output: Six modified production files + six modified test files + .go-arch-lint.yml with all four components declared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-anthropic-surface/03.1-CONTEXT.md
@.planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md
@.planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md
@.planning/phases/03.1-anthropic-surface/03.1-VALIDATION.md
@CLAUDE.md
@docs/briefs/go_port_brief.md

<interfaces>
<!-- Source-of-truth code excerpts referenced by tasks below. Executor uses these directly. -->

From internal/canonical/chunk.go (already in place — DO NOT modify):
- `ChunkKind` enum: `ChunkKindText`, `ChunkKindThought`, `ChunkKindToolCall`, `ChunkKindPlan`
- `Chunk` struct with pointer fields `Text *TextChunk`, `Thought *ThoughtChunk`, ...
- `ThoughtChunk.Content` is the reasoning text payload.

From internal/canonical/chat.go (already in place — DO NOT modify):
- `ContentKind` enum: `ContentKindText`, `ContentKindThinking`, `ContentKindImage`, `ContentKindToolUse`, `ContentKindToolResult` — `ContentKindThinking` is the dormant Phase 2 seam (D-09) that Phase 3.1 activates.
- `ContentPart.Kind` discriminator + `ContentPart.Text` string payload (used for both text and thinking parts).

From internal/engine/engine.go (already in place — DO NOT modify shape):
- `type Stream interface { Chunks() <-chan canonical.Chunk; Result() (*canonical.FinalResult, error) }`
- `func (e *Engine) Run(ctx, req) (*Run, error)`
- `func (r *Run) Stream() Stream`
- `func (r *Run) SessionID() string`

From internal/config/config.go current state (lines 100-122):
- Existing pattern: `ollamaPath := getEnvStr("OLLAMA_PATH_PREFIX", "/api")` + `OllamaPathPrefix string` Config field.
- Helper `getEnvStrSliceComma(key string, def []string) []string` already returns the default when env is empty or all-blank.
- Error accumulation pattern: `errs = append(errs, fmt.Errorf("ALLOWED_IPS: %w", err))` then `return Config{}, fmt.Errorf("config: invalid env vars: %w", errors.Join(errs...))`.

From internal/server/server.go current state (lines 59-60, 141-163):
- Existing Config field: `OllamaPath string` + `OllamaProtectedRouter chi.Router` + `OllamaVersionHandler http.HandlerFunc`.
- Existing mount block uses `s.router.Route(cfg.OllamaPath, func(r chi.Router) { r.Use(auth.Bearer(...)); r.Use(auth.IPAllowlist(...)); r.Mount("/", cfg.OllamaProtectedRouter) })` and gates on `cfg.OllamaPath != "" && cfg.OllamaProtectedRouter != nil`.

From internal/auth/bearer.go current state (lines 20-49):
- `Bearer(cfg Config)` reads only `Authorization: Bearer <token>` then constant-time-compares against `cfg.Tokens`. On no-match calls `writeOllamaError(w, http.StatusUnauthorized, "Invalid or missing API key")`.

From internal/engine/collect.go current state (lines 48-65 — TARGET FOR D-02):
- Loop is `for chunk := range run.stream.Chunks() { if chunk.Kind == canonical.ChunkKindText && chunk.Text != nil { sb.WriteString(chunk.Text.Content) } }` with explicit comment "ChunkKindThought / ChunkKindToolCall / ChunkKindPlan are intentionally dropped in Phase 2. Phase 3.1 wires Thought through Anthropic's thinking content block".
- `assembleChatResponse(req, sb.String(), final)` currently produces a single `ContentPart{Kind: ContentKindText, Text: text}`.

From internal/engine/build_acp.go current state (lines 71-88 — TARGET FOR D-11):
- Per-role switch: `case canonical.RoleAssistant: text := joinTextParts(m.Content); if text != "" { fmt.Fprintf(&b, "[Assistant]\n%s\n\n", text) }`.
- `joinTextParts(parts)` walks parts and concatenates only `p.Kind == ContentKindText` text.

From internal/adapter/ollama/render.go current state (lines 30-34, 153-164):
- `joinThinkingContent(parts)` already walks `ContentKindThinking` parts (Phase 2 forward design).
- `ollamaChatResponseMessage.Thinking` already has `omitempty` JSON tag.
- After D-02 makes Collect emit a thinking part, `joinThinkingContent` will populate `Thinking` automatically — Phase 2 tests asserting `Thinking == ""` need to be updated OR the adapter's Phase 2 D-09 "drop on emit" footnote applies (planner's discretion D-02 follow-on).

From .go-arch-lint.yml current state (Phase 1 baseline — MISSING adapter_ollama, engine, pool, adapter_anthropic components):
- Currently declares only acp, canonical, config, server, version, testutil components with permissive deps.
- Phase 2 added Ollama adapter, engine, pool files in source tree but did NOT update the lint config — Phase 3.1 must add ALL FOUR (adapter_ollama from Phase 2, plus adapter_anthropic + engine + pool) to satisfy TRST-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Foundation extensions — auth dual-header + config (ENABLED_SURFACES + ANTHROPIC_PATH_PREFIX) + server mount fields</name>
  <files>internal/auth/bearer.go, internal/auth/auth_test.go, internal/auth/auth_internal_test.go, internal/config/config.go, internal/config/config_test.go, internal/config/config_internal_test.go, internal/server/server.go, internal/server/server_test.go</files>
  <read_first>
    - internal/auth/bearer.go (full file — current Bearer function, ~49 lines)
    - internal/auth/auth.go (writeOllamaError envelope helper)
    - internal/auth/auth_test.go (existing TestBearer table-driven shape)
    - internal/config/config.go (full file — getEnvStr, getEnvStrSliceComma, parseCIDRs, error accumulation)
    - internal/config/config_test.go (existing TestLoad cases for OLLAMA_PATH_PREFIX shape)
    - internal/server/server.go (full file — Config struct, NewFromConfig OllamaPath mount block)
    - internal/server/server_test.go (existing TestNewFromConfig Ollama mount test as template)
    - .planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md §`internal/auth/bearer.go` (lines ~1076-1153), §`internal/config/config.go` (lines ~1162-1237), §`internal/server/server.go` (lines ~1240-1294)
    - .planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md §Pattern 3 Dual-Auth and §Pattern 4 Surface Gating
  </read_first>
  <behavior>
    - Test: a request with `Authorization: Bearer good-token` passes auth.Bearer with `AUTH_TOKEN=good-token`.
    - Test: a request with `x-api-key: good-token` (NO Authorization header) passes auth.Bearer with `AUTH_TOKEN=good-token`.
    - Test: a request with both `Authorization: Bearer wrong` AND `x-api-key: good-token` is REJECTED (Authorization wins; falling back only when Authorization is absent or non-Bearer per D-15 contract).
    - Test: a request with neither header returns 401 + Ollama-shape `{"error":"Invalid or missing API key"}` (acceptable per RESEARCH.md Pattern 3 "option 2" — adapter does not need to re-render).
    - Test: an empty `AUTH_TOKEN` env continues to bypass auth (no-auth mode preserved).
    - Test: `config.Load` with `ENABLED_SURFACES` unset returns `Config.EnabledSurfaces == []string{"ollama","anthropic"}`.
    - Test: `config.Load` with `ENABLED_SURFACES=ollama` returns `EnabledSurfaces == []string{"ollama"}` (anthropic disabled).
    - Test: `config.Load` with `ENABLED_SURFACES=anthropic` returns `EnabledSurfaces == []string{"anthropic"}` (ollama disabled).
    - Test: `config.Load` with `ENABLED_SURFACES=ollama,olama` (typo) returns error mentioning `olama` and unknown-surface message (D-16 fail-fast). Validation set is `{"ollama","anthropic"}` — Phase 3 will extend to add `"openai"`.
    - Test: `config.Load` with `ANTHROPIC_PATH_PREFIX` unset returns `AnthropicPathPrefix == "/v1"`. With `ANTHROPIC_PATH_PREFIX=/anthropic/v1` returns `/anthropic/v1`.
    - Test: `server.NewFromConfig` with `AnthropicPath="/v1"` and non-nil `AnthropicProtectedRouter` mounts a route accessible at `/v1/messages` with `auth.Bearer` applied; unauthenticated request → 401.
    - Test: `server.NewFromConfig` with `AnthropicPath="/v1"` and nil `AnthropicProtectedRouter` skips the mount block (nil-safe gate). Server still starts; `/v1/messages` returns 404.
  </behavior>
  <action>
    Extend `internal/auth/bearer.go` per D-15 (mirrors PATTERNS.md §bearer.go and RESEARCH.md Pattern 3): refactor inline `authHeader := r.Header.Get("Authorization") ... strings.HasPrefix(...)` into a package-private helper `extractToken(r *http.Request) string` that tries `Authorization: Bearer <token>` first and falls back to `r.Header.Get("x-api-key")` when Authorization is absent or non-Bearer. The constant-time-compare loop over `cfg.Tokens` and the `writeOllamaError(w, http.StatusUnauthorized, "Invalid or missing API key")` 401 path stay unchanged.

    Extend `internal/auth/auth_test.go` (whitebox `package auth`) with a `TestBearer_DualHeader` table-driven test covering: Bearer-only valid, x-api-key-only valid, both-headers-Bearer-wrong, neither, empty Tokens passthrough. If existing table tests assert "Authorization required" wording, update only the test cases that exercise the new x-api-key fallback path.

    Extend `internal/config/config.go` per D-16 + D-19. Add Config fields `EnabledSurfaces []string` and `AnthropicPathPrefix string`. In `Load`: read `getEnvStrSliceComma("ENABLED_SURFACES", []string{"ollama","anthropic"})` and `getEnvStr("ANTHROPIC_PATH_PREFIX", "/v1")`. Add a package-private `validateEnabledSurfaces(in []string) error` that returns an error when any name is not in the set `{"ollama","anthropic"}` — error message MUST name the offending surface ("ENABLED_SURFACES: unknown surface %q (allowed: ollama, anthropic)"). Append the error to the `errs` slice using the existing pattern (mirrors `ALLOWED_IPS: %w` accumulation around config.go line 89-94). Phase 3 will widen the allowlist to include `"openai"`.

    Extend `internal/config/config_test.go` with cases for ENABLED_SURFACES default, override (single surface), unknown-name error (fail-fast). Add ANTHROPIC_PATH_PREFIX default + override cases mirroring the existing OLLAMA_PATH_PREFIX table rows.

    Extend `internal/server/server.go` per D-17. Add Config fields after `OllamaVersionHandler`: `AnthropicPath string` + `AnthropicProtectedRouter chi.Router`. In `NewFromConfig` add a PARALLEL mount block AFTER the existing Ollama `r.Route(cfg.OllamaPath, ...)` block (PATTERNS.md §server.go lines ~1271-1285): `if cfg.AnthropicPath != "" && cfg.AnthropicProtectedRouter != nil { s.router.Route(cfg.AnthropicPath, func(r chi.Router) { r.Use(auth.Bearer(auth.Config{Logger: cfg.Logger, Tokens: cfg.AuthTokens})); r.Use(auth.IPAllowlist(auth.Config{Logger: cfg.Logger, AllowedPrefixes: cfg.AllowedPrefixes, TrustXForwardedFor: cfg.AuthTrustXFF})); r.Mount("/", cfg.AnthropicProtectedRouter) }) }`. No new exempt route is needed (D-18 — no /v1/version equivalent; existing /api/version and /health remain exempt).

    Extend `internal/server/server_test.go` with a `TestNewFromConfig_AnthropicMount` mirroring the existing Ollama mount test: assert `/v1/anything` returns 401 without auth, returns 200 with valid bearer when AnthropicProtectedRouter is a stub `chi.NewRouter()` with a `r.Post("/anything", ...)` handler.

    Do NOT inline implementation code in this action — PATTERNS.md §bearer/config/server contains the exact excerpts to mirror. The action above names ONLY the identifiers and contracts.
  </action>
  <verify>
    <automated>go test ./internal/auth/ ./internal/config/ ./internal/server/ -race -count=1 -v -run 'Bearer_DualHeader|TestBearer$|TestLoad|TestNewFromConfig' 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `internal/auth/bearer.go` declares `func extractToken(r *http.Request) string` and the existing `Bearer` middleware calls it.
    - `internal/config/config.go` declares `EnabledSurfaces []string` and `AnthropicPathPrefix string` Config fields, plus package-private `validateEnabledSurfaces`.
    - `internal/server/server.go` declares `AnthropicPath string` and `AnthropicProtectedRouter chi.Router` Config fields and a nil-safe parallel mount block.
    - All behavior tests above pass under `go test ./internal/auth/ ./internal/config/ ./internal/server/ -race -count=1`.
    - `go vet ./...` exits 0.
    - `golangci-lint run` exits 0 on the modified files (errcheck, errorlint, gosec, revive, wrapcheck all green).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine — thought aggregation (D-02), inbound thinking ACP block (D-11), Ollama render follow-on</name>
  <files>internal/engine/collect.go, internal/engine/collect_test.go, internal/engine/build_acp.go, internal/engine/build_acp_test.go, internal/adapter/ollama/render.go, internal/adapter/ollama/render_test.go, internal/adapter/ollama/handlers_test.go</files>
  <read_first>
    - internal/engine/collect.go (full file — current single-builder loop lines 48-65, assembleChatResponse signature lines 86-107)
    - internal/engine/collect_test.go (existing TestCollect_DropsThoughtChunks test — must be flipped per D-02)
    - internal/engine/build_acp.go (full file — buildBlocks per-role switch lines 71-88, joinTextParts lines 128-139)
    - internal/engine/build_acp_test.go (existing buildBlocks golden cases)
    - internal/adapter/ollama/render.go (chatResponseToWire + joinThinkingContent lines 27-73, 153-164)
    - internal/adapter/ollama/render_test.go (existing Thinking-field assertion)
    - internal/adapter/ollama/handlers_test.go (TestHandleChat_Happy + any Thinking-field assertions)
    - .planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md §`internal/engine/collect.go` (lines ~1297-1382) and §`internal/engine/build_acp.go` (lines ~1386-1468)
    - .planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md §Architectural Responsibility Map row "Thought aggregation in Collect" and "Inbound thinking → ACP block content"
  </read_first>
  <behavior>
    - Test (collect): a stream emitting interleaved `ChunkKindText`("Hello ") + `ChunkKindThought`("reasoning ") + `ChunkKindText`("world") chunks → `Collect` returns `ChatResponse` with `Message.Content[0] == {Kind: ContentKindText, Text: "Hello world"}` and `Message.Content[1] == {Kind: ContentKindThinking, Text: "reasoning "}` (text and thinking aggregated separately; text part comes first, thinking second).
    - Test (collect): a stream emitting only ChunkKindText chunks → `Message.Content` has length 1 (no thinking part appended when thoughts builder is empty).
    - Test (collect): a stream emitting only `ChunkKindThought` → `Message.Content[0] == {Kind: ContentKindText, Text: ""}` and `Message.Content[1] == {Kind: ContentKindThinking, Text: ...}` — defensive empty-text part preserved so Phase 2 Ollama render keeps a stable shape.
    - Test (build_acp): a request whose assistant message contains both a text part and a thinking part produces an ACP transcript with the thinking content reaching the agent (planner picks: a new `[Reasoning]\n<thinking>\n\n` section per assistant message, OR `[Assistant]\n(thinking)\n<thinking>\n\n<text>\n\n` fold — RESEARCH.md and PATTERNS.md both leave this to planner; pick `[Reasoning]` option (a) for clarity and to keep [Assistant] semantically pure for response text only).
    - Test (build_acp): a request whose assistant message contains text only (no thinking part) still produces only the existing `[Assistant]\n<text>\n\n` section (regression guard for Phase 2 cases).
    - Test (ollama render): after D-02, a `ChatResponse` carrying a ContentKindThinking part populates `ollamaChatResponseMessage.Thinking` with the thinking text. (Pick option (a) from PATTERNS.md D-02 follow-on — KEEP populating because the field has `omitempty` and LangFlow tolerates it; no Node parity regression since Node has no thinking-emission path.)
    - Test (ollama render): a `ChatResponse` with no thinking part still produces a response with `Thinking == ""` (omitted from JSON).
    - Regression: every existing Phase 2 Ollama test (`TestHandleChat_*`, `TestChatResponseToWire`) continues to pass — if any test now sees a non-empty `Thinking` field because the fake engine emits Thought, update the assertion to reflect the new aggregated shape.
  </behavior>
  <action>
    Modify `internal/engine/collect.go` per D-02. Replace the single `var sb strings.Builder` + Text-only chunk loop (lines ~50-58) with TWO builders — `var sb, thoughtSB strings.Builder` — and a chunk switch:
    `case canonical.ChunkKindText: if chunk.Text != nil { sb.WriteString(chunk.Text.Content) }`
    `case canonical.ChunkKindThought: if chunk.Thought != nil { thoughtSB.WriteString(chunk.Thought.Content) }`
    Other cases (ToolCall, Plan) remain dropped (Phase 6 will wire them). Update the call to `assembleChatResponse` to pass the thinking string as a new parameter.

    Modify `assembleChatResponse` (signature change): add `thinking string` parameter AFTER the existing text param. Build `content := []canonical.ContentPart{{Kind: canonical.ContentKindText, Text: text}}`. When `thinking != ""`, append `{Kind: canonical.ContentKindThinking, Text: thinking}`. Update the PreHook short-circuit branch (line ~30) to pass `""` for thinking (short-circuit responses have no streamed thoughts) — verify all callers in collect.go are updated.

    Modify `internal/engine/collect_test.go`. Flip the existing `TestCollect_DropsThoughtChunks` (or rename to `TestCollect_AggregatesThoughtsAsThinkingPart`) so it asserts the NEW behavior. Add a new case for the "text + thought interleave" and "thought-only stream" scenarios from the behavior block above.

    Modify `internal/engine/build_acp.go` per D-11 (pick option (a) — `[Reasoning]` section). Add a small helper `joinThinkingParts(parts []canonical.ContentPart) string` mirroring `joinTextParts` (gate on `ContentKindThinking` instead of `ContentKindText`). In the `case canonical.RoleAssistant:` arm, after emitting `[Assistant]\n%s\n\n` for non-empty text, also emit `[Reasoning]\n%s\n\n` for non-empty thinking. Inbound thinking from Anthropic conversation history reaches kiro-cli as a separate bracketed section, semantically distinct from response text.

    Modify `internal/engine/build_acp_test.go`. Add a golden case `TestBuildBlocks_AssistantWithThinking` asserting the `[Reasoning]\n<thinking>\n\n` section appears AFTER the `[Assistant]\n<text>\n\n` section. Keep existing Phase 2 cases passing (they have no thinking parts).

    Modify `internal/adapter/ollama/render.go` per the D-02 follow-on. The existing `joinThinkingContent` already walks `ContentKindThinking` parts and the `Thinking` field already has `omitempty`. The follow-on decision: KEEP populating `Thinking` (PATTERNS.md option (a)) because LangFlow tolerates the field via `omitempty` and Node parity isn't a regression risk (Node simply never emits the field; we now do when there IS thinking content). No code change is required in render.go — the existing helpers will pick up the new thinking part automatically. Comment this decision in the file header (or above the relevant struct field).

    Modify `internal/adapter/ollama/render_test.go` and `internal/adapter/ollama/handlers_test.go` ONLY where existing assertions break — search for assertions that `ollamaChatResponseMessage.Thinking == ""` when the input chunks include Thought; update those to reflect the new behavior. Do NOT add new tests here (handled in this plan's other test additions).
  </action>
  <verify>
    <automated>go test ./internal/engine/ ./internal/adapter/ollama/ -race -count=1 -v -run 'TestCollect|TestBuildBlocks|TestChatResponseToWire|TestHandleChat' 2>&1 | tail -60</automated>
  </verify>
  <done>
    - `internal/engine/collect.go` uses two `strings.Builder`s and `assembleChatResponse` accepts a `thinking string` parameter.
    - `internal/engine/build_acp.go` declares `joinThinkingParts` and emits `[Reasoning]` after `[Assistant]` for messages with thinking content.
    - All new and modified tests pass under `go test ./internal/engine/ ./internal/adapter/ollama/ -race -count=1`.
    - All pre-existing Phase 2 Ollama tests pass (regression-clean).
    - `golangci-lint run` exits 0 on the modified files.
  </done>
</task>

<task type="auto">
  <name>Task 3: .go-arch-lint.yml — declare adapter_anthropic + adapter_ollama + engine + pool components and TRST-04 boundaries</name>
  <files>.go-arch-lint.yml</files>
  <read_first>
    - .go-arch-lint.yml (full file — current Phase 1 baseline content)
    - .planning/phases/03.1-anthropic-surface/03.1-PATTERNS.md §`.go-arch-lint.yml` (lines ~1596-1670)
    - .planning/phases/03.1-anthropic-surface/03.1-RESEARCH.md §Security Domain row "Adapter package importing internal/engine"
    - docs/briefs/go_port_brief.md §3.13 (Adapter-over-canonical layout) and §3.8 / §3.12 (Architectural layer invariants, TRST-04)
  </read_first>
  <action>
    Update `.go-arch-lint.yml` (TRST-04 — referenced from CLAUDE.md as ".go-arch-lint.yml boundary check is enforced"). The current file is the Phase 1 baseline and does NOT declare adapter_ollama, engine, or pool components even though Phase 2 added those packages to the source tree (gap acknowledged in PATTERNS.md note "the current snapshot is the Phase 1 baseline; Phase 2 may have already added adapter_ollama").

    Add four new components to the `components:` block (preserving existing acp, canonical, config, server, version, testutil entries):
      adapter_ollama: in: adapter/ollama/**
      adapter_anthropic: in: adapter/anthropic/**
      engine: in: engine/**
      pool: in: pool/**

    Add four new entries to the `deps:` block enforcing TRST-04 boundaries:
      adapter_ollama: anyVendorDeps: true; mayDependOn: [canonical]
      adapter_anthropic: anyVendorDeps: true; mayDependOn: [canonical]
      engine: anyVendorDeps: true; mayDependOn: [canonical, acp]
      pool: anyVendorDeps: true; mayDependOn: [canonical, acp, engine]

    Rationale per layer: adapter_* must NOT import internal/engine (consumer-defined Engine interface declared in adapter package); engine MAY import acp (via acp_adapter.go boundary file); pool MAY import engine (it satisfies engine.ACPClient). `internal/canonical` keeps `anyVendorDeps: true` and no `mayDependOn` — it depends on nothing under internal/.

    If running `go-arch-lint check` after the update reveals existing Phase 2 violations (e.g., adapter/ollama imports engine somewhere it shouldn't), STOP and report — those are Phase 2 regressions that need their own fix, not Phase 3.1 scope. The expectation is that Phase 2 already structured imports correctly and this lint config is catching up.

    Do NOT widen mayDependOn lists beyond what is listed above. The point of TRST-04 is the boundary; permissive deps re-introduce the architectural drift the brief §3.13 explicitly forbids.
  </action>
  <verify>
    <automated>go-arch-lint check 2>&1 | tee /tmp/go-arch-lint.out; grep -E "adapter_anthropic|adapter_ollama|engine|pool" .go-arch-lint.yml | head -20</automated>
  </verify>
  <done>
    - `.go-arch-lint.yml` declares all four new components (adapter_ollama, adapter_anthropic, engine, pool) with the exact mayDependOn lists above.
    - `go-arch-lint check` exits 0 (any unexpected violations are fixed or escalated as out-of-Phase-3.1 issues).
    - `grep -c "^  adapter_anthropic:" .go-arch-lint.yml` returns at least 2 (one in components, one in deps).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted env → config.Load | Operator-provided `ENABLED_SURFACES`, `ANTHROPIC_PATH_PREFIX`, `AUTH_TOKEN` strings cross into typed config. Misconfig must fail fast. |
| Client → auth.Bearer | Inbound HTTP requests carry credentials in `Authorization: Bearer` or `x-api-key` headers. Token comparison must be constant-time. |
| Adapter package → engine package | TRST-04 architectural boundary. Adapter MUST NOT import internal/engine (consumer-defined interface in the adapter package is satisfied structurally by *engine.Engine wired from cmd/main.go). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-AUTH | Spoofing / Elevation of Privilege | `internal/auth/bearer.go` (extractToken dual-header path) | mitigate | extractToken tries Authorization: Bearer FIRST then x-api-key; both paths feed the same `crypto/subtle.ConstantTimeCompare` loop over `cfg.Tokens`. An attacker setting `Authorization: Bearer wrong` and `x-api-key: good` is rejected (Authorization wins; fallback only when Authorization absent/non-Bearer). Test `TestBearer_DualHeader` enforces this precedence. |
| T-3.1-MISCONFIG | Tampering / Operational | `internal/config/config.go` (ENABLED_SURFACES parsing) | mitigate | `validateEnabledSurfaces` rejects unknown names (e.g., typo `anthrpic`) at startup with a clear error naming the offending value. `config.Load` returns the error; main.go exits non-zero. Prevents the silent-disable footgun (RESEARCH.md Pitfall 10). |
| T-3.1-TIMING | Information Disclosure | `internal/auth/bearer.go` token compare | accept | Loop over `cfg.Tokens` is NOT constant-time across the LIST — accepted per Phase 2 RESEARCH.md "leaks how many tokens are configured, not token bytes". Each per-token compare uses `subtle.ConstantTimeCompare`. No change in Phase 3.1. |
| T-3.1-HDRINJ | Tampering | `internal/auth/bearer.go` x-api-key value | mitigate | Token compared byte-equal via `subtle.ConstantTimeCompare`; never echoed in headers or logs. An x-api-key with control characters is rejected by the constant-time-compare (no constant-time bypass exists for malformed input). |
| T-3.1-ARCHLINT | Architecture drift | `internal/adapter/anthropic` (and other internal/ packages) | mitigate | `.go-arch-lint.yml` declares `adapter_anthropic: mayDependOn: [canonical]` — CI gate fails if the adapter ever imports `internal/engine`. The check is enforced at every commit per CLAUDE.md. Phase 8 may widen this to add `plugin`. |
| T-3.1-SC | Tampering (Supply Chain) | npm/pip/cargo installs | N/A | Phase 3.1 introduces no new third-party deps (RESEARCH.md §Standard Stack). No package legitimacy gate required for this phase. |
</threat_model>

<verification>
- All tests in `internal/auth/`, `internal/config/`, `internal/server/`, `internal/engine/`, `internal/adapter/ollama/` pass under `go test -race -count=1`.
- `go vet ./...` exits 0.
- `golangci-lint run` exits 0 (strict config preserved — errcheck, errorlint, gosec, revive, wrapcheck, etc.).
- `go-arch-lint check` exits 0 with the four new components declared.
- Setting `ENABLED_SURFACES=ollama,anthrpic` (typo) at startup causes `loop24-gateway` to exit non-zero with a clear error naming `anthrpic`.
- Setting `AUTH_TOKEN=test123` and curling `curl -H 'x-api-key: test123' http://localhost:11434/api/chat -d '...'` is authorized; setting only `Authorization: Bearer wrong` is rejected (401).
</verification>

<success_criteria>
- The Bearer middleware accepts both header styles (D-15 satisfied — partial credit toward ANTH-04).
- `ENABLED_SURFACES` and `ANTHROPIC_PATH_PREFIX` env vars are read with the correct defaults (D-16, D-19 — SURF-08 partial).
- `server.Config` carries the parallel Anthropic mount fields and uses them when wired (D-17 satisfied).
- `engine.Collect` aggregates Thought chunks into a `ContentKindThinking` part (D-02 satisfied — foundation for ANTH-07 outbound).
- `engine.buildBlocks` preserves inbound thinking via a `[Reasoning]` section (D-11 satisfied — foundation for ANTH-07 inbound).
- `.go-arch-lint.yml` declares the adapter_anthropic boundary and CI passes (TRST-04 active for the next plan's adapter work).
- All Phase 2 tests continue to pass — no regressions.
</success_criteria>

<output>
Create `.planning/phases/03.1-anthropic-surface/03.1-01-SUMMARY.md` when done. Include: list of files modified, key decisions activated (D-02, D-11, D-15, D-16, D-17, D-19), test runtimes, any deviations from the planner's intent.
</output>
