---
phase: 03.1-anthropic-surface
verified: 2026-05-24T15:57:51Z
status: human_needed
score: 6/6 success criteria verified (with 2 partial — see gaps)
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "SC #4 — Tool-call round-trip: `tool_use` rendered with `input` as object"
    status: partial
    reason: "render.go contentBlock tags Input with json:omitempty. When canonical ToolUsePart.Input is nil or empty map, encoding/json drops the field entirely from the wire — but the Anthropic spec marks tool_use.input as REQUIRED (must always be a JSON object, even {}). Any no-arg tool call surfaces as a parse exception in @anthropic-ai/sdk. The existing TestChatResponseToMessage_ToolUseInputIsObject test happens to pass {q:..,limit:5} which masks the defect. Surfaced in 03.1-REVIEW.md as BLOCKER CR-01."
    artifacts:
      - path: "internal/adapter/anthropic/render.go"
        issue: "Line 48: `Input map[string]any \\`json:\"input,omitempty\"\\`` drops nil/empty input from the wire. Line 104-112: chatResponseToMessage passes Input through without coercing nil to map[string]any{}."
    missing:
      - "Drop omitempty on contentBlock.Input OR coerce nil/empty Input to map[string]any{} at render-time"
      - "Regression test TestChatResponseToMessage_ToolUseEmptyInput_RendersEmptyObject asserting the wire carries \"input\":{} for a nil-Input ToolUsePart"
  - truth: "SC #6 — Gateway accepts both `x-api-key` and `Authorization: Bearer …` auth modes"
    status: partial
    reason: "extractToken in bearer.go uses strings.HasPrefix(authHeader, \"Bearer \") — exact case match. RFC 7235 §2.1 + RFC 6750 require the auth-scheme token to be case-insensitive. A client sending `Authorization: bearer s3cret` (lowercase) silently falls through to x-api-key — when no x-api-key is also supplied the valid lowercase-Bearer credential is rejected with 401. Additionally, the D-15 documented downgrade-attack guard (\"Authorization wins over x-api-key\") is BROKEN for lowercase clients: bearer wrong + x-api-key good auths as good. Existing TestBearer_DualHeader uses only canonical `Bearer` spelling. Surfaced in 03.1-REVIEW.md as BLOCKER CR-02."
    artifacts:
      - path: "internal/auth/bearer.go"
        issue: "Line 79: `if authHeader := r.Header.Get(\"Authorization\"); strings.HasPrefix(authHeader, bearerPrefix)` — exact-case prefix match where bearerPrefix is `\"Bearer \"`. Lowercase scheme tokens fall through silently."
    missing:
      - "Replace strings.HasPrefix(authHeader, bearerPrefix) with strings.EqualFold(authHeader[:len(bearerPrefix)], bearerPrefix) with a length guard"
      - "Coverage in auth_internal_test.go: TestExtractToken_LowercaseBearerSchemeAccepted + TestExtractToken_LowercaseBearer_DoesNotFallThroughToXAPIKey"
deferred: []
human_verification:
  - test: "Boot gateway + sanity probes"
    expected: "kiro-cli on PATH and authenticated; `make build && AUTH_TOKEN=test-token-123 KIRO_CMD=kiro-cli ENABLED_SURFACES=ollama,anthropic ./bin/loop24-gateway` starts; `curl -s http://localhost:11434/health | jq` returns 200 with pool stats; `curl -s -X POST http://localhost:11434/v1/messages` (no headers) returns 401."
    why_human: "Requires running the binary with a live kiro-cli subprocess; verifies operator boot script + Phase 2 auth carry-forward + Anthropic mount actually reachable on :11434."
  - test: "Non-streaming curl probe — x-api-key AND Authorization: Bearer (D-15 dual auth)"
    expected: "Run `curl -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{\"model\":\"auto\",\"max_tokens\":256,\"stream\":false,\"messages\":[{\"role\":\"user\",\"content\":\"say hi\"}]}'` → 200 + Anthropic Message JSON with non-empty content[0].text + stop_reason: \"end_turn\". Repeat with `Authorization: Bearer test-token-123` instead of x-api-key — same shape returned."
    why_human: "Verifies dual-header auth end-to-end with real kiro-cli. Note: case-sensitive Bearer match (CR-02) means lowercase `bearer` will silently fail; test with the canonical case the operator's clients use."
  - test: "Streaming curl probe — raw SSE byte inspection against Anthropic spec"
    expected: "Run `curl -N -i -X POST http://localhost:11434/v1/messages -H 'x-api-key: test-token-123' -H 'anthropic-version: 2023-06-01' -H 'Content-Type: application/json' -d '{\"model\":\"auto\",\"max_tokens\":256,\"stream\":true,\"messages\":[{\"role\":\"user\",\"content\":\"say hi\"}]}'`. Response includes `Content-Type: text/event-stream`. Body shows `event: message_start\\ndata: {...}\\n\\n` then several `event: content_block_*\\ndata: {...}\\n\\n` frames, finishing with `event: message_stop\\ndata: {\"type\":\"message_stop\"}\\n\\n`. Visually compare framing to https://docs.anthropic.com/en/api/messages-streaming — no extra whitespace, no missing newlines, `data:` always followed by a single space then JSON."
    why_human: "Visual/byte-level SSE framing verification against the Anthropic spec example. Golden-fixture tests pin scripted-chunk byte output; real-kiro output may differ subtly (kiro-cli's actual chunk cadence). Human-eye check catches drift the automated framing state-machine could miss."
  - test: "loop24-client non-streaming round-trip with ANTHROPIC_BASE_URL (load-bearing ANTH-07 + ANTH-04)"
    expected: "From `~/Projects/repos/local/loop24-client`, set `ANTHROPIC_BASE_URL=http://localhost:11434` + `ANTHROPIC_API_KEY=test-token-123` and invoke `a.messages.create({model:\"auto\",max_tokens:256,messages:[{role:\"user\",content:\"hi\"}]})`. SDK returns a Message object with non-empty content[0].text. No SDK schema-validation exception. No parse exception."
    why_human: "THIS IS THE LOAD-BEARING ANTH-07 / ANTH-04 ACCEPTANCE BAR. Cannot be verified programmatically — requires the real @anthropic-ai/sdk parser walking the response shape with its Zod schema. If CR-01 (tool_use.input omitempty drop) trips during a happy-path text response (it would only fire if the response contains a tool_use block), it would surface here as a parse exception. ALSO: this is where the operator-deferred HUMAN-UAT step 4 from Plan 04 Task 3 materializes."
  - test: "loop24-client streaming round-trip with ANTHROPIC_BASE_URL (load-bearing ANTH-07 streaming)"
    expected: "Same env as above. Invoke `a.messages.stream({...})` and consume: `for await (const event of stream) console.log(event.type); const final = await stream.finalMessage();`. Prints event sequence: message_start → content_block_start → content_block_delta (1+) → content_block_stop → message_delta → message_stop. Final message has non-empty content[0].text. No SDK errors."
    why_human: "Cannot be verified programmatically — requires the real @anthropic-ai/sdk MessageStream parser walking the SSE byte stream. Operator-deferred HUMAN-UAT step 5 from Plan 04 Task 3 materializes here."
  - test: "Surface gating cross-check — ENABLED_SURFACES=ollama + ENABLED_SURFACES=anthrpic typo"
    expected: "Stop gateway, restart with `ENABLED_SURFACES=ollama ./bin/loop24-gateway`. `curl -i -X POST http://localhost:11434/v1/messages …` returns 404; `curl -i -X POST http://localhost:11434/api/chat …` works. Stop, restart with `ENABLED_SURFACES=anthrpic` (typo) — gateway EXITS NON-ZERO at boot with a clear error message naming `anthrpic`."
    why_human: "Automated test (TestNewApp_SurfaceGating) already covers route presence under three permutations via degraded-mode httptest seam, but human verification confirms the live binary exhibits the same behavior under real env-var loading (not Config struct literal) and that the fail-fast exit message is operator-readable. Operator-deferred HUMAN-UAT step 6 from Plan 04 Task 3 materializes here."
---

# Phase 03.1: Anthropic Surface — Verification Report

**Phase Goal:** Bring the second adapter online on the same port, sharing the same canonical engine — loop24-client (GSD Pi CLI) configured with `ANTHROPIC_BASE_URL=http://localhost:11434` completes an end-to-end Messages-API chat against `kiro-cli` and gets back an Anthropic-compatible response, **including SSE streaming day-one** because `@anthropic-ai/sdk`'s `messages.stream()` is the dominant call site in the client. This phase proves the adapter-over-canonical layout supports a second surface alongside Ollama and validates that streaming is a first-class concern for the Anthropic shape.

**Verified:** 2026-05-24T15:57:51Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `curl -X POST http://localhost:11434/v1/messages …` returns an Anthropic-compatible JSON response from the same canonical engine that serves `/api/chat` | VERIFIED (autonomous) | `internal/adapter/anthropic/handlers.go:106` calls `a.cfg.Engine.Collect(r.Context(), req)` → renders via `chatResponseToMessage`. `cmd/loop24-gateway/main.go:71-253` wires `anthropic.New(anthropic.Config{Engine: anthropicEngineAdapter{engine: a.engine}})` — both adapters share the SAME `*engine.Engine` instance (one canonical engine, two surfaces). `internal/server/server.go:183-194` mounts `cfg.AnthropicProtectedRouter` under `cfg.AnthropicPath` with the same auth.Bearer + auth.IPAllowlist chain as Ollama. Anthropic Message response shape pinned by `render.go` (id/type/role/model/content/stop_reason/stop_sequence/usage). Live end-to-end curl behavior — HUMAN-UAT step 2. |
| 2 | loop24-client with `ANTHROPIC_BASE_URL=http://localhost:11434` completes both `messages.create({stream:false})` AND `messages.stream({...})` end-to-end against the gateway with zero loop24-client changes | NEEDS HUMAN | The autonomous portion is fully wired: `internal/adapter/anthropic/integration_test.go` provides TestIntegration_RealKiroCLI_NonStreaming + TestIntegration_RealKiroCLI_Streaming + TestIntegration_RealKiroCLI_RedactedThinkingFollowUp (skip cleanly without LOOP24_INTEGRATION=1; per operator note, these PASS locally with kiro-cli on PATH). The SDK round-trip itself cannot be verified programmatically — requires @anthropic-ai/sdk's Zod parser. HUMAN-UAT steps 4 + 5. |
| 3 | Streaming emits the full Anthropic SSE event sequence (message_start → content_block_* → message_delta → message_stop) with ping keepalives every ~15s, sourced from the same canonical chunk channel | VERIFIED | `internal/adapter/anthropic/sse.go` implements the full event sequence via `runSSEEmitter` (line 289) → `runSSEEmitterLoop` (line 359) → `applyChunk` (line 204) → `finalizeStream` (line 395). `const PingInterval = 15 * time.Second` (line 24); `time.NewTicker(PingInterval)` (line 330) — grep checks pass (no magic literal). Byte-exact golden fixtures (`testdata/sse_text_only.golden`, `testdata/sse_text_then_thinking.golden`, `testdata/sse_message_start.golden`) pin wire shape. 17 sse_test.go behavior tests + 4 golden tests cover state-machine transitions, ping interleave, ctx cancellation, Flusher missing, Flush count. D-05 single-goroutine writer invariant enforced by goleak + go-test -race. |
| 4 | Tool-call round-trip: `tool_use` rendered with `input` as object; `tool_result` accepted inbound | PARTIAL — see gap | Outbound non-streaming: `render.go:48` declares `Input map[string]any \`json:\"input,omitempty\"\`` which JSON-marshals as OBJECT (TestChatResponseToMessage_ToolUseInputIsObject round-trip-asserts). **DEFECT (CR-01):** the omitempty tag DROPS the field entirely when Input is nil OR empty map; Anthropic spec marks `tool_use.input` as REQUIRED. Inbound: `wire.go:244-252` decodes `tool_use` → ContentKindToolUse + ToolUse.Input passes through unchanged. Inbound `tool_result` accepted (wire.go:254-264 with D-09 string OR array-of-blocks content normalization). |
| 5 | `ENABLED_SURFACES` introduced with default `ollama,anthropic`. Setting `ENABLED_SURFACES=ollama` disables the Anthropic adapter at boot. `internal/adapter/anthropic` imports only `internal/canonical` + `internal/plugin`; `.go-arch-lint.yml` boundary check passes | VERIFIED | `internal/config/config.go:56-67,143-144` declares `EnabledSurfaces []string` (default `[ollama,anthropic]`) + `AnthropicPathPrefix string` (default `/v1`). `validateEnabledSurfaces` (line 159) fail-fasts on unknown names with message naming the offender (line 170). `cmd/loop24-gateway/main.go:174-193` gates `anthropic.New` on `slices.Contains(cfg.EnabledSurfaces, \"anthropic\")`. `TestNewApp_SurfaceGating` (cmd/loop24-gateway/main_test.go:76) PASSES all 3 sub-tests: Default mounts both, OllamaOnly returns 404 on /v1/messages, AnthropicOnly returns 404 on /api/chat. `go-arch-lint check` exits 0 ("OK - No warnings found"); confirmed no non-test file in `internal/adapter/anthropic/` imports `loop24-gateway/internal/engine` (TRST-04 boundary holds). Note: `internal/plugin` does not yet exist — Phase 8 territory; the success criterion's wording is forward-looking. |
| 6 | Missing `anthropic-version` returns canonical `invalid_request_error` in Anthropic's error envelope shape. Gateway accepts both `x-api-key` and `Authorization: Bearer …` auth modes | PARTIAL — see gap | Header check VERIFIED: `handlers.go:41-44` returns `writeError(w, 400, errInvalidRequest, "anthropic-version header is required")` with the Anthropic envelope shape (TestHandleMessages_MissingVersion_400 pins). 8 error_type constants in `errors.go:43-71` match D-20 literals. Dual-auth: `auth/bearer.go:77-83` `extractToken` tries Authorization: Bearer first, falls back to x-api-key. **DEFECT (CR-02):** prefix match is exact-case (`"Bearer "`); RFC 7235 §2.1 + RFC 6750 require case-insensitive scheme match. Lowercase `bearer` clients silently fall through to x-api-key, breaking both the dual-auth contract AND the D-15 Authorization-wins downgrade-attack guard for lowercase clients. Canonical-case clients (which loop24-client uses) work — HUMAN-UAT step 2 verifies. |

**Score:** 6/6 success criteria verified (with 2 partial — see gaps)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/adapter/anthropic/adapter.go` | Adapter struct + Config + New + ProtectedRouter + locally-declared Engine/RunHandle/Stream interfaces (TRST-04) | VERIFIED | 121 lines. Declares Engine/RunHandle/Stream interfaces locally; no internal/engine import. New(cfg) defensively replaces nil Logger. ProtectedRouter() registers exactly POST /messages. |
| `internal/adapter/anthropic/decode.go` | decodeJSONBody[T any] + isMaxBytesError + writeJSON | VERIFIED | 46 lines. Permissive (no DisallowUnknownFields per D-10). |
| `internal/adapter/anthropic/errors.go` | 8 error_type constants + envelope + writeError + writeSSEError | VERIFIED | 134 lines. All 8 constants present matching D-20. writeSSEError emits mid-stream `event: error\\ndata: <json>\\n\\n` + Flush. |
| `internal/adapter/anthropic/wire.go` | anthropicMessagesRequest decoder + wireToChatRequest including system/tool_result/resource_link/thinking/image normalization; explicit drop-at-wire-decode for redacted_thinking (D-13) | VERIFIED | 361 lines. Switch on 8 content-block types (text/image/tool_use/tool_result/thinking/redacted_thinking/resource_link/document+search_result drop). D-08 system normalization, D-09 tool_result.content normalization, D-13 redacted_thinking explicit drop with debug log, D-14 resource_link → ResourceLinks. |
| `internal/adapter/anthropic/render.go` | chatResponseToMessage + anthropicMessage/contentBlock/usage wire types + mapStopReason + genMessageID | VERIFIED (with CR-01 partial — see gap) | 184 lines. anthropicMessage StopReason/StopSequence are *string (nullable per spec). genMessageID uses crypto/rand. mapStopReason maps 5 canonical values + StopUnknown → nil. **CR-01 partial:** Input field has omitempty → drops on nil/empty Input. |
| `internal/adapter/anthropic/handlers.go` | handleMessages full non-streaming + streaming branch → real runSSEEmitter (Plan 02 stub deleted) | VERIFIED | 116 lines. Stub deleted (was 184 LOC). Streaming branch now calls real runSSEEmitter from sse.go. T-02-33 leak guards on every error path. |
| `internal/adapter/anthropic/sse.go` | real runSSEEmitter + select-loop + 11 SSE event payload types + PingInterval named const (W7) | VERIFIED | 432 lines. PingInterval const + time.NewTicker(PingInterval) verified by grep. Five-layer design (constants, payload types, sseEmitter struct, writeEvent, applyChunk+runSSEEmitter+finalizeStream). |
| `internal/adapter/anthropic/integration_test.go` | Real-kiro round-trip + streaming + redacted_thinking follow-up + strict SSE framing | VERIFIED | 478 lines. 3 tests gated on LOOP24_INTEGRATION=1; skip cleanly otherwise. Strict line-by-line SSE framing state machine. RedactedThinkingFollowUp verifies D-13 drop-at-wire-decode is kiro-cli-safe. |
| `internal/adapter/anthropic/testmain_test.go` | goleak.VerifyTestMain wired | VERIFIED | 22 lines. `goleak.VerifyTestMain(m)`. |
| `internal/adapter/anthropic/testdata/*.golden` | 3 byte-exact SSE golden fixtures | VERIFIED | sse_message_start.golden, sse_text_only.golden, sse_text_then_thinking.golden — byte-pinned wire shape. |
| `cmd/loop24-gateway/main.go` | Wired anthropic.New behind slices.Contains(cfg.EnabledSurfaces, "anthropic") + shared engine handle + parallel server.NewFromConfig mount | VERIFIED | Lines 174-193 gate construction. Lines 247-248 pass AnthropicPath + AnthropicProtectedRouter. anthropicEngineAdapter (line 291) bridges *engine.Engine.Run → anthropic.RunHandle without breaking TRST-04 (lives in cmd/, not adapter/). |
| `cmd/loop24-gateway/main_test.go` | TestNewApp_SurfaceGating (Default + OllamaOnly + AnthropicOnly via httptest seam) | VERIFIED | Line 76 declares TestNewApp_SurfaceGating; all 3 sub-tests PASS in 0.2s (verified by go test -v output). Distinguishes 405 (mounted, GET on POST route) from 404 (not mounted). |
| `internal/config/config.go` | EnabledSurfaces + AnthropicPathPrefix Config fields + validateEnabledSurfaces fail-fast | VERIFIED | Lines 56-67 + 143-144 fields; line 159 validator; line 170 fail-fast error message names offender. |
| `internal/server/server.go` | AnthropicPath + AnthropicProtectedRouter Config fields + parallel mount block under same auth chain | VERIFIED | Lines 71-72 fields; lines 183-194 parallel r.Route with auth.Bearer + auth.IPAllowlist. Nil-safe gate (line 183). |
| `internal/auth/bearer.go` | extractToken with x-api-key fallback | VERIFIED (with CR-02 partial — see gap) | Line 77 extractToken. **CR-02 partial:** case-sensitive `"Bearer "` prefix match. |
| `internal/engine/collect.go` | Thought aggregation into ContentKindThinking part (D-02) | VERIFIED | 2 references to ChunkKindThought; assembleChatResponse accepts thinking param; emits ContentKindThinking ContentPart when thoughts present. |
| `internal/engine/build_acp.go` | Inbound ContentKindThinking → [Reasoning] section (D-11) | VERIFIED | joinThinkingParts helper (line 159); `[Reasoning]\\n<thinking>\\n\\n` emitted after `[Assistant]` for assistant messages with thinking content (line 88-90). |
| `.go-arch-lint.yml` | adapter_anthropic + adapter_ollama + engine + pool components with TRST-04 boundaries | VERIFIED | All four components declared (grep confirms). `go-arch-lint check` exits 0 ("OK - No warnings found"). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cmd/loop24-gateway/main.go` | `anthropic.New` | `anthropicAdapter = anthropic.New(anthropic.Config{Logger, Engine: anthropicEngineAdapter{engine: a.engine}})` | WIRED | Line 174-193 conditional construction. Imports `loop24-gateway/internal/adapter/anthropic` (line 39). |
| `handleMessages` (non-streaming) | `Engine.Collect` | `a.cfg.Engine.Collect(r.Context(), req)` | WIRED | handlers.go:106. Result passed to chatResponseToMessage and writeJSON. |
| `handleMessages` (streaming) | `Engine.Run` → `runSSEEmitter` | `runHandle, _ := a.cfg.Engine.Run(...); runSSEEmitter(...)` | WIRED | handlers.go:83 + 92. Real emitter from sse.go (Plan 02 stub deleted, confirmed by `! grep -q runSSEEmitterStub handlers.go`). |
| `runSSEEmitter` | Anthropic SSE wire framing | `writeEvent` emits `event: <name>\\ndata: <json>\\n\\n` + Flush | WIRED | sse.go:163-173 single source of truth. Byte-exact golden tests confirm framing. |
| `runSSEEmitter` | ctx.Done + ticker + chunks select-loop | sse.go:362-380 three-case select | WIRED | D-05 single-goroutine invariant. ctx-cancel test asserts 100ms teardown. |
| `wireToChatRequest` | `canonical.ChatRequest.ResourceLinks` | resource_link blocks append to `req.ResourceLinks` (D-14) | WIRED | wire.go:281-289. |
| `wireToChatRequest` | redacted_thinking drop (D-13) | `case "redacted_thinking": logger.Debug(...)` no-append | WIRED | wire.go:273-279. Test asserts 0 ContentParts emitted + debug log fired. |
| `chatResponseToMessage` | Anthropic Message wire shape | `out.Content` walks resp.Message.Content + mapStopReason | WIRED | render.go:78-131. Field order pinned for byte-exact JSON. |
| `auth.Bearer` middleware | extractToken (dual-header) | `provided := extractToken(r)` then constant-time-compare loop | WIRED (with CR-02 partial) | bearer.go:40. extractToken contract has the case-sensitivity defect. |
| `server.NewFromConfig` | AnthropicProtectedRouter mount under AnthropicPath | `s.router.Route(cfg.AnthropicPath, …)` with auth.Bearer + auth.IPAllowlist | WIRED | server.go:183-194. Nil-safe gate. |
| `.go-arch-lint.yml` | TRST-04 boundary enforcement | adapter_anthropic.mayDependOn=[canonical] | WIRED | `go-arch-lint check` exits 0. Confirmed by grep: zero non-test imports of internal/engine from internal/adapter/anthropic. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `handlers.handleMessages` (non-streaming) | `resp *canonical.ChatResponse` | `a.cfg.Engine.Collect(ctx, req)` which calls into `engine.Collect` → pool.Acquire → kiro-cli subprocess → chunk channel aggregation | YES (autonomous unit tests use fakeEngine; integration test exercises real kiro-cli per operator note PASS locally) | FLOWING |
| `handlers.handleMessages` (streaming) | `runHandle anthropic.RunHandle` then chunk channel | `a.cfg.Engine.Run(ctx, req)` returns anthropicRunHandleAdapter wrapping *engine.Run; runSSEEmitter consumes Stream().Chunks() | YES (golden tests use scriptedChunks; integration test exercises real kiro-cli per operator note PASS locally) | FLOWING |
| `runSSEEmitter` | `flusher http.Flusher` + emitted bytes | Type-assertion on http.ResponseWriter; writeEvent calls fmt.Fprintf + Flush after every frame | YES (TestSSEEmitter_FlushCountEqualsWriteEvents asserts) | FLOWING |
| `wireToChatRequest` | `req.Messages` content parts | Decoded from `anthropicMessagesRequest.Messages` via decodeContentBlocks | YES (18 wire_test cases) | FLOWING |
| `chatResponseToMessage` | `out.Content[].Input` (tool_use) | `part.ToolUse.Input` passed through as map[string]any | PARTIAL — nil/empty Input is dropped from wire via omitempty (CR-01) | STATIC (defect) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Codebase compiles | `go build ./...` | exit 0, no output | PASS |
| Full test suite green | `go test ./... -count=1` | All packages PASS: acp, adapter/anthropic, adapter/ollama, auth, canonical, config, engine, pool, server, cmd | PASS |
| Anthropic adapter tests | `go test ./internal/adapter/anthropic/ -count=1` | ok 0.334s — 76 unit tests + 4 golden tests | PASS |
| Surface-gating test | `go test ./cmd/loop24-gateway/ -run TestNewApp_SurfaceGating -v` | PASS — Default/OllamaOnly/AnthropicOnly all assert route presence correctly | PASS |
| Integration tests skip cleanly without env | `LOOP24_INTEGRATION=0 go test ./internal/adapter/anthropic/ -run TestIntegration -v` | SKIP — clear "set LOOP24_INTEGRATION=1 to run" message on all 3 tests | PASS |
| PingInterval const grep | `grep -q 'PingInterval.*=.*15.*time.Second' internal/adapter/anthropic/sse.go` + `grep -q 'time.NewTicker(PingInterval)' internal/adapter/anthropic/sse.go` | Both exit 0 (W7 anchored, no magic literal) | PASS |
| Plan 02 stub deletion | `! grep -q 'TestHandleMessages_StreamingBranchPlaceholder' internal/adapter/anthropic/handlers_test.go` AND `! grep -q 'runSSEEmitterStub' internal/adapter/anthropic/handlers.go` | Both exit 0 (B4 part 2 closure) | PASS |
| TRST-04 boundary | `grep -rE "loop24-gateway/internal/engine" internal/adapter/anthropic/ --include='*.go' | grep -v '_test.go'` | 0 matches (test files alone may import engine; production code does not) | PASS |
| Arch-lint check | `go-arch-lint check` | "OK - No warnings found" — all 11 components clean | PASS |
| Real-kiro integration tests | `LOOP24_INTEGRATION=1 go test ./internal/adapter/anthropic/ -run TestIntegration` (requires kiro-cli on PATH) | Per operator instruction: all 3 PASS locally on operator machine (NonStreaming, Streaming, RedactedThinkingFollowUp). Cannot re-run in verification context without kiro-cli auth + LOOP24_INTEGRATION env. | PASS (operator-attested) |

### Probe Execution

No project-defined probe scripts (`scripts/*/tests/probe-*.sh`) exist in this repository. The verification relies on the Go test suite (`go test ./...`) as the equivalent automated probe — full results above. No probe contract violated.

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| (no project probes declared) | — | — | N/A |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ANTH-01 | 03.1-02-PLAN | Non-streaming `/v1/messages` returns Anthropic-shape JSON (id/type/role/model/content/stop_reason/usage) | SATISFIED | render.go anthropicMessage + chatResponseToMessage. TestChatResponseToMessage_GoldenWireShape pins full wire shape. |
| ANTH-02 | 03.1-03-PLAN | Streaming SSE event sequence + framing + 15s ping keepalive | SATISFIED | sse.go runSSEEmitter + golden tests + PingInterval named const. 17 sse_test behavior tests pass. |
| ANTH-03 | 03.1-02-PLAN, 03.1-03-PLAN | tool_use input as object (NOT JSON-string); tool_result content normalization inbound | PARTIAL (CR-01) | render.go contentBlock.Input map[string]any tagged omitempty — drops on nil/empty Input (gap). Inbound tool_use + tool_result fully wired (wire.go:244-264). Streaming input_json_delta deferred to Phase 6 per CONTEXT `<deferred>`. |
| ANTH-04 | 03.1-01-PLAN, 03.1-02-PLAN, 03.1-04-PLAN | anthropic-version required + accept-and-ignore beta + dual-header auth | PARTIAL (CR-02) — NEEDS HUMAN | handlers.go:41-49 header checks. bearer.go:77 extractToken (case-sensitive Bearer defect — CR-02 gap). Final closure requires loop24-client SDK round-trip HUMAN-UAT step 4. |
| ANTH-05 | 03.1-02-PLAN | System prompt mapping (string OR array of blocks → canonical System) | SATISFIED | wire.go:135-154 D-08 normalization. TestWire_System_* cases. |
| ANTH-06 | 03.1-02-PLAN | 8 error_type values + Anthropic error envelope shape | SATISFIED | errors.go:43-71 constants + envelope. TestErrorTypeConstants + TestWriteError_EnvelopeShape (8 sub-tests). Documented 401-from-auth-middleware exception (Ollama envelope) per RESEARCH.md §Pattern 3 option 2 — Phase 8 lifts. |
| ANTH-07 | 03.1-01-PLAN (D-02+D-11), 03.1-02-PLAN, 03.1-03-PLAN | Thinking content blocks in both directions (outbound non-streaming + streaming; inbound preserved) | SATISFIED — NEEDS HUMAN for final closure | Inbound: wire.go:266-271 thinking → ContentKindThinking. Outbound non-streaming: render.go:99-103. Outbound streaming: sse.go thinkingDelta + thinkingBlockHeader. D-11 [Reasoning] section in build_acp.go for inbound thinking → kiro-cli. Final loop24-client round-trip closure HUMAN-UAT steps 4+5. |
| SURF-08 | 03.1-01-PLAN, 03.1-04-PLAN | ANTHROPIC_PATH_PREFIX (default /v1); ENABLED_SURFACES surface gating | SATISFIED | config.go:56-67,122,143-170 fields + validator. server.go:183-194 parallel mount. TestNewApp_SurfaceGating PASSES 3 sub-tests. Live env-loader fail-fast verified by HUMAN-UAT step 6. |

**No orphaned requirements:** All 8 phase-mapped requirements (ANTH-01..07 + SURF-08) are claimed across the 4 plans and evidenced in code.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `internal/adapter/anthropic/render.go` | 48 | `Input map[string]any \`json:"input,omitempty"\`` — drops required Anthropic spec field when nil/empty | WARNING | Wire-format defect for no-arg tool calls; @anthropic-ai/sdk Zod parse exception. Captured as gap (CR-01). |
| `internal/auth/bearer.go` | 79 | Case-sensitive `"Bearer "` HasPrefix — violates RFC 7235/6750 case-insensitivity | WARNING | Lowercase-scheme clients silently fail or downgrade to x-api-key. Captured as gap (CR-02). |
| `internal/adapter/anthropic/handlers.go` | 92-102 | `runSSEEmitter` error logged at Debug — operators miss real frame-write failures | INFO | Observability gap; not a behavioral defect. Surfaced in REVIEW as CR-03. Recommend Warn level for non-context errors. |
| `internal/adapter/anthropic/render.go` | 180-182 | `genMessageID` crypto/rand failure returns fixed `"msg_01_rand_unavailable"` — concurrent collisions possible | INFO | Surfaced in REVIEW as WR-01. crypto/rand failure is exceptionally rare on supported platforms; collision risk is theoretical. |
| `internal/adapter/anthropic/sse.go` | 204-270 | applyChunk emits empty content_block_start + content_block_stop when first chunk's pointer is nil | INFO | Surfaced in REVIEW as WR-02. Defensive; nil-pointer chunks should not occur on canonical channels. |
| `cmd/loop24-gateway/main.go` | 341 | `var _ = errors.Is` — dead-code anti-pattern to keep import alive | INFO | Surfaced in REVIEW as IN-01. Cosmetic; remove import or implement intended errors.As introspection. |

No debt markers (`TBD`, `FIXME`, `XXX`) found in phase-modified production files outside test code — phase delivery is auditable.

### Human Verification Required

6 items need human testing. The first 6 items are operator-deferred HUMAN-UAT steps from Plan 04 Task 3 (per the user's instruction to defer HUMAN-UAT step 3 to phase-level HUMAN-UAT.md materialization). They form the load-bearing ANTH-07 / ANTH-04 acceptance bar.

1. **Boot gateway + sanity probes**
   - Test: `make build && AUTH_TOKEN=test-token-123 KIRO_CMD=kiro-cli ENABLED_SURFACES=ollama,anthropic ./bin/loop24-gateway`; then `curl -s http://localhost:11434/health | jq` (expect 200 with pool stats) + `curl -s -X POST http://localhost:11434/v1/messages` (no headers — expect 401).
   - Why human: live binary + kiro-cli subprocess; verifies operator-visible boot path.

2. **Non-streaming curl probe — x-api-key AND Authorization: Bearer (D-15 dual auth)**
   - Test: POST /v1/messages with each auth header in turn (model:auto, max_tokens:256, stream:false, "say hi").
   - Expected: 200 + Anthropic Message JSON with non-empty text + stop_reason="end_turn" for both header forms.
   - Why human: end-to-end dual-auth verification with real kiro-cli. **Note CR-02:** lowercase `bearer` scheme is broken (gap); use canonical `Bearer`.

3. **Streaming curl probe — raw SSE byte inspection**
   - Test: `curl -N -i -X POST .../v1/messages` with stream:true; visually verify framing against https://docs.anthropic.com/en/api/messages-streaming.
   - Why human: byte-level framing eyeball check. Golden tests pin scripted-chunk bytes; real kiro-cli output may differ subtly.

4. **loop24-client non-streaming round-trip with ANTHROPIC_BASE_URL** *(LOAD-BEARING ANTH-07/ANTH-04)*
   - Test: From `~/Projects/repos/local/loop24-client`, set `ANTHROPIC_BASE_URL=http://localhost:11434` + `ANTHROPIC_API_KEY=test-token-123` and invoke `a.messages.create({...})`.
   - Expected: SDK returns Message object with non-empty content[0].text; no schema exception.
   - Why human: only the real @anthropic-ai/sdk Zod parser can verify wire shape compliance. If CR-01 affects a happy-path response containing a tool_use block, parse exception surfaces here.

5. **loop24-client streaming round-trip with ANTHROPIC_BASE_URL** *(LOAD-BEARING ANTH-07)*
   - Test: Same env; invoke `a.messages.stream({...})` and consume with `for await`.
   - Expected: full event sequence printed; final message has non-empty content; no SDK errors.
   - Why human: only the real @anthropic-ai/sdk MessageStream parser can verify SSE framing compliance.

6. **Surface gating cross-check — ENABLED_SURFACES=ollama + ENABLED_SURFACES=anthrpic typo**
   - Test: Restart with `ENABLED_SURFACES=ollama` (verify /v1/messages → 404) and with `ENABLED_SURFACES=anthrpic` (verify gateway EXITS NON-ZERO with operator-readable error naming `anthrpic`).
   - Why human: TestNewApp_SurfaceGating uses degraded-mode httptest seam; human verifies real env-loader + fail-fast path with operator-eye on the diagnostic.

### Gaps Summary

Two real wire-format defects surfaced by the standard-depth code review (03.1-REVIEW.md) materially affect two success criteria. Both should ideally be closed before phase sign-off, but neither blocks the load-bearing autonomous deliverable (the adapter mounts, the SSE state machine emits the canonical sequence, the engine handle is shared, surface gating works). The operator may choose to:

- **Close both gaps in a follow-up gap-closure plan** before phase verification flips to `passed`, OR
- **Accept the deviations via VERIFICATION.md `overrides:` entries** if the lowercase-Bearer / nil-Input edge cases are deemed out-of-scope for Phase 3.1 (note: closing the gaps is recommended — CR-01 is a Zod parse exception waiting to happen; CR-02 is a silent auth-mode downgrade that does not match the documented D-15 contract).

**Group:** Both gaps are Anthropic / RFC spec-compliance issues surfaced in code review; neither is a wiring or scope defect. CR-03 (SSE error log level) and the WR/IN findings from the review are observability-quality, not goal-achievement gates.

**Phase 3.1 autonomous portion is COMPLETE:**
- All 4 plans landed (4/4 commits per Plan 04 SUMMARY).
- All 38 reviewed files compile + lint + arch-lint clean.
- Full test suite green: 10 packages PASS in ~12s aggregate.
- Live integration tests pass on the operator machine (per the user's note: TestNewApp_SurfaceGating + TestIntegration_RealKiroCLI_{NonStreaming,Streaming,RedactedThinkingFollowUp} all PASS locally).
- The HUMAN-UAT step deferred per operator instruction is materialized above as 6 human-verification items (matching Plan 04 Task 3's six blocks).

Status is `human_needed` (not `passed`) because the 6 human-verification items above include the load-bearing loop24-client SDK round-trips (steps 4 + 5) — the literal ANTH-07 acceptance bar from CONTEXT.md `<specifics>`. Per Step 9 of the verification process, the presence of human-verification items takes priority over a passing autonomous score.

---

_Verified: 2026-05-24T15:57:51Z_
_Verifier: Claude (gsd-verifier)_
