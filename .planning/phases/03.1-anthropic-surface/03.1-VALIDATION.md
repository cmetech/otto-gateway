---
phase: 3.1
slug: anthropic-surface
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-24
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `03.1-RESEARCH.md` Validation Architecture section.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib `testing` + `httptest`) |
| **Config file** | `go.mod` (no extra config — stdlib runner) |
| **Quick run command** | `go test ./internal/adapter/anthropic/...` |
| **Full suite command** | `go test ./... -race -count=1` |
| **Estimated runtime** | ~30 seconds (quick), ~90 seconds (full suite with race) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/adapter/anthropic/... -race -count=1`
- **After every plan wave:** Run `go test ./... -race -count=1`
- **Before `/gsd:verify-work`:** Full suite must be green AND `golangci-lint run`, `go vet ./...`, `gosec ./...`, `go-arch-lint check` all pass
- **Max feedback latency:** 30 seconds (quick); 90 seconds (full)

---

## Per-Task Verification Map

> Filled in by gsd-planner during PLAN.md generation. Each task in each plan must reference one row here (or declare `manual` with justification).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.1-W0-01 | 00 | 0 | (foundation) | — | go-arch-lint enforces `internal/adapter/anthropic` imports only `internal/canonical` + `internal/plugin` | unit | `go-arch-lint check` | ❌ W0 | ⬜ pending |
| 3.1-W0-02 | 00 | 0 | SURF-08 | — | `ENABLED_SURFACES` parses env into a set; unknown surfaces emit warning, valid surfaces registered | unit | `go test ./internal/config -run TestEnabledSurfaces -race` | ❌ W0 | ⬜ pending |
| 3.1-W0-03 | 00 | 0 | ANTH-06 | T-3.1-AUTH | Dual auth: `x-api-key` AND `Authorization: Bearer …` both accepted; absent both → 401 with Anthropic error envelope | unit | `go test ./internal/middleware -run TestAuthDual -race` | ❌ W0 | ⬜ pending |
| 3.1-01-01 | 01 | 1 | ANTH-01 | — | `POST /v1/messages` non-streaming returns Anthropic-shaped JSON (`id`, `type:"message"`, `role:"assistant"`, `content[]`, `stop_reason`, `usage`) | integration | `go test ./internal/adapter/anthropic -run TestMessagesNonStreaming -race` | ❌ W0 | ⬜ pending |
| 3.1-01-02 | 01 | 1 | ANTH-06 | T-3.1-HDR | Missing `anthropic-version` returns 400 with `{"type":"error","error":{"type":"invalid_request_error","message":"…"}}` | unit | `go test ./internal/adapter/anthropic -run TestVersionHeaderRequired -race` | ❌ W0 | ⬜ pending |
| 3.1-02-01 | 02 | 2 | ANTH-02, ANTH-03 | — | SSE renderer emits the full event sequence with `event:` and `data:` framing from a `<-chan canonical.Chunk` | golden | `go test ./internal/adapter/anthropic -run TestSSESequenceGolden -race` | ❌ W0 | ⬜ pending |
| 3.1-02-02 | 02 | 2 | ANTH-02 | — | `ping` keepalive frames emitted every ~15s during idle stream (verified with fake clock) | unit | `go test ./internal/adapter/anthropic -run TestSSEPingKeepalive -race` | ❌ W0 | ⬜ pending |
| 3.1-02-03 | 02 | 2 | ANTH-03 | — | Client disconnect mid-stream maps `StopCancelled` → `"end_turn"` (or chosen mapping), no goroutine leak | unit | `go test ./internal/adapter/anthropic -run TestStreamCancel -race && goleak verify` | ❌ W0 | ⬜ pending |
| 3.1-03-01 | 03 | 3 | ANTH-04 | — | `tool_use` content blocks render `input` as object (not JSON string) in canonical → Anthropic wire shape | golden | `go test ./internal/adapter/anthropic -run TestToolUseInputObject -race` | ❌ W0 | ⬜ pending |
| 3.1-03-02 | 03 | 3 | ANTH-04 | — | Inbound `tool_result` content blocks accepted on `messages.create()` requests and forwarded to canonical | unit | `go test ./internal/adapter/anthropic -run TestToolResultInbound -race` | ❌ W0 | ⬜ pending |
| 3.1-04-01 | 04 | 4 | ANTH-05 | T-3.1-ERR | All 8 Anthropic error_type values map from canonical error categories with correct HTTP status | unit | `go test ./internal/adapter/anthropic -run TestErrorEnvelopeMapping -race` | ❌ W0 | ⬜ pending |
| 3.1-04-02 | 04 | 4 | ANTH-07, SURF-08 | — | E2E: loop24-client with `ANTHROPIC_BASE_URL=http://localhost:11434` completes both `messages.create({stream:false})` and `messages.stream({...})` against a fake plugin, zero client changes | integration | `go test ./internal/adapter/anthropic -run TestE2EWithLoop24Client -race -tags=e2e` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/adapter/anthropic/adapter.go` — package skeleton with `Register(r chi.Router, deps Deps)` stub
- [ ] `internal/adapter/anthropic/handlers_test.go` — table-driven handler tests (covers ANTH-01, ANTH-06)
- [ ] `internal/adapter/anthropic/render_test.go` — SSE state-machine tests (covers ANTH-02, ANTH-03)
- [ ] `internal/adapter/anthropic/wire_test.go` — request/response shape tests (covers ANTH-04)
- [ ] `internal/adapter/anthropic/errors_test.go` — error envelope mapping tests (covers ANTH-05)
- [ ] `internal/adapter/anthropic/testdata/golden/` — golden SSE event sequences captured from real `@anthropic-ai/sdk` samples
- [ ] `internal/adapter/anthropic/testdata/fakes/plugin.go` — deterministic `internal/plugin` fake that emits a known canonical chunk stream
- [ ] `.go-arch-lint.yml` — boundary entry for `internal/adapter/anthropic` (imports only `internal/canonical` + `internal/plugin`)
- [ ] `go.uber.org/goleak` — already in `go.mod` from Phase 2; verify import path

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| loop24-client (`../loop24-client`) end-to-end chat with real `kiro-cli` worker — both streaming and non-streaming | ANTH-07 | Requires live `kiro-cli` ACP worker subprocess + real loop24-client invocation; integration test uses a fake plugin and that proves the gateway side, but the full client-to-worker round-trip is exercised here | 1) Start gateway with `ENABLED_SURFACES=ollama,anthropic` and a real `KIRO_CMD`. 2) Set `ANTHROPIC_BASE_URL=http://localhost:11434` and `ANTHROPIC_API_KEY=…` in loop24-client env. 3) Run loop24-client CLI: send a chat (non-stream), confirm response. 4) Run loop24-client CLI: send a chat with `--stream`, confirm streamed tokens arrive incrementally and the SSE sequence completes cleanly. |
| Wireshark/curl verification of exact SSE byte sequence against `@anthropic-ai/sdk` v0.x | ANTH-02, ANTH-03 | Golden test asserts the canonical bytes, but a human spot-check against the live SDK's streaming reader catches framing regressions that golden tests with stale captures would miss | `curl -N -X POST http://localhost:11434/v1/messages -H 'x-api-key: …' -H 'anthropic-version: 2023-06-01' -d '{"model":"auto","max_tokens":256,"stream":true,"messages":[{"role":"user","content":"hi"}]}'` and visually compare with a known-good Anthropic API response. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick) / < 90s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
