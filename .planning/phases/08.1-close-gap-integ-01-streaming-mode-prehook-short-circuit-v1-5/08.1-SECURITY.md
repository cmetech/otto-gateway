---
status: SECURED
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
threats_total: 18
threats_closed: 18
threats_open: 0
register_authored_at_plan_time: true
asvs_level: 2
audit_date: 2026-05-28
auditor: claude-opus-4-7 (gsd-secure-phase)
block_on: high
verdict: SECURED
---

# Phase 08.1 Security Audit

## Audit Stance

Each declared threat in the PLAN.md `<threat_model>` blocks (deduplicated across all 5 plans into an 18-entry register) was verified by direct code/doc inspection. No mitigation was accepted on documentation or intent alone — every `mitigate` disposition was matched to grep evidence at the cited file:line, every `accept` disposition was matched to the documented accepted-risk language at its cited location, and every `transfer` disposition (none present here) would require external evidence. Implementation files were treated as read-only; only this SECURITY.md was created.

## Threat verification matrix

| # | Threat ID | Category | Disposition | Status | Evidence |
|---|-----------|----------|-------------|--------|----------|
| 1 | T-08.1-AUTH-BYPASS-STREAM | Tampering / EoP | mitigate | CLOSED | Pre-header 401 guards verified at all 4 streaming sites: `internal/adapter/ollama/handlers.go:198-207` (handleChat), `internal/adapter/ollama/handlers.go:337-344` (handleGenerate), `internal/adapter/openai/handlers.go:146-154` (handleChatCompletions), `internal/adapter/anthropic/handlers.go:191-199` (handleMessages). All four use hard-coded `http.StatusUnauthorized` literal (D-04), all sit BETWEEN `eng.Run` and the SSE/NDJSON emitter call, all `return` immediately after writeError. |
| 2 | T-08.1-HEADER-LEAK | Information Disclosure | mitigate | CLOSED | Structural placement confirmed: all 4 guards precede the runSSEEmitter/runNDJSONEmitter calls. writeError helpers verified to set `Content-Type: application/json` BEFORE any WriteHeader: `internal/adapter/ollama/handlers.go:540`, `internal/adapter/openai/errors.go:62`, `internal/adapter/anthropic/errors.go:83`. Plan 02 negative byte-marker assertions on the wire confirmed at `internal/adapter/ollama/handlers_shortcircuit_test.go:148-155` (no `data:`, `event:`, `"done":true`), `internal/adapter/openai/handlers_shortcircuit_test.go:204-211`, `internal/adapter/anthropic/handlers_shortcircuit_test.go:171-178`. **Supplementary evidence:** Code-review-fix iteration tightened OpenAI Content-Type assertion (per `<constraints>`). |
| 3 | T-08.1-WATCHDOG-PANIC | DoS | mitigate | CLOSED | StopWatchdog nil-guard verified at all 4 sites: `internal/adapter/ollama/handlers.go:202-204`, `internal/adapter/ollama/handlers.go:339-341`, `internal/adapter/openai/handlers.go:149-151`, `internal/adapter/anthropic/handlers.go:194-196`. All four mirror the canonical template at `internal/adapter/anthropic/collect.go:69-71` (the `if stop := h.StopWatchdog(); stop != nil { stop() }` pattern). |
| 4 | T-08.1-INTERFACE-DRIFT | Integrity | mitigate | CLOSED | All four test fakes carry the propagated stub: `internal/adapter/ollama/ndjson_test.go:55` (pointer receiver), `internal/adapter/ollama/integration_test.go:48-50` (value receiver forwarder), `internal/adapter/openai/sse_golden_test.go:125` (pointer receiver), `internal/adapter/openai/integration_test.go:123-125` (value receiver forwarder). Compile-time enforcement: Plan 01 SUMMARY confirms `go build ./...` exits 0 after the interface change (which would fail if any fake were missing the method). |
| 5 | T-08.1-DERIVED-STATUS | Tampering (drift) | accept | CLOSED | Accepted-risk language present in PLAN 01 `<threat_model>` lines 252-253: "v1 AuthHook is the only short-circuit producer and always means 401. Hard-coding 401 per D-04 keeps the streaming and non-streaming branches symmetric. Revisit when a non-401 producer (rate-limit → 429, content-mod → 403) lands. Tracked in Deferred Ideas." Hard-coded `http.StatusUnauthorized` literal verified at all 4 streaming sites and the 4 non-streaming siblings (`internal/adapter/ollama/handlers.go:148`, `:313`, `internal/adapter/anthropic/handlers.go:235`, `internal/adapter/openai/handlers.go:179`, `:288`). Acceptance is consistent with current AuthHook-only short-circuit producer. |
| 6 | T-8-AUTH-BYPASS (carry-over) | Tampering | accept | CLOSED | Accepted-risk language present at `docs/operating.md:280-289` (`#### Accepted v1 risks` section): "Non-engine routes (e.g., /api/tags, /api/ps, /api/show, DELETE /v1/sessions/:id) consequently lose bearer-token gating at the server layer. The IP allowlist (ALLOWED_IPS) still applies. These are read-only catalog stubs / direct-registry operations — they do NOT reach the engine and have no PII surface." Operator-side mitigation (IP allowlist) documented and verified. Cross-referenced in `.planning/PROJECT.md:137` and `.planning/REQUIREMENTS.md:79-80` (AUTH-01/AUTH-02 Carve-outs). |
| 7 | T-08.1-PARTIAL-FIX | Tampering (false-pass) | mitigate | CLOSED | Two independently-named regression tests confirmed at `internal/adapter/ollama/handlers_shortcircuit_test.go:166` (`TestOllama_StreamingShortCircuit_Chat`) and `:182` (`TestOllama_StreamingShortCircuit_Generate`). Both exercise the same assertion set against independently-routed handlers. Pitfall 6 (two streaming branches in Ollama) is structurally covered. |
| 8 | T-08.1-WIRE-TYPE-DRIFT | Tampering | mitigate | CLOSED | Type field assertion verified at `internal/adapter/openai/handlers_shortcircuit_test.go:180` (asserts `Type == "authentication_error"`), `internal/adapter/anthropic/handlers_shortcircuit_test.go:140` (asserts outer `type == "error"` + inner `error.type == "authentication_error"`). E2E rows enforce the same: `tests/e2e/plugin_chain_test.go:430-431` (openai_stream), `:483-484` (anthropic_stream). |
| 9 | T-08.1-EMPTY-BODY | Tampering | mitigate | CLOSED | Non-empty Error.Message assertions present at all 4 adapter-level tests (verified via grep of "T-08.1-EMPTY-BODY" sentinel comments in test files) and all 3 E2E streaming rows (`tests/e2e/plugin_chain_test.go:391`, `:434`, `:487`). |
| 10 | T-08.1-CI-DRIFT | Tampering (silent erosion) | mitigate | CLOSED | Canonical sequence visible in `Makefile:207`: `ci: fmt-check vet build lint test-race arch-lint examples`. Explicit `.PHONY` declaration at `Makefile:18`. CI workflow `.github/workflows/ci.yml` has explicit steps for Install gofumpt (line 62), Format check (line 65), Vet (line 76), Build (line 79), Example tests (line 105), govulncheck (line 114). Delegation pattern documented at `.github/workflows/ci.yml:58`. **Supplementary evidence:** Code-review-fix iteration pinned govulncheck to `v1.1.4` via `GOVULNCHECK_VERSION` env var at `.github/workflows/ci.yml:38` and consumed at line 97 (per `<constraints>`). |
| 11 | T-08.1-FALSE-GREEN | Tampering (linter masks) | accept | CLOSED | Accepted-risk language present in PLAN 03 `<threat_model>` line 194: "The brief §3.12 sequence calls for an explicit go vet step; golangci-lint's govet covers the same ground but a separate step is clearer in CI logs and matches the brief. No functional change." Explicit `vet` target at `Makefile:63-64` and `Vet` step at `.github/workflows/ci.yml:76` both run `go vet ./...` independently of golangci-lint. |
| 12 | T-08.1-FMTCHECK-FALLBACK | Tampering | mitigate | CLOSED | Makefile fmt-check fallback to `gofmt -d` verified at `Makefile:50-52`: `else diff="$$(gofmt -d . 2>&1)"; tool="gofmt"; fi`. CI workflow installs gofumpt explicitly via `go install` at `.github/workflows/ci.yml:62-63` so silent skip is impossible. govulncheck pin to v1.1.4 noted as supplementary fix per `<constraints>`. |
| 13 | T-6.1-ADMIN-AUTH-EXEMPT (WARNING-02) | Information Disclosure | accept | CLOSED | Accepted-risk language present at `docs/operating.md:411-425` (`### v1 no-auth posture`): "`/admin` and its sub-routes (`/admin/api/snapshot`, `/admin/logs/stream`) are auth-exempt in v1, regardless of whether `AUTH_TOKEN` is set. This is intentional (CONTEXT decision D-01)... **Deployments outside a trusted network MUST either:** Wait for Phase 8... Add a reverse-proxy-layer auth shim (nginx auth_basic, oauth2-proxy, Cloudflare Access, etc.) in front of the gateway." Cross-referenced in `.planning/PROJECT.md:136` (Security carve-outs), `docs/operating.md:47-65` (Auth posture quick reference), `.planning/REQUIREMENTS.md:79` (AUTH-01 Carve-outs). Operator-side mitigation (bind-localhost or reverse-proxy auth) is durably documented. |
| 14 | T-08.1-DOC-DRIFT | Repudiation | mitigate | CLOSED | Cross-references use literal section names (drift-resistant), not line numbers. Verified at `.planning/PROJECT.md:136-137` ("see `docs/operating.md` section `### v1 no-auth posture`"; "see `docs/operating.md` section `#### Accepted v1 risks`"), `docs/operating.md:55,61` (cross-refs within quick reference), `.planning/REQUIREMENTS.md:79-80` (AUTH-01/02 carve-out cross-refs by section name). |
| 15 | T-08.1-UNDOC-CARVE-OUTS | Information Disclosure | mitigate | CLOSED | Security carve-outs annotation present at `.planning/PROJECT.md:135-137` (bulleted list under Constraints, enumerating /admin auth-exempt + Ollama list-mode-stub bypass). Auth posture quick reference subsection present at `docs/operating.md:47-65` (early-document, line < 250 boundary per plan requirement). Both surfaces grep-detectable: "auth-exempt" (2 hits in PROJECT.md, multiple in operating.md), "list-mode" (1 hit each surface). |
| 16 | T-08.1-CHECKBOX-DRIFT | Repudiation | mitigate | CLOSED | Mechanical flip per audit canonical column verified: `.planning/REQUIREMENTS.md` shows 62/62 `[x]` in bulleted list, 62/62 `Complete` in traceability table, 0 `[ ]` / 0 `Pending`. PLUG-06 row at line 196, OBSV-04 row at line 203. Coverage comment reads "62 total" (stale "58 total" scrubbed). AUTH-01 line 79 carries verbatim `**Carve-outs:**` annotation; AUTH-02 line 80 carries the shorter cross-reference per Pattern I. |
| 17 | T-08.1-OVER-FLIP | Tampering | mitigate | CLOSED | Plan 05 SUMMARY documents exact flips (44 bulleted + 42 table) sourced from audit Final Status column. The 6 Slice A-blocked partials (PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02) were authorized to flip because depends_on=["08.1-02"] gates Slice A green at execution time. No row flipped without audit Final Status `satisfied`. Plan 05 SUMMARY explicitly states: "every flip authorized by .planning/v1.5-MILESTONE-AUDIT.md Final Status == 'satisfied'; no satisfaction judgment originated in this plan". |
| 18 | T-08.1-PARTIAL-LIST-MISMATCH | Integrity | mitigate | CLOSED | Bulleted list and traceability table representations synchronized. Verified: `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` → 62; `grep -c '| Complete |' .planning/REQUIREMENTS.md` → 62. `grep -c '^- \[ \]'` → 0; `grep -c '| Pending |'` → 0. 100% agreement between the two representations. |

## Accepted-risk language (transcribed for audit trail)

### T-08.1-DERIVED-STATUS (PLAN 01 threat register)

> "v1 AuthHook is the only short-circuit producer and always means 401. Hard-coding 401 per D-04 keeps the streaming and non-streaming branches symmetric. Revisit when a non-401 producer (rate-limit → 429, content-mod → 403) lands. Tracked in Deferred Ideas."

### T-8-AUTH-BYPASS (docs/operating.md:280-289)

> "T-8-AUTH-BYPASS (non-engine routes lose bearer-token gating). Phase 8 moved bearer-token validation from the auth.Bearer chi middleware to plugin.AuthHook on the canonical engine chain. Non-engine routes (e.g., /api/tags, /api/ps, /api/show, DELETE /v1/sessions/:id) consequently lose bearer-token gating at the server layer. The IP allowlist (ALLOWED_IPS) still applies. These are read-only catalog stubs / direct-registry operations — they do NOT reach the engine and have no PII surface."

### T-08.1-FALSE-GREEN (PLAN 03 threat register)

> "The brief §3.12 sequence calls for an explicit go vet step; golangci-lint's govet covers the same ground but a separate step is clearer in CI logs and matches the brief. No functional change."

### T-6.1-ADMIN-AUTH-EXEMPT (docs/operating.md:411-425)

> "`/admin` and its sub-routes (/admin/api/snapshot, /admin/logs/stream) are auth-exempt in v1, regardless of whether AUTH_TOKEN is set. This is intentional (CONTEXT decision D-01)... Deployments outside a trusted network MUST either: Wait for Phase 8 (plugin hook chain), which will gate /admin/* behind the same AuthHook that protects /v1/* and /api/*. Add a reverse-proxy-layer auth shim (nginx auth_basic, oauth2-proxy, Cloudflare Access, etc.) in front of the gateway."

## Unregistered Flags

None. All 5 plan SUMMARY files declare no new threat flags emerged during implementation:

- Plan 01 (`## Threat Surface Notes`): "No threat flags emerged from the implementation that were not already in the register."
- Plan 02 (`## Threat Surface Notes`): "No threat flags emerged from the implementation that were not already in the register."
- Plan 03 (`## Threat Flags`): "No new security-relevant surface introduced. The new fmt/vet/build/examples targets are read-only CI verification steps; they cannot mutate code, network state, or the file system beyond bin/ and go test cache."
- Plan 04 (`## Threat Flags`): "No new security-relevant surfaces introduced (zero code change, zero new gateway behavior, zero new env vars)."
- Plan 05: doc-only (REQUIREMENTS.md refresh + AUTH-01/02 annotation) — no code surface introduced.

## Supplementary evidence (per `<constraints>`)

Two code-review-fix iteration commits (3ceddce + 4b9752a, referenced in audit invocation `<constraints>`) supplemented threat mitigations:

1. **T-08.1-CI-DRIFT supplementary** — govulncheck pinned to `v1.1.4` at `.github/workflows/ci.yml:38` (`GOVULNCHECK_VERSION: v1.1.4`) and consumed via env-var reference at line 97 (`govulncheck@${{ env.GOVULNCHECK_VERSION }}`). Prevents silent supply-chain drift if upstream govulncheck releases a breaking version.
2. **T-08.1-HEADER-LEAK supplementary** — OpenAI Content-Type assertion was tightened during code-review-fix. The current adapter-level test at `internal/adapter/openai/handlers_shortcircuit_test.go` uses `strings.HasPrefix(ct, "application/json")` because httptest may append `; charset=utf-8`; the E2E test at `tests/e2e/plugin_chain_test.go:404` enforces the same prefix invariant on the booted-binary path.

## Verification commands (reproducibility)

```bash
# T-08.1-AUTH-BYPASS-STREAM, T-08.1-HEADER-LEAK, T-08.1-WATCHDOG-PANIC structural verification
grep -n "ShortCircuitResponse()" internal/adapter/{ollama,openai,anthropic}/handlers.go
grep -n "StopWatchdog()" internal/adapter/{ollama,openai,anthropic}/handlers.go
grep -n "http.StatusUnauthorized" internal/adapter/{ollama,openai,anthropic}/handlers.go

# T-08.1-INTERFACE-DRIFT
grep -n "ShortCircuitResponse" internal/adapter/{ollama,openai,anthropic}/adapter.go cmd/otto-gateway/main.go

# T-08.1-CI-DRIFT
grep -nE "^fmt-check:|^vet:|^examples:|^ci:|^\.PHONY:" Makefile
grep -nE "gofumpt|go vet|go build|Example|govulncheck" .github/workflows/ci.yml

# T-6.1-ADMIN-AUTH-EXEMPT, T-08.1-UNDOC-CARVE-OUTS, T-08.1-DOC-DRIFT
grep -nE "auth-exempt|Security carve|list-mode|/admin|Auth posture" .planning/PROJECT.md
grep -nE "Auth posture quick reference|v1 no-auth posture|Accepted v1 risks" docs/operating.md

# T-08.1-CHECKBOX-DRIFT, T-08.1-PARTIAL-LIST-MISMATCH
grep -c '^- \[x\]' .planning/REQUIREMENTS.md   # expect 62
grep -c '^- \[ \]' .planning/REQUIREMENTS.md   # expect 0
grep -c '| Complete |' .planning/REQUIREMENTS.md # expect 62
grep -c '| Pending |' .planning/REQUIREMENTS.md  # expect 0
```

## Audit Trail

| Date | Auditor | Action | Verdict |
|------|---------|--------|---------|
| 2026-05-28 | claude-opus-4-7 (gsd-secure-phase) | Initial security audit of Phase 08.1 (close-gap INTEG-01 streaming + v1.5 WARNINGs); verified 18 deduplicated threats across 5 plans against ASVS L2 baseline (bearer-token + IP allowlist + subprocess spawn surface per CLAUDE.md) | SECURED — 18/18 threats CLOSED; 0 OPEN; 0 unregistered flags |

## Verdict

**SECURED.** Every declared threat in the deduplicated 18-entry register resolves to CLOSED:
- 11 `mitigate` threats: each verified by direct grep evidence at the cited file:line.
- 4 `accept` threats: each accepted-risk language found at its documented location, with operator-side mitigation (IP allowlist, bind-localhost, reverse-proxy auth) documented and grep-detectable.
- 0 `transfer` threats present in register.
- 0 unregistered flags from SUMMARY files (all 5 plans explicitly declared none).

No implementation file was modified during this audit. Phase 08.1 may ship.
