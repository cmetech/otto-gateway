---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
fixed_at: 2026-05-28T00:00:00Z
review_path: .planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-REVIEW.md
iteration: 1
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 08.1: Code Review Fix Report

**Fixed at:** 2026-05-28
**Source review:** `.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 2 (all Warning-severity; no Critical; Info skipped per `fix_scope: critical_warning`)
- Fixed: 2
- Skipped: 0
- Adapter tests post-fix: `go test -race -count=1 ./internal/adapter/openai/...` -> PASS (2.771s)
- Build post-fix: `go build ./...` -> clean

## Fixed Issues

### WR-01: CI workflow pins golangci-lint and go-arch-lint but not govulncheck

**Files modified:** `.github/workflows/ci.yml`
**Commit:** 3ceddce
**Applied fix:** Changed `GOVULNCHECK_VERSION: latest` to `GOVULNCHECK_VERSION: v1.1.4` (line 38) so the env block matches the workflow preamble's stated pin policy. v1.1.4 is the current stable govulncheck release (golang/vuln). The workflow's `Install govulncheck` step at line 97 already consumes `${{ env.GOVULNCHECK_VERSION }}`, so no downstream step changes were needed.

**Out-of-scope companions left for follow-up** (raised in REVIEW.md as IN-04, not WR-01):
- `mvdan.cc/gofumpt@latest` at line 63 — same drift hazard, but classified Info.
- `golangci/golangci-lint-action@v6` at line 86 — major-version tag rather than commit SHA, also Info.

### WR-02: OpenAI streaming short-circuit test uses `HasPrefix` while siblings require exact match

**Files modified:** `internal/adapter/openai/handlers_shortcircuit_test.go`
**Commit:** 4b9752a
**Applied fix:** Replaced

    if !strings.HasPrefix(ct, "application/json") { ... }

with

    if ct != "application/json" {
        t.Errorf("Content-Type: got %q, want exactly %q (Pitfall 3 / T-08.1-HEADER-LEAK — text/event-stream leak OR charset suffix == regression)",
            ct, "application/json")
    }

This matches the Ollama (`handlers_shortcircuit_test.go:128`) and Anthropic (`handlers_shortcircuit_test.go:135`) sibling assertions. The `strings` import is preserved (still used by `strings.NewReader` for the request body at line 148).

**Verification of the resolution path described in the prompt:** The OpenAI `writeError` helper (`internal/adapter/openai/errors.go:62`) sets `Content-Type: application/json` with no charset suffix; `net/http` does not post-process user-set Content-Type values during the `httptest.Server` round-trip. The test passes post-fix under `-race -count=1`, confirming the assertion holds and no handler-side `writeError` defect is being masked. If a future regression appends `; charset=utf-8` at the handler layer, this canary will now catch it on the OpenAI surface (previously it would have slipped through `HasPrefix`).

## Skipped Issues

None — both in-scope Warning findings were fixed.

The four Info findings (IN-01, IN-02, IN-03, IN-04) are out of scope under `fix_scope: critical_warning` and were not attempted.

---

_Fixed: 2026-05-28_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
