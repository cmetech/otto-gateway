---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - internal/adapter/ollama/handlers_shortcircuit_test.go
  - internal/adapter/openai/handlers_shortcircuit_test.go
  - internal/adapter/anthropic/handlers_shortcircuit_test.go
  - internal/adapter/ollama/ndjson_test.go
  - internal/adapter/openai/sse_golden_test.go
  - internal/adapter/anthropic/handlers_test.go
  - tests/e2e/plugin_chain_test.go
autonomous: true
requirements: [PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02]
tags: [auth, streaming, gateway, prehook, short-circuit, testing]

must_haves:
  truths:
    - "An automated adapter-level test fails when the Ollama handleChat streaming short-circuit guard is removed"
    - "An automated adapter-level test fails when the Ollama handleGenerate streaming short-circuit guard is removed"
    - "An automated adapter-level test fails when the OpenAI handleChatCompletions streaming short-circuit guard is removed"
    - "An automated adapter-level test fails when the Anthropic handleMessages streaming short-circuit guard is removed"
    - "An E2E table-driven test exercises real binary boot + bad-bearer + stream:true on all three surfaces and asserts pre-header 401 + Content-Type=application/json + zero SSE/NDJSON byte markers"
    - "Each adapter-level test asserts that the response body contains no SSE/NDJSON markers (data:, event:, \"done\":true) — the load-bearing header-leak regression detector"
  artifacts:
    - path: "internal/adapter/ollama/handlers_shortcircuit_test.go"
      provides: "Adapter-level streaming short-circuit tests for handleChat + handleGenerate (per D-11 and Pitfall 6 — Ollama gets TWO tests)"
      contains: "TestOllama_StreamingShortCircuit_Chat"
    - path: "internal/adapter/openai/handlers_shortcircuit_test.go"
      provides: "Adapter-level streaming short-circuit test for handleChatCompletions (per D-11)"
      contains: "TestOpenAI_StreamingShortCircuit_ChatCompletions"
    - path: "internal/adapter/anthropic/handlers_shortcircuit_test.go"
      provides: "Adapter-level streaming short-circuit test for handleMessages (per D-11)"
      contains: "TestAnthropic_StreamingShortCircuit_Messages"
    - path: "tests/e2e/plugin_chain_test.go"
      provides: "TestE2E_BadBearer_AllThreeSurfaces extended with three stream:true rows (per D-09 + D-10)"
      contains: "ollama_stream"
  key_links:
    - from: "internal/adapter/ollama/handlers_shortcircuit_test.go"
      to: "ollama handleChat / handleGenerate streaming short-circuit guard"
      via: "fakeRunHandle with non-nil scResp + stream:true request body"
      pattern: "ShortCircuitResponse"
    - from: "internal/adapter/openai/handlers_shortcircuit_test.go"
      to: "openai handleChatCompletions streaming short-circuit guard"
      via: "fakeRunHandle with non-nil scResp + stream:true request body"
      pattern: "ShortCircuitResponse"
    - from: "internal/adapter/anthropic/handlers_shortcircuit_test.go"
      to: "anthropic handleMessages streaming short-circuit guard"
      via: "fakeRunHandle with non-nil scResp + stream:true request body"
      pattern: "ShortCircuitResponse"
    - from: "tests/e2e/plugin_chain_test.go new stream:true rows"
      to: "real gateway binary + bad-bearer per-surface assertions"
      via: "table-driven row with assertEnvelope checking 401 + Content-Type + envelope shape + no byte markers"
      pattern: "ollama_stream|openai_stream|anthropic_stream"
---

<objective>
Prove the Plan 01 short-circuit guards work and stay correct under future refactors. Add (a) four adapter-level streaming-short-circuit tests in three new `handlers_shortcircuit_test.go` files — Ollama gets two tests per Pitfall 6 (handleChat + handleGenerate are independent streaming branches), OpenAI gets one, Anthropic gets one — and (b) three new `stream:true` rows on `TestE2E_BadBearer_AllThreeSurfaces` in `tests/e2e/plugin_chain_test.go` per D-09 + D-10.

To make the adapter-level tests possible without re-architecting existing fakes, extend each surface's `fakeRunHandle` with a synthetic `scResp *canonical.ChatResponse` field that the existing nil-returning `ShortCircuitResponse()` stub (added in Plan 01) now consults — when `scResp != nil` the method returns it, otherwise returns nil (preserving every existing test's behavior).

Purpose: D-10 negative byte-marker assertions (no `data:`, no `event:`, no `"done":true`) plus the positive `Content-Type: application/json` assertion are the strongest regression detectors against the Pitfall 3 header-leak pattern. The adapter-level tests give sub-100ms feedback; the E2E rows give full binary-boot wire-shape coverage. Together they make INTEG-01 a durable closure rather than a one-time fix.

Output: All four adapter-level tests green; `TestE2E_BadBearer_AllThreeSurfaces` runs six rows (three non-streaming pre-existing + three new streaming) with all six green; `make test-race` green; `goleak.VerifyTestMain` (already wired per-package via `testmain_test.go`) inherits to the new test files automatically with no per-file setup needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-CONTEXT.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-RESEARCH.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-PATTERNS.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-01-SUMMARY.md

@internal/adapter/anthropic/handlers_test.go
@internal/adapter/ollama/ndjson_test.go
@internal/adapter/openai/sse_golden_test.go
@tests/e2e/plugin_chain_test.go
@internal/canonical
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapter-level streaming short-circuit tests for all four sites (Pattern E)</name>
  <files>
    internal/adapter/ollama/handlers_shortcircuit_test.go,
    internal/adapter/openai/handlers_shortcircuit_test.go,
    internal/adapter/anthropic/handlers_shortcircuit_test.go,
    internal/adapter/ollama/ndjson_test.go,
    internal/adapter/openai/sse_golden_test.go,
    internal/adapter/anthropic/handlers_test.go
  </files>
  <behavior>
    Behavior 1 — Ollama handleChat: POST `/api/chat` with `Authorization: Bearer bad` and body containing `"stream":true`, where the test-injected fakeEngine returns a fakeRunHandle with `scResp = newAuthErrorResp()`. Response MUST: have status 401; have Content-Type `application/json`; decode as `{"error":"<non-empty string>"}`; body MUST NOT contain `data: `, `event: `, or `"done":true`.

    Behavior 2 — Ollama handleGenerate: POST `/api/generate` with same setup. Same response invariants. Per Pitfall 6 this is independently registered on chi and the guard is at a different line — needs its own test.

    Behavior 3 — OpenAI handleChatCompletions: POST `/v1/chat/completions` with same bad-bearer + `"stream":true` setup. Response MUST: have status 401; have Content-Type `application/json`; decode as `{"error":{"message":"<non-empty>","type":"authentication_error"}}` (note `type` field — Pitfall 5); body MUST NOT contain `data: `, `data: [DONE]`, or `event: `.

    Behavior 4 — Anthropic handleMessages: POST `/v1/messages` with header `anthropic-version: 2023-06-01`, bad-bearer, body `"stream":true`. Response MUST: have status 401; have Content-Type `application/json`; decode as `{"type":"error","error":{"type":"authentication_error","message":"<non-empty>"}}`; body MUST NOT contain `event: ` (the strongest Anthropic SSE-leak marker per RESEARCH "load-bearing assertion for that surface") nor `event: message_start` nor `data: `.

    Behavior 5 — happy-path regression baseline: existing fakeRunHandle behavior (scResp nil → ShortCircuitResponse returns nil → handler falls through to the emitter as before) MUST continue to work. Every existing adapter test in the three packages must still pass with the extended fakeRunHandle definition.
  </behavior>
  <read_first>
    - internal/adapter/anthropic/handlers_test.go (lines 143-150 — fakeStream shape; lines 152-167 — fakeRunHandle struct + methods + ShortCircuitResponse stub at 161; line 167 — newTestAdapter; line 176 — doPost helper; line 210 — assertErrorEnvelope helper) — the test-harness template Pattern E references
    - internal/adapter/ollama/ndjson_test.go (lines 38-45 — current fakeRunHandle struct + methods + Plan 01 stub) — must be extended in-place with the synthetic scResp field
    - internal/adapter/openai/sse_golden_test.go (lines 108-115 — current fakeRunHandle struct + methods + Plan 01 stub) — same
    - internal/adapter/anthropic/handlers_test.go (lines 152-161 — existing fakeRunHandle with ShortCircuitResponse stub at 161) — same extension shape; field is already nil-defaulted but needs the scResp wiring
    - internal/adapter/ollama/handlers_test.go (existing handler-level test harness: fakeEngine, newTestAdapter equivalent, doPost equivalent, assertErrorEnvelope equivalent) — reuse helpers
    - internal/adapter/openai/handlers_test.go (same)
    - internal/canonical types: ChatResponse, Message, ContentPart, ContentKindText, RoleAssistant, StopError, StopReason — confirm field names before constructing newAuthErrorResp helper
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern E around lines 243-303; the wire-assertion shape table around lines 295-302)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-11 (adapter-level test count; Pitfall 6 makes Ollama get 2)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Wave 0 Gaps" section (around lines 696-707) — the sampling-rate analysis explaining why all three negative byte-marker assertions must hold simultaneously
  </read_first>
  <action>
    Per Pattern E + D-11 + Pitfall 6:

    Step 1 — Extend each surface's existing fakeRunHandle (preserving the Plan 01 stub shape):
    - internal/adapter/ollama/ndjson_test.go: add an exported-within-package field `scResp *canonical.ChatResponse` (no tag) to the fakeRunHandle struct around line 38. Rewrite the Plan 01 stub `func (h *fakeRunHandle) ShortCircuitResponse() *canonical.ChatResponse { return nil }` to instead return `h.scResp`. Default zero-value remains nil so every existing test that didn't set scResp keeps the existing behavior.
    - internal/adapter/openai/sse_golden_test.go: same change to the fakeRunHandle struct around line 108. Pointer receiver `*fakeRunHandle` matches existing methods.
    - internal/adapter/anthropic/handlers_test.go: same change to the fakeRunHandle struct around line 152. The Plan 01 stub already lives at line 161; rewrite it to return the field.

    Step 2 — Create the three new test files per Pattern E:

    File: internal/adapter/ollama/handlers_shortcircuit_test.go (NEW)
    - Package: `package ollama` (whitebox test, same package — so it sees fakeRunHandle).
    - Helper: `newAuthErrorResp() *canonical.ChatResponse` returning a value with `Message.Content[0]` set to `{Kind: canonical.ContentKindText, Text: "Invalid or missing API key"}`, `Message.Role: canonical.RoleAssistant`, `StopReason: canonical.StopError`. Shape mirrors plugin.AuthHook.synthesizeAuthError per D-11 / Claude's discretion.
    - Test `TestOllama_StreamingShortCircuit_Chat`: construct fakeEngine (existing) whose Run returns a fakeRunHandle with `scResp: newAuthErrorResp()` and a closed empty chunk channel. Build adapter via newTestAdapter equivalent (reuse existing helper from handlers_test.go or ndjson_test.go), POST `/api/chat` with body `{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":true}`. Assert: w.Code == 401; w.Header().Get("Content-Type") == "application/json"; body decodes via `var env struct{Error *string \`json:"error"\`}` to a non-nil non-empty Error; `bytes.Contains(body, []byte("data: "))` is false; `bytes.Contains(body, []byte("event: "))` is false; `bytes.Contains(body, []byte("\"done\":true"))` is false.
    - Test `TestOllama_StreamingShortCircuit_Generate`: same fakeEngine + fakeRunHandle wiring. POST `/api/generate` with body `{"model":"auto","prompt":"hi","stream":true}` (use the existing per-handler request shape from ndjson_test.go or handlers_test.go for /api/generate). Same five assertions. Per Pitfall 6 this is a separate test, not a sub-case.

    File: internal/adapter/openai/handlers_shortcircuit_test.go (NEW)
    - Package: `package openai`.
    - Helper: same `newAuthErrorResp()` shape.
    - Test `TestOpenAI_StreamingShortCircuit_ChatCompletions`: construct fakeEngine whose Run returns fakeRunHandle with scResp non-nil; POST `/v1/chat/completions` with `"stream":true` body; assert 401 + Content-Type application/json + body decodes as `{Error: {Message, Type}}` with `Type == "authentication_error"` (Pitfall 5 — must match the errAuthentication constant); body MUST NOT contain `data: `, `data: [DONE]`, `event: `.
    - Do NOT add a test for handleCompletions — it does not have a streaming branch (RESEARCH "OpenAI has only ONE streaming branch"; line 227 silently downgrades stream:true to false).

    File: internal/adapter/anthropic/handlers_shortcircuit_test.go (NEW)
    - Package: `package anthropic`.
    - Helper: same `newAuthErrorResp()` shape.
    - Test `TestAnthropic_StreamingShortCircuit_Messages`: construct fakeEngine whose Run returns fakeRunHandle with scResp non-nil; POST `/v1/messages` with header `anthropic-version: 2023-06-01` and body `{"model":"auto","max_tokens":16,"messages":[{"role":"user","content":"hi"}],"stream":true}`; assert 401 + Content-Type application/json + body decodes as `{Type:"error", Error:{Type:"authentication_error", Message}}`; body MUST NOT contain `event: ` (Anthropic SSE opens with `event: message_start\n` per RESEARCH — this is the load-bearing negative marker for this surface), nor `event: message_start`, nor `data: `.

    Step 3 — Goleak inheritance: do NOT add `goleak.VerifyTestMain` to the new files. All three packages already have `goleak.VerifyTestMain` in `testmain_test.go` per RESEARCH §"Claude's Discretion / Goleak gating" verified note; the new files inherit the gate automatically.

    Constraints:
    - Do NOT create new helpers in production code. The synthetic `newAuthErrorResp()` lives in test files only.
    - Do NOT modify the production short-circuit guards from Plan 01 — this plan tests them, doesn't change them.
    - Use whitebox testing (same package) so the extended fakeRunHandle struct is reachable.
    - Each test file is small (~100 lines including helper); the new file boundary keeps the dual-discriminator pattern discoverable per Claude's discretion recommendation in RESEARCH.

    After all three test files exist and the three fakeRunHandle extensions are in place, run `go test -race -count=1 ./internal/adapter/...` and confirm all four new test names pass plus all existing tests in those packages still pass (Behavior 5 baseline).
  </action>
  <verify>
    <automated>go test -race -count=1 -run 'TestOllama_StreamingShortCircuit_Chat|TestOllama_StreamingShortCircuit_Generate|TestOpenAI_StreamingShortCircuit_ChatCompletions|TestAnthropic_StreamingShortCircuit_Messages' ./internal/adapter/...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f internal/adapter/ollama/handlers_shortcircuit_test.go && test -f internal/adapter/openai/handlers_shortcircuit_test.go && test -f internal/adapter/anthropic/handlers_shortcircuit_test.go` succeeds
    - Source: `grep -c '^func Test' internal/adapter/ollama/handlers_shortcircuit_test.go` returns at least 2 (Pitfall 6 — chat + generate as separate tests)
    - Source: each of the three new files declares a `newAuthErrorResp` helper returning `*canonical.ChatResponse` with `StopReason: canonical.StopError`
    - Source: each test file contains a `bytes.Contains` negative assertion against `data: ` or `event: ` (regression detector for header-leak per Pitfall 3)
    - Source: each surface's fakeRunHandle now has an `scResp *canonical.ChatResponse` field; the ShortCircuitResponse method returns `h.scResp` (not the bare `return nil` from Plan 01)
    - CLI: `go test -race -count=1 -run TestOllama_StreamingShortCircuit_Chat ./internal/adapter/ollama/` exits 0
    - CLI: `go test -race -count=1 -run TestOllama_StreamingShortCircuit_Generate ./internal/adapter/ollama/` exits 0
    - CLI: `go test -race -count=1 -run TestOpenAI_StreamingShortCircuit_ChatCompletions ./internal/adapter/openai/` exits 0
    - CLI: `go test -race -count=1 -run TestAnthropic_StreamingShortCircuit_Messages ./internal/adapter/anthropic/` exits 0
    - CLI: `go test -race -count=1 ./internal/adapter/...` exits 0 (all existing tests still pass — Behavior 5 baseline)
  </acceptance_criteria>
  <done>
    Three new test files exist; four adapter-level streaming-short-circuit tests are green; the three fakeRunHandle definitions across ollama/openai/anthropic test files now consult a synthetic `scResp` field; existing test suite in each of the three packages still passes; goleak gate inherits automatically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend TestE2E_BadBearer_AllThreeSurfaces with three stream:true rows (Pattern F)</name>
  <files>tests/e2e/plugin_chain_test.go</files>
  <behavior>
    Behavior 1 — ollama_stream row: POST `/api/chat` against the booted real gateway binary with bad bearer and body containing `"stream":true`. Response status MUST be 401. Response Content-Type header MUST be `application/json`. Body MUST decode as `{"error":"<non-empty>"}`. Body MUST NOT contain the bytes `"done":true`.

    Behavior 2 — openai_stream row: POST `/v1/chat/completions` with bad bearer and `"stream":true` body. Status 401. Content-Type `application/json`. Body decodes as `{"error":{"message":"<non-empty>","type":"authentication_error"}}`. Body MUST NOT contain `data: ` or `data: [DONE]`.

    Behavior 3 — anthropic_stream row: POST `/v1/messages` with `anthropic-version: 2023-06-01` header, bad bearer, and `"stream":true` body. Status 401. Content-Type `application/json`. Body decodes as `{"type":"error","error":{"type":"authentication_error","message":"<non-empty>"}}`. Body MUST NOT contain `event: ` (per RESEARCH, the load-bearing Anthropic SSE-leak marker) nor `event: message_start`.

    Behavior 4 — non-regression: the existing three non-streaming rows (ollama, openai, anthropic; non-streaming bad-bearer) continue to pass with unchanged assertions and unchanged behavior. The table now runs 6 rows in one binary boot per D-09.
  </behavior>
  <read_first>
    - tests/e2e/plugin_chain_test.go (existing TestE2E_BadBearer_AllThreeSurfaces around line 238; per-row anonymous struct shape lines 253-334; assertEnvelope func signature line 251 declares `func(t *testing.T, body []byte)` — current signature lacks Content-Type access. The harness signature widening is part of this task per PATTERNS.md Pattern F note around line 400)
    - tests/e2e/plugin_chain_test.go (the binary boot helpers and `gateOrSkip`/`resolveKiro` — confirm these are still applicable; the existing non-streaming rows use them)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern F around lines 307-401; the new-row templates around lines 336-398; the harness signature widening note at line 400)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-09 (extend in-place, 6 rows total, naming convention), D-10 (the four wire assertions: 401 + Content-Type=application/json + native envelope + zero SSE/NDJSON byte markers), D-12 (no positive-control rows — happy-path streaming already covered by Phase 4)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Validation Architecture" Phase Requirements → Test Map (around lines 680-690) for the INTEG-01 E2E command shape
  </read_first>
  <action>
    Per Pattern F + D-09 + D-10:

    Step 1 — Harness signature widening (Pattern F note at line 400 of PATTERNS.md): the existing table struct's `assertEnvelope func(t *testing.T, body []byte)` field cannot reach the Content-Type header or read body markers without redundant captures. Widen the field to one of these shapes (recommended: the simpler `(t, body, ct)` form):
    - `assertEnvelope func(t *testing.T, body []byte, ct string)` — passes the Content-Type header value as a string argument.

    Update the three existing non-streaming rows to use the new signature. Non-streaming rows can leave the new `ct` argument unused via `_` or simply not assert against it (their existing assertions stay; only the function signature changes). The caller (the test loop body) computes `ct := resp.Header.Get("Content-Type")` after `resp, err := client.Do(req)` and passes it into assertEnvelope.

    Step 2 — Add three new rows after the existing three rows in the table per Pattern F templates around lines 336-398 of PATTERNS.md:

    Row 1 — ollama_stream:
    - name: `"ollama_stream"`
    - path: `/api/chat`
    - body: JSON with `"stream":true`, model `"auto"`, single user message `"hi"`
    - assertEnvelope: asserts ct == "application/json"; asserts body does NOT contain `"done":true`; decodes body into `var env struct{Error *string \`json:"error"\`}` and asserts Error non-nil and non-empty

    Row 2 — openai_stream:
    - name: `"openai_stream"`
    - path: `/v1/chat/completions`
    - body: JSON with `"stream":true`, model `"auto"`, single user message
    - assertEnvelope: asserts ct == "application/json"; asserts body does NOT contain `data: ` and does NOT contain `data: [DONE]`; decodes body into nested struct asserting Error.Type == "authentication_error" and Error.Message non-empty (Pitfall 5 — the Type field is load-bearing)

    Row 3 — anthropic_stream:
    - name: `"anthropic_stream"`
    - path: `/v1/messages`
    - headers: `{"anthropic-version": "2023-06-01"}`
    - body: JSON with `"stream":true`, model `"auto"`, max_tokens 16, single user message
    - assertEnvelope: asserts ct == "application/json"; asserts body does NOT contain `event: ` (the strongest catch per RESEARCH §"the load-bearing assertion for that surface") and does NOT contain `event: message_start`; decodes body into nested struct asserting Type == "error" and Error.Type == "authentication_error"

    Step 3 — Confirm the table now executes six rows in one binary boot. Do NOT add positive-control rows (D-12 — happy-path streaming already covered by Phase 4 D-09 tests).

    Step 4 — Do NOT touch any other E2E test. Do NOT add a parallel test file. Slice A's in-place extension policy (D-09) is the explicit decision.

    Run with the existing E2E harness: `OTTO_E2E=1 go test -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...`. If `kiro-cli` is unavailable the existing `gateOrSkip` helper skips cleanly — no change needed per RESEARCH Environment Availability note.
  </action>
  <verify>
    <automated>OTTO_E2E=1 go test -tags e2e -count=1 -timeout 120s -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'name:.*"ollama_stream"\|name:.*"openai_stream"\|name:.*"anthropic_stream"' tests/e2e/plugin_chain_test.go` returns 3
    - Source: `grep -c 'name:.*"ollama"\|name:.*"openai"\|name:.*"anthropic"' tests/e2e/plugin_chain_test.go` returns at least 3 (preserves the 3 existing non-streaming rows)
    - Source: at least one row references `bytes.Contains` or equivalent with `"data: "` as the negative byte-marker assertion; at least one row references `"event: "`; at least one row references `"done":true`
    - Source: the assertEnvelope function signature contains `ct string` or equivalent way to access the Content-Type header
    - CLI: `OTTO_E2E=1 go test -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...` exits 0 OR cleanly skips if kiro-cli is unavailable in the runner environment
    - CLI: `go vet ./...` exits 0 (table struct widening is syntactically correct)
    - Behavior: each of the six row names (3 existing + 3 new) reports PASS in the test output when kiro is available
  </acceptance_criteria>
  <done>
    TestE2E_BadBearer_AllThreeSurfaces now runs six rows per D-09; D-10's four wire assertions are encoded in the three new rows; the harness signature is widened to pass the Content-Type header into assertEnvelope; existing three non-streaming rows still pass; the E2E command in RESEARCH "Phase Requirements → Test Map" works as documented.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fakeRunHandle → handler | Same-package whitebox test fixture. Synthetic scResp field allows test code to drive the short-circuit branch without standing up a real PreHook chain. Boundary is owned by the test author. |
| E2E test client → real gateway binary | Untrusted bearer over real HTTP. The E2E test boots the actual otto-gateway binary and sends real requests; this is the most authentic wire-shape verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-HEADER-LEAK | Information Disclosure | runSSEEmitter / runNDJSONEmitter and any future inlined header-write before the short-circuit guard | mitigate | This plan's adapter-level tests + E2E rows are the regression gate. D-10 negative byte-marker assertions (no `data:`, no `event:`, no `"done":true`) plus the positive Content-Type assertion together detect any future inlining of header writes before the short-circuit check. Future contributor moving the guard after a header set fails these tests. |
| T-08.1-PARTIAL-FIX | Tampering (false-pass via incomplete coverage) | Ollama handleGenerate (the second streaming branch, easy to miss per Pitfall 6) | mitigate | Plan 02 splits Ollama coverage into two independently-named tests (`TestOllama_StreamingShortCircuit_Chat` + `TestOllama_StreamingShortCircuit_Generate`). A future contributor fixing one but not the other fails the corresponding test. |
| T-08.1-WIRE-TYPE-DRIFT | Tampering | OpenAI/Anthropic error envelope `type` field constant | mitigate | Per Pitfall 5: the Type field assertion (must equal `"authentication_error"`) catches any drift from `errAuthentication` to a different constant. Pi-SDK and `@anthropic-ai/sdk` use this field for retry logic. |
| T-08.1-EMPTY-BODY | Tampering | All four sites if writeError fails to render a body | mitigate | All four adapter-level tests + all three E2E rows assert non-empty Error.Message (or non-empty Error string for Ollama). |

</threat_model>

<verification>

After both tasks complete:

1. `go test -race -count=1 ./internal/adapter/...` exits 0 with the four new tests passing alongside all existing tests.
2. `OTTO_E2E=1 go test -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...` exits 0 (or skips cleanly when kiro is unavailable in the runner environment).
3. `make test-race` exits 0 (full repo race suite still green).
4. No new test file references `goleak.VerifyTestMain` directly (all four new tests inherit the gate from per-package testmain_test.go).
5. Commits: Task 1 can be one commit; Task 2 can be a second commit (atomic-commit discipline applies to Plan 01's interface/handler change, not to Plan 02's tests). Recommended: one commit per task for blast-radius hygiene.

</verification>

<success_criteria>

- Three new files exist: `internal/adapter/ollama/handlers_shortcircuit_test.go`, `internal/adapter/openai/handlers_shortcircuit_test.go`, `internal/adapter/anthropic/handlers_shortcircuit_test.go`
- Four new tests are green: `TestOllama_StreamingShortCircuit_Chat`, `TestOllama_StreamingShortCircuit_Generate`, `TestOpenAI_StreamingShortCircuit_ChatCompletions`, `TestAnthropic_StreamingShortCircuit_Messages`
- Each surface's fakeRunHandle now consults a synthetic `scResp` field; default zero-value nil preserves existing test behavior
- `TestE2E_BadBearer_AllThreeSurfaces` now executes six rows per D-09 with three new `stream:true` rows asserting D-10's full set of invariants
- `make test-race` exits 0; E2E test passes when kiro is available

</success_criteria>

<output>
Create `.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-02-SUMMARY.md` when done. Record: each new test file path; each new test name and pass status; the six-row E2E table layout (3 existing + 3 new); confirmation that goleak gate inherited automatically; the `make test-race` and E2E command outputs. Note that Slice A is now fully closed with both the production fix (Plan 01) and the regression coverage (Plan 02); the requirement IDs PLUG-02 / PLUG-04 / STRM-01 / STRM-02 / ANTH-01 / ANTH-02 can now move from partial → satisfied (executed in Plan 05's REQUIREMENTS.md refresh).
</output>
