---
phase: 08.1
slug: close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-28
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (stdlib) — `goleak.VerifyTestMain` already wired per adapter package via existing `testmain_test.go` |
| **Config file** | none — `go.mod` is the source of truth |
| **Quick run command** | `go test ./internal/adapter/...` |
| **Full suite command** | `make test-race` (after Plan 03 lands: `make ci` is the full canonical brief §3.12 sequence) |
| **Estimated runtime** | ~15 s quick · ~60 s full race · ~3 m E2E |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/adapter/...`
- **After every plan wave:** Run `make test-race`
- **Before `/gsd-verify-work`:** Full `make ci` (post-Plan-03) must be green; on the local dev host this is the brief §3.12 canonical sequence end-to-end
- **Max feedback latency:** < 60 s for the inner loop; ~3 min when E2E runs

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement(s) | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.1-01-01 | 01 | 1 | PLUG-02, PLUG-04, ANTH-01 | T-08.1-INTERFACE-DRIFT | RunHandle interfaces and main.go shims declare/forward `ShortCircuitResponse()`; existing fakes compile-stub it | unit | `go build ./... && go test ./internal/adapter/... -count=1` | ❌ W0 (new code) | ⬜ pending |
| 08.1-01-02 | 01 | 1 | PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 | T-08.1-AUTH-BYPASS-STREAM, T-08.1-WATCHDOG-PANIC, T-08.1-HEADER-LEAK | All 4 streaming branches (ollama×2, openai×1, anthropic×1) emit pre-header 401 + native JSON envelope on short-circuit; StopWatchdog called nil-safe | unit + go vet | `go vet ./... && go test ./internal/adapter/... -race -count=1` | ❌ W0 | ⬜ pending |
| 08.1-02-01 | 02 | 2 | PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 | T-08.1-AUTH-BYPASS-STREAM, T-08.1-HEADER-LEAK | Per-surface adapter-level tests catch any future short-circuit-guard regression (Ollama×2, OpenAI×1, Anthropic×1) | unit (adapter) | `go test ./internal/adapter/{ollama,openai,anthropic}/ -run ShortCircuit -race -count=1` | ❌ W0 (new files) | ⬜ pending |
| 08.1-02-02 | 02 | 2 | PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 | T-08.1-AUTH-BYPASS-STREAM | E2E binary boot exercises bad-bearer + stream:true across all three surfaces; asserts 401 + Content-Type=application/json + zero `data:`/`event:`/NDJSON-done markers | e2e | `OTTO_E2E=1 go test ./tests/e2e/ -run TestE2E_BadBearer_AllThreeSurfaces -count=1` | ❌ W0 | ⬜ pending |
| 08.1-03-01 | 03 | 3 | TRST-01..TRST-08, BLD-03 | T-08.1-CI-DRIFT | Makefile `ci` target chains the canonical brief §3.12 sequence (fmt-check → vet → build → lint → test-race → arch-lint → examples → govulncheck) | infra | `make ci` (exit 0 on clean checkout) | ❌ W0 (new targets) | ⬜ pending |
| 08.1-03-02 | 03 | 3 | TRST-01..TRST-08 | T-08.1-CI-DRIFT | `.github/workflows/ci.yml` runs explicit gofumpt -d, go vet, go build, and go test -run Example steps inline (no implicit delegation through `make test-race` only) | infra | `act -j ci` or remote run; locally: `yamllint .github/workflows/ci.yml && grep -E 'gofumpt -d\|go vet\|go build\|go test -run Example' .github/workflows/ci.yml` | ❌ W0 | ⬜ pending |
| 08.1-04-01 | 04 | 3 | AUTH-01, AUTH-02, OBSV-04, PLUG-06 | T-6.1-ADMIN-AUTH-EXEMPT (accepted v1 risk) | PROJECT.md visibly states /admin is auth-exempt by design (Phase 6.1 D-01) | doc-assertion | `grep -E 'auth-exempt' .planning/PROJECT.md` | ✅ (existing file) | ⬜ pending |
| 08.1-04-02 | 04 | 3 | AUTH-01, AUTH-02, OBSV-04 | T-8-AUTH-BYPASS (accepted v1 risk) | docs/operating.md early Auth-posture quick-reference subsection cross-links to existing v1 no-auth posture and Accepted v1 risks sections | doc-assertion | `grep -E 'Auth posture quick reference\|list-mode' docs/operating.md` | ✅ (existing file) | ⬜ pending |
| 08.1-05-01 | 05 | 4 | PLUG-06, OBSV-04 + (post-A) PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 | T-08.1-CHECKBOX-DRIFT | REQUIREMENTS.md traceability table refreshed — PLUG-06 + OBSV-04 rows present; "62 total" comment; AUTH-01/02 carve-out annotations | doc-assertion | `grep -E 'PLUG-06\|OBSV-04\|62 total' .planning/REQUIREMENTS.md` and audit cross-check script | ✅ (existing file) | ⬜ pending |
| 08.1-05-02 | 05 | 4 | (sweep all) PLUG-*, STRM-*, ANTH-*, AUTH-*, OBSV-*, TRST-* per audit | T-08.1-CHECKBOX-DRIFT | Every requirement marked "satisfied" in `.planning/v1.5-MILESTONE-AUDIT.md` Final Status column is `[x]` in REQUIREMENTS.md; partials that depend solely on Slice A are flipped post-Plan-02 | doc-assertion | `node bin/lib/audit-cross-check.cjs` (or equivalent: diff audit Final Status column vs REQUIREMENTS.md checkbox state) | ❌ W0 (sweep is the artifact) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Sampling continuity:* Wave 1 → Wave 2 connect via the adapter-level unit tests in Plan 02 (the cheapest signal that Slice A is correct). Wave 3 → Wave 4 connect via Plan 05's audit cross-check, which depends on Plan 02 (Slice A green) AND Plan 03 (trust-gate chain in place) AND Plan 04 (docs cross-links exist). No 3 consecutive tasks lack an automated `<verify>`.

---

## Wave 0 Requirements

- [x] `internal/adapter/ollama/handlers_shortcircuit_test.go` — new file in Plan 02 Task 1 (covers `handleChat` + `handleGenerate`, two test cases)
- [x] `internal/adapter/openai/handlers_shortcircuit_test.go` — new file in Plan 02 Task 1
- [x] `internal/adapter/anthropic/handlers_shortcircuit_test.go` — new file in Plan 02 Task 1
- [x] `internal/adapter/ollama/ndjson_test.go` — fakeRunHandle extended (Plan 02 Task 1) to carry the synthetic auth-error response
- [x] `internal/adapter/openai/sse_golden_test.go` — fakeRunHandle extended (Plan 02 Task 1)
- [x] `internal/adapter/anthropic/handlers_test.go` — existing fake extended (Plan 02 Task 1) per Pattern D
- [x] `tests/e2e/plugin_chain_test.go` — `TestE2E_BadBearer_AllThreeSurfaces` extended with stream:true table rows (Plan 02 Task 2)
- [x] `Makefile` — new `fmt-check`, `vet`, `examples` targets and an updated `ci` chain (Plan 03 Task 1)

*Test framework: `go test` is repo-standard; `goleak.VerifyTestMain` per package is already wired (RESEARCH.md confirms). No install needed; everything Wave-0 is captured as work in Plan 02 + Plan 03.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-compile smoke (last step of brief §3.12 chain) | TRST-08, BLD-03 | Cross-build is an existing repo target — automated in CI but the local trust-gate run on macOS dev box requires a network-available toolchain matrix; manual confirms the matrix runs cleanly | `make build-cross` (existing target) — confirm linux/amd64, linux/arm64, darwin/arm64, windows/amd64 all produce a binary; binary size sanity-check |
| WARNING-03 checkbox-flip sweep correctness | (sweep) | The flip is mechanical against the audit Final Status column but each row requires reading two cells — risk of dropped requirements is non-zero; manual eyeball on the diff after Plan 05's automated pass | After Plan 05 task 2: `git diff .planning/REQUIREMENTS.md` and visually confirm only `[ ] Pending` → `[x] Satisfied` flips for rows the audit marks "satisfied" |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60 s for the inner loop
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-28 (orchestrator pass after plan-checker WARNING-2 remediation)
