---
phase: 08.1
slug: close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (stdlib) |
| **Config file** | none — go.mod |
| **Quick run command** | `go test ./internal/adapter/...` |
| **Full suite command** | `make test-race` |
| **Estimated runtime** | ~60 seconds (race), ~15 seconds (quick) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/adapter/...`
- **After every plan wave:** Run `make test-race`
- **Before `/gsd-verify-work`:** Full suite must be green plus `make ci` (the canonical trust-gate sequence WARNING-01 establishes)
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

> Populated when PLAN.md tasks are finalized. Each task with a real code change gets one row keyed by task ID. Each WARNING fix gets one row keyed to a doc-source assertion.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.1-01-01 | 01 | 1 | PLUG-02 | T-8-AUTH-BYPASS / — | Streaming bad-bearer returns 401 + JSON envelope; no SSE/NDJSON header leak | unit (adapter) | `go test ./internal/adapter/ollama/ -run TestHandleChat_StreamingShortCircuit` | ❌ W0 (test added by Slice A) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/adapter/ollama/handlers_test.go` — add streaming short-circuit fake/test (or new `handlers_shortcircuit_test.go`)
- [ ] `internal/adapter/openai/handlers_test.go` — same
- [ ] `internal/adapter/anthropic/handlers_test.go` — same
- [ ] `tests/e2e/plugin_chain_test.go` — extend `TestE2E_BadBearer_AllThreeSurfaces` with `stream:true` table rows

*Test framework: `go test` is already in use across the repo; `goleak.VerifyTestMain` is already wired in each adapter package's `testmain_test.go`, so new test files inherit goroutine-leak detection automatically. No framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-compile smoke (WARNING-01 step) | TRST-* | Cross-build matrix is run in CI but is not part of `go test` | `make build-cross` (or equivalent target after WARNING-01 fix lands) — confirm linux/amd64, linux/arm64, darwin/arm64, windows/amd64 all produce a binary |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
