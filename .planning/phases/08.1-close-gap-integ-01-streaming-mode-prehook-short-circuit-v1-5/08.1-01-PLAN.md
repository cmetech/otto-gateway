---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapter/ollama/adapter.go
  - internal/adapter/openai/adapter.go
  - cmd/otto-gateway/main.go
  - internal/adapter/ollama/ndjson_test.go
  - internal/adapter/ollama/integration_test.go
  - internal/adapter/openai/sse_golden_test.go
  - internal/adapter/openai/integration_test.go
  - internal/adapter/ollama/handlers.go
  - internal/adapter/openai/handlers.go
  - internal/adapter/anthropic/handlers.go
autonomous: true
requirements: [PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02]
tags: [auth, streaming, gateway, prehook, short-circuit]

must_haves:
  truths:
    - "A bad-bearer streaming request on /api/chat returns HTTP 401 + JSON envelope before any NDJSON header is written"
    - "A bad-bearer streaming request on /api/generate returns HTTP 401 + JSON envelope before any NDJSON header is written"
    - "A bad-bearer streaming request on /v1/chat/completions returns HTTP 401 + OpenAI error envelope before any SSE header is written"
    - "A bad-bearer streaming request on /v1/messages returns HTTP 401 + Anthropic error envelope before any SSE header is written"
    - "go build ./... succeeds after the interface change because every consumer (main.go shims + test fakes) satisfies the new method"
    - "The disconnect watchdog is torn down on the short-circuit path via StopWatchdog() with a nil-guard"
  artifacts:
    - path: "internal/adapter/ollama/adapter.go"
      provides: "ollama.RunHandle interface with ShortCircuitResponse() method (per D-05)"
      contains: "ShortCircuitResponse() *canonical.ChatResponse"
    - path: "internal/adapter/openai/adapter.go"
      provides: "openai.RunHandle interface with ShortCircuitResponse() method (per D-05)"
      contains: "ShortCircuitResponse() *canonical.ChatResponse"
    - path: "cmd/otto-gateway/main.go"
      provides: "ollamaRunHandleAdapter + openaiRunHandleAdapter ShortCircuitResponse forwarders (per D-07)"
      contains: "func (h ollamaRunHandleAdapter) ShortCircuitResponse"
    - path: "internal/adapter/ollama/handlers.go"
      provides: "Streaming short-circuit guards in handleChat + handleGenerate (per D-01/D-02/D-03/D-04)"
      contains: "ShortCircuitResponse()"
    - path: "internal/adapter/openai/handlers.go"
      provides: "Streaming short-circuit guard in handleChatCompletions (per D-01/D-02/D-03/D-04)"
      contains: "ShortCircuitResponse()"
    - path: "internal/adapter/anthropic/handlers.go"
      provides: "Streaming short-circuit guard in handleMessages (per D-01/D-02/D-03/D-04)"
      contains: "ShortCircuitResponse()"
  key_links:
    - from: "internal/adapter/ollama/handlers.go handleChat / handleGenerate streaming branch"
      to: "runHandle.ShortCircuitResponse()"
      via: "post-eng.Run guard pre-runNDJSONEmitter"
      pattern: "ShortCircuitResponse\\(\\); sc != nil"
    - from: "internal/adapter/openai/handlers.go handleChatCompletions streaming branch"
      to: "runHandle.ShortCircuitResponse()"
      via: "post-eng.Run guard pre-runSSEEmitter"
      pattern: "ShortCircuitResponse\\(\\); sc != nil"
    - from: "internal/adapter/anthropic/handlers.go handleMessages streaming branch"
      to: "runHandle.ShortCircuitResponse()"
      via: "post-eng.Run guard pre-runSSEEmitter"
      pattern: "ShortCircuitResponse\\(\\); sc != nil"
    - from: "cmd/otto-gateway/main.go ollamaRunHandleAdapter + openaiRunHandleAdapter"
      to: "h.run.ShortCircuitResponse()"
      via: "delegation forwarder mirroring anthropicRunHandleAdapter at line 861-865"
      pattern: "h\\.run\\.ShortCircuitResponse\\(\\)"
---

<objective>
Close the INTEG-01 BLOCKER by extending `ollama.RunHandle` and `openai.RunHandle` with `ShortCircuitResponse() *canonical.ChatResponse` (D-05), forwarding it through `cmd/otto-gateway/main.go`'s per-surface shim adapters (D-07), propagating the stub to every existing test fake (D-08), and inserting the pre-header 401 + native JSON envelope guard into all four streaming handler sites (D-01..D-04). This plan is the **atomic-commit Slice A integration**: every change in this plan MUST land in a single commit because partial application breaks `go build` (Pitfall 1, Pitfall 2, Pitfall 7).

Purpose: Closes the streaming side of PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 — currently bad-bearer `stream:true` requests silently emit empty 200 SSE/NDJSON streams instead of 401. Pi-SDK hard-codes `stream:true`, so this is the most-trafficked client code path and the highest-severity v1.5 audit finding.

Output: Compiling repository with surface-symmetric pre-header 401 short-circuit on all four streaming sites; non-streaming behavior unchanged; existing tests still pass against the now-extended interfaces because every fake has the `return nil` stub. Wire-shape regressions are not yet detected — that's Plan 02's job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-CONTEXT.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-RESEARCH.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-PATTERNS.md

@internal/adapter/anthropic/adapter.go
@internal/adapter/anthropic/collect.go
@internal/adapter/anthropic/handlers.go
@internal/adapter/ollama/adapter.go
@internal/adapter/ollama/handlers.go
@internal/adapter/openai/adapter.go
@internal/adapter/openai/handlers.go
@cmd/otto-gateway/main.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ShortCircuitResponse() to ollama + openai RunHandle interfaces and forward through main.go shims (Patterns A + C + D)</name>
  <files>
    internal/adapter/ollama/adapter.go,
    internal/adapter/openai/adapter.go,
    cmd/otto-gateway/main.go,
    internal/adapter/ollama/ndjson_test.go,
    internal/adapter/ollama/integration_test.go,
    internal/adapter/openai/sse_golden_test.go,
    internal/adapter/openai/integration_test.go
  </files>
  <read_first>
    - internal/adapter/anthropic/adapter.go (lines 54-72) — verbatim template for the new interface method declaration and doc comment shape
    - internal/adapter/ollama/adapter.go (current RunHandle interface around line 52) — current method set this task extends
    - internal/adapter/openai/adapter.go (current RunHandle interface around line 53) — current method set this task extends
    - cmd/otto-gateway/main.go (lines 861-865 for the anthropicRunHandleAdapter.ShortCircuitResponse forwarder; lines 928-942 for ollamaRunHandleAdapter; lines 947-961 for openaiRunHandleAdapter) — verbatim templates
    - internal/adapter/anthropic/handlers_test.go (line 161 — fakeRunHandle.ShortCircuitResponse() stub) — verbatim template for the test-fake stub
    - internal/adapter/ollama/ndjson_test.go (lines 38-45 — fakeRunHandle struct + methods) — receiver pointer-ness must match (it's *fakeRunHandle)
    - internal/adapter/ollama/integration_test.go (lines 43-47 — testRunHandleAdapter struct + methods) — value receiver
    - internal/adapter/openai/sse_golden_test.go (lines 108-115 — fakeRunHandle struct + methods) — receiver pointer-ness is *fakeRunHandle
    - internal/adapter/openai/integration_test.go (lines 118-122 — realRunHandle struct + methods) — value receiver; this one is the engine-backed shim so it forwards to h.run.ShortCircuitResponse() (Pattern D variant 2)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern A excerpt around line 49; Pattern C excerpt around line 178; Pattern D excerpt around line 207) — the verified analog templates
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-05, D-07, D-08 blocks
  </read_first>
  <action>
    Execute Patterns A + C + D from 08.1-PATTERNS.md in one mechanical pass per D-05 + D-07 + D-08. Order MUST be: interface decls first, main.go shims second, test-fake stubs third — Pitfall 7 / Shared Pattern S5 atomic-commit discipline. Concrete sites:

    Pattern A (interface decls — per D-05):
    - internal/adapter/ollama/adapter.go RunHandle interface (line ~52): append the new method signature `ShortCircuitResponse() *canonical.ChatResponse` with the doc comment recommended in PATTERNS.md Pattern A excerpt (the dual-discriminator-aware version explicitly naming the 08.1 INTEG-01 fix and the non-streaming `StopReason == canonical.StopError` discriminator on Collect-returned responses). Import `internal/canonical` is already present.
    - internal/adapter/openai/adapter.go RunHandle interface (line ~53): same change. The doc comment text is parallel; mention OpenAI's non-streaming sibling at handlers.go:165 uses the StopReason discriminator.

    Pattern C (main.go shim forwarders — per D-07):
    - cmd/otto-gateway/main.go: insert `ShortCircuitResponse() *canonical.ChatResponse` forwarder method after line 942 (end of ollamaRunHandleAdapter methods). Verbatim shape from anthropicRunHandleAdapter at lines 861-865 — value receiver `h`, body `return h.run.ShortCircuitResponse()`. Doc comment cites "Phase 08.1 INTEG-01" and "Delegates to the concrete *engine.Run accessor".
    - cmd/otto-gateway/main.go: insert the same forwarder after line 961 (end of openaiRunHandleAdapter methods). Same shape.

    Pattern D (test-fake stub propagation — per D-08):
    - internal/adapter/ollama/ndjson_test.go: append `func (h *fakeRunHandle) ShortCircuitResponse() *canonical.ChatResponse { return nil }` at the end of the existing fakeRunHandle methods (after line 45). Pointer receiver `*fakeRunHandle` to match existing methods on the type.
    - internal/adapter/ollama/integration_test.go: append `func (h testRunHandleAdapter) ShortCircuitResponse() *canonical.ChatResponse { return nil }` at the end of testRunHandleAdapter methods (after line 47). Value receiver to match.
    - internal/adapter/openai/sse_golden_test.go: append `func (h *fakeRunHandle) ShortCircuitResponse() *canonical.ChatResponse { return nil }` at the end of fakeRunHandle methods (after line 115). Pointer receiver to match.
    - internal/adapter/openai/integration_test.go: append `func (h realRunHandle) ShortCircuitResponse() *canonical.ChatResponse { return h.run.ShortCircuitResponse() }` at the end of realRunHandle methods (after line 122). Value receiver; engine-backed shim forwards to h.run per Pattern D variant 2 (it mirrors the main.go production shim because it wraps a real *engine.Run).

    Do NOT touch internal/adapter/anthropic/* in this task (already complete). Do NOT touch any handlers.go file in this task — Task 2 handles that. Do NOT add or modify any test that exercises wire behavior — Plan 02 owns that.

    After all changes are in place, run `go build ./...` to confirm the interface change satisfies every consumer per Pitfall 1 + Pitfall 2 warning signs. The build MUST be green before proceeding to Task 2.
  </action>
  <verify>
    <automated>go build ./... && grep -c 'ShortCircuitResponse() \*canonical.ChatResponse' internal/adapter/ollama/adapter.go internal/adapter/openai/adapter.go cmd/otto-gateway/main.go internal/adapter/ollama/ndjson_test.go internal/adapter/ollama/integration_test.go internal/adapter/openai/sse_golden_test.go internal/adapter/openai/integration_test.go</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'ShortCircuitResponse() \*canonical.ChatResponse' internal/adapter/ollama/adapter.go` succeeds
    - Source: `grep -q 'ShortCircuitResponse() \*canonical.ChatResponse' internal/adapter/openai/adapter.go` succeeds
    - Source: `grep -c 'ShortCircuitResponse' cmd/otto-gateway/main.go` returns at least 3 (anthropic preexisting + 2 new for ollama + openai)
    - Source: each of the 4 test fake files contains a method whose name is exactly `ShortCircuitResponse`
    - CLI: `go build ./...` exits 0
    - CLI: `go vet ./...` exits 0
    - Behavior: existing test suite still compiles — `go test -count=1 ./internal/adapter/...` exits 0 (existing tests pass; new short-circuit behavior not yet asserted)
  </acceptance_criteria>
  <done>
    Interface method declared in ollama + openai RunHandle interfaces; main.go forwards via two new shim methods after lines 942 and 961; four test-fake files each contain a `ShortCircuitResponse` method with the correct receiver pointer-ness; `go build ./...` and existing `go test ./internal/adapter/...` are green; no handler-side behavior changed yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert pre-header 401 short-circuit guards in all four streaming handler sites (Pattern B)</name>
  <files>
    internal/adapter/ollama/handlers.go,
    internal/adapter/openai/handlers.go,
    internal/adapter/anthropic/handlers.go
  </files>
  <read_first>
    - internal/adapter/anthropic/collect.go (lines 60-73 — the canonical ShortCircuitResponse + StopWatchdog nil-guard template per Shared Pattern S3)
    - internal/adapter/ollama/handlers.go (lines 19-34 — shortCircuitMessage helper; lines 147-150 — non-streaming Ollama 3-arg writeError signature per Shared Pattern S2; lines 185-196 — handleChat streaming branch insertion site; lines 309-323 — handleGenerate streaming branch insertion site)
    - internal/adapter/openai/handlers.go (lines 17-30 — shortCircuitMessage helper; lines 159-168 — non-streaming OpenAI 4-arg writeError(w, status, errAuthentication, msg) signature; lines 132-142 — handleChatCompletions streaming branch insertion site)
    - internal/adapter/anthropic/handlers.go (lines 215-223 — non-streaming Anthropic 4-arg writeError(w, status, errAuthentication, msg) signature; lines 228-241 — shortCircuitMessage helper; lines 175-186 — handleMessages streaming branch insertion site)
    - internal/adapter/openai/errors.go (errAuthentication constant — confirm spelling)
    - internal/adapter/anthropic/errors.go (errAuthentication constant — confirm spelling)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern B excerpts around lines 86-167; Shared Pattern S1, S2, S3 around lines 543-585)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-01 (pre-header 401), D-02 (check site after eng.Run, before emitter), D-03 (StopWatchdog teardown), D-04 (hard-code http.StatusUnauthorized)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Common Pitfalls" section — Pitfall 3 (header-leak), Pitfall 4 (StopWatchdog nil panic), Pitfall 5 (wrong errorType constant), Pitfall 6 (Ollama has TWO branches)
  </read_first>
  <action>
    Insert the short-circuit guard block at four exact insertion points per Pattern B in 08.1-PATTERNS.md and the canonical sites table in 08.1-RESEARCH.md "Ask 3". The block in each case lives BETWEEN the `eng.Run` err-guard end and the emitter call (D-02). All four blocks share the same skeleton; only the `writeError` signature differs per surface (Shared Pattern S2 / Pitfall 5).

    Surface-specific instructions (per Pattern B and Shared Pattern S2):

    Site 1 — internal/adapter/ollama/handlers.go handleChat (insertion BETWEEN current line 192 err-guard close and line 193 `start := time.Now()`):
    - Local variable name is `run` (per handlers.go:188 — `run, err := eng.Run(streamCtx, req)`).
    - writeError signature is the 3-arg flat envelope: `writeError(w, http.StatusUnauthorized, shortCircuitMessage(sc))`.
    - StopWatchdog nil-guard mandatory per D-03 + Pitfall 4 (`if stop := run.StopWatchdog(); stop != nil { stop() }`).
    - Hard-coded `http.StatusUnauthorized` per D-04 — no derivation.
    - `return` immediately after writeError — no fall-through to runNDJSONEmitter.
    - Doc comment names "Phase 08.1 INTEG-01" and cross-references "non-streaming sibling at handlers.go:147-150".

    Site 2 — internal/adapter/ollama/handlers.go handleGenerate (insertion BETWEEN current line 316 err-guard close and line 317 `start := time.Now()`):
    - Same shape as Site 1. Per Pitfall 6, this site is independent of Site 1 — both must be inserted, easy to miss the second one. Local variable name is `run` (per handlers.go:312).

    Site 3 — internal/adapter/openai/handlers.go handleChatCompletions (insertion BETWEEN current line 141 err-guard close and line 142 `if err := runSSEEmitter(...)`):
    - Local variable name is `runHandle` (per handlers.go:134 — `runHandle, err := eng.Run(streamCtx, req)`).
    - writeError signature is the 4-arg nested envelope per Shared Pattern S2: `writeError(w, http.StatusUnauthorized, errAuthentication, shortCircuitMessage(sc))`. Reuses the `errAuthentication` constant already used at handlers.go:165-167.
    - StopWatchdog nil-guard mandatory.
    - Hard-coded `http.StatusUnauthorized`.
    - `return` immediately after writeError.

    Site 4 — internal/adapter/anthropic/handlers.go handleMessages (insertion BETWEEN current line 185 err-guard close and line 186 `if err := runSSEEmitter(...)`):
    - Local variable name is `runHandle` (per handlers.go:177 — `runHandle, err := eng.Run(streamCtx, req)`).
    - writeError signature is the 4-arg nested envelope: `writeError(w, http.StatusUnauthorized, errAuthentication, shortCircuitMessage(sc))`. Reuses the `errAuthentication` constant already used at handlers.go:220-223.
    - StopWatchdog nil-guard mandatory.
    - Hard-coded `http.StatusUnauthorized`.
    - `return` immediately after writeError.

    Each inserted block carries a comment naming "Phase 08.1 INTEG-01 D-01..D-04" plus the non-streaming-sibling line reference for that surface. Use the existing per-surface `shortCircuitMessage()` helper (Shared Pattern S1) verbatim — do NOT create new helpers, do NOT modify the helpers, do NOT push the check into the emitter (Anti-Pattern per 08.1-PATTERNS.md). Do NOT add a `StatusCode` field to `canonical.ChatResponse`. Do NOT introduce derived status codes (D-04). Do NOT touch handleCompletions in openai/handlers.go (per RESEARCH "OpenAI has only ONE streaming branch" — handleCompletions downgrades stream:true to false at line 227).

    After all four sites are in place, run `go vet ./...` (catches accidental shadow/unused) and `go build ./...` (catches any drift from interfaces) before committing.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -v '^\s*//' internal/adapter/ollama/handlers.go internal/adapter/openai/handlers.go internal/adapter/anthropic/handlers.go | grep -c 'ShortCircuitResponse(); sc != nil'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'ShortCircuitResponse(); sc != nil' internal/adapter/ollama/handlers.go` returns at least 2 (handleChat + handleGenerate sites; non-comment line bodies)
    - Source: `grep -c 'ShortCircuitResponse(); sc != nil' internal/adapter/openai/handlers.go` returns at least 1 (handleChatCompletions site)
    - Source: `grep -c 'ShortCircuitResponse(); sc != nil' internal/adapter/anthropic/handlers.go` returns at least 1 (handleMessages site)
    - Source: each of the four short-circuit blocks contains `http.StatusUnauthorized` as a literal (no `mapStatus`, no `derivedCode`) and contains the StopWatchdog nil-guard `stop != nil { stop() }` shape
    - Source: each Ollama short-circuit block uses the 3-arg writeError signature (no `errAuthentication`); each OpenAI and Anthropic block uses the 4-arg signature with `errAuthentication`
    - CLI: `go build ./...` exits 0
    - CLI: `go vet ./...` exits 0
    - CLI: `go test -race -count=1 ./internal/adapter/...` exits 0 (existing tests still pass — short-circuit branch is unreachable from existing fakes because their stubs return nil per Task 1)
  </acceptance_criteria>
  <done>
    Four streaming-handler sites contain the short-circuit guard inserted after eng.Run err-guard and before the SSE/NDJSON emitter call; each block uses the surface-correct writeError signature and the canonical StopWatchdog nil-guard; hard-coded 401 status; non-streaming behavior unchanged; `go build`, `go vet`, and the existing race test suite for the three adapter packages are green. Commit this task TOGETHER with Task 1 (Slice A atomic commit per Shared Pattern S5 / Pitfall 7).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → adapter handler | Untrusted bearer token + JSON body crosses here. AuthHook (PreHook) validates bearer; short-circuit envelope flows back as 401. INTEG-01 is the streaming-side gap in this boundary's enforcement. |
| Adapter handler → engine.Run | Trusted internal contract. `runHandle` is the adapter-owned interface; `ShortCircuitResponse()` is the new method bridging the engine's PreHook short-circuit decision into the streaming branch. |
| Engine → ACP subprocess | Out of scope for this plan (no ACP-side change). The short-circuit branch specifically *prevents* the subprocess from being engaged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-AUTH-BYPASS-STREAM | Tampering / Elevation of Privilege | Ollama/OpenAI/Anthropic streaming handlers; eng.Run boundary | mitigate | Per D-01..D-04 + Pattern B: insert pre-header 401 + native JSON envelope guard at all four streaming sites between eng.Run and the emitter call; verify with Plan 02 adapter-level + E2E tests. Closes the INTEG-01 BLOCKER. |
| T-08.1-HEADER-LEAK | Information Disclosure | runSSEEmitter / runNDJSONEmitter | mitigate | Guard sits BEFORE any SSE/NDJSON header set; `writeError` writes Content-Type=application/json. Plan 02 D-10 negative byte-marker assertions are the regression gate. This plan establishes the structural placement; Plan 02 proves the wire shape. |
| T-08.1-WATCHDOG-PANIC | Denial of Service | StopWatchdog teardown in short-circuit branch | mitigate | Per D-03 + Pitfall 4: mandatory `if stop := runHandle.StopWatchdog(); stop != nil { stop() }` nil-guard mirroring collect.go:69-71. Without the guard, a short-circuit Run (where stopWatchdog is nil per engine.go:150) panics. |
| T-08.1-INTERFACE-DRIFT | Integrity (false test-passes) | Test fakes after RunHandle interface change | mitigate | Per D-08 + Pattern D: add `ShortCircuitResponse() *canonical.ChatResponse { return nil }` stub to every fake in same atomic commit; compiler enforces the contract surface. Pitfall 2 / Pitfall 7 atomic-commit discipline. |
| T-08.1-DERIVED-STATUS | Tampering (drift from spec) | canonical.ChatResponse → HTTP status mapping | accept | v1 AuthHook is the only short-circuit producer and always means 401. Hard-coding 401 per D-04 keeps the streaming and non-streaming branches symmetric. Revisit when a non-401 producer (rate-limit → 429, content-mod → 403) lands. Tracked in Deferred Ideas. |
| T-8-AUTH-BYPASS (carry-over) | Tampering | Ollama list-mode stubs (/api/tags, /api/ps, ...) | accept | Out of scope for Plan 01 (annotation only happens in Plan 04). These endpoints have no model-execution surface; IP allowlist remains. Documented in REQUIREMENTS.md AUTH-01/02 + docs/operating.md by Plan 04. |

</threat_model>

<verification>

After both tasks complete:

1. `go build ./...` succeeds (interface change + shim forwarders + test stubs all in place).
2. `go vet ./...` succeeds (no shadowed identifiers, no unreachable branches in inserted blocks).
3. `go test -race -count=1 ./internal/adapter/...` exits 0 (existing tests still compile and pass; new short-circuit branch is unreachable from existing fakes because each fake's ShortCircuitResponse returns nil per Task 1).
4. `grep -c 'ShortCircuitResponse' cmd/otto-gateway/main.go` is at least 3 (Anthropic preexisting + Ollama + OpenAI forwarders).
5. `grep -c 'ShortCircuitResponse(); sc != nil' internal/adapter/{ollama,openai,anthropic}/handlers.go` totals at least 4 across the three files.
6. **Commit Tasks 1 and 2 as a single atomic commit** per Shared Pattern S5. Commit message: `feat(adapter): short-circuit streaming PreHook to pre-header 401 (INTEG-01)` (or similar). No partial commits — Pitfall 7.

</verification>

<success_criteria>

- ollama.RunHandle and openai.RunHandle interfaces declare `ShortCircuitResponse() *canonical.ChatResponse` with TRST-04-compliant doc comments
- cmd/otto-gateway/main.go contains ShortCircuitResponse forwarders for both ollamaRunHandleAdapter and openaiRunHandleAdapter, mirroring the anthropicRunHandleAdapter shape from lines 861-865
- Four test fakes (ollama/ndjson_test.go fakeRunHandle, ollama/integration_test.go testRunHandleAdapter, openai/sse_golden_test.go fakeRunHandle, openai/integration_test.go realRunHandle) each have a ShortCircuitResponse method with the correct receiver pointer-ness
- Four streaming handler sites (ollama/handleChat, ollama/handleGenerate, openai/handleChatCompletions, anthropic/handleMessages) each contain the pre-header 401 short-circuit guard with hard-coded http.StatusUnauthorized, surface-correct writeError signature, and StopWatchdog nil-guard
- `go build ./...`, `go vet ./...`, and `go test -race -count=1 ./internal/adapter/...` are all green
- Both tasks land in a single atomic commit

</success_criteria>

<output>
Create `.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-01-SUMMARY.md` when done. Record: exact file:line ranges modified for each of Patterns A/B/C/D; the commit SHA carrying the atomic Slice A integration; confirmation that `go build`, `go vet`, and `go test -race ./internal/adapter/...` are green; note that wire-shape regression coverage lands in Plan 02.
</output>
