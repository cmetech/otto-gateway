---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
verified: 2026-05-28T00:00:00Z
status: passed
verdict: verified
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  notes: "Initial verification of Phase 08.1 close-out."
deferred_acknowledged:
  - item: "Pre-existing gofumpt drift across cmd/ and internal/adapter/* surfaced by make fmt-check"
    tracked_in: ".planning/phases/08.1-.../deferred-items.md"
    origin_phases: ["Phase 2", "Phase 3.1", "Phase 8"]
    note: "NOT a Phase 08.1 failure — Plan 03's fmt-check target is working as designed (visibility outcome). Forbidden from being fixed in Plan 03's scope."
  - item: "internal/admin/tail_test.go uses testing.Context() requiring Go 1.24 while go.mod is Go 1.23"
    tracked_in: ".planning/phases/08.1-.../deferred-items.md"
    origin_phases: ["Phase 6.1"]
    note: "NOT a Phase 08.1 failure — surfaced by Plan 03's new make vet target (visibility outcome)."
---

# Phase 08.1: Close gap — INTEG-01 streaming-mode PreHook short-circuit + v1.5 audit WARNINGs Verification Report

**Phase Goal:** Close the v1.5 milestone audit's BLOCKER and four WARNINGs in one phase:
- (a) Slice A — fix INTEG-01 streaming-mode PreHook short-circuit so bad-bearer `stream:true` requests return pre-header 401 + native JSON envelope across all three surfaces (Ollama, OpenAI, Anthropic) instead of empty 200 SSE/NDJSON streams.
- (b) Slice B — close WARNING-01 (CI/Makefile trust-gate sequence), WARNING-02 (`/admin` annotation), WARNING-03 (REQUIREMENTS.md traceability refresh), WARNING-04 (T-8-AUTH-BYPASS annotation).

**Verified:** 2026-05-28
**Status:** passed
**Verdict:** verified
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Streaming short-circuit guard exists at all 4 sites and fires BEFORE any SSE/NDJSON header is written | VERIFIED | `grep -n "ShortCircuitResponse"` returns: ollama/handlers.go:198 (handleChat), ollama/handlers.go:337 (handleGenerate), openai/handlers.go:146 (handleChatCompletions), anthropic/handlers.go:191 (handleMessages). All 4 sites place the guard between `eng.Run` and the emitter call. Each writes `writeError(w, http.StatusUnauthorized, ...)` and returns immediately. Watchdog nil-guard `if stop := h.StopWatchdog(); stop != nil { stop() }` present in all 4 sites. |
| 2 | Regression tests (adapter unit + E2E binary boot) exist and pass | VERIFIED | `go test -race -count=1 ./internal/adapter/...` → all 3 adapter packages PASS (anthropic 1.497s, ollama 2.028s, openai 2.919s). `OTTO_E2E=1 go test -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...` → all 6 sub-tests PASS (3 non-streaming pre-existing + 3 new streaming rows). Adapter-level `TestOllama_StreamingShortCircuit_Chat`, `TestOllama_StreamingShortCircuit_Generate`, `TestOpenAI_StreamingShortCircuit_ChatCompletions`, `TestAnthropic_StreamingShortCircuit_Messages` all exist and assert negative SSE/NDJSON byte markers (`data: `, `event: `, `"done":true`) per Pitfall 6 and T-08.1-HEADER-LEAK. |
| 3 | Brief §3.12 trust-gate sequence explicit in BOTH Makefile and CI | VERIFIED | Makefile: `fmt-check:`, `vet:`, `examples:` targets present (lines 46, 63, 69) and chained explicitly in `ci: fmt-check vet build lint test-race arch-lint examples` at line 207. CI workflow (.github/workflows/ci.yml): `Install gofumpt`, `Format check (gofumpt -d)`, `Vet` (`go vet ./...`), `Build (host)` (`go build ./...`), `Example tests` (`go test -run Example ./...`) all present as discrete pipeline steps. |
| 4 | REQUIREMENTS.md refreshed: PLUG-06 + OBSV-04 rows added, "62 total" comment, all 62 checkboxes [x], 6 partials flipped, AUTH-01/02 annotated | VERIFIED | `grep -c '^- \[x\]'` → 62; `grep -c '^- \[ \]'` → 0; `grep -c '\| Complete \|'` → 62; `grep -c '\| Pending \|'` → 0; `grep -c '62 total'` → 1; `grep -c '58 total'` → 0; PLUG-06 row at line 196 and OBSV-04 row at line 203, both `Complete`. The 6 Slice A-blocked partials (PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02) are all `[x]`. AUTH-01 and AUTH-02 carry verbatim `**Carve-outs:**` annotations cross-referencing `### v1 no-auth posture` and `#### Accepted v1 risks` in docs/operating.md by literal section name. |
| 5 | Documentation annotations exist: PROJECT.md (Security carve-outs) + docs/operating.md (Auth posture quick reference) | VERIFIED | PROJECT.md line 135: `Security carve-outs` bullet enumerates both /admin auth-exempt-by-design (Phase 6.1 D-01) and Ollama list-mode-stub bypass. docs/operating.md line 47: `### Auth posture quick reference` subsection (well above the 250-line boundary) with cross-references to `### v1 no-auth posture` (line 411) and `#### Accepted v1 risks` (line 278) by literal section name. Canonical detail sections verified byte-identical (shifted 21 lines by Plan 04's insertion). |
| 6 | No regressions: `go build ./...` clean; adapter race tests green; `go vet` clean on touched packages | VERIFIED | `go build ./...` → exit 0. `go test -race -count=1 ./internal/adapter/...` → all PASS. `go vet ./internal/adapter/... ./cmd/... ./tests/e2e/...` → exit 0. (Pre-existing fmt-check + admin tail_test.go vet issues confirmed via deferred-items.md as out-of-scope; they are exactly the visibility outcome WARNING-01 was scoped to surface.) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/adapter/ollama/adapter.go` | RunHandle.ShortCircuitResponse() interface decl | VERIFIED | Line 73: `ShortCircuitResponse() *canonical.ChatResponse` (doc comment at lines 64-72 references Phase 08.1 INTEG-01 and the non-streaming sibling at handlers.go:147-150). |
| `internal/adapter/openai/adapter.go` | RunHandle.ShortCircuitResponse() interface decl | VERIFIED | Line 74: `ShortCircuitResponse() *canonical.ChatResponse` (doc comment at lines 65-73 references Phase 08.1 INTEG-01 and the non-streaming sibling at handlers.go:165-168). |
| `internal/adapter/anthropic/adapter.go` | RunHandle.ShortCircuitResponse() interface decl | VERIFIED | Line 72: `ShortCircuitResponse() *canonical.ChatResponse` (preexisting from Phase 8 SC1). |
| `cmd/otto-gateway/main.go` | Forwarding shims for all 3 surfaces | VERIFIED | anthropicRunHandleAdapter.ShortCircuitResponse line 863 (preexisting), ollamaRunHandleAdapter.ShortCircuitResponse line 946 (new), openaiRunHandleAdapter.ShortCircuitResponse line 971 (new). All forward to `h.run.ShortCircuitResponse()`. |
| `internal/adapter/ollama/handlers.go` | Streaming guard at handleChat AND handleGenerate | VERIFIED | handleChat guard at lines 193-207; handleGenerate guard at lines 332-344. Both use 3-arg `writeError(w, http.StatusUnauthorized, shortCircuitMessage(sc))` flat envelope per Ollama wire convention. |
| `internal/adapter/openai/handlers.go` | Streaming guard at handleChatCompletions | VERIFIED | Lines 142-154. Uses 4-arg `writeError(w, http.StatusUnauthorized, errAuthentication, shortCircuitMessage(sc))` nested envelope. |
| `internal/adapter/anthropic/handlers.go` | Streaming guard at handleMessages | VERIFIED | Lines 186-199. Uses 4-arg `writeError(w, http.StatusUnauthorized, errAuthentication, shortCircuitMessage(sc))` nested envelope. |
| `internal/adapter/ollama/handlers_shortcircuit_test.go` | Two tests for chat + generate per Pitfall 6 | VERIFIED | `TestOllama_StreamingShortCircuit_Chat` (line 166) + `TestOllama_StreamingShortCircuit_Generate` (line 182). Both assert status 401, exact Content-Type, native envelope, and absence of `data: `/`event: `/`"done":true` markers. |
| `internal/adapter/openai/handlers_shortcircuit_test.go` | One test for chat completions | VERIFIED | `TestOpenAI_StreamingShortCircuit_ChatCompletions` (line 142). Asserts status 401, `application/json` prefix Content-Type, nested envelope with `error.type == "authentication_error"`, absence of SSE markers. |
| `internal/adapter/anthropic/handlers_shortcircuit_test.go` | One test for messages | VERIFIED | `TestAnthropic_StreamingShortCircuit_Messages` (line 120). Asserts status 401, exact Content-Type, double-wrapped envelope (outer `type:"error"` + inner `error.type:"authentication_error"`), absence of `event: `/`event: message_start`/`data: ` markers. |
| `tests/e2e/plugin_chain_test.go` | TestE2E_BadBearer_AllThreeSurfaces extended with 3 stream:true rows | VERIFIED | Three new rows added at lines 364 (ollama_stream), 396 (openai_stream), 439 (anthropic_stream). `assertEnvelope` widened from `(t, body)` to `(t, body, ct)`. All 6 rows pass in E2E binary boot. |
| `Makefile` | `fmt-check`, `vet`, `examples` targets and explicit `ci` chain | VERIFIED | Targets at lines 46, 63, 69; `ci:` at line 207 chains them in brief §3.12 order. `.PHONY` updated at line 18. |
| `.github/workflows/ci.yml` | Explicit gofumpt -d, go vet, go build, Example tests | VERIFIED | Steps at lines 62-79 (Install gofumpt, Format check, Vet, Build) and line 106 (Example tests). All present and in-log readable. |
| `.planning/PROJECT.md` | Security carve-outs annotation | VERIFIED | Line 135 bullet enumerates both /admin (Phase 6.1 D-01) and Ollama list-mode-stub bypass with cross-refs to docs/operating.md. |
| `docs/operating.md` | Auth posture quick reference + canonical sections intact | VERIFIED | Quick reference subsection at line 47; canonical sections `#### Accepted v1 risks` (line 278) and `### v1 no-auth posture` (line 411) shifted but byte-identical. |
| `.planning/REQUIREMENTS.md` | PLUG-06 + OBSV-04 rows; 62 total; 62/62 [x]; 0 [ ]; AUTH-01/02 carve-out annotations | VERIFIED | All grep counts match expected. PLUG-06 row at line 196, OBSV-04 row at line 203, both Complete. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `eng.Run` (4 handler sites) | `runHandle.ShortCircuitResponse()` | direct method call after Run | WIRED | All 4 sites match the pattern `runHandle, err := eng.Run(streamCtx, req); ...; if sc := runHandle.ShortCircuitResponse(); sc != nil { ... return }` |
| `*engine.Run` | `ollama.RunHandle` interface | `cmd/otto-gateway/main.go:946` (ollamaRunHandleAdapter.ShortCircuitResponse) | WIRED | Returns `h.run.ShortCircuitResponse()` |
| `*engine.Run` | `openai.RunHandle` interface | `cmd/otto-gateway/main.go:971` (openaiRunHandleAdapter.ShortCircuitResponse) | WIRED | Returns `h.run.ShortCircuitResponse()` |
| `*engine.Run` | `anthropic.RunHandle` interface | `cmd/otto-gateway/main.go:863` (anthropicRunHandleAdapter.ShortCircuitResponse, preexisting) | WIRED | Returns `h.run.ShortCircuitResponse()` |
| AuthHook short-circuit response | Native 401 envelope per surface | `shortCircuitMessage()` + `writeError(401, ...)` per adapter | WIRED | Each adapter has its own `shortCircuitMessage()` helper that extracts `Message.Content[0].Text` and feeds the surface-specific writeError. E2E test asserts the wire envelope decodes correctly per surface. |
| Adapter-level tests | Plan 01 short-circuit guards | per-surface `shortCircuitFakeEngine` + `fakeRunHandle.scResp` field | WIRED | Each test file builds a fake engine that returns a handle with `scResp = newAuthErrorResp()`. The handler calls `runHandle.ShortCircuitResponse()`, hits the guard, fires writeError. Negative byte-marker assertions confirm the emitter never ran. |
| E2E rows | Real AuthHook chain | otto-gateway binary boot + `Bearer wrongtoken` + AUTH_TOKEN=`validtoken` | WIRED | The real binary boots via `bootGateway`, the canonical AuthHook short-circuits with synthetic auth-error `canonical.ChatResponse`, the streaming branch catches it via `runHandle.ShortCircuitResponse()` and renders the per-surface native 401 envelope before SSE/NDJSON headers are written. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `runHandle.ShortCircuitResponse()` | `*canonical.ChatResponse` (sc) | `*engine.Run` accessor (preexisting Phase 8 SC1) populated by AuthHook on PreHook short-circuit | Yes — E2E test confirms the live AuthHook produces a non-nil response with non-empty `Message.Content[0].Text` | FLOWING |
| `shortCircuitMessage(sc)` | string | extracts `sc.Message.Content[0].Text` per adapter helper | Yes — adapter-level tests assert non-empty error.message in the envelope; E2E tests assert non-empty body | FLOWING |
| AUTH-01/02 carve-out cross-refs | Section anchors `### v1 no-auth posture`, `#### Accepted v1 risks` | docs/operating.md (literal section names) | Yes — `grep -c 'v1 no-auth posture' docs/operating.md` → 2 (heading + cross-ref); `grep -c 'Accepted v1 risks' docs/operating.md` → 2 (heading + cross-ref) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Repo builds cleanly | `go build ./...` | exit 0, no output | PASS |
| Adapter race tests pass | `go test -race -count=1 ./internal/adapter/...` | 3 packages OK (anthropic 1.497s, ollama 2.028s, openai 2.919s) | PASS |
| New short-circuit unit tests pass | `go test -race -count=1 -run 'TestOllama_StreamingShortCircuit\|TestOpenAI_StreamingShortCircuit\|TestAnthropic_StreamingShortCircuit' ./internal/adapter/...` | 3 packages OK; all 4 named tests pass | PASS |
| E2E bad-bearer all 6 rows pass | `OTTO_E2E=1 go test -v -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...` | All 6 subtests PASS in 4.27s: ollama, openai, anthropic (non-streaming) + ollama_stream, openai_stream, anthropic_stream | PASS |
| go vet clean on touched packages | `go vet ./internal/adapter/... ./cmd/... ./tests/e2e/...` | exit 0 | PASS |
| Makefile ci chain ordering | `grep '^ci:' Makefile` | `ci: fmt-check vet build lint test-race arch-lint examples` — matches brief §3.12 canonical order | PASS |
| CI workflow has explicit steps | `grep -cE 'gofumpt -d\|go vet \./\.\.\.\|go build \./\.\.\.\|go test -run Example' .github/workflows/ci.yml` | All four patterns present (4 distinct steps in the workflow) | PASS |

### Requirements Coverage

Phase 08.1 declared requirements: PLUG-02, PLUG-04, PLUG-06, STRM-01, STRM-02, ANTH-01, ANTH-02, AUTH-01, AUTH-02, OBSV-04, TRST-01..TRST-08, BLD-03.

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| PLUG-02 | PreHook short-circuit renders in native surface shape | SATISFIED | All 4 streaming branches now render native 401 + JSON envelope on PreHook short-circuit (Plan 01); adapter-level + E2E tests pin the wire shape (Plan 02). Flipped to `[x]` in REQUIREMENTS.md line 71. |
| PLUG-04 | AuthHook day-one registration | SATISFIED | Behavior unchanged from Phase 8; streaming-path coverage gap now closed. `[x]` line 73. |
| PLUG-06 | Hooks introspection / day-one chain | SATISFIED | Added as `Complete` traceability row in Plan 05 (line 196); pre-existing bulleted definition was already present, now traced. |
| STRM-01 | Ollama NDJSON streaming | SATISFIED | StopError mapping gap closed by pre-header 401 guard; happy path unchanged. `[x]` line 33. |
| STRM-02 | OpenAI + Anthropic SSE streaming | SATISFIED | Same as STRM-01 (different surface). `[x]` line 34. |
| ANTH-01 | Anthropic Messages API shape | SATISFIED | Streaming short-circuit emits correct anthropic error envelope (`{type:"error", error:{type:"authentication_error", message:...}}`). `[x]` line 23. |
| ANTH-02 | Anthropic SSE event sequence | SATISFIED | Streaming short-circuit returns 401 + JSON before any SSE event is emitted (pre-header), preserving the contract that events imply a valid stream. `[x]` line 24. |
| AUTH-01 | Bearer auth via AUTH_TOKEN | SATISFIED | Bearer-token auth unchanged. Carve-out annotation for `/admin` + Ollama list-mode-stub bypass added per D-15 + D-17 with cross-refs to docs/operating.md. `[x]` line 79. |
| AUTH-02 | IP allowlist | SATISFIED | IP allowlist unchanged. Carve-out annotation added. `[x]` line 80. |
| OBSV-04 | Observability artifact | SATISFIED | Added as `Complete` traceability row (line 203). |
| TRST-01 | golangci-lint strict config | SATISFIED | CI workflow explicit golangci-lint step preserved; Makefile `lint` target retained. `[x]` line 96. |
| TRST-02 | govulncheck | SATISFIED | govulncheck step preserved in both Makefile and CI. `[x]` line 97. (Note: REVIEW WR-01 flagged unpinned version — quality issue, not goal-blocker.) |
| TRST-03 | go test -race | SATISFIED | `make test-race` and CI `-race` step preserved. `[x]` line 98. |
| TRST-04 | Architecture boundaries | SATISFIED | go-arch-lint step preserved. `[x]` line 99. |
| TRST-05 | (TRST-05 row in REQUIREMENTS.md) | SATISFIED | `[x]` line 100. |
| TRST-06 | (TRST-06 row) | SATISFIED | `[x]` line 101. |
| TRST-07 | (TRST-07 row) | SATISFIED | `[x]` line 102. |
| TRST-08 | Pre-commit hooks | SATISFIED | `[x]` line 103. |
| BLD-03 | Binary embeds version via -ldflags | SATISFIED | `[x]` line 91. |

All 19 declared requirements are SATISFIED.

### Anti-Patterns Found

Scanned modified files (handlers.go × 3, handlers_shortcircuit_test.go × 3, main.go, Makefile, ci.yml, PROJECT.md, operating.md, REQUIREMENTS.md) for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/placeholder/coming soon/will be here.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none in scope of phase changes) | — | — | — | No debt markers introduced or left in Phase 08.1 changes. |

The 2 WARNINGs from the in-phase code REVIEW (WR-01 govulncheck pin policy; WR-02 OpenAI test assertion asymmetry) are quality items, not goal-blockers, and do not affect verdict.

### Behavioral / Build Gates Run (Verifier Side)

These commands were executed by the verifier (not just the executor's self-check):

```
$ go build ./...
(exit 0)

$ go test -race -count=1 ./internal/adapter/...
ok      otto-gateway/internal/adapter/anthropic 1.497s
ok      otto-gateway/internal/adapter/ollama    2.028s
ok      otto-gateway/internal/adapter/openai    2.919s

$ go test -race -count=1 -run 'TestOllama_StreamingShortCircuit|TestOpenAI_StreamingShortCircuit|TestAnthropic_StreamingShortCircuit' ./internal/adapter/...
ok      otto-gateway/internal/adapter/anthropic 1.425s
ok      otto-gateway/internal/adapter/ollama    1.252s
ok      otto-gateway/internal/adapter/openai    1.583s

$ OTTO_E2E=1 go test -v -tags e2e -count=1 -run TestE2E_BadBearer_AllThreeSurfaces ./tests/e2e/...
--- PASS: TestE2E_BadBearer_AllThreeSurfaces (4.27s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/ollama (0.00s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/openai (0.00s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/anthropic (0.00s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/ollama_stream (0.00s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/openai_stream (0.00s)
    --- PASS: TestE2E_BadBearer_AllThreeSurfaces/anthropic_stream (0.00s)

$ go vet ./internal/adapter/... ./cmd/... ./tests/e2e/...
(exit 0)
```

All gates green from a fresh verifier process — not just executor self-report.

### Deferred Items (acknowledged, NOT counted against verdict)

Both items below were surfaced by Plan 03's new make targets working as designed — they are visibility outcomes from making the trust gates explicit, exactly what WARNING-01 was scoped to deliver:

1. **Pre-existing gofumpt drift** across 16+ files in cmd/ and internal/adapter/* (Phase 2/3.1/8 origin). Plan 03's scope boundary explicitly forbids touching files outside Makefile + ci.yml. Tracked in `deferred-items.md`.

2. **internal/admin/tail_test.go testing.Context() Go-1.24-requires-1.23-go.mod mismatch** (Phase 6.1 origin). Plan 03's scope boundary forbids modifying `internal/admin/*`. Tracked in `deferred-items.md`.

These will need a follow-up cleanup phase (run `make fmt` across tree + bump go.mod to 1.24 OR rewrite the testing.Context calls). They do NOT block the v1.5 milestone close.

### Human Verification Required

(none — all phase deliverables are programmatically verifiable; the manual cross-compile smoke item in 08.1-VALIDATION.md is a future Phase 9 concern, not a Phase 08.1 goal.)

### Gaps Summary

No gaps. All six observable truths VERIFIED, all 19 declared requirements SATISFIED, all build/test/vet gates green, all interface/handler/test/doc artifacts present and correctly wired, no debt markers introduced, no behavioral spot-check failures.

The phase goal — **Close INTEG-01 BLOCKER + the four v1.5 audit WARNINGs in one phase** — is achieved. The streaming-mode PreHook short-circuit now returns pre-header 401 + native JSON envelope across all 4 streaming sites (Ollama × 2 per Pitfall 6, OpenAI × 1, Anthropic × 1); WARNING-01/02/03/04 are durably closed in code (Makefile/CI) and docs (PROJECT.md, docs/operating.md, REQUIREMENTS.md).

**Recommended next step:** Re-run `/gsd-audit-milestone v1.5` to confirm INTEG-01 + 4 WARNINGs resolve; if clean, mark Phase 08.1 complete in ROADMAP.md and close the v1.5 milestone. Then schedule a small cleanup phase for the two deferred items above.

---

*Verified: 2026-05-28*
*Verifier: Claude (gsd-verifier)*
