---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
plan: 04
subsystem: docs
tags: [docs, security, auth, admin, warning-02, warning-04]
requires:
  - 08.1-02 (Plan 04 deferred its REQUIREMENTS.md AUTH-01/02 edits to Plan 05; PROJECT.md/operating.md edits are independent of Plan 02 code work)
provides:
  - .planning/PROJECT.md visibly carries Security carve-outs annotation surfacing both /admin auth-exempt-by-design (Phase 6.1 D-01) and Ollama list-mode-stub bypass (T-8-AUTH-BYPASS).
  - docs/operating.md has an early-document `### Auth posture quick reference` subsection (line 47, well below the line-250 boundary) cross-linking to the existing canonical detail sections by literal section name.
  - WARNING-02 (PROJECT.md + operating.md slices) durably annotated.
  - WARNING-04 (PROJECT.md slice + operating.md index slice) durably annotated.
affects:
  - .planning/PROJECT.md (Constraints section, new "Security carve-outs" bullet immediately after "Security" bullet)
  - docs/operating.md (new `## Auth posture` top-level section + `### Auth posture quick reference` subsection inserted between PowerShell quick-start and `## Subcommands`)
tech_stack:
  added: []
  patterns:
    - "Annotation-by-cross-reference (Pattern I from 08.1-PATTERNS.md): visibility surfaces in entry-point docs cross-link to canonical detail sections by literal section name rather than duplicating text."
key_files:
  created: []
  modified:
    - .planning/PROJECT.md
    - docs/operating.md
decisions:
  - D-15 (WARNING-02 PROJECT.md + operating.md slices): /admin auth-exempt carve-out surfaced via a new "Security carve-outs" Constraints bullet in PROJECT.md and an "Auth posture quick reference" subsection in operating.md. REQUIREMENTS.md slice deferred to Plan 05.
  - D-17 (WARNING-04 operating.md index slice): T-8-AUTH-BYPASS list-mode-stub annotation already exists at the canonical location (now lines 278-294 in operating.md, shifted from 257-273 by the 21-line insertion). This plan only ADDED an index reference in the new quick-reference subsection and a parallel listing in PROJECT.md; canonical text is byte-identical.
metrics:
  duration_human: "~5 minutes"
  completed: 2026-05-28
  tasks_completed: 2
  tasks_total: 2
  commits: 2
  files_modified: 2
---

# Phase 08.1 Plan 04: Close WARNING-02 + WARNING-04 (PROJECT.md + operating.md slices) Summary

**One-liner:** Surfaced both intentional v1 bearer-auth carve-outs (`/admin` exempt-by-design + Ollama list-mode-stub bypass) in PROJECT.md's Constraints section and at the top of docs/operating.md as a quick-reference index, cross-linking to the canonical detail sections by literal name without duplicating text.

## What Shipped

Two doc-only tasks, two atomic commits:

1. **Task 1 (commit `1ec4014`)** — Added a `Security carve-outs` bullet to `.planning/PROJECT.md`'s Constraints section immediately after the existing `Security` bullet (line 135). The bullet enumerates both carve-outs with cross-references to the canonical detail sections in `docs/operating.md`:
   - `/admin` UI is auth-exempt by design (Phase 6.1 D-01) → see `docs/operating.md` `### v1 no-auth posture`.
   - Ollama list-mode stubs (`/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) bypass `AuthHook`; `ALLOWED_IPS` still applies → see `docs/operating.md` `#### Accepted v1 risks`.

2. **Task 2 (commit `534b55f`)** — Added a new `## Auth posture` top-level section to `docs/operating.md` between the PowerShell quick-start (ends line 43) and `## Subcommands` (now line 70, was line 45). The section contains an `### Auth posture quick reference` subsection at line 47 with:
   - Two-sentence framing ("Otto Gateway enforces bearer-token auth via `AuthHook` on all model-execution routes. Two routes are exempt by intentional design…").
   - Bullet 1 — `/admin` carve-out with cross-reference to `### v1 no-auth posture` below.
   - Bullet 2 — Ollama list-mode-stub carve-out (full endpoint list) with cross-reference to `#### Accepted v1 risks` below.
   - Bold caveat at the bottom: "**Do not expose `:11434/admin` or the Ollama list-mode endpoints on untrusted networks without operator-side mitigation.**"

   Canonical detail sections at lines 278-294 (`#### Accepted v1 risks`) and 411-425 (`### v1 no-auth posture`) — both shifted down 21 lines from the pre-Task-2 positions (257-273 and 390-405) — are byte-identical. Optional T-8 bolding skipped: the existing `**bold prefix**` already carries enough visual weight.

## Exact Insertion Locations

**`.planning/PROJECT.md`** (pre-edit line 134-135 → post-edit line 134-138):
- Inserted a new bulleted Constraints item between the existing `Security` bullet (line 134) and the `Backward compat` bullet (was line 135, now line 138). Title: `Security carve-outs`.
- The Constraints section uses bulleted narrative (not the Key Decisions table); the new bullet matches that shape exactly.

**`docs/operating.md`** (pre-edit line 44 → post-edit line 45-67):
- Inserted a new `## Auth posture` heading at line 45 (was where the trailing blank line of the PowerShell quick-start lived).
- New `### Auth posture quick reference` subsection at line 47.
- Section ends at line 65 with the bold caveat; line 66 is a blank line; line 67 is `## Subcommands` (was line 45 before this plan).

Net delta: +3 lines in PROJECT.md, +21 lines in operating.md, 0 lines deleted from either.

## Verbatim Text Added

### `.planning/PROJECT.md` (inserted between lines 134 and 138)

```markdown
- **Security carve-outs**: Two intentional bearer-auth exemptions in v1; operators MUST be aware before exposing the gateway on untrusted networks.
  1. **`/admin` observability UI is auth-exempt by design** (Phase 6.1 D-01). The operator binds the gateway to localhost or fronts it with reverse-proxy auth (nginx `auth_basic`, oauth2-proxy, Cloudflare Access). See `docs/operating.md` section `### v1 no-auth posture` for the canonical rationale and operator guidance, and the `### Auth posture quick reference` index near the top of that doc.
  2. **Ollama list-mode stubs bypass `AuthHook`** (`/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) because they do not route through the canonical engine. The IP allowlist (`ALLOWED_IPS`) still applies. These endpoints have no model-execution surface — accepted v1 risk. See `docs/operating.md` section `#### Accepted v1 risks` (under `### Phase 8 — Plugin chain (hooks)`) for the durable annotation.
```

### `docs/operating.md` (inserted between former lines 43 and 45)

```markdown
## Auth posture

### Auth posture quick reference

Otto Gateway enforces bearer-token auth via `AuthHook` on all model-execution
routes. Two routes are exempt by intentional design; document them here so
operators don't surface them on untrusted networks.

- **`/admin` UI is auth-exempt by design** (Phase 6.1 D-01). Bind the gateway
  to localhost or front with reverse-proxy auth in production. See
  `### v1 no-auth posture` below (under `## Admin Observability UI`) for full
  rationale and operator guidance.
- **Ollama list-mode stubs bypass `AuthHook`** — `/api/tags`, `/api/ps`,
  `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`,
  `/api/create` — because they don't route through the canonical engine. The
  IP allowlist (`ALLOWED_IPS`) still applies. Accepted v1 risk — see
  `#### Accepted v1 risks` below (under `### Phase 8 — Plugin chain (hooks)`).

**Do not expose `:11434/admin` or the Ollama list-mode endpoints on untrusted
networks without operator-side mitigation.**
```

## Canonical Detail Sections Unchanged

Confirmed byte-identical (only line-shifted by the 21-line insertion):

- **`#### Accepted v1 risks`** — was lines 257-273, now lines 278-294. Verified the two existing T-8-AUTH-BYPASS bullets are intact.
- **`### v1 no-auth posture`** — was lines 390-405, now lines 411-425. Verified the deployment-on-untrusted-network MUST-either guidance is intact.

Per the plan: "do not duplicate; do not rewrite; do not optionally bold the existing T-8 bullet — the existing `**bold prefix**` already carries enough visual weight." All three constraints honored.

## Verification

Plan `<verification>` block (10 items):

1. `grep -c 'auth-exempt' .planning/PROJECT.md` → **2** (≥1 required) ✓
2. `grep -c '/admin' .planning/PROJECT.md` → **1** (≥1 required) ✓
3. `grep -c 'list-mode' .planning/PROJECT.md` → **1**, AND both `/api/tags` and `/api/ps` are present in the enumerated list ✓
4. `grep -c 'Auth posture quick reference' docs/operating.md` → **1** (≥1 required) ✓
5. Quick-reference line number: **47** (must be <250) ✓
6. `grep -c 'v1 no-auth posture' docs/operating.md` → **2** (existing detail heading at line 411 + new cross-reference at line 55) ✓
7. `grep -c 'Accepted v1 risks' docs/operating.md` → **2** (existing detail heading at line 278 + new cross-reference at line 61) ✓
8. Visual review of PROJECT.md: Constraints section reads cleanly, no malformed bullets, Key Decisions table intact ✓
9. Visual review of docs/operating.md: canonical detail sections at (now) 278-294 and 411-425 byte-identical to pre-edit state ✓
10. Plan 04 committed independently (Plan 03 is a separate worktree-agent in the same wave) ✓

## Deviations from Plan

**None.** The plan was executed exactly as written. No Rule 1/2/3 deviations; no Rule 4 architectural questions; no auth gates; no checkpoints.

The plan explicitly DEFERRED the REQUIREMENTS.md AUTH-01/02 text annotations to Plan 05 because Plan 05 already does the broad REQUIREMENTS.md refresh (WARNING-03). Plan 04 honored that deferral — REQUIREMENTS.md was not touched.

## Threat Flags

No new security-relevant surfaces introduced (zero code change, zero new gateway behavior, zero new env vars). The threat register entries in the plan (T-8-AUTH-BYPASS carry-over, T-6.1-ADMIN-AUTH-EXEMPT, T-08.1-DOC-DRIFT, T-08.1-UNDOC-CARVE-OUTS) are all `accept` or `mitigate` dispositions, all mitigated by the durable annotations this plan added:

- T-08.1-DOC-DRIFT: cross-references use literal section names (e.g., `"v1 no-auth posture"`, `"Accepted v1 risks"`) — grep-detectable. No line-number references that would drift.
- T-08.1-UNDOC-CARVE-OUTS: both carve-outs now appear in both common entry points (PROJECT.md project overview + docs/operating.md early index). Future audit re-runs grep `"auth-exempt"` or `"Auth posture quick reference"` and find durable text.

## Known Stubs

None. This plan is doc-only; no UI surfaces, no data sources, no placeholder values.

## Self-Check: PASSED

- ✓ `.planning/PROJECT.md` modified (commit `1ec4014`)
- ✓ `docs/operating.md` modified (commit `534b55f`)
- ✓ Commit `1ec4014` exists in `git log --oneline`
- ✓ Commit `534b55f` exists in `git log --oneline`
- ✓ Plan verification (10 items) all pass
- ✓ Files outside scope (REQUIREMENTS.md, Makefile, .github/workflows/, internal/, cmd/, tests/) NOT modified
- ✓ Canonical detail sections in operating.md byte-identical

## Commits

| Task | Commit | Files | Description |
|------|--------|-------|-------------|
| 1    | `1ec4014` | `.planning/PROJECT.md` | Add Security carve-outs annotation (WARNING-02 + WARNING-04 PROJECT.md slice) |
| 2    | `534b55f` | `docs/operating.md` | Add Auth posture quick reference subsection (WARNING-02 + WARNING-04 operating.md slice) |

## Handoff to Plan 05

Plan 05 owns the broad REQUIREMENTS.md refresh (WARNING-03) AND the deferred REQUIREMENTS.md AUTH-01/02 text annotations (WARNING-02 + WARNING-04 REQUIREMENTS.md slices). The cross-reference targets already exist in both `.planning/PROJECT.md` and `docs/operating.md`; Plan 05 can mirror the same cross-reference style ("see `docs/operating.md` `### v1 no-auth posture`") when annotating AUTH-01 and AUTH-02.

## TDD Gate Compliance

N/A — plan frontmatter is `type: execute` (not `type: tdd`); tasks have `tdd="false"`. No TDD gates apply.
