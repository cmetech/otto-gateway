---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
plan: 04
type: execute
wave: 3
depends_on: ["08.1-02"]
files_modified:
  - .planning/PROJECT.md
  - docs/operating.md
autonomous: true
requirements: [AUTH-01, AUTH-02, OBSV-04, PLUG-06]
tags: [docs, security, auth, admin]

must_haves:
  truths:
    - "PROJECT.md visibly states /admin is auth-exempt by design (Phase 6.1 D-01) and points operators to the docs/operating.md canonical sections"
    - "docs/operating.md has an early-document Auth posture quick reference subsection cross-linking to both the v1 no-auth posture and Accepted v1 risks sections"
    - "The /admin auth-exempt carve-out language is discoverable from PROJECT.md, not just buried in operating.md"
    - "The T-8-AUTH-BYPASS list-mode-stub annotation language is preserved in docs/operating.md (already exists at lines 257-273) and surfaceable from PROJECT.md"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Security carve-outs annotation near the existing Security Key Decision row (per D-15) calling out /admin auth-exempt and list-mode-stub bypass"
      contains: "auth-exempt"
    - path: "docs/operating.md"
      provides: "Auth posture quick reference subsection cross-linking to v1 no-auth posture (existing line 390-405) and Accepted v1 risks (existing line 257-273)"
      contains: "Auth posture quick reference"
  key_links:
    - from: ".planning/PROJECT.md Security row"
      to: "docs/operating.md sections (v1 no-auth posture, Accepted v1 risks)"
      via: "explicit cross-references in the carve-out annotation"
      pattern: "operating\\.md"
    - from: "docs/operating.md Auth posture quick reference subsection"
      to: "v1 no-auth posture + Accepted v1 risks sections"
      via: "internal markdown link or in-line section reference"
      pattern: "no-auth posture"
---

<objective>
Close WARNING-02 (`/admin` auth-exempt-by-design annotation) and WARNING-04 (T-8-AUTH-BYPASS list-mode-stub annotation) doc-side per D-15 and D-17, with the caveat that this plan ONLY touches PROJECT.md and docs/operating.md. The REQUIREMENTS.md AUTH-01/02 text annotations are deferred to Plan 05 because Plan 05 already does the broad REQUIREMENTS.md refresh (WARNING-03) and stacking them avoids two consecutive plans both touching the same file.

WARNING-02 background: The Phase 6.1 D-01 decision intentionally shipped `/admin` as auth-exempt (operator chooses bind-localhost or reverse-proxy). PROJECT.md doesn't currently call this out; an operator reading PROJECT.md alone would assume all routes are auth-protected when `AUTH_TOKEN` is set. Per RESEARCH §"Ask 7", docs/operating.md already has the canonical text at lines 390-405 (`### v1 no-auth posture` under `## Admin Observability UI`). This plan adds a Security carve-outs annotation to PROJECT.md and an `### Auth posture quick reference` subsection earlier in docs/operating.md that serves as the index point to the existing detail sections.

WARNING-04 background: The Ollama list-mode stubs (`/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) bypass `AuthHook` because they don't route through the canonical engine — IP allowlist remains. Per RESEARCH §"Ask 7", docs/operating.md already has the canonical annotation at lines 257-273 (`#### Accepted v1 risks` under `### Phase 8 — Plugin chain (hooks)`). This plan's job is NOT to rewrite that section but to make it discoverable from the Auth posture quick reference and to surface the existence of the accepted-risk in PROJECT.md.

Purpose: Per RESEARCH §"WARNING-02 is doc-only because the intentional decision is correct" and §"WARNING-04 is the only accepted risk in v1.5" — visibility is the audit-closing change. The annotations are durable records that prevent future audits from re-flagging the same findings as new.

Output: PROJECT.md has a visible Security carve-outs annotation; docs/operating.md gains an `### Auth posture quick reference` index subsection (without rewriting the canonical detail sections); zero code change, zero new gateway behavior. REQUIREMENTS.md AUTH-01/02 text annotations are owned by Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-CONTEXT.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-RESEARCH.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-PATTERNS.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-02-SUMMARY.md

@.planning/PROJECT.md
@docs/operating.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Security carve-outs annotation to PROJECT.md (Pattern I — PROJECT.md slice)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md in full (most relevant: the Security row near line 134 per RESEARCH "Ask 7" and the surrounding Key Decisions / Constraints table)
    - docs/operating.md lines 257-273 (existing `#### Accepted v1 risks` section — quote nothing, just cross-reference)
    - docs/operating.md lines 390-405 (existing `### v1 no-auth posture` section — same)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern I around lines 514-538)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-15 (annotate in 3 places: PROJECT.md, REQUIREMENTS.md, docs/operating.md; this plan covers PROJECT.md only) and D-17 (T-8-AUTH-BYPASS annotation in REQUIREMENTS.md AUTH-01/02 + docs/operating.md; this plan does not yet touch REQUIREMENTS.md)
  </read_first>
  <action>
    Per Pattern I + D-15:

    Step 1 — Locate the Security row in PROJECT.md near line 134 (Key Decisions / Constraints table; the row that currently mentions `AUTH_TOKEN` and `ALLOWED_IPS` defaults). Read the surrounding rows to confirm the table format and column count.

    Step 2 — Add a "Security carve-outs" annotation. Two acceptable shapes:
    - Inline append to the existing Security row's last column (if the column is narrative-shaped — single sentence appended).
    - New separate row immediately below the existing Security row titled "Security carve-outs" with the carve-out text.

    Pick whichever matches the existing table's narrative style. The annotation text MUST name two things:
    1. `/admin` is auth-exempt by design (Phase 6.1 D-01) — operator binds localhost or fronts with reverse-proxy auth. Cross-reference `docs/operating.md` section `### v1 no-auth posture`.
    2. Ollama list-mode stubs (briefly list: `/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) bypass `AuthHook` because they don't route through the canonical engine; IP allowlist still applies. Cross-reference `docs/operating.md` section `#### Accepted v1 risks`.

    Step 3 — Keep wording terse: PROJECT.md is project-overview level, not operator manual. One short paragraph or two compact bullets — the canonical detail already lives in docs/operating.md and the carve-out text in CONTEXT.md D-15 + D-17 (verbatim or paraphrased; the CONTEXT.md D-17 quote block around lines 316-321 is the upper bound on the wording the planner should use; shorter is fine).

    Step 4 — Do NOT rewrite the existing Security row text. Do NOT add a new full section heading — annotation lives inside the Constraints/Key Decisions table area. Do NOT duplicate the docs/operating.md content; cross-reference it instead.

    Constraints:
    - The cross-references MUST be section-named (e.g., the literal string "v1 no-auth posture" or "Accepted v1 risks") so a reader can grep for the section. Bare path references break when sections are renamed.
    - Do NOT introduce new env vars (`ADMIN_AUTH_TOKEN` is explicitly Deferred per CONTEXT.md "Out of scope (locked)").
    - Do NOT introduce a new "Security" top-level section. The annotation rides on the existing structure.
  </action>
  <verify>
    <automated>grep -c 'auth-exempt' .planning/PROJECT.md; grep -c '/admin' .planning/PROJECT.md; grep -c 'operating.md' .planning/PROJECT.md; grep -c 'list-mode' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'auth-exempt' .planning/PROJECT.md` returns at least 1
    - Source: `grep -c '/admin' .planning/PROJECT.md` returns at least 1
    - Source: `grep -c 'operating.md' .planning/PROJECT.md` returns at least 1 (cross-reference present)
    - Source: PROJECT.md mentions either the literal string "list-mode" or enumerates at minimum `/api/tags` and `/api/ps` (the two most recognizable list-mode stubs)
    - Source: PROJECT.md references "Phase 6.1 D-01" or equivalent decision pointer so the historical context is preserved
    - Behavior: a developer skimming PROJECT.md alone can identify both carve-outs without needing to read docs/operating.md
  </acceptance_criteria>
  <done>
    PROJECT.md visibly carries the Security carve-outs annotation; both /admin and list-mode-stub bypasses are named; cross-references point to the docs/operating.md sections by name; nothing duplicated; nothing rewritten in the existing Security row.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Auth posture quick reference subsection to docs/operating.md (Pattern I — operating.md slice)</name>
  <files>docs/operating.md</files>
  <read_first>
    - docs/operating.md in full or at least lines 1-50 (top-of-document structure to pick the insertion point), lines 257-273 (existing `#### Accepted v1 risks` section), lines 355-405 (the `## Admin Observability UI` section including `### v1 no-auth posture` at line 390-405)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern I around lines 514-538)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Ask 7" (around lines 635-647) — the recommendation to add a single index point earlier in the doc that links to both detail sections
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-15 (operating.md security section gets the bold caveat) and D-17 (T-8-AUTH-BYPASS verbatim text already exists in operating.md — do not rewrite)
  </read_first>
  <action>
    Per Pattern I + D-15 + D-17 + RESEARCH "Ask 7":

    Step 1 — Pick the insertion point for the new `### Auth posture quick reference` subsection. Per RESEARCH "Ask 7" recommendation, the subsection should appear EARLY in the document (before the existing detail sections at lines 257-273 and 390-405) so operators encounter it first. Read lines 1-60 of operating.md to find a sensible parent heading; the current top-of-doc usually has `## Gateway config flags` or similar — the quick-reference subsection should live as an `###` under whichever `##` heading covers configuration/auth first. If no obvious parent exists, add it as a new `## Auth posture` top-level heading directly after the initial overview prose.

    Step 2 — Write the subsection. Required content:
    - Two-sentence framing: "Otto Gateway enforces bearer-token auth via AuthHook on all model-execution routes. Two routes are exempt by intentional design; document them here so operators don't surface them on untrusted networks."
    - Bullet 1 — `/admin`: "`/admin` UI is auth-exempt by design (Phase 6.1 D-01). Bind the gateway to localhost or front with reverse-proxy auth in production. See `### v1 no-auth posture` below for full rationale and operator guidance."
    - Bullet 2 — list-mode stubs: "Ollama list-mode stubs (`/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) bypass AuthHook because they don't route through the canonical engine. IP allowlist (`ALLOWED_IPS`) still applies. Accepted v1 risk — see `#### Accepted v1 risks` below."
    - One bold caveat line at the bottom: "**Do not expose `:11434/admin` or the Ollama list-mode endpoints on untrusted networks without operator-side mitigation.**"

    Step 3 — Do NOT rewrite, duplicate, or move the canonical detail sections at lines 257-273 (`#### Accepted v1 risks`) or 390-405 (`### v1 no-auth posture`). Reference them by section name only.

    Step 4 — Optionally bold the existing T-8-AUTH-BYPASS bullet at lines 257-273 (per RESEARCH "Ask 7" — "possibly bold the existing T-8-AUTH-BYPASS bullet for emphasis, but do not duplicate text") — this is a single line edit if the planner judges the existing bullet needs more visual weight. Skip if the existing formatting already stands out.

    Constraints:
    - Use markdown headings consistent with the existing doc style (probably `###` or `####`; match what's already there).
    - Cross-references use section names ("`### v1 no-auth posture`", "`#### Accepted v1 risks`") not line numbers — line numbers drift.
    - Do NOT mention `ADMIN_AUTH_TOKEN` (Deferred per CONTEXT.md "Out of scope").
    - Do NOT add operator instructions for using reverse-proxy auth — that detail belongs in `### v1 no-auth posture` (where it already lives at lines 390-405).
  </action>
  <verify>
    <automated>grep -c 'Auth posture quick reference' docs/operating.md; grep -c 'auth-exempt' docs/operating.md; grep -c 'list-mode' docs/operating.md; grep -c 'v1 no-auth posture' docs/operating.md; grep -c 'Accepted v1 risks' docs/operating.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'Auth posture quick reference' docs/operating.md` returns at least 1
    - Source: `grep -c '/admin' docs/operating.md` returns at least 2 (existing mentions plus the new subsection)
    - Source: `grep -c 'list-mode' docs/operating.md` returns at least 1 in the new subsection
    - Source: the new subsection appears in the FIRST quarter of the file (so it's encountered before the existing detail sections at lines 257-273 and 390-405) — verify with `grep -n 'Auth posture quick reference' docs/operating.md` and confirm the line number is well below 250
    - Source: the new subsection contains cross-references to both `### v1 no-auth posture` and `#### Accepted v1 risks` by literal section name
    - Source: the canonical detail sections at lines 257-273 and 390-405 are unchanged (or only have the existing T-8-AUTH-BYPASS bullet bolded — no other edits)
    - Behavior: an operator skimming the doc encounters the auth carve-outs index before getting to mid-doc detail
  </acceptance_criteria>
  <done>
    docs/operating.md has an early-document `### Auth posture quick reference` subsection naming both carve-outs and cross-linking to the existing detail sections; the existing canonical sections are untouched (other than optional bolding); operator now has one index point.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (human reader) → PROJECT.md | Operator reads PROJECT.md for project overview before deployment. Missing carve-out callout in this surface is a documentation gap, not a code vulnerability — but it leads to operator misconfiguration which IS a real risk. |
| Operator (human reader) → docs/operating.md | Operator reads operating.md for deployment guidance. Visibility of carve-outs in the index portion of the doc determines whether they're encountered or skipped. |
| Audit re-runner → annotated docs | Future `/gsd-audit-milestone` runs grep the annotated text; durable annotations prevent re-flagging the same findings as new WARNINGs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-BYPASS (carry-over, accepted v1 risk — WARNING-04) | Tampering | Ollama list-mode stubs | accept | This plan ANNOTATES the existing canonical text (already at docs/operating.md lines 257-273) and surfaces its existence in PROJECT.md per D-17. No code change. The annotation is the durable record. |
| T-6.1-ADMIN-AUTH-EXEMPT (WARNING-02) | Information Disclosure | `/admin` route bind/exposure | accept | Per Phase 6.1 D-01 intentional decision. Per D-15: annotate in PROJECT.md + REQUIREMENTS.md + docs/operating.md (this plan covers PROJECT.md + operating.md; REQUIREMENTS.md is in Plan 05). The operator-facing canonical text at lines 390-405 already specifies bind-localhost or reverse-proxy as the operator-side mitigation; this plan only surfaces the existence of that section earlier in the doc. |
| T-08.1-DOC-DRIFT | Repudiation (future audit re-flagging the same finding) | PROJECT.md / docs/operating.md cross-references | mitigate | Cross-references use section names (literal string matchable) rather than line numbers (which drift on file edits). |
| T-08.1-UNDOC-CARVE-OUTS | Information Disclosure (operator deploys on untrusted network unaware of carve-outs) | PROJECT.md missing the visibility annotation | mitigate | The Security carve-outs annotation in PROJECT.md and the Auth posture quick reference subsection in docs/operating.md together surface the carve-outs in both common entry points for project research. |

</threat_model>

<verification>

After both tasks complete:

1. `grep -c 'auth-exempt' .planning/PROJECT.md` returns at least 1.
2. `grep -c '/admin' .planning/PROJECT.md` returns at least 1.
3. `grep -c 'list-mode' .planning/PROJECT.md` returns at least 1 (or `/api/tags` and `/api/ps` are both grep-hit).
4. `grep -c 'Auth posture quick reference' docs/operating.md` returns at least 1.
5. `grep -n 'Auth posture quick reference' docs/operating.md` reports a line number below 250 (subsection appears before the existing detail sections).
6. `grep -c 'v1 no-auth posture' docs/operating.md` returns at least 2 (the existing detail heading at line 390 plus the new cross-reference in the quick reference subsection).
7. `grep -c 'Accepted v1 risks' docs/operating.md` returns at least 2 (existing detail heading at line ~257 plus the new cross-reference).
8. Visual review: PROJECT.md still reads cleanly and the Constraints/Key Decisions table is structurally intact.
9. Visual review: docs/operating.md still reads cleanly and the canonical detail sections at 257-273 and 390-405 are unchanged (other than the optional T-8 bolding).
10. Plans 03 and 04 can be committed together in one commit (no file overlap; both close documentation-side WARNINGs). Or one commit per plan if preferred for blast-radius hygiene.

</verification>

<success_criteria>

- PROJECT.md has the Security carve-outs annotation surfacing both /admin auth-exempt and list-mode-stub bypass with cross-references to docs/operating.md sections
- docs/operating.md has an early-document `### Auth posture quick reference` subsection that lists both carve-outs and cross-links to the canonical detail sections by name
- The canonical detail sections at lines 257-273 and 390-405 are unchanged (or only the existing T-8 bullet is bolded for emphasis)
- WARNING-02 (operator visibility of /admin exempt-by-design) is durably annotated in PROJECT.md and docs/operating.md
- WARNING-04 (operator visibility of list-mode-stub bypass) is durably annotated in PROJECT.md
- No code change; no test change; no behavior change
- Plan 05 picks up the REQUIREMENTS.md AUTH-01/02 text annotations as part of its broad refresh

</success_criteria>

<output>
Create `.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-04-SUMMARY.md` when done. Record: the exact insertion locations in PROJECT.md (line numbers) and docs/operating.md (line numbers); the verbatim text added in each; confirmation that the canonical detail sections at lines 257-273 and 390-405 are unchanged (or only T-8 bolded); note that REQUIREMENTS.md AUTH-01/02 text annotations land in Plan 05's broad refresh.
</output>
