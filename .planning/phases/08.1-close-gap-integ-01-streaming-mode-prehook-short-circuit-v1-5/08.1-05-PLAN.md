---
phase: 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
plan: 05
type: execute
wave: 4
depends_on: ["08.1-02", "08.1-03", "08.1-04"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLUG-02, PLUG-04, PLUG-06, STRM-01, STRM-02, ANTH-01, ANTH-02, AUTH-01, AUTH-02, OBSV-04, TRST-01, TRST-02, TRST-03, TRST-04, TRST-05, TRST-06, TRST-07, TRST-08]
tags: [docs, requirements, traceability, audit]

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table contains a PLUG-06 row and an OBSV-04 row, neither of which existed before this plan"
    - "REQUIREMENTS.md coverage comment reads '62 total' (not the stale '58 total' or older '53 total')"
    - "Every requirement the v1.5 audit marks 'satisfied' in its Final Status column has its REQUIREMENTS.md checkbox flipped to [x] — including PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02 because Slice A's code (Plan 01 + Plan 02) is green by the time this plan runs"
    - "REQUIREMENTS.md AUTH-01 and AUTH-02 text carries the carve-out annotation language per D-15 + D-17 — /admin auth-exempt and Ollama list-mode-stub bypass both explicit"
    - "Phase-2/3/3.1/4 partials that depend solely on Slice A code (per D-14 second-commit discipline) are flipped to [x] in this same plan because Plan 02 has already verified them"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table refreshed to true v1.5 close-out state per D-14; AUTH-01/02 annotated per D-15 + D-17; PLUG-06 + OBSV-04 rows added"
      contains: "PLUG-06"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: ".planning/v1.5-MILESTONE-AUDIT.md Final Status column (lines 226-298)"
      via: "mechanical flip per audit canonical truth-table"
      pattern: "Complete"
    - from: ".planning/REQUIREMENTS.md AUTH-01 and AUTH-02 text"
      to: "docs/operating.md v1 no-auth posture + Accepted v1 risks sections"
      via: "carve-out annotation pointing to operating.md detail per D-15 + D-17"
      pattern: "carve-out|auth-exempt|list-mode"
---

<objective>
Close WARNING-03 from the v1.5 audit per D-14 by executing the broad REQUIREMENTS.md traceability refresh in one pass: (1) add the missing `PLUG-06` and `OBSV-04` rows to the traceability table, (2) correct the stale "58 total" coverage comment to "62 total" (and update the "Mapped to phases: 58 ✓" line) plus rewrite the historical note about the earlier "53 total", and (3) flip every `[ ]` checkbox the v1.5 audit's "Final Status" column marks as `satisfied` to `[x]` — including the six partials (PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02) whose closure depends on Slice A code (Plan 01) and Slice A tests (Plan 02), both of which are green by the time this plan runs because of the `depends_on: ["08.1-02"]` ordering.

Additionally, append the carve-out annotation language to AUTH-01 and AUTH-02 text per D-15 + D-17 — Plan 04 explicitly deferred this REQUIREMENTS.md slice to this plan to avoid two consecutive plans both editing REQUIREMENTS.md.

Purpose: REQUIREMENTS.md is the durable traceability record for the v1.5 milestone. The audit's Final Status column IS the source of truth per RESEARCH §"WARNING-03 broad refresh (D-14) is a one-pass artifact" — this plan transcribes that truth into the requirements file. Future audits read REQUIREMENTS.md as ground truth; the refresh makes the file match the actual v1.5 close-out state.

Output: REQUIREMENTS.md is the correct v1.5 snapshot. WARNING-03 closed. WARNING-02 (REQUIREMENTS.md side) and WARNING-04 (REQUIREMENTS.md side) closed via the AUTH-01/02 annotation. PLUG-02 / PLUG-04 / STRM-01 / STRM-02 / ANTH-01 / ANTH-02 move from `Pending` (partial) to `Complete` reflecting the just-shipped Slice A. WARNING-01 closure (Plan 03) is reflected by flipping TRST-* rows the audit marks satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-CONTEXT.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-RESEARCH.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-PATTERNS.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-02-SUMMARY.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-03-SUMMARY.md
@.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-04-SUMMARY.md

@.planning/REQUIREMENTS.md
@.planning/v1.5-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PLUG-06 + OBSV-04 rows and correct the coverage comment in REQUIREMENTS.md (Pattern H steps B-1 and B-2)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md in full (225 lines per RESEARCH "Ask 6") — note especially: PLUG-06 definition at line 75, OBSV-04 definition at line 85, AUTH-01 at line 79, AUTH-02 at line 80, the traceability table starting around line 152, the coverage comment block at lines 216-220
    - .planning/v1.5-MILESTONE-AUDIT.md (the Final Status column around lines 226-298 — the source of truth for Task 2's checkbox flips; for this task confirm PLUG-06 and OBSV-04 both appear with status `satisfied`)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern H, sections B-1 and B-2, around lines 476-510)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-14 (broad refresh: rows + comment + checkbox flip — this task covers the row addition + comment correction; Task 2 handles checkbox flips and AUTH annotation)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Ask 6" (around lines 620-633) — confirms the existing line numbers and the row format
  </read_first>
  <action>
    Per Pattern H B-1 + B-2 + D-14:

    Step 1 — Add the missing rows. Read the traceability table layout around lines 152-213 to confirm the row format (typically `| <REQ-ID> | Phase <N> | <Status> |`). Insert two rows:
    - PLUG-06 row: insert after PLUG-05 in the PLUG-* block (likely around line 195 per RESEARCH "Ask 6"). Format: `| PLUG-06 | Phase 8 | Complete |`. PLUG-06 is defined at REQUIREMENTS.md line 75 (full text already present); the audit (v1.5-MILESTONE-AUDIT.md) confirms it ships and is satisfied. Use `Complete` directly since it's already satisfied per the audit Final Status.
    - OBSV-04 row: insert after OBSV-03 in the OBSV-* block (likely around line 201 per RESEARCH "Ask 6"). Format: `| OBSV-04 | Phase 8 | Complete |`. OBSV-04 is defined at REQUIREMENTS.md line 85; the audit confirms it ships and is satisfied.

    Step 2 — Correct the coverage comment block at lines 216-220:
    - Change `- v1 requirements: 58 total` → `- v1 requirements: 62 total`. (The previous "53 total" / "58 total" pair were both incorrect per RESEARCH "Ask 6" — actual count after adding PLUG-06 + OBSV-04 is 62.)
    - Change `- Mapped to phases: 58 ✓` → `- Mapped to phases: 62 ✓`.
    - Rewrite the historical note. Current text: `> Note: an earlier draft of this file listed "53 total" requirements; the actual count of REQ-IDs above is 58. Corrected here as part of the roadmap traceability pass.`. Replace with: `> Note: an earlier draft of this file listed "53 total" requirements; the actual count of REQ-IDs above is 62 (after the v1.5 close-out added PLUG-06 + OBSV-04 to the traceability table). Corrected in Phase 08.1 D-14.` (Or equivalent — the essential change is the count 58→62 and naming Phase 08.1 D-14 as the corrector.)

    Step 3 — Do NOT yet flip any other checkbox in this task. Task 2 owns the broad checkbox refresh and the AUTH-01/02 annotation. This separation keeps the diff reviewable.

    Constraints:
    - Use the exact row format already present in the table. No new columns, no new row metadata.
    - PLUG-06 + OBSV-04 are entered as `Complete` directly (not `Pending`), because both are satisfied per audit Final Status and this is the v1.5 close-out refresh, not a backfill.
    - Do NOT renumber any other rows; preserve REQ-ID ordering.
    - Do NOT touch AUTH-01 or AUTH-02 text in this task — Task 2 owns that.

    After changes, run `wc -l .planning/REQUIREMENTS.md` to confirm the file grew by exactly the expected number of lines (2 new rows + minimal comment edit).
  </action>
  <verify>
    <automated>grep -c '^| PLUG-06 |' .planning/REQUIREMENTS.md; grep -c '^| OBSV-04 |' .planning/REQUIREMENTS.md; grep -c 'v1 requirements: 62 total' .planning/REQUIREMENTS.md; grep -c 'Mapped to phases: 62' .planning/REQUIREMENTS.md; grep -c 'v1 requirements: 58 total' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^| PLUG-06 |' .planning/REQUIREMENTS.md` returns at least 1
    - Source: `grep -c '^| OBSV-04 |' .planning/REQUIREMENTS.md` returns at least 1
    - Source: `grep -c 'v1 requirements: 62 total' .planning/REQUIREMENTS.md` returns at least 1
    - Source: `grep -c 'Mapped to phases: 62' .planning/REQUIREMENTS.md` returns at least 1
    - Source: `grep -c 'v1 requirements: 58 total' .planning/REQUIREMENTS.md` returns 0 (stale value scrubbed)
    - Source: the historical note references "Phase 08.1" so future readers can find the corrector
    - Source: PLUG-06 row column 2 reads "Phase 8" (matching its origin phase); OBSV-04 row column 2 reads "Phase 8"
    - Source: PLUG-06 row column 3 reads "Complete"; OBSV-04 row column 3 reads "Complete"
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md traceability table contains the two new rows; the coverage comment reads "62 total" everywhere; the historical note references Phase 08.1 D-14; no other table row touched; no AUTH text touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip ~50 checkboxes per audit Final Status and annotate AUTH-01/02 with D-15 + D-17 carve-outs (Pattern H step B-3 + Pattern I REQUIREMENTS.md slice)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md after Task 1's edits — confirm the new PLUG-06 + OBSV-04 rows landed correctly and the coverage comment is updated
    - .planning/v1.5-MILESTONE-AUDIT.md (lines 226-298 — the Final Status column; this is the canonical truth-table per RESEARCH §"WARNING-03 broad refresh is a one-pass artifact")
    - .planning/REQUIREMENTS.md AUTH-01 line 79 and AUTH-02 line 80 (current text) — these get the carve-out annotation appended per Pattern I
    - .planning/phases/08.1-.../08.1-PATTERNS.md (Pattern H step B-3 around line 510; Pattern I REQUIREMENTS.md slice around lines 522-533)
    - .planning/phases/08.1-.../08.1-CONTEXT.md D-14 (the "flip every [ ] checkbox the audit identifies as satisfied" instruction including the 6 partials; the second-commit discipline collapses into a single commit here because Plan 02 SUMMARY confirms Slice A green), D-15 (PROJECT.md + REQUIREMENTS.md AUTH-01/02 + docs/operating.md three-place annotation; this task handles REQUIREMENTS.md), D-17 (T-8-AUTH-BYPASS annotation in REQUIREMENTS.md AUTH-01/02 + docs/operating.md; same)
    - .planning/phases/08.1-.../08.1-RESEARCH.md "Ask 6" (around lines 627-633) — confirms the AUTH-01/02 text already in place needs the D-15 + D-17 sentences appended
    - .planning/phases/08.1-.../08.1-02-SUMMARY.md — confirms Slice A is green so the 6 partials may be flipped in the same commit
  </read_first>
  <action>
    Per Pattern H step B-3 + Pattern I + D-14 + D-15 + D-17:

    Step 1 — Broad checkbox refresh (Pattern H B-3). The source of truth is `.planning/v1.5-MILESTONE-AUDIT.md` Final Status column (around lines 226-298). For every REQ-ID in REQUIREMENTS.md that:
    (a) currently has `- [ ]` (Pending) in the bulleted requirements list and `Pending` in the traceability table row, AND
    (b) the audit marks as `satisfied` in its Final Status column,

    flip both representations:
    - Bulleted list: `- [ ]` → `- [x]`.
    - Traceability table column 3: `Pending` → `Complete`.

    Both representations live in the same file but the checkbox is in the bulleted requirement list (lines 1-150ish per RESEARCH file structure) while the table is at lines 152-213. Keep them in sync — Pattern H step B-3.

    Special handling for the six Slice A-blocked partials (PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02): per D-14 the original plan was to flip these in a second Slice B commit AFTER Slice A's code green. By the time this plan runs, `depends_on: ["08.1-02"]` guarantees Plan 02 SUMMARY exists confirming Slice A green. Therefore flip these six in this same commit. Plan 02 SUMMARY is the proof artifact.

    Phase-2/3/3.1/4 partials per CONTEXT.md "Phase-2/3/3.1/4 partials remain `[ ]` until Slice A merges — but since Slice A and Slice B ship in the same phase, the flip happens after Slice A's plan completes and before Slice B's checkbox-flip task runs": the partials covered here are the same six PLUG/STRM/ANTH IDs (the Phase-2/3/3.1/4 wording is the audit's framing for the same six rows). Confirm with the audit Final Status — if the audit marks any additional Phase-2/3/3.1/4 requirement as still partial without an in-08.1 satisfaction path, leave it `[ ]`. The audit is canonical.

    Do NOT flip a row to `[x]` unless the audit Final Status says `satisfied`. The plan does not have authority to judge satisfaction — only the audit does (per RESEARCH §"the audit IS the source of truth").

    Step 2 — Annotate AUTH-01 and AUTH-02 text per D-15 + D-17 + Pattern I. The current text at REQUIREMENTS.md lines 79-80:
    - AUTH-01 (line 79): `- [ ] **AUTH-01**: Bearer-token auth via AUTH_TOKEN env var (comma-separated list). Empty means no auth (matches Node default).`
    - AUTH-02 (line 80): `- [ ] **AUTH-02**: IP allowlist via ALLOWED_IPS env var (comma-separated). Empty means allow-all.`

    Per the audit Final Status, both flip to `- [x]` (Step 1 handles the checkbox flip). Per D-15 + D-17, append the carve-out language. Use Pattern I's REQUIREMENTS.md slice excerpt (around lines 525-533 of PATTERNS.md) as the upper-bound wording. Specifically:

    For AUTH-01, append the two carve-out sentences from Pattern I excerpt:
    - "**Carve-outs:** (1) `/admin` route is auth-exempt by design (Phase 6.1 D-01; operator binds localhost or fronts with reverse-proxy auth — see `docs/operating.md` `### v1 no-auth posture`). (2) Ollama list-mode stubs (`/api/tags`, `/api/ps`, `/api/show`, `/api/copy`, `/api/delete`, `/api/pull`, `/api/push`, `/api/create`) bypass `AuthHook` because they do not route through the canonical engine. IP allowlist remains. Accepted v1 risk — these endpoints have no model-execution surface (see `docs/operating.md` `#### Accepted v1 risks`)."

    For AUTH-02, append the shorter cross-reference per Pattern I:
    - "**Carve-outs:** same as AUTH-01 — `/admin` is exempt-by-design; Ollama list-mode stubs bypass the canonical chain (IP allowlist still applies to them)."

    Step 3 — Do NOT touch AUTH-03 (per RESEARCH "Ask 6" — AUTH-03 already covers exempts but does not address `/admin`; no annotation needed).

    Step 4 — Confirm Phase 4 / Phase 3 / Phase 3.1 / Phase 2 / Phase 8 partial requirements that depend on Slice A code (the six already-identified) all show `[x]` and `Complete` after this task. Any other Final-Status-satisfied row in the audit also flips.

    Constraints:
    - The audit IS the source of truth. Do not invent satisfaction beyond what the audit declares.
    - Cross-references in AUTH-01/02 use section names ("`### v1 no-auth posture`", "`#### Accepted v1 risks`") not line numbers.
    - Do NOT add new rows here (Task 1 owns that).
    - Do NOT change the table format. Only the third column entry changes.
    - Each checkbox state must agree between the bulleted list and the table row for the same REQ-ID. A `[x]` in the list paired with `Pending` in the table is a Task 2 failure.

    Run `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` before and after to confirm a net increase of approximately 50 checkboxes (per CONTEXT.md D-14 "~50 requirements").
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*AUTH-01\*\*' .planning/REQUIREMENTS.md; grep -c '^- \[x\] \*\*AUTH-02\*\*' .planning/REQUIREMENTS.md; grep -c '^- \[x\] \*\*PLUG-02\*\*' .planning/REQUIREMENTS.md; grep -c '^- \[x\] \*\*STRM-01\*\*' .planning/REQUIREMENTS.md; grep -c '^- \[x\] \*\*ANTH-01\*\*' .planning/REQUIREMENTS.md; grep -c 'auth-exempt' .planning/REQUIREMENTS.md; grep -c 'list-mode' .planning/REQUIREMENTS.md; grep -c '| Complete |' .planning/REQUIREMENTS.md; grep -c '| Pending |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^- \[x\] \*\*AUTH-01\*\*' .planning/REQUIREMENTS.md` returns 1 (AUTH-01 flipped)
    - Source: `grep -c '^- \[x\] \*\*AUTH-02\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: `grep -c '^- \[x\] \*\*PLUG-02\*\*' .planning/REQUIREMENTS.md` returns 1 (Slice A-blocked partial flipped)
    - Source: `grep -c '^- \[x\] \*\*PLUG-04\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: `grep -c '^- \[x\] \*\*STRM-01\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: `grep -c '^- \[x\] \*\*STRM-02\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: `grep -c '^- \[x\] \*\*ANTH-01\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: `grep -c '^- \[x\] \*\*ANTH-02\*\*' .planning/REQUIREMENTS.md` returns 1
    - Source: AUTH-01 line contains the literal string "auth-exempt" or "Carve-outs"
    - Source: AUTH-01 line contains the literal string "list-mode" or enumerates `/api/tags`
    - Source: AUTH-01 line contains a cross-reference to `docs/operating.md` (the section names "v1 no-auth posture" or "Accepted v1 risks" are referenced)
    - Source: AUTH-02 line contains the carve-out language (shorter than AUTH-01 per Pattern I)
    - Source: `grep -c '| Complete |' .planning/REQUIREMENTS.md` shows a net increase of approximately 50 from pre-plan baseline
    - Source: `grep -c '| Pending |' .planning/REQUIREMENTS.md` shows a corresponding net decrease
    - Source: every flipped checkbox in the bulleted list has the matching table row also showing `Complete` (manual spot-check the six PLUG/STRM/ANTH partials + AUTH-01/02 + a handful of TRST-* rows)
    - Behavior: REQUIREMENTS.md reads as a correct v1.5 snapshot — a fresh `/gsd-audit-milestone v1.5` re-run would not re-flag WARNING-03 nor the AUTH-01/02 annotation gap
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md bulleted requirements list and traceability table are synchronized to the audit's Final Status truth-table; the six Slice A-blocked partials are flipped to satisfied; AUTH-01 and AUTH-02 carry the carve-out annotation language per D-15 + D-17 with cross-references to docs/operating.md sections; ~50 checkboxes net-flipped to `[x]`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1.5-MILESTONE-AUDIT.md → REQUIREMENTS.md | The audit's Final Status column is the canonical truth-table; REQUIREMENTS.md must transcribe it faithfully. Drift between the two is the WARNING-03 finding. |
| Future audit re-runner → REQUIREMENTS.md | Future `/gsd-audit-milestone v1.5` runs read REQUIREMENTS.md; any unflipped satisfied row gets re-flagged as a new gap. Faithful transcription closes the loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-CHECKBOX-DRIFT | Repudiation (REQUIREMENTS.md disagrees with audit Final Status) | REQUIREMENTS.md traceability table + bulleted list | mitigate | Pattern H B-3 mechanical flip per audit canonical column. Pattern I AUTH-01/02 annotation language verbatim from CONTEXT.md D-15 + D-17 (within the upper-bound wording per Pattern I excerpt). |
| T-08.1-OVER-FLIP | Tampering (flipping rows the audit hasn't marked satisfied) | Task 2 checkbox refresh | mitigate | The audit IS the source of truth. The action explicitly forbids flipping satisfaction beyond what the audit declares. Phase-2/3/3.1/4 partials beyond the six Slice A-blocked ones stay `[ ]` if the audit says so. |
| T-08.1-PARTIAL-LIST-MISMATCH | Integrity (bulleted list out of sync with table) | REQUIREMENTS.md two-representation requirement | mitigate | Acceptance criteria require synchronized state between bulleted list checkboxes and table row status. Task 2 explicitly calls out the keep-in-sync invariant. |
| T-8-AUTH-BYPASS (carry-over, accepted v1 risk — WARNING-04) | Tampering | Ollama list-mode stubs | accept | AUTH-01/02 text now carries the durable annotation per D-17. The accepted-risk language references docs/operating.md `#### Accepted v1 risks` for full operator context. |
| T-6.1-ADMIN-AUTH-EXEMPT (WARNING-02) | Information Disclosure | `/admin` route bind/exposure | accept | AUTH-01/02 text now carries the durable annotation per D-15. The bind-localhost / reverse-proxy guidance lives in docs/operating.md `### v1 no-auth posture`; AUTH-01/02 cross-references it. |

</threat_model>

<verification>

After both tasks complete:

1. `grep -c '^| PLUG-06 |' .planning/REQUIREMENTS.md` returns at least 1.
2. `grep -c '^| OBSV-04 |' .planning/REQUIREMENTS.md` returns at least 1.
3. `grep -c 'v1 requirements: 62 total' .planning/REQUIREMENTS.md` returns at least 1.
4. `grep -c 'v1 requirements: 58 total' .planning/REQUIREMENTS.md` returns 0 (stale value scrubbed).
5. `grep -c '^- \[x\] \*\*PLUG-02\*\*' .planning/REQUIREMENTS.md`, `^- \[x\] \*\*PLUG-04\*\*`, `^- \[x\] \*\*STRM-01\*\*`, `^- \[x\] \*\*STRM-02\*\*`, `^- \[x\] \*\*ANTH-01\*\*`, `^- \[x\] \*\*ANTH-02\*\*` each return 1 (six Slice A partials all flipped).
6. AUTH-01 line contains both "auth-exempt" / "Carve-outs" language and "list-mode" / `/api/tags` enumeration.
7. AUTH-02 line contains the shorter carve-out language with cross-reference back to AUTH-01.
8. The bulleted-list `[x]` count and the table `Complete` count are consistent (no row flipped in one representation but not the other).
9. A spot-check `/gsd-audit-milestone v1.5` re-run (if available in workflow) would not re-flag WARNING-03 nor the AUTH-01/02 annotation gap. (This is the acceptance criterion; running the audit is optional.)
10. Commit Tasks 1 and 2 together OR as two sequential commits per developer preference. CONTEXT.md D-14's "second commit" discipline is moot because Plan 02 SUMMARY exists at this point — both tasks may land in one commit.

</verification>

<success_criteria>

- REQUIREMENTS.md traceability table includes PLUG-06 and OBSV-04 rows
- Coverage comment reads "62 total" and the historical note references "Phase 08.1"
- ~50 checkboxes are net-flipped from `[ ]` to `[x]` matching the v1.5 audit Final Status column
- The six Slice A-blocked partials (PLUG-02, PLUG-04, STRM-01, STRM-02, ANTH-01, ANTH-02) are flipped to satisfied
- AUTH-01 and AUTH-02 carry the carve-out annotation language per D-15 + D-17 with cross-references to docs/operating.md sections
- Bulleted list checkboxes and traceability table rows are synchronized (same status for the same REQ-ID in both representations)
- A fresh v1.5 audit re-run would not re-flag WARNING-03, WARNING-02 REQUIREMENTS.md slice, or WARNING-04 REQUIREMENTS.md slice
- Phase 08.1's v1.5-closure cycle is complete after this plan

</success_criteria>

<output>
Create `.planning/phases/08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5/08.1-05-SUMMARY.md` when done. Record: the exact set of REQ-IDs flipped from `[ ]` to `[x]` and their corresponding table-row Pending→Complete transitions; the verbatim AUTH-01 and AUTH-02 annotation language as it now lives in REQUIREMENTS.md; confirmation that PLUG-06 and OBSV-04 rows landed; confirmation that the coverage comment reads "62 total". Note that Phase 08.1 v1.5-closure is complete after this plan and recommend the next workflow step: a fresh `/gsd-audit-milestone v1.5` run to confirm no findings remain, then mark Phase 08.1 complete in ROADMAP.md.
</output>
