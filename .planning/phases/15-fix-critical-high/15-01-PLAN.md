---
phase: 15-fix-critical-high
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/pool/pool.go
  - internal/pool/config.go
  - internal/acp/client.go
  - cmd/otto-gateway/main.go
  - internal/server/server.go
  - internal/admin/sse.go
  - internal/pool/regression_rel_pool_01_test.go
  - internal/pool/regression_rel_pool_02_test.go
  - internal/pool/regression_rel_pool_03_test.go
  - internal/server/regression_rel_http_01_test.go
autonomous: true
requirements:
  - REL-POOL-01
  - REL-POOL-02
  - REL-POOL-03
  - REL-HTTP-01
must_haves:
  truths:
    - "Pool re-queues the slot on transient respawn failure and Stats().Size stays >= 1"
    - "NewSession returns ErrPoolExhausted (not a hang) when all slots are busy past POOL_ACQUIRE_TIMEOUT_MS"
    - "Adapters map ErrPoolExhausted to HTTP 503 with Retry-After: 5 and a surface-native error body"
    - "Ctrl-C during generation leaves zero kiro-cli processes after binary exits"
    - "A second Ctrl-C during the shutdown grace forces immediate cleanup + exit 130"
    - "pool.Close() cancels all in-flight sessions during the 30s shutdown grace"
    - "os.Exit(1) in main.go no longer bypasses defer cleanup()"
    - "A queued request acquiring a recycled slot never receives a silent empty 200"
    - "Admin SSE (log-tail) connection unwinds within 1s of Ctrl-C instead of blocking the full 30s grace"
  artifacts:
    - path: "internal/pool/pool.go"
      provides: "ErrPoolExhausted sentinel, bounded acquire select with timeoutC arm, transient-error re-queue"
      contains: "ErrPoolExhausted"
    - path: "internal/pool/config.go"
      provides: "AcquireTimeout time.Duration field parsed from POOL_ACQUIRE_TIMEOUT_MS"
      contains: "AcquireTimeout"
    - path: "internal/acp/client.go"
      provides: "Identity-guarded activeStream nil in both awaitPromptResult arms"
      contains: "c.activeStream == stream"
    - path: "internal/server/server.go"
      provides: "shutdownCh chan struct{} + RegisterOnShutdown + two-signal goroutine"
      contains: "shutdownCh"
    - path: "internal/admin/sse.go"
      provides: "sseLoop selects on shutdownCh and exits within 1s of gateway shutdown"
      contains: "shutdownCh"
    - path: "internal/adapter/openai/sse.go"
      provides: "ErrPoolExhausted -> 503 with D-07 OpenAI body and Retry-After: 5"
      contains: "pool_exhausted"
    - path: "internal/adapter/openai/sse.go"
      provides: "Retry-After: 5 header on pool exhaustion 503"
      contains: "Retry-After"
    - path: "internal/adapter/ollama/ndjson.go"
      provides: "ErrPoolExhausted -> 503 with D-07 Ollama body"
      contains: "pool_exhausted:"
    - path: "internal/adapter/anthropic/sse.go"
      provides: "ErrPoolExhausted -> 503 with D-07 Anthropic body"
      contains: "overloaded_error"
  key_links:
    - from: "internal/pool/pool.go"
      to: "internal/pool/config.go"
      via: "p.cfg.AcquireTimeout field read in NewSession acquire select"
      pattern: "AcquireTimeout"
    - from: "internal/server/server.go"
      to: "internal/admin/sse.go"
      via: "s.shutdownCh passed to sseLoop via sseHandler"
      pattern: "shutdownCh"
    - from: "cmd/otto-gateway/main.go"
      to: "internal/server/server.go"
      via: "RunUntilSignal returns non-nil error → explicit cleanup() before os.Exit(1)"
      pattern: "cleanup()"
    - from: "internal/adapter/openai/sse.go"
      to: "internal/pool/pool.go"
      via: "errors.Is(err, pool.ErrPoolExhausted) guard maps acquire timeout to 503"
      pattern: "ErrPoolExhausted"
    - from: "internal/adapter/ollama/ndjson.go"
      to: "internal/pool/pool.go"
      via: "errors.Is(err, pool.ErrPoolExhausted) guard maps acquire timeout to 503"
      pattern: "ErrPoolExhausted"
    - from: "internal/adapter/anthropic/sse.go"
      to: "internal/pool/pool.go"
      via: "errors.Is(err, pool.ErrPoolExhausted) guard maps acquire timeout to 503"
      pattern: "ErrPoolExhausted"
---

<objective>
Fix 4 confirmed reliability findings: P-1 (Critical), P-2 (High), P-3 (High), H-1 (High).

Purpose: Close the load-bearing pool exhaustion + orphaned-process + stale-stream + SSE-shutdown-block failure modes that make the gateway unreliable under everyday Ctrl-C and pool-pressure scenarios.

Output:
- internal/pool/pool.go — bounded acquire select + ErrPoolExhausted sentinel + transient-error re-queue (D-04, D-07, D-08 per REL-POOL-01)
- internal/pool/config.go — AcquireTimeout field parsed from POOL_ACQUIRE_TIMEOUT_MS (D-04 per REL-POOL-01)
- internal/acp/client.go — identity-guarded activeStream nil in awaitPromptResult (per REL-POOL-03)
- cmd/otto-gateway/main.go — explicit cleanup() before os.Exit(1) (D-05 per REL-POOL-02)
- internal/server/server.go — shutdownCh + RegisterOnShutdown + two-signal goroutine (D-05 per REL-POOL-02 + REL-HTTP-01)
- internal/admin/sse.go — sseLoop selects on shutdownCh (per REL-HTTP-01)
- internal/adapter/{openai/sse.go,ollama/ndjson.go,anthropic/sse.go} — ErrPoolExhausted → 503 + Retry-After:5 with D-07 surface-native bodies (per REL-POOL-01)
- 4 regression tests unskipped and green

Plan structure decision (per D-01a): P-2 and H-1 BOTH require editing internal/server/server.go:Run()
to add a shutdownCh + RegisterOnShutdown block. Running them in separate parallel plans would
guarantee a merge conflict. This plan owns ALL server.go edits: it installs shutdownCh in server.go
(for P-2 in-flight cancel + H-1 SSE unwind), and passes it into sseLoop in admin/sse.go. Plan 15-02
(H-2, H-3) then has no server.go dependency and can run in parallel in the same wave.
</objective>

<execution_context>
@/Users/coreyellis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coreyellis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-fix-critical-high/15-CONTEXT.md
@.planning/phases/15-fix-critical-high/15-RESEARCH.md
@.planning/phases/15-fix-critical-high/15-PATTERNS.md
@.planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md
</context>

<phase_context>
## Why This Plan Owns Both P-2 and H-1

RESEARCH.md Pitfall 3 and the Architectural Responsibility Map both confirm that P-2 and H-1 share
exactly one code site: the `http.Server{}` construction block in `internal/server/server.go:Run()`.
Both fixes add `shutdownCh chan struct{}` to the Server struct and wire `srv.RegisterOnShutdown`
to close it. Separating them into two parallel plans would produce a guaranteed merge conflict.

Resolution chosen: MERGE (per D-01a "merge OR serialize"). Plan 15-01 owns all server.go edits.
Plan 15-02 owns only openai/sse.go and ollama/ndjson.go — zero server.go overlap.
All three plans remain in Wave 1 (parallel execution via worktree isolation).

## TDD Shape for Phase 15

All 4 regression test files were written in Phase 14 with t.Skip markers. Each task below
removes the t.Skip AND makes the production fix in the SAME atomic commit (D-03). The
test going from SKIP to PASS is the verification proof for that finding.

Three tests require assertion-polarity flips (not just t.Skip removal):
- regression_rel_pool_01_test.go: "want 0" → "want 1" (slot stays in pool after re-queue)
- regression_rel_pool_02_test.go: "cancelsBefore == 0" → "cancelsAfter >= 2"
- regression_rel_http_01_test.go: "elapsed >= 2s confirms bug" → "elapsed < 1s confirms fix"
</phase_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P-1 — Bounded acquire + ErrPoolExhausted + transient-error re-queue (REL-POOL-01)</name>
  <files>internal/pool/pool.go, internal/pool/config.go, internal/pool/regression_rel_pool_01_test.go</files>
  <read_first>
    Read internal/pool/pool.go lines 491-540 (NewSession acquire select + respawnSlot error handling, including the existing ctx-cancel re-queue at lines 525-532 — this is the re-queue pattern to copy for the transient-error arm).
    Read internal/pool/config.go lines 91-130 (Config struct + applyDefaults — model for adding AcquireTimeout field and its env parse).
    Read internal/pool/regression_rel_pool_01_test.go in full (understand the t.Skip, pre-fix assertion "want 0", and the post-fix expectation "want 1"; also note the ErrPoolExhausted assertion to add).
    Read internal/pool/pool.go top ~30 lines for the var/const block where ErrPoolExhausted sentinel should be declared.
  </read_first>
  <behavior>
    - pool.Config gains an AcquireTimeout time.Duration field (default 30s from POOL_ACQUIRE_TIMEOUT_MS env, parsed via strconv.ParseInt + time.Duration(n)*time.Millisecond in applyDefaults).
    - A new package-level sentinel: var ErrPoolExhausted = errors.New("pool: all workers busy; retry in 5s") declared near top of pool.go after imports.
    - NewSession acquire select gains a timeoutC arm using time.NewTimer(p.cfg.AcquireTimeout) with defer timer.Stop(); when timeoutC fires, returns ErrPoolExhausted.
    - The transient (non-ctx) respawnSlot error arm at pool.go:534 changes from p.removeSlot(slot) to the same re-queue select pattern already at lines 525-532: select { case p.slots <- slot: debugLog(...); case <-p.closing: }.
    - regression_rel_pool_01_test.go: remove t.Skip; flip assertion from "want 0" to "want 1" (slot re-queued so pool size stays 1); add assertion that NewSession with an expired context returns an error wrapping ErrPoolExhausted.
  </behavior>
  <action>
    1. In internal/pool/config.go: add `AcquireTimeout time.Duration` field to Config struct with godoc "// AcquireTimeout is the maximum time NewSession will block waiting for a free slot. Zero uses default 30s. Env: POOL_ACQUIRE_TIMEOUT_MS." In applyDefaults, parse POOL_ACQUIRE_TIMEOUT_MS via os.Getenv + strconv.ParseInt; if absent or zero, set to 30*time.Second. Match the PingInterval field pattern exactly.

    2. In internal/pool/pool.go: declare package sentinel var ErrPoolExhausted near other var declarations. Add timeoutC local in NewSession before the acquire select: if p.cfg.AcquireTimeout > 0, create a time.NewTimer(p.cfg.AcquireTimeout) with defer timer.Stop() and assign timer.C to timeoutC (a nil channel when AcquireTimeout == 0 so the arm is never selected). Add `case <-timeoutC: return "", ErrPoolExhausted` to the acquire select. In the respawnSlot error path at line ~534, replace p.removeSlot(slot) with the same re-queue select block already at lines 525-532 (copy verbatim, change the debugLog key from "pool.respawn.deferred" to "pool.respawn.transient_requeue"). Return a wrapped error that does not wrap ErrPoolExhausted (the re-queue error is distinct from exhaustion).

    3. Per-surface 503 mapping: check internal/adapter/openai/sse.go, internal/adapter/ollama/ndjson.go, and internal/adapter/anthropic/sse.go for where they call pool.NewSession or engine.NewSession. Add an errors.Is(err, pool.ErrPoolExhausted) guard that writes a 503 response with the D-07 surface-native body (see CONTEXT.md D-07 for exact JSON shapes) and a `Retry-After: 5` header before returning. Import pool package if not already imported; use errors package for errors.Is traversal. D-07 bodies:
       - OpenAI: {"error":{"type":"server_error","code":"pool_exhausted","message":"all workers busy; retry in 5s","param":null}}
       - Ollama: {"error":"pool_exhausted: all workers busy; retry in 5s"}
       - Anthropic: {"type":"error","error":{"type":"overloaded_error","message":"all workers busy; retry in 5s"}}

    4. In internal/pool/regression_rel_pool_01_test.go: remove the t.Skip line. Change the assertion that checks pool size == 0 to assert pool size >= 1 (slot was re-queued). Add assertion that a NewSession call made with an already-cancelled context returns an error satisfying errors.Is(err, pool.ErrPoolExhausted) or wraps it — match whatever the test's fixture does. Do NOT modify any Phase 16 test files (regression_rel_pool_04_test.go etc.).

    5. Atomic commit per D-03: all 4 changes (config.go, pool.go, adapter 503 mappings, test unskip) in a single commit with message "fix(REL-POOL-01): bounded pool acquire + ErrPoolExhausted + transient re-queue".

    Note on PII bracket shape: any log marker string uses [...] not <...> (project memory).
    Note on gosec: no external subprocess is called in this task; G204 is not triggered here.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test -race ./internal/pool/ -run TestRegression_REL_POOL_01 -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `TestRegression_REL_POOL_01` output shows PASS (not SKIP, not FAIL).
    - Pool Stats().Size == 1 after a transient respawn failure (slot re-queued, not removed).
    - NewSession with expired context returns error wrapping ErrPoolExhausted.
    - grep -v '^//' internal/pool/pool.go | grep -c 'ErrPoolExhausted' returns >= 2 (declaration + usage in acquire select).
    - grep -c 'AcquireTimeout' internal/pool/config.go returns >= 2 (field + applyDefaults parse).
    - grep -c '"code":"pool_exhausted"' internal/adapter/openai/sse.go returns >= 1 (D-07 OpenAI 503 body).
    - grep -c '"error":"pool_exhausted:' internal/adapter/ollama/ndjson.go returns >= 1 (D-07 Ollama 503 body).
    - grep -c 'overloaded_error' internal/adapter/anthropic/sse.go returns >= 1 (D-07 Anthropic 503 body).
    - grep -c 'Retry-After.*5' internal/adapter/openai/sse.go returns >= 1 (Retry-After: 5 header on 503).
    - go build ./... exits 0 (adapter 503 changes compile cleanly on all surfaces).
  </acceptance_criteria>
  <done>TestRegression_REL_POOL_01 PASS under -race; pool size stays >= 1 after transient spawn failure; all adapters build cleanly with 503 mapping; D-07 surface-native bodies and Retry-After:5 confirmed by grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P-2 + H-1 — Shutdown plumbing: shutdownCh + two-signal + SSE unwind + cleanup fix (REL-POOL-02 + REL-HTTP-01)</name>
  <files>cmd/otto-gateway/main.go, internal/server/server.go, internal/admin/sse.go, internal/pool/regression_rel_pool_02_test.go, internal/server/regression_rel_http_01_test.go</files>
  <read_first>
    Read cmd/otto-gateway/main.go lines 120-140 (deferred cleanup + RunUntilSignal call + os.Exit(1) — the exact lines to change for P-2, including verifying cleanup is sync.Once-guarded or can be called twice safely).
    Read internal/server/server.go lines 340-410 (Run() http.Server construction block + shutdownCtx setup + RunUntilSignal single-signal goroutine — both functions to extend).
    Read internal/admin/sse.go lines 160-210 (sseLoop signature and select block — add shutdownCh parameter and new select arm).
    Read internal/pool/regression_rel_pool_02_test.go in full (t.Skip, pre-fix "cancelsBefore == 0" assertion, post-fix "cancelsAfter >= 2" target).
    Read internal/server/regression_rel_http_01_test.go in full (t.Skip, pre-fix "elapsed >= 2s confirms bug", post-fix "elapsed < 1s confirms fix" — polarity flip required).
  </read_first>
  <behavior>
    - cmd/otto-gateway/main.go: the os.Exit(1) at line 131 is preceded by an explicit cleanup() call so pool.Close() runs before the process exits. defer cleanup() at line 127 stays (handles nil-return case). cleanup must be idempotent (check whether it already uses sync.Once; if not, wrap with a sync.Once before making it called twice).
    - internal/server/server.go Server struct gains a field: shutdownCh chan struct{} initialized in New() or first-use. Run() adds srv.RegisterOnShutdown that closes shutdownCh (idempotent: select { case <-s.shutdownCh: default: close(s.shutdownCh) }). RunUntilSignal() single-signal goroutine becomes two-stage: after first SIGINT cancels derivedCtx, a second nested select catches a second SIGINT and triggers force-cancel + a forceExitCh send so Run returns immediately.
    - internal/admin/sse.go: sseLoop gains a shutdownCh <-chan struct{} parameter. The select block adds case <-shutdownCh: return errors.New("admin: gateway shutting down"). The sseHandler (or wherever sseLoop is called) passes s.shutdownCh.
    - regression_rel_pool_02_test.go: remove t.Skip; flip assertion from "cancelsBefore == 0" to "cancelsAfter >= 2" (pool.Close cancelled both in-flight sessions).
    - regression_rel_http_01_test.go: remove t.Skip; flip assertion from "elapsed >= minBlockDuration" to "elapsed < 1*time.Second". POLARITY FLIP: the test was written to confirm the bug (slow shutdown); after fix it confirms the fix (fast shutdown). Ensure minBlockDuration constant or variable (if present) is no longer the comparison target.
  </behavior>
  <action>
    1. In cmd/otto-gateway/main.go: Inspect cleanup() definition — if it does NOT use sync.Once, wrap it so it is idempotent (a local once sync.Once in main() or a package-level one, matching existing style). Then change the RunUntilSignal error path from `os.Exit(1)` to explicit `cleanup(); closeLogger(); os.Exit(1)`. The deferred cleanup() at line 127 stays — it handles normal (nil error) returns. No new functions; no new env vars.

    2. In internal/server/server.go: Add `shutdownCh chan struct{}` to the Server struct. Initialize it in the Server constructor (or lazy-init in Run with a sync.Once — match existing style). In Run(), after the srv construction block, add: `srv.RegisterOnShutdown(func() { select { case <-s.shutdownCh: default: close(s.shutdownCh) } })`. In RunUntilSignal(), replace the single-signal goroutine with a two-stage version per the PATTERNS.md fix pattern: first stage catches sigCh → cancel(); second nested select (inside the first stage) catches a second sigCh → force-signal the main goroutine via an internal forceExitCh channel. Return from RunUntilSignal with a sentinel error (e.g. errors.New("force-exit")) so main.go's explicit cleanup() path runs.

    3. In internal/admin/sse.go: Add shutdownCh <-chan struct{} as the last parameter to sseLoop. Add `case <-shutdownCh: return errors.New("admin: gateway shutting down")` to the for/select block. Update the call site inside the file to pass s.shutdownCh (the handler closure must capture s). No other sseLoop call sites expected; verify with grep before assuming.

    4. In internal/pool/regression_rel_pool_02_test.go: remove t.Skip. Change the assertion from checking cancelsBefore == 0 to checking cancelsAfter >= 2. Keep the test structure unchanged; only flip the direction of the assertion.

    5. In internal/server/regression_rel_http_01_test.go: remove t.Skip. Change elapsed >= minBlockDuration to elapsed < 1*time.Second. POLARITY FLIP: the test was written to confirm the bug (slow shutdown); after fix it confirms the fix (fast shutdown). Ensure minBlockDuration constant or variable (if present) is no longer the comparison target.

    6. Atomic commit per D-03: all 5 file changes in one commit: "fix(REL-POOL-02,REL-HTTP-01): shutdown plumbing — shutdownCh + two-signal + SSE unwind + explicit cleanup".
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test -race ./internal/pool/ -run TestRegression_REL_POOL_02 -v -count=1 2>&1 | tail -10 && go test -race ./internal/server/ -run TestRegression_REL_HTTP_01 -v -count=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TestRegression_REL_POOL_02 output shows PASS (not SKIP, not FAIL). cancelsAfter >= 2.
    - TestRegression_REL_HTTP_01 output shows PASS (not SKIP, not FAIL). elapsed < 1s.
    - grep -c 'shutdownCh' internal/server/server.go returns >= 3 (field decl + RegisterOnShutdown close + init).
    - grep -c 'shutdownCh' internal/admin/sse.go returns >= 2 (parameter + select arm).
    - grep -v 'os.Exit' cmd/otto-gateway/main.go | grep -c 'cleanup()' returns >= 2 (deferred + explicit before exit).
    - go build ./... exits 0.
  </acceptance_criteria>
  <done>TestRegression_REL_POOL_02 and TestRegression_REL_HTTP_01 both PASS under -race; admin SSE unwinds in < 1s on shutdown; no os.Exit bypasses cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: P-3 — CAS-guarded activeStream nil in awaitPromptResult (REL-POOL-03)</name>
  <files>internal/acp/client.go, internal/pool/regression_rel_pool_03_test.go</files>
  <read_first>
    Read internal/acp/client.go lines 855-900 (awaitPromptResult function — both arms that unconditionally set c.activeStream = nil at lines 868-870 (ctx arm) and 894-896 (frame arm); note the streamMu lock already in place; note that stream is the parameter in scope).
    Read internal/pool/regression_rel_pool_03_test.go in full (t.Skip, pre-fix assertion "chunksDelivered < iterations", post-fix target "chunksDelivered == iterations").
    Read internal/acp/client.go lines 840-860 to understand how awaitPromptResult is called and what stream refers to (the *Stream parameter passed to the function).
  </read_first>
  <behavior>
    - In awaitPromptResult, both arms that do `c.streamMu.Lock(); c.activeStream = nil; c.streamMu.Unlock()` gain an identity check: the nil assignment only happens if c.activeStream == stream (this goroutine is still the owner). No type change, no atomic.Pointer — the existing streamMu is sufficient.
    - regression_rel_pool_03_test.go: remove t.Skip; the assertion changes from "chunksDelivered < iterations" to "chunksDelivered == iterations".
    - No other files in internal/acp/ are touched in this task.
  </behavior>
  <action>
    1. In internal/acp/client.go, locate the ctx arm at lines 868-870 and the frame arm at lines 894-896. In both arms, change the unconditional `c.activeStream = nil` to a guarded form: `if c.activeStream == stream { c.activeStream = nil }`. The streamMu lock/unlock already wraps both — do not change the locking structure. This is the minimal fix per PATTERNS.md P-3 pattern and RESEARCH.md Pitfall 5 (mutex+identity, not atomic.Pointer).

    2. Before coding: grep internal/acp/client.go for all write sites of c.activeStream to verify no read site exists outside streamMu (confirm assumption A1 from RESEARCH.md). If any unguarded read is found, note in the commit message but do not expand scope — Phase 15 only fixes the write-side race; Phase 16 scope handles any residual race.

    3. In internal/pool/regression_rel_pool_03_test.go: remove t.Skip. Change the assertion from "chunksDelivered < iterations" to "chunksDelivered == iterations". Do NOT change any Phase 16 test files.

    4. Atomic commit per D-03: both file changes together: "fix(REL-POOL-03): identity-guarded activeStream nil in awaitPromptResult".
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test -race ./internal/pool/ -run TestRegression_REL_POOL_03 -v -count=1 2>&1 | tail -10 && go test -race ./internal/acp/... -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TestRegression_REL_POOL_03 output shows PASS (not SKIP, not FAIL). chunksDelivered == iterations.
    - go test -race ./internal/acp/... exits 0 (no regressions in ACP package).
    - grep -c 'c.activeStream == stream' internal/acp/client.go returns 2 (one per arm).
    - go build ./... exits 0.
  </acceptance_criteria>
  <done>TestRegression_REL_POOL_03 PASS under -race; identity-guarded nil prevents stale goroutine from wiping a newer session's stream pointer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → pool config | POOL_ACQUIRE_TIMEOUT_MS is an operator-supplied integer parsed at boot; validated by applyDefaults (no sign check needed here — 0 maps to default; negative values would need a Phase 16 REL-CFG-01 fix) |
| PID file → shutdown signal | shutdownCh is an internal channel; no external input crosses this boundary |
| Subprocess spawn (pool.respawnSlot) | Already under gosec scrutiny; P-1 re-queue does not change spawn logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | pool.NewSession acquire select | mitigate | ErrPoolExhausted + Retry-After:5 bounds wait; callers see typed 503 instead of hanging indefinitely |
| T-15-02 | Tampering | awaitPromptResult activeStream | mitigate | Identity check prevents stale goroutine from corrupting a newer session's stream pointer |
| T-15-03 | Denial of Service | admin sseLoop blocking shutdown | mitigate | shutdownCh case exits sseLoop within poll interval; Shutdown() drains normally |
| T-15-SC | Tampering | subprocess spawn in pool | accept | No new spawn call sites added in this plan; gosec G204 baseline already gates existing sites |
</threat_model>

<verification>
## Cross-task verification (all 4 findings)

After all three tasks complete and all three commits land:

```bash
cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway
go test -race ./internal/pool/ -run 'TestRegression_REL_POOL_0[123]' -v -count=1
go test -race ./internal/server/ -run TestRegression_REL_HTTP_01 -v -count=1
go test -race ./internal/pool/ ./internal/acp/... ./internal/server/ ./internal/admin/...
go build ./...
```

All four regression tests PASS (not SKIP, not FAIL). go build exits 0. No new race conditions.
</verification>

<success_criteria>
1. TestRegression_REL_POOL_01 PASS: pool size >= 1 after transient respawn failure; acquire timeout returns ErrPoolExhausted (REL-POOL-01).
2. TestRegression_REL_POOL_02 PASS: pool.Close() cancels >= 2 in-flight sessions; cancelsAfter >= 2 (REL-POOL-02).
3. TestRegression_REL_HTTP_01 PASS: elapsed < 1s — admin SSE unwinds in < 1s on Ctrl-C (REL-HTTP-01).
4. TestRegression_REL_POOL_03 PASS: chunksDelivered == iterations — no silent empty 200s (REL-POOL-03).
5. os.Exit(1) in main.go is preceded by explicit cleanup() — verified by code review (no test covers main.go directly; see RESEARCH.md Pitfall 2).
6. Per-surface adapters map ErrPoolExhausted to 503 + Retry-After:5 with D-07 bodies — verified by grep in acceptance_criteria.
7. POOL_ACQUIRE_TIMEOUT_MS env var is net-new; existing deployments pick up the 30s default automatically.
8. go test -race ./internal/pool/ ./internal/acp/... ./internal/server/ ./internal/admin/... exits 0 (no regressions).
</success_criteria>

<output>
Create `.planning/phases/15-fix-critical-high/15-01-SUMMARY.md` when done.
Include: which files changed, actual lines modified per finding, commit SHAs, test output snippets, any deviations from plan, and whether REQUIREMENTS.md REL-POOL-01/02/03 and REL-HTTP-01 were flipped to fulfilled.
</output>
