---
phase: 15-fix-critical-high
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapter/openai/sse.go
  - internal/adapter/ollama/ndjson.go
  - internal/adapter/openai/regression_rel_http_02_test.go
  - internal/adapter/openai/regression_rel_http_03_test.go
  - internal/adapter/ollama/regression_rel_http_03_test.go
autonomous: true
requirements:
  - REL-HTTP-02
  - REL-HTTP-03
must_haves:
  truths:
    - "On the OpenAI idle-timeout path, StopWatchdog is NOT called — the watchdog fires naturally and issues ACP Cancel before slot return"
    - "On the OpenAI write-error path, ACP Cancel is issued (explicitly or via watchdog) before slot return"
    - "When a kiro-cli worker dies mid-stream (rerr != nil in finalizeSSE), OpenAI clients receive a data: error frame followed by data: [DONE]"
    - "When a kiro-cli worker dies mid-stream (rerr != nil in finalizeNDJSON), Ollama clients receive a done:true done_reason:error envelope"
    - "Both mid-stream death events are logged at WARN with session_id, worker_pid, kiro_exit_code (if available), and bytes_streamed fields (per D-09/D-10)"
    - "Anthropic surface already emits a terminal error frame and requires no change (asymmetry documented in SUMMARY)"
  artifacts:
    - path: "internal/adapter/openai/sse.go"
      provides: "idleC arm no longer calls StopWatchdog; finalizeSSE emits error frame + [DONE] + WARN log on rerr != nil"
      contains: "upstream_disconnect"
    - path: "internal/adapter/ollama/ndjson.go"
      provides: "finalizeNDJSON emits done:true + done_reason:error + WARN log on rerr != nil"
      contains: "upstream_disconnect: worker terminated mid-stream"
  key_links:
    - from: "internal/adapter/openai/sse.go"
      to: "ACP cancel"
      via: "idleC arm no longer suppresses watchdog; watchdog fires Cancel(sessionID) naturally"
      pattern: "StopWatchdog"
    - from: "internal/adapter/openai/sse.go"
      to: "OpenAI SSE client"
      via: "finalizeSSE writes error JSON frame + [DONE] when rerr != nil"
      pattern: "upstream_disconnect"
    - from: "internal/adapter/ollama/ndjson.go"
      to: "Ollama NDJSON client"
      via: "finalizeNDJSON writes done:true frame + done_reason:error when rerr != nil"
      pattern: "upstream_disconnect"
---

<objective>
Fix 2 confirmed reliability findings: H-2 (High) and H-3 (High).

Purpose: Close the OpenAI idle-timeout ACP Cancel suppression and the silent mid-stream worker
death paths that leave clients with half-finished answers and leak pool slots into an
indeterminate state.

Output:
- internal/adapter/openai/sse.go — H-2: remove StopWatchdog from idleC arm; H-3: emit surface-native terminal error frame + WARN log in finalizeSSE (D-09, D-10 per REL-HTTP-02, REL-HTTP-03)
- internal/adapter/ollama/ndjson.go — H-3: emit done:true + done_reason:error + WARN log in finalizeNDJSON (D-09, D-10 per REL-HTTP-03)
- 3 regression tests unskipped and green (H-2 test in openai package; H-3 test in both openai and ollama packages — both unskipped in same commit per D-03)

Plan structure note: This plan has zero server.go overlap. Plan 15-01 owns all server.go edits.
H-2 and H-3 both touch internal/adapter/openai/sse.go at different functions — H-2 edits
the streaming loop body (idleC + applyChunk arms, ~lines 460-484), H-3 edits finalizeSSE
(~lines 543-557). These are non-overlapping regions and can be fixed sequentially in tasks
within this plan with no merge conflict.
</objective>

<execution_context>
@/Users/coreyellis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coreyellis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-fix-critical-high/15-CONTEXT.md
@.planning/phases/15-fix-critical-high/15-RESEARCH.md
@.planning/phases/15-fix-critical-high/15-PATTERNS.md
@.planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md
</context>

<phase_context>
## TDD Shape for Phase 15

All regression test files were written in Phase 14 with t.Skip markers. Each task removes the
t.Skip AND makes the production fix in the SAME atomic commit (D-03). The test going from SKIP
to PASS is the verification proof.

For H-3, TWO test files must be unskipped in the same commit: both openai and ollama regression
tests (D-03 requires both together since H-3 spans two files).

H-2 assertion polarity flip: the pre-fix test asserts watchdogCalled==true && watchdogStopped==true
(demonstrating the bug — StopWatchdog was called). The post-fix assertion is watchdogCalled==false
(StopWatchdog is no longer called on the idle path). This is a polarity flip.

H-3 polarity flip (OpenAI): pre-fix body does NOT contain "data: [error" and NOT "data: [DONE]".
Post-fix body DOES contain both.

H-3 polarity flip (Ollama): pre-fix body does NOT contain "done_reason":"error". Post-fix DOES.

## Anthropic Surface Asymmetry (confirmed in RESEARCH.md)

internal/adapter/anthropic/sse.go:785-795 already emits `event: error` via writeSSEError on
rerr != nil. No change needed for Anthropic in H-3. Document this in the SUMMARY.
</phase_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H-2 — Remove StopWatchdog from OpenAI idle-timeout path (REL-HTTP-02)</name>
  <files>internal/adapter/openai/sse.go, internal/adapter/openai/regression_rel_http_02_test.go</files>
  <read_first>
    Read internal/adapter/openai/sse.go lines 455-490 (the streaming loop — specifically the idleC arm at ~460-462 and the applyChunk write-error arm at ~482-484; both call run.StopWatchdog() which is the bug).
    Read internal/adapter/ollama/ndjson.go lines 425-455 (the correct idle-timeout analog — does NOT call StopWatchdog; instead the watchdog fires naturally when cancelFn runs on return; this is the behavior to mirror in openai/sse.go).
    Read internal/adapter/openai/regression_rel_http_02_test.go in full (t.Skip, pre-fix assertion "watchdogCalled==true && watchdogStopped==true", post-fix target "watchdogCalled==false").
  </read_first>
  <behavior>
    - The idleC arm in openai/sse.go removes the run.StopWatchdog() call (and associated if stop := ...; stop != nil { stop() } block). The WARN log + error frame + return that are already in the idleC arm remain unchanged. This mirrors the Ollama ndjson.go idle-timeout path which already does NOT call StopWatchdog.
    - The applyChunk write-error arm: per RESEARCH.md H-2 fix pattern Option A (simpler), the same StopWatchdog removal is applied. The deferred cancelFn on handler return will trigger the watchdog naturally.
    - regression_rel_http_02_test.go: remove t.Skip. Change pre-fix assertion "watchdogCalled && watchdogStopped" to post-fix assertion "!watchdogStopped" (StopWatchdog was never called on the idle path). POLARITY FLIP REQUIRED — read current assertion form exactly from the file before inverting.
  </behavior>
  <action>
    1. In internal/adapter/openai/sse.go, locate the idleC arm (around line 460). Remove the block: `if stop := run.StopWatchdog(); stop != nil { stop() }`. The existing Warn log, error frame write (`fmt.Fprintf`), Flush, and return remain. Do NOT remove any other code in this arm.

    2. In the same file, locate the applyChunk write-error arm (around line 482). Remove the same StopWatchdog block there too. Again the return and any existing error handling remains; only the StopWatchdog suppression is removed.

    3. Verify assumption: grep the entire function for any other StopWatchdog call sites between the loop open and finalizeSSE. If H-3's finalizeSSE also calls StopWatchdog, do NOT remove it there — that is in scope for Task 2, not Task 1.

    4. In internal/adapter/openai/regression_rel_http_02_test.go: remove t.Skip. Read the exact assertion that checks watchdogCalled/watchdogStopped, then flip it: the post-fix condition is that StopWatchdog was NOT called on the idle-timeout path (watchdogCalled == false or watchdogStopped == false, depending on how the test mock tracks it — use the exact variable names in the test file).

    5. Atomic commit per D-03: "fix(REL-HTTP-02): remove StopWatchdog suppression from OpenAI idle-timeout path".
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test -race ./internal/adapter/openai/ -run TestRegression_REL_HTTP_02 -v -count=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TestRegression_REL_HTTP_02 output shows PASS (not SKIP, not FAIL). Assertion: StopWatchdog NOT called on idle path.
    - grep -c 'StopWatchdog' internal/adapter/openai/sse.go after fix is 0 or 1 (only in finalizeSSE if H-3 fix hasn't landed yet); the idleC and applyChunk arms have no StopWatchdog call.
    - go test -race ./internal/adapter/openai/ exits 0 (no regressions in openai adapter).
    - go build ./... exits 0.
  </acceptance_criteria>
  <done>TestRegression_REL_HTTP_02 PASS under -race; idle-timeout path no longer suppresses ACP Cancel watchdog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H-3 — Surface-native terminal error frames + WARN log on mid-stream worker death (REL-HTTP-03)</name>
  <files>internal/adapter/openai/sse.go, internal/adapter/ollama/ndjson.go, internal/adapter/openai/regression_rel_http_03_test.go, internal/adapter/ollama/regression_rel_http_03_test.go</files>
  <read_first>
    Read internal/adapter/openai/sse.go lines 535-560 (finalizeSSE — the rerr != nil branch that currently logs at Debug and returns without emitting a terminal frame; this is the bug site for H-3 OpenAI).
    Read internal/adapter/openai/sse.go lines 460-478 (the idle-timeout path — this is the exact analog: it logs at Warn, writes a data: error JSON frame, writes data: [DONE], flushes; mirror this pattern in finalizeSSE).
    Read internal/adapter/ollama/ndjson.go lines 535-555 (finalizeNDJSON — the rerr != nil branch that currently logs at Debug and returns without emitting a terminal frame; H-3 Ollama bug site).
    Read internal/adapter/ollama/ndjson.go lines 430-455 (idle-timeout path — the analog that already emits done:true + done_reason:error; copy this pattern for finalizeNDJSON).
    Read internal/adapter/openai/regression_rel_http_03_test.go in full (t.Skip, pre-fix assertion "body does NOT contain data: [error", post-fix body DOES contain error frame + [DONE]).
    Read internal/adapter/ollama/regression_rel_http_03_test.go in full (t.Skip, pre-fix assertion "body does NOT contain done_reason:error", post-fix body DOES).
    Read internal/adapter/anthropic/sse.go lines 780-810 to confirm the Anthropic surface already emits event: error — DO NOT touch this file.
    Read internal/pool/pool.go top ~50 lines and the Slot struct definition to locate the field that carries the kiro-cli PID (likely Slot.Pid or similar) — needed for the worker_pid log field.
  </read_first>
  <behavior>
    - In finalizeSSE (openai/sse.go), the rerr != nil branch: change logger.Debug to logger.Warn with the D-09/D-10 required fields: "session_id" (run.SessionID()), "worker_pid" (from the pool Slot/run struct — inspect the run/slot object available in finalizeSSE for a Pid or PID field), "kiro_exit_code" (extract via errors.As(rerr, &exitErr) where exitErr is *exec.ExitError, then exitErr.ExitCode(); if not available, omit the field with a code comment: "// kiro_exit_code omitted: rerr chain has no *exec.ExitError"), "bytes_streamed" (best-effort: use e.bytesStreamed counter if it exists on the emitter struct, else 0 with a comment), and "err" (rerr). After the Warn, write the D-09 OpenAI error envelope and data: [DONE] then flush, then return as before.
    - In finalizeNDJSON (ollama/ndjson.go), the rerr != nil branch: change logger.Debug to logger.Warn with the same D-09/D-10 required fields: "session_id" (run.SessionID()), "worker_pid" (from the run/slot struct — same inspection as OpenAI), "kiro_exit_code" (same errors.As extraction; omit with comment if not available), "bytes_streamed" (best-effort counter; 0 with comment if not present), and "err" (rerr). After the Warn, add the isChat/!isChat branch with done:true + done_reason:error envelope. Then return as before.
    - Both openai and ollama H-3 regression tests: remove t.Skip in the same commit. Both tests now assert that their respective error envelopes ARE present in the response body.
  </behavior>
  <action>
    1. In internal/adapter/openai/sse.go, finalizeSSE function, rerr != nil branch: Change the Debug log call to a Warn call with all 4 D-09/D-10 required fields plus "err". The call must include: "session_id" and run.SessionID(), "worker_pid" and the slot/run PID field (inspect the run object — check for run.Slot().Pid, run.WorkerPID(), or similar; use the actual field name found in the struct), "kiro_exit_code" obtained via `var exitErr *exec.ExitError; if errors.As(rerr, &exitErr) { ... exitErr.ExitCode() }` (log the field only if errors.As succeeds; if it does not succeed, skip the field — do NOT log a zero value), "bytes_streamed" and e.bytesStreamed (if the field exists on the emitter struct; otherwise log 0 with a comment "// bytes_streamed: counter not yet wired"), and "err" and rerr. After the Warn, add the two fmt.Fprintf calls per D-09 (exact JSON strings in CONTEXT.md D-09 and PATTERNS.md H-3 openai fix pattern). Add e.flusher.Flush() after the second Fprintf. Keep the existing StopWatchdog block (if present in this branch) and the return statement. Import "os/exec" if not already imported; import "errors" if not already imported.

    2. In internal/adapter/ollama/ndjson.go, finalizeNDJSON function, rerr != nil branch: Apply the same D-09/D-10 field expansion. Change the Debug log to Warn with: "session_id" and run.SessionID(), "worker_pid" and the slot/run PID field (same struct inspection as step 1), "kiro_exit_code" (same errors.As pattern), "bytes_streamed" (best-effort; 0 with comment if no counter), and "err" and rerr. After the Warn, add the isChat/!isChat branch pattern from the idle-timeout path (lines 437-452): construct aggregateOllamaResponse, call chatResponseToWire or generateResponseToWire, set frame.Done = true, frame.DoneReason = "error", frame.Error = "upstream_disconnect: worker terminated mid-stream", call marshalAndWrite. Then return as before with the same emptyResp. Import "os/exec" and "errors" if not already imported.

    3. In both regression test files: remove t.Skip in the SAME commit. Flip assertions from NOT-contains to DOES-contain per RESEARCH.md H-3 polarity specification. The openai test checks the response body for `"code":"upstream_disconnect"` and `[DONE]`. The ollama test checks for `"done_reason":"error"` and `"done":true`.

    4. Confirm Anthropic surface is correct (DO NOT EDIT internal/adapter/anthropic/sse.go). Note in commit message: "Anthropic surface already emits event: error — no change needed".

    5. Atomic commit per D-03: all 4 file changes together: "fix(REL-HTTP-03): surface-native terminal error frames on mid-stream worker death (OpenAI + Ollama)". Both test files must be in this single commit.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test -race ./internal/adapter/openai/ ./internal/adapter/ollama/ -run TestRegression_REL_HTTP_03 -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TestRegression_REL_HTTP_03 in openai package: PASS (not SKIP, not FAIL). Response body contains `upstream_disconnect` and `[DONE]`.
    - TestRegression_REL_HTTP_03 in ollama package: PASS (not SKIP, not FAIL). Response body contains `done_reason` and `error` and `done:true`.
    - Both tests unskipped in the same commit (verify via git log --stat HEAD).
    - grep -c 'upstream_disconnect' internal/adapter/openai/sse.go returns >= 1.
    - grep -c 'upstream_disconnect' internal/adapter/ollama/ndjson.go returns >= 1.
    - grep -c 'Warn.*sse worker terminated' internal/adapter/openai/sse.go returns >= 1 (WARN not Debug).
    - grep -c 'Warn.*ndjson worker terminated' internal/adapter/ollama/ndjson.go returns >= 1 (WARN not Debug).
    - grep -E 'worker_pid.*kiro_exit_code|kiro_exit_code.*worker_pid' internal/adapter/openai/sse.go returns >= 1 (D-09/D-10: both fields present in the Warn call, per D-09 locked decision).
    - grep -E 'worker_pid.*kiro_exit_code|kiro_exit_code.*worker_pid' internal/adapter/ollama/ndjson.go returns >= 1 (D-09/D-10: both fields present in the Warn call, per D-09 locked decision).
    - go test -race ./internal/adapter/openai/ ./internal/adapter/ollama/ ./internal/adapter/anthropic/ exits 0 (no regressions; Anthropic unchanged).
    - go build ./... exits 0.
  </acceptance_criteria>
  <done>Both H-3 regression tests PASS under -race; mid-stream worker death emits surface-native terminal frames on OpenAI and Ollama; WARN log contains all 4 D-09 fields (session_id, worker_pid, kiro_exit_code, bytes_streamed); Anthropic unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiro-cli stdout → SSE/NDJSON frame writer | Error frames written on worker death are constructed from static strings + session_id (internal UUID) — no untrusted data echoed to client |
| rerr chain → client error message | Error message is a static string ("upstream_disconnect: worker terminated mid-stream") not a passthrough of rerr.Error() — no internal detail leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | finalizeSSE / finalizeNDJSON error frame | accept | Error message is static string; no kiro-cli internal error detail exposed to client |
| T-15-05 | Denial of Service | ACP Cancel on idle-timeout path | mitigate | H-2 fix ensures Cancel fires before slot returns; no slot stuck in indeterminate state |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; package legitimacy audit in RESEARCH.md confirms clean |
</threat_model>

<verification>
## Cross-task verification (H-2 + H-3)

After both tasks complete and commits land:

```bash
cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway
go test -race ./internal/adapter/openai/ -run 'TestRegression_REL_HTTP_0[23]' -v -count=1
go test -race ./internal/adapter/ollama/ -run TestRegression_REL_HTTP_03 -v -count=1
go test -race ./internal/adapter/openai/ ./internal/adapter/ollama/ ./internal/adapter/anthropic/
go build ./...
```

All three regression tests PASS. go build exits 0. No regressions in any adapter package.
Anthropic surface untouched (document asymmetry in SUMMARY).
</verification>

<success_criteria>
1. TestRegression_REL_HTTP_02 PASS: StopWatchdog NOT called on OpenAI idle-timeout path; ACP Cancel fires naturally via watchdog (REL-HTTP-02).
2. TestRegression_REL_HTTP_03 (OpenAI) PASS: finalizeSSE emits error JSON frame + [DONE] when rerr != nil; logged at WARN with all 4 D-09 fields (REL-HTTP-03).
3. TestRegression_REL_HTTP_03 (Ollama) PASS: finalizeNDJSON emits done:true + done_reason:error envelope when rerr != nil; logged at WARN with all 4 D-09 fields (REL-HTTP-03).
4. Both H-3 test files unskipped in a single atomic commit (D-03 compliance).
5. Anthropic surface confirmed correct and untouched; asymmetry documented in SUMMARY.
6. go test -race ./internal/adapter/openai/ ./internal/adapter/ollama/ ./internal/adapter/anthropic/ exits 0 (no regressions).
</success_criteria>

<output>
Create `.planning/phases/15-fix-critical-high/15-02-SUMMARY.md` when done.
Include: which files changed, actual lines modified per finding, commit SHAs, test output snippets, Anthropic asymmetry note, WARN log field coverage (confirm all 4 D-09 fields present), and whether REQUIREMENTS.md REL-HTTP-02 and REL-HTTP-03 were flipped to fulfilled.
</output>
