---
phase: 06-tool-call-path
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/adapter/anthropic/wire.go
  - internal/adapter/anthropic/sse.go
  - internal/adapter/anthropic/handlers.go
  - internal/adapter/anthropic/wire_test.go
  - internal/adapter/anthropic/handlers_test.go
  - internal/adapter/anthropic/sse_golden_test.go
  - internal/adapter/anthropic/render_test.go
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, anthropic, sse, tool-use, no-coerce]

must_haves:
  truths:
    - "Anthropic POST /v1/messages with tools[] decodes tools[].input_schema into canonical.ToolSpec.Parameters (closes TODO(Phase 6) at wire.go:242) per D-14"
    - "Anthropic tool_choice ({type:'auto'|'any'|'tool', name?}) decodes into canonical.ToolChoice per D-14"
    - "Non-streaming /v1/messages with tool_calls returns content[] with a tool_use block carrying object input (NOT JSON string) — Phase 3.1 render path already shipped; Slice 1 collect.go now produces ContentKindToolUse parts so the path is reachable"
    - "Streaming /v1/messages emits the full SDK-expected content_block_start (tool_use, input: empty-object) → one content_block_delta (input_json_delta with full args) → content_block_stop sequence per D-07 Anthropic"
    - "Anthropic surface does NOT call engine.CoerceToolCall — per D-01 verification, model-emitted bare JSON text on /v1/messages is preserved verbatim (no synthetic tool_use synthesis)"
  artifacts:
    - path: internal/adapter/anthropic/wire.go
      provides: "anthropicToolSpec → canonical.ToolSpec decoder (closes TODO(Phase 6) at line 242)"
    - path: internal/adapter/anthropic/sse.go
      provides: "applyChunk handles ChunkKindToolCall: content_block_start{tool_use, input:{}} + input_json_delta + content_block_stop with proper block-index discipline"
    - path: internal/adapter/anthropic/sse_golden_test.go
      provides: "TestSSE_Golden_ToolUse multi-event wire-shape lock + block-index correctness"
  key_links:
    - from: "internal/adapter/anthropic/wire.go::wireToChatRequest"
      to: "req.Tools (canonical.ToolSpec slice) + req.ToolChoice (canonical.ToolChoice)"
      via: "per-tool loop with Parameters: t.InputSchema; polymorphic tool_choice decode"
      pattern: "canonical\\.ToolSpec\\{.*Parameters.*InputSchema"
    - from: "internal/engine/collect.go (Slice 1)"
      to: "internal/adapter/anthropic/render.go::tool_use block emission (Phase 3.1 ship)"
      via: "Message.Content ContentKindToolUse parts populated by Slice 1 Task 2"
      pattern: "ContentKindToolUse"
    - from: "internal/adapter/anthropic/sse.go::applyChunk"
      to: "content_block_start{tool_use} + content_block_delta{input_json_delta} + content_block_stop"
      via: "extension of the 3-step state machine; CR-01 pointer-to-empty-map for input field"
      pattern: "tool_use"
---

<objective>
Anthropic vertical slice for Phase 6. Closes TOOL-01 (Anthropic tool_use side per SC #4 — already partly shipped via Phase 3.1 render path, now reachable thanks to Slice 1's collect.go ContentKindToolUse extraction), TOOL-02 (Anthropic NO-coerce verification per D-01 — proves the intentional asymmetry), and TOOL-03 (Anthropic tools[] normalization per D-14, closes the `TODO(Phase 6)` at `internal/adapter/anthropic/wire.go:242`).

Purpose: After this plan, loop24-client / GSD Pi with `ANTHROPIC_BASE_URL=http://localhost:11434` and `tools: [...]` receives correct tool_use blocks in Anthropic's native wire shape. The Phase 3.1 render path (`internal/adapter/anthropic/render.go:113-131` — CR-01 pointer-to-map fix preserving "empty input but field present") gets exercised for the first time because Slice 1's `engine.Collect` now emits parallel `ContentKindToolUse` parts. Streaming emits the SDK-expected full event sequence: `content_block_start` (tool_use header, `input:{}` per CR-01) → one `content_block_delta` with `input_json_delta` carrying the entire serialized args → `content_block_stop`. The block-index discipline from Phase 3.1 D-04 state machine is extended without disruption.

CRITICAL ASYMMETRY: Anthropic does NOT call `engine.CoerceToolCall`. Per D-01, running coerce on Anthropic would silently rewrite `messages.stream()` consumers' assistant text into tool_use blocks, surprising the client. This plan adds NO coerce hook in handlers.go and includes a test to lock that asymmetry (bare JSON text on /v1/messages is preserved verbatim, no synthetic tool_use synthesis).

Output: Two modified source files (wire.go, sse.go) plus three test additions/extensions (wire_test.go, sse_golden_test.go, render_test.go). Zero new dependencies. Runs in parallel with Slices 2 + 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md

# Source files this plan modifies
@internal/adapter/anthropic/wire.go
@internal/adapter/anthropic/sse.go
@internal/adapter/anthropic/wire_test.go
@internal/adapter/anthropic/sse_golden_test.go
@internal/adapter/anthropic/render_test.go

# Phase 3.1 ship — DO NOT MODIFY but read for the CR-01 pattern to mirror in sse.go
@internal/adapter/anthropic/render.go
@internal/adapter/anthropic/handlers.go

# Cross-adapter reference — Ollama Phase 2 decoder is the proven analog for D-14 (Anthropic structurally similar)
@internal/adapter/ollama/wire.go

# Canonical contract from Slice 1
@internal/canonical/chat.go
@internal/canonical/chunk.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close TODO(Phase 6) — anthropicToolSpec → canonical.ToolSpec + tool_choice decode (D-14)</name>
  <files>internal/adapter/anthropic/wire.go, internal/adapter/anthropic/wire_test.go</files>
  <read_first>
    - internal/adapter/anthropic/wire.go (lines 137-141 anthropicToolSpec wire type already declared; lines 202-206 and lines 239-243 the TODO(Phase 6) markers — confirm exact line numbers in current file; line 66 ToolChoice json.RawMessage field; the full wireToChatRequest function — find the message walk loop and the existing tools decode-without-translate path that this task replaces)
    - internal/adapter/anthropic/wire_test.go (existing TestWireToChatRequest tests; understand the table shape)
    - internal/adapter/ollama/wire.go (lines 321-330 — the proven Phase 2 per-tool walk pattern + decodeMessageContent polymorphic decode for tool_choice variant of input)
    - internal/canonical/chat.go (canonical.ToolSpec{Name, Description, Parameters map[string]any}; canonical.ToolChoice{Type, Name})
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-14 Anthropic tools normalization)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/anthropic/wire.go lines 202-206/242" section)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Pattern 3 cross-surface decoder analog)
  </read_first>
  <behavior>
    - **anthropicToolSpec wire type already exists (lines 137-141) — no change.** Confirmed shape: `Name string`, `Description string`, `InputSchema map[string]any \`json:"input_schema,omitempty"\``. Anthropic's `input_schema` maps DIRECTLY to canonical `ToolSpec.Parameters` (both are JSON-Schema-shaped object maps).
    - **wireToChatRequest extension (closes TODO(Phase 6) at line 242):** after the existing message walk + before `return req`, add the per-tool loop:
      ```
      for _, t := range w.Tools {
          if t.Name == "" { continue }   // defensive — Anthropic spec requires name
          req.Tools = append(req.Tools, canonical.ToolSpec{
              Name:        t.Name,
              Description: t.Description,
              Parameters:  t.InputSchema,
          })
      }
      ```
      Remove the `// TODO(Phase 6)` comment block at line 239-243 (or wherever it sits in the current file).
    - **tool_choice decode (D-14 second sentence):** decode `w.ToolChoice` (currently `json.RawMessage` at line 66) polymorphically per Anthropic shape `{type:"auto"|"any"|"tool", name?}`:
      - JSON object `{"type":"auto"}` → `canonical.ToolChoice{Type:"auto"}`
      - JSON object `{"type":"any"}` → `canonical.ToolChoice{Type:"required"}` (Anthropic uses "any" where OpenAI uses "required"; normalize at the boundary so engine sees one canonical type — but if canonical.ToolChoice has no "any" → "required" normalization rule documented, prefer to preserve `Type:"any"` verbatim and let the engine treat both as semantic-equivalents. Recommendation: preserve "any" verbatim to keep canonical lossless; cross-surface semantic mapping is a future concern.)
      - JSON object `{"type":"tool","name":"<X>"}` → `canonical.ToolChoice{Type:"tool", Name:"X"}`
      - Empty / absent / unknown shape → `req.ToolChoice = nil` (accept-and-ignore).
      Use the polymorphic-decode pattern from `internal/adapter/openai/wire.go:138-164` `decodeMessageContent` as the structural model. Defensive: catch decode errors and accept-and-ignore.
    - **wire_test.go additions:**
      1. `TestWireToChatRequest_Tools_Anthropic` — request body `{"model":"auto","max_tokens":100,"messages":[{"role":"user","content":"hi"}],"tools":[{"name":"get_weather","description":"Get current weather","input_schema":{"type":"object","properties":{"location":{"type":"string"}}}}]}` decodes to `req.Tools` with one entry; Name/Description/Parameters populated; verify Parameters is the input_schema map verbatim.
      2. `TestWireToChatRequest_ToolChoice_Auto` — `tool_choice: {"type":"auto"}` decodes to `canonical.ToolChoice{Type:"auto"}`.
      3. `TestWireToChatRequest_ToolChoice_Any` — `tool_choice: {"type":"any"}` decodes to `canonical.ToolChoice{Type:"any"}` (verbatim preservation).
      4. `TestWireToChatRequest_ToolChoice_NamedTool` — `tool_choice: {"type":"tool","name":"get_weather"}` decodes to `canonical.ToolChoice{Type:"tool", Name:"get_weather"}`.
      5. `TestWireToChatRequest_ToolChoice_Unknown` — `tool_choice: 42` or `tool_choice: "auto"` (string, not object — Anthropic spec requires object) → `req.ToolChoice == nil`.
      6. `TestWireToChatRequest_Tools_Empty_Anthropic` — request without `tools` field → `req.Tools == nil` (no spurious empty slice).
  </behavior>
  <action>
    Modify `internal/adapter/anthropic/wire.go`:
      1. In `wireToChatRequest`, after the existing message walk + before `return req`, add the per-tool loop populating `req.Tools` from `w.Tools` (per behavior block — `InputSchema` maps to canonical `Parameters` directly).
      2. Add the polymorphic `tool_choice` decode populating `req.ToolChoice`. Use a typed-object-first / accept-and-ignore-on-failure pattern (mirror `decodeMessageContent` from `internal/adapter/openai/wire.go:138-164`).
      3. Remove the `TODO(Phase 6)` comment block (currently at lines 239-243 per RESEARCH; confirm in current file).

    Extend `internal/adapter/anthropic/wire_test.go` with the six test cases per the behavior block. Reuse any existing helper (likely a JSON-body-string → ChatRequest harness similar to Phase 3.1 tests).

    Decision recorded: tool_choice "any" is preserved verbatim in canonical (NOT normalized to "required"). Rationale: lossless preservation at the boundary; cross-surface semantic interpretation (any vs required) is engine/hook concern, not adapter. Document this decision in a code comment in wire.go above the polymorphic decode.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/anthropic/... -run "TestWireToChatRequest" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TODO(Phase 6)' internal/adapter/anthropic/wire.go` returns 0 (the TODO marker is removed).
    - `grep -n 'canonical.ToolSpec' internal/adapter/anthropic/wire.go` shows the canonical.ToolSpec assembly in wireToChatRequest.
    - `grep -n 'canonical.ToolChoice' internal/adapter/anthropic/wire.go` shows the canonical.ToolChoice assembly.
    - `grep -n 'InputSchema' internal/adapter/anthropic/wire.go` shows Parameters: t.InputSchema mapping.
    - `TestWireToChatRequest_Tools_Anthropic` passes — Parameters populated from input_schema.
    - All four ToolChoice variant tests pass (auto, any, named-tool, unknown-graceful-degradation).
    - `go test ./internal/adapter/anthropic/... -race -count=1` clean.
  </acceptance_criteria>
  <done>
    anthropic/wire.go closes the TODO(Phase 6) decoder; tools[] flow into canonical.ToolSpec and tool_choice flows into canonical.ToolChoice; lossless "any" preservation documented; six new tests lock the behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE applyChunk extension for ChunkKindToolCall (content_block_start{tool_use} + input_json_delta + content_block_stop) with CR-01 pointer-to-empty-map preservation</name>
  <files>internal/adapter/anthropic/sse.go, internal/adapter/anthropic/sse_golden_test.go, internal/adapter/anthropic/render_test.go</files>
  <read_first>
    - internal/adapter/anthropic/sse.go (FULL FILE — focus on applyChunk method around lines 204-220 (the header switch); lines 248-266 the delta switch; the existing text + thinking branches as the structural model; the 3-step state machine — identify-header → close+bump-on-kind-transition → open-block → emit-delta; the blockIndex discipline from Phase 3.1 D-04)
    - internal/adapter/anthropic/render.go (lines 113-131 — the CR-01 pointer-to-empty-map fix on `tool_use.input`. This pattern is the LOAD-BEARING shape constraint per RESEARCH Pitfall 1 — the streaming path must replicate the same `*map[string]any{}` indirection so omitempty preserves "empty input but field present")
    - internal/adapter/anthropic/sse_golden_test.go (existing golden-fixture style — testdata path conventions, byte-comparison patterns; the existing sse_text_only / sse_text_then_thinking / sse_message_start fixtures from Phase 3.1)
    - internal/adapter/anthropic/testdata/ (existing golden files — Phase 3.1 ships sse_text_only.txt and similar)
    - internal/adapter/anthropic/render_test.go (existing TestRender_ToolUse tests — confirm what's already covered for the non-streaming render path)
    - internal/canonical/chunk.go (ToolCallChunk{ID, Name, Args} from Slice 1)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-07 Anthropic streaming, Claude's Discretion §"content_block_start initial input value: {} vs null — pick {} (matches non-streaming render)")
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Code Examples "Anthropic tool_use streaming sequence"; Pitfall 1 input:null vs input:{}; Pitfall 7 block-index discipline)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/anthropic/sse.go" + Pattern F "Anthropic CR-01 pointer-to-map fix")
  </read_first>
  <behavior>
    - **No new types for tool_use OUTBOUND payload — reuse from render.go:** the `tool_use` content block shape is already defined for the non-streaming render path (`internal/adapter/anthropic/render.go:113-131`). For the SSE path, define streaming-specific wire types:
      - `toolUseBlockHeader { Type string \`json:"type"\`; ID string \`json:"id"\`; Name string \`json:"name"\`; Input *map[string]any \`json:"input,omitempty"\` }` — note `*map[string]any` per CR-01 (Pitfall 1). The pointer-to-empty-map is the load-bearing trick that makes `omitempty` emit `"input":{}` instead of `"input":null` (default Go encoding of a nil map). Anthropic's `@anthropic-ai/sdk` MessageStream parser REJECTS null input on tool_use blocks (per RESEARCH lines 326-327 Anthropic spec line 1093).
      - `inputJSONDelta { Type string \`json:"type"\`; PartialJSON string \`json:"partial_json"\` }` — `Type: "input_json_delta"`, `PartialJSON` carries the full serialized args (one delta per CONTEXT D-07 Anthropic — kiro emits atomically per D-06; no chunking needed).
    - **applyChunk header switch extension (lines 204-220):** add `case canonical.ChunkKindToolCall:` to the existing switch that currently handles `ChunkKindText` and `ChunkKindThought`. On ChunkKindToolCall with non-nil `c.ToolCall`:
      - Build the header via the CR-01 pattern:
        ```
        emptyMap := map[string]any{}
        header = toolUseBlockHeader{
            Type:  "tool_use",
            ID:    c.ToolCall.ID,
            Name:  c.ToolCall.Name,
            Input: &emptyMap,   // pointer-to-empty-map preserves "input":{} through omitempty
        }
        ```
      - The header flows into the existing step-2 close-then-bump path at the existing kind-transition lines (currently around line 222-232). DO NOT add a second blockIndex bump — Pitfall 7 (the existing step-2 already increments).
    - **applyChunk delta switch extension (lines 248-266):** add `case canonical.ChunkKindToolCall:` to the existing switch. On non-nil `c.ToolCall.Args`, marshal `c.ToolCall.Args` via `json.Marshal` → string. Emit ONE `content_block_delta` event:
      ```
      argsJSON, _ := json.Marshal(c.ToolCall.Args)
      return e.writeEvent("content_block_delta", contentBlockDelta{
          Type:  "content_block_delta",
          Index: e.blockIndex,
          Delta: inputJSONDelta{Type: "input_json_delta", PartialJSON: string(argsJSON)},
      })
      ```
      - Per D-07 Anthropic + CONTEXT specifics §"Anthropic streaming tool_use block index": this single-delta form is wire-spec-compliant (per Anthropic docs lines 416-425 — partial_json strings concatenate to form the input object; single delta carrying the entire JSON is valid).
    - **content_block_stop:** handled by the existing close-on-kind-transition or stream-close finalization path — no new code needed beyond the header + delta. The Phase 3.1 D-04 state machine already emits `content_block_stop` when a block closes.
    - **stop_reason on message_delta:** per RESEARCH Open Question 2 + Code Examples — when the turn ended with a tool call, Anthropic emits `stop_reason: "tool_use"` (not `end_turn`). Decision: the canonical StopReason enum extension (adding `StopToolUse`) is OUT OF SCOPE for this plan — the simplest plan-local approach is to post-fixup the `mapStopReason` call in render.go for streaming (`anthropic/sse.go` finalize path) when `Message.ToolCalls` is non-empty OR when `tool_use` blocks were emitted during the stream. Track `toolUseEmitted bool` on the sseEmitter; on finalize, if true override `stop_reason` to `"tool_use"`. (This mirrors the OpenAI Slice 3 finish_reason post-fixup pattern.)
    - **goleak / D-05 single-goroutine invariant:** `toolUseEmitted` and the streaming `[]ToolCall` aggregator are touched ONLY inside the select-loop goroutine. No mutex; no shared state.
    - **render_test.go regression (verify Phase 3.1 path is reachable):** add `TestRender_ToolUse_ViaCollectIntegration` (or similar) — construct a `*canonical.ChatResponse` with `Message.Content` containing a `ContentKindToolUse` part (simulating Slice 1's collect.go output), pass it through the existing Phase 3.1 render function, assert the `content[].type == "tool_use"`, `.input` is a JSON object literal (NOT null — the CR-01 fix preserved). This test verifies that Slice 1's `engine.Collect` ContentKindToolUse emission lights up the Phase 3.1 non-streaming render path.
    - **sse_golden_test.go addition — `TestSSE_Golden_ToolUse`:** golden-fixture-style test that drives a fake chunk stream `[text-chunk("I'll check weather. "), tool_call-chunk(ID=toolu_01, Name=get_weather, Args={location:"NYC"}), stream-close]`. Capture full SSE output. Assert the multi-event structure (line-by-line or full-byte fixture-file comparison):
      1. `event: message_start` + `data: {"type":"message_start", ...}` (existing).
      2. `event: content_block_start` + `data: {"type":"content_block_start","index":0,"content_block":{"type":"text","text":""}}` (existing).
      3. `event: content_block_delta` + `data: {"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"I'll check weather. "}}` (existing).
      4. `event: content_block_stop` + `data: {"type":"content_block_stop","index":0}` (existing).
      5. **NEW** `event: content_block_start` + `data: {"type":"content_block_start","index":1,"content_block":{"type":"tool_use","id":"toolu_01","name":"get_weather","input":{}}}` (note `input:{}`, NOT `null`).
      6. **NEW** `event: content_block_delta` + `data: {"type":"content_block_delta","index":1,"delta":{"type":"input_json_delta","partial_json":"{\"location\":\"NYC\"}"}}`.
      7. **NEW** `event: content_block_stop` + `data: {"type":"content_block_stop","index":1}`.
      8. `event: message_delta` + `data: {"type":"message_delta","delta":{"stop_reason":"tool_use",...},...}` (note `stop_reason:"tool_use"`, NOT `end_turn`).
      9. `event: message_stop` + `data: {"type":"message_stop"}` (existing).
      - **Byte-level assertions:** `"input":{}` literal appears in line 5; `"input":null` does NOT appear anywhere; `"stop_reason":"tool_use"` appears in line 8 (NOT `"end_turn"`); the block-index sequence is 0 → 0 → 0 → 0 → 1 → 1 → 1.
      - Pitfall 7 (block-index discipline): assert the index does NOT skip from 0 to 2 (which would indicate a double-increment).
  </behavior>
  <action>
    Modify `internal/adapter/anthropic/sse.go`:
      1. Add the two new wire types `toolUseBlockHeader` (with `Input *map[string]any` per CR-01) and `inputJSONDelta` (with `PartialJSON string`).
      2. In `applyChunk` header switch (currently around lines 204-220), add `case canonical.ChunkKindToolCall:` building `header = toolUseBlockHeader{Type:"tool_use", ID, Name, Input: &emptyMap}` where `emptyMap := map[string]any{}`. Let the existing step-2 close-then-bump path handle the kind transition (no extra blockIndex bump).
      3. In `applyChunk` delta switch (currently around lines 248-266), add `case canonical.ChunkKindToolCall:` marshaling `c.ToolCall.Args` via `json.Marshal` and emitting ONE `content_block_delta` event with `inputJSONDelta{Type:"input_json_delta", PartialJSON: string(argsJSON)}`.
      4. Add `toolUseEmitted bool` to the `sseEmitter` struct. Set true after the delta emit. On finalize/`message_delta` emission, if true override the `stop_reason` to `"tool_use"` (currently sourced from `mapStopReason(canonical.StopReason)`).

    Extend `internal/adapter/anthropic/render_test.go` with `TestRender_ToolUse_ViaCollectIntegration` per the behavior block — constructs a `*canonical.ChatResponse` with `Message.Content` containing a `ContentKindToolUse` part and asserts the non-streaming render emits a tool_use block with `input` as JSON object (NOT null). This locks the integration with Slice 1's collect.go change.

    Extend `internal/adapter/anthropic/sse_golden_test.go` with `TestSSE_Golden_ToolUse`. If golden fixtures are file-based (`testdata/sse_tool_use.txt`), create the fixture file with the exact expected output AND drive the test via the existing fixture-compare helper. The fixture file MUST include the `input:{}` literal and the `stop_reason:"tool_use"` override.

    CRITICAL PRESERVATION CHECKS (Pitfall 1 + Pitfall 7):
    - The `*map[string]any` pointer-to-empty-map is THE load-bearing CR-01 pattern. Do NOT shortcut to `map[string]any{}` directly — Go's encoding/json emits `null` for nil maps under `omitempty`; the pointer indirection is what preserves the field. Verify with a unit assertion (TestSSE_Golden_ToolUse line 5).
    - The blockIndex bumps EXACTLY once per kind transition. Do NOT add a second bump inside the tool_use branch. Verify with the index-sequence assertion in TestSSE_Golden_ToolUse (0→0→0→0→1→1→1, NEVER skipping to 2).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/anthropic/... -run "TestSSE_Golden_ToolUse|TestRender_ToolUse|TestSSE" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChunkKindToolCall" internal/adapter/anthropic/sse.go` returns ≥2 (header switch + delta switch).
    - `grep -n "toolUseBlockHeader" internal/adapter/anthropic/sse.go` shows the new wire type and its use in the header switch.
    - `grep -n "input_json_delta" internal/adapter/anthropic/sse.go` shows the inputJSONDelta usage in the delta switch.
    - `grep -n '\*map\[string\]any' internal/adapter/anthropic/sse.go` shows the pointer-to-map type on toolUseBlockHeader.Input (CR-01 pattern preserved).
    - `TestSSE_Golden_ToolUse` passes with the full multi-event sequence asserted.
    - `TestSSE_Golden_ToolUse` byte-level: `"input":{}` literal present; `"input":null` NOT present anywhere.
    - `TestSSE_Golden_ToolUse` byte-level: `"stop_reason":"tool_use"` in the message_delta line (NOT `"end_turn"`).
    - `TestSSE_Golden_ToolUse` block-index assertion: sequence is 0,0,0,0,1,1,1 (or whatever the test stream produces — but NEVER skipping kind-transition increments).
    - `TestRender_ToolUse_ViaCollectIntegration` passes — Slice 1's ContentKindToolUse → render path is reachable.
    - `go test ./internal/adapter/anthropic/... -race -count=1` clean.
  </acceptance_criteria>
  <done>
    Anthropic SSE applyChunk emits the SDK-expected tool_use sequence (content_block_start with input:{} per CR-01 → single input_json_delta → content_block_stop → message_delta with stop_reason:"tool_use"); block-index discipline preserved (Pitfall 7); Phase 3.1 non-streaming render path verified reachable via Slice 1's ContentKindToolUse emission.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify NO-coerce asymmetry — handlers.go MUST NOT call engine.CoerceToolCall (lock-test)</name>
  <files>internal/adapter/anthropic/handlers_test.go</files>
  <read_first>
    - internal/adapter/anthropic/handlers.go (FULL FILE — confirm no engine.CoerceToolCall call exists today; understand the non-streaming + streaming branch structure)
    - internal/adapter/anthropic/handlers_test.go (existing test scaffolding)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-01 "Anthropic does NOT call it" — locked decision; D-17 scenario 5 "Anthropic does NOT coerce")
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Anti-Patterns §"Running coerceToolCall on Anthropic")
    - internal/engine/coerce.go (Slice 1 — confirm the public function exists and would technically be importable here)
  </read_first>
  <behavior>
    - **Critical asymmetry test:** add a static-source assertion test `TestAnthropic_DoesNotCallCoerceToolCall` in `handlers_test.go`. This test reads the handlers.go source file at test time and asserts that the substring `engine.CoerceToolCall` does NOT appear anywhere in the file. This is a regression guard against a future refactor that "fixes inconsistency" by unifying the three adapter handlers — per D-01 the asymmetry is INTENTIONAL.
      - Use `os.ReadFile("handlers.go")` (or `filepath.Join` with the test working dir); the test file lives in the same package so the relative path is "handlers.go".
      - Assertion: `if strings.Contains(string(data), "engine.CoerceToolCall") { t.Fatal("Anthropic surface MUST NOT call engine.CoerceToolCall — see CONTEXT.md D-01. Running coerce would silently rewrite messages.stream() consumers' assistant text into tool_use blocks, surprising the client.") }`.
    - **Behavioral lock test:** add `TestAnthropic_ModelEmittedJSONText_PreservedVerbatim` — drive a fake-engine stream that returns `Message.Content[0] = {Kind: ContentKindText, Text: "{\"location\":\"NYC\"}"}` plus a request with `tools: [{name:"get_weather", input_schema: ...}]`. The non-streaming handler should produce an Anthropic response with `content[0].text == "{\"location\":\"NYC\"}"` (verbatim) and NO `tool_use` block synthesized. This proves D-17 scenario 5.
      - Use the existing fake-engine pattern from Phase 3.1 handlers_test.go (likely a mock implementing the engine.ACPClient interface — find it in the existing test file and reuse).
      - If no fake-engine harness exists in handlers_test.go (Phase 3.1 may have used a different pattern), simpler approach: skip this behavioral test and rely solely on the static-source test + the E2E scenario 5 in Slice 5. Document that decision in a code comment.
    - **Documentation comment in handlers.go:** add a top-level comment in `internal/adapter/anthropic/handlers.go` (above the non-streaming branch OR at the top of the file) noting: `// PHASE 6 INVARIANT: Anthropic does NOT call engine.CoerceToolCall — per CONTEXT D-01, running coerce on Anthropic would silently rewrite messages.stream() consumers' assistant text into tool_use blocks. See TestAnthropic_DoesNotCallCoerceToolCall for the regression guard.` This comment is the documentation half of the asymmetry; the test is the enforcement half.
  </behavior>
  <action>
    Add a new test `TestAnthropic_DoesNotCallCoerceToolCall` in `internal/adapter/anthropic/handlers_test.go` that reads `handlers.go` (via `os.ReadFile("handlers.go")`) at test time and asserts `engine.CoerceToolCall` does NOT appear in the source. The test message must explain WHY (cite CONTEXT D-01).

    Optionally add `TestAnthropic_ModelEmittedJSONText_PreservedVerbatim` if the existing handlers_test.go scaffolding has a fake-engine pattern reusable for this — drive bare JSON text through a non-streaming request with tools, assert content preserved + no tool_use synthesized. If no harness exists, skip this test (E2E scenario 5 in Slice 5 covers it) and document the decision.

    Add the PHASE 6 INVARIANT comment block to `internal/adapter/anthropic/handlers.go` per the behavior block. The comment is the human-readable documentation; the test is the enforcement guard.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/anthropic/... -run "TestAnthropic_DoesNotCallCoerceToolCall|TestAnthropic_ModelEmittedJSONText" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "engine.CoerceToolCall" internal/adapter/anthropic/handlers.go` returns 0 (asymmetry locked).
    - `grep -c "PHASE 6 INVARIANT" internal/adapter/anthropic/handlers.go` returns 1 (the documentation comment exists).
    - `TestAnthropic_DoesNotCallCoerceToolCall` passes (source-level static assertion).
    - If `TestAnthropic_ModelEmittedJSONText_PreservedVerbatim` was added: it passes (behavioral assertion — bare JSON in assistant text preserved, no tool_use synthesized).
    - `go test ./internal/adapter/anthropic/... -count=1` clean.
    - The test failure message on a future refactor that adds `engine.CoerceToolCall` to handlers.go would CITE D-01 and explain the rationale — readable failure message.
  </acceptance_criteria>
  <done>
    Source-level static assertion locks the no-coerce asymmetry per D-01; PHASE 6 INVARIANT comment documents the rationale; future refactors that "unify" the three handlers will fail loudly with a citation back to CONTEXT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client request → anthropic.wire.go | Untrusted `tools[]` + `tool_choice` from client. Decoded by the new typed decoders. Anthropic-specific `input_schema` shape. |
| engine.Collect → anthropic.handlers.go non-streaming | Trusted canonical response. Notably: Anthropic does NOT call CoerceToolCall — the absence of this code path is itself a security/correctness property (no untrusted text gets reinterpreted as a tool invocation). |
| anthropic.sse.go writer goroutine → HTTP response stream | Untrusted tool-args (from canonical.ToolCallChunk.Args sourced from kiro) flow into SSE wire bytes (input_json_delta partial_json). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering / SDK conformance | anthropic/sse.go toolUseBlockHeader.Input field | mitigate | CR-01 pointer-to-empty-map pattern preserves `"input":{}` (object) through json.Marshal omitempty. Without this, default Go encoding emits `"input":null` which `@anthropic-ai/sdk` MessageStream parser REJECTS — block delivery fails. The test `TestSSE_Golden_ToolUse` asserts the literal `"input":{}` byte sequence (and `"input":null` absence) as the gate. |
| T-06-09 | Spoofing (intentional invariant lock) | anthropic/handlers.go (lack of CoerceToolCall) | mitigate | Per D-01, Anthropic-native clients (`@anthropic-ai/sdk` / loop24-client) do NOT emit JSON-as-text the way LangChain does. Running coerce here would silently rewrite assistant text into synthesized tool_use blocks — a wire-shape forgery the client never sent. The TestAnthropic_DoesNotCallCoerceToolCall static-source assertion is the regression gate. |
| T-06-10 | Tampering (block-index attack via misaligned state) | anthropic/sse.go applyChunk blockIndex discipline | mitigate | Per Pitfall 7, the Phase 3.1 D-04 state machine bumps blockIndex exactly once per kind transition. The Phase 6 tool_use branch uses the existing step-2 path — does NOT add a second bump. TestSSE_Golden_ToolUse asserts the index sequence (0→0→0→0→1→1→1, no skip to 2). |
| T-V5-03 (inherited) | DoS (oversized tool args from kiro stream) | anthropic/sse.go json.Marshal of c.ToolCall.Args | mitigate | ACP framer line cap bounds upstream notification size. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. |
</threat_model>

<verification>
After all three tasks complete:
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./...` — clean.
2. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/anthropic/... -race -count=1` — all unit + golden + asymmetry-lock tests pass under race detector + testmain_test.go goleak gate.
3. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` — clean.
4. `grep -c "TODO(Phase 6)" internal/adapter/anthropic/wire.go` returns 0.
5. `grep -c "engine.CoerceToolCall" internal/adapter/anthropic/handlers.go` returns 0 (D-01 asymmetry locked).
6. `grep -c '\*map\[string\]any' internal/adapter/anthropic/sse.go` returns ≥1 (CR-01 pattern present on the streaming side).
</verification>

<success_criteria>
- TOOL-01 (Anthropic side, per SC #4): non-streaming `/v1/messages` returns `content[]` with `tool_use` block carrying object input — Phase 3.1 path verified reachable via Slice 1's ContentKindToolUse emission.
- TOOL-02 (Anthropic NO-coerce verification, per D-01 + D-17 scenario 5): static-source assertion test locks the no-CoerceToolCall asymmetry; bare JSON in assistant text preserved verbatim.
- TOOL-03 (Anthropic side, per SC #4 + D-14): `TODO(Phase 6)` at `internal/adapter/anthropic/wire.go:242` closed; `tools[].input_schema` decodes to `canonical.ToolSpec.Parameters`; `tool_choice` polymorphic decode populates `canonical.ToolChoice` (auto / any / named-tool / unknown-graceful).
- D-07 Anthropic: streaming emits `content_block_start{tool_use, input:{}}` → one `content_block_delta{input_json_delta}` → `content_block_stop` with proper block-index discipline; `message_delta` carries `stop_reason:"tool_use"` (NOT `end_turn`); golden fixture locks the byte-level shape.
- CR-01 pattern preserved on streaming side (Pitfall 1 — `*map[string]any` pointer-to-empty-map).
- Block-index discipline preserved (Pitfall 7 — exactly one bump per kind transition).
- Race detector + goleak gate clean.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-04-SUMMARY.md` summarizing: the closed TODO(Phase 6) decoder, the SSE tool_use multi-event sequence, the CR-01 + Pitfall 7 preservation, the intentional NO-coerce asymmetry lock, and the cross-confirm with Slice 1's ContentKindToolUse → render integration.
</output>
