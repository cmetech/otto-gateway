---
phase: 06-tool-call-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/canonical/chunk.go
  - internal/acp/translate.go
  - internal/acp/translate_test.go
  - internal/engine/coerce.go
  - internal/engine/coerce_test.go
  - internal/engine/collect.go
  - internal/engine/collect_test.go
  - internal/engine/build_acp.go
  - internal/engine/build_acp_test.go
autonomous: false
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, coerce, canonical, engine, acp]

must_haves:
  truths:
    - "kiro-emitted tool_call ACP notifications produce a real canonical.ToolCallChunk (kind=ChunkKindToolCall) with ID, Name, Args populated"
    - "engine.Collect aggregates kiro-native ChunkKindToolCall into the response's assistant content as `[tool: <name>]\\n` narration text (per iteration-3 fix to HIGH #1 — non-streaming Ollama/OpenAI render kiro-native tool_calls as narration text). engine.Collect does NOT populate Message.ToolCalls from any chunk source."
    - "Message.ToolCalls population contract (per D-03/D-05/D-07 — iteration-3 reworded): generic engine.Collect does NOT populate Message.ToolCalls from any chunk source. Ollama and OpenAI surfaces populate Message.ToolCalls ONLY via engine.CoerceToolCall (the coerce-from-text path — D-05). Anthropic surface (D-07 exception) populates Message.ToolCalls via its adapter-local Collect (internal/adapter/anthropic/collect.go, Option A1 from 06-04) from kiro-native ChunkKindToolCall chunks. This is the documented exception that mirrors Anthropic's wire protocol where tool_use blocks are native."
    - "engine.CoerceToolCall is canonical-typed, idempotent, returns bool, and follows the locked D-09 9-step algorithm exactly"
    - "engine.buildBlocks emits the full JSON tool catalog inside [Available tools] when req.Tools is non-empty (D-16)"
    - "Property tests at internal/engine/coerce_test.go (per D-20 + ROADMAP SC #5) use testing/quick with MaxCount=1000 to prove never-panic + idempotent + no-match-no-mutation invariants for CoerceToolCall"
    - "Node source byte-level fidelity check on coerceToolCall + pickBestTool is resolved at the END of Wave 1 (before Wave 2 fans out) — blocking human-verify checkpoint per REVIEW concern #3"
  artifacts:
    - path: internal/canonical/chunk.go
      provides: "ToolCallChunk.ID field added (per D-08)"
      contains: "ID string"
    - path: internal/engine/coerce.go
      provides: "CoerceToolCall, pickBestTool, stripFences — Ollama/OpenAI's sole producer of Message.ToolCalls per the per-surface contract"
      exports: ["CoerceToolCall"]
    - path: internal/engine/coerce_test.go
      provides: "Property + table tests + Example_CoerceToolCall"
    - path: internal/engine/collect.go
      provides: "Aggregates ChunkKindToolCall into assistant text as `[tool: <name>]\\n` narration (per iteration-3 fix to HIGH #1); does NOT populate Message.ToolCalls from any chunk source; content-text + thinking aggregation unchanged"
    - path: internal/engine/build_acp.go
      provides: "JSON tool catalog inside [Available tools] section, with debug log when marshal-error fallback fires"
    - path: internal/acp/translate.go
      provides: "tool_call / tool_call_chunk → canonical.Chunk{Kind: ChunkKindToolCall, ToolCall: &ToolCallChunk{ID, Name, Args}}"
  key_links:
    - from: "internal/acp/translate.go (case tool_call/tool_call_chunk)"
      to: "canonical.ToolCallChunk{ID, Name, Args}"
      via: "body.ToolCallID, body.Title, body.Args"
      pattern: "ChunkKindToolCall.*ToolCall:.*ToolCallChunk"
    - from: "internal/engine/coerce.go::CoerceToolCall"
      to: "resp.Message.ToolCalls (appended synthetic entry — sole producer for Ollama/OpenAI per the per-surface contract)"
      via: "pickBestTool top-level-key property overlap scoring"
      pattern: "pickBestTool"
    - from: "internal/engine/collect.go::Collect (ChunkKindToolCall branch)"
      to: "resp.Message.Content[textIdx].Text (appended `[tool: <name>]\\n` narration)"
      via: "per-chunk text-append; Message.ToolCalls untouched"
      pattern: "\\[tool:"
    - from: "internal/engine/build_acp.go"
      to: "ACP [Available tools] block content"
      via: "json.Marshal(req.Tools) inside fenced ```json``` block; debug-log on marshal failure"
      pattern: "\\[Available tools\\]"
---

> **Revised:** 2026-05-26 — incorporates 06-REVIEWS.md feedback (concerns: HIGH #2 two-path rule clarification, HIGH #3 Node byte-fidelity move from Wave 3 to Wave 1, LOW #6 marshal-fallback debug log). Aggregation contract for engine.Collect was rewritten: Message.ToolCalls is now populated EXCLUSIVELY by engine.CoerceToolCall. kiro-native tool_call chunks pass through Collect unchanged and become per-surface thought-text [tool: <name>]\n narration in Wave 2 emitters (Anthropic excepted — see Slice 4 for the explicit Anthropic D-07 native tool_use exception). Slice 5's Node byte-fidelity checkpoint moves here as the final blocking task.
>
> **Revised (iteration 3):** 2026-05-26 — fixes iteration-2 regression on non-streaming kiro-native rendering (06-REVIEWS.md iteration 2 HIGH #1/#2/#3). Iteration 2 made `engine.Collect` drop `ChunkKindToolCall` entirely, but that broke non-streaming Ollama/OpenAI which receive only `ChatResponse` from `Collect`. Iteration 3 has `engine.Collect` AGGREGATE `ChunkKindToolCall` into the response's assistant text as `[tool: <name>]\n` narration — Message.ToolCalls remains untouched by Collect for all surfaces. The "sole producer of Message.ToolCalls" invariant is reworded as a per-surface contract: Ollama/OpenAI populate via `CoerceToolCall`; Anthropic (D-07 exception) populates via its adapter-local Collect (06-04).

<objective>
Foundation slice for Phase 6. Activates the dormant canonical tool-call seams across canonical → acp → engine so subsequent Wave 2 adapter slices have a working canonical pivot to render from. Closes the Phase 1.1 `tool_call` placeholder in `internal/acp/translate.go:235-242`, adds `ID string` to `canonical.ToolCallChunk` (per D-08), implements `engine.CoerceToolCall` per the locked D-09 9-step algorithm (per D-01), and emits the JSON tool catalog inside `[Available tools]` (per D-16).

Per the iteration-3-reworded contract, `engine.Collect`:
- Aggregates `ChunkKindToolCall` into the assistant text content as `[tool: <name>]\n` narration. This is what non-streaming Ollama/OpenAI render — they receive only `ChatResponse` from `Collect` and need the kiro-native tool-call information to survive into the final body.
- Does NOT populate `Message.ToolCalls` from `ChunkKindToolCall` chunks (or from any other chunk source). `Message.ToolCalls` population is a per-surface concern:
  - **Ollama and OpenAI** populate `Message.ToolCalls` ONLY via `engine.CoerceToolCall` (the coerce-from-text path).
  - **Anthropic** populates `Message.ToolCalls` via its adapter-local `collect.go` (06-04 Option A1) from kiro-native `ChunkKindToolCall` chunks. This is the D-07 exception.

In streaming paths, adapters consume the chunk stream directly and render `ChunkKindToolCall` themselves (per 06-02/03/04). The `Collect` aggregation is for the non-streaming branch only.

Purpose: Without this foundation, Wave 2 adapter slices have nothing to render — the canonical pivot is empty (kiro chunks collapse to thought text; coerce doesn't exist; tool catalog reaches kiro empty). Wave 2 plans for Ollama / OpenAI / Anthropic can run in parallel only because this plan provides the single canonical contract they all consume. The Node byte-fidelity checkpoint (final task) gates Wave 2 — if the algorithm diverges from Node, Wave 2 work is built on the wrong contract.

Output: Four files modified (canonical/chunk.go, acp/translate.go, engine/collect.go, engine/build_acp.go) plus two new files (engine/coerce.go, engine/coerce_test.go) plus three test files updated/extended (acp/translate_test.go, engine/collect_test.go, engine/build_acp_test.go). One blocking human-verify checkpoint at the end of the slice. Plan is `autonomous: false` due to the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-REVIEWS.md

# Existing source files this plan modifies — read before editing.
@internal/canonical/chunk.go
@internal/canonical/chat.go
@internal/acp/translate.go
@internal/acp/translate_test.go
@internal/engine/collect.go
@internal/engine/collect_test.go
@internal/engine/build_acp.go
@internal/engine/build_acp_test.go
@internal/engine/pickcwd.go
@internal/engine/pickcwd_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ToolCallChunk.ID field + ACP translate.go promotion to real ChunkKindToolCall + Available-tools JSON catalog with debug-log fallback</name>
  <files>internal/canonical/chunk.go, internal/acp/translate.go, internal/acp/translate_test.go, internal/engine/build_acp.go, internal/engine/build_acp_test.go</files>
  <read_first>
    - internal/canonical/chunk.go (current ToolCallChunk shape lines 47-53; ChunkKindToolCall enum line 13-14; Chunk.ToolCall pointer field line 29-30 — confirm no existing ID field, confirm no JSON tags per Phase 2 D-11 invariant)
    - internal/acp/translate.go (lines 50-72 sessionUpdateBody — confirm ToolCallID/Title/Args already decoded; lines 235-242 tool_call/tool_call_chunk placeholder branch — the one being replaced; lines 250-254 plan branch as the real-ChunkKind* return pattern to mirror)
    - internal/acp/translate_test.go (full file — find TestTranslateUpdate_VarianceMatrix Phase 1.1 D-22; existing tool_call expectation that today asserts ChunkKindThought with `[tool: ...]` text; this MUST be updated to assert ChunkKindToolCall with populated ToolCallChunk fields)
    - internal/engine/build_acp.go (lines 64-69 placeholder header — the one D-16 replaces; lines 52-63 [Output format] + System branches for fmt.Fprintf + bracketed-section pattern; lines 116-122 malformed-image continue discipline for defensive marshal-error handling)
    - internal/engine/build_acp_test.go (full file — find existing TestBuildBlocks tests to extend with TestBuildBlocks_AvailableTools_JSONCatalog)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-08, D-03, D-16)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md (sections "MOD internal/canonical/chunk.go", "MOD internal/acp/translate.go", "MOD internal/engine/build_acp.go")
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (LOW concern #6 — debug log on marshal-fallback)
  </read_first>
  <behavior>
    - **canonical.ToolCallChunk.ID:** new exported `ID string` field. Doc comment notes "populated from `toolCallId` on the ACP wire OR synthesized by `coerceToolCall` when the source is text-not-kiro." No JSON tag (canonical invariant per Phase 2 D-11). Existing `canonical.ToolCallChunk{Name: ..., Args: ...}` named-field struct literals MUST still compile.
    - **internal/acp/translate.go tool_call/tool_call_chunk branch (D-03 part 1 — canonical extraction only):** the existing `case "tool_call", "tool_call_chunk":` discriminator stays. The body of the branch is replaced: instead of returning `canonical.Chunk{Kind: ChunkKindThought, Thought: &ThoughtChunk{Content: "[tool: <title>]\n"}}`, return `canonical.Chunk{Kind: canonical.ChunkKindToolCall, ToolCall: &canonical.ToolCallChunk{ID: body.ToolCallID, Name: firstNonEmpty(body.Title, "unknown"), Args: body.Args}}, true`. The `firstNonEmpty` fallback for `Title` stays. The `[tool: <name>]\n` narration text is now produced by `engine.Collect` (for non-streaming) and by per-surface emitters (for streaming) — NOT by the translator.
    - **translate_test.go updates:** the existing test (likely `TestTranslateUpdate_VarianceMatrix` Phase 1.1 D-22) asserts the OLD `ChunkKindThought + [tool: …]` outcome for tool_call notifications — UPDATE the expectation to `ChunkKindToolCall` with populated `ToolCall.ID`, `ToolCall.Name`, `ToolCall.Args`. Add a fresh subtest covering `tool_call_chunk` notification variant (same expected output — both routed identically per Pitfall 8 / kiro emits atomically).
    - **engine/build_acp.go [Available tools] catalog (D-16) — with debug-log fallback per REVIEW LOW #6:** replace the placeholder `b.WriteString("[Available tools]\nEmit a tool_call ACP notification…\n\n")` (lines 64-69) with: header line + blank line + fenced ```json block containing `json.Marshal(req.Tools)` output + closing fence + trailing `\n\n`. Format must match the Node reference (acp_server_node_reference.md §"Bracketed sections" lines 155-159).
      - **Defensive marshal-error path (REVIEW LOW #6):** if `json.Marshal(req.Tools)` returns an error (unexpected for valid `[]canonical.ToolSpec` map-and-string-only data, but possible with pathological `Parameters` maps containing non-serializable types), DO log a debug line: `slog.Default().Debug("engine/build_acp: tools marshal failed; emitting header-only [Available tools]", "err", err.Error(), "tools_count", len(req.Tools))`. Then emit the header-only placeholder so kiro at least sees the section. Test must cover this path.
    - **build_acp_test.go extension:** add `TestBuildBlocks_AvailableTools_JSONCatalog` covering: (a) `req.Tools == nil` → no `[Available tools]` section at all (existing guard); (b) `req.Tools = []canonical.ToolSpec{{Name:"get_weather", Description:"Get current weather", Parameters: map[string]any{"type":"object", "properties": map[string]any{"location": map[string]any{"type":"string"}}, "required":[]any{"location"}}}}` → section present, contains `"name":"get_weather"`, contains `"description":"Get current weather"`, contains `"properties"`, contains `"location"`. Assert via `strings.Contains` on the joined block text. (c) multi-tool case (two tools) — assert order preserved (json.Marshal of a slice is deterministic). (d) marshal-failure case — inject a tool with `Parameters: map[string]any{"bad": make(chan int)}` (channels are not JSON-serializable); assert that (i) the section header is still present, (ii) no panic, (iii) `build_acp` returns successfully (does not propagate the marshal error).
  </behavior>
  <action>
    Modify `internal/canonical/chunk.go` to add a new exported `ID string` field to `ToolCallChunk` after `Args` (or before `Name` — pick most natural order). Add a doc comment per the behavior block above. No JSON tag.

    Modify `internal/acp/translate.go` `case "tool_call", "tool_call_chunk":` branch (currently lines 235-242) to return a real `canonical.Chunk{Kind: canonical.ChunkKindToolCall, ToolCall: &canonical.ToolCallChunk{ID: body.ToolCallID, Name: firstNonEmpty(body.Title, "unknown"), Args: body.Args}}, true`. Remove the `[tool: %s]\n` thought-text formatting from this branch (the narration is now produced by engine.Collect for non-streaming and by per-surface emitters for streaming).

    Update `internal/acp/translate_test.go`: locate the existing assertion (TestTranslateUpdate_VarianceMatrix and any sibling tool_call cases) that today expects `ChunkKindThought` with `[tool: …]` text — change to expect `ChunkKindToolCall` with non-nil `ToolCall` whose `ID`, `Name`, `Args` match the input notification body. Add a new subtest covering `tool_call_chunk` discriminator (same expected output). Both tests must include cases where `Title` is empty (falls back to `"unknown"`) and where `Args` is nil (passes through as nil map).

    Modify `internal/engine/build_acp.go` `[Available tools]` branch (currently lines 64-69) per D-16. Add `encoding/json` and `log/slog` to the import block (line 12-18) if not already present. Build the fenced JSON catalog inside the bracketed section. Use `fmt.Fprintf(&b, ...)` matching the existing System / Output-format pattern. On marshal error, emit the debug log line per REVIEW LOW #6, then fall back to the existing placeholder body (header only) so kiro behavior degrades gracefully. Use `slog.Default()` so callers can inject their own logger via the `slog` global; do NOT plumb a logger through `buildBlocks` (would churn the signature for an edge case).

    Extend `internal/engine/build_acp_test.go` with `TestBuildBlocks_AvailableTools_JSONCatalog` covering the FOUR subcases in the behavior block (nil, single, multi, marshal-failure). Use `strings.Contains` for substring assertions; do NOT use byte-equality (json.Marshal output of `map[string]any` is unstable across Go versions due to map key sort order — but for `[]canonical.ToolSpec` the outer order IS stable, and the `Parameters map[string]any` interior keys go through `json.Marshal` which sorts map keys alphabetically per `encoding/json` spec, so substring checks remain stable).

    Per D-08 doc-comment requirement on the canonical addition: the ToolCallChunk.ID field's doc must call out its two-source nature (ACP wire vs synthesized in coerce) — important for future readers per CONTEXT specifics §ToolCallChunk.ID.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/canonical/... ./internal/acp/... ./internal/engine/... -run "TestTranslateUpdate|TestBuildBlocks_AvailableTools|TestTypes" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ID string" internal/canonical/chunk.go` returns the new field inside the `ToolCallChunk` struct definition.
    - `grep -cv '^[[:space:]]*//' internal/canonical/chunk.go | grep -c "json:" || true` — canonical types remain JSON-tag-free per Phase 2 D-11 (filter out commented-out JSON tags before counting).
    - `grep -n "ChunkKindToolCall" internal/acp/translate.go` shows the kind is returned from `case "tool_call", "tool_call_chunk":` branch.
    - `grep -v '^[[:space:]]*//' internal/acp/translate.go | grep -c '\[tool:'` returns 0 (the formatting moved to engine.Collect for non-streaming and per-surface emitters for streaming; this plan removes it from the active translator code — filter out comments).
    - `grep -n "tools marshal failed" internal/engine/build_acp.go` shows the REVIEW LOW #6 debug log on the marshal-fallback path.
    - `go test ./internal/acp/... -run TestTranslateUpdate -count=1` passes; the updated test asserts ChunkKindToolCall with populated ToolCall.ID/Name/Args.
    - `go test ./internal/engine/... -run TestBuildBlocks_AvailableTools_JSONCatalog -count=1` passes; the test exercises empty-tools, single-tool, multi-tool, AND marshal-failure cases.
    - `go build ./...` succeeds with no compile errors.
    - `make lint` returns clean for the modified files (no new lint findings).
  </acceptance_criteria>
  <done>
    canonical.ToolCallChunk has an exported ID field; ACP translate.go promotes tool_call / tool_call_chunk notifications to ChunkKindToolCall with full canonical extraction; engine.buildBlocks emits the JSON tool catalog inside [Available tools] when req.Tools is non-empty WITH a debug log on the marshal-failure fallback path per REVIEW LOW #6; corresponding tests updated/added and passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engine.Collect aggregates kiro-native ChunkKindToolCall into assistant text as `[tool: &lt;name&gt;]\n` narration; does NOT populate Message.ToolCalls from any chunk source (iteration-3 fix to HIGH #1)</name>
  <files>internal/engine/collect.go, internal/engine/collect_test.go</files>
  <read_first>
    - internal/engine/collect.go (full file — focus on lines 56-83 stream switch; lines 118-146 assembleChatResponse signature; the existing text + thought aggregator pattern; the intentionally-dropped ChunkKindToolCall comment at lines 67-68)
    - internal/engine/collect_test.go (full file — find existing TestCollect_* tests; understand the fake-stream / fake-chunk helper pattern to add new aggregation tests)
    - internal/canonical/chat.go (lines 73-76 Tools/ToolChoice; lines 140-148 ToolCalls/ToolCallID on Message; lines 208-241 ToolCall + ToolSpec + ToolChoice + ToolUsePart types; line 38-46 ContentKindText)
    - internal/canonical/chunk.go (updated by Task 1 — ToolCallChunk.ID, .Name, .Args)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-03 wire rendering: `[tool: <name>]\n` narration for kiro-native; D-05 coerce-synthesized is the OTHER path; D-07 Anthropic exception)
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (HIGH #1 — engine.Collect must aggregate ChunkKindToolCall into text narration so non-streaming Ollama/OpenAI render correctly; HIGH #3 — Message.ToolCalls population contract is per-surface, not "sole producer")
  </read_first>
  <behavior>
    - **Message.ToolCalls population contract (iteration-3 reworded — HIGH #3):**
      - Generic `engine.Collect` does NOT populate `Message.ToolCalls` from any chunk source.
      - Ollama and OpenAI surfaces populate `Message.ToolCalls` ONLY via `engine.CoerceToolCall` (the coerce-from-text path — D-05).
      - Anthropic surface (D-07 exception) populates `Message.ToolCalls` via its adapter-local Collect (`internal/adapter/anthropic/collect.go`, Option A1 from 06-04) from kiro-native `ChunkKindToolCall` chunks. This is the documented exception that mirrors Anthropic's wire protocol where `tool_use` blocks are native.
    - **collect.go ChunkKindToolCall handling (iteration-3 fix — HIGH #1):** `engine.Collect` MUST aggregate `ChunkKindToolCall` into the response's assistant content as `[tool: <name>]\n` narration text. This is what non-streaming Ollama/OpenAI render — they receive only `ChatResponse` from `Collect`, never the raw chunk stream. Without this aggregation, kiro-native tool calls disappear entirely on non-streaming paths.
      - On `ChunkKindToolCall`: append `"[tool: " + chunk.ToolCall.Name + "]\n"` to the text accumulator that feeds `Message.Content`'s `ContentKindText` part. If `chunk.ToolCall` is nil or `chunk.ToolCall.Name == ""`, use the literal `"[tool: unknown]\n"` as a defensive fallback (matches the `firstNonEmpty(body.Title, "unknown")` discipline in translate.go).
      - DO NOT populate `Message.ToolCalls`. DO NOT append a `ContentKindToolUse` part. Both of those are downstream responsibilities (CoerceToolCall for Ollama/OpenAI; the adapter-local Collect for Anthropic).
      - `ChunkKindPlan` continues to drop (no Phase 6 work).
    - **Comment block update:** replace the existing "intentionally dropped" comment at lines 67-68 with:
      ```go
      // ChunkKindToolCall is aggregated into Message.Content as `[tool: <name>]\n`
      // narration text per Phase 6 D-03. This is what non-streaming Ollama/OpenAI
      // render — they receive only *canonical.ChatResponse from this function and
      // need the kiro-native tool-call information to survive into the final body.
      //
      // Message.ToolCalls population contract (per Phase 6 D-03/D-05/D-07):
      //   - Generic engine.Collect (this function) does NOT populate Message.ToolCalls
      //     from any chunk source.
      //   - Ollama and OpenAI surfaces populate Message.ToolCalls ONLY via
      //     engine.CoerceToolCall (the coerce-from-text path — D-05), invoked by
      //     the adapter handlers AFTER this function returns.
      //   - Anthropic surface (D-07 exception) populates Message.ToolCalls via its
      //     adapter-local Collect (internal/adapter/anthropic/collect.go, 06-04
      //     Option A1) from kiro-native ChunkKindToolCall chunks. That adapter
      //     uses engine.Run + its own aggregator and bypasses this function.
      //
      // ChunkKindPlan still drops (no Phase 6 work).
      ```
    - **No assembleChatResponse signature change.** `Message.ToolCalls` stays empty after Collect returns. The text accumulator just gets a new contributor source (ChunkKindToolCall).
    - **collect_test.go updates — three new tests:**
      1. `TestCollect_AggregatesKiroNativeToolCallAsNarration` (REVIEW iteration-2 HIGH #1 fix): inject a stream of `[text-chunk("checking weather "), tool_call-chunk(ID=tc_1, Name=get_weather, Args={location:"NYC"}), text-chunk(" done")]` and assert the resulting `*canonical.ChatResponse`:
         1. `resp.Message.ToolCalls` is empty/nil (Collect does NOT populate it).
         2. `resp.Message.Content` has exactly one `ContentKindText` part whose `Text` contains `"checking weather "`, then `"[tool: get_weather]\n"`, then `" done"` in that order. Use `strings.Contains` with the three substrings AND `strings.Index(...A) < strings.Index(...B)` ordering assertions.
         3. No `ContentKindToolUse` parts appended.
      2. `TestCollect_KiroNativeToolCall_OnlyChunk`: inject just `[tool_call-chunk(Name=search_web, Args={q:"go"})]` (no surrounding text). Assert `Content[0].Text == "[tool: search_web]\n"` (exact match) and `Message.ToolCalls` is empty.
      3. `TestCollect_KiroNativeToolCall_NilName_Fallback`: inject `[tool_call-chunk(ID="tc_x", Name="", Args=nil)]` (defensive — should not happen given translate.go's firstNonEmpty fallback, but lock it). Assert `Content[0].Text == "[tool: unknown]\n"`.
    - **Backward compat:** existing tests (text-only, thought-only, mixed text+thought) must continue to pass without modification.
  </behavior>
  <action>
    Update `internal/engine/collect.go`:
      1. In the chunk-stream switch (currently around lines 56-83), change the `ChunkKindToolCall` case from a drop to a text-append: on `ChunkKindToolCall`, derive `name := "unknown"` if `c.ToolCall == nil || c.ToolCall.Name == ""` else `c.ToolCall.Name`, then append `"[tool: " + name + "]\n"` to whatever text accumulator feeds `Message.Content`'s `ContentKindText` part. Use the existing text aggregator pattern (the same builder/slice that `ChunkKindText` writes to).
      2. Do NOT modify `assembleChatResponse`'s signature. Do NOT add a `toolCalls` parameter. Do NOT add any `Message.ToolCalls = ...` anywhere in collect.go. Do NOT append `ContentKindToolUse` parts.
      3. Replace the comment block at lines 67-68 (or wherever the `ChunkKindToolCall` case sits) with the per-surface population contract comment per the behavior block. Cite D-03/D-05/D-07.

    Extend `internal/engine/collect_test.go` with the THREE new tests per the behavior block (`TestCollect_AggregatesKiroNativeToolCallAsNarration`, `TestCollect_KiroNativeToolCall_OnlyChunk`, `TestCollect_KiroNativeToolCall_NilName_Fallback`). Reuse the existing fake-stream helper pattern.

    NOTE — Anthropic's path bypasses this function entirely. Per 06-04 (Option A1), the Anthropic handler calls `CollectAnthropicChat` (which wraps `engine.Run`) instead of `engine.Collect`. Anthropic-native tool_use rendering does NOT go through this function. The `[tool: <name>]\n` narration aggregated here is consumed only by Ollama and OpenAI non-streaming render paths.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/engine/... -run "TestCollect" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Message.ToolCalls population contract" internal/engine/collect.go` shows the updated comment block citing the per-surface contract.
    - `grep -v '^[[:space:]]*//' internal/engine/collect.go | grep -c "Message.ToolCalls"` returns 0 (no assignment to Message.ToolCalls in collect.go's active code — filter out comments).
    - `grep -v '^[[:space:]]*//' internal/engine/collect.go | grep -c "ContentKindToolUse"` returns 0 (no append of ContentKindToolUse parts — filter out comments).
    - `grep -n "\\[tool: " internal/engine/collect.go` returns ≥1 (the new narration aggregator appends the `[tool: ` prefix).
    - `go test ./internal/engine/... -run TestCollect -race -count=1` passes including the THREE new tests (AggregatesKiroNativeToolCallAsNarration, KiroNativeToolCall_OnlyChunk, KiroNativeToolCall_NilName_Fallback).
    - Existing tests (text-only, thought-only) still pass — Message.ToolCalls stays nil and no ContentKindToolUse parts appear.
    - `go vet ./internal/engine/...` clean.
  </acceptance_criteria>
  <done>
    engine.Collect aggregates kiro-native ChunkKindToolCall chunks into the assistant text content as `[tool: <name>]\n` narration (fix to iteration-2 HIGH #1 regression — non-streaming Ollama/OpenAI rendering path is restored); Message.ToolCalls remains untouched by Collect for all surfaces; comment block cites the per-surface population contract (D-03/D-05/D-07); three new tests lock the narration behavior; no race-detector findings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: engine.CoerceToolCall implementation + property tests + Example function (Ollama/OpenAI sole producer of Message.ToolCalls per the per-surface contract)</name>
  <files>internal/engine/coerce.go, internal/engine/coerce_test.go</files>
  <read_first>
    - internal/engine/pickcwd.go (full file — pure-helper signature pattern + nil guards)
    - internal/engine/pickcwd_test.go (full file — testing/quick.Config{MaxCount: 1000} pattern lines 204-260; Example_pickCwd at lines 246-260; table-driven case pattern at lines 19-84 — this is the structural model for coerce_test.go)
    - internal/engine/build_acp.go (package + import pattern, lines 10-18)
    - internal/canonical/chat.go (ToolCall/ToolSpec/ContentPart shape lines 60-241 — field names; ContentKindText/Thinking/ToolUse constants)
    - internal/canonical/chunk.go (updated by Task 1 — ToolCallChunk.ID extension)
    - docs/reference/acp_server_node_reference.md §"Load-bearing weirdness: `coerceToolCall`" (lines 166-195 — algorithm narrative; this is the source of truth for D-09 / D-10 per CONTEXT line 471 — Node code wins on disagreement)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-01, D-02, D-09, D-10, D-11, D-12)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md ("Pattern 1: `coerceToolCall`" + "Code Examples §`coerceToolCall` algorithm" + Pitfalls 3, 4, 6)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW internal/engine/coerce.go" + "NEW internal/engine/coerce_test.go" sections)
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (LOW concern — add focused test that inline fenced JSON inside prose is NOT coerced; HIGH #3 — Message.ToolCalls population contract per-surface)
  </read_first>
  <behavior>
    - **Exported signature (D-01):** `func CoerceToolCall(req *canonical.ChatRequest, resp *canonical.ChatResponse) bool` in package engine. Returns `true` if it rewrote `resp`, `false` otherwise. No error return (parse failures are non-fatal per D-09 Step 5).
    - **Per-surface contract reinforcement (REVIEW HIGH #3 iteration-3 wording):** `CoerceToolCall` is the producer of `Message.ToolCalls` FOR the Ollama and OpenAI surfaces. It mutates `resp.Message.ToolCalls` (append) and `resp.Message.Content[textIdx].Text` (empty out). It does NOT touch `ContentKindToolUse` parts. Anthropic does NOT call `CoerceToolCall`; Anthropic's `Message.ToolCalls` population is handled by the adapter-local `CollectAnthropicChat` (06-04, D-07 exception).
    - **Important interaction with engine.Collect (iteration-3 narration):** because Task 2 makes `engine.Collect` aggregate kiro-native `ChunkKindToolCall` into the assistant text as `"[tool: <name>]\n"` narration, the assistant text seen by `CoerceToolCall` in a kiro-native scenario will START with that narration text — which does NOT begin with `{` or `` ``` ``. Therefore `CoerceToolCall`'s Step 3 (raw JSON parse) and Step 4 (fence-strip retry) both fail, Step 5 returns false, and the response is preserved verbatim (narration text intact, no Message.ToolCalls appended). This is the intended interaction: kiro-native + Ollama/OpenAI non-streaming → narration text reaches the wire; coerce does not fire. No special-case logic in `CoerceToolCall` is needed.
    - **D-09 9-step algorithm — IMMUTABLE:**
      1. Defensive nil guards + skip: if `req == nil || resp == nil || len(req.Tools) == 0 || len(resp.Message.ToolCalls) > 0` → return false. (Idempotency invariant D-02 — running twice no-ops on the second call because Message.ToolCalls is now non-empty.)
      2. Extract assistant text: locate the single `ContentKindText` part in `resp.Message.Content`. If absent or empty (after `strings.TrimSpace`), return false.
      3. Try `json.Unmarshal` on the raw text.
      4. If fail, run `stripFences(text)` (private helper) to strip ```json or bare ``` fences. If fence-strip returned `(text, false)` (no fence detected at start AND end of trimmed text), return false. If fence-strip succeeded, retry `json.Unmarshal` on the stripped text.
      5. If still fail after fence-strip → return false. (Text preserved — never mutate response on parse failure.)
      6. `pickBestTool`: for each `ToolSpec`, count how many keys in `parsed.(map[string]any)` appear as **top-level** keys of `spec.Parameters["properties"]`. Pick the highest scorer; ties broken by **first-declared in `req.Tools` order**.
      7. If the top score is zero → return false. Text preserved. (Node behavior — better than firing the wrong tool.)
      8. Replace `Content[textIdx].Text` with `""`; append synthetic `ToolCall{ID: fmt.Sprintf("call_%d", time.Now().UnixNano()), Name: bestSpec.Name, Arguments: parsed}` to `resp.Message.ToolCalls`.
      9. Return true.
    - **`pickBestTool(parsed map[string]any, tools []canonical.ToolSpec) (*canonical.ToolSpec, int)` — locked rules (D-10, Pitfall 4):**
      - Iterate `tools` in declaration order (slice index, NOT map iteration — Pitfall 4).
      - For each spec: extract `spec.Parameters["properties"]` as `map[string]any`. If absent / not a map / empty, skip this spec (zero score, can't compare).
      - Score = count of keys in `parsed` that appear as top-level keys in `properties`. Top-level only — no recursion (D-10).
      - Track `bestIdx int` and `bestScore int`. On strict `>` update both. On equality (`score == bestScore`), DO NOT update — first-declared wins (deterministic tie-break per D-10).
      - Return `(&tools[bestIdx], bestScore)`. If all specs had zero score, `bestScore == 0`.
    - **`stripFences(text string) (string, bool)` — locked rules (D-10):**
      - After `strings.TrimSpace`, check for `strings.HasPrefix(t, "```json\n")` then `strings.HasSuffix(t, "\n```")`; if both true, return `(trimmed-inner, true)`.
      - Else check `strings.HasPrefix(t, "```\n")` then `strings.HasSuffix(t, "\n```")`; if both true, return `(trimmed-inner, true)`.
      - Tolerate CRLF: also try `\r\n` variants OR run the result through `strings.ReplaceAll(_, "\r\n", "\n")` BEFORE the prefix/suffix check.
      - Returns `(text, false)` if no fence detected — `text` is the trim-spaced original (caller should NOT use the fenced text on a no-fence return; the `false` signal means "give up").
      - Per Pitfall 3: the fence MUST wrap the ENTIRE text (no prose before/after the fence). Two `HasPrefix`/`HasSuffix` chains with no inner search ensures this.
      - **No regex** (Pitfall 3 catastrophic backtracking + Don't-Hand-Roll guidance).
    - **D-12 property tests (testing/quick.Config{MaxCount: 1000}):**
      1. `TestCoerceToolCall_NeverPanics` — random `(text string, toolNames []string)` inputs through CoerceToolCall; defer-recover guard for nil-input panic. Per RESEARCH lines 435-453.
      2. `TestCoerceToolCall_Idempotent` — for arbitrary `(req, resp)`, `Coerce(Coerce(x))` mutation == `Coerce(x)` mutation (re-invocation no-ops because Message.ToolCalls is now non-empty).
      3. `TestCoerceToolCall_NoMatchNoMutation` — when `pickBestTool` scores zero (e.g., parsed JSON has no overlap with any tool's properties), the response is bit-identical to input. Compare `resp.Message.Content` and `resp.Message.ToolCalls` before/after.
      4. `TestCoerceToolCall_TieBreaker` — table-driven: two tools with identical property keys, parsed JSON matches both equally — first-declared wins. Deterministic across 1000 iterations.
      5. `TestCoerceToolCall_AlwaysTerminates` — implicit via testing/quick MaxCount=1000 — no infinite loop; combined with `NeverPanics` covers Pitfall 3 catastrophic-backtracking safety.
    - **Table-driven cases (D-09 + D-10 explicit edge cases — UPDATED per REVIEW LOW suggestion):** `TestCoerceToolCall_AlgorithmCases` covers the 9-step matrix:
      - raw JSON parse success (Step 3 → 6)
      - fenced JSON ```json … ``` parse success (Step 4 → 6)
      - bare fence ``` … ``` parse success (Step 4 → 6)
      - **NEW per REVIEW: inline fenced JSON inside prose** — text like `"Here's the result: \`\`\`json\n{...}\n\`\`\`\nDone."` → MUST NOT coerce (fence is not at start AND end of trimmed text). Lock the Pitfall 3 "entire text" requirement. Asserts no mutation.
      - **NEW iteration-3: kiro-native narration text** — text starts with `"[tool: get_weather]\n"` (the output of Task 2's narration aggregator) → MUST NOT coerce (not JSON-shaped, no fence). Locks the kiro-native + Ollama/OpenAI non-streaming interaction described in the behavior block above.
      - parse failure (no JSON, no fence) → no-coerce (Step 5)
      - parse failure (truncated/invalid JSON) → no-coerce (Step 5)
      - parsed value is array → no-coerce (D-10 — not a map[string]any)
      - parsed value is scalar/null → no-coerce (D-10)
      - parsed empty `{}` → score 0 → no-coerce (D-10)
      - tool with empty/missing parameters.properties → skip in scoring (D-10)
      - tools[] empty → no-coerce (Step 1)
      - resp.Message.ToolCalls already populated → no-coerce (Step 1 — idempotency D-02)
    - **Example_CoerceToolCall (TRST-07):** runnable godoc example per pickcwd_test.go:246-260 pattern. Demonstrates the bare-JSON-parse → synthetic tool_call happy path with `// Output:` comment for `go test -run Example` verification.
    - **Logger / debug surface:** none. `CoerceToolCall` is silent — the bool return lets adapters decide to log a `coerce=true` tag. Per CONTEXT D-01 ("Returns bool so adapters can debug-log when a coerce fired").
  </behavior>
  <action>
    Create new file `internal/engine/coerce.go` with package `engine` and imports for `encoding/json`, `fmt`, `strings`, `time`, and `otto-gateway/internal/canonical`. Implement `CoerceToolCall` per D-09 steps 1-9. Implement private `pickBestTool` and `stripFences` helpers per the locked rules above. ID synthesis uses `fmt.Sprintf("call_%d", time.Now().UnixNano())` per D-11. Add a package-level doc comment block on `CoerceToolCall` citing D-01, D-02, D-09 from CONTEXT, AND the per-surface population contract (Ollama/OpenAI use this function; Anthropic uses adapter-local Collect per D-07), and the load-bearing LangChain-compat narrative from go_port_brief.md §1.4.

    Create new file `internal/engine/coerce_test.go` per the testing/quick precedent at `internal/engine/pickcwd_test.go`. Implement the five property tests + table-driven cases (INCLUDING the NEW "inline fenced JSON inside prose" case per REVIEW LOW AND the NEW "kiro-native narration text" no-coerce case per iteration-3) + Example function per the behavior block. Use `testing/quick.Config{MaxCount: 1000}`. Defer-recover the nil-input panic guard exactly per pickcwd_test.go:240-260.

    Per RESEARCH Pitfall 6: do NOT pre-copy `resp` before mutation. The function mutates in-place — caller must use the same pointer after the call.

    Per RESEARCH §"`coerceToolCall` algorithm" caveat — Node source byte-level fidelity is Assumption A1, validated by Task 4 (the NEW blocking checkpoint in THIS plan; moved from Slice 5 per REVIEW HIGH #3). This task implements the algorithm per the narrative reference; Task 4 reconciles against the Node source bytes BEFORE Wave 2 fans out.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/engine/... -run "TestCoerceToolCall|Example_CoerceToolCall" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `internal/engine/coerce.go` exists and exports `CoerceToolCall(req *canonical.ChatRequest, resp *canonical.ChatResponse) bool`.
    - `grep -c "^func CoerceToolCall" internal/engine/coerce.go` returns 1; `grep -c "^func pickBestTool" internal/engine/coerce.go` returns 1; `grep -c "^func stripFences" internal/engine/coerce.go` returns 1.
    - `grep -c "regexp" internal/engine/coerce.go` returns 0 (no regex per Pitfall 3 + Don't-Hand-Roll guidance).
    - `internal/engine/coerce_test.go` exists with all five property tests AND Example_CoerceToolCall AND the algorithm cases table AND the new InlineFencedJSON-in-prose no-coerce case AND the new kiro-native narration no-coerce case.
    - `go test ./internal/engine -run "TestCoerceToolCall_NeverPanics" -count=1` passes (1000 random inputs).
    - `go test ./internal/engine -run "TestCoerceToolCall_Idempotent" -count=1` passes.
    - `go test ./internal/engine -run "TestCoerceToolCall_NoMatchNoMutation" -count=1` passes.
    - `go test ./internal/engine -run "TestCoerceToolCall_TieBreaker" -count=1` passes deterministically across 1000 runs.
    - `go test ./internal/engine -run "TestCoerceToolCall_AlgorithmCases" -count=1` passes (all D-09/D-10 cases INCLUDING the inline-fenced-prose and kiro-native-narration no-coerce cases).
    - `go test ./internal/engine -run "Example_CoerceToolCall" -count=1` passes (TRST-07 godoc validated).
    - `go test ./internal/engine/... -race -count=1` clean.
    - `go vet ./internal/engine/...` and lint clean on the new files.
  </acceptance_criteria>
  <done>
    engine.CoerceToolCall implements the locked D-09 9-step algorithm with first-declared tie-break (D-10) and unix-nano ID synthesis (D-11); documented as the producer of Message.ToolCalls for Ollama/OpenAI (per per-surface contract — Anthropic excepted); property tests prove never-panic + idempotent + no-match-no-mutation + deterministic-tie-break invariants per D-12; table-driven cases include the inline-fenced-in-prose no-coerce assertion AND the kiro-native narration no-coerce assertion (proves the engine.Collect aggregation from Task 2 interacts correctly with this function); Example function validates the godoc form; no race-detector findings.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT — MOVED FROM SLICE 5 PER REVIEW HIGH #3): Node source byte-level fidelity check on coerceToolCall + pickBestTool (RESEARCH Assumption A1)</name>
  <what-built>
    Task 3 implemented `internal/engine/coerce.go` per the D-09 9-step algorithm derived from `docs/reference/acp_server_node_reference.md` §"Load-bearing weirdness: coerceToolCall" (lines 166-195). The Node source itself (`acp-ollama-server.js`) is NOT in this checkout — the parent directory `gitlab.rosetta.ericssondevops.com/loop_24/acp_server` does not exist on this machine. RESEARCH §Assumption A1 flagged this as the ONE non-verified assumption: the algorithm is verified against the narrative reference but NOT against the Node source bytes. Per CONTEXT line 471: "Where Node's code and the narrative doc disagree, the Node code wins."

    **Per REVIEW HIGH #3, this checkpoint MOVED FROM Slice 06-05 (Wave 3) to here (end of Wave 1).** Reasoning from the reviewer: if Node behavior diverges from the narrative, Wave 2 adapters (06-02/03/04) would be built around the wrong algorithm. The checkpoint MUST resolve before Wave 2 fans out.

    This checkpoint validates that our Go implementation matches Node's behavior at the byte level. Until it resolves, Wave 2 plans (06-02, 06-03, 06-04) are BLOCKED.
  </what-built>
  <how-to-verify>
    Choose ONE of the following paths to verify byte-level fidelity:

    **Path A — clone the Node repo (preferred):**
    1. `git clone <node-repo-url> /tmp/acp_server` (the URL is in PROJECT.md / docs/briefs; ask the operator if unclear).
    2. Open `/tmp/acp_server/acp-ollama-server.js`.
    3. Locate the `coerceToolCall` function and the `pickBestTool` function.
    4. Open `internal/engine/coerce.go` in this repo.
    5. Diff the two implementations behaviorally:
       - Does the order-of-checks match D-09 steps 1-9? (Skip-condition guards; raw parse; fenced parse with ```json and bare ``` variants; pickBestTool top-level scoring; zero-score → no-op; rewrite content + append tool_call.)
       - Does `pickBestTool` use first-declared tie-break OR something different (e.g., total property count secondary)?
       - Does fence-stripping handle CRLF, leading/trailing whitespace, etc.?
       - Is the synthetic tool_call ID format different (e.g., `tool_<uuid>` vs `call_<unix-nano>`)?
       - Does either function have a behavior the other doesn't?
    6. If ALL behaviors match → respond "approved — byte fidelity confirmed (Path A)".
    7. If ANY drift is found → describe the divergence in the response (which function, which step, what Node does vs what Go does); planner will create a follow-up correction in `coerce.go` per CONTEXT line 471 "Node code wins" BEFORE Wave 2 begins.

    **Path B — paste the Node function bodies for cross-check:**
    1. Open `acp-ollama-server.js` in the Node repo (wherever it lives in your environment).
    2. Copy the bodies of `coerceToolCall` and `pickBestTool` (the full functions).
    3. Paste them into your response as code blocks.
    4. Claude (the next session resuming this checkpoint) will diff against `internal/engine/coerce.go` and flag any drift.

    **Path C — ship with ASSUMED-vs-Node-source note (LAST RESORT):**
    If the Node source is genuinely inaccessible (repo not available, permissions blocked, etc.), explicitly accept the risk:
    1. Confirm that LangChain integration smoke tests post-ship will catch any drift (if Pi-SDK / LangFlow real users hit the gateway with JSON-as-text, divergent behavior will surface as bug reports).
    2. Respond "approved — Path C accepted; post-ship LangChain integration smoke is the verification."
    3. Planner will add a follow-up `perf-baseline-vs-node.md`-style milestone-deferral todo to verify post-ship.

    **CRITICAL — gate semantics:** Per REVIEW HIGH #3, Wave 2 (Slices 06-02, 06-03, 06-04) is BLOCKED until this checkpoint resolves. Even Path C ("approved with risk accepted") unblocks Wave 2 — the gate enforces a conscious decision, not a fix-everything bar.

    Resume signal: "approved" (with which path) OR describe drift / blocker.
  </how-to-verify>
  <resume-signal>Type "approved (Path A/B/C)" or describe drift / blocker</resume-signal>
  <files>internal/engine/coerce.go (read-only diff target; no write unless drift found)</files>
  <action>
    HUMAN-VERIFY CHECKPOINT — NOT an autonomous Claude action. The operator performs the byte-fidelity diff per the how-to-verify steps and types the resume signal. Claude (the next session) reads the response: on "approved", Wave 2 unblocks; on described drift, edits `internal/engine/coerce.go` to match Node behavior before Wave 2 begins.

    Per planner doctrine: legitimacy checkpoints (Node byte-fidelity is in this category — algorithmic-fidelity gate) are NEVER auto-approvable. `workflow.auto_advance` is ignored on this task.
  </action>
  <verify>
    <human-check>Operator confirms (Path A/B/C) per how-to-verify; resume signal received. No automated command — fidelity is a human judgment call.</human-check>
  </verify>
  <done>
    Operator has reconciled `internal/engine/coerce.go` against the Node source (or accepted the post-ship verification path), and has typed an approval response that names the path taken. Wave 2 unblocks ONLY after this checkpoint resolves.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiro-cli stdout → acp.translate | Untrusted JSON-RPC notification stream from subprocess. `tool_call` body fields (`toolCallId`, `title`, `args`) flow into canonical.ToolCallChunk and onward to HTTP response bodies. kiro itself is trusted to invoke the LLM, but its output is treated as untrusted input per defensive parsing discipline established in Phase 1.1 D-22. |
| Assistant-text in resp.Message.Content → engine.CoerceToolCall | The model can emit any text including malicious JSON. `coerceToolCall` parses this untrusted content via `encoding/json.Unmarshal`. |
| `req.Tools` → engine.buildBlocks → ACP prompt | Client-supplied tool catalog flows into the ACP prompt sent to kiro. Tool descriptions are operator/client-controlled; kiro is the intended consumer. |
| ChunkKindToolCall.Name → engine.Collect narration | Untrusted tool name from kiro stdout (translate.go) flows into assistant text as `[tool: <name>]\n`. The name is bounded by ACP framer line cap; no injection vector because the text is rendered as opaque body bytes (not interpreted by client SDKs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | acp.translate.go tool_call body decode | mitigate | Use `encoding/json` with the already-defensive `sessionUpdateBody` shape (Phase 1.1 D-22 tolerant decoder). Title/Args/ToolCallID fields use omitempty + fallback (`firstNonEmpty` for title). |
| T-06-02 | DoS | acp.translate.go tool_call args | mitigate | ACP framer line cap (existing `internal/acp/framer.go`) bounds JSON-RPC notification size; Task 1 does not introduce a new size surface. Verify framer cap is ≥1MB per RESEARCH §Security Domain. |
| T-V5-01 | Tampering / Input Validation | engine/coerce.go (parses untrusted assistant text) | mitigate | `encoding/json.Unmarshal` rejects malformed JSON cleanly (Step 5 of D-09 preserves text on parse failure — no information leak). `pickBestTool` reads only top-level property names (string comparison, no eval, no injection vector per D-10). Per RESEARCH §Security Domain V5: "JSON injection via assistant-text in coerce path → result is `map[string]any` (not eval'd code); only keys are matched against tool-spec property names." |
| T-V5-02 | DoS (catastrophic regex backtracking) | engine/coerce.go::stripFences | mitigate | NO regex (per Pitfall 3 + Don't-Hand-Roll guidance). Two `strings.HasPrefix`/`HasSuffix` chains with no nested quantifiers — linear time guaranteed. |
| T-06-03 | Spoofing | engine/coerce.go synthetic ID | accept | `call_<unix-nano>` IDs are opaque per OpenAI/Anthropic specs; no cryptographic claim. Sub-microsecond collision is theoretically possible but Phase 6 emits one tool_call per request and IDs are scoped per-response — cross-response collision has no security impact. Documented as non-secret in code comment. (Defer to crypto/rand only if parallel_tool_calls is implemented — currently deferred per CONTEXT `<deferred>`.) |
| T-V8-01 | Information disclosure | engine/build_acp.go [Available tools] JSON catalog → kiro stdin | accept | Tool descriptions are client-supplied and kiro is the intended consumer. Catalog content does NOT echo to slog (verify via grep — Task 1 logs only `len(req.Tools)` and `err.Error()` on the marshal-fallback path, never the catalog payload). |
| T-06-A1 (RESEARCH Assumption A1) | Tampering (algorithm drift from Node ground truth) | engine.CoerceToolCall byte-level behavior | mitigate via human-verify checkpoint | Task 4 (the moved-from-Slice-5 blocking checkpoint per REVIEW HIGH #3) validates against Node source BEFORE Wave 2 fans out. Cannot be auto-approved. |
| T-06-18 (NEW iteration-3) | Tampering (kiro-supplied tool name → narration text injection) | engine.Collect ChunkKindToolCall narration appender | accept | Tool name from kiro stdout is appended verbatim into assistant text as `[tool: <name>]\n`. Since the surrounding text is opaque body bytes (not interpreted by SDKs as control tokens), and the ACP framer caps frame size, the only attack surface is operator/user confusion if a malicious kiro emits a deceptive tool name — equivalent to existing text-injection capabilities of any LLM. No new structural risk. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A for this plan — zero new dependencies (verified RESEARCH §Package Legitimacy Audit). `goleak` and `testing/quick` are already in go.mod. |
</threat_model>

<verification>
After all four tasks complete:
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./...` — full build clean.
2. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/canonical/... ./internal/acp/... ./internal/engine/... -race -count=1` — all unit + property tests pass under race detector.
3. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` — lint + race + govulncheck + tests all green.
4. `grep -v '^[[:space:]]*//' internal/canonical/chunk.go | grep -c 'json:'` returns 0 (canonical invariant — filter out commented-out tags).
5. `grep -n "ChunkKindToolCall" internal/acp/translate.go internal/engine/collect.go` shows: translate.go emits the kind; collect.go aggregates the kind into narration text.
6. `grep -v '^[[:space:]]*//' internal/engine/collect.go | grep -c 'Message.ToolCalls'` returns 0 (collect.go does NOT touch Message.ToolCalls per per-surface contract).
7. `grep -n "\\[tool: " internal/engine/collect.go` returns ≥1 (the iteration-3 narration aggregator).
8. `grep -n "Available tools" internal/engine/build_acp.go` shows the bracketed-section header is preserved and the JSON catalog body follows.
9. Task 4 (Node byte-fidelity checkpoint) has resolved with an operator-typed approval (Path A/B/C).
</verification>

<success_criteria>
- canonical.ToolCallChunk has `ID string` field (D-08) with the doc comment about its two-source nature; no JSON tag.
- internal/acp/translate.go tool_call / tool_call_chunk branch promotes to `ChunkKindToolCall` with populated `ToolCall.ID`, `ToolCall.Name`, `ToolCall.Args` (closes Phase 1.1 placeholder per D-03 part 1).
- engine.Collect aggregates kiro-native `ChunkKindToolCall` into the assistant text as `[tool: <name>]\n` narration (iteration-3 fix to HIGH #1 — non-streaming Ollama/OpenAI render correctly); does NOT populate Message.ToolCalls from any chunk source.
- Message.ToolCalls population contract honored per surface (HIGH #3 iteration-3 wording): generic engine does not populate; Ollama/OpenAI populate via CoerceToolCall; Anthropic populates via adapter-local Collect (D-07 exception, owned by 06-04).
- engine.CoerceToolCall(req, resp) is exported, canonical-typed, idempotent (D-02), follows the locked D-09 9-step algorithm with D-10 edge cases (INCLUDING the inline-fenced-prose AND the new kiro-native narration no-coerce cases), and uses unix-nano ID per D-11.
- engine.buildBlocks emits the full JSON tool catalog inside `[Available tools]` when `req.Tools` is non-empty (D-16) with a debug log on the marshal-fallback path (REVIEW LOW #6).
- Property tests cover NeverPanics + Idempotent + NoMatchNoMutation + TieBreaker invariants per D-12 with MaxCount=1000.
- Example_CoerceToolCall runs clean (TRST-07).
- Node byte-fidelity checkpoint resolved per REVIEW HIGH #3 (Path A/B/C approved) — Wave 2 unblocks.
- Zero new external dependencies; `go.uber.org/goleak` + `testing/quick` were already available.
- `make ci` green (lint + race + govulncheck + tests).
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-01-SUMMARY.md` when done summarizing: the canonical extension, the algorithm files added, the test files added/extended, the iteration-3 narration aggregator in collect.go (the fix to iteration-2 HIGH #1 regression), the per-surface Message.ToolCalls population contract wording (HIGH #3), the Node byte-fidelity checkpoint resolution (Path A/B/C), and the line-count delta. Confirm Wave 2 may proceed.
</output>
