---
phase: 06-tool-call-path
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/adapter/openai/wire.go
  - internal/adapter/openai/handlers.go
  - internal/adapter/openai/render.go
  - internal/adapter/openai/sse.go
  - internal/adapter/openai/wire_test.go
  - internal/adapter/openai/render_test.go
  - internal/adapter/openai/sse_golden_test.go
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, openai, sse, render, coerce, streaming-coerce]

must_haves:
  truths:
    - "OpenAI POST /v1/chat/completions with tools[] decodes into req.Tools via PER-ENTRY json.RawMessage + openAIToolSpec unmarshal (iteration-3 fix to MEDIUM #4 — type-invalid sibling entries are skipped without dropping valid siblings) per D-13"
    - "Non-streaming /v1/chat/completions with tool_calls returns choices[].message.tool_calls[].function.arguments as a JSON-encoded STRING (NOT object) per SC #1"
    - "Non-streaming /v1/chat/completions with a kiro-native tool_call renders the `[tool: <name>]\\n` narration text in `choices[].message.content` (sourced from engine.Collect's iteration-3 aggregator in 06-01) and does NOT emit native tool_calls or finish_reason:tool_calls (no coerce hit on narration text)"
    - "Non-streaming /v1/chat/completions with model-emitted JSON-as-text invokes engine.CoerceToolCall and returns a synthetic tool_calls entry"
    - "STREAMING /v1/chat/completions tracks `sawKiroNativeToolCall` across the chunk stream (iteration-3 fix to HIGH #2). At stream end: if true, coerce is SKIPPED entirely and any buffered text is flushed as plain text-delta frames; if false, the existing buffered-text + CoerceToolCall path runs."
    - "STREAMING /v1/chat/completions with model-emitted JSON-as-text (no preceding kiro-native tool_call) invokes engine.CoerceToolCall at stream end on the buffered assistant text and emits the multi-frame tool_calls SSE shape INSTEAD of the buffered text deltas (REVIEW HIGH #1 streaming-coerce gap fix)"
    - "Streaming /v1/chat/completions with kiro-native ChunkKindToolCall renders [tool: <name>]\\n text deltas (role-emitted-once, then per-chunk delta.content); NO native delta.tool_calls[] frame from kiro-native; terminal finish_reason is NOT tool_calls when only kiro-native fired (per HIGH #2 two-path rule)"
    - "tool_choice OpenAI shape ('auto' | 'required' | 'none' | {type:'function', function:{name}}) decodes into canonical.ToolChoice"
    - "When tool_call is the first chunk in the stream, the role frame is still emitted exactly once before the tool-call frames (REVIEW LOW #8 role-emit-once-with-tool-call-first test)"
  artifacts:
    - path: internal/adapter/openai/wire.go
      provides: "PER-ENTRY json.RawMessage decoding of openAIToolSpec (iteration-3 fix to MEDIUM #4); ToolChoice decode; canonical.ToolSpec assembly"
    - path: internal/adapter/openai/handlers.go
      provides: "engine.CoerceToolCall invocation after engine.Collect (non-streaming branch); streaming branch threads req to SSE emitter for end-of-stream coerce"
    - path: internal/adapter/openai/render.go
      provides: "chatResponseToCompletion populates choices[].message.tool_calls with JSON-string Arguments (coerce-synthesized only); finish_reason post-fixup"
    - path: internal/adapter/openai/sse.go
      provides: "applyChunk handles ChunkKindToolCall as text-delta narration (NO native delta.tool_calls for kiro-native per REVIEW HIGH #2); sawKiroNativeToolCall-aware end-of-stream coerce (iteration-3 fix to HIGH #2 — skip-or-coerce-or-flush)"
    - path: internal/adapter/openai/sse_golden_test.go
      provides: "TestSSE_Golden_StreamingCoerce + TestSSE_Golden_KiroNative_TextDelta + TestSSE_Golden_ToolCallFirst_RoleEmitOnce + TestStream_NativeToolCall_ThenJSONText_NoCoerce + TestStream_NativeToolCall_Only_NoCoerce multi-frame wire-shape locks"
  key_links:
    - from: "internal/adapter/openai/wire.go::wireToChatRequest"
      to: "req.Tools (canonical.ToolSpec slice)"
      via: "PER-ENTRY json.RawMessage → openAIToolSpec unmarshal loop with skip-on-failure (iteration-3 MEDIUM #4 fix)"
      pattern: "json.RawMessage"
    - from: "internal/adapter/openai/handlers.go (non-streaming)"
      to: "engine.CoerceToolCall"
      via: "direct call between Collect and render"
      pattern: "engine\\.CoerceToolCall\\(req, resp\\)"
    - from: "internal/adapter/openai/sse.go (end-of-stream)"
      to: "engine.CoerceToolCall on buffered assistant text — ONLY when sawKiroNativeToolCall == false"
      via: "stream-end aggregator builds synthetic resp with accumulated text; emits multi-frame tool_calls SSE if coerce hits, OR releases buffered text deltas if coerce misses or is skipped (iteration-3)"
      pattern: "engine\\.CoerceToolCall.*synthetic"
    - from: "internal/adapter/openai/render.go::chatResponseToCompletion"
      to: "responseMessage.ToolCalls with JSON-string arguments"
      via: "json.Marshal(tc.Arguments) → string"
      pattern: "json\\.Marshal"
---

> **Revised:** 2026-05-26 — incorporates 06-REVIEWS.md feedback (concerns: HIGH #1 streaming coerce gap, HIGH #2 two-path rule — kiro-native ChunkKindToolCall renders as text-delta narration NOT native delta.tool_calls, MEDIUM mixed valid/invalid tools test, LOW #8 role-emit-exactly-once with tool-call-first stream test). The previous plan's "applyChunk emits delta.tool_calls for kiro-native ChunkKindToolCall" path is REMOVED — that path is replaced by text-delta narration. Native delta.tool_calls frames fire ONLY from coerce-synthesized entries at end-of-stream. finish_reason:"tool_calls" override fires ONLY when coerce hits.
>
> **Revised (iteration 3):** 2026-05-26 — fixes iteration-2 regressions (06-REVIEWS.md iteration 2 HIGH #2 streaming coerce double-fire, MEDIUM #4 mixed-validity tools tolerance). Adds `sawKiroNativeToolCall bool` to the SSE streaming emitter with skip-or-coerce-or-flush logic at stream end (mirrors 06-02 iteration-3 change). Changes `chatCompletionRequest.Tools` from `[]openAIToolSpec` to `[]json.RawMessage` so type-invalid sibling entries can be skipped per-entry without failing the whole request decode. Picks up iteration-3 06-01 narration aggregator: non-streaming kiro-native tool_calls now appear in `choices[].message.content` as `[tool: <name>]\n` text — no additional changes needed in the OpenAI non-streaming render path.

<objective>
OpenAI vertical slice for Phase 6. Closes TOOL-01 (OpenAI JSON-string-args side per SC #1, COERCE-synthesized only per two-path rule), TOOL-02 (OpenAI coerce side per D-01, BOTH non-streaming AND streaming per REVIEW HIGH #1), and TOOL-03 (OpenAI tools[] normalization per D-13 with iteration-3 MEDIUM #4 per-entry tolerance) end-to-end through the OpenAI surface: per-entry-tolerant Tools decoder (replacing Phase 3's `json.RawMessage` accept-and-ignore) → engine.CoerceToolCall hook-in (non-streaming + streaming end-of-stream with sawKiroNativeToolCall skip) → render (non-streaming, JSON-string args) → SSE applyChunk + end-of-stream coerce multi-frame (streaming) + golden fixtures → unit tests.

**Two-path rule enforcement (REVIEW HIGH #2):** Per CONTEXT D-03/D-05, kiro-native `ChunkKindToolCall` renders as text-delta narration ONLY (the OpenAI equivalent of Ollama's `[tool: <name>]\n` line). The previous plan's "applyChunk emits `delta.tool_calls` for kiro-native" path is REMOVED. Native `delta.tool_calls[]` frames AND the terminal `finish_reason:"tool_calls"` fire ONLY when `engine.CoerceToolCall` populates `Message.ToolCalls` at end-of-stream (or in the non-streaming render path).

**Streaming coerce path with `sawKiroNativeToolCall` skip (REVIEW HIGH #1 + iteration-3 HIGH #2 fix):** OpenAI clients commonly stream by default. The SSE emitter buffers assistant text deltas (`ChunkKindText`) when `len(req.Tools) > 0` AND the accumulated text looks JSON-shaped (starts with `{` or `` ``` ``). It ALSO tracks `sawKiroNativeToolCall bool` set true on the first `ChunkKindToolCall`. At stream close:
- **If `sawKiroNativeToolCall == true` (iteration-3):** SKIP coerce. RELEASE any buffered text frames as plain text-delta SSE frames (don't drop them). Emit terminal `finish_reason:"stop"` (NOT `"tool_calls"`).
- **Else on coerce HIT:** discard buffered text frames, emit multi-frame native `tool_calls[]` SSE shape (frame 1 id+name+empty arguments; frame 2 arguments JSON-string; terminal frame with `finish_reason:"tool_calls"`), then `data: [DONE]`.
- **Else on coerce MISS:** release buffered text frames in order, then terminal `finish_reason:"stop"`, then `data: [DONE]`.

**Mixed-validity tools tolerance (iteration-3 fix to MEDIUM #4):** decode `tools` as `[]json.RawMessage`, then unmarshal each entry independently into `openAIToolSpec` in a loop. Entries that fail per-entry unmarshal are skipped with a debug log; valid siblings are preserved. This is the only way to tolerate type-invalid sibling entries without failing the whole request decode.

Output: Four modified source files + five new SSE golden tests (StreamingCoerce, KiroNative_TextDelta, ToolCallFirst_RoleEmitOnce, Stream_NativeToolCall_ThenJSONText_NoCoerce, Stream_NativeToolCall_Only_NoCoerce) + extended wire/render tests. Zero new dependencies. Runs in parallel with Slices 2 + 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-REVIEWS.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md

# Source files this plan modifies
@internal/adapter/openai/wire.go
@internal/adapter/openai/handlers.go
@internal/adapter/openai/render.go
@internal/adapter/openai/sse.go
@internal/adapter/openai/wire_test.go
@internal/adapter/openai/render_test.go
@internal/adapter/openai/sse_golden_test.go

# Cross-adapter reference — Ollama's Phase 2 forward-seam decoder is the proven analog for D-13
@internal/adapter/ollama/wire.go

# Canonical contract from Slice 1
@internal/canonical/chat.go
@internal/canonical/chunk.go
@internal/engine/coerce.go
@internal/engine/collect.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-entry-tolerant openAIToolSpec decode via []json.RawMessage (iteration-3 fix to MEDIUM #4) + ToolChoice decode + mixed valid/invalid tools test (D-13)</name>
  <files>internal/adapter/openai/wire.go, internal/adapter/openai/wire_test.go</files>
  <read_first>
    - internal/adapter/openai/wire.go (line 38 `Tools json.RawMessage` accept-and-ignore — the Phase 3 placeholder this task replaces; line 39 surrounding fields for ToolChoice / FunctionCall context; lines 138-164 decodeMessageContent polymorphic-decode pattern — used as the model for tool_choice decode)
    - internal/adapter/ollama/wire.go (lines 46-55 ollamaToolSpec + ollamaToolSpecFunction; lines 321-330 decode loop in wireToChatRequest — the PROVEN Phase 2 pattern this task mirrors per D-13)
    - internal/adapter/openai/wire_test.go (existing TestWireToChatRequest tests; understand the table-driven shape)
    - internal/canonical/chat.go (lines 221-241 canonical.ToolSpec + canonical.ToolChoice — confirm field names: ToolSpec{Name, Description, Parameters}; ToolChoice{Type, Name})
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-13 OpenAI tools normalization)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/wire.go" section + Pattern C "Adapter-over-canonical wire-decode")
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (iteration-2 MEDIUM #4 — type-invalid sibling entries; iteration-3 directive — use `[]json.RawMessage` and unmarshal each entry independently)
  </read_first>
  <behavior>
    - **Wire types — new (mirror Ollama):**
      - `openAIToolSpec struct { Type string \`json:"type,omitempty"\`; Function *openAIToolSpecFunction \`json:"function,omitempty"\` }` — `Type` is always `"function"` per OpenAI spec but defensively decoded.
      - `openAIToolSpecFunction struct { Name string \`json:"name"\`; Description string \`json:"description,omitempty"\`; Parameters map[string]any \`json:"parameters,omitempty"\` }` — byte-identical shape to `ollamaToolSpecFunction` (OpenAI and Ollama agreed on this years ago).
    - **Tools field replacement (D-13 + iteration-3 MEDIUM #4 fix):** at `chatCompletionRequest.Tools` (currently `json.RawMessage` line 38), change to `Tools []json.RawMessage \`json:"tools,omitempty"\``. The Phase 3 accept-and-ignore behavior is removed; the field is decoded by Go's `encoding/json` as a slice of raw byte slices — this succeeds AS LONG AS the outer value is a JSON array, regardless of what each entry looks like. This is the key iteration-3 change: type-invalid sibling entries no longer fail the outer decode.
    - **wireToChatRequest extension (iteration-3 per-entry tolerance):** after the existing message walk + before `return req`, add a per-entry loop that unmarshals each `json.RawMessage` independently:
      ```go
      for i, raw := range w.Tools {
          var t openAIToolSpec
          if err := json.Unmarshal(raw, &t); err != nil {
              // Per-entry unmarshal failure — skip with debug log; preserve valid siblings.
              slog.Default().Debug("openai: tools entry decode failed; skipping",
                  "index", i,
                  "err", err.Error())
              continue
          }
          if t.Function == nil || t.Function.Name == "" {
              continue  // skip malformed-but-decoded entries (empty/missing function)
          }
          req.Tools = append(req.Tools, canonical.ToolSpec{
              Name:        t.Function.Name,
              Description: t.Function.Description,
              Parameters:  t.Function.Parameters,
          })
      }
      ```
      The TWO failure modes are now handled symmetrically: (a) type-invalid JSON shape (per-entry unmarshal fails) → skip with debug log; (b) semantically-invalid decoded struct (Function nil or name empty) → skip silently. Either way, valid siblings are preserved in their original declaration order.
    - **ToolChoice decode (D-13 second sentence):** locate the existing `tool_choice` field in `chatCompletionRequest` (it may currently be `FunctionCall json.RawMessage` at line 39 OR a different name — check the file). Decode polymorphically into `canonical.ToolChoice`:
      - JSON string `"auto"` → `canonical.ToolChoice{Type: "auto"}`
      - JSON string `"required"` → `canonical.ToolChoice{Type: "required"}` (OpenAI spec value; "any" is Anthropic-only, do NOT map here)
      - JSON string `"none"` → `canonical.ToolChoice{Type: "none"}`
      - JSON object `{"type":"function","function":{"name":"..."}}` → `canonical.ToolChoice{Type: "function", Name: "..."}`
      - Use the polymorphic-decode pattern at `internal/adapter/openai/wire.go:138-164` `decodeMessageContent` — attempt typed-object decode first; on failure, attempt string decode; on second failure, set nil (accept-and-ignore for unknown shapes).
    - **wire_test.go additions:**
      1. `TestWireToChatRequest_Tools` — request with `tools: [{type:"function", function:{name:"get_weather", description:"Get current weather", parameters:{type:"object", properties:{location:{type:"string"}}, required:["location"]}}}]` decodes to `req.Tools` with one entry; Name/Description/Parameters all match exactly. Multi-tool case preserves order.
      2. **UPDATED per iteration-3 MEDIUM #4 — `TestWireToChatRequest_Tools_MixedValidInvalid`**: request body has a tools array with FOUR entries:
         - `{type:"function", function:{name:"valid_a", parameters:{}}}` (valid)
         - `{type:"function", function:42}` (TYPE-INVALID — function is a number, fails per-entry unmarshal because `*openAIToolSpecFunction` expects an object)
         - `{type:"function", function:{name:""}}` (decoded-but-semantically-invalid — empty name)
         - `{type:"function", function:{name:"valid_b", parameters:{}}}` (valid)

         Assert `req.Tools` has TWO entries (valid_a first, valid_b second). The type-invalid entry (function:42) is skipped via the per-entry unmarshal-fail path; the empty-name entry is skipped via the post-unmarshal validation. The test PROVES that type-invalid siblings no longer fail the whole request decode — this is the iteration-3 fix to MEDIUM #4.
      3. `TestWireToChatRequest_ToolChoice_Auto` — `tool_choice: "auto"` decodes to `canonical.ToolChoice{Type:"auto"}`. Similar for `"required"`, `"none"`.
      4. `TestWireToChatRequest_ToolChoice_Function` — `tool_choice: {"type":"function","function":{"name":"get_weather"}}` decodes to `canonical.ToolChoice{Type:"function", Name:"get_weather"}`.
      5. `TestWireToChatRequest_ToolChoice_Unknown` — `tool_choice: 42` (numeric) accepts-and-ignores → `req.ToolChoice == nil`. Defensive parse.
    - **Backward compat:** requests without `tools` field continue to work — `req.Tools` stays nil (the per-entry loop is a no-op on empty input).
  </behavior>
  <action>
    Modify `internal/adapter/openai/wire.go`:
      1. Replace `Tools json.RawMessage` at line 38 with `Tools []json.RawMessage \`json:"tools,omitempty"\`` (iteration-3 — array of raw entries, decoded individually below).
      2. Add the two new wire types `openAIToolSpec` + `openAIToolSpecFunction` adjacent to the existing wire-type block.
      3. Locate the `tool_choice` field — if it's currently a typed/raw field, replace with a polymorphic-decode pattern. Either add a `ToolChoice json.RawMessage` field on the wire struct and decode it inside `wireToChatRequest`, OR if there's already a custom `UnmarshalJSON` on `chatCompletionRequest`, extend it there. The polymorphic decode pattern is at lines 138-164 (decodeMessageContent).
      4. In `wireToChatRequest`, after the existing message walk + before `return req`, add the per-entry loop per the behavior block (iteration-3 MEDIUM #4 fix — unmarshal each `json.RawMessage` independently, skip on per-entry failure with debug log, skip on missing/empty Function.Name). Add `log/slog` to imports if not already present.

    Extend `internal/adapter/openai/wire_test.go` with the FIVE test cases per the behavior block. The MixedValidInvalid case must include AT LEAST one type-invalid entry (function:42 or similar — something that fails the per-entry unmarshal, not just empty-name decode-success-but-bad-data).

    Per the cross-adapter pattern note: OpenAI and Ollama `tools[].function` schemas are byte-identical per public spec. The new openAIToolSpec types are intentionally structurally identical to Ollama's — this is NOT duplication to factor out into shared code (TRST-04 forbids it; each adapter owns its wire types per Phase 4 D-08 no-shared-driver). The duplication is acceptable; the canonical pivot is the abstraction layer.

    Per iteration-3 directive: do NOT decode `Tools []openAIToolSpec` directly. The whole point of the iteration-3 change is to use `[]json.RawMessage` so that type-invalid entries fail at per-entry unmarshal, not at outer request decode. If you forget and use `[]openAIToolSpec`, the test `TestWireToChatRequest_Tools_MixedValidInvalid` will fail because the whole request body will be rejected on the type-invalid sibling.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestWireToChatRequest" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Tools \[\]json.RawMessage" internal/adapter/openai/wire.go` shows the iteration-3 field declaration (NOT `[]openAIToolSpec`).
    - `grep -n "openAIToolSpec" internal/adapter/openai/wire.go` shows BOTH the wire type definition AND its use inside the per-entry unmarshal loop.
    - `grep -n "json.Unmarshal" internal/adapter/openai/wire.go | grep -i tool` returns ≥1 (the per-entry unmarshal call).
    - `grep -n "canonical.ToolSpec" internal/adapter/openai/wire.go` shows the canonical.ToolSpec assembly in wireToChatRequest.
    - `grep -n "canonical.ToolChoice" internal/adapter/openai/wire.go` shows the canonical.ToolChoice assembly.
    - `TestWireToChatRequest_Tools` passes — single + multi-tool cases.
    - `TestWireToChatRequest_Tools_MixedValidInvalid` passes — type-invalid sibling (function:42) is skipped via per-entry unmarshal failure; valid siblings preserved (iteration-3 MEDIUM #4 fix).
    - `TestWireToChatRequest_ToolChoice_Auto`, `_Required`, `_None`, `_Function`, `_Unknown` all pass.
    - `go test ./internal/adapter/openai/... -race -count=1` clean.
  </acceptance_criteria>
  <done>
    OpenAI Tools and ToolChoice flow into the canonical pivot via typed decoders; Phase 3 json.RawMessage accept-and-ignore replaced; D-13 closed; iteration-3 MEDIUM #4 fix: type-invalid sibling tools entries are skipped via per-entry unmarshal failure, valid siblings preserved; tests cover the four ToolChoice variants and the unknown-shape graceful-degradation case AND the mixed-valid/invalid-tools sibling-preservation case (including a TRUE type-invalid entry, not just empty-name).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handlers.go coerce hook-in + render.go JSON-string args populate (non-streaming, coerce-synthesized only)</name>
  <files>internal/adapter/openai/handlers.go, internal/adapter/openai/render.go, internal/adapter/openai/render_test.go</files>
  <read_first>
    - internal/adapter/openai/handlers.go (lines 70-92 streaming branch; lines 95-103 non-streaming Collect + render — coerce insertion point)
    - internal/adapter/openai/render.go (full file — focus on line 94-97 responseMessage struct MISSING ToolCalls field; lines 117-153 chatResponseToCompletion; lines 142 json.Marshal usage as the JSON-marshal idiom; the existing genMessageID at line ?? for crypto/rand pattern)
    - internal/adapter/openai/render_test.go (existing tests for the table-driven shape)
    - internal/canonical/chat.go (Message.ToolCalls, ToolCall{ID, Name, Arguments} — Arguments is map[string]any)
    - internal/engine/coerce.go (CoerceToolCall signature from Slice 1)
    - internal/engine/collect.go (06-01 Task 2 — confirm engine.Collect now produces `[tool: <name>]\n` text in Message.Content for kiro-native tool_calls in non-streaming path)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-01 coerce invocation, D-07 OpenAI shape, SC #1 JSON-string args)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/handlers.go" + "MOD internal/adapter/openai/render.go" sections)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Pitfall 6 — pass resp pointer directly)
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (HIGH #2 two-path rule — Message.ToolCalls only from coerce; LOW #7 defensive guard on debug log; HIGH #3 iteration-3 per-surface contract)
  </read_first>
  <behavior>
    - **handlers.go non-streaming branch (D-01 hook-in per CONTEXT line 629):** between the existing `resp, err := eng.Collect(...)` (with err-check at lines 95-103) and `writeJSON(w, chatResponseToCompletion(resp, wire.Model))`, insert (with REVIEW LOW #7 defensive length guard):
      ```go
      if engine.CoerceToolCall(req, resp) {
          var firstName string
          if len(resp.Message.ToolCalls) > 0 {
              firstName = resp.Message.ToolCalls[0].Name
          }
          a.cfg.Logger.Debug("openai: coerce fired", "tool", firstName)
      }
      ```
      Pass `resp` directly (Pitfall 6).
      **Important interaction with 06-01 narration aggregator (iteration-3):** when kiro emits a `tool_call` notification, 06-01 Task 2's narration aggregator makes `engine.Collect` produce `resp.Message.Content[textIdx].Text` containing `"[tool: <name>]\n"`. `CoerceToolCall` then attempts to parse this text as JSON (fails), then fence-strip (fails), then returns false. The narration text is preserved in `Message.Content` and reaches `chatResponseToCompletion` which renders it in `choices[].message.content`. No `Message.ToolCalls` entry is appended; `finish_reason` is NOT `"tool_calls"`. This is the intended interaction — the OpenAI non-streaming kiro-native path renders narration text in `message.content` exactly like Ollama.
    - **handlers.go streaming branch — coerce moves to sse.go (REVIEW HIGH #1 + iteration-3 HIGH #2):** the streaming branch does NOT call `engine.CoerceToolCall` inline. Instead, it passes `req` (the canonical request — needed for `req.Tools` at end-of-stream coerce) into the SSE emitter (currently the emitter only receives the chunk channel, model, etc.). The new emitter signature accepts `req *canonical.ChatRequest`. Add a code comment: `// Streaming coerce lives in sse.go — see REVIEW HIGH #1. CoerceToolCall must run AFTER all text deltas accumulate, before the terminal finish_reason frame composes. Iteration 3: sse.go also tracks sawKiroNativeToolCall and skips coerce when true.`
    - **render.go wire-type extension (CRITICAL per PATTERNS.md):** `responseMessage` (line 94-97) currently has Role + Content. Add:
      - `ToolCalls []openAIToolCall \`json:"tool_calls,omitempty"\``
      - New types: `openAIToolCall { ID string \`json:"id"\`; Type string \`json:"type"\`; Function openAIToolCallFunction \`json:"function"\` }`
      - `openAIToolCallFunction { Name string \`json:"name"\`; Arguments string \`json:"arguments"\` }` — Arguments is `string`, NOT `map[string]any` (the OpenAI wire spec serializes arguments as a JSON-encoded string — opposite of Ollama).
    - **render.go chatResponseToCompletion extension (per-surface contract comment per HIGH #3 iteration-3):** in the existing `out.Choices = []completionChoice{...}` assembly (lines 117-153), populate `responseMessage.ToolCalls` from `resp.Message.ToolCalls`:
      ```go
      // resp.Message.ToolCalls is populated for OpenAI ONLY by engine.CoerceToolCall
      // (coerce-from-text path). kiro-native tool_calls render as [tool: <name>]\n
      // narration text — for non-streaming the text comes from engine.Collect's
      // narration aggregator (06-01 Task 2) and flows through resp.Message.Content
      // into choices[0].message.content; for streaming it comes from sse.go's
      // per-chunk ChunkKindToolCall handler. Per Phase 6 D-03/D-05 (Ollama/OpenAI
      // side of the per-surface population contract; Anthropic has its own D-07
      // exception).
      for _, tc := range resp.Message.ToolCalls {
          argsJSON, err := json.Marshal(tc.Arguments)
          if err != nil { argsJSON = []byte("{}") }
          out.Choices[0].Message.ToolCalls = append(out.Choices[0].Message.ToolCalls, openAIToolCall{
              ID:       tc.ID,
              Type:     "function",
              Function: openAIToolCallFunction{Name: tc.Name, Arguments: string(argsJSON)},
          })
      }
      ```
    - **finish_reason mapping:** when `resp.Message.ToolCalls` is non-empty (only the coerce path populates it for OpenAI per per-surface contract), override `FinishReason = "tool_calls"`. Post-fixup pattern: after the per-choice assembly, if `len(resp.Message.ToolCalls) > 0` set `out.Choices[0].FinishReason = "tool_calls"`. This handles RESEARCH Open Question 2 the OpenAI-specific way; the canonical StopReason enum extension is deferred.
    - **render_test.go addition — `TestChatResponseToCompletion_ToolCalls`:**
      1. `resp.Message.ToolCalls = nil` → wire output has NO `tool_calls` key in `choices[0].message` (omitempty); existing snapshot unchanged.
      2. `resp.Message.ToolCalls = [{ID:"call_123", Name:"get_weather", Arguments: map[string]any{"location":"NYC"}}]` → wire output: `choices[0].message.tool_calls[0].id == "call_123"`, `.type == "function"`, `.function.name == "get_weather"`, `.function.arguments == "{\"location\":\"NYC\"}"` (JSON-encoded STRING value). Assert via JSON-round-trip into a typed shape that `arguments` is `string` not `object`.
      3. Single-tool case: `choices[0].finish_reason == "tool_calls"` (the post-fixup overrode the default `StopReason → stop` mapping).
      4. Multi-tool case: two entries preserved in order.
      5. **NEW iteration-3 — `KiroNativeNarration_NoToolCalls`:** `resp.Message.Content[0].Text == "[tool: get_weather]\n"` AND `resp.Message.ToolCalls == nil` → wire output has the narration text in `choices[0].message.content` AND NO `tool_calls` key AND `finish_reason != "tool_calls"`. Locks the non-streaming kiro-native render path that depends on 06-01 Task 2's narration aggregator.
    - **Wire-shape canary (D-07 OpenAI vs Ollama):** the JSON-decode-roundtrip assertion that `arguments` is `string` is the canary. Combined with Slice 2's Ollama assertion (object), this is THE Phase 6 wire-shape divergence axis lock.
  </behavior>
  <action>
    Modify `internal/adapter/openai/handlers.go` non-streaming branch: insert the CoerceToolCall block with the defensive length-guard per the behavior block. Add a streaming-branch top comment noting the coerce-moved-to-emitter rationale per REVIEW HIGH #1 + iteration-3 sawKiroNativeToolCall.

    Modify `internal/adapter/openai/handlers.go` streaming branch: change the SSE emitter invocation to pass the canonical `req` pointer.

    Modify `internal/adapter/openai/render.go`:
      1. Add the three new wire types `openAIToolCall`, `openAIToolCallFunction` adjacent to the existing `responseMessage` type. Field tags and types per the behavior block (Arguments is `string`).
      2. Add `ToolCalls []openAIToolCall \`json:"tool_calls,omitempty"\`` to `responseMessage` (currently lines 94-97).
      3. In `chatResponseToCompletion` (lines 117-153), populate `out.Choices[0].Message.ToolCalls` from `resp.Message.ToolCalls` using `json.Marshal(tc.Arguments)` (string conversion). On marshal error fall back to `"{}"` (defensive). Add the per-surface contract doc comment per the behavior block.
      4. Add the finish_reason post-fixup: after the per-choice assembly, if `len(resp.Message.ToolCalls) > 0` set `out.Choices[0].FinishReason = "tool_calls"`.

    Extend `internal/adapter/openai/render_test.go` with `TestChatResponseToCompletion_ToolCalls` covering the five cases (including the iteration-3 KiroNativeNarration_NoToolCalls case). Use the existing test scaffolding pattern in render_test.go. The byte-level wire-shape assertion (Arguments is `string`) MUST appear explicitly.

    Per Pitfall 6: pass `resp` pointer directly through handlers.go — no pre-copy.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestChatResponseToCompletion_ToolCalls|TestHandle" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "engine.CoerceToolCall" internal/adapter/openai/handlers.go` shows exactly one call in the non-streaming branch.
    - `grep -B2 -A4 "engine.CoerceToolCall" internal/adapter/openai/handlers.go` shows the `len(resp.Message.ToolCalls) > 0` defensive guard per REVIEW LOW #7.
    - `grep -n "REVIEW HIGH #1" internal/adapter/openai/handlers.go` shows the streaming-branch comment block.
    - `grep -n "ToolCalls \[\]openAIToolCall" internal/adapter/openai/render.go` shows the field added to responseMessage.
    - `grep -n "per-surface" internal/adapter/openai/render.go` shows the doc comment block.
    - `grep -n "openAIToolCallFunction" internal/adapter/openai/render.go` shows the new wire type with `Arguments string` (NOT `map[string]any`).
    - `TestChatResponseToCompletion_ToolCalls` passes with the byte-level assertion `arguments` value is a JSON STRING (escaped quotes), the wire-shape divergence canary opposite of Ollama.
    - `TestChatResponseToCompletion_ToolCalls` asserts `finish_reason == "tool_calls"` when `Message.ToolCalls` is non-empty.
    - **`TestChatResponseToCompletion_ToolCalls/KiroNativeNarration_NoToolCalls` passes** — narration text in `choices[0].message.content`, no tool_calls field, finish_reason NOT "tool_calls" (iteration-3 lock).
    - `go test ./internal/adapter/openai/... -race -count=1` clean (including the existing testmain_test.go goleak gate).
  </acceptance_criteria>
  <done>
    handlers.go invokes engine.CoerceToolCall on non-streaming path with defensive length-guard; render.go assembles choices[].message.tool_calls with JSON-STRING arguments (coerce-synthesized only per per-surface contract) + finish_reason:tool_calls post-fixup; wire-shape divergence canary locked opposite of Ollama; iteration-3 lock: kiro-native narration text passes through correctly without re-synthesizing tool_calls; streaming branch threads req to SSE emitter; no goroutine leaks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SSE applyChunk — kiro-native ChunkKindToolCall as text-delta narration + sawKiroNativeToolCall-aware STREAMING COERCE (iteration-3 fix to HIGH #2) + five golden fixtures</name>
  <files>internal/adapter/openai/sse.go, internal/adapter/openai/sse_golden_test.go</files>
  <read_first>
    - internal/adapter/openai/sse.go (full file — focus on applyChunk method around lines 110-116 drop-branch (the one being modified); lines 55-58 chunkDelta wire type MISSING ToolCalls field; lines 119-128 existing roleSent gate as the structural model for role-emit-once; the buildChunk + writeData + flusher.Flush() framing discipline; the stream finalize / terminal frame composer for the finish_reason override)
    - internal/adapter/openai/sse_golden_test.go (existing golden-fixture style — testdata path conventions, byte-comparison patterns)
    - internal/adapter/openai/testdata/ (existing golden files for the format conventions)
    - internal/canonical/chunk.go (Chunk.ToolCall field, ToolCallChunk{ID, Name, Args} from Slice 1)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-07 OpenAI multi-frame for coerce-synthesized; D-03/D-05 two-path rule for kiro-native)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md ("OpenAI streaming tool_calls multi-frame" code example lines 396-405; Pitfall 2 — do NOT split arguments across JSON-escape boundaries)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/sse.go" section)
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (HIGH #1 streaming coerce, HIGH #2 kiro-native is text-delta NOT delta.tool_calls, LOW #8 role-emit-exactly-once when tool_call is the first chunk; iteration-2 HIGH #2 — sawKiroNativeToolCall flag with skip-or-coerce-or-flush logic)
  </read_first>
  <behavior>
    - **Emitter signature change:** the SSE emit function gains a `req *canonical.ChatRequest` parameter for end-of-stream coerce.
    - **Wire-type extension on chunkDelta (lines 55-58):** add `ToolCalls []chunkDeltaToolCall \`json:"tool_calls,omitempty"\`` (used ONLY by the coerce-synthesized end-of-stream emission, NOT by per-chunk kiro-native handling). New types:
      - `chunkDeltaToolCall { Index int \`json:"index"\`; ID string \`json:"id,omitempty"\`; Type string \`json:"type,omitempty"\`; Function chunkDeltaToolCallFunction \`json:"function"\` }`
      - `chunkDeltaToolCallFunction { Name string \`json:"name,omitempty"\`; Arguments string \`json:"arguments"\` }`
    - **Kiro-native `ChunkKindToolCall` — text-delta narration + sawKiroNativeToolCall flag (REVIEW HIGH #2 + iteration-3 HIGH #2 fix):** in `applyChunk`, replace the existing drop-branch (lines 110-116) with a switch covering `ChunkKindText` (existing) and `ChunkKindToolCall` (new text-delta narration). For ChunkKindToolCall with non-nil `c.ToolCall`:
      1. If `!e.roleSent`, emit the role frame first (re-use lines 119-128 gate); set `e.roleSent = true`.
      2. Emit a single text-delta frame with `delta.content = "[tool: <c.ToolCall.Name>]\n"`. This is the OpenAI equivalent of Ollama's narration line. NO `delta.tool_calls` is emitted from this path.
      3. **Set `sawKiroNativeToolCall = true` on the emitter state (iteration-3 fix to HIGH #2).**
      - **REVIEW LOW #8 — `TestSSE_Golden_ToolCallFirst_RoleEmitOnce`** verifies this gate: the test drives `[tool_call(get_weather, {location:NYC})]` as the FIRST chunk (no preceding text chunk). Assert that exactly ONE role frame appears, before the tool_call text-delta frame.
    - **Streaming coerce path (REVIEW HIGH #1 + iteration-3 HIGH #2 fix):**
      - Emitter state: `var textBuffer strings.Builder`, `var buffering bool`, `var deferredTextFrames [][]byte`, `var coerceHit bool`, **`var sawKiroNativeToolCall bool` (iteration-3)**.
      - On `ChunkKindText`: if `len(req.Tools) > 0` AND the accumulated `strings.TrimSpace(textBuffer.String() + chunk.Text)` begins with `{` or `` ``` ``, append to buffer + build the would-be text-delta SSE frame and append to deferredTextFrames (do NOT flush). Otherwise emit per Phase 4 (flush each text-delta frame).
      - On stream close — iteration-3 skip-or-coerce-or-flush logic:
        - **If `sawKiroNativeToolCall == true` (iteration-3 fix):** SKIP coerce. RELEASE `deferredTextFrames` (if any) to the wire in order, then emit the terminal frame with `finish_reason:"stop"` (or whatever the canonical StopReason maps to — NOT `"tool_calls"`), then `data: [DONE]`.
        - **Else if `buffering == false`:** emit the terminal frame per existing logic (no coerce; finish_reason mapped from canonical StopReason), then `data: [DONE]`.
        - **Else (`sawKiroNativeToolCall == false && buffering == true`):** build synthetic `*canonical.ChatResponse{Message: {Content: []ContentPart{{Kind: ContentKindText, Text: textBuffer.String()}}}}`, call `engine.CoerceToolCall(req, syntheticResp)`.
          - On coerce HIT: DISCARD `deferredTextFrames` (do NOT release). Emit the multi-frame native tool_calls sequence per D-07 OpenAI:
            - Frame A (role frame if `!roleSent`).
            - Frame B (id + name, empty arguments): `data: {"id":...,"choices":[{"index":0,"delta":{"tool_calls":[{"index":0,"id":"<tc.ID>","type":"function","function":{"name":"<tc.Name>","arguments":""}}]},"finish_reason":null}]}`.
            - Frame C (arguments JSON string): marshal `tc.Arguments` via `json.Marshal`. Emit `data: {"id":...,"choices":[{"index":0,"delta":{"tool_calls":[{"index":0,"function":{"arguments":"<json-string>"}}]},"finish_reason":null}]}`.
            - Terminal frame: `data: {"id":...,"choices":[{"index":0,"delta":{},"finish_reason":"tool_calls"}]}`.
            - `data: [DONE]`.
          - On coerce MISS: release `deferredTextFrames` to the wire in order (each `flush()`), then emit the terminal frame normally (finish_reason mapped from canonical StopReason), then `data: [DONE]`.
    - **Two-path rule reinforcement (REVIEW HIGH #2):** native `delta.tool_calls[]` frames and `finish_reason:"tool_calls"` fire ONLY from coerce-synthesized entries (the buffered-text-end-of-stream path above). Kiro-native ChunkKindToolCall renders ONLY as text-delta narration.
    - **Pitfall 2 — no JSON-escape-boundary splits:** the multi-frame coerce-emission pattern emits the full JSON args as ONE complete string atom in frame C.
    - **emit-tracker state (single-goroutine invariant):** `textBuffer`, `buffering`, `deferredTextFrames`, `coerceHit`, `sawKiroNativeToolCall` are touched ONLY inside the select-loop goroutine.
    - **FIVE new golden fixtures:**
      1. **`TestSSE_Golden_StreamingCoerce_BareJSON`** (REVIEW HIGH #1): drive `[text("{"), text("\"location\":"), text("\"NYC\""), text("}")]` with tools=[get_weather]. Assert byte-level output:
         - Frame 1: role frame (delta.role="assistant").
         - NO text-delta frames before the tool_call frames (buffered text was discarded on coerce hit).
         - Frame 2: tool_call frame B (id+name, empty arguments).
         - Frame 3: tool_call frame C (arguments JSON string, e.g. `"arguments":"{\"location\":\"NYC\"}"`).
         - Frame 4: terminal `finish_reason:"tool_calls"`.
         - Frame 5: `data: [DONE]`.
      2. **`TestSSE_Golden_KiroNative_TextDelta`** (REVIEW HIGH #2 verification): drive `[text("I'll check. "), tool_call(ID=tc_1, Name=get_weather, Args={location:"NYC"}), text(" Done.")]`. Assert:
         - Frame 1: role frame.
         - Frame 2: text-delta "I'll check. ".
         - Frame 3: text-delta "[tool: get_weather]\n" (kiro-native narration).
         - Frame 4: text-delta " Done.".
         - Frame 5: terminal — `finish_reason:"stop"` (NOT `"tool_calls"`, since coerce did not fire).
         - Frame 6: `data: [DONE]`.
         - Byte-level assertion: `delta.tool_calls` does NOT appear anywhere in the output (kiro-native does NOT produce native tool_calls per REVIEW HIGH #2).
         - Byte-level assertion: `finish_reason:"tool_calls"` does NOT appear (only `finish_reason:"stop"` or whatever StopReason maps to).
      3. **`TestSSE_Golden_ToolCallFirst_RoleEmitOnce`** (REVIEW LOW #8): drive `[tool_call(ID=tc_1, Name=get_weather, Args={...})]` as the FIRST chunk (no preceding text). Assert:
         - Frame 1: role frame appears EXACTLY ONCE.
         - Frame 2: text-delta "[tool: get_weather]\n".
         - Terminal frame + DONE.
         - Byte-level assertion: `grep -c '"role":"assistant"'` returns exactly 1 (proves role emit-once gate works even when tool_call is the first chunk).
      4. **`TestStream_NativeToolCall_ThenJSONText_NoCoerce`** (iteration-3 fix to HIGH #2): drive `[tool_call(ID=tc_1, Name=get_weather, Args={location:"NYC"}), text("{"), text("\"location\":"), text("\"Tokyo\""), text("}")]` with tools=[get_weather]. Assert:
         - Frame 1: role frame.
         - Frame 2: text-delta "[tool: get_weather]\n" (kiro-native narration; sets sawKiroNativeToolCall=true).
         - Subsequent text triggers buffering (text starts with `{`, tools non-empty).
         - At stream end: `sawKiroNativeToolCall == true`, so coerce is SKIPPED. The buffered text frames are RELEASED to the wire as plain text-delta frames in order: `"{"`, `"\"location\":"`, `"\"Tokyo\""`, `"}"`.
         - Terminal: `finish_reason:"stop"` (NOT `"tool_calls"`).
         - `data: [DONE]`.
         - Byte-level: `delta.tool_calls` does NOT appear; `finish_reason:"tool_calls"` does NOT appear.
      5. **`TestStream_NativeToolCall_Only_NoCoerce`** (iteration-3 fix to HIGH #2): drive `[tool_call(ID=tc_1, Name=get_weather, Args={location:"NYC"})]` (no surrounding text). Assert:
         - Frame 1: role frame.
         - Frame 2: text-delta "[tool: get_weather]\n".
         - No buffered text frames (no text chunks).
         - Terminal: `finish_reason:"stop"` (NOT `"tool_calls"`).
         - `data: [DONE]`.
         - Byte-level: `delta.tool_calls` does NOT appear; `finish_reason:"tool_calls"` does NOT appear.
  </behavior>
  <action>
    Modify `internal/adapter/openai/sse.go`:
      1. Change the emit function signature to take `req *canonical.ChatRequest`.
      2. Extend `chunkDelta` (lines 55-58) with `ToolCalls []chunkDeltaToolCall \`json:"tool_calls,omitempty"\``. Add the two new wire types `chunkDeltaToolCall` + `chunkDeltaToolCallFunction` with the field tags from the behavior block.
      3. In `applyChunk`, replace the existing kind-mismatch drop-branch (lines 110-116) with a switch over `c.Kind`:
         - `case canonical.ChunkKindText:` — if `len(req.Tools) > 0` AND (buffering OR `strings.TrimSpace(textBuffer.String() + c.Text)` starts with `{` or `` ``` ``), append to buffer + build would-be SSE frame + append to deferredTextFrames (NO flush). Otherwise emit per Phase 4 (flush each text-delta).
         - `case canonical.ChunkKindToolCall:` — role frame if needed (re-use roleSent gate), then text-delta frame with `delta.content = "[tool: <c.ToolCall.Name>]\n"`. **Set `sawKiroNativeToolCall = true` (iteration-3).** NO delta.tool_calls.
         - Other kinds: preserve existing drop+debug-log discipline.
      4. Add emitter-local state: `textBuffer strings.Builder`, `buffering bool`, `deferredTextFrames [][]byte`, `coerceHit bool`, **`sawKiroNativeToolCall bool` (iteration-3)**.
      5. In the stream finalize / terminal-frame emit logic — implement iteration-3 skip-or-coerce-or-flush:
         - If `sawKiroNativeToolCall == true`: skip coerce. Release any deferredTextFrames to the wire in order. Emit terminal frame with finish_reason mapped from StopReason (NOT tool_calls). Emit `data: [DONE]`.
         - Else if `buffering == false`: emit terminal frame normally + DONE. No coerce.
         - Else (`sawKiroNativeToolCall == false && buffering == true`): build synthetic resp, call `engine.CoerceToolCall(req, syntheticResp)`. On hit, discard deferred frames, emit the multi-frame native tool_calls sequence (role-if-needed → frame B id+name → frame C arguments → terminal `finish_reason:"tool_calls"`), then DONE. On miss, release deferred frames in order, then terminal frame (finish_reason mapped from StopReason), then DONE.
      6. Remove the previous `toolCallEmitted` tracker (it was for kiro-native, which no longer triggers finish_reason override). The new flag is `sawKiroNativeToolCall` (iteration-3 expansion of the original `coerceHit` discipline).
      7. Use the existing `writeData` / `buildChunk` / `flusher.Flush()` framing.

    Extend `internal/adapter/openai/sse_golden_test.go` with the FIVE new tests per the behavior block (StreamingCoerce_BareJSON, KiroNative_TextDelta, ToolCallFirst_RoleEmitOnce, Stream_NativeToolCall_ThenJSONText_NoCoerce, Stream_NativeToolCall_Only_NoCoerce). If the file uses testdata-file fixtures, create the corresponding fixture files; if inline-string comparison, do that. The byte-level assertions (`grep -c` on key substrings) must appear explicitly in the test bodies.

    Per Pitfall 2: do NOT split the JSON arguments across multiple deltas. Frame C carries the complete args as one string atom.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestSSE_Golden|TestSSE|TestStream_" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChunkKindToolCall" internal/adapter/openai/sse.go` shows the applyChunk text-delta branch (NOT a delta.tool_calls emission).
    - `grep -n "engine.CoerceToolCall" internal/adapter/openai/sse.go` shows exactly one call (the end-of-stream coerce on buffered text, guarded by sawKiroNativeToolCall check).
    - `grep -n "sawKiroNativeToolCall" internal/adapter/openai/sse.go` shows the flag declaration AND the assignment in the ChunkKindToolCall case AND the conditional skip at stream close (iteration-3). At least 3 hits.
    - `grep -n "textBuffer\|deferredTextFrames\|coerceHit" internal/adapter/openai/sse.go` shows the new emitter state.
    - `grep -n 'finish_reason.*tool_calls' internal/adapter/openai/sse.go` shows the override fires only when `coerceHit` is true (NOT from kiro-native chunks).
    - `TestSSE_Golden_StreamingCoerce_BareJSON` passes — buffered text discarded on coerce hit; multi-frame native tool_calls + terminal finish_reason:tool_calls emitted.
    - `TestSSE_Golden_KiroNative_TextDelta` passes — kiro-native emits text-delta narration; NO `delta.tool_calls` in output; terminal `finish_reason` is NOT `"tool_calls"`.
    - `TestSSE_Golden_ToolCallFirst_RoleEmitOnce` passes — exactly one role frame even when tool_call is the first chunk (REVIEW LOW #8).
    - **`TestStream_NativeToolCall_ThenJSONText_NoCoerce` passes (iteration-3 fix to HIGH #2)** — kiro-native ran, narration emitted, buffered JSON text released as plain text-delta frames, no coerce fired, terminal finish_reason is "stop" (not "tool_calls"), no delta.tool_calls.
    - **`TestStream_NativeToolCall_Only_NoCoerce` passes (iteration-3 fix to HIGH #2)** — kiro-native ran, narration emitted, no buffered text, no coerce fired, terminal finish_reason is "stop", no delta.tool_calls.
    - `go test ./internal/adapter/openai/... -race -count=1` clean.
    - `make lint` clean for openai/sse.go modifications.
  </acceptance_criteria>
  <done>
    SSE applyChunk renders kiro-native ChunkKindToolCall as text-delta narration (REVIEW HIGH #2 two-path rule) AND sets sawKiroNativeToolCall=true (iteration-3 fix to HIGH #2); end-of-stream coerce on buffered assistant text emits the multi-frame native tool_calls sequence atomically when CoerceToolCall fires (REVIEW HIGH #1 streaming-coerce gap fix); iteration-3 skip-or-coerce-or-flush logic: if sawKiroNativeToolCall is true, skip coerce and flush buffered text as plain text-delta frames; five golden fixtures lock the streaming-coerce, kiro-native-text-delta, role-emit-once-with-tool-call-first, native-then-JSON-no-coerce, and native-only-no-coerce paths; no JSON-escape-boundary splits; single-goroutine invariant preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client request → openai.wire.go | Untrusted `tools[]` + `tool_choice` from client. Decoded by the new per-entry json.RawMessage decoder with skip-on-failure discipline (iteration-3 MEDIUM #4 fix). |
| engine.Collect → openai.handlers.go non-streaming | Trusted canonical response; CoerceToolCall parses untrusted assistant text (mitigation in engine/coerce.go from Slice 1). |
| openai.sse.go writer goroutine → HTTP response stream | Untrusted text deltas potentially buffered before streaming-coerce decision. Coerce-synthesized tool-args flow as JSON-strings. Kiro-native renders as text-delta only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5-01 (inherited) | Tampering / Input Validation | engine.CoerceToolCall called from openai/handlers.go AND openai/sse.go | mitigate | Mitigation in Slice 1 engine/coerce.go. Per Pitfall 6: pass resp/syntheticResp pointer directly, no pre-copy. |
| T-06-06 | Information disclosure | openai/render.go json.Marshal of tool args → response body | accept | Args are client-relayed data — the client sent the tools[] and is the intended recipient of tool_calls. No leak. |
| T-06-07 | Tampering | openai/wire.go polymorphic tool_choice decode | mitigate | Defensive polymorphic decode — typed-object first, fall back to string, fall back to nil. Unknown shapes accept-and-ignore. |
| T-06-15 (iteration-3 MEDIUM #4 fix) | Tampering (single-entry tools[] corruption) | openai/wire.go per-entry json.RawMessage loop | mitigate | Iteration-3 changes `Tools` from `[]openAIToolSpec` to `[]json.RawMessage` and unmarshals each entry independently with skip-on-failure + debug log. Type-invalid sibling entries (function:42) and decoded-but-semantically-invalid entries (empty name) are both skipped; valid siblings preserved in declaration order. TestWireToChatRequest_Tools_MixedValidInvalid locks the behavior. |
| T-V5-03 | DoS (oversized tool args from kiro stream) | openai/sse.go json.Marshal of buffered text | mitigate | ACP framer line cap bounds upstream notification size; textBuffer growth bounded by per-request response budget. Buffer fires only on JSON-shape heuristic. |
| T-06-05 (inherited) | Tampering | openai/sse.go textBuffer + state | mitigate | D-05 single-goroutine invariant — touched only inside the select-loop goroutine. Race detector test is the gate. |
| T-06-19 (NEW iteration-3) | Tampering (coerce double-fire after kiro-native chunk) | openai/sse.go streaming coerce vs sawKiroNativeToolCall | mitigate | iteration-3 introduces the `sawKiroNativeToolCall` flag (mirrors 06-02). Once a kiro-native ChunkKindToolCall passes through applyChunk, end-of-stream coerce is suppressed even if buffered JSON-shaped text would otherwise hit. Prevents the iteration-2 regression where buffered JSON after a kiro-native could produce a spurious second tool_call. TestStream_NativeToolCall_ThenJSONText_NoCoerce locks the behavior. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. |
</threat_model>

<verification>
After all three tasks complete:
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./...` — clean.
2. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -race -count=1` — all unit + golden tests pass under race detector + testmain_test.go goleak gate.
3. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` — clean.
4. Wire-shape canary CROSS-CHECK: `grep -A3 "TestChatResponseToCompletion_ToolCalls" internal/adapter/openai/render_test.go | grep 'arguments.*\\\\"'` shows the JSON-escape-quotes lock; `grep -A3 "TestChatResponseToWire_ToolCalls" internal/adapter/ollama/render_test.go | grep '"arguments":{'` shows the object-literal lock — the two assertions are intentionally OPPOSITE shapes.
5. `grep -n "ChunkKindToolCall" internal/adapter/openai/sse.go` returns ≥1 (the applyChunk text-delta branch).
6. `grep -n 'delta.tool_calls' internal/adapter/openai/sse_golden_test.go` — `TestSSE_Golden_KiroNative_TextDelta` asserts the substring does NOT appear in kiro-native output.
7. `grep -n "Tools \[\]json.RawMessage" internal/adapter/openai/wire.go` returns 1 (iteration-3 MEDIUM #4 fix — per-entry decode).
8. `grep -c "sawKiroNativeToolCall" internal/adapter/openai/sse.go` returns ≥3 (iteration-3 flag declaration + assignment + conditional skip).
</verification>

<success_criteria>
- TOOL-01 (OpenAI side, per SC #1): non-streaming AND streaming `/v1/chat/completions` returns `choices[].message.tool_calls[].function.arguments` as a JSON-encoded STRING — when coerce fires.
- TOOL-02 (OpenAI side, per SC #3): BOTH non-streaming AND streaming `/v1/chat/completions` with model-emitted JSON-as-text invokes `engine.CoerceToolCall` and returns a synthetic tool_calls entry (REVIEW HIGH #1 streaming-coerce gap closed).
- TOOL-03 (OpenAI side, per SC #4 + D-13 + iteration-3 MEDIUM #4): OpenAI `tools[].function` decodes into `req.Tools` via per-entry json.RawMessage unmarshal; type-invalid sibling entries (function:42) are skipped via per-entry unmarshal failure; OpenAI `tool_choice` polymorphic decode populates `req.ToolChoice`. Mixed valid/invalid tools entries preserve valid siblings.
- REVIEW HIGH #2 two-path rule honored: kiro-native ChunkKindToolCall renders as text-delta narration; NO `delta.tool_calls[]` frames and NO `finish_reason:"tool_calls"` from the kiro-native path. Multi-frame native tool_calls and finish_reason:"tool_calls" fire ONLY from coerce-synthesized entries.
- **iteration-3 fix to HIGH #2 honored:** `sawKiroNativeToolCall` flag prevents end-of-stream coerce from firing after a kiro-native tool_call passed through during the stream. Any buffered JSON-shaped text after a kiro-native chunk is released as plain text-delta frames. No double-fire.
- REVIEW LOW #8 honored: role frame emits exactly once even when tool_call is the first chunk.
- Wire-shape divergence axis canary locked: OpenAI Arguments is JSON-string (Slice 3) vs Ollama Arguments is object (Slice 2).
- Non-streaming kiro-native rendering inherits from 06-01 Task 2 narration aggregator — non-streaming kiro-native tool_calls reach `choices[0].message.content` as `[tool: <name>]\n` text without any additional logic in this plan's render path beyond the iteration-3 KiroNativeNarration_NoToolCalls test lock.
- Race detector + goleak gate clean.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-03-SUMMARY.md` summarizing: the typed Tools/ToolChoice decoders (D-13 closed, iteration-3 MEDIUM #4 per-entry json.RawMessage tolerance), the JSON-string-arguments wire path, the streaming-coerce-at-end-of-stream implementation with iteration-3 sawKiroNativeToolCall skip, the kiro-native-text-delta two-path rule enforcement, the five new golden fixtures, and the wire-shape canary cross-confirm with Slice 2.
</output>
