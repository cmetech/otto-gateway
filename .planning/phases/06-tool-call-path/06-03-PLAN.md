---
phase: 06-tool-call-path
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/adapter/openai/wire.go
  - internal/adapter/openai/handlers.go
  - internal/adapter/openai/render.go
  - internal/adapter/openai/sse.go
  - internal/adapter/openai/wire_test.go
  - internal/adapter/openai/render_test.go
  - internal/adapter/openai/sse_golden_test.go
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, openai, sse, render, coerce]

must_haves:
  truths:
    - "OpenAI POST /v1/chat/completions with tools[] decodes into req.Tools via typed openAIToolSpec (replacing Phase 3 json.RawMessage accept-and-ignore) per D-13"
    - "Non-streaming /v1/chat/completions with tool_calls returns choices[].message.tool_calls[].function.arguments as a JSON-encoded STRING (NOT object) per SC #1"
    - "Non-streaming /v1/chat/completions with model-emitted JSON-as-text invokes engine.CoerceToolCall and returns a synthetic tool_calls entry"
    - "Streaming /v1/chat/completions emits the multi-frame OpenAI shape: frame 1 (id+name, empty arguments), frame 2 (arguments JSON string), terminal frame (finish_reason: tool_calls) per D-07 OpenAI"
    - "tool_choice OpenAI shape ('auto' | 'required' | 'none' | {type:'function', function:{name}}) decodes into canonical.ToolChoice"
  artifacts:
    - path: internal/adapter/openai/wire.go
      provides: "Typed openAIToolSpec + openAIToolSpecFunction; Tools decoded to canonical.ToolSpec; ToolChoice decoded"
    - path: internal/adapter/openai/handlers.go
      provides: "engine.CoerceToolCall invocation after engine.Collect (non-streaming branch only)"
    - path: internal/adapter/openai/render.go
      provides: "chatResponseToCompletion populates choices[].message.tool_calls with JSON-string Arguments"
    - path: internal/adapter/openai/sse.go
      provides: "applyChunk handles ChunkKindToolCall with multi-frame emission + finish_reason:tool_calls terminal"
    - path: internal/adapter/openai/sse_golden_test.go
      provides: "TestSSE_Golden_ToolCall multi-frame wire-shape lock"
  key_links:
    - from: "internal/adapter/openai/wire.go::wireToChatRequest"
      to: "req.Tools (canonical.ToolSpec slice)"
      via: "typed openAIToolSpec → canonical.ToolSpec loop (Phase 2 Ollama pattern)"
      pattern: "openAIToolSpec"
    - from: "internal/adapter/openai/handlers.go (non-streaming)"
      to: "engine.CoerceToolCall"
      via: "direct call between Collect and render"
      pattern: "engine\\.CoerceToolCall\\(req, resp\\)"
    - from: "internal/adapter/openai/render.go::chatResponseToCompletion"
      to: "responseMessage.ToolCalls with JSON-string arguments"
      via: "json.Marshal(tc.Arguments) → string"
      pattern: "json\\.Marshal"
    - from: "internal/adapter/openai/sse.go::applyChunk"
      to: "multi-frame OpenAI tool_calls SSE shape"
      via: "two data: frames + terminal finish_reason"
      pattern: "finish_reason.*tool_calls"
---

<objective>
OpenAI vertical slice for Phase 6. Closes TOOL-01 (OpenAI JSON-string-args side per SC #1), TOOL-02 (OpenAI coerce side per D-01), and TOOL-03 (OpenAI tools[] normalization per D-13) end-to-end through the OpenAI surface: typed Tools decoder (replacing Phase 3's `json.RawMessage` accept-and-ignore) → engine.CoerceToolCall hook-in → render (non-streaming, JSON-string args) → SSE applyChunk multi-frame (streaming) + golden fixture → unit tests.

Purpose: After this plan, a Pi-SDK chat CLI with `base_url=http://localhost:11434/v1` and `tools: [...]` receives correct tool_calls in OpenAI's native wire shape for BOTH paths — kiro-native AND coerce. The wire-shape divergence canary fires here: OpenAI emits `arguments` as a JSON-encoded string (the opposite of Ollama's plain object from Slice 2). Streaming follows the multi-frame OpenAI convention (id+name in frame 1, arguments in frame 2, terminal `finish_reason: "tool_calls"`) per RESEARCH-verified examples from OpenAI's own docs.

Output: Two new test additions (wire_test.go extension, render_test.go extension, sse_golden_test.go extension or new). Four modified source files. Zero new dependencies. The slice runs in parallel with Slices 2 (Ollama) and 4 (Anthropic) — all three depend only on Slice 1's canonical pivot. No file-level overlap with the other Wave-2 slices.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md

# Source files this plan modifies
@internal/adapter/openai/wire.go
@internal/adapter/openai/handlers.go
@internal/adapter/openai/render.go
@internal/adapter/openai/sse.go
@internal/adapter/openai/wire_test.go
@internal/adapter/openai/render_test.go
@internal/adapter/openai/sse_golden_test.go

# Cross-adapter reference — Ollama's Phase 2 forward-seam decoder is the proven analog for D-13
@internal/adapter/ollama/wire.go

# Canonical contract from Slice 1
@internal/canonical/chat.go
@internal/canonical/chunk.go
@internal/engine/coerce.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typed openAIToolSpec decode (replace json.RawMessage) + ToolChoice decode (D-13)</name>
  <files>internal/adapter/openai/wire.go, internal/adapter/openai/wire_test.go</files>
  <read_first>
    - internal/adapter/openai/wire.go (line 38 `Tools json.RawMessage` accept-and-ignore — the Phase 3 placeholder this task replaces; line 39 surrounding fields for ToolChoice / FunctionCall context; lines 138-164 decodeMessageContent polymorphic-decode pattern — used as the model for tool_choice decode)
    - internal/adapter/ollama/wire.go (lines 46-55 ollamaToolSpec + ollamaToolSpecFunction; lines 321-330 decode loop in wireToChatRequest — the PROVEN Phase 2 pattern this task mirrors per D-13)
    - internal/adapter/openai/wire_test.go (existing TestWireToChatRequest tests; understand the table-driven shape)
    - internal/canonical/chat.go (lines 221-241 canonical.ToolSpec + canonical.ToolChoice — confirm field names: ToolSpec{Name, Description, Parameters}; ToolChoice{Type, Name})
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-13 OpenAI tools normalization)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/wire.go" section + Pattern C "Adapter-over-canonical wire-decode")
  </read_first>
  <behavior>
    - **Wire types — new (mirror Ollama):**
      - `openAIToolSpec struct { Type string \`json:"type,omitempty"\`; Function *openAIToolSpecFunction \`json:"function,omitempty"\` }` — `Type` is always `"function"` per OpenAI spec but defensively decoded.
      - `openAIToolSpecFunction struct { Name string \`json:"name"\`; Description string \`json:"description,omitempty"\`; Parameters map[string]any \`json:"parameters,omitempty"\` }` — byte-identical shape to `ollamaToolSpecFunction` (OpenAI and Ollama agreed on this years ago).
    - **Tools field replacement (D-13):** at `chatCompletionRequest.Tools` (currently `json.RawMessage` line 38), replace with `Tools []openAIToolSpec \`json:"tools,omitempty"\``. The accept-and-ignore Phase 3 behavior is removed — Tools now decode to canonical.
    - **wireToChatRequest extension:** after the existing message walk + before `return req`, add a per-tool loop populating `req.Tools` from `w.Tools` (mirror `internal/adapter/ollama/wire.go:321-330` verbatim with the new wire-type names):
      ```
      for _, t := range w.Tools {
          if t.Function == nil { continue }
          req.Tools = append(req.Tools, canonical.ToolSpec{
              Name:        t.Function.Name,
              Description: t.Function.Description,
              Parameters:  t.Function.Parameters,
          })
      }
      ```
    - **ToolChoice decode (D-13 second sentence):** locate the existing `tool_choice` field in `chatCompletionRequest` (it may currently be `FunctionCall json.RawMessage` at line 39 OR a different name — check the file). Decode polymorphically into `canonical.ToolChoice`:
      - JSON string `"auto"` → `canonical.ToolChoice{Type: "auto"}`
      - JSON string `"required"` → `canonical.ToolChoice{Type: "required"}` (OpenAI spec value; "any" is Anthropic-only, do NOT map here)
      - JSON string `"none"` → `canonical.ToolChoice{Type: "none"}`
      - JSON object `{"type":"function","function":{"name":"..."}}` → `canonical.ToolChoice{Type: "function", Name: "..."}`
      - Use the polymorphic-decode pattern at `internal/adapter/openai/wire.go:138-164` `decodeMessageContent` — attempt typed-object decode first; on failure, attempt string decode; on second failure, set nil (accept-and-ignore for unknown shapes).
    - **wire_test.go additions:**
      1. `TestWireToChatRequest_Tools` — request with `tools: [{type:"function", function:{name:"get_weather", description:"Get current weather", parameters:{type:"object", properties:{location:{type:"string"}}, required:["location"]}}}]` decodes to `req.Tools` with one entry; Name/Description/Parameters all match exactly. Multi-tool case preserves order.
      2. `TestWireToChatRequest_ToolChoice_Auto` — `tool_choice: "auto"` decodes to `canonical.ToolChoice{Type:"auto"}`. Similar for `"required"`, `"none"`.
      3. `TestWireToChatRequest_ToolChoice_Function` — `tool_choice: {"type":"function","function":{"name":"get_weather"}}` decodes to `canonical.ToolChoice{Type:"function", Name:"get_weather"}`.
      4. `TestWireToChatRequest_ToolChoice_Unknown` — `tool_choice: 42` (numeric) accepts-and-ignores → `req.ToolChoice == nil`. Defensive parse.
    - **Backward compat:** requests without `tools` field continue to work — `req.Tools` stays nil (the per-tool loop is a no-op on empty input).
  </behavior>
  <action>
    Modify `internal/adapter/openai/wire.go`:
      1. Replace `Tools json.RawMessage` at line 38 with `Tools []openAIToolSpec \`json:"tools,omitempty"\``.
      2. Add the two new wire types `openAIToolSpec` + `openAIToolSpecFunction` adjacent to the existing wire-type block.
      3. Locate the `tool_choice` field — if it's currently a typed/raw field, replace with a polymorphic-decode pattern. Either add a `ToolChoice json.RawMessage` field on the wire struct and decode it inside `wireToChatRequest`, OR if there's already a custom `UnmarshalJSON` on `chatCompletionRequest`, extend it there. The polymorphic decode pattern is at lines 138-164 (decodeMessageContent).
      4. In `wireToChatRequest`, after the existing message walk + before `return req`, add: the per-tool loop populating `req.Tools` (mirror Ollama lines 321-330), AND the polymorphic `tool_choice` decode populating `req.ToolChoice`.

    Extend `internal/adapter/openai/wire_test.go` with the four test cases per the behavior block. Reuse the existing table-driven helper if `wire_test.go` already has one; otherwise inline per existing test patterns.

    Per the cross-adapter pattern note: OpenAI and Ollama `tools[].function` schemas are byte-identical per public spec. The new openAIToolSpec types are intentionally structurally identical to Ollama's — this is NOT duplication to factor out into shared code (TRST-04 forbids it; each adapter owns its wire types per Phase 4 D-08 no-shared-driver). The duplication is acceptable; the canonical pivot is the abstraction layer.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestWireToChatRequest" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "json.RawMessage" internal/adapter/openai/wire.go` returns a lower count than before (the Tools-as-RawMessage line removed). The Read of wire.go before vs after confirms.
    - `grep -n "openAIToolSpec" internal/adapter/openai/wire.go` shows both the wire type definition and the loop body referencing it.
    - `grep -n "canonical.ToolSpec" internal/adapter/openai/wire.go` shows the canonical.ToolSpec assembly in wireToChatRequest.
    - `grep -n "canonical.ToolChoice" internal/adapter/openai/wire.go` shows the canonical.ToolChoice assembly.
    - `TestWireToChatRequest_Tools` passes — single + multi-tool cases.
    - `TestWireToChatRequest_ToolChoice_Auto`, `_Required`, `_None`, `_Function`, `_Unknown` all pass.
    - `go test ./internal/adapter/openai/... -race -count=1` clean.
  </acceptance_criteria>
  <done>
    OpenAI Tools and ToolChoice flow into the canonical pivot via typed decoders; Phase 3 json.RawMessage accept-and-ignore replaced; D-13 closed; tests cover the four ToolChoice variants and the unknown-shape graceful-degradation case.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handlers.go coerce hook-in + render.go JSON-string args populate (non-streaming)</name>
  <files>internal/adapter/openai/handlers.go, internal/adapter/openai/render.go, internal/adapter/openai/render_test.go</files>
  <read_first>
    - internal/adapter/openai/handlers.go (lines 70-92 streaming branch; lines 95-103 non-streaming Collect + render — coerce insertion point)
    - internal/adapter/openai/render.go (full file — focus on line 94-97 responseMessage struct MISSING ToolCalls field; lines 117-153 chatResponseToCompletion; lines 142 json.Marshal usage as the JSON-marshal idiom; the existing genMessageID at line ?? for crypto/rand pattern)
    - internal/adapter/openai/render_test.go (existing tests for the table-driven shape)
    - internal/canonical/chat.go (Message.ToolCalls, ToolCall{ID, Name, Arguments} — Arguments is map[string]any)
    - internal/engine/coerce.go (CoerceToolCall signature from Slice 1)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-01 coerce invocation, D-07 OpenAI shape, SC #1 JSON-string args)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/handlers.go" + "MOD internal/adapter/openai/render.go" sections)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Pitfall 6 — pass resp pointer directly)
  </read_first>
  <behavior>
    - **handlers.go non-streaming branch (D-01 hook-in per CONTEXT line 629):** between the existing `resp, err := eng.Collect(...)` (with err-check at lines 95-103) and `writeJSON(w, chatResponseToCompletion(resp, wire.Model))`, insert: `if engine.CoerceToolCall(req, resp) { a.cfg.Logger.Debug("openai: coerce fired", "tool", resp.Message.ToolCalls[0].Name) }`. Pass `resp` directly (Pitfall 6).
    - **handlers.go streaming branch — NO coerce call.** Add a code comment noting the asymmetry (same rationale as Ollama Slice 2 Task 2).
    - **render.go wire-type extension (CRITICAL per PATTERNS.md):** `responseMessage` (line 94-97) currently has Role + Content. Add:
      - `ToolCalls []openAIToolCall \`json:"tool_calls,omitempty"\``
      - New types: `openAIToolCall { ID string \`json:"id"\`; Type string \`json:"type"\`; Function openAIToolCallFunction \`json:"function"\` }`
      - `openAIToolCallFunction { Name string \`json:"name"\`; Arguments string \`json:"arguments"\` }` — Arguments is `string`, NOT `map[string]any` (the OpenAI wire spec serializes arguments as a JSON-encoded string — opposite of Ollama).
    - **render.go chatResponseToCompletion extension:** in the existing `out.Choices = []completionChoice{...}` assembly (lines 117-153), populate `responseMessage.ToolCalls` from `resp.Message.ToolCalls`:
      ```
      for _, tc := range resp.Message.ToolCalls {
          argsJSON, err := json.Marshal(tc.Arguments)
          if err != nil { argsJSON = []byte("{}") }
          out.Choices[0].Message.ToolCalls = append(out.Choices[0].Message.ToolCalls, openAIToolCall{
              ID:       tc.ID,
              Type:     "function",
              Function: openAIToolCallFunction{Name: tc.Name, Arguments: string(argsJSON)},
          })
      }
      ```
    - **finish_reason mapping:** when `resp.Message.ToolCalls` is non-empty AND the canonical `StopReason` is `StopEndTurn` (kiro's normal end), `mapFinishReason` should return `"tool_calls"` (OpenAI spec). Either modify `mapFinishReason` to take the response (so it can inspect ToolCalls) OR add a post-fixup in `chatResponseToCompletion` that overrides `FinishReason = "tool_calls"` after the per-choice assembly when `len(resp.Message.ToolCalls) > 0`. Recommendation: the post-fixup is simpler and avoids signature churn. (This handles RESEARCH Open Question 2 in the OpenAI-specific way; the canonical StopReason enum extension is deferred to a future plan if needed.)
    - **render_test.go addition — `TestChatResponseToCompletion_ToolCalls`:**
      1. `resp.Message.ToolCalls = nil` → wire output has NO `tool_calls` key in `choices[0].message` (omitempty); existing snapshot unchanged.
      2. `resp.Message.ToolCalls = [{ID:"call_123", Name:"get_weather", Arguments: map[string]any{"location":"NYC"}}]` → wire output: `choices[0].message.tool_calls[0].id == "call_123"`, `.type == "function"`, `.function.name == "get_weather"`, `.function.arguments == "{\"location\":\"NYC\"}"` (JSON-encoded STRING value). Assert via JSON-round-trip into a typed shape that `arguments` is `string` not `object`.
      3. Single-tool case: `choices[0].finish_reason == "tool_calls"` (the post-fixup overrode the default `StopReason → stop` mapping).
      4. Multi-tool case: two entries preserved in order.
    - **Wire-shape canary (D-07 OpenAI vs Ollama):** the JSON-decode-roundtrip assertion that `arguments` is `string` is the canary. Combined with Slice 2's Ollama assertion (object), this is THE Phase 6 wire-shape divergence axis lock.
  </behavior>
  <action>
    Modify `internal/adapter/openai/handlers.go` non-streaming branch: insert `if engine.CoerceToolCall(req, resp) { a.cfg.Logger.Debug("openai: coerce fired", "tool", resp.Message.ToolCalls[0].Name) }` between the existing Collect (lines 95-103) and the writeJSON call. Add a streaming-branch top comment noting the no-coerce asymmetry per CONTEXT line 298.

    Modify `internal/adapter/openai/render.go`:
      1. Add the three new wire types `openAIToolCall`, `openAIToolCallFunction` adjacent to the existing `responseMessage` type (or wherever wire-type definitions cluster). Field tags and types per the behavior block (Arguments is `string`).
      2. Add `ToolCalls []openAIToolCall \`json:"tool_calls,omitempty"\`` to `responseMessage` (currently lines 94-97).
      3. In `chatResponseToCompletion` (lines 117-153), populate `out.Choices[0].Message.ToolCalls` from `resp.Message.ToolCalls` using `json.Marshal(tc.Arguments)` (string conversion). On marshal error fall back to `"{}"` (defensive — should never happen for valid map[string]any).
      4. Add the finish_reason post-fixup: after the per-choice assembly, if `len(resp.Message.ToolCalls) > 0` set `out.Choices[0].FinishReason = "tool_calls"`.

    Extend `internal/adapter/openai/render_test.go` with `TestChatResponseToCompletion_ToolCalls` covering the four cases. Use the existing test scaffolding pattern in render_test.go. The byte-level wire-shape assertion (Arguments is `string`) MUST appear explicitly.

    Per Pitfall 6: pass `resp` pointer directly through handlers.go — no pre-copy.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestChatResponseToCompletion_ToolCalls|TestHandle" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "engine.CoerceToolCall" internal/adapter/openai/handlers.go` shows exactly one call in the non-streaming branch.
    - `grep -n "ToolCalls \[\]openAIToolCall" internal/adapter/openai/render.go` shows the field added to responseMessage.
    - `grep -n "openAIToolCallFunction" internal/adapter/openai/render.go` shows the new wire type with `Arguments string` (NOT `map[string]any`).
    - `TestChatResponseToCompletion_ToolCalls` passes with the byte-level assertion `arguments` value is a JSON STRING (e.g., contains escaped quotes `"arguments":"{\"location\":\"NYC\"}"`), the wire-shape divergence canary opposite of Ollama.
    - `TestChatResponseToCompletion_ToolCalls` asserts `finish_reason == "tool_calls"` when `Message.ToolCalls` is non-empty.
    - `go test ./internal/adapter/openai/... -race -count=1` clean (including the existing testmain_test.go goleak gate).
  </acceptance_criteria>
  <done>
    handlers.go invokes engine.CoerceToolCall on non-streaming path; render.go assembles choices[].message.tool_calls with JSON-STRING arguments + finish_reason:tool_calls post-fixup; wire-shape divergence canary locked opposite of Ollama; no goroutine leaks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SSE multi-frame tool_call emission + golden fixture</name>
  <files>internal/adapter/openai/sse.go, internal/adapter/openai/sse_golden_test.go</files>
  <read_first>
    - internal/adapter/openai/sse.go (full file — focus on applyChunk method around lines 110-116 drop-branch (the one being replaced); lines 55-58 chunkDelta wire type MISSING ToolCalls field; lines 119-128 existing roleSent gate as the structural model for emit-once-before-content; the buildChunk + writeData + flusher.Flush() framing discipline)
    - internal/adapter/openai/sse_golden_test.go (existing golden-fixture style — testdata path conventions, byte-comparison patterns)
    - internal/adapter/openai/testdata/ (existing golden files for the format conventions)
    - internal/canonical/chunk.go (Chunk.ToolCall field, ToolCallChunk{ID, Name, Args} from Slice 1)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-07 OpenAI multi-frame, Claude's Discretion §"OpenAI streaming tool_calls[] delta")
    - .planning/phases/06-tool-call-path/06-RESEARCH.md ("OpenAI streaming tool_calls multi-frame" code example lines 396-405; Pitfall 2 — do NOT split arguments across JSON-escape boundaries)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/openai/sse.go" section)
  </read_first>
  <behavior>
    - **Wire-type extension on chunkDelta (lines 55-58):** add `ToolCalls []chunkDeltaToolCall \`json:"tool_calls,omitempty"\``. New types:
      - `chunkDeltaToolCall { Index int \`json:"index"\`; ID string \`json:"id,omitempty"\`; Type string \`json:"type,omitempty"\`; Function chunkDeltaToolCallFunction \`json:"function"\` }`
      - `chunkDeltaToolCallFunction { Name string \`json:"name,omitempty"\`; Arguments string \`json:"arguments"\` }` — `Arguments` is JSON-encoded string (matching the non-streaming Slice 3 Task 2 render path).
    - **applyChunk extension (replace lines 110-116 drop-branch):** the existing `if c.Kind != canonical.ChunkKindText { drop }` short-circuit must be replaced with a switch covering `ChunkKindText` (existing behavior unchanged) and `ChunkKindToolCall` (new). For ChunkKindThought + other kinds, retain the drop discipline.
    - **Multi-frame tool_call emit (D-07 OpenAI + RESEARCH §multi-frame):** on `ChunkKindToolCall` with non-nil `c.ToolCall`:
      1. If `!e.roleSent`, emit the existing role frame first (re-use the lines 119-128 gate); then set `e.roleSent = true`.
      2. Frame 1 (id + name, empty arguments): `data: {"id":"<chatcmpl-…>","object":"chat.completion.chunk","created":<unix>,"model":"…","choices":[{"index":0,"delta":{"tool_calls":[{"index":0,"id":"<c.ToolCall.ID>","type":"function","function":{"name":"<c.ToolCall.Name>","arguments":""}}]},"finish_reason":null}]}`
      3. Frame 2 (arguments JSON string): marshal `c.ToolCall.Args` via `json.Marshal`. Emit `data: {"id":...,"choices":[{"index":0,"delta":{"tool_calls":[{"index":0,"function":{"arguments":"<json-string>"}}]},"finish_reason":null}]}` — note: the frame omits `id` and `type` on the second-frame tool_call entry (the SDK accumulates via `index`).
      4. Terminal frame (when stream closes with ToolCalls present): handled by the existing stream-finalizer — add the override: when finalizing AND tool_calls were seen, the terminal frame's `finish_reason` is `"tool_calls"` (not `"stop"`). The existing finalize code likely calls `mapFinishReason(stopReason)` — same post-fixup pattern as Slice 3 Task 2: if the emitter tracked that tool_calls were emitted, override to `"tool_calls"`.
    - **Pitfall 2 — no JSON-escape-boundary splits:** the multi-frame pattern emits the full JSON args as ONE complete string atom in frame 2. Do NOT chunk the JSON across multiple `arguments` deltas (kiro emits atomically per D-06; there's nothing to chunk).
    - **Discretion fallback:** per CONTEXT Discretion §"OpenAI streaming tool_calls[] delta also emits a separate delta.tool_calls[] chunk to start (with id + function.name) and a second chunk with arguments, or single combined frame", the multi-frame form is the recommended choice (matches OpenAI's own examples + Bifrost reference per RESEARCH). Single combined frame is acceptable if the multi-frame logic becomes load-bearing — but the golden fixture lock here commits to multi-frame.
    - **emit-tracker state (single-goroutine invariant):** track `toolCallEmitted bool` (or count) on the `sseEmitter` struct, set true after frame 2; the finalize path reads it to decide whether to override `finish_reason`. Touched only inside the select-loop goroutine (Phase 4 D-05 invariant).
    - **Golden fixture (`TestSSE_Golden_ToolCall`):** in `sse_golden_test.go` (extend existing or create new test in the same file), drive a fake chunk stream `[text-chunk("I'll check weather. "), tool_call-chunk(ID=call_1, Name=get_weather, Args={location:"NYC"}), stream-close]` through the SSE emitter. Capture the full byte output. Assert the multi-line structure:
      1. First `data:` line carries role frame (delta.role="assistant").
      2. Second `data:` line carries text-content frame (`delta.content == "I'll check weather. "`).
      3. Third `data:` line carries frame 1 tool_call (id + name + empty arguments).
      4. Fourth `data:` line carries frame 2 tool_call (arguments JSON-string).
      5. Fifth `data:` line carries terminal (`delta:{}`, `finish_reason:"tool_calls"`).
      6. Sixth line is exactly `data: [DONE]`.
      7. Byte-level: assert `"arguments":"{\"location\":\"NYC\"}"` (escaped-quote string) appears in frame 2.
      8. Assert `"finish_reason":"tool_calls"` appears in frame 5 (NOT `"stop"`).
    - **No char-deltas / no fake-streaming (Anti-pattern in RESEARCH):** kiro emits atomically; the emitter does NOT split the JSON across multiple frames. Frame 1 is just the id+name skeleton; frame 2 is the entire serialized args.
  </behavior>
  <action>
    Modify `internal/adapter/openai/sse.go`:
      1. Extend `chunkDelta` (lines 55-58) with `ToolCalls []chunkDeltaToolCall \`json:"tool_calls,omitempty"\``. Add the two new wire types `chunkDeltaToolCall` + `chunkDeltaToolCallFunction` with the field tags from the behavior block.
      2. In `applyChunk`, replace the existing kind-mismatch drop-branch (lines 110-116) with a switch over `c.Kind`: keep the existing ChunkKindText path; add `case canonical.ChunkKindToolCall:` emitting the multi-frame sequence (role frame if needed → frame 1 id+name → frame 2 arguments); for other kinds preserve the drop+debug-log behavior.
      3. Track `toolCallEmitted bool` (or a counter) on the `sseEmitter` struct. Set true after frame 2.
      4. In the stream finalize / terminal-frame emit logic, when `toolCallEmitted` is true override `finish_reason` to `"tool_calls"` (instead of mapping from canonical StopReason).
      5. Use the existing `writeData` / `buildChunk` / `flusher.Flush()` framing — do NOT introduce a parallel emission path. D-05 single-goroutine invariant preserved.

    Extend `internal/adapter/openai/sse_golden_test.go` with `TestSSE_Golden_ToolCall`. If the file uses testdata-file fixtures (`testdata/sse_tool_call.txt`), create the corresponding fixture file with the expected line-by-line output AND drive the test through the fixture-compare helper. If the file uses inline-string comparison, do the same inline. Per the existing patterns in sse_golden_test.go.

    Per Pitfall 2: do NOT split the JSON arguments across multiple deltas. Frame 2 carries the complete args as one string atom.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -run "TestSSE_Golden_ToolCall|TestSSE" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChunkKindToolCall" internal/adapter/openai/sse.go` shows the applyChunk branch added.
    - `grep -n "chunkDeltaToolCall" internal/adapter/openai/sse.go` shows the new wire type and its use inside applyChunk.
    - `grep -n 'finish_reason.*tool_calls' internal/adapter/openai/sse.go` shows the override path firing when toolCallEmitted is true.
    - `TestSSE_Golden_ToolCall` passes with the full multi-frame sequence asserted (frame 1 id+name, frame 2 arguments-string, terminal finish_reason).
    - `TestSSE_Golden_ToolCall` byte-level: the arguments-string frame contains `"arguments":"{\"location\":\"NYC\"}"` with escape-quotes, NOT a JSON object literal.
    - `go test ./internal/adapter/openai/... -race -count=1` clean.
    - `make lint` clean for openai/sse.go modifications.
  </acceptance_criteria>
  <done>
    SSE applyChunk emits the OpenAI multi-frame tool_call sequence atomically (id+name → arguments → terminal finish_reason:tool_calls); golden fixture locks the byte-level wire shape; no JSON-escape-boundary splits; single-goroutine invariant preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client request → openai.wire.go | Untrusted `tools[]` + `tool_choice` from client. Decoded by the new typed decoders. |
| engine.Collect → openai.handlers.go non-streaming | Trusted canonical response; CoerceToolCall parses untrusted assistant text (mitigation lives in engine/coerce.go from Slice 1). |
| openai.sse.go writer goroutine → HTTP response stream | Untrusted tool-args flow into SSE wire bytes (JSON-string encoded). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5-01 (inherited) | Tampering / Input Validation | engine.CoerceToolCall called from openai/handlers.go | mitigate | Mitigation in Slice 1 engine/coerce.go. Per Pitfall 6: pass resp pointer directly, no pre-copy. |
| T-06-06 | Information disclosure | openai/render.go json.Marshal of tool args → response body | accept | Args are client-relayed data — the client sent the tools[] and is the intended recipient of tool_calls. No leak. |
| T-06-07 | Tampering | openai/wire.go polymorphic tool_choice decode | mitigate | Defensive polymorphic decode — typed-object first, fall back to string, fall back to nil. Unknown shapes accept-and-ignore (graceful degradation). No raw-string concatenation into the canonical struct. |
| T-V5-03 | DoS (oversized tool args from kiro stream) | openai/sse.go json.Marshal of c.ToolCall.Args | mitigate | ACP framer line cap (existing internal/acp/framer.go) bounds the incoming notification size. json.Marshal of map[string]any has no inherent size cap but the cap is already enforced upstream at the framer boundary. |
| T-06-05 (inherited) | Tampering | openai/sse.go toolCallEmitted tracker | mitigate | D-05 single-goroutine invariant — touched only inside the select-loop goroutine. Race detector test is the gate. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. |
</threat_model>

<verification>
After all three tasks complete:
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./...` — clean.
2. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/openai/... -race -count=1` — all unit + golden tests pass under race detector + testmain_test.go goleak gate.
3. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` — clean.
4. Wire-shape canary CROSS-CHECK: `grep -A3 "TestChatResponseToCompletion_ToolCalls" internal/adapter/openai/render_test.go | grep 'arguments.*\\\\"'` shows the JSON-escape-quotes lock; `grep -A3 "TestChatResponseToWire_ToolCalls" internal/adapter/ollama/render_test.go | grep '"arguments":{'` shows the object-literal lock — the two assertions are intentionally OPPOSITE shapes.
5. `grep -n "ChunkKindToolCall" internal/adapter/openai/sse.go` returns ≥1 (the applyChunk branch).
</verification>

<success_criteria>
- TOOL-01 (OpenAI side, per SC #1): non-streaming `/v1/chat/completions` returns `choices[].message.tool_calls[].function.arguments` as a JSON-encoded STRING.
- TOOL-02 (OpenAI side, per SC #3): non-streaming `/v1/chat/completions` with model-emitted JSON-as-text invokes `engine.CoerceToolCall` and returns a synthetic tool_calls entry.
- TOOL-03 (OpenAI side, per SC #4 + D-13): OpenAI `tools[].function` decodes into `req.Tools`; OpenAI `tool_choice` polymorphic decode populates `req.ToolChoice`.
- D-07 OpenAI: streaming emits multi-frame sequence (id+name → arguments → terminal `finish_reason: "tool_calls"`); golden fixture locks the byte-level shape.
- Wire-shape divergence axis canary locked: OpenAI Arguments is JSON-string (Slice 3) vs Ollama Arguments is object (Slice 2).
- Race detector + goleak gate clean.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-03-SUMMARY.md` summarizing: the typed Tools/ToolChoice decoders (D-13 closed), the JSON-string-arguments wire path, the multi-frame SSE golden fixture, and the wire-shape canary cross-confirm with Slice 2.
</output>
