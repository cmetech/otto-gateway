---
phase: 06-tool-call-path
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/adapter/ollama/wire.go
  - internal/adapter/ollama/handlers.go
  - internal/adapter/ollama/render.go
  - internal/adapter/ollama/ndjson.go
  - internal/adapter/ollama/render_test.go
  - internal/adapter/ollama/wire_test.go
  - internal/adapter/ollama/ndjson_test.go
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, ollama, render, ndjson, coerce]

must_haves:
  truths:
    - "Non-streaming POST /api/chat with tools[] and a kiro tool_call response returns message.tool_calls[].function.arguments as a plain JSON object (NOT a JSON-encoded string) per SC #2"
    - "Non-streaming POST /api/chat with tools[] and a model-emitted bare JSON text response invokes engine.CoerceToolCall and returns a synthetic message.tool_calls[] entry with content empty"
    - "Streaming POST /api/chat with tools[] emits the final done:true NDJSON line carrying message.tool_calls[] (atomic — no per-delta tool_calls frames) per D-07 Ollama"
    - "Per-chunk ChunkKindToolCall emission renders an operator-visible [tool: <name>]\\n thought-text delta on the wire (Node parity per D-03 wire rendering)"
    - "Phase 2 forward-seam ollamaToolSpec → canonical.ToolSpec decoder is regression-tested (no change)"
  artifacts:
    - path: internal/adapter/ollama/wire.go
      provides: "ollamaChatResponseMessage.ToolCalls field added"
      contains: "ToolCalls []ollamaToolCall"
    - path: internal/adapter/ollama/handlers.go
      provides: "engine.CoerceToolCall invocation after engine.Collect (non-streaming branch only)"
    - path: internal/adapter/ollama/render.go
      provides: "chatResponseToWire populates Message.ToolCalls from resp.Message.ToolCalls"
    - path: internal/adapter/ollama/ndjson.go
      provides: "Per-chunk [tool: <name>]\\n thought-text emit + tool_calls aggregator for done:true line"
    - path: internal/adapter/ollama/render_test.go
      provides: "TestChatResponseToWire_ToolCalls covering plain-object args"
    - path: internal/adapter/ollama/ndjson_test.go
      provides: "TestNDJSON_ToolCalls_DoneLine golden fixture"
  key_links:
    - from: "internal/adapter/ollama/handlers.go (non-streaming branch)"
      to: "engine.CoerceToolCall"
      via: "direct call between Collect and render"
      pattern: "engine\\.CoerceToolCall\\(req, resp\\)"
    - from: "internal/adapter/ollama/render.go::chatResponseToWire"
      to: "resp.Message.ToolCalls → ollamaChatResponseMessage.ToolCalls"
      via: "per-canonical-ToolCall loop, Arguments passed verbatim as map[string]any"
      pattern: "ollamaToolCall"
    - from: "internal/adapter/ollama/ndjson.go (streaming aggregator)"
      to: "final done:true line carrying message.tool_calls[]"
      via: "[]ollamaToolCall accumulator on the emitter goroutine"
      pattern: "ChunkKindToolCall"
---

<objective>
Ollama vertical slice for Phase 6. Closes TOOL-01 (Ollama plain-object args side per SC #2) and TOOL-02 (Ollama coerce side per D-01) end-to-end through the Ollama surface: canonical → engine.CoerceToolCall hook-in → render (non-streaming) → ndjson emitter (streaming) → unit + property tests. TOOL-03 (Ollama tools normalization) is already complete from Phase 2's forward seam (`internal/adapter/ollama/wire.go:321-330`) — this plan adds a regression test to lock it in.

Purpose: After this plan, an existing LangFlow flow pointing at `/api/chat` with `tools: [...]` receives correct tool_calls in Ollama's native wire shape for BOTH paths — kiro-native (via canonical Message.ToolCalls aggregation from Slice 1) AND model-emitted JSON-as-text (via CoerceToolCall). The wire-shape divergence canary (CONTEXT specifics §"Tool-call argument shape divergence is THE Phase 6 wire-test axis") gets its Ollama side locked: arguments are plain objects, not JSON strings.

Output: Two new test files (render_test.go extension, ndjson_test.go extension) covering both paths. Four modified source files (wire.go, handlers.go, render.go, ndjson.go). Zero new dependencies. The slice runs in parallel with Slices 3 (OpenAI) and 4 (Anthropic) because all three share only the canonical pivot from Slice 1 — no file-level overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md

# Source files this plan modifies
@internal/adapter/ollama/wire.go
@internal/adapter/ollama/handlers.go
@internal/adapter/ollama/render.go
@internal/adapter/ollama/ndjson.go
@internal/adapter/ollama/render_test.go
@internal/adapter/ollama/wire_test.go
@internal/adapter/ollama/ndjson_test.go

# Canonical contract from Slice 1 — must use these field names exactly.
@internal/canonical/chat.go
@internal/canonical/chunk.go
@internal/engine/coerce.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ollamaChatResponseMessage.ToolCalls + render.go populate from resp.Message.ToolCalls (non-streaming)</name>
  <files>internal/adapter/ollama/wire.go, internal/adapter/ollama/render.go, internal/adapter/ollama/render_test.go</files>
  <read_first>
    - internal/adapter/ollama/wire.go (lines 40 ollamaMessage with ToolCalls already; lines 60-67 ollamaToolCall + ollamaToolCallFunction wire types; lines 86-90 ollamaChatResponseMessage MISSING ToolCalls field — this is the gap; lines 321-330 ollamaToolSpec → canonical.ToolSpec Phase 2 decoder for the request side)
    - internal/adapter/ollama/render.go (lines 27-74 chatResponseToWire — the function being extended; lines 56-73 out construction pattern)
    - internal/adapter/ollama/render_test.go (existing TestChatResponseToWire tests to extend; understand the table-driven shape)
    - internal/canonical/chat.go (lines 140-148 Message.ToolCalls; lines 208-219 canonical.ToolCall with field name Arguments — note the asymmetry vs ChunkKindToolCall.Args)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("MOD internal/adapter/ollama/render.go" + "MOD internal/adapter/ollama/ndjson.go" sections — CRITICAL note on wire-type gap)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-04 plain-object args; D-15 Ollama already decodes Tools)
  </read_first>
  <behavior>
    - **wire.go addition (CRITICAL — wire-type gap from Phase 2):** `ollamaChatResponseMessage` at lines 86-90 currently has Role + Content + Thinking but NO ToolCalls field. Add `ToolCalls []ollamaToolCall \`json:"tool_calls,omitempty"\`` matching the request-side `ollamaMessage` at line 40. The existing `ollamaToolCall` + `ollamaToolCallFunction` types at lines 60-67 are reused unchanged. `Arguments map[string]any` is the existing wire-type field name — passes through Go's `encoding/json` as a plain JSON object (NOT a string).
    - **render.go chatResponseToWire extension:** the existing `out := &ollamaChatResponse{...}` construction (lines 56-73) populates Role/Content/Thinking. Add: loop over `resp.Message.ToolCalls` (canonical) and build `[]ollamaToolCall` with `Function: ollamaToolCallFunction{Name: tc.Name, Arguments: tc.Arguments}`. The canonical `Arguments` is `map[string]any` — assigned directly to the wire `Arguments map[string]any` field. NO `json.Marshal` to string here (that's OpenAI's wire path per D-04 vs Ollama plain-object per CONTEXT specifics §wire-shape divergence axis).
    - **nil-handling:** if `resp.Message.ToolCalls` is nil/empty, do NOT populate `out.Message.ToolCalls` — omitempty handles the absent case. (Backward compat: existing text-only responses must still serialize identically to pre-Phase-6 — verified by golden comparison.)
    - **render_test.go addition — `TestChatResponseToWire_ToolCalls`:** table-driven cases:
      1. `resp.Message.ToolCalls = nil` → wire output has NO `tool_calls` key (omitempty); existing snapshot unchanged.
      2. `resp.Message.ToolCalls = [{ID:"call_123", Name:"get_weather", Arguments: map[string]any{"location":"NYC"}}]` → wire output has `"tool_calls":[{"function":{"name":"get_weather","arguments":{"location":"NYC"}}}]`. Arguments value is a JSON OBJECT, not a string. Assert via JSON-round-trip into a typed shape OR via `strings.Contains` for the substring `"arguments":{"location":"NYC"}` (the absence of escaped quotes inside the value is the Ollama-vs-OpenAI canary).
      3. Multi-tool: two entries preserved in order.
    - **Round-trip assertion (D-12):** add a sub-case in `TestChatResponseToWire_ToolCalls` that wire-encodes then JSON-decodes the response back into a generic `map[string]any` and asserts `tool_calls[0].function.arguments` is `map[string]any` (not `string`). This is the byte-level wire-shape lock per CONTEXT specifics §axis.
    - **Phase 2 regression — wire.go decoder regression test:** in `wire_test.go` (existing file), add `TestWireToChatRequest_Tools_Phase2Regression` (or extend an existing test) asserting that a request with `tools: [{type:"function", function:{name:"get_weather", description:"...", parameters:{type:"object", properties:{location:{type:"string"}}}}}]` decodes into `req.Tools` with exactly one `canonical.ToolSpec{Name:"get_weather", Description:"...", Parameters: map[string]any{...}}`. The Phase 2 forward-seam decoder at lines 321-330 is not modified — this test locks it as a regression guard against future refactors.
  </behavior>
  <action>
    Modify `internal/adapter/ollama/wire.go`: add a new `ToolCalls []ollamaToolCall \`json:"tool_calls,omitempty"\`` field to `ollamaChatResponseMessage` (currently lines 86-90). The field name and tag must match the request-side `ollamaMessage` at line 40 for symmetry. Do NOT modify the existing `ollamaToolCall` / `ollamaToolCallFunction` types (lines 60-67) — they're already correct.

    Modify `internal/adapter/ollama/render.go::chatResponseToWire`: extend the `out := &ollamaChatResponse{...}` assembly (lines 56-73) to populate `out.Message.ToolCalls` from `resp.Message.ToolCalls` via a per-entry loop. The canonical `ToolCall.Arguments map[string]any` is assigned verbatim to the wire `ollamaToolCallFunction.Arguments map[string]any` — no JSON-string encoding.

    Extend `internal/adapter/ollama/render_test.go` with `TestChatResponseToWire_ToolCalls` covering the three cases plus the byte-level wire-shape round-trip assertion. Use the existing helper-style if `render_test.go` already has one; otherwise inline the assertions per the existing test patterns.

    Extend `internal/adapter/ollama/wire_test.go` with `TestWireToChatRequest_Tools_Phase2Regression` locking the existing Phase 2 forward-seam decoder behavior. This protects against accidental regression when Wave 2 Slice 3 (OpenAI) authors might be tempted to refactor cross-adapter "for consistency."

    Per Pitfall 6 from RESEARCH: this task does NOT yet touch handlers.go — the coerce hook-in is Task 2. Render alone is testable in isolation by constructing `*canonical.ChatResponse` with pre-populated `Message.ToolCalls` (simulating the engine.Collect output from Slice 1 — which Task 2 of Slice 1 already verifies in TestCollect_AggregatesToolCalls).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/ollama/... -run "TestChatResponseToWire_ToolCalls|TestWireToChatRequest_Tools" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ToolCalls \[\]ollamaToolCall" internal/adapter/ollama/wire.go` shows the field added to `ollamaChatResponseMessage` (in addition to the existing one on `ollamaMessage`).
    - `grep -n "json.Marshal" internal/adapter/ollama/render.go` returns 0 NEW marshal calls in the tool_calls path (Ollama wire spec passes Arguments as plain map, NOT JSON string — vs OpenAI which DOES marshal).
    - `TestChatResponseToWire_ToolCalls` byte-level assertion shows `"arguments":{"location":"NYC"}` (object) in the wire output — NOT `"arguments":"{\"location\":\"NYC\"}"` (escaped string). This is the canary for the Phase 6 wire-shape divergence axis.
    - `TestWireToChatRequest_Tools_Phase2Regression` passes — Phase 2 decoder unchanged.
    - `go test ./internal/adapter/ollama/... -race -count=1` clean.
  </acceptance_criteria>
  <done>
    ollamaChatResponseMessage carries ToolCalls; chatResponseToWire populates it from canonical Message.ToolCalls with plain-object Arguments; new render_test.go assertion proves the wire-shape divergence canary (objects, not JSON strings); Phase 2 forward-seam decoder regression-tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handlers.go coerce hook-in (non-streaming) + ndjson.go streaming aggregator + per-chunk [tool: …] thought-text + done:true line tool_calls</name>
  <files>internal/adapter/ollama/handlers.go, internal/adapter/ollama/ndjson.go, internal/adapter/ollama/ndjson_test.go</files>
  <read_first>
    - internal/adapter/ollama/handlers.go (lines 73-83 Collect non-streaming branch — coerce insertion point; lines 85-99 streaming branch — Coerce is NOT called here per CONTEXT line 298 / two-path rule)
    - internal/adapter/ollama/ndjson.go (full file — focus on emitNDJSONChunk per-chunk switch around line 60-123; finalizeNDJSON around lines 197-243 done:true line composer; chatResponseToWire call at line 213-232 passes nil — note the nil-guard discipline)
    - internal/adapter/ollama/ndjson_test.go (existing tests as the structural model for TestNDJSON_ToolCalls_DoneLine golden subtests)
    - internal/canonical/chunk.go (Chunk + ChunkKindToolCall + ToolCallChunk shape from Slice 1)
    - internal/engine/coerce.go (CoerceToolCall signature from Slice 1)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md (sections "MOD ollama/handlers.go" + "MOD ollama/ndjson.go")
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-01 coerce invocation, D-03 wire rendering [tool: <name>]\n, D-07 Ollama atomic done-line, D-04 plain-object args)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Pitfall 5 — DO preserve [tool: <name>] thought-text per-line AND emit structured tool_calls on done line; Pitfall 6 — pass resp pointer directly, no pre-copy)
  </read_first>
  <behavior>
    - **handlers.go non-streaming branch (D-01 hook-in per CONTEXT):** between the existing `eng.Collect` call (currently around lines 73-83) and the existing `writeJSON(w, chatResponseToWire(resp, start, wire.Model))`, insert: `if engine.CoerceToolCall(req, resp) { a.cfg.Logger.Debug("ollama: coerce fired", "tool", resp.Message.ToolCalls[0].Name) }`. Per Pitfall 6: pass `resp` directly (pointer) — do NOT pre-copy.
    - **handlers.go streaming branch — NO coerce call.** Per CONTEXT line 298 + D-01: the two-path rule applies SPECIFICALLY to streaming wire shape. Coerce-synthesized tool_calls live ONLY in the non-streaming Collect path. Streaming chunks emit live before any aggregation — the kiro-native path is the only streaming tool_call source. Add a code comment at the top of the streaming branch noting this asymmetry for future readers.
    - **ndjson.go per-chunk emit branch (D-03 wire rendering — Pitfall 5):** in `emitNDJSONChunk` (currently around lines 60-123), add `case canonical.ChunkKindToolCall:` that emits a thought-text NDJSON delta with `{"message":{"role":"assistant","content":"[tool: <chunk.ToolCall.Name>]\n"}, "done":false, ...}` matching the existing thought-emit shape (whichever line/format Phase 1.1 + Phase 4 settled on for thought streaming). This preserves the operator-visible narration (Node parity / LangFlow expectation). The CONTEXT Discretion option to grow this to `[tool: <name>(args)]\n` is rejected for this plan — stay strict to Node form for byte-fidelity. (Operator-DX expansion is a Phase-9 or future tweak.)
    - **ndjson.go streaming aggregator (D-07 Ollama atomic done-line):** alongside the existing text/thought aggregation, accumulate a `[]ollamaToolCall` slice on the emitter goroutine as each `ChunkKindToolCall` arrives. On `finalizeNDJSON` (lines 197-243), pass the accumulated tool_calls into the done:true line composition. The cleanest seam: change `finalizeNDJSON` to take the accumulated `*canonical.ChatResponse`-equivalent (or just the `[]ollamaToolCall` slice) and pass it through `chatResponseToWire` (which now populates ToolCalls per Task 1).
      - Per PATTERNS.md note: `chatResponseToWire(nil, start, model)` at line 213-232 today passes nil. The simplest evolution is: build a synthetic `*canonical.ChatResponse{Message: canonical.Message{ToolCalls: accumulatedTC}}` and pass it — chatResponseToWire's existing nil-guard discipline + the new ToolCalls populate logic handles the rest.
    - **D-05 single-goroutine invariant preserved:** the `[]ollamaToolCall` accumulator is touched ONLY inside the select-loop goroutine that processes chunks. No mutex, no shared state.
    - **Streaming wire-shape canary (D-07 Ollama):** the test must assert that NO intermediate NDJSON line contains a `tool_calls` field — only the final `done:true` line has it. Per-chunk lines for ChunkKindToolCall carry the `[tool: <name>]\n` thought-text content instead.
    - **ndjson_test.go addition — `TestNDJSON_ToolCalls_DoneLine`:** golden-fixture-style test that drives a fake chunk stream through the emitter:
      1. text-chunk("hello") → expect line `{"message":{"content":"hello",...},"done":false}` (no tool_calls).
      2. tool_call-chunk(ID=call_1, Name=get_weather, Args={location:NYC}) → expect line `{"message":{"content":"[tool: get_weather]\n",...},"done":false}` (thought-text emit per D-03 — no tool_calls on this intermediate line).
      3. (stream close) → expect final line `{"message":{...,"tool_calls":[{"function":{"name":"get_weather","arguments":{"location":"NYC"}}}]},"done":true,"done_reason":"stop",...}`.
      4. Assert `arguments` value is a JSON object literal (substring `"arguments":{"location":"NYC"}` — no escaped quotes).
      5. Assert NO intermediate line contains `tool_calls` (use line-by-line iteration + `strings.Contains` negation).
    - **goleak gate:** the ndjson tests should run under the existing `testmain_test.go` goleak verifier (per Phase 4 / 5 discipline, already in place for `internal/adapter/ollama/`). No new gate needed.
  </behavior>
  <action>
    Modify `internal/adapter/ollama/handlers.go` non-streaming branch: insert `if engine.CoerceToolCall(req, resp) { a.cfg.Logger.Debug("ollama: coerce fired", "tool", resp.Message.ToolCalls[0].Name) }` between the existing `resp, err := eng.Collect(...)` (with err-check) and `writeJSON(w, chatResponseToWire(resp, start, wire.Model))`. Use `a.cfg.Logger` (or whatever the existing pattern is — check the file for the slog accessor).

    Add a code comment at the streaming branch top noting "Coerce is intentionally NOT called on the streaming path — per CONTEXT D-01 / line 298 two-path rule, coerce-synthesized tool_calls live only in the non-streaming Collect path."

    Modify `internal/adapter/ollama/ndjson.go` `emitNDJSONChunk` per-chunk switch: add `case canonical.ChunkKindToolCall:` emitting a thought-text NDJSON delta with content `[tool: <chunk.ToolCall.Name>]\n` per D-03. Use the existing thought-chunk emission shape (find it in the file — likely `case canonical.ChunkKindThought:` emits content with thinking-marker similar to thought handling).

    Modify `internal/adapter/ollama/ndjson.go` to accumulate `[]ollamaToolCall` (or `[]canonical.ToolCall`) per emitter as chunks flow. On `finalizeNDJSON` (lines 197-243), thread the accumulated tool_calls into the done:true line. Simplest: build a synthetic `*canonical.ChatResponse{Message: canonical.Message{ToolCalls: accumulated}}` and pass to `chatResponseToWire(syntheticResp, start, model)` (which Task 1 made populate-aware).

    Extend `internal/adapter/ollama/ndjson_test.go` with `TestNDJSON_ToolCalls_DoneLine` per the behavior block. Use the existing fake-chunk-stream pattern (whichever is in `ndjson_test.go` today — likely a `httptest.ResponseRecorder` + a channel-driven chunk source).

    Critical Pitfall-6 reminder: in handlers.go pass `resp` directly to CoerceToolCall and to chatResponseToWire — do NOT capture `respCopy := *resp` because the ToolCalls slice append would not propagate.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/ollama/... -run "TestNDJSON_ToolCalls_DoneLine|TestChat_" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "engine.CoerceToolCall" internal/adapter/ollama/handlers.go` shows exactly one call (non-streaming branch). Zero hits in the streaming branch.
    - `grep -n "Coerce is intentionally NOT" internal/adapter/ollama/handlers.go` shows the streaming-branch comment.
    - `grep -n "ChunkKindToolCall" internal/adapter/ollama/ndjson.go` shows both the per-chunk thought-text emission AND the aggregator append.
    - `TestNDJSON_ToolCalls_DoneLine` passes including the multi-line wire-shape assertion (intermediate lines have NO tool_calls; final done:true line does).
    - `TestNDJSON_ToolCalls_DoneLine` byte-level assertion: `arguments` value in the done:true line is a JSON object literal (no escaped quotes).
    - `go test ./internal/adapter/ollama/... -race -count=1` clean (including the existing testmain_test.go goleak gate).
    - `go vet ./internal/adapter/ollama/...` clean.
  </acceptance_criteria>
  <done>
    handlers.go invokes engine.CoerceToolCall on the non-streaming path only; ndjson.go emits operator-visible [tool: <name>] thought-text per chunk AND accumulates a structured tool_calls slice for atomic emission on the done:true line; new test locks the wire shape (objects not strings, no intermediate tool_calls); no goroutine leaks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client request → ollama.handlers.go | Untrusted `tools[]` catalog from clients. Decoded by Phase 2 forward seam (no change). |
| engine.Collect → handlers.go non-streaming branch | Trusted canonical response from engine, but Message.Content text is ultimately model-generated (untrusted) — CoerceToolCall parses it as JSON. |
| ndjson.go writer goroutine → HTTP response stream | Untrusted tool-args from canonical.ToolCallChunk flow into NDJSON wire bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5-01 (inherited from Slice 1) | Tampering / Input Validation | engine.CoerceToolCall called from ollama/handlers.go | mitigate | Mitigation lives in `internal/engine/coerce.go` (Slice 1 Task 3); this plan just invokes it. Per Pitfall 6: pass `resp` pointer directly, no pre-copy (CoerceToolCall's slice mutation must be observed by the caller). |
| T-06-04 | Information disclosure | a.cfg.Logger.Debug "coerce fired" log line with tool name | accept | Tool name is client-supplied + non-sensitive (it's the function name to invoke); descriptions / args NOT logged. Per RESEARCH §Security Domain V8: logs follow Phase 5 truncateForLog discipline — no Args payload echoed. |
| T-06-05 | Tampering | ndjson.go tool_calls aggregator on emitter goroutine | mitigate | D-05 single-goroutine invariant — `[]ollamaToolCall` accumulator touched ONLY inside the select-loop. No mutex / no shared state across goroutines. Race detector test (`go test -race`) is the gate. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. |

(Engine-level threats T-V5-01, T-V5-02, T-06-03, T-V8-01 are mitigated upstream in Slice 1's engine/coerce.go + engine/build_acp.go.)
</threat_model>

<verification>
After both tasks complete:
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./...` — clean build.
2. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go test ./internal/adapter/ollama/... -race -count=1` — all unit + golden + handler tests pass under race detector + testmain_test.go goleak gate.
3. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` — lint + race + govulncheck + tests clean.
4. `grep -n "engine.CoerceToolCall" internal/adapter/ollama/handlers.go` returns exactly one match.
5. `grep -n "ChunkKindToolCall" internal/adapter/ollama/ndjson.go` returns ≥2 matches (per-chunk + aggregator).
6. Wire-shape canary: a manual JSON-decode of `TestNDJSON_ToolCalls_DoneLine`'s final-line output shows `tool_calls[0].function.arguments` is `map[string]any`, not `string`.
</verification>

<success_criteria>
- TOOL-01 (Ollama side, per SC #2): non-streaming `/api/chat` with tools[] returns `message.tool_calls[].function.arguments` as plain JSON object.
- TOOL-02 (Ollama side, per SC #3): non-streaming `/api/chat` with model-emitted JSON-as-text invokes `engine.CoerceToolCall` and returns a synthetic tool_calls entry.
- TOOL-03 (Ollama side, per SC #4): Phase 2 forward-seam decoder is regression-tested (test will fail loudly if a future refactor breaks it).
- D-07 Ollama: streaming emits the final done:true NDJSON line carrying `message.tool_calls[]` atomically; no per-line `tool_calls` deltas.
- D-03 wire rendering: per-chunk `ChunkKindToolCall` produces an operator-visible `[tool: <name>]\n` thought-text NDJSON line (Node parity / LangFlow expectation).
- Race detector + goleak gate clean.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-02-SUMMARY.md` summarizing: wire-type fix (ollamaChatResponseMessage.ToolCalls), the four modified files, the two new/extended test files, and a confirmation that the wire-shape canary (plain-object arguments vs OpenAI's JSON-string) is locked.
</output>
