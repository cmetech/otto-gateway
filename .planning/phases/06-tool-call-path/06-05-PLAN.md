---
phase: 06-tool-call-path
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - tests/e2e/tools_fixtures.go
  - tests/e2e/tools_ollama_test.go
  - tests/e2e/tools_openai_test.go
  - tests/e2e/tools_anthropic_test.go
  - tests/e2e/tools_cancel_test.go
autonomous: false
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, e2e, cancel, fake-kiro, human-uat, byte-fidelity]

must_haves:
  truths:
    - "Full D-17 E2E matrix runs against the real otto-gateway binary with a controllable fake-kiro-cli (KIRO_CMD override): scenarios 1-4 + 6-11 for Ollama and OpenAI; scenarios 1, 2, 5, 9 for Anthropic; scenario 12 cross-surface mid-stream cancel"
    - "Cross-surface assertion: identical input (modulo model field) produces identical canonical.ChatResponse across all three surfaces — verified via tool_calls round-trip equivalence at the E2E response-body level"
    - "Scenario 5 (Anthropic NO-coerce) E2E-verifies the D-01 asymmetry: bare JSON text on /v1/messages preserved verbatim (no tool_use block synthesized)"
    - "Scenario 12 (mid-stream cancel during tool_call) verifies Phase 4 D-06 watchdog + Phase 5 slot survival + goleak.VerifyNone across all three surfaces"
    - "Node source byte-level fidelity check on coerceToolCall + pickBestTool (RESEARCH Assumption A1) is performed via a blocking human-verify checkpoint before phase sign-off"
    - "loop24-client messages.stream() against the live binary completes a tool_use round-trip (HUMAN-UAT per VALIDATION.md Manual-Only)"
  artifacts:
    - path: tests/e2e/tools_fixtures.go
      provides: "Shared 3-tool catalog (get_weather, read_file, search_web) + fake-kiro-cli script template + JSON-RPC notification builder helpers"
    - path: tests/e2e/tools_ollama_test.go
      provides: "TestE2E_Tools_Ollama covering scenarios 1-4, 6-11 (D-17 Ollama coverage)"
    - path: tests/e2e/tools_openai_test.go
      provides: "TestE2E_Tools_OpenAI covering scenarios 1-4, 6-11"
    - path: tests/e2e/tools_anthropic_test.go
      provides: "TestE2E_Tools_Anthropic covering scenarios 1, 2, 5, 9 (5 = NO-coerce verification)"
    - path: tests/e2e/tools_cancel_test.go
      provides: "TestE2E_Tools_Cancel covering scenario 12 per-surface with goleak.VerifyNone"
  key_links:
    - from: "tests/e2e/tools_*_test.go"
      to: "real otto-gateway binary + controllable fake-kiro-cli"
      via: "bootGateway(t, map[string]string{\"KIRO_CMD\": fakeKiroScript(t, notifs)}) — extraEnv overlay per e2e_test.go:158-160"
      pattern: "fakeKiroScript"
    - from: "tests/e2e/tools_cancel_test.go (scenario 12)"
      to: "Phase 4 D-06 watchdog + Phase 5 slot survival"
      via: "mid-stream client disconnect during tool_call notification"
      pattern: "Chat_DisconnectSmoke"
    - from: "checkpoint: Node source byte-fidelity"
      to: "internal/engine/coerce.go (Slice 1 Task 3)"
      via: "human-verify diff of coerceToolCall + pickBestTool against acp-ollama-server.js"
      pattern: "RESEARCH Assumption A1"
---

<objective>
Cross-surface integration slice for Phase 6. Operationalizes the full D-17 12-scenario E2E matrix against the live otto-gateway binary with a controllable fake-kiro-cli (via `KIRO_CMD` env override per `tests/e2e/e2e_test.go:158-160`). Closes the verification gaps from Slices 2/3/4 by exercising the canonical → engine → adapter → wire path end-to-end. Adds two blocking checkpoints required by VALIDATION.md Manual-Only Verifications + RESEARCH Assumption A1:
1. Node source byte-level fidelity check on `coerceToolCall` + `pickBestTool` (Assumption A1 — Node source is NOT in this checkout; CONTEXT line 471 "Node code wins on disagreement").
2. loop24-client `messages.stream()` HUMAN-UAT against the live binary (the conformance test for `@anthropic-ai/sdk` MessageStream).

Purpose: Slices 2/3/4 prove each surface in isolation via unit + golden tests. This slice proves all three surfaces produce wire-correct output under a real `kiro-cli`-shaped subprocess (the fake controllable variant), proves the cross-surface canonical equivalence (same canonical response from identical input), proves the no-coerce-on-Anthropic asymmetry at the E2E layer, and proves the mid-stream cancel path doesn't leak goroutines or kill slots. The two human-verify checkpoints close the SDK conformance + Node byte-fidelity loops that automated tests cannot fully cover.

Output: One non-test fixture file + four E2E test files (3 per-surface + 1 cancel). Plus two blocking checkpoint tasks (Node byte-fidelity + loop24-client UAT). The slice is NOT autonomous — `autonomous: false` because of the checkpoints. Run AFTER Wave 2 slices 2/3/4 have all merged and `make ci` is green on the unit/golden suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md
@.planning/phases/06-tool-call-path/06-02-SUMMARY.md
@.planning/phases/06-tool-call-path/06-03-SUMMARY.md
@.planning/phases/06-tool-call-path/06-04-SUMMARY.md

# E2E test infrastructure
@tests/e2e/e2e_test.go
@tests/e2e/ollama_e2e_test.go
@tests/e2e/openai_e2e_test.go
@tests/e2e/anthropic_e2e_test.go
@tests/e2e/pool_sessions_e2e_test.go

# Internal references for fixture design (in-package fake-server pattern as design inspiration)
@internal/acp/fakeacp_test.go

# Canonical references for cross-surface assertions
@internal/canonical/chat.go
@internal/canonical/chunk.go
@internal/engine/coerce.go

# CRITICAL — Node reference that the byte-fidelity checkpoint validates against
@docs/reference/acp_server_node_reference.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/e2e/tools_fixtures.go — shared 3-tool catalog + fake-kiro-cli script template + JSON-RPC notification builders</name>
  <files>tests/e2e/tools_fixtures.go</files>
  <read_first>
    - tests/e2e/e2e_test.go (FULL FILE — focus on bootGateway lines 140-180, gateOrSkip line 97, the extraEnv overlay seam at lines 158-160 which lets us swap KIRO_CMD per test; TestMain at line 57)
    - tests/e2e/anthropic_e2e_test.go (lines 7-13 the shared-helpers doc-comment — the canonical recipe for "this file is part of e2e_test package, reuses shared helpers, MUST NOT redefine them"; the package + build-tag pattern)
    - tests/e2e/ollama_e2e_test.go (lines 32-56 ollamaRequest helper, lines 309-385 Chat_DisconnectSmoke — the mid-stream-cancel analog for scenario 12)
    - internal/acp/fakeacp_test.go (lines 322-360 emitUpdate + variantToolCallWrapped — the in-package fake-server tool_call notification shape; this is the STRUCTURAL model for the fixture's notification builders; the JSON-RPC framing is the source of truth)
    - internal/acp/framer.go (line-cap settings — relevant for "is our fake kiro emitting frames that pass through the real framer")
    - internal/acp/translate.go (sessionUpdateBody field shapes — toolCallId, title, args — what the fake kiro must emit)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 + D-18 + D-19 — E2E test design; Claude's Discretion §test data — tool names get_weather / read_file / search_web)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_fixtures.go" section + Pattern E "E2E real-binary harness")
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Wave 0 Gaps — `tests/e2e/tools_fixtures.go` requirements)
  </read_first>
  <behavior>
    - **File header — build tag + package:** `//go:build e2e` first line, blank line, `package e2e_test`. NO `_test.go` suffix so multiple `_test.go` files share the symbols (RESEARCH line 608). Comment block explaining the file's role and the no-redefine constraint (similar to `anthropic_e2e_test.go:7-13`).
    - **Exported 3-tool catalog (Discretion §test data):**
      - `var ToolsCatalog = []ToolSpecJSON{...}` — three tools used identically across all per-surface tests for diff readability:
        1. `get_weather` — Description "Get the current weather for a location"; parameters `{type:"object", properties:{location:{type:"string"}, unit:{type:"string", enum:["celsius","fahrenheit"]}}, required:["location"]}`.
        2. `read_file` — Description "Read a file from the filesystem"; parameters `{type:"object", properties:{path:{type:"string"}}, required:["path"]}`.
        3. `search_web` — Description "Search the web for a query"; parameters `{type:"object", properties:{query:{type:"string"}, max_results:{type:"integer"}}, required:["query"]}`.
      - `type ToolSpecJSON struct { Name string; Description string; Parameters map[string]any }` — a JSON-marshalable shape, NOT importing canonical (tests/e2e/ is intentionally adapter-output-only and avoids internal/ deps where possible).
      - Per-surface request-body builders: `OllamaToolsRequest(prompt string, stream bool) []byte`, `OpenAIToolsRequest(prompt string, stream bool) []byte`, `AnthropicToolsRequest(prompt string, stream bool, maxTokens int) []byte` — each produces a valid JSON body with the 3-tool catalog embedded in the surface's native shape (OpenAI: `tools[].function.{name,description,parameters}`; Anthropic: `tools[].{name,description,input_schema}`; Ollama: `tools[].function.{name,description,parameters}`).
    - **Fake-kiro-cli script template (D-19):** `func FakeKiroScript(t *testing.T, notifications []byte) string` — writes a controllable script into `t.TempDir()` and returns its absolute path. The path is what tests pass as the `KIRO_CMD` env overlay.
      - **Implementation choice:** the simplest portable approach is to compile a tiny Go binary at test-init time (NOT a shell script — Windows-portable per CLAUDE.md cross-compile constraint). At `TestMain` time or first-use, compile `tests/e2e/cmd/fake-kiro-cli/main.go` (NEW file if not present) via `go build`; cache the resulting binary path under `t.TempDir()`. The compiled binary reads JSON-RPC frames from stdin, responds to `initialize` + `session/new` with canned responses, then on `session/prompt` emits the supplied `notifications` byte slice (which the test pre-built as concatenated JSON-RPC frames) followed by a `session/prompt` response with the supplied stop_reason. **Alternative:** if a `tests/e2e/cmd/fake-kiro-cli/main.go` already exists from a prior quick task, reuse it and just add a notification-injection seam (env var, stdin signal, or file path).
      - The fake binary must support the same framer line shape as real kiro-cli — it links against `internal/acp/framer.go` OR uses a hand-rolled framer matching the LF-terminated JSON-RPC discipline.
      - **Notification scripting API:** `notifications` is a `[]byte` of concatenated JSON-RPC notification frames. Helper builders in `tools_fixtures.go` produce them: `NotifText(sessionID, text string) []byte`, `NotifThought(sessionID, content string) []byte`, `NotifToolCall(sessionID, toolCallID, name string, args map[string]any) []byte`, `NotifPlan(sessionID string, entries []string) []byte`. Each returns a JSON-RPC frame mirroring the `internal/acp/fakeacp_test.go::emitUpdate variantToolCallWrapped` shape (lines 322-360) — `session/update` method, sessionId + update + sessionUpdate discriminator + body fields.
    - **Cross-surface response-equivalence helpers:** `func AssertSameCanonicalToolCall(t *testing.T, ollamaBody, openaiBody, anthropicBody []byte)` — decodes each response body into its native shape, extracts the tool_call name + args (with per-surface wire-shape awareness — Ollama object args, OpenAI JSON-string args, Anthropic input object), asserts equivalence on (name, args-as-map[string]any). The wire shape diverges (per Phase 6 D-07) but the canonical content is identical — this helper IS the canonical-equivalence proof.
    - **Goleak helper:** `func GoleakVerifyAtEnd(t *testing.T)` — wraps `goleak.VerifyNone(t)` with ignore-list for known background goroutines (the gateway binary's own goroutines AND any in-test cleanup). Reuse Phase 4/5 patterns if they exist in the e2e package.
    - **Documentation:** top-of-file comment explains:
      - The fixture is shared across 4 test files; do not redefine symbols.
      - The fake-kiro binary is compiled lazily via `sync.Once` to avoid per-test build cost.
      - The 3-tool catalog is the canonical reference — any new tool added here affects all 4 test files.
  </behavior>
  <action>
    Create `tests/e2e/tools_fixtures.go` with the `//go:build e2e` tag and `package e2e_test`. Implement:
      1. The exported `ToolsCatalog` 3-tool slice and the `ToolSpecJSON` type per the behavior block.
      2. The three per-surface request-body builders (`OllamaToolsRequest`, `OpenAIToolsRequest`, `AnthropicToolsRequest`) producing valid JSON bodies with the catalog embedded.
      3. The `FakeKiroScript(t, notifications) string` helper that compiles (or finds cached) a fake-kiro-cli binary and returns its absolute path.
      4. The four notification builders (`NotifText`, `NotifThought`, `NotifToolCall`, `NotifPlan`).
      5. The `AssertSameCanonicalToolCall` cross-surface equivalence helper.
      6. The `GoleakVerifyAtEnd` helper wrapping goleak.VerifyNone with a known-good ignore-list.

    Decide whether to create `tests/e2e/cmd/fake-kiro-cli/main.go` (a tiny Go binary that reads JSON-RPC stdin and emits scripted notifications) OR reuse an existing fake from a prior quick task. Default: create the new tiny binary. Compile it once per `TestMain` via `sync.Once` and cache the path in a package-level variable.

    The fake-kiro binary's behavior:
      - On stdin frame `initialize` → respond with `{result:{protocolVersion:1, agentCapabilities:{promptCapabilities:{...}}}}` matching the kiro-cli 2.4.1 shape (see Phase 1.1 SUMMARY for the exact shape used in tests).
      - On stdin frame `session/new` → respond with `{result:{sessionId:"e2e-session-1", models:{availableModels:[{id:"auto", name:"Auto"}]}}}`.
      - On stdin frame `session/prompt` → emit the pre-supplied `notifications` bytes verbatim to stdout, then emit `{result:{stopReason:"end_turn"}}` correlated to the prompt's id.
      - On `session/cancel` → respond `{result:{}}` and exit cleanly (scenario 12 path).
      - On EOF → exit cleanly.
      - Pre-supplied notifications: read from an env var like `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE` whose value is a path to a file written by `FakeKiroScript` containing the concatenated JSON-RPC frames.

    Per CLAUDE.md cross-compile constraint: the fake binary must be pure Go (no cgo). The build runs in TestMain via `go build` against a stable subdirectory — no cross-compile issues since tests always run on the host.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools" -count=1 -short 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/tools_fixtures.go` exists with the `//go:build e2e` tag and `package e2e_test`.
    - `grep -c '^//go:build e2e' tests/e2e/tools_fixtures.go` returns 1.
    - `grep -c 'package e2e_test' tests/e2e/tools_fixtures.go` returns 1.
    - `grep -n 'ToolsCatalog' tests/e2e/tools_fixtures.go` shows the exported 3-tool slice.
    - `grep -n 'FakeKiroScript' tests/e2e/tools_fixtures.go` shows the helper function.
    - `grep -n 'NotifToolCall' tests/e2e/tools_fixtures.go` shows the notification builder.
    - `tests/e2e/cmd/fake-kiro-cli/main.go` exists OR a prior fake-kiro is reused (document the decision in a tools_fixtures.go comment).
    - `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -count=1` compiles successfully (no symbol-collision errors with existing e2e_test.go).
    - The fake-kiro binary builds without errors via `go build ./tests/e2e/cmd/fake-kiro-cli/...`.
    - `go vet ./tests/e2e/...` clean.
  </acceptance_criteria>
  <done>
    tests/e2e/tools_fixtures.go provides the shared 3-tool catalog + fake-kiro-cli script template + JSON-RPC notification builders + cross-surface canonical-equivalence assertion helper; the fake-kiro binary compiles and supports the required scenarios (initialize / session/new / session/prompt with scripted notifications / session/cancel).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-surface E2E tests — Ollama (scenarios 1-4, 6-11), OpenAI (scenarios 1-4, 6-11), Anthropic (scenarios 1, 2, 5, 9)</name>
  <files>tests/e2e/tools_ollama_test.go, tests/e2e/tools_openai_test.go, tests/e2e/tools_anthropic_test.go</files>
  <read_first>
    - tests/e2e/tools_fixtures.go (Task 1 output — the symbols this task consumes)
    - tests/e2e/ollama_e2e_test.go (subtests pattern at lines 34-100; ollamaRequest at lines 32-56)
    - tests/e2e/openai_e2e_test.go (full file — subtests pattern for the OpenAI surface)
    - tests/e2e/anthropic_e2e_test.go (lines 34-67 streaming subtest pattern; postMessages helper; assertStrictSSE helper)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 — the 12-scenario matrix; D-19 — file split per surface)
    - .planning/phases/06-tool-call-path/06-VALIDATION.md (Per-Task Verification Map V-01 through V-20 — these E2E tests close V-01, V-02, V-03, V-04 (e2e half), V-05 (e2e half), V-06, V-14, V-15, V-16, V-17)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_{ollama,openai,anthropic}_test.go" section)
  </read_first>
  <behavior>
    - **tools_ollama_test.go — `TestE2E_Tools_Ollama` covering D-17 scenarios 1-4, 6-11:**
      Each scenario is a subtest. Common setup: `gateOrSkip(t)`, build notifications for the scenario, boot gateway with `KIRO_CMD=FakeKiroScript(t, notifications)`, POST request, assert response shape.
      1. **NativeToolCall_NonStreaming** (D-17 scenario 1): notifications = `NotifToolCall("e2e-session-1", "tc_001", "get_weather", {"location":"NYC"})`; POST `/api/chat` with `stream:false` + tools catalog. Assert response JSON: `message.tool_calls[0].function.name == "get_weather"`, `.function.arguments == {"location":"NYC"}` (plain object), no `content` text.
      2. **NativeToolCall_Streaming** (D-17 scenario 2): same notifications + `stream:true`; assert NDJSON output: intermediate line has `content:"[tool: get_weather]\n"`; final `done:true` line has `message.tool_calls[]` populated with object args (NOT JSON string).
      3. **Coerce_BareJSON** (D-17 scenario 3): notifications = `NotifText("e2e-session-1", "{\"location\":\"Boston\"}")`; tools catalog has `get_weather`; assert response has `message.tool_calls[0]` synthesized; `.function.arguments == {"location":"Boston"}` (object); `message.content == ""` (text replaced).
      4. **Coerce_FencedJSON** (D-17 scenario 4): notifications text = `"```json\n{\"location\":\"Boston\"}\n```"`; assert same outcome as scenario 3 — fence stripped.
      5. **EmptyTools_NoCoerce** (D-17 scenario 6): request body has NO `tools` field; notifications text = `"{\"location\":\"Boston\"}"`; assert response preserves text; `tool_calls` absent.
      6. **NoMatch_NoCoerce** (D-17 scenario 7): notifications text = `"{\"unrelated\":\"data\"}"`; tools catalog has `get_weather` (no overlap with `unrelated`); assert text preserved, no synthesis.
      7. **MalformedJSON_NoCoerce** (D-17 scenario 8): notifications text = `"{not valid"`; assert text preserved, no synthesis.
      8. **ExistingToolCalls_CoerceSkipped** (D-17 scenario 9): notifications = NotifToolCall + NotifText with JSON-shaped body; assert kiro-native tool_call preserved, coerce no-ops (single tool_call in response, NOT two).
      9. **MultiTool_TieBreaker** (D-17 scenario 10): tools catalog has TWO tools with overlapping property keys (e.g., `tool_a` with properties `{loc:string}` and `tool_b` with properties `{loc:string}`, declared in that order); notifications text = `"{\"loc\":\"NYC\"}"`; assert response has `tool_calls[0].function.name == "tool_a"` (first-declared wins per D-10).
      10. **EmptyParams_Skipped** (D-17 scenario 11): tools catalog has one tool with `parameters: {type:"object"}` (no properties) + another tool with `get_weather`'s normal schema; notifications text = `"{\"location\":\"NYC\"}"`; assert response uses `get_weather` (the empty-params tool is skipped in scoring per D-10).
    - **tools_openai_test.go — `TestE2E_Tools_OpenAI` covering scenarios 1-4, 6-11:**
      Same 10 subtests as Ollama but POST `/v1/chat/completions` with OpenAI request body shape. Wire-shape assertions differ:
        - `choices[0].message.tool_calls[0].function.name == "get_weather"`
        - `choices[0].message.tool_calls[0].function.arguments == "{\"location\":\"NYC\"}"` (JSON STRING, with escaped quotes — the canary)
        - On streaming (scenario 2): SSE frames assertion — frame 1 has `delta.tool_calls[0].id` + `.function.name` + empty arguments; frame 2 has `delta.tool_calls[0].function.arguments == "{\"location\":\"NYC\"}"`; terminal frame has `delta:{}`, `finish_reason:"tool_calls"`; final line `data: [DONE]`.
    - **tools_anthropic_test.go — `TestE2E_Tools_Anthropic` covering scenarios 1, 2, 5, 9:**
      Subtests:
      1. **NativeToolCall_NonStreaming** (scenario 1): notifications = NotifToolCall; POST `/v1/messages` with `stream:false`; assert `content[0].type == "tool_use"`, `.id == "tc_001"`, `.name == "get_weather"`, `.input == {"location":"NYC"}` (object, NOT null per CR-01). Assert `stop_reason == "tool_use"`.
      2. **NativeToolCall_Streaming** (scenario 2): notifications = NotifToolCall + stream:true; assert SSE event sequence per Slice 4 golden: `content_block_start{tool_use,input:{}}` → `content_block_delta{input_json_delta}` → `content_block_stop`; `message_delta` has `stop_reason:"tool_use"`.
      3. **NoCoerce_BareJSON** (scenario 5 — THE D-01 asymmetry verification): notifications = NotifText("{\"location\":\"NYC\"}"); request has tools catalog. Assert response `content[0].type == "text"`, `.text == "{\"location\":\"NYC\"}"` (verbatim preserved); assert NO `tool_use` content block exists in `content[]`. This is the E2E proof of D-01.
      4. **ExistingToolCalls_NoSecondaryProcessing** (scenario 9): notifications = NotifText("some thoughts") + NotifToolCall + NotifText("{\"location\":\"NYC\"}"). Assert kiro-native tool_use is preserved as ONE tool_use block; the bare-JSON text is NOT converted to a second tool_use (since Anthropic doesn't coerce); the JSON-text content is preserved verbatim alongside the tool_use.
    - **Cross-surface canonical equivalence test:** add `TestE2E_Tools_CrossSurface_CanonicalEquivalence` that runs the SAME notifications (scenario 1: NotifToolCall) against all three surfaces, decodes each response body, and calls `AssertSameCanonicalToolCall(t, ollamaBody, openaiBody, anthropicBody)` from the fixture file. Proves CONTEXT D-17 "Cross-surface assertions: All three surfaces produce the same canonical.ChatResponse for identical input."
    - **goleak gate (D-21):** wrap each `TestE2E_Tools_*` in `defer GoleakVerifyAtEnd(t)` per the fixture helper. Per Phase 4 / 5 discipline.
    - **Subtest naming convention:** all subtests use the names listed above (NativeToolCall_NonStreaming etc.) so a future failure in CI is greppable to the D-17 scenario number via comments.
  </behavior>
  <action>
    Create `tests/e2e/tools_ollama_test.go` with the `//go:build e2e` tag and `package e2e_test`. Implement `TestE2E_Tools_Ollama` with the 10 subtests per the behavior block. Per scenario: construct notifications via the fixture builders, boot the gateway with `bootGateway(t, map[string]string{"KIRO_CMD": FakeKiroScript(t, notifs)})`, POST the request via `ollamaRequest` (existing helper), assert the response shape.

    Create `tests/e2e/tools_openai_test.go` with `TestE2E_Tools_OpenAI` covering the same 10 subtests in OpenAI's native shape. POST to `/v1/chat/completions`. Wire-shape assertions reflect the OpenAI conventions: `choices[].message.tool_calls`, JSON-string arguments, multi-frame SSE on streaming.

    Create `tests/e2e/tools_anthropic_test.go` with `TestE2E_Tools_Anthropic` covering scenarios 1, 2, 5, 9. POST to `/v1/messages` with the Anthropic body shape. Scenario 5 is the LOAD-BEARING D-01 asymmetry verification — assert verbatim text preservation + NO tool_use synthesis.

    Add `TestE2E_Tools_CrossSurface_CanonicalEquivalence` in one of the three files (or as a standalone in `tools_cross_test.go` if cleaner). Runs the SAME notifications against all three surfaces and calls `AssertSameCanonicalToolCall` to prove canonical equivalence.

    Per VALIDATION.md sampling rate: this test pack runs under `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run TestE2E_Tools` and is the Phase 6 gate before sign-off.

    Per RESEARCH Pitfall 8 — kiro emits atomically: confirm during scenario 1 that exactly ONE tool_call notification produces exactly ONE tool_calls entry in the response (no spurious aggregation).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools_(Ollama|OpenAI|Anthropic|CrossSurface)" -count=1 -race -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist with the `//go:build e2e` tag and `package e2e_test`.
    - `TestE2E_Tools_Ollama` has subtests named: NativeToolCall_NonStreaming, NativeToolCall_Streaming, Coerce_BareJSON, Coerce_FencedJSON, EmptyTools_NoCoerce, NoMatch_NoCoerce, MalformedJSON_NoCoerce, ExistingToolCalls_CoerceSkipped, MultiTool_TieBreaker, EmptyParams_Skipped.
    - `TestE2E_Tools_OpenAI` has the same 10 subtest names.
    - `TestE2E_Tools_Anthropic` has subtests: NativeToolCall_NonStreaming, NativeToolCall_Streaming, NoCoerce_BareJSON, ExistingToolCalls_NoSecondaryProcessing.
    - `TestE2E_Tools_CrossSurface_CanonicalEquivalence` exists and uses `AssertSameCanonicalToolCall`.
    - `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run TestE2E_Tools -count=1` passes (all 24+ subtests + cross-surface).
    - Wire-shape divergence canary verified at E2E: Ollama args are objects, OpenAI args are JSON strings, Anthropic input is object — proven by the per-surface assertions.
    - Goroutine-leak gate clean (defer GoleakVerifyAtEnd in each TestE2E_Tools_*).
    - Anthropic scenario 5 (NoCoerce_BareJSON) is the load-bearing D-01 verification — its failure indicates a regression where Anthropic accidentally started calling CoerceToolCall.
    - `go vet ./tests/e2e/...` clean.
  </acceptance_criteria>
  <done>
    Full D-17 E2E matrix (24+ scenario subtests) passes against the live binary with controllable fake-kiro; cross-surface canonical equivalence proven; D-01 NO-coerce asymmetry verified at the E2E layer (scenario 5); no goroutine leaks; wire-shape divergence canary holds across all three surfaces.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tools_cancel_test.go — scenario 12 (mid-stream cancel during tool_call) per-surface with goleak</name>
  <files>tests/e2e/tools_cancel_test.go</files>
  <read_first>
    - tests/e2e/ollama_e2e_test.go (Chat_DisconnectSmoke at lines 309-385 — the proven mid-stream-cancel pattern this test replicates)
    - tests/e2e/tools_fixtures.go (Task 1 — NotifText / NotifToolCall + FakeKiroScript)
    - tests/e2e/pool_sessions_e2e_test.go (Phase 5 dead-slot survival test — verifies pool/health endpoint shows slot alive after a session cancellation)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 scenario 12 — mid-stream disconnect during tool_call)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Phase 5 dead-slot reference, Phase 4 D-06 watchdog)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_cancel_test.go" — mid-stream disconnect analog)
  </read_first>
  <behavior>
    - **`TestE2E_Tools_Cancel` per-surface subtests:**
      1. **Ollama_CancelDuringToolCall:**
         - Notifications = `NotifText("starting") + NotifToolCall("tc_cancel", "search_web", {query:"long running"}) + NotifText("more text after tool call")`. The text after tool_call gives the test time to disconnect before the stream completes.
         - Boot gateway with `KIRO_CMD=FakeKiroScript(t, notifications)`.
         - POST `/api/chat` with `stream:true` + tools catalog using `http.Client` with manual `Request.Body` access so the test can cancel mid-stream.
         - Use `context.WithCancel`; read a few NDJSON lines (enough to see the tool_call indicator); call `cancel()` mid-stream.
         - Assert: a `session/cancel` JSON-RPC frame was emitted (verified via the fake-kiro binary logging — add a flag to the fake that writes received frames to a file for test assertion).
         - Assert: pool slot survives — POST `/api/chat` again (with a simple text request, no tools, fresh stream) and verify it succeeds within reasonable time. Or POST `/health/agents` and verify slot count is unchanged.
         - Assert: `goleak.VerifyNone(t)` passes (no leaked goroutines from the canceled stream).
      2. **OpenAI_CancelDuringToolCall:** same shape, POST `/v1/chat/completions` with SSE streaming, cancel mid-stream, same assertions.
      3. **Anthropic_CancelDuringToolCall:** same shape, POST `/v1/messages` with `stream:true`, cancel mid-stream, same assertions.
    - **Fake-kiro instrumentation:** the fake-kiro binary needs a mode where it logs received frames to a file (path supplied via env var `OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE`). The test reads this file after `cancel()` to assert a `session/cancel` frame was sent. If Task 1 didn't add this seam, add it here (modify `tests/e2e/cmd/fake-kiro-cli/main.go` to log frames OR pass through an env-supplied filehandle).
    - **goleak discipline:** `goleak.VerifyNone(t)` MUST be called AFTER all client connections close, all gateway responses finalize, all stream watchdogs unwind. Use a short `time.Sleep(50 * time.Millisecond)` before the goleak check to allow async cleanup (Phase 4 D-06 watchdog stop, Phase 5 slot release).
    - **Slot survival assertion:** the cleanest proof is a fresh request against the same gateway boot — if the pool replaced the slot or the stream leaked into "busy" state, the fresh request would hang or fail. Use a short timeout (5s) on the fresh request — fast failure on regression.
    - **All three sub-tests share the same fixture pattern** but each uses its own request/assertion path. No shared mutable state between sub-tests; each spins up its own gateway boot via `bootGateway`.
  </behavior>
  <action>
    Create `tests/e2e/tools_cancel_test.go` with `//go:build e2e` tag + `package e2e_test`. Implement `TestE2E_Tools_Cancel` with the three per-surface sub-tests per the behavior block.

    Each sub-test:
      1. Build the notifications sequence using Task 1's fixture builders.
      2. Boot gateway with the fake-kiro and an env var pointing to a temp-file path for frame logging (`OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE=...`).
      3. POST the streaming request with `context.WithCancel`-derived request context.
      4. Read partial response lines (enough to confirm streaming started — typically 2-3 lines).
      5. Call `cancel()` on the context.
      6. Wait a short interval (50ms-200ms) for the gateway to propagate cancel and clean up.
      7. Read the frame-log file; assert `"method":"session/cancel"` substring present (or use a typed JSON parse on each frame).
      8. Make a fresh request to verify slot survival (simple text request with no tools); assert it completes within 5s.
      9. `defer GoleakVerifyAtEnd(t)` for the goroutine-leak gate (from Task 1).

    If Task 1's fake-kiro binary doesn't already log received frames, extend it in this task. Add an env-var-driven file-logging seam: `if path := os.Getenv("OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE"); path != "" { /* open file, log each frame */ }`. Document the change in the fake-kiro main.go header comment.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools_Cancel" -count=1 -race -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/tools_cancel_test.go` exists with `//go:build e2e` + `package e2e_test`.
    - `TestE2E_Tools_Cancel` has three sub-tests: `Ollama_CancelDuringToolCall`, `OpenAI_CancelDuringToolCall`, `Anthropic_CancelDuringToolCall`.
    - Each sub-test asserts the `session/cancel` frame was emitted (via the fake-kiro frame-log).
    - Each sub-test asserts pool slot survival (fresh request completes within 5s after cancel).
    - Each sub-test runs `goleak.VerifyNone(t)` (via `GoleakVerifyAtEnd`) and passes.
    - `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run TestE2E_Tools_Cancel -race -count=1` passes (all 3 sub-tests).
    - No goroutine leaks detected.
    - The fake-kiro binary's frame-logging seam is documented in its main.go header.
  </acceptance_criteria>
  <done>
    Scenario 12 mid-stream cancel verified across all three surfaces; session/cancel emission confirmed via frame-log; pool slot survives the cancel (Phase 5 dead-slot discipline); goroutine-leak gate clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): Node source byte-level fidelity check on coerceToolCall + pickBestTool (RESEARCH Assumption A1)</name>
  <what-built>
    Slice 1 implemented `internal/engine/coerce.go` per the D-09 9-step algorithm derived from `docs/reference/acp_server_node_reference.md` §"Load-bearing weirdness: coerceToolCall" (lines 166-195). The Node source itself (`acp-ollama-server.js`) is NOT in this checkout — the parent directory `gitlab.rosetta.ericssondevops.com/loop_24/acp_server` does not exist on this machine. RESEARCH §Assumption A1 flagged this as the ONE non-verified assumption: the algorithm is verified against the narrative reference but NOT against the Node source bytes. Per CONTEXT line 471: "Where Node's code and the narrative doc disagree, the Node code wins."

    This checkpoint validates that our Go implementation matches Node's behavior at the byte level.
  </what-built>
  <how-to-verify>
    Choose ONE of the following paths to verify byte-level fidelity:

    **Path A — clone the Node repo (preferred):**
    1. `git clone <node-repo-url> /tmp/acp_server` (the URL is in PROJECT.md / docs/briefs; ask the operator if unclear).
    2. Open `/tmp/acp_server/acp-ollama-server.js`.
    3. Locate the `coerceToolCall` function and the `pickBestTool` function.
    4. Open `internal/engine/coerce.go` in this repo.
    5. Diff the two implementations behaviorally:
       - Does the order-of-checks match D-09 steps 1-9? (Skip-condition guards; raw parse; fenced parse with ```json and bare ``` variants; pickBestTool top-level scoring; zero-score → no-op; rewrite content + append tool_call.)
       - Does `pickBestTool` use first-declared tie-break OR something different (e.g., total property count secondary)?
       - Does fence-stripping handle CRLF, leading/trailing whitespace, etc.?
       - Is the synthetic tool_call ID format different (e.g., `tool_<uuid>` vs `call_<unix-nano>`)?
       - Does either function have a behavior the other doesn't?
    6. If ALL behaviors match → respond "approved — byte fidelity confirmed".
    7. If ANY drift is found → describe the divergence in the response (which function, which step, what Node does vs what Go does); planner will create a follow-up plan to reconcile per CONTEXT line 471 "Node code wins."

    **Path B — paste the Node function bodies for cross-check:**
    1. Open `acp-ollama-server.js` in the Node repo (wherever it lives in your environment).
    2. Copy the bodies of `coerceToolCall` and `pickBestTool` (the full functions).
    3. Paste them into your response as code blocks.
    4. Claude (the next session resuming this checkpoint) will diff against `internal/engine/coerce.go` and flag any drift.

    **Path C — ship with ASSUMED-vs-Node-source note (LAST RESORT):**
    If the Node source is genuinely inaccessible (repo not available, permissions blocked, etc.), explicitly accept the risk:
    1. Confirm that LangChain integration smoke tests post-ship will catch any drift (if Pi-SDK / LangFlow real users hit the gateway with JSON-as-text, divergent behavior will surface as bug reports).
    2. Respond "approved — Path C accepted; post-ship LangChain integration smoke is the verification."
    3. Planner will add a follow-up `perf-baseline-vs-node.md`-style milestone-deferral todo to verify post-ship.

    Resume signal: "approved" (with which path) OR describe drift / blocker.
  </how-to-verify>
  <resume-signal>Type "approved (Path A/B/C)" or describe drift / blocker</resume-signal>
  <files>internal/engine/coerce.go (read-only diff target; no write)</files>
  <action>
    HUMAN-VERIFY CHECKPOINT — NOT an autonomous Claude action. The operator performs the byte-fidelity diff per the how-to-verify steps and types the resume signal. Claude (the next session) reads the response: on "approved", proceeds to phase sign-off; on described drift, drafts a reconciliation plan per CONTEXT line 471 "Node code wins."
  </action>
  <verify>
    <human-check>Operator confirms (Path A/B/C) per how-to-verify; resume signal received. No automated command — fidelity is a human judgment call.</human-check>
  </verify>
  <done>
    Operator has reconciled `internal/engine/coerce.go` against the Node source (or accepted the post-ship verification path), and has typed an approval response that names the path taken.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (CHECKPOINT): loop24-client messages.stream() HUMAN-UAT against the live binary (Anthropic SDK conformance)</name>
  <what-built>
    Slice 4 implemented Anthropic SSE streaming for tool_use blocks per D-07 Anthropic and the SDK-expected event sequence (content_block_start with input:{} + input_json_delta with full args + content_block_stop + message_delta with stop_reason:"tool_use"). The golden-fixture test (`TestSSE_Golden_ToolUse`) locks the byte-level shape, but per VALIDATION.md Manual-Only Verifications: SDK parser conformance requires the REAL `@anthropic-ai/sdk` MessageStream to consume the stream and surface a complete `tool_use` block to the application layer. Only loop24-client (which uses the SDK in production) can verify this end-to-end.
  </what-built>
  <how-to-verify>
    1. Start the gateway from this repo: `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go run ./cmd/otto-gateway` (or use the existing dev-run script `scripts/otto-gw.sh` if present per CLAUDE.md).
    2. Verify the gateway is listening: `curl -s http://localhost:11434/health | jq` (or whatever port is configured per `HTTP_ADDR` env).
    3. Switch to the loop24-client repo: `cd ../loop24-client` (or wherever it lives on your machine).
    4. Run the gateway against loop24-client's smoke test (if a Phase 3.1 smoke script exists, reuse it; otherwise use the ANTHROPIC_BASE_URL pattern from Phase 3.1 SUMMARY):
       ```
       ANTHROPIC_BASE_URL=http://localhost:11434 ANTHROPIC_API_KEY=e2e-token npm run smoke:tool-use
       ```
       OR a minimal Node script that imports `@anthropic-ai/sdk`, configures the base URL, and runs `client.messages.stream({model:"auto", max_tokens:256, messages:[{role:"user", content:"What's the weather in NYC?"}], tools: [{name:"get_weather", description:"Get weather", input_schema:{type:"object", properties:{location:{type:"string"}}, required:["location"]}}]})`.
    5. Observe the script output and verify:
       - The script does NOT crash with a JSON parse error during streaming (would indicate `input:null` regression — Pitfall 1).
       - The script does NOT crash with a block-index mismatch (would indicate Pitfall 7 regression).
       - The SDK emits `content_block_start` → `content_block_delta` → `content_block_stop` events for the tool_use block (visible via `stream.on('contentBlock', ...)` listener if scripted explicitly).
       - The final `Message` object (resolved from `stream.finalMessage()`) includes a complete `tool_use` content block with `input` as a JavaScript OBJECT (NOT null, NOT undefined, NOT a string), containing the expected key `location`.
       - The final `Message.stop_reason == "tool_use"` (NOT `"end_turn"`).
    6. If all checks pass → respond "approved — loop24-client conformance confirmed".
    7. If the SDK throws / surfaces an incomplete block / shows `stop_reason: "end_turn"` instead of `"tool_use"` → describe the symptom in the response; planner will route to gap-closure per the specific failure mode (Pitfall 1 — CR-01 regression; Pitfall 7 — block-index regression; finalize override missing).

    Resume signal: "approved" OR describe the failure mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe failure symptom</resume-signal>
  <files>cmd/otto-gateway/main.go (read-only — boot target); internal/adapter/anthropic/sse.go (read-only — wire shape under verification)</files>
  <action>
    HUMAN-VERIFY CHECKPOINT — NOT an autonomous Claude action. The operator starts the gateway, runs loop24-client `messages.stream()` with tools per how-to-verify, observes SDK behavior, and types the resume signal. Claude (the next session) reads the response: on "approved", proceeds to phase sign-off; on described failure, routes to gap-closure per the specific failure mode (Pitfall 1 — CR-01 regression; Pitfall 7 — block-index regression; finalize override missing).
  </action>
  <verify>
    <human-check>Operator runs the SDK smoke test against the live gateway and confirms a complete tool_use block surfaces with object input + stop_reason:"tool_use"; resume signal received. No automated command — only the real `@anthropic-ai/sdk` parser can vote.</human-check>
  </verify>
  <done>
    Operator has run loop24-client (or equivalent `@anthropic-ai/sdk` script) against the live binary, observed a successful tool_use round-trip with object input + stop_reason:"tool_use", and typed an approval response.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake-kiro binary stdin/stdout | Test-controlled binary; runs in a `t.TempDir()`-isolated process. Notification content is test-supplied — not a real attack surface but the binary must not panic on malformed scripted notifications. |
| E2E HTTP requests → gateway | Real HTTP traffic against the live gateway; tests authenticate via `AUTH_TOKEN=e2e-token` (existing test convention). Tests do NOT bypass auth. |
| Frame-log file from fake-kiro | Test-process-owned file in `t.TempDir()`; no security boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | DoS (test harness flake) | E2E test goroutine leaks | mitigate | `GoleakVerifyAtEnd(t)` per test, with a 50ms async-cleanup grace period. Per Phase 4/5 discipline. |
| T-06-12 | Spoofing (regression — surfaces start emitting wrong shape) | Cross-surface canonical equivalence | mitigate | `TestE2E_Tools_CrossSurface_CanonicalEquivalence` asserts the canonical content is identical across all three surfaces. A regression where one surface starts emitting different tool_call arguments would fail loudly. |
| T-06-13 | Tampering (intentional asymmetry erosion) | TestE2E_Tools_Anthropic/NoCoerce_BareJSON | mitigate | E2E test for D-01 asymmetry — bare JSON text on /v1/messages preserved verbatim. Combined with Slice 4 Task 3's source-level static assertion, the asymmetry has defense-in-depth. |
| T-06-A1 (RESEARCH Assumption A1) | Tampering (algorithm drift from Node ground truth) | engine.CoerceToolCall byte-level behavior | mitigate via human-verify checkpoint | Task 4 — blocking checkpoint validates against Node source. Cannot be auto-approved; `workflow.auto_advance` ignored on legitimacy checkpoints per planner doctrine. |
| T-06-14 | Spoofing (Anthropic SDK parser regression) | loop24-client conformance | mitigate via human-verify checkpoint | Task 5 — blocking checkpoint with the real `@anthropic-ai/sdk` MessageStream. Golden fixture (Slice 4) is necessary-but-not-sufficient; real SDK consumption is the conformance vote. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. Fake-kiro is pure-Go stdlib. |
</threat_model>

<verification>
After all 5 tasks complete (3 automated + 2 checkpoint approvals):
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools" -race -count=1 -timeout 10m` — all E2E tests green (24+ per-surface subtests + cancel subtests + cross-surface).
2. Node byte-fidelity checkpoint resolved (approved Path A/B/C).
3. loop24-client HUMAN-UAT checkpoint resolved (approved).
4. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` clean (lint + race + govulncheck + unit + golden).
5. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -timeout 10m` — full Phase 6 sign-off gate green.
6. Update `.planning/phases/06-tool-call-path/06-VALIDATION.md` to set `nyquist_compliant: true` in frontmatter, set Approval: `approved`, and check off all Validation Sign-Off boxes.
</verification>

<success_criteria>
- D-17 12-scenario E2E matrix fully exercised against the live otto-gateway binary with controllable fake-kiro-cli.
- Cross-surface canonical equivalence proven (identical input → identical canonical response across Ollama / OpenAI / Anthropic).
- D-01 NO-coerce asymmetry verified at the E2E layer (Anthropic scenario 5 — bare JSON preserved verbatim).
- Scenario 12 mid-stream cancel proven across all three surfaces with session/cancel emission + slot survival + no goleak.
- Node source byte-level fidelity check on coerceToolCall + pickBestTool resolved (Path A/B/C approved).
- loop24-client `messages.stream()` HUMAN-UAT against the live binary completes successfully — SDK surfaces a complete tool_use block with object input and stop_reason:"tool_use".
- `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -race` clean.
- `make ci` clean.
- Phase 6 VALIDATION.md `nyquist_compliant` set to `true` after this slice completes.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-05-SUMMARY.md` summarizing:
- The fake-kiro fixture infrastructure (path, scenarios supported).
- The D-17 E2E matrix scenario-pass-count.
- The cross-surface canonical equivalence assertion (which scenarios it covers).
- Resolution of Node byte-fidelity checkpoint (Path taken, any drift found).
- Resolution of loop24-client HUMAN-UAT (PASS / FAIL + symptoms if FAIL).
- Total Phase 6 sign-off — all SC1..SC5 from ROADMAP §"Phase 6: Tool-Call Path" verified.
- VALIDATION.md `nyquist_compliant: true` confirmed.
</output>
