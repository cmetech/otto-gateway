---
phase: 06-tool-call-path
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - tests/e2e/tools_fixtures.go
  - tests/e2e/tools_ollama_test.go
  - tests/e2e/tools_openai_test.go
  - tests/e2e/tools_anthropic_test.go
  - tests/e2e/tools_cancel_test.go
  - tests/e2e/cmd/fake-kiro-cli/main.go
  - tests/e2e/tools_testmain_test.go
autonomous: false
requirements: [TOOL-01, TOOL-02, TOOL-03]
requirements_addressed: [TOOL-01, TOOL-02, TOOL-03]
tags: [tool-call, e2e, cancel, fake-kiro, human-uat]

must_haves:
  truths:
    - "Full D-17 E2E matrix runs against the real otto-gateway binary with a controllable fake-kiro-cli: scenarios 1-4 + 6-11 for Ollama and OpenAI; scenarios 1, 2, 5, 9 for Anthropic; scenario 12 cross-surface mid-stream cancel"
    - "fake-kiro-cli binary handles ALL ACP methods the gateway issues: initialize, session/new, session/set_model, session/prompt, session/cancel, ping, plus EOF cleanup (REVIEW HIGH #5)"
    - "FakeKiro fixture returns BOTH a command path AND an env overlay so tests can wire notifications file paths cleanly (REVIEW HIGH #5 — API change from FakeKiroScript(t,notifs) string to FakeKiro(t,script) (cmd string, env map[string]string))"
    - "Fake-kiro-cli binary is compiled ONCE at package init via TestMain and stored at a package-level path that survives subtest t.TempDir() lifetimes (iteration-3 fix to MEDIUM #6 — package-level lifetime, not per-test temp dir)"
    - "Cross-surface assertion: identical input (modulo model field) produces identical canonical.ChatResponse across all three surfaces — verified via tool_calls round-trip equivalence at the E2E response-body level"
    - "Scenario 5 (Anthropic NO-coerce) E2E-verifies the D-01 asymmetry: bare JSON text on /v1/messages preserved verbatim (no tool_use block synthesized)"
    - "Scenario 12 (mid-stream cancel during tool_call) verifies Phase 4 D-06 watchdog + Phase 5 slot survival + goleak.VerifyNone across all three surfaces (per-subtest, per D-21)"
    - "loop24-client messages.stream() against the live binary completes a tool_use round-trip (HUMAN-UAT per VALIDATION.md Manual-Only)"
  artifacts:
    - path: tests/e2e/cmd/fake-kiro-cli/main.go
      provides: "Compiled fake-kiro binary supporting initialize, session/new, session/set_model, session/prompt, session/cancel, ping, EOF cleanup (REVIEW HIGH #5)"
    - path: tests/e2e/tools_testmain_test.go
      provides: "TestMain compiles fake-kiro-cli into os.TempDir()/fake-kiro-cli-<pid> at package init (iteration-3 fix to MEDIUM #6 — package-level lifetime, defer-cleanup on m.Run() exit)"
    - path: tests/e2e/tools_fixtures.go
      provides: "Shared 3-tool catalog (get_weather, read_file, search_web) + FakeKiro builder reading package-level binary path + JSON-RPC notification helpers + cross-surface canonical equivalence assertion + frame-log helper + GoleakVerifyAtEnd with documented ignore-list"
    - path: tests/e2e/tools_ollama_test.go
      provides: "TestE2E_Tools_Ollama covering scenarios 1-4, 6-11 with per-subtest goleak gating"
    - path: tests/e2e/tools_openai_test.go
      provides: "TestE2E_Tools_OpenAI covering scenarios 1-4, 6-11 with per-subtest goleak gating"
    - path: tests/e2e/tools_anthropic_test.go
      provides: "TestE2E_Tools_Anthropic covering scenarios 1, 2, 5, 9 (5 = NO-coerce verification) with per-subtest goleak gating"
    - path: tests/e2e/tools_cancel_test.go
      provides: "TestE2E_Tools_Cancel covering scenario 12 per-surface with per-subtest goleak.VerifyNone"
  key_links:
    - from: "tests/e2e/tools_*_test.go"
      to: "real otto-gateway binary + controllable fake-kiro-cli (compiled once at package init)"
      via: "cmd, env := FakeKiro(t, script); bootGateway(t, mergeEnv(env, map[string]string{\"KIRO_CMD\": cmd}))"
      pattern: "FakeKiro\\(t,"
    - from: "tests/e2e/tools_testmain_test.go::TestMain"
      to: "tests/e2e/tools_fixtures.go::fakeKiroBinaryPath (package-level var)"
      via: "TestMain compiles into os.TempDir()/fake-kiro-cli-<pid>, sets package-level var, defer-deletes on m.Run() exit"
      pattern: "fakeKiroBinaryPath"
    - from: "tests/e2e/tools_cancel_test.go (scenario 12)"
      to: "Phase 4 D-06 watchdog + Phase 5 slot survival"
      via: "mid-stream client disconnect during tool_call notification + frame-log assertion of session/cancel emission"
      pattern: "session/cancel"
---

> **Revised:** 2026-05-26 — incorporates 06-REVIEWS.md feedback (concerns: HIGH #5 fake-kiro API + ACP-method coverage; LOW #10 fake-kiro main.go listed in files_modified). Task 4 (Node byte-fidelity checkpoint) MOVED to 06-01 per REVIEW HIGH #3 — Wave 2 must not build on an unvalidated coerce algorithm. This plan keeps the loop24-client UAT checkpoint as the final task. Plan was NOT split per the reviewer's optional suggestion — the four task boundary (fixtures/matrix/cancel/UAT) is internally cohesive and splitting adds wave-dependency complexity without proportionate benefit. Plan task count drops from 5 to 4 (the Node fidelity checkpoint moved out).
>
> **Revised (iteration 2):** 2026-05-26 — addresses plan-checker BLOCKER + WARNINGS #2-5. WARNING #4: Task 1's `<automated>` block split into two non-truncated commands (`go build ./tests/e2e/cmd/fake-kiro-cli/...` + `go vet ./tests/e2e/...`) — removed `-short` (incorrect for the e2e suite), removed `head -30` truncation; acceptance criterion changed from "compiles successfully (no symbol-collision errors)" to "binary compiles + package vet-clean." WARNING #5: Task 2's `defer GoleakVerifyAtEnd(t)` moved from the parent-test body into each `t.Run` subtest so a leak in subtest N is not attributed to subtest N+1 (matches Task 3's pattern, per CONTEXT D-21). A `// goleak ignore-list: ...` comment now lives in the `GoleakVerifyAtEnd` helper in `tools_fixtures.go` documenting the gateway child-process and HTTP idle-conn allowances.
>
> **Revised (iteration 3):** 2026-05-26 — fixes iteration-2 MEDIUM #6 (fake-kiro sync.Once + t.TempDir lifetime bug) and MEDIUM #7 (go vet missing -tags e2e). Adds `tests/e2e/tools_testmain_test.go` with `TestMain` that compiles fake-kiro-cli into `os.TempDir()/fake-kiro-cli-<pid>` at package init, stores the path in a package-level `fakeKiroBinaryPath` var in `tools_fixtures.go`, and defer-deletes on `m.Run()` exit. `FakeKiro` reads the package-level path (no more sync.Once + t.TempDir which got cleaned up after the first test). Task 1's verify command updated to `go vet -tags e2e ./tests/e2e/...`. Smoke test added: `TestFakeKiro_BinaryExistsAfterMultipleSubtests` proves the binary path is valid across two sequential t.Run subtests.

<objective>
Cross-surface integration slice for Phase 6. Operationalizes the full D-17 12-scenario E2E matrix against the live otto-gateway binary with a controllable fake-kiro-cli. Closes the verification gaps from Slices 2/3/4 by exercising the canonical → engine → adapter → wire path end-to-end. Adds one blocking checkpoint required by VALIDATION.md Manual-Only Verifications: loop24-client `messages.stream()` HUMAN-UAT against the live binary (the conformance test for `@anthropic-ai/sdk` MessageStream).

**Note on the moved Node byte-fidelity checkpoint:** Per REVIEW HIGH #3 the Node byte-fidelity check on `coerceToolCall` + `pickBestTool` was moved to the end of Slice 06-01 (Wave 1) so it gates Wave 2 fan-out. The checkpoint is no longer in this slice.

**Key API change (REVIEW HIGH #5):** the fake-kiro fixture API is `FakeKiro(t *testing.T, script Script) (cmd string, env map[string]string)` (returning BOTH a command path AND an env overlay). The env overlay is the way notifications and frame-logging are wired into the binary without inventing wrapper scripts. Tests merge the returned env into their `bootGateway` extraEnv call.

**Fake-kiro ACP-method coverage (REVIEW HIGH #5):** the fake binary handles every ACP method the gateway issues during a normal request: `initialize`, `session/new`, `session/set_model`, `session/prompt`, `session/cancel`, `ping`, plus EOF cleanup.

**Iteration-3 fix to MEDIUM #6 — fake-kiro binary lifetime (TestMain-managed package-level path):** the previous plan compiled the fake binary via `sync.Once` into a `t.TempDir()` from the first test that called `FakeKiro`. That first test's temp dir gets cleaned up after the test completes — leaving subsequent tests with a cached path to a deleted binary. The fix: compile the binary at package init in `TestMain` into `os.TempDir()/fake-kiro-cli-<pid>` (managed at the package level, NOT per-test), store the absolute path in a package-level `fakeKiroBinaryPath` var, and defer-delete on `m.Run()` exit. `FakeKiro` reads this package-level var. A smoke test `TestFakeKiro_BinaryExistsAfterMultipleSubtests` proves the path is valid across two sequential `t.Run` subtests.

**Iteration-3 fix to MEDIUM #7 — go vet `-tags e2e`:** Task 1's verify command changes from `go vet ./tests/e2e/...` to `go vet -tags e2e ./tests/e2e/...` so the e2e-only files (which have `//go:build e2e`) are actually inspected.

**Per-subtest goleak gating (per CONTEXT D-21, WARNING #5):** `defer GoleakVerifyAtEnd(t)` lives inside each `t.Run("...", func(t *testing.T) { ... })` block, NOT at the parent test level. A parent-level defer would attribute a subtest-N leak to subtest N+1 (or worse, swallow it). The helper carries an inline `// goleak ignore-list:` comment documenting the gateway child-process goroutine and HTTP idle-conn allowances.

Purpose: Slices 2/3/4 prove each surface in isolation via unit + golden tests. This slice proves all three surfaces produce wire-correct output under a real `kiro-cli`-shaped subprocess, proves the cross-surface canonical equivalence, proves the no-coerce-on-Anthropic asymmetry at the E2E layer, and proves the mid-stream cancel path doesn't leak goroutines or kill slots. The HUMAN-UAT checkpoint closes the SDK conformance loop that automated tests cannot fully cover.

Output: One new fake-kiro binary source file + one TestMain file + one fixture file + four E2E test files (3 per-surface + 1 cancel) + one blocking checkpoint task (loop24-client UAT). Plan is NOT autonomous — `autonomous: false` because of the checkpoint. Run AFTER Wave 2 slices 2/3/4 have all merged and `make ci` is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tool-call-path/06-CONTEXT.md
@.planning/phases/06-tool-call-path/06-RESEARCH.md
@.planning/phases/06-tool-call-path/06-PATTERNS.md
@.planning/phases/06-tool-call-path/06-VALIDATION.md
@.planning/phases/06-tool-call-path/06-REVIEWS.md
@.planning/phases/06-tool-call-path/06-01-SUMMARY.md
@.planning/phases/06-tool-call-path/06-02-SUMMARY.md
@.planning/phases/06-tool-call-path/06-03-SUMMARY.md
@.planning/phases/06-tool-call-path/06-04-SUMMARY.md

# E2E test infrastructure
@tests/e2e/e2e_test.go
@tests/e2e/ollama_e2e_test.go
@tests/e2e/openai_e2e_test.go
@tests/e2e/anthropic_e2e_test.go
@tests/e2e/pool_sessions_e2e_test.go

# Internal references for fixture design (in-package fake-server pattern as design inspiration)
@internal/acp/fakeacp_test.go
@internal/acp/framer.go

# Canonical references for cross-surface assertions
@internal/canonical/chat.go
@internal/canonical/chunk.go
@internal/engine/coerce.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/e2e/cmd/fake-kiro-cli/main.go (NEW BINARY) + tests/e2e/tools_testmain_test.go (NEW — TestMain compiles binary at package init, iteration-3 fix to MEDIUM #6) + tests/e2e/tools_fixtures.go (shared catalog + builder helpers + smoke self-test for binary lifetime)</name>
  <files>tests/e2e/cmd/fake-kiro-cli/main.go, tests/e2e/tools_testmain_test.go, tests/e2e/tools_fixtures.go</files>
  <read_first>
    - tests/e2e/e2e_test.go (FULL FILE — focus on bootGateway lines 140-180, gateOrSkip line 97, the extraEnv overlay seam at lines 158-160 which lets us swap KIRO_CMD per test; TestMain at line 57 — confirm whether there's already a TestMain in the package; if so the new TestMain must coexist or extend rather than collide)
    - tests/e2e/anthropic_e2e_test.go (lines 7-13 the shared-helpers doc-comment — the canonical recipe for "this file is part of e2e_test package, reuses shared helpers, MUST NOT redefine them"; the package + build-tag pattern)
    - tests/e2e/ollama_e2e_test.go (lines 32-56 ollamaRequest helper, lines 309-385 Chat_DisconnectSmoke — the mid-stream-cancel analog for scenario 12)
    - internal/acp/fakeacp_test.go (FULL FILE — the in-package fake-server pattern is the structural model for the fake-kiro binary; understand which ACP methods it responds to and the response shapes; the variantToolCallWrapped + emitUpdate pattern is the model for notification building)
    - internal/acp/framer.go (line-cap settings — relevant for "is our fake kiro emitting frames that pass through the real framer"; the LF-terminated JSON-RPC framing discipline)
    - internal/acp/translate.go (sessionUpdateBody field shapes — toolCallId, title, args — what the fake kiro must emit)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 + D-18 + D-19 — E2E test design; Claude's Discretion §test data; D-21 — per-subtest goleak gating)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_fixtures.go" section + Pattern E "E2E real-binary harness")
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Wave 0 Gaps)
    - .planning/phases/06-tool-call-path/06-REVIEWS.md (HIGH #5 — FakeKiro returns (cmd, env); fake supports initialize/session/new/session/set_model/session/prompt/session/cancel/ping + EOF; LOW #10 — fake-kiro-cli/main.go listed in files_modified; iteration-2 MEDIUM #6 — fake-kiro sync.Once + t.TempDir lifetime bug; iteration-2 MEDIUM #7 — go vet missing -tags e2e)
  </read_first>
  <behavior>
    - **`tests/e2e/cmd/fake-kiro-cli/main.go` — NEW binary (REVIEW HIGH #5 + LOW #10):**
      - `package main`. Pure Go, no cgo (per CLAUDE.md cross-compile constraint).
      - Behavior: reads JSON-RPC frames from stdin (LF-terminated lines), responds via stdout (LF-terminated lines), exits on EOF.
      - **ACP methods supported (REVIEW HIGH #5 — full coverage):**
        1. `initialize` (id correlated, params with protocolVersion/clientInfo/clientCapabilities): respond with `{result:{protocolVersion:1, agentCapabilities:{promptCapabilities:{image:true,audio:false,embeddedContext:true}}}}` matching the kiro-cli 2.4.1 shape used in Phase 1.1.
        2. `session/new` (params include `mcpServers:[]`): respond with `{result:{sessionId:"e2e-session-1", models:{availableModels:[{id:"auto", name:"Auto"}, {id:"sonnet", name:"Sonnet"}]}}}`.
        3. `session/set_model` (params include sessionId + modelId): respond with `{result:{}}` (no-op success). Gateway calls this when client requests a specific model.
        4. `session/prompt` (params include sessionId + prompt blocks): emit the pre-supplied `notifications` bytes verbatim to stdout (each notification is a complete JSON-RPC frame already), THEN respond to the prompt's id with `{result:{stopReason:"end_turn"}}` (or whatever stop-reason the test scripted via env var). The notification bytes come from the test via env var `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE` (a path to a file the test wrote containing concatenated JSON-RPC notification frames).
        5. `session/cancel` (params include sessionId): respond with `{result:{}}`. Optionally log the cancel frame to `OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE` if that env var is set (used by scenario 12 cancel tests).
        6. `ping` (Phase 1 D-05 heartbeat): respond with `{result:{}}` (acknowledged). Without this, the gateway's heartbeat would fail and the slot would be marked dead.
        7. EOF / stdin close: gracefully exit with code 0. No panic.
      - **Per `session/request_permission` REQUEST (kiro → gateway path):** the fake-kiro binary does NOT send these unsolicited unless a notification frame in the script triggers it. The auto-grant is Phase 1 gateway-side logic — the fake just needs to optionally emit `session/request_permission` frames when a script wants to test the path. For Phase 6 E2E we do not test this; the fake's notification stream skips it.
      - **Frame-logging seam (for scenario 12):** if env var `OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE` is set to a file path, the fake opens that file for append and writes each received JSON-RPC frame as a single line. This is how the cancel test asserts that the gateway emitted `session/cancel`. Use `os.OpenFile(..., os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0644)` + ensure close on EOF.
      - **Top-of-file doc comment:** `// fake-kiro-cli is a controllable substitute for kiro-cli used by tests/e2e/.
        // It speaks the same JSON-RPC dialect (initialize, session/new, session/set_model,
        // session/prompt, session/cancel, ping) but emits pre-scripted notifications during
        // session/prompt instead of actually invoking an LLM. The notification script is
        // supplied via env var OTTO_FAKE_KIRO_NOTIFICATIONS_FILE. Received frames are
        // optionally logged to OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE for cancel-path tests.
        // Per CLAUDE.md: pure-Go, no cgo, cross-compile clean.
        // The binary is compiled by tests/e2e/tools_testmain_test.go's TestMain at package
        // init (iteration-3 fix to MEDIUM #6 — package-level lifetime, NOT per-test temp dir).`
    - **`tests/e2e/tools_testmain_test.go` — NEW (iteration-3 fix to MEDIUM #6):**
      - `//go:build e2e` first line + `package e2e_test`.
      - Implements `func TestMain(m *testing.M)`. If the e2e package already has a TestMain in `e2e_test.go`, this new file's TestMain CONFLICTS with it. In that case, EXTEND the existing TestMain by adding a separate compile-helper function `compileFakeKiroOnce()` called from the existing TestMain BEFORE `m.Run()`. Document which path was taken in the file's top comment.
      - Logic:
        1. Resolve a binary path: `binaryPath := filepath.Join(os.TempDir(), fmt.Sprintf("fake-kiro-cli-%d", os.Getpid()))`. Using `os.Getpid()` makes the path unique per `go test` invocation, so parallel test invocations don't clobber each other.
        2. Compile via `exec.Command("go", "build", "-o", binaryPath, "./tests/e2e/cmd/fake-kiro-cli")`. Set the working directory to the repo root (resolve via `runtime.Caller` or known relative path). Run; on error, `os.Exit(2)` with the build output.
        3. Set the package-level var `fakeKiroBinaryPath = binaryPath` (declared in `tools_fixtures.go`).
        4. `code := m.Run()`.
        5. Defer-delete `os.Remove(binaryPath)` on exit (use a `defer` BEFORE `m.Run` so it fires even on panic).
        6. `os.Exit(code)`.
      - **Lifetime contract** (documented in the file): "The compiled fake-kiro-cli binary is package-scoped and survives the entire `go test -tags e2e ./tests/e2e/...` run. It is created at TestMain entry and deleted at TestMain exit. Tests that call `FakeKiro` get the absolute path via the package-level `fakeKiroBinaryPath` var — NOT via `sync.Once` into `t.TempDir()` (the iteration-2 bug per MEDIUM #6: the first test's temp dir gets cleaned up after that test, leaving later tests with a path to a deleted binary)."
      - **Smoke self-test in tools_fixtures.go or this file (iteration-3 MEDIUM #6 verification):** add `TestFakeKiro_BinaryExistsAfterMultipleSubtests`:
        1. Call `FakeKiro(t, Script{})` in subtest "first".
        2. Assert the returned `cmd` path exists via `os.Stat`.
        3. Call `FakeKiro(t, Script{})` in subtest "second".
        4. Assert the returned `cmd` path STILL exists via `os.Stat`.
        5. The first subtest's cleanup happens BEFORE the second runs (t.Run subtests run sequentially by default), so if the binary lifetime were tied to per-subtest t.TempDir, the second subtest's path would no longer exist. The test passes ONLY if the binary lifetime is package-scoped.
    - **`tests/e2e/tools_fixtures.go`:**
      - `//go:build e2e` first line + `package e2e_test`.
      - **Package-level var (iteration-3 fix to MEDIUM #6):** `var fakeKiroBinaryPath string` (set by TestMain in tools_testmain_test.go). Doc comment: "Set at package init by tools_testmain_test.go's TestMain. Survives all subtests. DO NOT reassign from within tests."
      - **`FakeKiro` function (REVIEW HIGH #5 — API change; iteration-3 reads package-level path):**
        ```go
        type Script struct {
            Notifications []byte  // pre-built JSON-RPC notification frames (concatenated)
            StopReason    string  // "end_turn", "tool_use", or "" (defaults to "end_turn")
            LogFrames     bool    // if true, fake logs received frames for assertion
        }

        func FakeKiro(t *testing.T, script Script) (cmd string, env map[string]string) {
            t.Helper()
            if fakeKiroBinaryPath == "" {
                t.Fatal("fakeKiroBinaryPath not initialized — TestMain in tools_testmain_test.go must run first")
            }
            // 1. Write script.Notifications to a temp file under t.TempDir() (per-test cleanup).
            //    Per-test temp dir is FINE for the notifications file — it lives only as long
            //    as the test that wrote it. The BINARY is what needs package-level lifetime
            //    (iteration-3 fix to MEDIUM #6).
            // 2. Build env overlay:
            //    OTTO_FAKE_KIRO_NOTIFICATIONS_FILE=<temp-file-path under t.TempDir()>
            //    OTTO_FAKE_KIRO_STOP_REASON=<script.StopReason or "end_turn">
            //    OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE=<another path under t.TempDir() if LogFrames>
            // 3. Return (fakeKiroBinaryPath, env).
        }
        ```
        Per REVIEW HIGH #5 the (cmd, env) return shape is what lets tests cleanly merge with `bootGateway`'s `extraEnv`. Per iteration-3 MEDIUM #6, the binary path is package-level (always valid for the duration of the test process); only the notification + frame-log files use `t.TempDir()` (their lifetimes are tied to the calling test, which is correct).
      - **Exported 3-tool catalog:**
        - `var ToolsCatalog = []ToolSpecJSON{...}` (get_weather, read_file, search_web).
        - `type ToolSpecJSON struct { Name string; Description string; Parameters map[string]any }`.
      - **Per-surface request-body builders:** `OllamaToolsRequest`, `OpenAIToolsRequest`, `AnthropicToolsRequest` — produce valid JSON bodies in each surface's tools shape.
      - **Notification builders:** `NotifText(sessionID, text string) []byte`, `NotifThought(sessionID, content string) []byte`, `NotifToolCall(sessionID, toolCallID, name string, args map[string]any) []byte`, `NotifPlan(sessionID string, entries []string) []byte`. Each returns a complete JSON-RPC notification frame.
      - **Cross-surface equivalence helper:** `AssertSameCanonicalToolCall(t, ollamaBody, openaiBody, anthropicBody []byte)` — decodes each, extracts (name, args-as-map), asserts equivalence.
      - **goleak helper with documented ignore-list (WARNING #5):** `GoleakVerifyAtEnd(t)` wrapping `goleak.VerifyNone` with a known-good ignore-list. The function body MUST include a `// goleak ignore-list: ...` comment block describing which residual goroutines are allowed: (a) gateway child-process residue from `os/exec`-managed kiro subprocesses (the `Wait` goroutine; the stderr-relay goroutine), (b) HTTP transport idle-conn maintenance from the test client's `http.Transport`. The comment lives in `tools_fixtures.go` alongside the helper rather than expanding the plan further. Per CONTEXT D-21 this helper is invoked PER-SUBTEST by callers (not at the parent-test level).
      - **Frame-log read helper for scenario 12:** `func ReadFakeKiroFrames(t, framesPath string) []map[string]any` — reads the frames file line-by-line, decodes each as JSON, returns the slice. Tests use this to assert `session/cancel` was sent.
      - **`mergeEnv` helper:** `func mergeEnv(a, b map[string]string) map[string]string` — combines two env-overlay maps with b taking precedence on conflict. Used to merge `FakeKiro`'s returned env with the test's own `KIRO_CMD` override.
  </behavior>
  <action>
    Create `tests/e2e/cmd/fake-kiro-cli/main.go` (NEW file, per REVIEW LOW #10) with the binary per the behavior block. Use a switch over the JSON-RPC method name to dispatch. Use `bufio.Scanner` (with a large buffer matching `internal/acp/framer.go`'s line cap) for stdin reading. Use plain `fmt.Println` (line-buffered) or direct stdout writes for response emission. Honor the frame-log env var on every received frame.

    Create `tests/e2e/tools_testmain_test.go` (NEW file — iteration-3 fix to MEDIUM #6). If the e2e package already has a TestMain in `e2e_test.go`, EXTEND that TestMain via a compile-helper invoked before `m.Run`, and add only a smoke-test placeholder file here; OTHERWISE add a fresh TestMain per the behavior block. Either way, the package-level `fakeKiroBinaryPath` var ends up populated before any test runs.

    Create `tests/e2e/tools_fixtures.go` (NEW file) with `//go:build e2e` + `package e2e_test`. Implement:
      1. `var fakeKiroBinaryPath string` (package-level, set by TestMain — iteration-3 MEDIUM #6).
      2. `ToolsCatalog` + `ToolSpecJSON` per the behavior block (3 tools).
      3. The three per-surface request-body builders.
      4. `FakeKiro(t, script) (cmd, env)` per REVIEW HIGH #5 + iteration-3 MEDIUM #6 — reads package-level `fakeKiroBinaryPath`, writes notifications to `t.TempDir()`, returns the binary path + env overlay map.
      5. The four notification builders (Text, Thought, ToolCall, Plan).
      6. `AssertSameCanonicalToolCall` cross-surface equivalence helper.
      7. `GoleakVerifyAtEnd` with the documented `// goleak ignore-list: ...` comment block per WARNING #5 (gateway child-process residue + HTTP idle conns).
      8. `ReadFakeKiroFrames` for scenario 12 frame-log inspection.
      9. `mergeEnv` helper for combining env overlays.
      10. `TestFakeKiro_BinaryExistsAfterMultipleSubtests` smoke self-test per the behavior block (iteration-3 MEDIUM #6 verification — proves the binary path survives across subtests).

    Document the API change from the previous plan version in a top-of-file comment: `// REVIEW HIGH #5: FakeKiro returns (cmd, env) where env carries notification-file and frame-log paths. The previous FakeKiroScript(t, []byte) string API tried to encode this via wrapper scripts and was insufficient for the gateway's full ACP method set. Iteration 3: the fake-kiro binary is compiled at package init by tools_testmain_test.go's TestMain into os.TempDir() (NOT t.TempDir()), so the binary path survives all subtests — fixes iteration-2 MEDIUM #6 lifetime bug.`
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./tests/e2e/cmd/fake-kiro-cli/... && go vet -tags e2e ./tests/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/cmd/fake-kiro-cli/main.go` exists; `go build ./tests/e2e/cmd/fake-kiro-cli/...` succeeds — binary compiles.
    - **`go vet -tags e2e ./tests/e2e/...` clean (iteration-3 fix to MEDIUM #7 — `-tags e2e` ensures e2e-only files are inspected).**
    - `grep -n "session/set_model\|ping" tests/e2e/cmd/fake-kiro-cli/main.go` shows both methods are handled (REVIEW HIGH #5 ACP coverage).
    - `grep -n "OTTO_FAKE_KIRO_NOTIFICATIONS_FILE\|OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE" tests/e2e/cmd/fake-kiro-cli/main.go` shows both env vars are consumed.
    - `tests/e2e/tools_testmain_test.go` exists with the `//go:build e2e` tag and TestMain (or compile-helper if extending existing TestMain).
    - **`grep -n "fakeKiroBinaryPath" tests/e2e/tools_testmain_test.go` shows the TestMain sets the package-level var (iteration-3 MEDIUM #6).**
    - **`grep -n "fake-kiro-cli-" tests/e2e/tools_testmain_test.go` shows the os.Getpid()-suffixed path naming.**
    - `tests/e2e/tools_fixtures.go` exists with the `//go:build e2e` tag and `package e2e_test`.
    - `grep -c '^//go:build e2e' tests/e2e/tools_fixtures.go` returns 1.
    - **`grep -n "var fakeKiroBinaryPath string" tests/e2e/tools_fixtures.go` shows the package-level var declaration (iteration-3 MEDIUM #6).**
    - `grep -n 'func FakeKiro(t \*testing.T, script Script) (cmd string, env map\[string\]string)' tests/e2e/tools_fixtures.go` shows the new (cmd, env) signature (REVIEW HIGH #5).
    - `grep -n 'ToolsCatalog\|NotifToolCall\|AssertSameCanonicalToolCall\|ReadFakeKiroFrames\|mergeEnv\|GoleakVerifyAtEnd' tests/e2e/tools_fixtures.go` shows the helpers.
    - `grep -n 'goleak ignore-list' tests/e2e/tools_fixtures.go` returns ≥1 (WARNING #5 — ignore-list documented in code, not in the plan).
    - **`TestFakeKiro_BinaryExistsAfterMultipleSubtests` passes** (iteration-3 MEDIUM #6 verification — proves binary path survives across two sequential t.Run subtests).
  </acceptance_criteria>
  <done>
    tests/e2e/cmd/fake-kiro-cli/main.go binary compiles cleanly and handles all six ACP methods + EOF cleanup; tests/e2e/tools_testmain_test.go TestMain compiles the binary at package init into os.TempDir() with a per-pid suffix and defer-deletes on m.Run() exit (iteration-3 fix to MEDIUM #6 — package-level binary lifetime, NOT per-test temp dir); tests/e2e/tools_fixtures.go reads the package-level fakeKiroBinaryPath var, exports the FakeKiro(t,script) (cmd, env) API, 3-tool catalog, per-surface request builders, notification builders, cross-surface equivalence helper, frame-log reader, env-merge helper, and `GoleakVerifyAtEnd` with the documented ignore-list comment per WARNING #5; smoke self-test `TestFakeKiro_BinaryExistsAfterMultipleSubtests` proves the iteration-3 fix; `go vet -tags e2e ./tests/e2e/...` is clean (iteration-3 fix to MEDIUM #7). REVIEW HIGH #5 fake-kiro scope and REVIEW LOW #10 files_modified listing are both addressed. The actual e2e test run lives in Task 2; this task only proves the fixtures, binary, and TestMain compile and the lifetime contract holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-surface E2E tests — Ollama (scenarios 1-4, 6-11), OpenAI (scenarios 1-4, 6-11), Anthropic (scenarios 1, 2, 5, 9) + cross-surface canonical equivalence (with per-subtest goleak)</name>
  <files>tests/e2e/tools_ollama_test.go, tests/e2e/tools_openai_test.go, tests/e2e/tools_anthropic_test.go</files>
  <read_first>
    - tests/e2e/tools_fixtures.go (Task 1 output — the symbols this task consumes including the new FakeKiro(t,script) (cmd, env) API and the `GoleakVerifyAtEnd` helper with its documented ignore-list; the package-level fakeKiroBinaryPath var per iteration-3 MEDIUM #6)
    - tests/e2e/ollama_e2e_test.go (subtests pattern at lines 34-100; ollamaRequest at lines 32-56)
    - tests/e2e/openai_e2e_test.go (full file — subtests pattern for the OpenAI surface)
    - tests/e2e/anthropic_e2e_test.go (lines 34-67 streaming subtest pattern; postMessages helper; assertStrictSSE helper)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 — the 12-scenario matrix; D-19 — file split per surface; D-21 — per-subtest goleak gating)
    - .planning/phases/06-tool-call-path/06-VALIDATION.md (Per-Task Verification Map V-01 through V-20)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_{ollama,openai,anthropic}_test.go" section)
  </read_first>
  <behavior>
    - **bootGateway invocation shape (REVIEW HIGH #5):** each test that needs the fake-kiro builds its env merge in two steps:
      ```go
      cmd, env := FakeKiro(t, Script{Notifications: notifs, StopReason: "end_turn"})
      fullEnv := mergeEnv(env, map[string]string{"KIRO_CMD": cmd})
      bootGateway(t, fullEnv)
      ```
      The (cmd, env) split lets the test add or override env vars cleanly (e.g., scenario 12 adds `OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE` via a custom script).
    - **Per-subtest goleak gating (WARNING #5, per CONTEXT D-21):** `defer GoleakVerifyAtEnd(t)` is the FIRST line inside each `t.Run("...", func(t *testing.T) { ... })` block. It is NOT placed at the parent test body. Rationale: with `t.Run` subtests, a parent-level deferred goleak fires once at the parent's end and can attribute a leak in subtest N to subtest N+1 (or hide it entirely). Per-subtest gating matches Task 3's pattern. Pattern to apply uniformly:
      ```go
      t.Run("ScenarioName", func(t *testing.T) {
          defer GoleakVerifyAtEnd(t)
          // ... subtest body ...
      })
      ```
    - **tools_ollama_test.go — `TestE2E_Tools_Ollama` covering D-17 scenarios 1-4, 6-11:** 10 subtests covering native tool_call non-streaming + streaming, coerce-from-bare-JSON, coerce-from-fenced-JSON, empty-tools, no-match, malformed-JSON, existing-tool-calls (kiro-native + JSON text — kiro-native preserved as thought-text per two-path rule, JSON text untouched by coerce skip), multi-tool-tie-breaker, empty-params skipped. All assertions follow Slice 02's plain-object-arguments canary.
      - **REVIEW HIGH #1 verification — streaming-coerce scenario:** subtest **Coerce_BareJSON_Streaming** with `stream:true` in the request → assert the final NDJSON done:true line carries `message.tool_calls[]` AND no intermediate text lines containing the partial JSON (buffered + discarded on coerce hit).
      - **REVIEW HIGH #2 verification — kiro-native streaming:** subtest **NativeToolCall_Streaming** asserts intermediate NDJSON lines carry `[tool: <name>]\n` content; the final done:true line does NOT carry `message.tool_calls[]` from kiro-native.
      - **iteration-3 HIGH #1 verification — non-streaming kiro-native narration:** subtest **NativeToolCall_NonStreaming** (scenario 1) asserts `message.content` contains the `[tool: get_weather]\n` narration text (sourced from 06-01 Task 2's engine.Collect aggregator) AND `message.tool_calls` is absent/empty AND `done_reason` (or equivalent) reflects normal completion (not "tool_calls"). This proves the iteration-3 fix to HIGH #1 works end-to-end for Ollama.
      - **iteration-3 fix to HIGH #2 verification — streaming native-then-JSON no-coerce:** subtest **NativeToolCall_ThenJSONText_NoCoerce_Streaming** asserts that after a kiro-native tool_call passes through the stream, subsequent JSON-shaped text is flushed as plain text NDJSON lines (not coerced); the done line carries no tool_calls.
    - **tools_openai_test.go — `TestE2E_Tools_OpenAI` covering scenarios 1-4, 6-11:** same 10 subtests in OpenAI's native shape. Wire-shape assertions: JSON-string arguments (escaped quotes), multi-frame SSE on streaming coerce, `finish_reason:"tool_calls"` only when coerce fires.
      - **REVIEW HIGH #1 verification — streaming-coerce scenario:** subtest **Coerce_BareJSON_Streaming** asserts the SSE output contains the multi-frame tool_calls sequence (frame B id+name, frame C arguments JSON-string, terminal `finish_reason:"tool_calls"`) AND NO intermediate text deltas from the partial JSON.
      - **REVIEW HIGH #2 verification — kiro-native streaming:** subtest **NativeToolCall_Streaming** asserts SSE delta-content carries `[tool: <name>]\n` text; NO `delta.tool_calls[]` from kiro-native; terminal `finish_reason` is NOT `"tool_calls"`.
      - **REVIEW LOW #8 verification — role-emit-once with tool-call first:** subtest **ToolCallFirstStreamChunk_RoleEmitOnce** asserts exactly one role frame even when tool_call is the first chunk.
      - **iteration-3 HIGH #1 verification — non-streaming kiro-native narration:** subtest **NativeToolCall_NonStreaming** (scenario 1) asserts `choices[0].message.content` contains `[tool: get_weather]\n` AND `choices[0].message.tool_calls` is absent/empty AND `finish_reason` is NOT `"tool_calls"`.
      - **iteration-3 fix to HIGH #2 verification — streaming native-then-JSON no-coerce:** subtest **NativeToolCall_ThenJSONText_NoCoerce_Streaming** asserts that after a kiro-native ChunkKindToolCall passes through, subsequent JSON-shaped text is released as plain text-delta SSE frames; terminal `finish_reason:"stop"` (not `"tool_calls"`); no `delta.tool_calls`.
    - **tools_anthropic_test.go — `TestE2E_Tools_Anthropic` covering scenarios 1, 2, 5, 9:**
      1. **NativeToolCall_NonStreaming** (scenario 1, REVIEW MEDIUM #4 verification): non-streaming `/v1/messages` with kiro `tool_call` → assert `content[0].type == "tool_use"`, `.id == "tc_001"`, `.name == "get_weather"`, `.input == {"location":"NYC"}` (object, NOT null), **`stop_reason == "tool_use"`** (NOT `end_turn` — the override Task 3 in 06-04 added).
      2. **NativeToolCall_Streaming** (scenario 2): streaming Anthropic tool_use sequence — content_block_start{tool_use,input:{}} → content_block_delta{input_json_delta} → content_block_stop → message_delta{stop_reason:"tool_use"}.
      3. **NoCoerce_BareJSON** (scenario 5 — D-01 asymmetry verification): notifications = NotifText("{\"location\":\"NYC\"}"); request has tools catalog. Assert response `content[0].type == "text"`, `.text == "{\"location\":\"NYC\"}"` (verbatim); NO `tool_use` content block. Assert `stop_reason == "end_turn"` (no tool_use → no override).
      4. **ExistingToolCalls_NoSecondaryProcessing** (scenario 9): notifications = NotifText("some thoughts") + NotifToolCall + NotifText("{\"location\":\"NYC\"}"). Assert kiro-native tool_use preserved as ONE tool_use block; the bare-JSON text is NOT converted to a second tool_use (no coerce on Anthropic); the JSON-text content preserved verbatim alongside the tool_use.
    - **Cross-surface canonical equivalence test:** `TestE2E_Tools_CrossSurface_CanonicalEquivalence` runs the SAME notifications (scenario 1: NotifToolCall) against all three surfaces and calls `AssertSameCanonicalToolCall`. Proves CONTEXT D-17 cross-surface assertion. Per the iteration-3 reworded contract, equivalence here is scoped to the canonical tool-call identity (name + args) — Ollama/OpenAI render as narration text (no tool_calls field on non-streaming) while Anthropic renders as native tool_use. The helper must normalize accordingly (e.g., parse the narration `[tool: get_weather]\n` text for Ollama/OpenAI and the `tool_use` block for Anthropic, then assert the extracted name + args match). Document this normalization in a test comment.
    - **Subtest naming convention:** subtests grep-friendly to D-17 scenario numbers via comments.
  </behavior>
  <action>
    Create `tests/e2e/tools_ollama_test.go` with the `//go:build e2e` tag and `package e2e_test`. Implement `TestE2E_Tools_Ollama` with the 10 base subtests + the iteration-3 NativeToolCall_NonStreaming (HIGH #1 verification) + NativeToolCall_ThenJSONText_NoCoerce_Streaming (HIGH #2 verification) subtests, AND the Coerce_BareJSON_Streaming + NativeToolCall_Streaming subtests for REVIEW HIGH #1/#2. Per subtest:
      1. **First line inside `t.Run`: `defer GoleakVerifyAtEnd(t)` (WARNING #5 — per-subtest gating).**
      2. Build notifications via the fixture builders.
      3. `cmd, env := FakeKiro(t, Script{Notifications: notifs})`.
      4. `bootGateway(t, mergeEnv(env, map[string]string{"KIRO_CMD": cmd}))`.
      5. POST the request via `ollamaRequest`.
      6. Assert response shape.

    Create `tests/e2e/tools_openai_test.go` with `TestE2E_Tools_OpenAI` covering the same 10 base subtests PLUS the iteration-3 NativeToolCall_NonStreaming + NativeToolCall_ThenJSONText_NoCoerce_Streaming subtests PLUS the REVIEW HIGH #1/#2/LOW #8 subtests, all with the same per-subtest goleak pattern. POST to `/v1/chat/completions`.

    Create `tests/e2e/tools_anthropic_test.go` with `TestE2E_Tools_Anthropic` covering scenarios 1, 2, 5, 9, again with per-subtest goleak. POST to `/v1/messages`. Scenario 1 asserts `stop_reason:"tool_use"` (the REVIEW MEDIUM #4 verification at the E2E layer). Scenario 5 is the D-01 asymmetry verification.

    Add `TestE2E_Tools_CrossSurface_CanonicalEquivalence` in `tools_anthropic_test.go` (or a standalone `tools_cross_test.go`). Calls `AssertSameCanonicalToolCall` with bodies from all three surfaces. Per the iteration-3 reworded contract, the helper must normalize: parse narration text for Ollama/OpenAI, parse native tool_use for Anthropic, then assert canonical identity (name + args) matches.

    For uniformity, do NOT place a goleak defer at the parent-test body level — every `t.Run` block carries its own `defer GoleakVerifyAtEnd(t)` as line 1.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools_(Ollama|OpenAI|Anthropic|CrossSurface)" -count=1 -race -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist with the `//go:build e2e` tag and `package e2e_test`.
    - `TestE2E_Tools_Ollama` has subtests covering scenarios 1-4 + 6-11 PLUS Coerce_BareJSON_Streaming (REVIEW HIGH #1) + NativeToolCall_Streaming (REVIEW HIGH #2) + **NativeToolCall_NonStreaming (iteration-3 HIGH #1 — narration in message.content)** + **NativeToolCall_ThenJSONText_NoCoerce_Streaming (iteration-3 HIGH #2)**.
    - `TestE2E_Tools_OpenAI` has the same scenario coverage PLUS Coerce_BareJSON_Streaming (multi-frame native tool_calls) + NativeToolCall_Streaming (text-delta narration, no delta.tool_calls) + ToolCallFirstStreamChunk_RoleEmitOnce (REVIEW LOW #8) + **NativeToolCall_NonStreaming (iteration-3 HIGH #1)** + **NativeToolCall_ThenJSONText_NoCoerce_Streaming (iteration-3 HIGH #2)**.
    - `TestE2E_Tools_Anthropic` has subtests: NativeToolCall_NonStreaming (asserts stop_reason:"tool_use" per REVIEW MEDIUM #4), NativeToolCall_Streaming, NoCoerce_BareJSON, ExistingToolCalls_NoSecondaryProcessing.
    - `TestE2E_Tools_CrossSurface_CanonicalEquivalence` exists and uses `AssertSameCanonicalToolCall` with the iteration-3 normalization (narration text vs native tool_use).
    - All tests call `FakeKiro(t, script)` with the new (cmd, env) API (REVIEW HIGH #5).
    - **`grep -c "defer GoleakVerifyAtEnd(t)" tests/e2e/tools_ollama_test.go` ≥ 14** (10 base + 4 new for HIGH #1/HIGH #2 verifications — WARNING #5 per-subtest gating).
    - **`grep -c "defer GoleakVerifyAtEnd(t)" tests/e2e/tools_openai_test.go` ≥ 15** (10 base + Coerce_BareJSON_Streaming + NativeToolCall_Streaming + ToolCallFirstStreamChunk_RoleEmitOnce + iteration-3 NativeToolCall_NonStreaming + NativeToolCall_ThenJSONText_NoCoerce_Streaming).
    - `grep -c "defer GoleakVerifyAtEnd(t)" tests/e2e/tools_anthropic_test.go` ≥ 4 (scenarios 1, 2, 5, 9; plus the cross-surface test if added in this file).
    - `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run TestE2E_Tools -count=1` passes (all 30+ subtests + cross-surface).
    - Goroutine-leak gate clean for each subtest individually.
    - `go vet -tags e2e ./tests/e2e/...` clean.
  </acceptance_criteria>
  <done>
    Full D-17 E2E matrix passes against the live binary with the new FakeKiro(t,script) (cmd, env) API and per-subtest goleak gating (WARNING #5 per CONTEXT D-21); cross-surface canonical equivalence proven (with iteration-3 narration vs native tool_use normalization); D-01 NO-coerce asymmetry verified at the E2E layer (scenario 5); REVIEW HIGH #1 streaming-coerce verified for Ollama + OpenAI; REVIEW HIGH #2 two-path rule verified across all three surfaces; iteration-3 HIGH #1 non-streaming kiro-native narration verified for Ollama + OpenAI; iteration-3 HIGH #2 streaming native-then-JSON no-coerce verified for Ollama + OpenAI; REVIEW MEDIUM #4 non-streaming stop_reason:tool_use verified at E2E; no goroutine leaks; wire-shape divergence canary holds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tools_cancel_test.go — scenario 12 (mid-stream cancel during tool_call) per-surface with frame-log assertion + per-subtest goleak</name>
  <files>tests/e2e/tools_cancel_test.go</files>
  <read_first>
    - tests/e2e/ollama_e2e_test.go (Chat_DisconnectSmoke at lines 309-385 — the proven mid-stream-cancel pattern this test replicates)
    - tests/e2e/tools_fixtures.go (Task 1 — FakeKiro + Script + ReadFakeKiroFrames + GoleakVerifyAtEnd)
    - tests/e2e/pool_sessions_e2e_test.go (Phase 5 dead-slot survival test — verifies pool/health endpoint shows slot alive after a session cancellation)
    - .planning/phases/06-tool-call-path/06-CONTEXT.md (D-17 scenario 12 — mid-stream disconnect during tool_call; D-21 — per-subtest goleak gating)
    - .planning/phases/06-tool-call-path/06-RESEARCH.md (Phase 5 dead-slot reference, Phase 4 D-06 watchdog)
    - .planning/phases/06-tool-call-path/06-PATTERNS.md ("NEW tests/e2e/tools_cancel_test.go" — mid-stream disconnect analog)
  </read_first>
  <behavior>
    - **`TestE2E_Tools_Cancel` per-surface subtests** (three subtests — Ollama, OpenAI, Anthropic):
      - Each subtest:
        1. **First line inside `t.Run`: `defer GoleakVerifyAtEnd(t)` (per CONTEXT D-21; this task already followed the per-subtest pattern that Task 2 now matches per WARNING #5).**
        2. Build notifications: `NotifText("starting") + NotifToolCall("tc_cancel", "search_web", {query:"long running"}) + NotifText("more text after tool call")`. The trailing text provides time to disconnect before the stream completes.
        3. `cmd, env := FakeKiro(t, Script{Notifications: notifs, LogFrames: true})`. LogFrames=true sets `OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE` in the returned env so the fake-kiro binary appends received frames to a file path the test can later inspect.
        4. `bootGateway(t, mergeEnv(env, map[string]string{"KIRO_CMD": cmd}))`.
        5. POST the streaming request with `context.WithCancel`-derived request context.
        6. Read a few response lines (enough to confirm streaming started).
        7. `cancel()`.
        8. Sleep ~50-200ms for the gateway to propagate cancel.
        9. Get the frame-log path from the env overlay (the fake-kiro writes received frames there) and call `frames := ReadFakeKiroFrames(t, framesPath)`.
        10. Assert exists a frame in `frames` where `frame["method"] == "session/cancel"` (frame-log assertion).
        11. Assert pool slot survives: make a fresh non-streaming request to the gateway with no tools; assert it completes within 5s.
    - **`OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE` discovery in tests:** the env overlay returned by `FakeKiro` includes this key when `Script.LogFrames` is true. Tests retrieve the path via `env["OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE"]` BEFORE calling `mergeEnv` (or read it back from the merged map; either works since FakeKiro's overlay isn't overridden).
    - **goleak discipline:** the per-subtest `defer GoleakVerifyAtEnd(t)` fires AFTER all client connections close, all gateway responses finalize, all stream watchdogs unwind (`t.Run` cleans up before defer evaluates). Use a short `time.Sleep(50 * time.Millisecond)` before the goleak check to allow async cleanup — implement this inside the helper rather than at the call site.
    - **Slot survival assertion:** the cleanest proof is a fresh request against the same gateway boot — if the pool replaced the slot or the stream leaked into "busy" state, the fresh request would hang or fail. Use a 5s timeout — fast failure on regression.
    - **All three sub-tests use the same fixture pattern** but each uses its own request/assertion path. No shared mutable state; each spins up its own gateway boot via `bootGateway`.
  </behavior>
  <action>
    Create `tests/e2e/tools_cancel_test.go` with `//go:build e2e` tag + `package e2e_test`. Implement `TestE2E_Tools_Cancel` with three per-surface sub-tests per the behavior block.

    Each sub-test:
      1. `defer GoleakVerifyAtEnd(t)` as the first line inside `t.Run`.
      2. Build notifications via the fixture builders.
      3. `cmd, env := FakeKiro(t, Script{Notifications: notifs, LogFrames: true})`.
      4. Extract the frame-log path: `framesPath := env["OTTO_FAKE_KIRO_RECEIVED_FRAMES_FILE"]`.
      5. `bootGateway(t, mergeEnv(env, map[string]string{"KIRO_CMD": cmd}))`.
      6. POST streaming request with `context.WithCancel`.
      7. Read partial response (2-3 lines).
      8. `cancel()`.
      9. `time.Sleep(150 * time.Millisecond)`.
      10. `frames := ReadFakeKiroFrames(t, framesPath)`.
      11. Assert `slices.ContainsFunc(frames, func(f map[string]any) bool { return f["method"] == "session/cancel" })`.
      12. Make fresh non-streaming request with 5s timeout; assert it completes.

    Per CLAUDE.md cross-compile and goleak discipline.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools_Cancel" -count=1 -race -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/tools_cancel_test.go` exists with `//go:build e2e` + `package e2e_test`.
    - `TestE2E_Tools_Cancel` has three sub-tests: `Ollama_CancelDuringToolCall`, `OpenAI_CancelDuringToolCall`, `Anthropic_CancelDuringToolCall`.
    - `grep -c "defer GoleakVerifyAtEnd(t)" tests/e2e/tools_cancel_test.go` returns ≥ 3 (one per subtest).
    - Each sub-test asserts the `session/cancel` frame was emitted (via `ReadFakeKiroFrames`).
    - Each sub-test asserts pool slot survival (fresh request completes within 5s after cancel).
    - `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run TestE2E_Tools_Cancel -race -count=1` passes (all 3 sub-tests).
    - No goroutine leaks detected per subtest.
  </acceptance_criteria>
  <done>
    Scenario 12 mid-stream cancel verified across all three surfaces with per-subtest goleak; session/cancel emission confirmed via fake-kiro frame-log read (REVIEW HIGH #5 frame-log seam); pool slot survives the cancel (Phase 5 dead-slot discipline); goroutine-leak gate clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): loop24-client messages.stream() HUMAN-UAT against the live binary (Anthropic SDK conformance)</name>
  <what-built>
    Slice 06-04 implemented Anthropic SSE streaming for tool_use blocks per D-07 Anthropic + the SDK-expected event sequence (content_block_start with input:{} + input_json_delta with full args + content_block_stop + message_delta with stop_reason:"tool_use"). Slice 06-04 Task 3 also added the non-streaming render.go stop_reason override per REVIEW MEDIUM #4. The golden-fixture tests lock the byte-level shape AND the E2E tests in Task 2 above verify the wire output via fake-kiro — but per VALIDATION.md Manual-Only Verifications: SDK parser conformance requires the REAL `@anthropic-ai/sdk` MessageStream to consume the stream and surface a complete `tool_use` block to the application layer. Only loop24-client (which uses the SDK in production) can verify this end-to-end.
  </what-built>
  <how-to-verify>
    1. Start the gateway from this repo: `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go run ./cmd/otto-gateway` (or use the existing dev-run script).
    2. Verify the gateway is listening: `curl -s http://localhost:11434/health | jq`.
    3. Switch to the loop24-client repo: `cd ../loop24-client` (or wherever it lives).
    4. Run the gateway against loop24-client's smoke test:
       ```
       ANTHROPIC_BASE_URL=http://localhost:11434 ANTHROPIC_API_KEY=e2e-token npm run smoke:tool-use
       ```
       OR a minimal Node script using `@anthropic-ai/sdk`'s `client.messages.stream({...tools: [get_weather]...})`.
    5. Observe and verify:
       - Script does NOT crash with a JSON parse error during streaming (would indicate `input:null` regression — Pitfall 1).
       - Script does NOT crash with a block-index mismatch (would indicate Pitfall 7 regression).
       - SDK emits `content_block_start` → `content_block_delta` → `content_block_stop` for tool_use.
       - Final `Message` from `stream.finalMessage()` includes a complete `tool_use` content block with `input` as a JavaScript OBJECT (NOT null/undefined/string), containing the expected key `location`.
       - `Message.stop_reason == "tool_use"` (NOT `"end_turn"`).
       - **(Optional but recommended)** also exercise the non-streaming path: `client.messages.create({...tools: [get_weather], stream: false ...})` should also return `stop_reason: "tool_use"` (REVIEW MEDIUM #4 verification at the SDK layer).
    6. On all checks passing → respond "approved — loop24-client conformance confirmed".
    7. On SDK throw / incomplete block / wrong stop_reason → describe symptom; planner routes to gap-closure per the specific failure mode (Pitfall 1 — CR-01 regression; Pitfall 7 — block-index regression; finalize override missing; render.go override missing).

    Resume signal: "approved" OR describe the failure mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe failure symptom</resume-signal>
  <files>cmd/otto-gateway/main.go (read-only — boot target); internal/adapter/anthropic/sse.go + render.go (read-only — wire shapes under verification)</files>
  <action>
    HUMAN-VERIFY CHECKPOINT — NOT an autonomous Claude action. Operator starts gateway, runs loop24-client `messages.stream()` AND `messages.create()` (the latter for REVIEW MEDIUM #4 non-streaming verification) per how-to-verify, observes SDK behavior, types resume signal. Claude reads the response: on "approved", proceeds to phase sign-off; on described failure, routes to gap-closure per the specific failure mode.
  </action>
  <verify>
    <human-check>Operator runs the SDK smoke tests against the live gateway and confirms (a) complete tool_use block surfaces with object input + stop_reason:"tool_use" in BOTH streaming AND non-streaming; resume signal received.</human-check>
  </verify>
  <done>
    Operator has run loop24-client (or equivalent `@anthropic-ai/sdk` script) against the live binary, observed a successful tool_use round-trip with object input + stop_reason:"tool_use" in both streaming AND non-streaming (REVIEW MEDIUM #4 verified at SDK layer), and typed an approval response.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake-kiro binary stdin/stdout | Test-controlled binary; runs in a process whose binary path is package-scoped (iteration-3 fix to MEDIUM #6). Notification content is test-supplied. The binary must not panic on malformed scripted notifications. |
| E2E HTTP requests → gateway | Real HTTP traffic against the live gateway; tests authenticate via `AUTH_TOKEN=e2e-token` (existing convention). |
| Frame-log file from fake-kiro | Test-process-owned file in `t.TempDir()`; no security boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | DoS (test harness flake) | E2E test goroutine leaks | mitigate | `GoleakVerifyAtEnd(t)` per-SUBTEST (WARNING #5; CONTEXT D-21), with a 50-200ms async-cleanup grace period inside the helper. The helper's documented `// goleak ignore-list:` block accounts for child-process residue and HTTP idle-conn maintenance. |
| T-06-12 | Spoofing (regression — surfaces start emitting wrong shape) | Cross-surface canonical equivalence | mitigate | `TestE2E_Tools_CrossSurface_CanonicalEquivalence` asserts canonical content is identical across all three surfaces (with iteration-3 normalization between narration text and native tool_use). |
| T-06-13 | Tampering (intentional asymmetry erosion) | TestE2E_Tools_Anthropic/NoCoerce_BareJSON | mitigate | E2E test for D-01 asymmetry — bare JSON text on /v1/messages preserved verbatim. Combined with Slice 06-04's behavioral + static-source tests, the asymmetry has defense-in-depth at three layers (unit-behavioral, unit-static, E2E). |
| T-06-17 (NEW per REVIEW HIGH #5) | Tampering (fake-kiro contract mismatch) | tests/e2e/cmd/fake-kiro-cli/main.go | mitigate | Fake binary handles all six ACP methods the gateway issues (initialize/session/new/session/set_model/session/prompt/session/cancel/ping). Missing methods would cause E2E flakes/deadlocks. (cmd, env) return shape from FakeKiro is the stable test contract. |
| T-06-21 (NEW iteration-3 MEDIUM #6) | DoS (fake-kiro binary lifetime bug → cascading flakes) | tests/e2e/tools_testmain_test.go binary lifetime | mitigate | TestMain compiles the binary at package init into os.TempDir() with a per-pid suffix (not t.TempDir() which gets cleaned up after the first test). Package-level lifetime means all subtests see a valid binary path. TestFakeKiro_BinaryExistsAfterMultipleSubtests smoke test locks the contract. |
| T-06-14 | Spoofing (Anthropic SDK parser regression) | loop24-client conformance | mitigate via human-verify checkpoint | Task 4 — blocking checkpoint with real `@anthropic-ai/sdk` MessageStream. Golden fixture + E2E are necessary-but-not-sufficient; real SDK consumption is the conformance vote. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies. Fake-kiro is pure-Go stdlib. |
</threat_model>

<verification>
After all 4 tasks complete (3 automated + 1 checkpoint):
1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -run "TestE2E_Tools" -race -count=1 -timeout 10m` — all E2E tests green.
2. loop24-client HUMAN-UAT checkpoint resolved (approved).
3. Node byte-fidelity checkpoint (now in Slice 06-01 Task 4 per REVIEW HIGH #3) is also resolved before this slice runs.
4. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci` clean.
5. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make ci && OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -timeout 10m` — full Phase 6 sign-off gate green.
6. `go vet -tags e2e ./tests/e2e/...` clean (iteration-3 fix to MEDIUM #7).
7. Update `.planning/phases/06-tool-call-path/06-VALIDATION.md` to set `nyquist_compliant: true`, Approval: `approved`.
</verification>

<success_criteria>
- D-17 12-scenario E2E matrix fully exercised against the live otto-gateway binary with the controllable fake-kiro-cli that handles all six ACP methods (REVIEW HIGH #5).
- New `FakeKiro(t, script) (cmd, env)` API replaces the previous `FakeKiroScript(t, []byte) string` (REVIEW HIGH #5).
- **iteration-3 fix to MEDIUM #6:** fake-kiro binary is compiled at package init by TestMain into os.TempDir() with a per-pid suffix; lifetime is package-scoped (not per-test); smoke test `TestFakeKiro_BinaryExistsAfterMultipleSubtests` proves the contract.
- **iteration-3 fix to MEDIUM #7:** `go vet -tags e2e ./tests/e2e/...` is the verification command (not bare `go vet`).
- Cross-surface canonical equivalence proven (with iteration-3 narration vs native tool_use normalization).
- D-01 NO-coerce asymmetry verified at the E2E layer (scenario 5).
- REVIEW HIGH #1 streaming-coerce verified for both Ollama and OpenAI (scenarios Coerce_BareJSON_Streaming).
- REVIEW HIGH #2 two-path rule verified at E2E (NativeToolCall_Streaming subtests for Ollama/OpenAI emit thought-text only; Anthropic exception confirms native tool_use sequence per D-07).
- **iteration-3 HIGH #1 verified at E2E:** non-streaming Ollama/OpenAI kiro-native tool_calls render `[tool: <name>]\n` narration in message.content (sourced from 06-01 engine.Collect aggregator).
- **iteration-3 HIGH #2 verified at E2E:** streaming Ollama/OpenAI native-then-JSON sequences do NOT coerce (sawKiroNativeToolCall skip-or-flush).
- REVIEW MEDIUM #4 non-streaming stop_reason:"tool_use" verified at E2E (Anthropic scenario 1).
- REVIEW LOW #8 role-emit-once-with-tool-call-first verified at E2E (OpenAI subtest).
- Scenario 12 mid-stream cancel verified with session/cancel emission via frame-log read + slot survival + per-subtest no goleak.
- Per-subtest goleak gating used uniformly across Tasks 2 + 3 (WARNING #5 per CONTEXT D-21) — `defer GoleakVerifyAtEnd(t)` is the first line inside each `t.Run` block, never at the parent-test body level.
- loop24-client `messages.stream()` HUMAN-UAT against the live binary completes successfully — SDK surfaces complete tool_use with object input + stop_reason:"tool_use" in both streaming AND non-streaming (REVIEW MEDIUM #4 SDK-layer verification).
- `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -race` clean.
- `make ci` clean.
- Phase 6 VALIDATION.md `nyquist_compliant: true` after sign-off.
</success_criteria>

<output>
Create `.planning/phases/06-tool-call-path/06-05-SUMMARY.md` summarizing:
- The new fake-kiro-cli binary + its ACP method coverage (REVIEW HIGH #5).
- The new FakeKiro(t,script) (cmd, env) fixture API (REVIEW HIGH #5).
- The new tools_testmain_test.go TestMain that compiles the binary at package init (iteration-3 fix to MEDIUM #6).
- `go vet -tags e2e ./tests/e2e/...` as the verification command (iteration-3 fix to MEDIUM #7).
- The per-subtest goleak gating discipline applied uniformly (WARNING #5 per CONTEXT D-21).
- The D-17 E2E matrix scenario-pass-count (now including iteration-3 narration + native-then-JSON subtests).
- Cross-surface canonical equivalence assertion (with iteration-3 normalization).
- REVIEW HIGH #1 streaming-coerce E2E verification.
- REVIEW HIGH #2 two-path rule E2E verification.
- iteration-3 HIGH #1 non-streaming kiro-native narration E2E verification.
- iteration-3 HIGH #2 streaming native-then-JSON no-coerce E2E verification.
- REVIEW MEDIUM #4 stop_reason:"tool_use" E2E + SDK verification.
- Resolution of loop24-client HUMAN-UAT (PASS / FAIL + symptoms if FAIL).
- Total Phase 6 sign-off — all SC1..SC5 from ROADMAP §"Phase 6: Tool-Call Path" verified.
- VALIDATION.md `nyquist_compliant: true` confirmed.
</output>
