---
phase: 20-code-review-backlog-burn-down
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/otto-tray/uihelpers_darwin.go
  - cmd/otto-tray/uihelpers_windows.go
  - cmd/otto-tray/tooltip.go
  - cmd/otto-tray/escapeApplescript_darwin_test.go
  - cmd/otto-tray/tray.go
  - internal/server/server.go
  - internal/server/run_direct_test.go
  - internal/pool/regression_rel_pool_02_test.go
  - internal/pool/respawn_ctx_cancel_test.go
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]

must_haves:
  truths:
    - "escapeApplescript translates raw \\n, \\r, \\t bytes into the two-char AppleScript escape sequences and strips other C0/DEL bytes (QUAL-01)"
    - "tooltipForState exists exactly once in cmd/otto-tray, in a build-tagged shared file (QUAL-02)"
    - "Server.Run() called directly with a nil forceCloseCh shuts down cleanly under ctx.Done() (QUAL-03)"
    - "tailLines no longer prepends with append([]string{t}, kept...) — uses collect-then-reverse (QUAL-04)"
    - "internal/pool/regression_rel_pool_02_test.go has no sessions/sessionsMu declarations (QUAL-05)"
    - "internal/pool/respawn_ctx_cancel_test.go line ~119 no longer references the removed removeSlot symbol as a live mechanism (QUAL-06)"
    - "make ci exits 0 after all six commits — race, vet, staticcheck, gosec, arch-lint all green"
  artifacts:
    - path: "cmd/otto-tray/escapeApplescript_darwin_test.go"
      provides: "Table-driven unit tests for QUAL-01 escapeApplescript behavior — RED before GREEN per D-20-02"
      contains: "TestEscapeApplescript"
    - path: "cmd/otto-tray/tooltip.go"
      provides: "Single shared tooltipForState implementation under //go:build darwin || windows (D-20-03)"
      contains: "tooltipForState"
    - path: "internal/server/run_direct_test.go"
      provides: "Regression test that s.Run(ctx) returns cleanly with a nil forceCloseCh (D-20-04 guard rail)"
      contains: "TestServer_Run_DirectShutdown"
  key_links:
    - from: "cmd/otto-tray/uihelpers_darwin.go (notifyImpl/infoDialog/confirmDialog)"
      to: "escapeApplescript (expanded escape set)"
      via: "direct function call inside fmt.Sprintf AS-script construction"
      pattern: "escapeApplescript\\("
    - from: "internal/server/server.go RunUntilSignal (lines ~542–545)"
      to: "Server.forceCloseCh (now allocated in RunUntilSignal, nil for direct Run callers)"
      via: "close(s.forceCloseCh) after RunUntilSignal-side allocation"
      pattern: "s\\.forceCloseCh\\s*=\\s*make\\(chan struct\\{\\}\\)"
    - from: "cmd/otto-tray/uihelpers_darwin.go + uihelpers_windows.go"
      to: "cmd/otto-tray/tooltip.go (the shared definition)"
      via: "build-tag combination //go:build darwin || windows"
      pattern: "func tooltipForState\\("
---

<objective>
Close six Info-level findings (QUAL-01..QUAL-06) deferred from Phase 16 and Phase 17 code reviews as a single mechanical-refactor batch. Refactor-only with one narrow behavioral expansion (QUAL-01: AppleScript escape set). Six atomic commits per D-20-09. CI must stay green after every commit.

Purpose: Close the v1.10.3 code-review backlog and clean the slate for v1.10.3 milestone close.
Output: 6 atomic commits — one per QUAL finding; updated traceability rows in REQUIREMENTS.md (executor concern at SUMMARY time, not part of the per-task commits).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md

# Source files touched by this plan (executors must read before editing)
@cmd/otto-tray/uihelpers_darwin.go
@cmd/otto-tray/uihelpers_windows.go
@cmd/otto-tray/tray.go
@internal/server/server.go
@internal/pool/regression_rel_pool_02_test.go
@internal/pool/respawn_ctx_cancel_test.go
</context>

## Artifacts this phase produces

**New files:**
- `cmd/otto-tray/tooltip.go` — `//go:build darwin || windows`, contains the single shared `tooltipForState` (D-20-03).
- `cmd/otto-tray/escapeApplescript_darwin_test.go` — `//go:build darwin`, table-driven unit tests for `escapeApplescript` (D-20-02).
- `internal/server/run_direct_test.go` — regression test asserting `s.Run(ctx)` returns cleanly with a nil `forceCloseCh` on `ctx.Done()` (D-20-04 / `<specifics>` QUAL-03 guard rail). May be merged into an existing `_test.go` file in `internal/server/` at executor discretion if a natural home exists; if so, the test function name `TestServer_Run_DirectShutdown` is preserved.

**Existing symbols modified (no new symbols):**
- `escapeApplescript` — expanded escape set (QUAL-01).
- `tooltipForState` — deduplicated into the shared file (QUAL-02).
- `Server.forceCloseCh` — allocation site moved to `RunUntilSignal`; field comment rewritten (QUAL-03).
- `tailLines` — algorithm swapped from O(n²) prepend to collect-then-reverse (QUAL-04).
- `internal/pool/regression_rel_pool_02_test.go` — dead `sessions`/`sessionsMu` declarations + references deleted (QUAL-05).
- `internal/pool/respawn_ctx_cancel_test.go` — stale `removeSlot` comment rewritten (QUAL-06).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: QUAL-01 — Expand escapeApplescript escape set (TDD R→G→R)</name>
  <files>cmd/otto-tray/escapeApplescript_darwin_test.go, cmd/otto-tray/uihelpers_darwin.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-01, D-20-02, `<specifics>` QUAL-01 escape representation)
    - cmd/otto-tray/uihelpers_darwin.go (current `escapeApplescript` at lines 131–140; call sites at 88–90, 104–106, 120–124)
  </read_first>
  <behavior>
    Per D-20-01, `escapeApplescript(s string) string` must, when called on a darwin build:
    - Continue escaping `"` and `\\` exactly as today (prepend a backslash to each occurrence in the emitted byte stream).
    - Translate raw byte 0x0A (`\n`) into the two-byte AppleScript escape sequence `\\n` — i.e. backslash + `n` — so multi-line input survives as readable text inside the emitted AS string literal instead of prematurely terminating it.
    - Translate raw byte 0x0D (`\r`) into the two-byte sequence `\\r`.
    - Translate raw byte 0x09 (`\t`) into the two-byte sequence `\\t`.
    - Strip ALL other C0 bytes (0x00..0x1F inclusive, excluding 0x09/0x0A/0x0D handled above) entirely — they are silently dropped from the output.
    - Strip DEL (0x7F) entirely.
    - Pass plain ASCII (0x20..0x7E except `"` and `\\`) and bytes >= 0x80 through unchanged (no UTF-8 mangling — function operates on bytes).

    Table-driven cases the new test file MUST cover (per D-20-02):
    - Empty input → empty output.
    - Plain ASCII (e.g. "hello world") → unchanged.
    - Embedded `"` → `\\"` (single byte → two bytes).
    - Embedded `\\` → `\\\\` (single byte → two bytes).
    - Embedded `\n` (0x0A) → `\\n` (two bytes: backslash + literal `n`).
    - Embedded `\r` (0x0D) → `\\r`.
    - Embedded `\t` (0x09) → `\\t`.
    - Raw 0x00 → stripped (zero bytes in output for that input byte).
    - Raw 0x1F → stripped.
    - Raw 0x7F → stripped.
    - Mixed: `"line1\nline2\twith \\"quote\\"\x00trailing"` → exercises every transformation in a single string.
  </behavior>
  <action>
    RED — Create `cmd/otto-tray/escapeApplescript_darwin_test.go` with a `//go:build darwin` build tag in package `main` (same package as the source). Add a single table-driven `TestEscapeApplescript(t *testing.T)` covering every case in `<behavior>`. Run `go test ./cmd/otto-tray/... -run TestEscapeApplescript -tags darwin` (or simply `go test ./cmd/otto-tray/...` on a darwin host) — the test MUST fail on at least one case (the `\n`/`\r`/`\t`/C0/DEL rows) against the current implementation that only escapes `"` and `\\`. Commit the failing test ONLY IF the project convention permits intermediate failing commits (it does NOT — per D-20-09 each QUAL closes in ONE commit, and per CI policy CI must stay green at every commit). Therefore: do NOT commit the RED state to main — keep RED→GREEN local; the single commit lands the test + implementation together.

    GREEN — Rewrite `escapeApplescript` in `cmd/otto-tray/uihelpers_darwin.go` per D-20-01. The new body iterates the input byte-by-byte. Outer dispatch on `c := s[i]`:
    - `c == '"' || c == '\\'`: append `\\` then `c`.
    - `c == '\n'`: append `\\` then `'n'`.
    - `c == '\r'`: append `\\` then `'r'`.
    - `c == '\t'`: append `\\` then `'t'`.
    - `c < 0x20 || c == 0x7F`: drop (continue without appending).
    - default: append `c`.

    Initial capacity hint stays `len(s)` (it is now an under-estimate in escape-heavy inputs but allocates the same as today's baseline).

    REFACTOR — Re-run `go test ./cmd/otto-tray/... -race`. All cases pass. Read the function for clarity; consider extracting the byte-classification table into a `switch` only if it improves readability (executor discretion — semantics are locked).

    Commit per D-20-09: `refactor(20): close QUAL-01 — expand escapeApplescript escape set + table-driven tests`. The commit touches exactly two files: the new test file and `uihelpers_darwin.go`.
  </action>
  <verify>
    <automated>go test ./cmd/otto-tray/... -run TestEscapeApplescript -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/otto-tray/escapeApplescript_darwin_test.go` exists with `//go:build darwin` directive on line 1 and declares `package main`.
    - `grep -n "func TestEscapeApplescript" cmd/otto-tray/escapeApplescript_darwin_test.go` returns exactly one match.
    - `cmd/otto-tray/uihelpers_darwin.go` `escapeApplescript` body contains a case for `'\n'` that appends two bytes — verified by `grep -n "'\\\\n'" cmd/otto-tray/uihelpers_darwin.go` (the Go source contains the two-rune literal `'\n'` as a comparison target).
    - `cmd/otto-tray/uihelpers_darwin.go` no longer relies solely on the `'"'`/`'\\'` dispatch — `grep -c "case '\\\\\\\\'" cmd/otto-tray/uihelpers_darwin.go` or equivalent verifies that additional branches exist (executor may convert form; the verification is functional via the test).
    - `go test ./cmd/otto-tray/... -race -count=1` exits 0 with `TestEscapeApplescript` in the PASS list.
    - Direct `Run`-only path of the existing tray code is unaffected — `go vet ./cmd/otto-tray/...` exits 0.
    - `git log --oneline -1` subject matches the regex `^refactor\(20\): close QUAL-01`.
    - The commit contains exactly two changed files (escapeApplescript_darwin_test.go added, uihelpers_darwin.go modified) — verifiable via `git show --stat HEAD`.
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-01 closed: `escapeApplescript` translates raw `\n`/`\r`/`\t` into the two-character AS escape sequences, strips other C0 + DEL, continues escaping `"` and `\\`; table-driven test covers every case in `<behavior>`; committed atomically; CI green.
  </done>
</task>

<task type="execute">
  <name>Task 2: QUAL-02 — Dedup tooltipForState into shared build-tagged file</name>
  <files>cmd/otto-tray/tooltip.go, cmd/otto-tray/uihelpers_darwin.go, cmd/otto-tray/uihelpers_windows.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-03)
    - cmd/otto-tray/uihelpers_darwin.go lines 39–46 (source copy A)
    - cmd/otto-tray/uihelpers_windows.go lines 35–42 (source copy B — verify byte-identical to A before deleting either)
  </read_first>
  <action>
    Per D-20-03: create `cmd/otto-tray/tooltip.go` with `//go:build darwin || windows` on the first line, package `main`. Body imports `fmt` and defines exactly one function:

    `func tooltipForState(state State, detail string) string` with body matching the two existing implementations: `s := fmt.Sprintf("OTTO Gateway · %s", state); if detail != "" { s += " (" + detail + ")" }; return s`.

    Then DELETE both existing implementations:
    - `cmd/otto-tray/uihelpers_darwin.go` lines 39–46 (the function + its `// tooltipForState ...` doc comment).
    - `cmd/otto-tray/uihelpers_windows.go` lines 35–42 (same function + comment).

    Doc comment on the new `tooltip.go` definition: a single line, e.g. `// tooltipForState returns the tray tooltip string for a given FSM state.` (preserved verbatim from existing copies).

    Verify byte-identity before deleting: `diff <(sed -n '39,46p' cmd/otto-tray/uihelpers_darwin.go) <(sed -n '35,42p' cmd/otto-tray/uihelpers_windows.go)` should report only the line-number context if the two function bodies are identical (the read_first reads confirm they are, as of the Phase 20 CONTEXT inventory).

    `imports`: if removing `tooltipForState` leaves `fmt` unused in `uihelpers_darwin.go` or `uihelpers_windows.go`, Go's `goimports`-equivalent will fail compile; check after the move and prune the import only if no other use of `fmt` remains in that file. (Both files have multiple `fmt.Sprintf` call sites in the AS-script construction / Windows balloon path, so `fmt` is expected to remain imported in both — verify.)

    Commit per D-20-09: `refactor(20): close QUAL-02 — dedup tooltipForState into shared build-tag file`. Commit touches exactly three files.
  </action>
  <verify>
    <automated>go build ./cmd/otto-tray/... && GOOS=windows go build ./cmd/otto-tray/... && go vet ./cmd/otto-tray/...</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/otto-tray/tooltip.go` exists with `//go:build darwin || windows` directive on line 1.
    - `grep -n "func tooltipForState" cmd/otto-tray/tooltip.go` returns exactly one match.
    - `grep -n "func tooltipForState" cmd/otto-tray/uihelpers_darwin.go` returns ZERO matches.
    - `grep -n "func tooltipForState" cmd/otto-tray/uihelpers_windows.go` returns ZERO matches.
    - `grep -rn "func tooltipForState" cmd/otto-tray/ | wc -l` reports exactly 1.
    - Darwin build passes: `go build ./cmd/otto-tray/...` exits 0 on darwin host.
    - Cross-build passes: `GOOS=windows go build ./cmd/otto-tray/...` exits 0.
    - `go vet ./cmd/otto-tray/...` exits 0.
    - `git log --oneline -1` subject matches `^refactor\(20\): close QUAL-02`.
    - `git show --stat HEAD` shows exactly three files changed: `tooltip.go` added, `uihelpers_darwin.go` modified, `uihelpers_windows.go` modified.
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-02 closed: `tooltipForState` defined exactly once under `//go:build darwin || windows` in `cmd/otto-tray/tooltip.go`; both prior duplicates deleted; darwin + windows builds clean; CI green.
  </done>
</task>

<task type="execute">
  <name>Task 3: QUAL-03 — Relocate forceCloseCh allocation to RunUntilSignal + add Run-direct regression test</name>
  <files>internal/server/server.go, internal/server/run_direct_test.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-04, D-20-05, `<code_context>` Server constructor pair note, `<specifics>` QUAL-03 nil-channel verification)
    - internal/server/server.go lines 175–230 (field block + New + NewWithCommit)
    - internal/server/server.go lines 254–296 (NewFromConfig — has its own `forceCloseCh: make(chan struct{})` line at 275)
    - internal/server/server.go lines 460–555 (Run's select arm at 473 + RunUntilSignal's force-close site at 542–546)
  </read_first>
  <action>
    Per D-20-04: relocate `forceCloseCh` allocation. The current state (verified by read_first):
    - Field declaration at server.go lines 182–187 (no change to declaration itself — only its doc comment is rewritten per D-20-05).
    - Allocation in `NewWithCommit` at line 208 (`forceCloseCh: make(chan struct{}),`).
    - Allocation in `NewFromConfig` at line 275 (`forceCloseCh: make(chan struct{}),`).
    - (`New` at line 194 delegates entirely to `NewWithCommit` — no direct allocation, no separate fix needed there per `<code_context>` verification.)
    - Select arm in `Run` at line 473 (`case <-s.forceCloseCh:`).
    - Close site in `RunUntilSignal` at lines 542–546.

    Changes:
    1. DELETE `forceCloseCh: make(chan struct{}),` from `NewWithCommit` (current line 208).
    2. DELETE `forceCloseCh: make(chan struct{}),` from `NewFromConfig` (current line 275).
    3. In `RunUntilSignal`, BEFORE the existing `go func() { runErrCh <- s.Run(derivedCtx) }()` block (current ~line 525), insert an allocation: `s.forceCloseCh = make(chan struct{})`. The allocation must happen before `s.Run(derivedCtx)` is invoked, so the goroutine that runs `Run` observes a non-nil channel in its select arm. Place the allocation directly after the comment block introducing the Run-spawn goroutine — exact placement at executor discretion within the constraint that it MUST precede `go func() { runErrCh <- s.Run(derivedCtx) }()`.
    4. REWRITE the field-level doc comment on `forceCloseCh` (current server.go lines 182–187) to state the new contract per D-20-05. Required semantic content (prose at executor discretion per "Claude's Discretion"):
       - "Allocated by `RunUntilSignal` before calling `Run`; nil when `Run` is called directly."
       - "The nil-channel select arm in `Run` is intentional — direct `Run` callers cannot force-close."
       - Brief reminder of what force-close does (mirror the existing WR-08 reference).
    5. OPTIONALLY add an inline pointer comment at the close site (server.go ~line 545, `close(s.forceCloseCh)`) such as `// Allocated above before s.Run was spawned — see field comment.` Executor discretion per D-20-05.

    `Run`'s signature is UNCHANGED. The select arm at line 473 is also UNCHANGED — a nil channel in a `select` case never fires (idiomatic Go), which is exactly the behavior we want for direct-`Run` callers.

    Regression test (per `<specifics>` QUAL-03 nil-channel verification): create `internal/server/run_direct_test.go` (or merge into an existing `_test.go` file at executor discretion — preserve test function name). Function `TestServer_Run_DirectShutdown(t *testing.T)`:
    - Constructs a Server via `New(config.Config{HTTPAddr: "127.0.0.1:0"}, slog.New(...discard...), "test")` (or the most appropriate existing constructor — `NewWithCommit` is fine; `NewFromConfig` works too but requires more config plumbing).
    - Verifies `s.forceCloseCh == nil` immediately after construction (sentinel check — guards against re-introduction of the allocation in constructors).
    - Spawns `go s.Run(ctx)` with a `context.WithCancel`.
    - After a brief sleep / short retry loop to confirm the server is listening (use the existing test util pattern if one exists in `internal/server/`; otherwise dial `s.addr` with a 1s deadline), calls `cancel()`.
    - Asserts `<-done` (channel that receives the Run return value) arrives within 5 seconds with `nil` error or an expected shutdown error consistent with current behavior (mirror whatever existing `Run` tests assert).

    The test MUST run under `-race`. It is the guard rail D-20-04 + `<specifics>` calls for.

    Commit per D-20-09: `refactor(20): close QUAL-03 — relocate forceCloseCh allocation to RunUntilSignal`. Commit touches exactly two files: `server.go` and `run_direct_test.go` (or the existing test file that absorbs the new test function).
  </action>
  <verify>
    <automated>go test ./internal/server/... -run TestServer_Run_DirectShutdown -race -count=3 && go test ./internal/server/... -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "forceCloseCh: make(chan struct{})" internal/server/server.go` returns ZERO matches (allocation no longer in constructor frontmatter).
    - `grep -n "s\\.forceCloseCh = make(chan struct{})" internal/server/server.go` returns exactly ONE match, and that match is inside `RunUntilSignal` (verified by line number being within the `func (s *Server) RunUntilSignal` body).
    - `grep -n "case <-s.forceCloseCh:" internal/server/server.go` returns exactly one match — the existing select arm at the current line ~473 is UNCHANGED.
    - `Run` function signature unchanged: `grep -n "func (s \\*Server) Run(ctx context.Context) error" internal/server/server.go` returns exactly one match.
    - Field-level doc comment on `forceCloseCh` mentions either "RunUntilSignal" AND "nil" — `grep -B5 "forceCloseCh chan struct{}" internal/server/server.go | grep -E "RunUntilSignal|nil" | wc -l` >= 1 (executor prose discretion; the semantic content must be visible to the grep).
    - File `internal/server/run_direct_test.go` exists (or `TestServer_Run_DirectShutdown` is present in an existing `internal/server/*_test.go` file).
    - `grep -rn "func TestServer_Run_DirectShutdown" internal/server/` returns exactly one match.
    - `go test ./internal/server/... -run TestServer_Run_DirectShutdown -race -count=3` exits 0 (3-iteration race check guards against synchronization regression).
    - `go test ./internal/server/... -race -count=1` exits 0 (full server test suite still green).
    - `git log --oneline -1` subject matches `^refactor\(20\): close QUAL-03`.
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-03 closed: `forceCloseCh` allocated only in `RunUntilSignal`; direct-`Run` callers observe `s.forceCloseCh == nil` and rely on the nil-channel select-never idiom; field comment documents the contract; new regression test asserts direct-`Run` shutdown is clean under `-race`; CI green.
  </done>
</task>

<task type="execute">
  <name>Task 4: QUAL-04 — Replace tailLines O(n²) prepend with collect-then-reverse</name>
  <files>cmd/otto-tray/tray.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-06)
    - cmd/otto-tray/tray.go lines 403–423 (current tailLines implementation)
  </read_first>
  <action>
    Per D-20-06: replace the O(n²) `kept = append([]string{t}, kept...)` prepend at `cmd/otto-tray/tray.go:417` with a collect-then-reverse algorithm. Same I/O contract: function returns the last `n` non-empty trimmed lines of `s`, joined with `"\n"`; for `n <= 0` returns `""`.

    New body:
    ```
    if n <= 0 {
        return ""
    }
    lines := strings.Split(s, "\n")
    kept := make([]string, 0, n)
    for i := len(lines) - 1; i >= 0; i-- {
        t := strings.TrimSpace(lines[i])
        if t == "" {
            continue
        }
        kept = append(kept, t)
        if len(kept) >= n {
            break
        }
    }
    // kept is in reverse order (most recent first); reverse in place.
    for i, j := 0, len(kept)-1; i < j; i, j = i+1, j-1 {
        kept[i], kept[j] = kept[j], kept[i]
    }
    return strings.Join(kept, "\n")
    ```

    The reverse-in-place loop is the standard Go two-index swap idiom. Capacity hint stays `n` (D-20-06). I/O is byte-equivalent to today.

    No new tests are required (existing tray tests cover the consumer). If an existing test exercises `tailLines` directly, ensure it still passes — `grep -rn "tailLines(" cmd/otto-tray/` will surface call sites; check before commit.

    Commit per D-20-09: `refactor(20): close QUAL-04 — tailLines collect-then-reverse (drop O(n²) prepend)`. Commit touches exactly one file: `cmd/otto-tray/tray.go`.
  </action>
  <verify>
    <automated>go test ./cmd/otto-tray/... -race -count=1 && go vet ./cmd/otto-tray/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "append(\\[\\]string{t}, kept\\.\\.\\.)" cmd/otto-tray/tray.go` returns ZERO matches (old prepend pattern is gone).
    - `grep -n "kept\\[i\\], kept\\[j\\] = kept\\[j\\], kept\\[i\\]" cmd/otto-tray/tray.go` returns at least one match (new reverse-in-place idiom present).
    - `grep -n "func tailLines" cmd/otto-tray/tray.go` returns exactly one match (signature unchanged).
    - `go test ./cmd/otto-tray/... -race -count=1` exits 0.
    - `GOOS=windows go build ./cmd/otto-tray/...` exits 0 (cross-build still clean since `tray.go` is shared code).
    - `git log --oneline -1` subject matches `^refactor\(20\): close QUAL-04`.
    - `git show --stat HEAD` shows exactly one file changed: `cmd/otto-tray/tray.go`.
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-04 closed: `tailLines` uses collect-then-reverse instead of O(n²) prepend; same I/O behavior; capacity hint unchanged; CI green.
  </done>
</task>

<task type="execute">
  <name>Task 5: QUAL-05 — Delete dead sessions/sessionsMu in regression_rel_pool_02_test.go</name>
  <files>internal/pool/regression_rel_pool_02_test.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-07)
    - internal/pool/regression_rel_pool_02_test.go lines 100–135 (the dead-var block + the write sites)
  </read_first>
  <action>
    Per D-20-07: delete the dead `sessions` / `sessionsMu` machinery from `internal/pool/regression_rel_pool_02_test.go`. Verified write-only state (read_first confirms):
    - Line 109: `sessions := make([]string, 0, 2)`
    - Line 110: `var sessionsMu sync.Mutex`
    - Line 122: `sessionsMu.Lock()`
    - Line 123: `sessions = append(sessions, sid)`
    - Line 124: `sessionsMu.Unlock()`

    Delete those five lines exactly. Nothing else in the test function references `sessions` or `sessionsMu` (verify with `grep -n "sessions\\|sessionsMu" internal/pool/regression_rel_pool_02_test.go` before deleting — only the five line-references above should appear). If `sync` is still imported for `var wg sync.WaitGroup` / `var resultWg sync.WaitGroup` (read_first shows it is — both remain after the deletion), leave the import.

    No behavior change. The Phase 17 workaround documented in surrounding comments (resultWg, per-instance sids) stays intact.

    Commit per D-20-09: `refactor(20): close QUAL-05 — drop dead sessions/sessionsMu vars from REL-POOL-02 test`. Commit touches exactly one file.
  </action>
  <verify>
    <automated>go test ./internal/pool/... -race -count=1 -run TestRELPool02</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sessions\\s*:=\\s*make(\\[\\]string" internal/pool/regression_rel_pool_02_test.go` returns ZERO matches.
    - `grep -n "sessionsMu\\s*sync\\.Mutex" internal/pool/regression_rel_pool_02_test.go` returns ZERO matches.
    - `grep -n "sessionsMu" internal/pool/regression_rel_pool_02_test.go` returns ZERO matches.
    - `grep -nE '\bsessions\b' internal/pool/regression_rel_pool_02_test.go` returns ZERO matches (no stray reads survive).
    - `go test ./internal/pool/... -race -count=1` exits 0 (full pool test suite green; REL-POOL-02 test still passes).
    - `go vet ./internal/pool/...` and `staticcheck ./internal/pool/...` (via `make ci`) exit 0 — no "declared and not used" diagnostics introduced.
    - `git log --oneline -1` subject matches `^refactor\(20\): close QUAL-05`.
    - `git show --stat HEAD` shows exactly one file changed: `internal/pool/regression_rel_pool_02_test.go`, with `-5` net line delta (5 deletions, 0 additions).
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-05 closed: `sessions` / `sessionsMu` declarations + writes deleted; no remaining references in the file; REL-POOL-02 regression still passes under `-race`; CI green.
  </done>
</task>

<task type="execute">
  <name>Task 6: QUAL-06 — Rewrite stale removeSlot comment in respawn_ctx_cancel_test.go</name>
  <files>internal/pool/respawn_ctx_cancel_test.go</files>
  <read_first>
    - .planning/phases/20-code-review-backlog-burn-down/20-CONTEXT.md (D-20-08, Claude's Discretion bullet about describe-or-delete)
    - internal/pool/respawn_ctx_cancel_test.go lines 115–125 (stale comment + the assertion it documents)
  </read_first>
  <action>
    Per D-20-08: the comment at line 119 currently reads (verified by read_first):

    ```
    // CRITICAL ASSERTION — pool size must be unchanged. Before the fix,
    // Size would now be 0 because NewSession's error branch unconditionally
    // called removeSlot.
    ```

    The `removeSlot` function was removed in Phase 17-03 (per STATE.md Decisions: "Plan 17-03 dead-code stale-comment policy: minimize the diff — kept 'removed in Phase 17' annotations rather than full comment removal"). Per D-20-08, executor picks ONE of these two options:

    Option A (describe — preferred when the historical context is load-bearing for future readers):
    Rewrite the comment to describe the CURRENT mechanism. The assertion still tests that `p.Stats().Size == 1` after a cancelled `NewSession`. Rewrite text along these lines (exact prose at executor discretion per Claude's Discretion bullet):

    ```
    // CRITICAL ASSERTION — pool size must be unchanged after a cancelled
    // NewSession. The error branch must re-queue the slot rather than
    // dropping it. (Phase 17-03 removed the unconditional removeSlot call
    // that previously dropped slots in this path; this assertion is the
    // regression guard.)
    ```

    Option B (delete — if the surrounding code reads self-explanatory):
    Delete the three-line comment entirely. The assertion `if got := p.Stats().Size; got != 1 { t.Fatalf(...) }` with its error message already conveys intent.

    Executor MUST choose one option and apply it. No code change, no test addition, no test deletion.

    Commit per D-20-09: `refactor(20): close QUAL-06 — refresh stale removeSlot comment in respawn_ctx_cancel test`. Commit touches exactly one file.
  </action>
  <verify>
    <automated>go test ./internal/pool/... -race -count=1 && go vet ./internal/pool/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "removeSlot" internal/pool/respawn_ctx_cancel_test.go` returns ZERO matches as a LIVE-MECHANISM reference — if Option A was chosen and the comment retains a single historical mention prefixed with "Phase 17-03 removed" (or equivalent), that is acceptable per the Phase 17 dead-code stale-comment policy. Acceptance: any remaining `removeSlot` occurrence must be inside a parenthetical or clause indicating it was REMOVED. Verifiable manually if needed; the automated check is `grep -cE "removeSlot[^_].*(removed|gone|deleted)" internal/pool/respawn_ctx_cancel_test.go` returns a positive count IF Option A was chosen and `removeSlot` survives in the comment; OR `grep -c "removeSlot" internal/pool/respawn_ctx_cancel_test.go` returns 0 if Option B was chosen.
    - `go test ./internal/pool/... -race -count=1` exits 0.
    - `go vet ./internal/pool/...` exits 0.
    - `git log --oneline -1` subject matches `^refactor\(20\): close QUAL-06`.
    - `git show --stat HEAD` shows exactly one file changed: `internal/pool/respawn_ctx_cancel_test.go`. Net diff is comment-only — no `+func`/`-func`, no `+if`/`-if`, no executable-line changes.
    - `make ci` exits 0.
  </acceptance_criteria>
  <done>
    QUAL-06 closed: stale `removeSlot` comment refreshed (described as removed) or deleted; no other changes; CI green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-controlled string → AppleScript dialog body | tray's `notify`/`infoDialog`/`confirmDialog` interpolate text into an `osascript -e` payload; QUAL-01 strengthens the escape applied at this boundary |
| direct `Server.Run` caller vs `RunUntilSignal` caller | the two call paths now have asymmetric `forceCloseCh` semantics (allocated vs nil); QUAL-03 changes the synchronization shape of the Run path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Injection (Tampering) | `cmd/otto-tray/uihelpers_darwin.go` `escapeApplescript` | mitigate | Expand escape set per D-20-01 (handle `\n`/`\r`/`\t` + strip C0/DEL); table-driven unit test in `escapeApplescript_darwin_test.go` covers every transformation. Defense-in-depth; no exploit is known today, but the function backs `//nolint:gosec G204` call sites so the escape contract must be auditable. |
| T-20-02 | Tampering (race / synchronization regression) | `internal/server/server.go` `Run` + `RunUntilSignal` (`forceCloseCh` allocation relocation) | mitigate | Keep `-race` CI gate mandatory; add `TestServer_Run_DirectShutdown` regression test exercising the direct-`Run` path with a nil `forceCloseCh` under `-race -count=3`; field comment documents the new contract so future contributors do not accidentally re-introduce constructor allocation. |
| T-20-03 | (none) | QUAL-02, QUAL-04, QUAL-05, QUAL-06 | accept | All four findings are pure refactors with no security or reliability surface: build-tag file move (QUAL-02), algorithm swap with byte-equivalent I/O (QUAL-04), dead-var deletion (QUAL-05), comment refresh (QUAL-06). No threat model. CI gates (build + race tests + vet + staticcheck) cover regression risk. |

</threat_model>

<verification>

## Phase-level verification

After all six commits land, verify the entire phase by:

1. `make ci` exits 0 — race, vet, staticcheck, gosec, arch-lint all green. This is the load-bearing trust gate (per cross-cutting constraints and CONTEXT.md `<code_context>`).
2. `git log --oneline -6` shows exactly six commits, subjects matching `^refactor\(20\): close QUAL-0[1-6]` in order or any order (the order is `01 → 02 → 03 → 04 → 05 → 06` per the task sequence here, but order does not affect correctness).
3. `GOOS=windows go build ./...` exits 0 — Windows cross-compile remains clean after the tray edits.
4. `go test ./cmd/otto-tray/... -race -count=1` exits 0 (darwin host).
5. `go test ./internal/server/... -race -count=1` exits 0 — including `TestServer_Run_DirectShutdown`.
6. `go test ./internal/pool/... -race -count=1` exits 0 — REL-POOL-02 + respawn_ctx_cancel both still pass.
7. REQUIREMENTS.md traceability rows for QUAL-01..QUAL-06 are flipped from `Open` to `Closed` (executor concern at SUMMARY time, NOT inside a per-QUAL commit — keeps the six commits mechanically pure per D-20-09).

</verification>

<success_criteria>

- All 6 QUAL findings closed via 6 atomic commits with subject form `refactor(20): close QUAL-XX — <summary>` per D-20-09.
- `make ci` exits 0 after every commit (running `make ci` after each commit during execution is NOT required — running it once at phase end is sufficient — but no commit may break CI; if any commit breaks CI, fix forward in the same commit before progressing).
- Behavior preserved except QUAL-01 (defense-in-depth escape-set expansion is the one intentional behavior change).
- `Server.Run` signature unchanged.
- `tooltipForState` defined exactly once across the tray package.
- No new package imports outside of the test file's standard testing imports (`testing` for QUAL-01 test; standard `internal/server` test deps for QUAL-03 test).
- REQUIREMENTS.md traceability rows for QUAL-01..QUAL-06 flipped to `Closed` in the SUMMARY-time commit (not in the per-QUAL commits).

</success_criteria>

<output>

Create `.planning/phases/20-code-review-backlog-burn-down/20-01-SUMMARY.md` when done, listing:
- The six commit hashes (one per QUAL).
- The artifacts produced (new files: `cmd/otto-tray/tooltip.go`, `cmd/otto-tray/escapeApplescript_darwin_test.go`, `internal/server/run_direct_test.go`).
- Confirmation that `make ci` exits 0 at phase end.
- Any deviations from D-20-XX with rationale (none expected — decisions are locked).
- Optional: `git show --stat` snippet for each of the six commits for traceability.

</output>
