---
phase: 01-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .go-arch-lint.yml
  - Makefile
autonomous: false
requirements:
  - TRST-01
  - TRST-02
  - TRST-08

must_haves:
  truths:
    - "make lint exits 0 with zero golangci-lint findings on the complete scaffold"
    - "make ci runs golangci-lint + go test -race + govulncheck and all three pass"
    - "pre-commit install has run and .git/hooks/pre-commit exists and is executable"
    - "pre-commit run --all-files exits 0 on the clean scaffold"
    - "EITHER go-arch-lint runs cleanly (if approved at checkpoint) OR a TODO is recorded in 01-03-SUMMARY.md stating architectural-boundary enforcement is manual until a replacement tool is approved"
    - ".go-arch-lint.yml exists with Phase 1 component declarations and empty dep-rules section — IF go-arch-lint was approved; otherwise this file is created but the arch-lint target is excluded from make ci"
    - "Implements CONTEXT.md decisions D-08 (no hosted CI in Phase 1; local-only trust gates via pre-commit + Makefile), D-09 (pre-commit framework = pre-commit Python; pins golangci-lint, gitleaks, go-mod-tidy-repo, go-vet-repo-mod), D-10 (brief-spirit trust-gate scope: TRST-01 golangci-lint strict + TRST-02 govulncheck + TRST-03 go test -race + TRST-04 go-arch-lint scaffolded with empty ruleset + TRST-05 goleak.VerifyTestMain + TRST-08 pre-commit; TRST-06 + TRST-07 deferred)"
  artifacts:
    - path: ".go-arch-lint.yml"
      provides: "go-arch-lint component declarations for Phase 1 packages; dep rules deferred to Phase 2 (created regardless of checkpoint result; arch-lint target excluded from ci if tool rejected)"
      contains: "version: 3"
    - path: "Makefile"
      provides: "ci target running lint + test-race + govulncheck; arch-lint target (conditional on checkpoint approval)"
  key_links:
    - from: "Makefile ci target"
      to: "$(go env GOPATH)/bin/govulncheck"
      via: "$(shell go env GOPATH)/bin/govulncheck ./... — not on PATH; must use full path"
      pattern: "govulncheck"
    - from: ".go-arch-lint.yml"
      to: "internal/*"
      via: "workdir: internal; component declarations match actual package directories"
      pattern: "workdir: internal"
---

<objective>
Trust gates active: verify go-arch-lint v1.15.0 installs correctly, scaffold .go-arch-lint.yml with Phase 1 components, extend Makefile ci target to include govulncheck, verify pre-commit install ran and .git/hooks/pre-commit exists, and confirm make lint + make ci + pre-commit run --all-files all pass on the completed Phase 1 scaffold.

Purpose: TRST-01, TRST-02, TRST-08 sign-off. This is the phase gate — all trust tools must green-light before Phase 1 is considered complete.
Output: Zero-finding lint pass, clean govulncheck, active pre-commit hooks installed and verified, and .go-arch-lint.yml scaffolded.
</objective>

<execution_context>
@/Users/coreyellis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coreyellis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
@.planning/phases/01-foundations/01-02-SUMMARY.md

<interfaces>
<!-- Key configuration patterns for executor. -->

From RESEARCH.md Pattern 12 — .go-arch-lint.yml:
  version: 3
  workdir: internal
  components:
    acp:
      in: acp/**
    canonical:
      in: canonical/**
    config:
      in: config/**
    server:
      in: server/**
    version:
      in: version/**
    testutil:
      in: testutil/**
  # No deps rules in Phase 1. Phase 2 activates rules for adapter↔canonical↔engine boundaries.

From RESEARCH.md Environment Availability:
  govulncheck: available at $(go env GOPATH)/bin/govulncheck (NOT on PATH)
  go-arch-lint: NOT installed yet; must be installed via go install
  GOPATH/bin: NOT in PATH in dev environment — Makefile must use $(shell go env GOPATH)/bin/ prefix

From RESEARCH.md govulncheck invocation for make ci:
  ci: lint test-race ## Full CI gate (lint + race-tests + vuln scan)
  \t$(shell go env GOPATH)/bin/govulncheck ./...
  (Note: $(shell ...) in recipe evaluates at recipe execution time — correct)

REVIEW FIX (Codex MEDIUM — reproducible install):
  go-arch-lint MUST be installed at the pinned reviewed version, not @latest:
    go install github.com/fe3dback/go-arch-lint@v1.15.0
  This ensures reproducibility: future runs of this plan install the same binary.
  The checkpoint below asks the user to confirm v1.15.0 specifically.

From existing .golangci.yml (verified in RESEARCH.md):
  Already v2 format; linter set matches brief §3.12; no changes needed.
  Test file exclusions active; cmd/*/main.go wrapcheck exclusion active.
  revive exported rule: all exported symbols need godoc — verify this passes after Plan 01+02.

From RESEARCH.md Pitfall 9:
  GOPATH/bin not in PATH. Makefile targets invoking installed tools must use full path:
  $(shell go env GOPATH)/bin/govulncheck
  $(shell go env GOPATH)/bin/go-arch-lint
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    go-arch-lint v1.15.0 is flagged [SUS] in RESEARCH.md Package Legitimacy Audit because v1.15.0 was released 18 days before the audit date (2026-05-23). The package has a 6-year history (v1.0.0: 2020-08-23, 20+ releases), and the SUS flag is likely a false positive from the recent release date. However, per the Package Legitimacy Gate protocol, a blocking human checkpoint is required before installing.

    The executor will install the pinned version: go install github.com/fe3dback/go-arch-lint@v1.15.0 (NOT @latest — pinned per review feedback for reproducibility).

    If you reject this tool, the phase proceeds without go-arch-lint: .go-arch-lint.yml is still created (for future use) but the arch-lint target is excluded from make ci. The 01-03-SUMMARY.md will record a TODO: "Architectural-boundary enforcement is manual until a replacement tool is approved." TRST-01, TRST-02, TRST-08 remain satisfiable without go-arch-lint.
  </what-built>
  <how-to-verify>
    Before the executor runs `go install github.com/fe3dback/go-arch-lint@v1.15.0`, please verify the package:

    1. Visit: https://pkg.go.dev/github.com/fe3dback/go-arch-lint
    2. Confirm: published by fe3dback, version history goes back to 2020, v1.15.0 is listed.
    3. Visit: https://github.com/fe3dback/go-arch-lint
    4. Confirm: active GitHub repository, recent commits match v1.15.0 release, no suspicious ownership changes.

    If the package looks legitimate (expected: YES — this is a false-positive SUS flag), type "approved" to proceed with installation of v1.15.0.
    If anything looks wrong (e.g., the package was recently transferred to a new owner, or has suspicious recent commits), type "reject" and the executor will skip go-arch-lint entirely and record the manual-enforcement TODO.
  </how-to-verify>
  <resume-signal>Type "approved" to install go-arch-lint@v1.15.0 and proceed. Type "reject" to skip go-arch-lint (make ci will exclude the arch-lint check; a TODO will be recorded in the SUMMARY).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install go-arch-lint@v1.15.0, scaffold .go-arch-lint.yml, extend Makefile ci target, verify pre-commit install, and run full trust-gate suite</name>
  <files>
    .go-arch-lint.yml,
    Makefile
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (D-08, D-09, D-10, TRST-01 through TRST-08 scope)
    - .planning/phases/01-foundations/01-RESEARCH.md (Pattern 12, govulncheck invocation, Environment Availability table, Pitfall 9, golangci-lint v2 Config Reference section)
    - Makefile (current state after Plan 01 edits — ci target may already exist; verify before adding)
    - .golangci.yml (verify version: "2" and linter set; no edits expected)
    - .pre-commit-config.yaml (verify pre-commit hooks config; run pre-commit install if not already done)
  </read_first>
  <action>
    Step 1 — Install go-arch-lint (only if checkpoint approved):
    - Run: go install github.com/fe3dback/go-arch-lint@v1.15.0
      (REVIEW FIX: pinned to @v1.15.0, NOT @latest, for reproducibility)
    - Verify binary exists: $(go env GOPATH)/bin/go-arch-lint --version
    - Expected output should mention v1.15.0.

    Step 2 — Create .go-arch-lint.yml (always, regardless of checkpoint result):
    - Use the exact YAML from RESEARCH.md Pattern 12:
      version: 3
      workdir: internal
      components: acp, canonical, config, server, version, testutil (each with in: <name>/**)
      No deps rules section. Comment: "# No deps rules in Phase 1. Phase 2 activates rules."
    - If approved: verify it parses: $(go env GOPATH)/bin/go-arch-lint check --project-path . exits 0
      (Empty ruleset should produce zero violations.)
    - If rejected: create the file anyway (it is referenced in future phases) but do NOT run go-arch-lint.

    Step 3 — Verify/update Makefile ci target:
    - The ci target was added in Plan 01. Verify it exists with: grep -v '^#' Makefile | grep -c "^ci:".
    - If missing: add it now following RESEARCH.md govulncheck invocation pattern.
    - If approved: add arch-lint target and include in ci:
        arch-lint: ## Check architecture boundaries (requires go-arch-lint@v1.15.0)
          $(shell go env GOPATH)/bin/go-arch-lint check --project-path .
        ci: lint test-race arch-lint ## Full CI gate (lint + race-tests + govulncheck + arch-lint)
          $(shell go env GOPATH)/bin/govulncheck ./...
    - If rejected: ci target stays: ci: lint test-race without arch-lint.
      Add arch-lint target marked as optional (comment in Makefile: "# arch-lint not active — pending tool approval"):
        arch-lint: ## Check architecture boundaries (NOT in ci — pending go-arch-lint approval)
          @echo "go-arch-lint not approved. Architectural boundary checks are manual."
    - govulncheck must use full path: $(shell go env GOPATH)/bin/govulncheck ./...

    Step 4 — Verify pre-commit hooks are installed (REVIEW FIX — Codex MEDIUM):
    - Run: ls -la .git/hooks/pre-commit
    - If NOT present or not executable: run pre-commit install (TRST-08 requires this).
    - After install, verify: test -x .git/hooks/pre-commit && echo "pre-commit hook installed"
    - This must pass before proceeding to Step 5.

    Step 5 — Run full trust-gate suite and fix any findings:
    - Run make lint → if any golangci-lint findings, fix them in the relevant source files.
    - Common issues to expect:
      * revive exported: any exported symbol missing godoc in Plans 01+02 files.
      * errcheck: any error return not checked.
      * wrapcheck: any error from external package returned unwrapped.
      * noctx: any HTTP handler missing ctx propagation.
    - For each finding: fix the source file (not the lint config), re-run make lint, iterate until zero findings.
    - Run make test-race → verify exits 0.
    - Run $(go env GOPATH)/bin/govulncheck ./... → verify exits 0 (should be clean on fresh deps).
    - Run make ci → verify all steps pass together.

    Step 6 — Verify pre-commit run --all-files:
    - Run: pre-commit run --all-files
    - This exercises gitleaks, golangci-lint, shellcheck (on scripts/loop24), go-mod-tidy, trailing-whitespace, end-of-file-fixer, check-yaml (on .go-arch-lint.yml).
    - Fix any pre-commit failures (most likely: missing trailing newline on new files, shellcheck on scripts/loop24, check-yaml on .go-arch-lint.yml).

    If go-arch-lint was rejected at checkpoint, record in 01-03-SUMMARY.md under a "Deferred Gates" section:
      "Architectural-boundary enforcement (TRST-04 / go-arch-lint): go-arch-lint@v1.15.0 was rejected at the supply-chain checkpoint. Architectural boundary checks are manual until a replacement tool is approved. TODO: re-evaluate in Phase 2 when adapter↔canonical↔engine boundaries are established."

    Note on linting scope: if golangci-lint finds issues in files created in Plans 01+02, fix those source files here. The trust-gate plan is responsible for achieving zero-findings across the entire Phase 1 codebase.
  </action>
  <verify>
    <automated>make ci 2>&1 | tail -40; echo "make ci exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - make lint exits 0: golangci-lint run ./... produces no output (zero findings)
    - make test-race exits 0: go test -race ./... passes all tests
    - make ci exits 0: lint + test-race + govulncheck all pass in sequence
    - .go-arch-lint.yml exists with version: 3 and workdir: internal: grep -c "version: 3" .go-arch-lint.yml >= 1 && grep -c "workdir: internal" .go-arch-lint.yml >= 1
    - If approved: go-arch-lint check --project-path . exits 0: $(go env GOPATH)/bin/go-arch-lint check --project-path . exits 0; installed version is v1.15.0: $(go env GOPATH)/bin/go-arch-lint --version | grep -c "1.15.0" >= 1
    - If rejected: 01-03-SUMMARY.md contains "Architectural-boundary enforcement" TODO
    - pre-commit hook is installed: test -x .git/hooks/pre-commit exits 0
    - pre-commit run --all-files exits 0 on clean scaffold
    - govulncheck passes: $(go env GOPATH)/bin/govulncheck ./... exits 0
  </acceptance_criteria>
  <done>
    make lint exits 0. make ci exits 0 (lint + test-race + govulncheck). .go-arch-lint.yml exists with Phase 1 components. pre-commit install verified (.git/hooks/pre-commit is executable). pre-commit run --all-files exits 0. All TRST-01, TRST-02, TRST-08 requirements satisfied. go-arch-lint either runs cleanly at v1.15.0 (if approved) or a manual-enforcement TODO is recorded (if rejected).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → go install | go install downloads go-arch-lint binary from pkg.go.dev proxy; supply chain risk mitigated by human checkpoint |
| pre-commit hook → shell | pre-commit executes golangci-lint and gitleaks against staged files; runs in dev environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | go install github.com/fe3dback/go-arch-lint@v1.15.0 | mitigate | Blocked by checkpoint:human-verify (SUS package flag from slopcheck). User must verify pkg.go.dev and GitHub before install proceeds. Version pinned to @v1.15.0 (not @latest) for reproducibility. |
| T-03-02 | Denial of Service | govulncheck fails on transitive CVE in dep | mitigate | govulncheck uses semantic call-graph analysis; flags only reachable vulnerabilities. If found, triage the specific CVE before Phase 1 close. |
| T-03-03 | Tampering | gitleaks false-positive on test fixture strings | accept | gitleaks runs on staged files only. If a test fixture contains a token-like string, add it to .gitleaks.toml allowlist with justification. |
| T-03-SC | Tampering | go install go-arch-lint@v1.15.0 | mitigate | Human checkpoint task above gates this install. Pinned version reduces risk of future supply-chain compromise via @latest tag movement. |
</threat_model>

<verification>
After Plan 03 completes:

1. `make lint` exits 0 — zero golangci-lint findings.
2. `make ci` exits 0 — lint + test-race + govulncheck all pass.
3. `test -x .git/hooks/pre-commit` exits 0 — pre-commit hook is installed and executable.
4. `pre-commit run --all-files` exits 0 on the clean Phase 1 scaffold.
5. `.go-arch-lint.yml` exists with `version: 3` and `workdir: internal`.
6. If approved: `$(go env GOPATH)/bin/go-arch-lint --version | grep "1.15.0"` exits 0; `go-arch-lint check --project-path .` exits 0.
7. If rejected: 01-03-SUMMARY.md records manual-enforcement TODO.
8. Phase 1 success criteria 2, 3, and 5 (from ROADMAP.md) are all satisfied.
</verification>

<success_criteria>
- TRST-01: make lint exits 0 with zero golangci-lint findings
- TRST-02: govulncheck exits 0 (make ci exercises this)
- TRST-08: pre-commit install ran; .git/hooks/pre-commit is executable; pre-commit run --all-files exits 0 on clean scaffold
- .go-arch-lint.yml scaffolded with Phase 1 components (dep rules activate Phase 2)
- make ci runs lint + test-race + govulncheck in sequence, all pass
- go-arch-lint pinned to v1.15.0 if approved (not @latest)
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-03-SUMMARY.md` when done.
</output>
