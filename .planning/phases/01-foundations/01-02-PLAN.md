---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/acp/framer.go
  - internal/acp/dispatcher.go
  - internal/acp/client.go
  - internal/acp/translate.go
  - internal/acp/stream.go
  - internal/acp/framer_test.go
  - internal/acp/dispatcher_test.go
  - internal/acp/client_test.go
  - internal/acp/integration_test.go
  - internal/acp/testmain_test.go
  - cmd/loop24-gateway/main.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - ACP-01
  - ACP-02
  - ACP-03
  - ACP-04
  - ACP-05
  - ACP-06
  - TRST-03

must_haves:
  truths:
    - "acp.New(cfg) spawns kiro-cli subprocess and returns a connected Client"
    - "acp.NewWithConn(rwc, cfg) accepts a pre-built io.ReadWriteCloser (Phase 5 pool path)"
    - "Client.Initialize(ctx) sends initialize JSON-RPC and returns without error"
    - "Client.NewSession(ctx, cwd) sends session/new and returns a session ID"
    - "Client.Ping(ctx) sends ping JSON-RPC; failed ping does not hang"
    - "session/request_permission notification auto-grants with {optionId:'allow_always',granted:true} (ACP-04)"
    - "session/update and _kiro.dev/session/update notifications translate to typed canonical.Chunk values (ACP-05)"
    - "Client.Close() drains goroutines and reaps subprocess without goroutine leaks"
    - "goleak.VerifyTestMain passes in internal/acp package — no goroutine leaks after Close()"
    - "Integration test spawns real kiro-cli, completes initialize+session/new+ping+auto-grant, skips cleanly when kiro-cli absent"
    - "SetModel issues session/set_model JSON-RPC request with {sessionId, modelId} params and waits on the response with context cancellation"
    - "go test -race ./internal/acp/... exits 0"
    - "Implements CONTEXT.md decisions D-01 (single internal/acp package with file-scoped layers framer.go + dispatcher.go + client.go), D-02 (context-first cancellation; ctx as first arg on every Client method), D-03 (Stream handle from day 1; Chunks channel + Result()), D-04 (translation lives inside internal/acp; imports internal/canonical), D-05 (Config-struct constructor, not functional options), D-06 (dual constructors: New spawns subprocess, NewWithConn accepts io.ReadWriteCloser), D-07 (Close semantics: cancel + drain + Wait; idempotent via sync.Once), D-18 (hybrid whitebox + blackbox test layout — package acp for unit, package acp_test for integration)"
  artifacts:
    - path: "internal/acp/framer.go"
      provides: "NDJSON framer: newFramer(r, w), readFrame() json.RawMessage, writeFrame(v any)"
      contains: "func newFramer"
    - path: "internal/acp/dispatcher.go"
      provides: "rpcFrame type, dispatcher struct, register/cancel/route methods"
      contains: "func (d *dispatcher) route"
    - path: "internal/acp/client.go"
      provides: "Client struct, Config struct, New, NewWithConn, Initialize, NewSession, Ping, Prompt, Cancel, Close"
      exports: ["Client", "Config", "Stream", "New", "NewWithConn", "SetModel"]
    - path: "internal/acp/translate.go"
      provides: "translateUpdate(sessionUpdateParams) canonical.Chunk"
      contains: "func translateUpdate"
    - path: "internal/acp/stream.go"
      provides: "Stream struct with Chunks channel and Result() method"
      exports: ["Stream", "FinalResult"]
    - path: "internal/acp/testmain_test.go"
      provides: "goleak.VerifyTestMain gate for the entire acp package test suite"
      contains: "goleak.VerifyTestMain"
    - path: "internal/acp/integration_test.go"
      provides: "Blackbox integration test gated by kiro-cli PATH detection (D-17)"
      contains: "resolveKiroCLI"
  key_links:
    - from: "internal/acp/client.go"
      to: "internal/acp/dispatcher.go"
      via: "dispatcher.route called by readLoop goroutine; notifications go to handleNotification"
      pattern: "c\\.disp\\.route"
    - from: "internal/acp/client.go"
      to: "internal/acp/translate.go"
      via: "translateUpdate called inside handleNotification for session/update frames"
      pattern: "translateUpdate"
    - from: "internal/acp/client.go"
      to: "internal/canonical"
      via: "canonical.Chunk produced by translate and pushed to activeStream"
      pattern: "canonical\\.Chunk"
    - from: "internal/acp/client.go"
      to: "os/exec"
      via: "exec.Command(cfg.Command, cfg.Args...) — gosec G204 nolint annotation required"
      pattern: "exec\\.Command"
---

<objective>
ACP client core: implement the three-file JSON-RPC-over-stdio client (framer + dispatcher + client) plus chunk translator and stream type, with full unit and integration test coverage. After this plan, the integration test spawns a real kiro-cli subprocess and completes a full ACP protocol exchange without goroutine leaks.

Purpose: Phase 1 success criterion #4 — the ACP client must work against a live kiro-cli. This unblocks Phase 2 (first real Ollama end-to-end).
Output: internal/acp/* fully implemented and tested; main.go updated to wire the ACP client; goroutine leak gate active.
</objective>

<execution_context>
@/Users/coreyellis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coreyellis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.planning/phases/01-foundations/01-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs. Read RESEARCH.md and PATTERNS.md for full code excerpts. -->

From internal/canonical/chunk.go (created in Plan 01):
  type ChunkKind int
  const ChunkKindText, ChunkKindThought, ChunkKindToolCall, ChunkKindPlan ChunkKind = iota...
  type Chunk struct { Kind ChunkKind; Text *TextChunk; Thought *ThoughtChunk; ToolCall *ToolCallChunk; Plan *PlanChunk }
  type TextChunk struct { Content string }
  type ThoughtChunk struct { Content string }
  type ToolCallChunk struct { Name string; Args map[string]any }
  type PlanChunk struct { Content string }

From RESEARCH.md Pattern 1 — framer:
  type framer struct { scanner *bufio.Scanner; enc *json.Encoder; mu sync.Mutex }
  newFramer(r io.Reader, w io.Writer) *framer — sc.Buffer(64KB, 1MB) MANDATORY
  readFrame() (json.RawMessage, error) — MUST copy scanner.Bytes() before returning
  writeFrame(v any) error — json.Encoder appends \n; protected by f.mu

From RESEARCH.md Pattern 2 — dispatcher:
  type rpcFrame struct { ID *uint64 `json:"id,omitempty"`; Method string; Result json.RawMessage; Error *rpcError; Params json.RawMessage }
  type rpcError struct { Code int `json:"code"`; Message string `json:"message"` }
  type dispatcher struct { mu sync.Mutex; pending map[uint64]chan<- rpcFrame; onNotif func(rpcFrame) }
  register(id uint64) <-chan rpcFrame — buffered-1 channel
  cancel(id uint64) — deletes from pending under mu
  route(frame rpcFrame) — checks ID==nil FIRST; nil→onNotif; non-nil→lookup+delete under same mu

From RESEARCH.md Pattern 3 — client skeleton:
  type Client struct { cfg Config; framer *framer; disp *dispatcher; wg sync.WaitGroup; cancel context.CancelFunc; closeOnce sync.Once; stdin io.WriteCloser; cmd *exec.Cmd; nextID atomic.Uint64; activeStream *Stream; streamMu sync.Mutex }
  newClient(rwc io.ReadWriteCloser, cfg Config) *Client — starts readLoop + pingLoop goroutines (wg.Add(2))
  readLoop(ctx context.Context) — defer wg.Done(); loops on readFrame → unmarshal → disp.route
  Close() — closeOnce.Do: cancel() → stdin.Close() → wg.Wait() → cmd.Wait() (ORDER IS MANDATORY)

From RESEARCH.md Pattern 4 — Prompt:
  func (c *Client) Prompt(ctx context.Context, sessionID string, blocks []canonical.Block) (*Stream, error)
  Register id in dispatcher; writeFrame the request; select { case <-ctx.Done(): cancel+send session/cancel; case resp := <-respCh: return newStream or error }

From RESEARCH.md Pattern 5 — handleNotification:
  switch frame.Method { case "session/request_permission": auto-grant; case "session/update", "_kiro.dev/session/update": translateUpdate → push to activeStream }
  CRITICAL: session/request_permission auto-grant uses nextID.Add(1) for the grant request ID.

From RESEARCH.md Pattern 6 — subprocess spawn:
  exec.Command(cfg.Command, cfg.Args...) — gosec G204 annotation required:
    //nolint:gosec // G204: kiro-cli command is env-var config; not user-controlled HTTP input
  cmd.Stderr = os.Stderr; cmd.Dir = cfg.Cwd; cmd.Env = append(os.Environ(), cfg.Env...)
  stdin, _ := cmd.StdinPipe(); stdout, _ := cmd.StdoutPipe(); cmd.Start()

From RESEARCH.md ACP Protocol Reference — outbound wire shapes:
  initialize: {"jsonrpc":"2.0","id":1,"method":"initialize","params":{"clientInfo":{"name":"loop24-gateway","version":"0.0.0"},"capabilities":{}}}
  session/new: {"jsonrpc":"2.0","id":2,"method":"session/new","params":{"cwd":"/path"}}
  ping: {"jsonrpc":"2.0","id":3,"method":"ping","params":{}}
  session/cancel NOTIFICATION (no id): {"jsonrpc":"2.0","method":"session/cancel","params":{"sessionId":"<uuid>"}}
  session/grant_permission: {"jsonrpc":"2.0","id":5,"method":"session/grant_permission","params":{"requestId":"<id>","optionId":"allow_always","granted":true}}

From RESEARCH.md Canonical Chunk Translation table:
  session/update type "text" → canonical.ChunkKindText, TextChunk{Content: u.Content}
  session/update type "thought" → canonical.ChunkKindThought, ThoughtChunk{Content: u.Content}
  session/update type "tool_call" → canonical.ChunkKindToolCall, ToolCallChunk{Name: u.ToolName, Args: u.Args}
  session/update type "plan" → canonical.ChunkKindPlan, PlanChunk{Content: u.Content}
  Unknown type → ChunkKindText (to avoid data loss)

From RESEARCH.md D-17 + D-18 + PATTERNS.md integration_test.go:
  package acp_test (blackbox); resolveKiroCLI(t) checks LOOP24_KIRO_BIN then exec.LookPath("kiro-cli"); t.Skip if neither found
  Test sequence: acp.New(cfg) → Initialize → NewSession → Ping → (trigger session/update if possible) → Close

From RESEARCH.md Pattern 10 — testmain:
  package acp (not acp_test); func TestMain(m *testing.M) { goleak.VerifyTestMain(m) }
  D-18: testmain_test.go is package acp (whitebox); covers all test files in directory.

From RESEARCH.md D-05 — Config struct:
  type Config struct { Logger *slog.Logger; Command string; Args []string; Cwd string; Env []string; PingInterval time.Duration }
  Required: Logger. Defaults: Command="kiro-cli", Args=[]string{"acp"}, PingInterval=60s.

SetModel method (ACP-03 — added per B2 checker fix):
  SetModel(ctx context.Context, sessionID, modelID string) error
  Wire shape: {"jsonrpc":"2.0","id":N,"method":"session/set_model","params":{"sessionId":"<uuid>","modelId":"<model>"}}
  Follows same request/await pattern as session/new: register id in dispatcher, writeFrame, select on respCh or ctx.Done.

From RESEARCH.md D-06 — dual constructors:
  New(cfg Config) (*Client, error) — spawns subprocess; errors on spawn failure.
  NewWithConn(rwc io.ReadWriteCloser, cfg Config) *Client — no subprocess; used by Phase 5 pool.

Sentinel errors (Claude's discretion per CONTEXT.md):
  var ErrSessionClosed = errors.New("acp: session closed")
  var ErrSubprocessExited = errors.New("acp: subprocess exited")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Framer, dispatcher, translate, stream — low-level ACP primitives with unit tests</name>
  <files>
    internal/acp/framer.go,
    internal/acp/dispatcher.go,
    internal/acp/translate.go,
    internal/acp/stream.go,
    internal/acp/framer_test.go,
    internal/acp/dispatcher_test.go,
    internal/acp/testmain_test.go
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-07, D-18, D-19)
    - .planning/phases/01-foundations/01-RESEARCH.md (Pattern 1, Pattern 2, Pattern 10, Pattern 11, Canonical Chunk Translation table, Pitfalls 3 and 4, ACP Protocol Reference)
    - .planning/phases/01-foundations/01-PATTERNS.md (internal/acp/framer.go, dispatcher.go, translate.go, testmain_test.go sections)
    - internal/canonical/chunk.go (types needed by translate.go)
    - internal/testutil/testutil.go (Logger(t) needed by tests)
  </read_first>
  <behavior>
    - framer_test.go: TestFramerRoundtrip writes a JSON object to a bytes.Buffer, calls readFrame, verifies the raw bytes match; TestFramerMultipleFrames writes 5 frames and reads them back in order; TestFramerLargeFrame writes a frame >64KB (default scanner limit) and verifies it is read without error (confirms sc.Buffer size override is working); TestFramerEOF verifies readFrame returns io.EOF when reader is closed; TestFramerCopySemantics writes two frames and verifies they are independent byte slices (not the same backing array)
    - dispatcher_test.go: TestDispatcherRoute verifies that a frame with a nil ID goes to onNotif; TestDispatcherPending verifies register → route delivers frame to channel; TestDispatcherCancel verifies cancel removes the pending entry so a routed frame is silently dropped; TestDispatcherConcurrent runs 10 goroutines each registering an ID and routes responses concurrently — go test -race must not flag this
    - translate_test.go: TestTranslateText/Thought/ToolCall/Plan/Unknown verifies each mapping
    - stream.go: Stream struct with Chunks <-chan canonical.Chunk and a done channel; push(chunk canonical.Chunk) method; close() method; Result() (*FinalResult, error) method blocks until done channel closed
    - testmain_test.go: package acp (whitebox); TestMain(m *testing.M) { goleak.VerifyTestMain(m) }
  </behavior>
  <action>
    Create framer.go, dispatcher.go, translate.go, stream.go, framer_test.go, dispatcher_test.go, testmain_test.go.

    framer.go (package acp, unexported types):
    - newFramer(r io.Reader, w io.Writer) *framer sets sc.Buffer(make([]byte, 64*1024), 1024*1024).
    - readFrame MUST copy scanner.Bytes() before returning — scanner reuses its buffer (Pitfall 4).
    - writeFrame must lock f.mu around enc.Encode (protect concurrent writes).
    - No exported symbols — all framer types are unexported.

    dispatcher.go (package acp, unexported types):
    - register creates buffered-1 channel (buffered so route never blocks even if caller gone).
    - route: check frame.ID == nil FIRST. If nil → onNotif(frame). Else acquire mu, lookup+delete (atomic: read+delete under same lock per Pitfall 3), release mu, send to channel if found.
    - cancel: mu.Lock, delete, mu.Unlock.
    - Do NOT use sync.Map — use sync.Mutex + map[uint64]chan<- rpcFrame (Claude's discretion from CONTEXT.md + RESEARCH.md).

    translate.go (package acp):
    - sessionUpdateParams struct with fields: SessionID string, Type string, Content string, ToolName string (for tool_call), Args map[string]any (for tool_call).
    - translateUpdate(u sessionUpdateParams) canonical.Chunk — switch on u.Type.
    - Unknown type falls back to canonical.ChunkKindText to avoid data loss.
    - permissionParams struct: RequestID string, Permission json.RawMessage.
    - grantParams struct: RequestID string, OptionID string, Granted bool — maps to optionId/granted in JSON.

    stream.go (package acp, exported):
    - Stream struct: Chunks <-chan canonical.Chunk (send side held internally); done chan struct{}; result *FinalResult; err error; mu sync.Mutex.
    - newStream(ctx context.Context, sessionID string) *Stream — buffered channel (e.g., 64 slots).
    - push(chunk canonical.Chunk) — non-blocking send with select; if full, log and drop.
    - close(result *FinalResult, err error) — stores result+err under mu; closes done channel; closes Chunks channel.
    - Result() (*FinalResult, error) — blocks on done channel; returns stored result+err.
    - FinalResult struct (exported): SessionID string, ChunkCount int (or similar minimal shape).

    testmain_test.go: package acp (WHITEBOX, NOT acp_test). Exactly:
      package acp
      import ("os" "testing" "go.uber.org/goleak")
      func TestMain(m *testing.M) { goleak.VerifyTestMain(m) }
    Note: This will cover goroutines from BOTH whitebox (package acp) and blackbox (package acp_test) test files in the same directory.

    Tests: use bytes.Buffer and io.Pipe for framer tests (no subprocess needed). Use testutil.Logger(t) where a logger is needed.
    wrapcheck: all errors from stdlib/external packages must be wrapped with fmt.Errorf("acp: context: %w", err).
    errcheck: all return values must be checked. In tests, use require-style: if err != nil { t.Fatal(err) }.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/Projects/repos/local/loop24-gateway && go test -race ./internal/acp/... -run "TestFramer|TestDispatcher|TestTranslate|TestStream" -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    framer.go, dispatcher.go, translate.go, stream.go, testmain_test.go, framer_test.go, dispatcher_test.go all exist and compile. go test -race ./internal/acp/... -run "TestFramer|TestDispatcher|TestTranslate" exits 0. goleak.VerifyTestMain finds no goroutine leaks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACP Client with goroutine lifecycle, exported RPC methods, and integration test</name>
  <files>
    internal/acp/client.go,
    internal/acp/client_test.go,
    internal/acp/integration_test.go,
    cmd/loop24-gateway/main.go,
    go.mod,
    go.sum
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (D-01 through D-07, D-17, D-18, D-22)
    - .planning/phases/01-foundations/01-RESEARCH.md (Patterns 3, 4, 5, 6; Pitfalls 1, 2, 7; ACP Protocol Reference outbound shapes; D-06 dual constructors; govulncheck install note)
    - .planning/phases/01-foundations/01-PATTERNS.md (internal/acp/client.go section including gosec G204 annotation and Close() shutdown order)
    - internal/acp/framer.go (just written — newFramer signature)
    - internal/acp/dispatcher.go (just written — dispatcher struct, register/cancel/route)
    - internal/acp/stream.go (just written — Stream, newStream, push, close, Result)
    - internal/acp/translate.go (just written — permissionParams, sessionUpdateParams, grantParams)
    - internal/canonical/chunk.go (Chunk type for Prompt and stream)
    - internal/config/config.go (Config.KiroCmd, KiroArgs, KiroCWD, PingInterval fields)
    - internal/version/version.go (version.Version for initialize clientInfo)
    - cmd/loop24-gateway/main.go (current Plan 01 wiring — add acp.New call)
    - .planning/phases/01-foundations/01-01-SUMMARY.md (Plan 01 output — what files exist)
  </read_first>
  <behavior>
    - client_test.go: TestNewWithConn creates a Client via NewWithConn with an io.Pipe-backed ReadWriteCloser; simulates an initialize response from the write side; verifies Initialize returns nil error; verifies Close() returns nil; goleak.VerifyNone(t) at end. TestCloseIdempotent calls Close() twice; verifies no panic or hang. TestPingTimeout calls Ping with a cancelled context; verifies error is context.Canceled. TestAutoGrantPermission simulates a session/request_permission notification arriving; verifies a grant_permission request is written to the pipe.
    - client_test.go: TestSetModel sends a session/set_model request via a mock dispatcher (io.Pipe-backed ReadWriteCloser); simulates a success result response; verifies SetModel returns nil error. TestSetModelCancelledContext calls SetModel with a pre-cancelled context; verifies error is context.Canceled.
    - integration_test.go: TestIntegration_FullCycle skips if kiro-cli absent; creates acp.New with real kiro-cli; calls Initialize → NewSession → Ping; verifies no error; calls Close; goleak.VerifyNone(t) verifies no leaks.
  </behavior>
  <action>
    Create client.go. Key constraints in order of criticality:

    1. gosec G204 MUST have inline justification comment on exec.Command line:
       //nolint:gosec // G204: kiro-cli command is env-var config; not user-controlled HTTP input
       (The golangci-lint config in .golangci.yml has wrapcheck and gosec active; without this comment, CI fails.)

    2. Close() shutdown order is non-negotiable (Pitfall 2 + Pitfall 7):
       c.closeOnce.Do(func() {
           c.cancel()              // 1. signal goroutines
           if c.stdin != nil { c.stdin.Close() }  // 2. EOF → subprocess → readLoop unblocks
           c.wg.Wait()            // 3. BEFORE cmd.Wait — drain reader+ping goroutines
           if c.cmd != nil { c.cmd.Wait() }  // 4. reap subprocess
       })

    3. handleNotification for session/request_permission (ACP-04 — Pitfall 1):
       - This arrives as a JSON-RPC notification (no id field). It goes through dispatcher.onNotif, NOT the pending map.
       - Auto-grant response: rpcRequest with nextID.Add(1) and method "session/grant_permission".
       - grantParams JSON: {"requestId":"...","optionId":"allow_always","granted":true}.
       - CRITICAL: optionId not optionID — match exact JSON field name from RESEARCH.md ACP Protocol Reference.

    4. readLoop: defer wg.Done() at top. Loop: readFrame → if err return (EOF = normal exit). Unmarshal to rpcFrame → disp.route. Log malformed frames with Warn but continue (don't kill session).

    5. pingLoop(ctx context.Context): defer wg.Done(). ticker := time.NewTicker(cfg.PingInterval). Loop: select { case <-ticker.C: call Ping with a short timeout ctx (e.g., 10s); if Ping errors, log and return (pool replaces dead slot in Phase 5). case <-ctx.Done(): return }.

    6. Initialize(ctx context.Context) error: send JSON-RPC "initialize" with params {"clientInfo":{"name":"loop24-gateway","version":"0.0.0"},"capabilities":{}}. Wait for response. Ignore result contents (informational per RESEARCH.md OQ-2).

    7. NewSession(ctx context.Context, cwd string) (string, error): send "session/new" with params {"cwd":cwd}. Unmarshal result to get sessionId field.

    8. Ping(ctx context.Context) error: send "ping" with params {}. Tolerate method-not-found error response (some kiro-cli versions may return that per RESEARCH.md ACP Protocol Reference comment).

    9. rpcRequest struct (unexported): JSONRPC string, ID uint64, Method string, Params any.
       rpcNotification struct (unexported): JSONRPC string, Method string, Params any.
       Use these for writeFrame calls; json.Encoder marshals them to correct wire format.

    10. activeStream management: when Prompt is called, set c.activeStream under c.streamMu.
        When stream is closed (all chunks received or error), clear c.activeStream.
        Phase 1 supports one in-flight Prompt at a time; Phase 5 pool enforces this at the slot level.

    11. newClient wires framer from rwc — for io.ReadWriteCloser, use newFramer(rwc, rwc).
        For subprocess: newFramer(stdout, stdin) where stdout=cmd.StdoutPipe() and stdin=cmd.StdinPipe().
        Store stdin as io.WriteCloser on Client struct for Close() to close it.

    Update cmd/loop24-gateway/main.go to add acp.New call:
    - Construct acp.Config{Logger: logger, Command: cfg.KiroCmd, Args: cfg.KiroArgs, Cwd: cfg.KiroCWD, PingInterval: cfg.PingInterval}.
    - acpClient, err := acp.New(acpCfg); if err != nil, log error and os.Exit(1).
    - defer func() { if err := acpClient.Close(); err != nil { logger.Error("acp close", "err", err) } }().
    - Pass acpClient to server.New() as a parameter (or hold it in main for shutdown ordering). For Phase 1, main.go owns the lifecycle; server.RunUntilSignal returns on signal, then defer fires Close().

    Install dependencies before writing imports:
    - go get github.com/go-chi/chi/v5@v5.3.0 (already added in Plan 01 if not done yet)
    - go get go.uber.org/goleak@v1.3.0
    - Also go install golang.org/x/vuln/cmd/govulncheck@latest (for make ci; but this is a CLI install, not a go.mod dep)
    Both go.mod and go.sum must be committed with the code.

    integration_test.go:
    - package acp_test (blackbox).
    - resolveKiroCLI(t *testing.T) string — check LOOP24_KIRO_BIN env first; then exec.LookPath("kiro-cli"); t.Skip("kiro-cli not found...") if neither found.
    - TestIntegration_FullCycle: construct acp.Config with resolved binary path as Command; acp.New(cfg); Initialize(ctx); NewSession(ctx, "/tmp"); Ping(ctx); acpClient.Close(); goleak.VerifyNone(t).
    - TestIntegration_AutoGrant: spawns real kiro-cli; sends a prompt that triggers a permission request; verifies the session continues unblocked (i.e., Close() doesn't hang). This test may be complex — if triggering a permission request deterministically is difficult, at minimum verify that NewSession+Ping succeed and Close() returns without hanging.
    - Use t.Setenv for any env vars; use t.TempDir() for any temp directories.
    - Timeout: wrap ctx with t.Context() or context.WithTimeout(context.Background(), 30*time.Second) to avoid hanging indefinitely.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/Projects/repos/local/loop24-gateway && go test -race ./internal/acp/... -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - internal/acp/client.go exists and compiles: go build ./internal/acp/... exits 0
    - All unexported types (framer, dispatcher) have no exported leakage
    - gosec G204 annotation present: grep -c "nolint:gosec" internal/acp/client.go >= 1
    - Close() shutdown order documented in code comments
    - go test -race ./internal/acp/... exits 0 (integration test skips if kiro-cli absent)
    - goleak.VerifyTestMain passes — no goroutine leak in acp package after any test
    - cmd/loop24-gateway/main.go compiles with acp.New wired: go build ./cmd/loop24-gateway/ exits 0
    - go.mod contains require entries for go-chi/chi/v5 and go.uber.org/goleak
    - make build exits 0 after go.mod changes
  </acceptance_criteria>
  <done>
    internal/acp/client.go implemented with New, NewWithConn, Initialize, NewSession, Ping, Prompt, Cancel, Close. Integration test skips cleanly on CI, runs against real kiro-cli on dev box. goleak.VerifyTestMain passes. go test -race ./internal/acp/... exits 0. cmd/loop24-gateway/main.go wires acp.New. go.mod/go.sum updated with new deps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env-var → subprocess spawn | KIRO_CMD and KIRO_ARGS from env (operator-controlled) become exec.Command args. Not HTTP-user-controlled but must not be interpolated from request data. |
| subprocess stdout → framer | Raw bytes from kiro-cli stdout; malformed JSON must be logged-and-dropped, not panic |
| HTTP request context → Prompt cancel | ctx.Done triggers session/cancel notification to kiro-cli |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | exec.Command(cfg.Command, cfg.Args...) — G204 | mitigate | Command and Args come from env vars set by operator at deploy time, not from any HTTP input. Annotate with //nolint:gosec // G204: env-var config, not user HTTP input. gosec will flag; nolint with justification satisfies TRST-01 zero-findings requirement. |
| T-02-02 | Denial of Service | Goroutine leak if Close() order wrong | mitigate | Close() order cancel()→stdin.Close()→wg.Wait()→cmd.Wait() enforced by code + test. goleak.VerifyTestMain is the automated gate. |
| T-02-03 | Denial of Service | ACP pipe EPIPE when subprocess crashes | mitigate | framer.writeFrame wraps errors; caller receives error and can call Close(). readLoop returns on any read error. No panic path. |
| T-02-04 | Spoofing | Malformed JSON from kiro-cli stdout | mitigate | json.Unmarshal to rpcFrame; unmarshal error → slog.Warn + continue. Never trust raw bytes directly. Typed structs prevent injection via JSON field values. |
| T-02-05 | Denial of Service | session/request_permission not auto-granted | mitigate | onNotif checks frame.Method == "session/request_permission" BEFORE any id check. Sends grant_permission with optionId:"allow_always". Integration test verifies (ACP-04). |
| T-02-SC | Tampering | go get go.uber.org/goleak + go-chi/chi/v5 | mitigate | Both verified [OK] in RESEARCH.md Package Legitimacy Audit (2026-05-23). Multi-year histories, established packages. No checkpoint needed. |
</threat_model>

<verification>
After Plan 02 completes:

1. `go test -race ./internal/acp/... -v` exits 0. On dev box (kiro-cli present): integration test runs and passes. On CI: integration test skips with "kiro-cli not found" message.
2. `goleak.VerifyTestMain` in testmain_test.go passes — no goroutine leak after any test.
3. `go build ./...` exits 0 (all packages including cmd/loop24-gateway with acp.New wired).
4. `make build` exits 0 — binary still serves /health after main.go changes.
5. `make lint` exits 0 — gosec G204 nolint annotation with justification present; no new lint findings.
6. `go test -race ./...` exits 0 (all packages combined).
7. `go.mod` contains `require github.com/go-chi/chi/v5 v5.3.0` and `require go.uber.org/goleak v1.3.0`.
8. Manual (dev box only): acp.New(cfg) with real kiro-cli → Initialize → NewSession → Ping → Close all return nil error, no hung goroutines.
</verification>

<success_criteria>
- ACP-01: acp.New spawns kiro-cli; subprocess terminates cleanly on Close()
- ACP-02: id correlation works under concurrent Prompt calls (go test -race passes)
- ACP-03: initialize + session/new + session/set_model + ping RPC methods implemented and tested
- ACP-04: session/request_permission auto-granted; integration test verifies kiro-cli unblocks
- ACP-05: session/update translated to typed canonical.Chunk values (unit test for translateUpdate)
- ACP-06: pingLoop goroutine exits cleanly on Close(); goleak confirms no leak
- TRST-03: go test -race ./internal/acp/... exits 0; goleak.VerifyTestMain passes
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-02-SUMMARY.md` when done.
</output>
