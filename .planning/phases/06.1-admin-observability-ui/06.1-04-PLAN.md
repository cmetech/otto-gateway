---
phase: 06.1-admin-observability-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 06.1-01
  - 06.1-02
  - 06.1-03
files_modified:
  - docs/operating.md
autonomous: false
requirements:
  - OBSV-*
tags:
  - admin
  - observability
  - cross-compile
  - distribution
  - phase-exit

must_haves:
  truths:
    - "`make cross` produces bin/otto-gateway-linux-amd64 AND bin/otto-gateway-windows-amd64.exe with CGO_ENABLED=0 — verified after the admin package + embed.FS assets are added (D-13 single-binary contract)"
    - "Binary size delta vs the pre-Phase-6.1 baseline is ≤30KB (per RESEARCH Pitfall 5 budget; UI-SPEC bans web fonts/icon libraries to make this achievable)"
    - "Operator manually verifies in a browser: dark-mode palette correct, summary strip auto-updates every 30s, pool grid + sessions table render, dead-slot visual contract works, log tail SSE streams with all four UI controls (level / grep / pause / resume), log rotation does not crash the tailer"
    - "Existing route regression suite (`internal/server/server_admin_test.go` from Plan 01) still asserts D-15/D-16 invariant in the FINAL post-Wave-3 state"
  artifacts:
    - path: "docs/operating.md"
      provides: "Admin UI section documenting GET /admin endpoints, OTTO_LOG dependency, v1 no-auth posture, browser support note (modern evergreen browsers with EventSource + fetch + ES2018+)"
      contains: "/admin"
  key_links:
    - from: "make cross"
      to: "bin/otto-gateway-linux-amd64 + bin/otto-gateway-windows-amd64.exe"
      via: "CGO_ENABLED=0 GOOS=linux/windows GOARCH=amd64 go build with embed.FS-baked admin assets"
      pattern: "bin/otto-gateway-linux-amd64"
---

<objective>
## Phase Goal

**As an** OTTO Gateway maintainer, **I want to** verify that the admin UI vertical slice (Plans 01-03) preserves the single-static-binary + trivial-cross-compile contract that motivated the Go port **so that** Phase 6.1 can ship without regressing Phase 1's distribution promise.

## Slice Scope (Slice D: Phase-exit verification + operator docs)

This plan ships ZERO new code paths. It is a verification + documentation slice:
- Run `make cross` on a macOS dev box and confirm `bin/otto-gateway-linux-amd64` + `bin/otto-gateway-windows-amd64.exe` are produced with `CGO_ENABLED=0`.
- Measure binary size delta vs the pre-Phase-6.1 baseline (captured from the most recent `main` commit or noted from Plan 01 SUMMARY); assert ≤30KB delta per RESEARCH Pitfall 5 budget.
- Run a blocking human-verify checkpoint where the operator opens `/admin` in a browser and confirms (a) dark-mode palette correctness, (b) 30s auto-refresh of summary strip + pool grid + sessions table, (c) dead-slot visual contract (2px red border) by killing a pool slot, (d) log tail SSE streaming including all four UI controls, (e) log rotation via `mv` + `touch` does not crash the tailer.
- Append a "Admin UI" section to `docs/operating.md` documenting the new endpoints, OTTO_LOG dependency, no-auth-in-v1 posture, and supported browsers.

**End-state user check:** Phase 6.1 is shippable — `make cross` green; binary size within budget; operator has manually confirmed all six SC bars from ROADMAP; `docs/operating.md` describes how to use `/admin`.

**Why this is a separate plan (not folded into Plan 03):** the cross-compile smoke + binary-size measurement + manual UAT are inherently sequential AFTER all source changes have landed. Folding them into Plan 03 would couple the development checklist to the verification gate; keeping them separate lets Plans 01-03 deliver clean atomic vertical slices and lets Plan 04 fail fast if cross-compile breaks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-admin-observability-ui/06.1-CONTEXT.md
@.planning/phases/06.1-admin-observability-ui/06.1-RESEARCH.md
@.planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md
@.planning/phases/06.1-admin-observability-ui/06.1-UI-SPEC.md
@./CLAUDE.md
@docs/operating.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cross-compile smoke + capture binary-size delta vs pre-Phase-6.1 baseline</name>
  <files>
    <!-- no source files modified; results recorded in 06.1-04-SUMMARY.md and presented at the human-verify checkpoint -->
  </files>
  <read_first>
    .planning/phases/06.1-admin-observability-ui/06.1-RESEARCH.md §Pitfall 5 (binary-size budget ≤30KB delta; UI-SPEC bans web fonts + icon libraries to make this achievable) + Standard Stack table (no new dependencies — entire phase composed of stdlib + chi/v5 + goleak),
    .planning/phases/06.1-admin-observability-ui/06.1-CONTEXT.md D-13 (single-binary + CGO_ENABLED=0 cross-compile contract),
    .planning/phases/06.1-admin-observability-ui/06.1-VALIDATION.md per-task verification map row 6.1-06-01 + 6.1-06-02 (make cross + binary-size delta),
    ./CLAUDE.md Constraints (Distribution: single static binary per OS/arch; Go 1.23+; no cgo; cross-compile from macOS),
    ./Makefile (cross target — `make cross` runs cross-linux-amd64 + cross-windows-amd64 with CGO_ENABLED=0)
  </read_first>
  <action>
    Execute the cross-compile + size measurement protocol. NO source files are modified by this task; the output is recorded in the Plan 04 SUMMARY and presented at the Task 2 checkpoint.

    1. **Capture baseline binary size (pre-Phase-6.1 reference):**
       - From the git history, identify the most recent commit BEFORE Plan 01 lands (the parent of 06.1-01 commit OR the latest commit on `main` at phase start). The exact commit SHA should be in 06.1-01-SUMMARY.md as a baseline note; if not, use `git log --oneline --before="<plan-01-commit-date>" -n 1`.
       - Check out that commit in a worktree OR run from a tagged baseline: `git worktree add /tmp/otto-baseline <baseline-sha>; cd /tmp/otto-baseline && make build && du -b bin/otto-gateway` → record byte count as `BASELINE_BYTES`. Clean up worktree.
       - If the baseline measurement cannot be reproduced (uncommon — e.g. git worktree fails), record the size of the LAST main-branch commit before this phase from CI artifacts, or accept the developer's value from 06.1-01-SUMMARY.md.

    2. **Build current host binary + measure:**
       - From the working tree: `make build && du -b bin/otto-gateway` → record byte count as `CURRENT_BYTES`.
       - Compute delta: `DELTA = CURRENT_BYTES - BASELINE_BYTES`.
       - Assert `DELTA <= 30 * 1024` (30KB budget per RESEARCH Pitfall 5).
       - If `DELTA > 30KB`, the gate FAILS — investigate: did someone add a web font? An icon library? A new dependency? Run `go list -deps ./... | wc -l` before/after to detect dep growth.

    3. **Run cross-compile:**
       - `make cross` → produces `bin/otto-gateway-linux-amd64` and `bin/otto-gateway-windows-amd64.exe`.
       - Assert both files exist + are non-empty (`du -b bin/otto-gateway-linux-amd64` and `du -b bin/otto-gateway-windows-amd64.exe`).
       - Assert `CGO_ENABLED=0` was used (read from the Makefile + add `go version -m bin/otto-gateway-linux-amd64 | grep CGO_ENABLED` for forensic confirmation; result should be `CGO_ENABLED=0`).
       - Assert each cross-compiled binary's size is within the 25MB BLD-04 hard limit: `du -m bin/otto-gateway-linux-amd64` → ≤25; same for windows-amd64.exe.

    4. **Run full CI gate as the phase-exit smoke:**
       - `make ci` (lint + test-race + arch-lint + govulncheck) must be green.
       - Capture the output summary.

    5. **Record results in `.planning/phases/06.1-admin-observability-ui/06.1-04-SUMMARY.md`:**
       - `BASELINE_BYTES`, `CURRENT_BYTES`, `DELTA` (and pass/fail vs 30KB budget).
       - `linux-amd64` and `windows-amd64.exe` sizes (and pass/fail vs 25MB BLD-04 limit).
       - `make ci` outcome.
       - `go.mod` diff (expected: empty — no new dependencies introduced by Phase 6.1).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && make build && test -f bin/otto-gateway && make cross && test -f bin/otto-gateway-linux-amd64 && test -f bin/otto-gateway-windows-amd64.exe && test "$(du -m bin/otto-gateway-linux-amd64 | awk '{print $1}')" -le 25 && test "$(du -m bin/otto-gateway-windows-amd64.exe | awk '{print $1}')" -le 25 && make ci</automated>
  </verify>
  <done>
    `make build`, `make cross`, and `make ci` all green from a clean working tree; `bin/otto-gateway-linux-amd64` + `bin/otto-gateway-windows-amd64.exe` exist with `CGO_ENABLED=0`; each is ≤25MB (BLD-04); host-binary delta vs pre-Phase-6.1 baseline is ≤30KB (or, if >30KB, the cause is investigated + documented + the budget is amended with developer approval at the Task 2 checkpoint); `go.mod` diff is empty (Phase 6.1 introduced zero new dependencies).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking human-verify checkpoint — operator opens /admin in a browser and confirms all six SC bars</name>
  <what-built>
    The complete Phase 6.1 admin observability slice — backend (`internal/admin/` package with snapshot endpoint, embedded HTML/CSS/JS, log-tail SSE), wiring (`internal/server.Config.AdminHandler` + `cmd/otto-gateway/main.go`), and UI (dark-mode page with summary strip, pool grid, sessions table, log tail panel). Task 1 has verified cross-compile + binary-size + CI gate. This checkpoint is the visual + interactive UAT that no automated test can perform.
  </what-built>
  <how-to-verify>
    Prerequisites: working `kiro-cli` installed; `OTTO_LOG=/tmp/otto-gateway.log` writable.

    1. **Start the binary via the existing wrapper script (so `OTTO_LOG` is set and `>>` log redirection populates the file):**
       ```
       cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway
       make build
       scripts/otto-gw start
       ```
       Wait ~3s for pool warmup. Confirm `scripts/otto-gw status` shows the server is up.

    2. **Open `http://localhost:11434/admin` in a modern browser (Chrome, Firefox, Safari, Edge — any post-2018 evergreen browser supports EventSource + fetch + ES2018+).**

    3. **Verify SC1 (page renders with brand palette + 4 sections):**
       - Page background is dark purple-navy (#28243D); cards are darker gray (#3A3A3A); foreground text is off-white (#FAFAFA).
       - Four sections in order: summary strip at top, Pool Slots grid (3-col at desktop width), Active Sessions table, Log Tail panel at bottom.
       - Section headings ("Pool Slots", "Active Sessions", "Log Tail") render in semibold 16px.

    4. **Verify SC2 (30s auto-refresh + last-updated tag):**
       - Summary strip shows status pill (HEALTHY in green), uptime, version, pool counts (e.g. `4/4 alive · 0 busy`), sessions active count, and a live-indicator zone on the right.
       - "Last updated Xs ago" counter ticks every 1s.
       - In another terminal, fire a request: `curl -X POST http://localhost:11434/v1/chat/completions -H "Content-Type: application/json" -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'`. Within 30s, observe pool busy count flip from 0 → 1 → 0 in the strip (without page reload).

    5. **Verify SC3 (non-interference — existing routes unchanged):**
       - `curl -i http://localhost:11434/health` returns 200 with the same JSON shape it returned before Phase 6.1.
       - `curl -i http://localhost:11434/health/agents` returns 200 with the same JSON shape.
       - The existing route regression test (`TestServer_AdminMountDoesNotPerturbExistingRoutes` from Plan 01) is green in `make test-race`.

    6. **Verify SC4 (auth posture):**
       - With `AUTH_TOKEN` UNSET (the script default): `curl http://localhost:11434/admin` returns 200; `curl http://localhost:11434/admin/api/snapshot` returns 200 JSON.
       - With `AUTH_TOKEN=secret` set + restart: `curl http://localhost:11434/admin` STILL returns 200 (auth-exempt per D-01); `curl http://localhost:11434/v1/chat/completions ...` WITHOUT a `Bearer secret` header returns 401 (existing auth surface still works).

    7. **Verify SC5 (single binary + cross-compile preserved):**
       - `ls -lh bin/otto-gateway bin/otto-gateway-linux-amd64 bin/otto-gateway-windows-amd64.exe` — all three exist.
       - Task 1 has already recorded the binary-size delta in 06.1-04-SUMMARY.md.

    8. **Verify SC6 (log tail SSE — nice-to-have but in this slice):**
       - Log tail panel shows backfill of recent log lines (up to 500).
       - The activity dot pulses in purple #AF78D2 when SSE is connected.
       - Fire another `/v1/chat/completions` request; observe new log lines appear in the panel within ~1s.
       - Click Pause → new lines stop appending; "N new" badge appears as more lines arrive.
       - Click Resume → buffered lines flush, badge clears.
       - Type a regex into the grep input (e.g. `/v1/`) — debounced 150ms; only matching lines render.
       - Type an invalid regex (e.g. `[`) — "Invalid regex — filter not applied" hint shows in orange; previous filter stays active.
       - Use level dropdown to filter to ERROR only — only error lines render.
       - **Log rotation test:** `mv /tmp/otto-gateway.log /tmp/otto-gateway.log.1 && touch /tmp/otto-gateway.log` then fire another request. Within ~1s the new log line should appear in the tail panel. The tailer did NOT crash; no error toast.

    9. **Verify dead-slot visual contract (CONTEXT.md §specifics):**
       - In another terminal, `pgrep -f kiro-cli` → pick one PID → `kill <pid>`.
       - Within ~30s (next snapshot poll), the pool grid card for that slot flips to: 2px solid #FF3232 border, red "Dead — respawning…" text in the body. The dot/badge clearly indicates dead state. Visually unmissable at a glance.

    10. **Stop the server cleanly:**
        ```
        scripts/otto-gw stop
        ```
        Confirm no orphaned processes (`pgrep -f otto-gateway` returns nothing).

    Reply with `approved` if all 10 checks pass; or describe what failed (with screenshots if a visual issue) so the executor can route a fix back through `/gsd-plan-phase --gaps 6.1`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <files>
    <!-- Manual UAT — no files modified by the operator's verification; results recorded in 06.1-04-SUMMARY.md by Task 1 outcome + this checkpoint approval line. -->
  </files>
  <action>Manual UAT — operator follows the 10-step <how-to-verify> protocol above. Claude does not perform any file edits during this task; the executor pauses and waits for the operator to reply "approved" (or describe failures) before proceeding to Task 3.</action>
  <verify>
    <human-check>Operator reports all 10 verification steps pass.</human-check>
    <automated>echo "Manual UAT — see how-to-verify protocol; resume signal recorded in 06.1-04-SUMMARY.md"</automated>
  </verify>
  <done>Operator typed "approved" (or any subsequent gap-closure plan from /gsd-plan-phase --gaps 6.1 has been completed and re-approved). Approval timestamp recorded in 06.1-04-SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: Append "Admin UI" section to docs/operating.md describing /admin endpoints, OTTO_LOG dependency, v1 no-auth posture, and supported browsers</name>
  <files>
    docs/operating.md
  </files>
  <read_first>
    docs/operating.md (existing structure — Phase 1 Plan 01-04 established this file; we extend it),
    .planning/phases/06.1-admin-observability-ui/06.1-CONTEXT.md (D-01 no-auth-in-v1 posture; D-08..D-12 log-tail UX; D-13 single-binary; D-14 brand palette),
    .planning/phases/06.1-admin-observability-ui/06.1-UI-SPEC.md §Layout & Interaction Contract (so the doc accurately describes the four sections + 30s polling + log-tail controls),
    .planning/phases/06.1-admin-observability-ui/06.1-RESEARCH.md §Environment Availability (OTTO_LOG resolution — defaults to /tmp/otto-gateway.log; degraded "Waiting for log activity…" when file absent)
  </read_first>
  <behavior>
    - `docs/operating.md` gains a new `## Admin Observability UI` section.
    - Section documents: endpoints (`GET /admin`, `GET /admin/api/snapshot`, `GET /admin/logs/stream`, `GET /admin/static/*`); `OTTO_LOG` env-var dependency for the log tail; v1 no-auth-in-v1 posture (with the explicit risk note from D-01 and the Phase 8 forward reference); supported browsers (modern evergreen — EventSource + fetch + ES2018+); how to access the page from a remote machine (`ssh -L 11434:localhost:11434 host`).
  </behavior>
  <action>
    Append the following section to `docs/operating.md` (place after any existing "Endpoints" or "Logs" section; before any "Troubleshooting"):

    ```markdown
    ## Admin Observability UI

    OTTO Gateway ships a dark-mode admin page at `GET /admin` that surfaces
    `/health` + `/health/agents` data through a styled HTML/CSS/JS bundle
    served from `embed.FS` (single static binary; no external runtime deps).

    ### Endpoints

    | Path | Purpose |
    |------|---------|
    | `GET /admin` | The HTML page (renders summary strip, pool slots grid, active sessions table, log tail panel) |
    | `GET /admin/api/snapshot` | Unified JSON snapshot composing pool + registry detail (polled client-side every 30s) |
    | `GET /admin/logs/stream` | SSE stream of new log lines from `OTTO_LOG` (backfill of last ≤500 lines on connect, then live forward) |
    | `GET /admin/static/*` | Embedded CSS + JS assets |

    ### OTTO_LOG dependency

    The log tail panel reads from the file pointed at by `OTTO_LOG`
    (defaults to `/tmp/otto-gateway.log`). When the gateway is launched
    via `scripts/otto-gw start`, the wrapper redirects stdout/stderr via
    shell `>>` to this file, and the admin page's tail panel renders
    incoming lines within ~1s.

    When the gateway is launched directly via `go run ./cmd/otto-gateway`
    or `./bin/otto-gateway` without `OTTO_LOG` or without shell
    redirection, the log file is empty/absent and the tail panel shows
    "Waiting for log activity…" indefinitely — this is a graceful
    degraded mode, not a failure.

    Log rotation: the tailer opens the file read-only with no exclusive
    lock and re-opens at EOF when it detects rotation (size shrink OR
    inode change via `os.SameFile`). `logrotate` create-and-rename
    strategies work; truncation (`> file`) also works. Historical
    content is NEVER backfilled on rotation.

    ### v1 no-auth posture

    `/admin` and its sub-routes (`/admin/api/snapshot`, `/admin/logs/stream`)
    are auth-exempt in v1, regardless of whether `AUTH_TOKEN` is set.
    This is intentional (CONTEXT decision D-01): the operator network
    is assumed trusted (localhost / private VPC). Anyone with HTTP
    access to the gateway can see pool slot labels, session IDs,
    last-used timestamps, and live log lines (which may include
    DEBUG-level request paths and headers).

    **Deployments outside a trusted network MUST either:**
    - Wait for Phase 8 (plugin hook chain), which will gate `/admin/*`
      behind the same `AuthHook` that protects `/v1/*` and `/api/*`.
    - Add a reverse-proxy-layer auth shim (nginx `auth_basic`, oauth2-proxy,
      Cloudflare Access, etc.) in front of the gateway.

    ### Supported browsers

    Any modern evergreen browser with `EventSource`, `fetch`, and ES2018+
    support: Chrome, Firefox, Safari, Edge (releases from 2019 onwards).
    Internet Explorer is NOT supported. No transpilation; no polyfills.

    ### Remote access

    `/admin` listens on the same port as the rest of the gateway
    (`:11434` by default). To access from a remote machine without
    exposing the port:

    ```
    ssh -L 11434:localhost:11434 user@gateway-host
    ```

    Then visit `http://localhost:11434/admin` in your local browser.
    ```

    Do NOT in this task: edit any other section of `docs/operating.md`. Do NOT add a CHANGELOG entry (project does not use one yet; if/when it does, that's a separate update).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -c '## Admin Observability UI' docs/operating.md && grep -c 'OTTO_LOG' docs/operating.md && grep -c '/admin/api/snapshot' docs/operating.md && grep -c '/admin/logs/stream' docs/operating.md</automated>
  </verify>
  <done>
    `docs/operating.md` contains a new `## Admin Observability UI` section documenting endpoints, `OTTO_LOG` dependency, v1 no-auth posture (with Phase 8 forward reference), supported browsers, and the `ssh -L` remote-access tip; section is grep-verifiable; no other section of operating.md is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces NO new trust boundaries — it is a verification + documentation slice. The threat model of Plans 01-03 applies unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-18 | Tampering (supply chain) | `make cross` cross-compiled binary contains unexpected code | accept | `go.mod` diff is empty (Plan 04 Task 1 verifies); the only build inputs are stdlib + chi/v5 + goleak (all unchanged from baseline). `govulncheck` runs in `make ci`. |
| T-6.1-19 | Information disclosure | operating.md documents the no-auth posture publicly | accept | The no-auth posture is already documented in CONTEXT.md D-01 and was approved as v1 risk. operating.md surfaces the same information to operators who need to make deployment decisions; transparency is the right tradeoff. |
| T-6.1-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE — Phase 6.1 introduced zero new packages; Task 1 verifies `go.mod` diff is empty. |
</threat_model>

<verification>
- `make build` succeeds; `bin/otto-gateway` exists.
- `make cross` succeeds; `bin/otto-gateway-linux-amd64` + `bin/otto-gateway-windows-amd64.exe` exist; each ≤25MB.
- Binary-size delta vs pre-Phase-6.1 baseline ≤30KB (or documented exception approved at Task 2 checkpoint).
- `make ci` (lint + test-race + arch-lint + govulncheck) is green.
- `go.mod` diff is empty (no new dependencies).
- `docs/operating.md` contains the Admin UI section (4 grep targets verified in Task 3 verify).
- Task 2 human-verify checkpoint returns `approved`.
</verification>

<success_criteria>
1. **`make cross` green:** linux-amd64 + windows-amd64.exe binaries produced from a macOS dev box with `CGO_ENABLED=0`.
2. **Binary-size budget held:** host-binary size delta vs pre-Phase-6.1 baseline ≤30KB; each cross-compiled binary ≤25MB (BLD-04).
3. **No new deps:** `go.mod` diff is empty.
4. **Manual UAT passes:** operator confirms all six ROADMAP SC bars in a browser session — palette, polling, non-interference, auth posture, single-binary, log tail.
5. **Dead-slot visual contract validated live:** killing a pool slot's kiro-cli child produces the 2px red-border dead-card render within ~30s.
6. **Log rotation handled:** `mv` + `touch` of `OTTO_LOG` does not crash the tailer; new lines appear within ~1s after rotation.
7. **`docs/operating.md` updated:** Admin UI section documents endpoints, OTTO_LOG dependency, v1 no-auth posture, supported browsers, remote-access tip.
8. **CI gate green:** `make ci` (lint + test-race + arch-lint + govulncheck) passes from the working tree.
9. **Phase exit signal:** Plans 01-04 all complete; `/gsd-verify-work 6.1` can proceed.
</success_criteria>

<output>
Create `.planning/phases/06.1-admin-observability-ui/06.1-04-SUMMARY.md` when done — record:
- Pre-phase baseline byte count + commit SHA used.
- Post-phase host-binary byte count + delta (pass/fail vs 30KB budget).
- Linux + Windows cross-compiled binary sizes + CGO_ENABLED=0 confirmation.
- `make ci` outcome + timestamp.
- Task 2 checkpoint approval (operator name + timestamp + any observed deviations).
- Confirmation that `docs/operating.md` Admin UI section is committed.
- Note any DEVIATIONS from the locked decisions D-01..D-16 (expected: none).
- Phase exit checklist: all ROADMAP SC1..SC6 satisfied? if not, list gaps for `/gsd-plan-phase --gaps 6.1`.
</output>
