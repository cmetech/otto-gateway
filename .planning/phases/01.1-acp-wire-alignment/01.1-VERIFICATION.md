---
phase: 01.1-acp-wire-alignment
verified: 2026-05-23T19:35:00Z
status: passed
score: 6/6 success criteria verified, 5/5 plan must_have-sets verified, 5/5 requirements satisfied (ACP-01..ACP-05)
overrides_applied: 0
requirements_verified:
  - ACP-01
  - ACP-02
  - ACP-03
  - ACP-04
  - ACP-05
roadmap_truths_verified:
  - "SC-1: Spec-compliant initialize request with protocolVersion:1, clientInfo, clientCapabilities{fs,terminal}; response's promptCapabilities captured + accessible"
  - "SC-2: Spec-compliant session/new with mcpServers:[]; sessionId/id fallback; availableModels surfaced as []canonical.ModelInfo"
  - "SC-3: Spec-compliant session/prompt with prompt+content defensive duplicate; wire text field 'text'; resource_link with required name; image fields present; StopReason on Stream.Result()"
  - "SC-4: session/update consumption — three method names; params.update unwrap + flat; sessionUpdate w/ type fallback; six discriminators incl CamelCase aliases; content extraction fallback chain"
  - "SC-5: session/request_permission handled as a REQUEST (response echoed on original frame id); session/grant_permission request path removed"
  - "SC-6: TestIntegration_RealKiroCLI_PromptRoundTrip — Phase 2 unblock gate — PASSES against kiro-cli 2.4.1 with goleak clean"
---

# Phase 01.1: ACP Wire Alignment — Verification Report

**Phase Goal:** Bring the Phase 1 ACP JSON-RPC client into compliance with the live ACP spec and the working Node implementation, so Phase 2 can invoke `session/prompt` against real `kiro-cli` and consume `session/update` notifications without empty prompts, dropped chunks, or permission-grant deadlocks. The phase ships with a real-kiro `session/prompt` round-trip integration test as the verification gate that Phase 1's smoke test stopped short of.

**Verified:** 2026-05-23T19:35:00Z
**Status:** **passed**
**Re-verification:** No — initial verification.

## Goal Achievement

### Roadmap Success Criteria

| #   | Criterion | Status | Evidence |
| --- | --------- | ------ | -------- |
| 1   | Spec-compliant `initialize` (protocolVersion:1, clientInfo, clientCapabilities.fs+terminal); response promptCapabilities captured & accessible | VERIFIED | `internal/acp/client.go:104-108` (`initializeParams` with `ProtocolVersion int`, `ClientInfo`, `ClientCapabilities`); `:119-129` (`clientCapabilities{Fs:fsCapabilities, Terminal:bool}` with `readTextFile`/`writeTextFile`); `:522` (`ProtocolVersion: 1` at call site); `:136-152` (`initializeResult.AgentCapabilities.PromptCapabilities` wire shape); `:549-562` (unmarshal + lock + assign to `c.caps`); `:1001-1005` (`PromptCapabilities()` accessor, RLock'd snapshot); real-kiro test logged `PromptCapabilities(): {Image:true Audio:false EmbeddedContext:false}`. |
| 2   | Spec-compliant `session/new` (mcpServers:[]); sessionId/id fallback; availableModels surfaced as []ModelInfo | VERIFIED | `internal/acp/client.go:154-167` (`mcpServer` struct + `sessionNewParams.MCPServers []mcpServer json:"mcpServers"`); `:584-587` (`make([]mcpServer, 0)` at call site — emits `[]` not `null`); `:174-192` (`sessionNewResult.SessionID`+`.ID`; nested `Models.AvailableModels[].ModelID`+`.Name`); `:606-609` (`firstNonEmpty(r.SessionID, r.ID)`; empty-both → error `"acp: session/new result: missing sessionId and id"`); `:611-624` (translate into `canonical.ModelInfo{ID:entry.ModelID, Name:entry.Name}` under `c.stateMu`); `:1014-1021` (`AvailableModels()` returns defensive copy); real-kiro test logged `AvailableModels(): 13 models, first = {ID:auto Name:auto}`. |
| 3   | Spec-compliant `session/prompt` (prompt+content); text uses `text`; resource_link has required `name`; image construction wired; StopReason on Stream.Result() | VERIFIED | `internal/acp/client.go:212-216` (`promptParams{SessionID, Prompt []wireBlock json:"prompt", Content []wireBlock json:"content"}`); `:734,742` (single translated wire slice shipped on both fields); `internal/acp/translate.go:333-341` (`wireBlock{Type, Text json:"text,omitempty", URI, Name json:"name,omitempty" REQUIRED, Title, MIMEType, Data}` — note text field is `text` not `content`); `:358-396` (`translateBlock` text arm uses `Text`; resource_link arm derives `Name` from `b.ResourceLink.Name` else `path.Base(net/url.Parse(URI).Path)` per D-04; image MIMEType+Data fields present in wire shape for Phase 2); `internal/acp/stream.go:13-23` (`FinalResult.StopReason canonical.StopReason` field); `internal/acp/client.go:783-795` (parse `result.stopReason`, `parseStopReason(r.StopReason)`, `s.close(&FinalResult{StopReason: stop}, nil)`); `internal/acp/stream.go:105-121` (merge semantics — preserves `SessionID` + `ChunkCount`, only StopReason flows in); real-kiro test logged `stream.Result().StopReason = 1 (non-error stop)` (=`StopEndTurn`). |
| 4   | `session/update` consumption — 3 method names; params.update wrap+flat; sessionUpdate w/type fallback; 6 discriminators incl CamelCase; body.content?.text ?? body.content ?? body.text | VERIFIED | `internal/acp/client.go:877` (case label `"session/update", "session/notification", "_kiro.dev/session/update"`); `internal/acp/translate.go:42-71` (`sessionUpdateParams` tolerant outer with `Update json.RawMessage` + flat fields `SessionUpdate/Type/Content/Text/.../Entries`); `:193-219` (`translateUpdate` re-unmarshals `u.Update` into `sessionUpdateBody` when wrapped, else copies flat fields); `:221` (`discriminator := normalizeUpdateType(firstNonEmpty(body.SessionUpdate, body.Type))`); `:122-155` (`normalizeUpdateType` snake-or-Camel → snake_case); `:222,268-300` (`extractContent` walks `content.text` via *string probe (WR-04 fix) → string fallback → body.Text); `:224-265` switch handles `agent_message_chunk`, `agent_thought_chunk`, `tool_call`, `tool_call_chunk`, `tool_call_update`, `plan` (+ default text-fallback for unknown discriminators); 14-row `TestTranslateUpdate_VarianceMatrix` + `TestNormalizeUpdateType` cover the cross-product; fake E2E `TestIntegration_FakeACP_E2E_MixedVariants` exercises five mixed variants in one session. |
| 5   | `session/request_permission` responded to on original frame id; legacy `session/grant_permission` request path removed | VERIFIED | `internal/acp/client.go:822-875` — `case "session/request_permission":` builds `rpcResponse{JSONRPC:"2.0", ID: frame.ID (json.RawMessage echoed), Result:{optionId:"allow_always", granted:true}}` and writes directly via `c.framer.writeFrame(...)` (WR-02 path avoids writeCh stall); `:93-97` (`rpcResponse` envelope shape — note `ID json.RawMessage` from CR-01 fix so string/numeric ids both round-trip verbatim); negative greps: `grep -RnE 'session/grant_permission' internal/` → ZERO matches; `grep -nE 'grantParams|grantID' internal/acp/client.go internal/acp/translate.go` → ZERO matches; `client_test.go:TestAutoGrantPermission` asserts the new response envelope shape (no method, id echoed, result map). |
| 6   | `internal/acp/integration_test.go` `TestIntegration_RealKiroCLI_PromptRoundTrip` (D-17 gated; Init→NewSession→Prompt("hi")→drain chunks→Result; ≥1 ChunkKindText non-empty; non-error StopReason; goleak.VerifyNone) — **Phase 2 unblock gate** | VERIFIED | `internal/acp/integration_test.go:283` (function definition); `:284` (`resolveKiroCLI(t)` skip-if-absent); `:307` (`ctx, cancel := context.WithTimeout(context.Background(), 90*time.Second)`); `:323` (`client.PromptCapabilities()` assertion); `:341` (`client.AvailableModels()` assertion); `:367-370` (Prompt on goroutine — WR-01/WR-07 backpressure fix); `:395-405` (concurrent chunk drain counts text chunks); `:432-439` (`StopReason` switch — `StopEndTurn`/`StopMaxTokens`/`StopMaxTurnRequests` acceptable, `StopUnknown` and refusal/cancelled treated as errors); `:302` (`goleak.VerifyNone(t)` deferred). **Executed against real kiro-cli 2.4.1:** `LOOP24_KIRO_BIN=$(which kiro-cli) go test ./internal/acp/... -run TestIntegration_RealKiroCLI_PromptRoundTrip -count=1 -timeout 120s -v` → **PASS in 2.85s**. Logged: `PromptCapabilities(): {Image:true Audio:false EmbeddedContext:false}`, `NewSession: OK (sessionID=468218ba-...)`, `AvailableModels(): 13 models, first = {ID:auto Name:auto}`, `Prompt: returned without deadlock (D-20 + WR-01 contract exercised)`, `received 3 text chunks; first chunk content (truncated to 80 chars): Hi`, `stream.Result().StopReason = 1 (non-error stop)`. No goroutine leaks. |

**Score: 6/6 ROADMAP success criteria verified against the codebase.**

### Per-Plan Must-Haves Verification

| Plan | Truths | Artifacts | Key Links | Status |
| ---- | ------ | --------- | --------- | ------ |
| 01.1-01 (canonical types) | 5/5 (StopReason 6-enum w/ StopUnknown zero, ModelInfo, PromptCapabilities, ResourceLinkBlock.Name, leaf-package preserved) | 4/4 (`stop_reason.go`, `model.go`, `capabilities.go`, `chunk.go` modified) | 1/1 (no internal/ imports under canonical — verified by `grep -RE '^[[:space:]]*"loop24-gateway/internal/' internal/canonical/ \| grep -v '_test.go'` returning zero) | VERIFIED |
| 01.1-02 (initialize + accessors) | 6/6 (initialize wire shape; PromptCapabilities captured + zero-value defaults; AvailableModels exists nil-until-session; signature stability for main.go; test passes) | 2/2 (`client.go`, `client_test.go`) | 2/2 (`Initialize → stateMu.Lock → c.caps`; `PromptCapabilities() → stateMu.RLock → c.caps`) | VERIFIED |
| 01.1-03 (session/new + session/prompt + StopReason) | 7/7 (mcpServers:[]; sessionId/id fallback; availableModels surfaced; prompt+content duplicate; wireBlock text/name; Stream.Result().StopReason; BlockKindImage NOT added per D-15) | 3/3 (`client.go`, `translate.go`, `stream.go`) | 3/3 (NewSession → stateMu/models; Prompt → parseStopReason; translateBlock(empty Name) → path.Base) | VERIFIED |
| 01.1-04 (notifications + permission RESPONSE) | 11/11 (3 method names; tolerant body; firstNonEmpty discriminator; content extraction fallback; permission RESPONSE on original id; six variant translations; variance matrix coverage; normalizer test; fake server spec-compliant; ONE consolidated E2E; TestAutoGrantPermission updated) | 5/5 (`client.go`, `translate.go`, `translate_test.go`, `fakeacp_test.go`, `integration_test.go`, `client_test.go`) | 3/3 (handleNotification(session/update*) → translateUpdate; permission → framer.writeFrame; variance matrix → translateUpdate) | VERIFIED |
| 01.1-05 (real-kiro round-trip) | 8/8 (test exists; skips cleanly absent kiro-cli; runs full lifecycle; asserts text chunk + non-empty; asserts non-error StopReason; asserts caps non-zero; asserts models non-empty; goleak clean) | 1/1 (`integration_test.go`) | 2/2 (`resolveKiroCLI → acp.Config.Command → acp.New`; test exercises Plans 01-04 wire alignment end-to-end) | VERIFIED |

### Required Artifacts (Three-Level Verification)

| Artifact | Exists | Substantive | Wired | Status |
| -------- | ------ | ----------- | ----- | ------ |
| `internal/canonical/stop_reason.go` | YES | YES (6 enum values w/ StopUnknown=iota=0; doc comments per-constant) | YES (referenced by `internal/acp/translate.go:105-120 parseStopReason`, `internal/acp/stream.go:22 FinalResult.StopReason`, `internal/acp/client.go:790 parseStopReason`, integration_test.go) | VERIFIED |
| `internal/canonical/model.go` | YES | YES (`ModelInfo{ID, Name string}` with doc comments) | YES (referenced by `internal/acp/client.go:271 c.models []canonical.ModelInfo`, `:614-619 ModelInfo{ID:entry.ModelID, Name:entry.Name}`, `:1014 AvailableModels()`) | VERIFIED |
| `internal/canonical/capabilities.go` | YES | YES (`PromptCapabilities{Image, Audio, EmbeddedContext bool}` with doc comments) | YES (referenced by `internal/acp/client.go:270 c.caps`, `:555-559 translation from wire`, `:1001 PromptCapabilities()`) | VERIFIED |
| `internal/canonical/chunk.go` (Name added) | YES | YES (ResourceLinkBlock.Name string field present at line 96) | YES (referenced by `translate.go:369-385 translateBlock(resource_link) Name fallback`) | VERIFIED |
| `internal/canonical/types_test.go` | YES | YES (presence + zero-value + distinctness asserted; 73 lines) | N/A (test file) | VERIFIED |
| `internal/acp/client.go` (whole) | YES | YES (1022 lines; all D-08..D-20 changes present per grep evidence above) | YES (used by integration_test.go, client_test.go, main.go presumably) | VERIFIED |
| `internal/acp/translate.go` (whole) | YES | YES (414 lines; helpers parseStopReason/normalizeUpdateType/firstNonEmpty/extractContent + tolerant sessionUpdateParams + new wireBlock shape) | YES (called from client.go:734,890 + translate_test.go) | VERIFIED |
| `internal/acp/stream.go` (StopReason field) | YES | YES (FinalResult.StopReason canonical.StopReason; merge semantics in close()) | YES (Stream.Result() returns *FinalResult; called from integration_test.go and fake E2E test) | VERIFIED |
| `internal/acp/integration_test.go` (TestIntegration_RealKiroCLI_PromptRoundTrip) | YES | YES (function spans lines 283-440; full 10-step D-24 implementation; concurrent prompt+drain goroutines per WR-01/WR-07) | YES (executed against real kiro-cli — PASS recorded) | VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `Initialize` | `c.stateMu / c.caps` | Lock after RPC response parse | WIRED | `client.go:560-562` shows `c.stateMu.Lock(); c.caps = caps; c.stateMu.Unlock()` |
| `PromptCapabilities()` | `c.caps` | RLock, return snapshot | WIRED | `client.go:1002-1004` |
| `NewSession` | `c.stateMu / c.models` | Lock after RPC response parse | WIRED | `client.go:622-624` |
| `AvailableModels()` | `c.models` | RLock, defensive copy via append-nil-slice | WIRED | `client.go:1015-1020` |
| `Prompt` | `parseStopReason` | frame.Result → promptResult.StopReason → parseStopReason → s.close(FinalResult) | WIRED | `client.go:784-795` |
| `Stream.close` | `FinalResult.StopReason` | merge non-zero StopReason onto existing s.result (preserves SessionID + ChunkCount) | WIRED | `stream.go:106-120` |
| `handleNotification("session/request_permission")` | outbound rpcResponse | direct `c.framer.writeFrame(json.RawMessage(data))` (bypasses writeCh per WR-02) | WIRED | `client.go:848-875` |
| `rpcResponse.ID` | inbound `frame.ID` | `json.RawMessage` echo (CR-01) so string/numeric ids both round-trip | WIRED | `client.go:93-97` (envelope), `:850` (assignment from frame) |
| `handleNotification(session/update*)` | `translateUpdate` | unmarshal sessionUpdateParams → translateUpdate → push to activeStream | WIRED | `client.go:877-909` |
| `translateUpdate` | discriminator switch | `normalizeUpdateType(firstNonEmpty(body.SessionUpdate, body.Type))` then switch over 5 cases + default | WIRED | `translate.go:221,224-265` |
| `translateBlock(resource_link, empty Name)` | `path.Base(url.Parse(URI).Path)` | `net/url` + `path` stdlib fallback | WIRED | `translate.go:369-385` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `Client.caps` | `c.caps canonical.PromptCapabilities` | `frame.Result` unmarshal from `initialize` RPC response (kiro-cli stdout via framer) | YES — real-kiro test logged `Image:true Audio:false EmbeddedContext:false` | FLOWING |
| `Client.models` | `c.models []canonical.ModelInfo` | `frame.Result.Models.AvailableModels[]` unmarshal from `session/new` RPC response | YES — real-kiro test logged 13 models populated | FLOWING |
| `FinalResult.StopReason` | `result.StopReason canonical.StopReason` | `frame.Result.stopReason` parsed via `parseStopReason` | YES — real-kiro test logged StopReason=1 (StopEndTurn) | FLOWING |
| `Stream.Chunks` | `chan canonical.Chunk` | readLoop → handleNotification(session/update*) → translateUpdate → push | YES — real-kiro test received 3 text chunks (first content `"Hi"`); fake E2E test received 5 mixed-variant chunks in expected order | FLOWING |
| `rpcResponse.ID` | `json.RawMessage` echo of inbound `frame.ID` | dispatcher route → handleNotification → frame.ID (json.RawMessage) → rpcResponse.ID | YES — exercised by fake E2E test (`fake.permissionResponseReceived` channel closes within the timeout) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Whole-repo build green | `go build ./...` | exit 0 | PASS |
| Vet clean | `go vet ./...` | exit 0 | PASS |
| Full acp + canonical test suite under -race | `go test ./internal/acp/... ./internal/canonical/... -count=1 -race -timeout 180s` | `ok loop24-gateway/internal/acp 6.095s`, `ok loop24-gateway/internal/canonical 1.401s`; exit 0 | PASS |
| Real-kiro gate test (Phase 2 unblock gate, SC-6) | `LOOP24_KIRO_BIN=$(which kiro-cli) go test ./internal/acp/... -run TestIntegration_RealKiroCLI_PromptRoundTrip -count=1 -timeout 120s -v` | `--- PASS: TestIntegration_RealKiroCLI_PromptRoundTrip (2.85s)`; exit 0 | PASS |
| SC-5 negative grep (grant_permission removed) | `grep -RnE 'session/grant_permission' internal/` | exit 1, zero matches | PASS |
| SC-5 negative grep (grantParams removed) | `grep -nE 'grantParams\|grantID' internal/acp/client.go internal/acp/translate.go` | exit 1, zero matches | PASS |
| SC-1 positive grep (protocolVersion: 1) | `grep -nE 'ProtocolVersion:\s*1' internal/acp/client.go` | line 522 matches | PASS |
| SC-2 positive grep (mcpServers field on wire) | `grep -nE 'MCPServers \[\]mcpServer' internal/acp/client.go` | line 166 matches | PASS |
| SC-4 positive grep (3 method names in case label) | `grep -nE 'session/update", "session/notification", "_kiro.dev/session/update' internal/acp/client.go` | line 877 matches | PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (No `scripts/*/tests/probe-*.sh` declared in any plan; this is a Go-test-driven phase) | N/A | N/A | N/A |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| ACP-01 | 01.1-05 (re-validation) | `kiro-cli` subprocess via `os/exec.CommandContext`; `KIRO_CMD`/`KIRO_ARGS` defaults | SATISFIED | `client.go:284` `exec.CommandContext(clientCtx, cfg.Command, cfg.Args...)`; `:51-56 applyDefaults` sets `"kiro-cli"` + `[]string{"acp"}`; real-kiro test successfully spawns the subprocess |
| ACP-02 | 01.1-04, 01.1-05 (re-validation) | JSON-RPC 2.0 over stdio with id correlation; one reader + one writer goroutine; pending request tracking | SATISFIED | `client.go:62-97` rpcRequest/rpcNotification/rpcResponse envelopes; `:322-326 c.wg.Add(3); go c.readLoop; go c.writerLoop; go c.pingLoop`; `:316-319 c.disp = &dispatcher{pending: make(map[uint64]chan<- rpcFrame)}`; `dispatcher.go` registers/cancels per-id; CR-01 fix ensures string/numeric ids both work |
| ACP-03 | 01.1-01..05 (re-validation) | `initialize`, `session/new`, `session/set_model`, `session/prompt`, `session/cancel`, `ping` supported | SATISFIED | `client.go` exports Initialize (512), NewSession (575), Ping (631), SetModel (662), Prompt (720), Cancel (801); the full set is exercised by `TestIntegration_RealKiroCLI_PromptRoundTrip` + `TestIntegration_FakeACP_E2E_MixedVariants` |
| ACP-04 | 01.1-04 (re-validation, refactored to RESPONSE per D-20) | Incoming `session/request_permission` notifications auto-granted with `{optionId:"allow_always", granted:true}` | SATISFIED | `client.go:822-875` builds and writes the response on the original frame id (D-20); `TestAutoGrantPermission` asserts the response envelope shape (no method, id echoed, result.optionId="allow_always", result.granted=true); fake E2E test exercises the path with `permissionResponseReceived` channel sync |
| ACP-05 | 01.1-04 (re-validation against live spec vocabulary) | Incoming `session/update` (and `_kiro.dev/session/update`) notifications translated into typed canonical chunks: text, thought, tool_call, plan | SATISFIED | `client.go:877` (3 method-name case label including `session/notification`); `translate.go:193-266 translateUpdate` produces `canonical.Chunk{Kind: ChunkKindText/Thought/Plan}` per discriminator; tool_call rendered as Thought per CONTEXT.md `<deferred>` policy with Phase 6 to land proper ToolCallChunk; 14-row variance matrix + fake E2E + real-kiro test all exercise this path |

**All ACP-01..ACP-05 satisfied via re-validation. No NEW requirements added by Phase 1.1 (per CONTEXT.md). No requirements orphaned.**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | `grep -nE 'TODO\|FIXME\|XXX\|TBD' internal/acp/client.go internal/acp/translate.go internal/acp/stream.go internal/canonical/*.go` returns zero matches | INFO | Zero debt markers in any file modified by this phase. |

### Human Verification Required

None. All six ROADMAP success criteria are programmatically verified:
- SC-1..SC-5 are verified by direct codebase grep + behavioral test execution (go test under -race).
- SC-6 (the Phase 2 unblock gate) was executed end-to-end against real `kiro-cli 2.4.1` during this verification run — see the Behavioral Spot-Checks row labeled "Real-kiro gate test" above; output is reproduced in full evidence form under the SC-6 row of the success-criteria table. The plan's `checkpoint:human-verify` Task 2 (Plan 05) was explicitly run as part of this verification, not deferred.

### Deferred Items

(None — no later-phase deferrals apply to Phase 1.1.) Items captured in `CONTEXT.md <deferred>` are intentional Phase 1.1 scope exclusions (Phase 2's D-08..D-10 canonical types; Phase 6's proper `ToolCallChunk`; Phase 9's hosted CI; etc.) — these are not gaps against Phase 1.1's goal.

### Gaps Summary

**No gaps.** The phase goal — bring the ACP client into compliance with the live spec + Node reference, and ship a real-kiro round-trip test as the Phase 2 unblock gate — is achieved in the codebase. All six ROADMAP success criteria verify positively against direct code evidence:
1. `initialize` wire shape (protocolVersion:1 + clientInfo + clientCapabilities with fs/terminal flags) is present and captures `agentCapabilities.promptCapabilities` into `c.caps` under `c.stateMu`, exposed via `PromptCapabilities()`.
2. `session/new` ships `mcpServers: []` defensively, accepts sessionId-or-id via `firstNonEmpty`, surfaces `availableModels[]` as `[]canonical.ModelInfo` via `AvailableModels()`.
3. `session/prompt` ships the defensive `prompt`+`content` duplicate, the spec-compliant `wireBlock{text, name, ...}` shape including resource_link Name with `path.Base(URI)` fallback, and parses `stopReason` into `Stream.Result().StopReason`.
4. `session/update` consumption matches all three method names, tolerates wrapped + flat body shapes, normalises CamelCase to snake_case discriminators, runs the three-step content extraction, and emits the correct `canonical.Chunk` for each of the six variant types — locked by `TestTranslateUpdate_VarianceMatrix` (14 rows) + `TestNormalizeUpdateType` + the fake-server E2E mixed-variants test.
5. `session/request_permission` is responded to on the original frame id with `optionId:"allow_always"` + `granted:true`; the legacy `session/grant_permission` request path and its `grantParams` struct are gone from the codebase (recursive grep confirms zero matches).
6. The real-kiro round-trip test exists and **passes against kiro-cli 2.4.1**: 3 text chunks received (first content `"Hi"`), 13 models surfaced, `PromptCapabilities` non-zero (Image:true), `StopReason=StopEndTurn`, `goleak.VerifyNone` clean. The Phase 2 unblock gate is open.

All five plans' must_haves are verified, all five requirements ACP-01..ACP-05 are satisfied (no orphans, no new IDs introduced as Phase 1.1 is a re-validation phase), the iteration-2 REVIEW.md status of "clean" is corroborated by direct codebase inspection, and there are zero unresolved debt markers in any file modified by this phase.

**Phase 1.1 is complete. Phase 2 is unblocked.**

---

_Verified: 2026-05-23T19:35:00Z_
_Verifier: Claude (gsd-verifier)_
