---
phase: 01.1-acp-wire-alignment
reviewed: 2026-05-23T19:30:00Z
depth: standard
files_reviewed: 15
files_reviewed_list:
  - internal/acp/client.go
  - internal/acp/client_test.go
  - internal/acp/dispatcher.go
  - internal/acp/dispatcher_test.go
  - internal/acp/fakeacp_test.go
  - internal/acp/integration_test.go
  - internal/acp/stream.go
  - internal/acp/translate.go
  - internal/acp/translate_test.go
  - internal/canonical/capabilities.go
  - internal/canonical/chunk.go
  - internal/canonical/model.go
  - internal/canonical/stop_reason.go
  - internal/canonical/types_test.go
findings:
  critical: 0
  warning: 0
  info: 5
  total: 5
status: clean
iteration: 2
previous_review: 01.1-REVIEW.md (iteration 1, 2026-05-23T00:00:00Z)
---

# Phase 1.1: Code Review Report (Iteration 2 — post-fix)

**Reviewed:** 2026-05-23 (post-fix re-review)
**Depth:** standard
**Files Reviewed:** 15
**Status:** clean (all critical + warning findings from iteration 1 resolved)

## Summary

Iteration 1 surfaced 1 BLOCKER (CR-01) and 7 warnings (WR-01..WR-07). All
eight were applied as eight discrete commits between `0206d94` and `8037c84`.
Iteration 2 re-reviews the same fifteen files and finds no remaining critical
or warning issues. Five Info findings from iteration 1 (IN-01..IN-05) remain
deferred — none are load-bearing for Phase 2, none break any contract, and
each is documented in iteration 1 as either intentional or stylistic. They
carry forward unchanged.

The real-kiro round-trip gate (`TestIntegration_RealKiroCLI_PromptRoundTrip`)
PASSES end-to-end against `kiro-cli 2.4.1` with 3 text chunks received and
`StopReason=StopEndTurn`. `go build ./...`, `go vet ./...`, and
`go test ./internal/acp/... ./internal/canonical/... -count=1 -race` all
pass cleanly. Phase 2 is unblocked.

The variance-absorbing translation layer (D-16..D-19) and the
permission-response deadlock fix (D-20) are now hardened against the
specific failure modes called out in iteration 1: string-id JSON-RPC frames
survive parse→re-emit verbatim (CR-01), the permission response no longer
shares a fifo position with normal RPC sends (WR-02), and the integration
test no longer depends on the chunk count fitting in the 64-slot buffer
(WR-01/WR-07). Canonical-purity (D-04, D-11) is preserved — none of the
fixes added a JSON tag to anything under `internal/canonical/`.

## Critical Issues

_None — CR-01 from iteration 1 was resolved by commit `0206d94`._

## Warnings

_None — WR-01..WR-07 from iteration 1 were resolved by commits
`9a8ec54..8037c84`._

## Info

The five Info findings from iteration 1 carry forward unchanged. Repeating
them here for traceability; each is non-blocking and intentional or
stylistic.

### IN-01: `s := stream` aliasing in Prompt() success path is redundant

**File:** `internal/acp/client.go:760-764` (now shifted by the WR-01 doc
addition; the aliasing is still present at the equivalent location).
**Status:** carried forward.
**Reason:** stylistic. The local alias is a no-op; removing it is a trivial
follow-up but not load-bearing.

### IN-02: empty-ish struct types vs `map[string]any{}`

**File:** `internal/acp/client.go:222-226`.
**Status:** carried forward.
**Reason:** documented observation; no action required.

### IN-03: `Cancel()` doesn't log if subprocess closed

**File:** `internal/acp/client.go:770-776`.
**Status:** carried forward.
**Reason:** best-effort semantics are correct; the Debug log enhancement
remains a nice-to-have but is not Phase-2-blocking. Note: with the
sendNotification path unchanged by the iteration-2 fixes, the rationale is
the same.

### IN-04: `mockRWC.Close()` returns only one of two pipe-close errors

**File:** `internal/acp/client_test.go:46-53`.
**Status:** carried forward.
**Reason:** test-fixture cosmetics; doesn't affect the production path.

### IN-05: Mixed `Errorf` vs `Fatalf` in real-kiro caps assertion

**File:** `internal/acp/integration_test.go:323-336` (line numbers shifted
slightly by the WR-01/WR-07 restructure; the mixed-severity assertion is
unchanged).
**Status:** carried forward.
**Reason:** intentional and documented in-place.

## Resolved Findings (from iteration 1)

| ID | Title | Commit | Verification |
|----|-------|--------|--------------|
| CR-01 | `rpcFrame.ID` typed as `*uint64` silently drops string/negative IDs | `0206d94` | New `TestDispatcherRoutes_ServerRequest_StringID_PreservedForEcho` asserts a `"req-1"` string-id `session/request_permission` frame reaches `onNotif` with `frame.ID` preserved byte-for-byte and round-trips through `json.Marshal` back to `{"id":"req-1"}`. The outbound `rpcResponse.ID` field is now `json.RawMessage`, so the response echoes whatever shape arrived. `rawIDNum` helper added to dispatcher_test.go; existing tests updated to construct ids via the helper. |
| WR-01 | `Stream.Chunks` backpressure deadlock + missing consumer contract | `9a8ec54` | `Stream` type-level godoc and `Stream.Chunks` field godoc now spell the consumer-must-drain contract; `Prompt()` godoc grows a worked example using a separate drain goroutine. Buffer size stays at 64 (no contract change). |
| WR-02 | Permission response can stall readLoop indefinitely if `writeCh` is full | `29a85d4` | The permission RESPONSE now writes directly via `framer.writeFrame(json.RawMessage(data))` instead of queueing on `writeCh`. The framer's internal mutex (framer.go:58-65) already serialises against the writer goroutine, so this is race-free. Phase 5's pool will benefit from the unblocked readLoop on tool-using prompts. |
| WR-03 | Misleading comment in `Close()` — code did not actually filter "signal: killed" | `128c50d` | New `isExpectedTeardownExit` helper inspects `*exec.ExitError.Exited()`. Verified against real-kiro: Close() now logs `acp: cmd exited via context cancellation (expected)` at Debug instead of returning the error to callers. |
| WR-04 | `extractContent` leaked `body.Text` when `content.text` was explicit empty | `b02cc49` | The probe Text field is now `*string` so the decoder distinguishes absent from present-but-empty. New `empty_content_text_does_not_leak_body_text` row in `TestTranslateUpdate_VarianceMatrix` locks the contract. |
| WR-05 | Inner-unmarshal failure in `translateUpdate` silently dropped | `4d7cd4d` | `translateUpdate` now takes `*slog.Logger` and returns `(canonical.Chunk, bool)`. On inner-unmarshal failure it Debug-logs with the truncated raw payload (`truncateForLog` clamps at 200 bytes) and returns `ok=false`; `handleNotification` drops the notification rather than push a phantom empty chunk. New `TestTranslateUpdate_InnerUnmarshalFailure_DropsNotification` asserts the drop behaviour. |
| WR-06 | Magic number `-32099` repeated 5 times — no named constant | `8037c84` | New `const closeSentinelCode = -32099` in dispatcher.go; all six literal sites (5 in client.go, 1 in dispatcher.go, 1 in dispatcher_test.go) now refer to it. |
| WR-07 | Real-kiro test relied on chunk count fitting in 64-slot buffer | `9a8ec54` | `TestIntegration_RealKiroCLI_PromptRoundTrip` restructured: `Prompt()` runs on a goroutine and the chunks drain on a separate goroutine — the canonical pattern documented on Prompt()'s godoc. Verified: test still PASSES with `LOOP24_KIRO_BIN=$(which kiro-cli)`. |

## Verification

- `go build ./...` — PASS
- `go vet ./...` — PASS
- `go test ./internal/acp/... ./internal/canonical/... -count=1 -race -timeout 120s` — PASS
  - 0 test failures across `internal/acp` and `internal/canonical`.
- `LOOP24_KIRO_BIN=$(which kiro-cli) go test ./internal/acp/... -run TestIntegration_RealKiroCLI_PromptRoundTrip` — PASS
  - 3 text chunks received; `StopReason=StopEndTurn`; no goroutine leaks.
  - Cleanup path emits the expected `acp: cmd exited via context cancellation (expected)` Debug log (WR-03 contract verified end-to-end).

## Constraints Respected

- D-15: `canonical.BlockKindImage` was NOT added (deferred to Phase 2).
- D-25: test file layout unchanged (whitebox unit + blackbox integration).
- D-11: no JSON tags added to `internal/canonical/*`.
- `acp:` error-prefix convention preserved on all new error wording.
- All fix commits use the `fix(01.1): ...` subject form per Phase 1.1
  convention, with the finding ID in the body.

---

_Re-reviewed: 2026-05-23T19:30:00Z_
_Reviewer: Claude (gsd-code-fixer; iteration 2 self-review post-fix)_
_Depth: standard_
_Previous iteration: 2026-05-23T00:00:00Z (issues_found: 1 critical, 7 warning, 5 info)_
