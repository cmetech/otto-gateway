---
phase: 01.1-acp-wire-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/canonical/chunk.go
  - internal/canonical/stop_reason.go
  - internal/canonical/model.go
  - internal/canonical/capabilities.go
  - internal/canonical/types_test.go
autonomous: true
requirements: [ACP-03, ACP-04, ACP-05]
requirements_addressed: [ACP-03, ACP-04, ACP-05]
tags: [acp, canonical, types]

must_haves:
  truths:
    - "internal/canonical exports StopReason enum with all 6 D-02 variants (zero-value is StopUnknown)"
    - "internal/canonical exports ModelInfo{ID,Name} struct"
    - "internal/canonical exports PromptCapabilities{Image,Audio,EmbeddedContext} struct"
    - "ResourceLinkBlock has a Name string field"
    - "go build ./... succeeds; internal/canonical imports nothing under internal/"
  artifacts:
    - path: "internal/canonical/stop_reason.go"
      provides: "StopReason enum type + constants"
      contains: "type StopReason int"
    - path: "internal/canonical/model.go"
      provides: "ModelInfo struct"
      contains: "type ModelInfo struct"
    - path: "internal/canonical/capabilities.go"
      provides: "PromptCapabilities struct"
      contains: "type PromptCapabilities struct"
    - path: "internal/canonical/chunk.go"
      provides: "ResourceLinkBlock.Name field added to existing struct"
      contains: "Name string"
  key_links:
    - from: "internal/canonical"
      to: "(nothing under internal/)"
      via: "no imports"
      pattern: "import"
---

<objective>
Add the minimum-floor canonical types required by every downstream Phase 1.1 plan (per CONTEXT.md D-01): the `StopReason` enum (D-02), the `ModelInfo` struct (D-03), the `PromptCapabilities` struct (D-03), and the new `Name` field on the existing `ResourceLinkBlock` (D-04 / D-14). These types unblock Plans 02 (PromptCapabilities accessor + capture), 03 (AvailableModels accessor, parseStopReason helper, Stream.Result), and 04 (none — notification path uses chunk types from Phase 1).

Purpose: Phase 1's `internal/canonical` package is a leaf, type-only package per Phase 1 D-04 (imports nothing under internal/). Phase 1.1 adds NEW value types here so the ACP client can capture handshake state (Plan 02 / Plan 03) and surface a typed stop reason (Plan 03) — no breaking change to existing types except the additive `Name` field on `ResourceLinkBlock`.

Output: Three new files (per CONTEXT.md §Claude's Discretion — split layout chosen for symmetry with Phase 1's single-file-per-concern style: `chunk.go` exists; `stop_reason.go`, `model.go`, `capabilities.go` join it). One additive field on `ResourceLinkBlock` in `chunk.go`. One test file asserting type presence and zero-value behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-acp-wire-alignment/01.1-CONTEXT.md
@.planning/phases/01.1-acp-wire-alignment/01.1-PATTERNS.md
@docs/reference/acp_wire_shapes.md
@.planning/phases/01-foundations/01-CONTEXT.md
@internal/canonical/chunk.go

<interfaces>
<!-- Existing canonical types this plan extends. Style reference for new files. -->
<!-- Extracted from internal/canonical/chunk.go — no JSON tags, no imports, per-field doc comments. -->

From internal/canonical/chunk.go (existing; this plan only ADDS Name to ResourceLinkBlock):
```go
package canonical

type ChunkKind int

const (
    ChunkKindText ChunkKind = iota
    ChunkKindThought
    ChunkKindToolCall
    ChunkKindPlan
)

type BlockKind int

const (
    BlockKindText BlockKind = iota
    BlockKindResourceLink
    // (BlockKindImage explicitly NOT added in Phase 1.1 per D-15 — Phase 2 adds it)
)

type ResourceLinkBlock struct {
    URI   string
    Title string
    // Name string — ADDED in this plan per D-04 / D-14
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical type files (StopReason, ModelInfo, PromptCapabilities) and the Name field on ResourceLinkBlock</name>
  <files>internal/canonical/stop_reason.go, internal/canonical/model.go, internal/canonical/capabilities.go, internal/canonical/chunk.go</files>
  <read_first>
    - internal/canonical/chunk.go (read the entire file — it is the style template for the new files; pay attention to the package doc comment lines 1-3, the `type ChunkKind int` + iota pattern, the per-field doc comments on struct fields, and the absence of JSON tags / imports)
    - .planning/phases/01.1-acp-wire-alignment/01.1-CONTEXT.md sections `<decisions>` → D-01, D-02, D-03, D-04 (verbatim values for the enum constants, struct field names, and the empty-Name fallback rule)
    - .planning/phases/01.1-acp-wire-alignment/01.1-PATTERNS.md section `### NEW: internal/canonical/{stop_reason,model,capabilities}.go` (the Sibling patterns section lists the exact `chunk.go` excerpts to copy)
    - docs/reference/acp_wire_shapes.md §1 (PromptCapabilities flag names: `image`, `audio`, `embeddedContext`), §2 (ModelInfo wire shape — `modelId` + `name`), §4 (stopReason wire values), §6 (ResourceLinkBlock required `name`)
    - .planning/phases/01-foundations/01-CONTEXT.md D-04 + D-11 (canonical is a leaf package with no internal/ imports; no JSON tags on canonical types)
  </read_first>
  <action>
Create `internal/canonical/stop_reason.go`. Declare `package canonical`. Define `type StopReason int`. Declare a `const ( ... )` block with values in this exact order (so the zero-value is `StopUnknown` per D-02 for forward-compat): `StopUnknown StopReason = iota`, then `StopEndTurn`, `StopMaxTokens`, `StopMaxTurnRequests`, `StopRefusal`, `StopCancelled`. Place a doc comment above each constant matching the style of `ChunkKindText` in `chunk.go`. No `String()` method in this plan (not needed; downstream code compares enum values directly). No imports. No JSON tags.

Create `internal/canonical/model.go`. Declare `package canonical`. Define `type ModelInfo struct` with two exported fields: `ID string` (doc comment: "ID is the model identifier (wire field `modelId`).") and `Name string` (doc comment: "Name is the human-readable display name."). No JSON tags. No imports.

Create `internal/canonical/capabilities.go`. Declare `package canonical`. Define `type PromptCapabilities struct` with three exported boolean fields in this order: `Image bool` (doc comment: "Image is true when the agent accepts image content blocks."), `Audio bool` (doc comment: "Audio is true when the agent accepts audio content blocks."), `EmbeddedContext bool` (doc comment: "EmbeddedContext is true when the agent accepts embedded-resource blocks."). No JSON tags. No imports.

Edit `internal/canonical/chunk.go`. Locate the existing `ResourceLinkBlock` struct definition (currently has `URI string` and `Title string` fields). Add a third field `Name string` between `URI` and `Title` (or after `Title` — be consistent with the spec field order in `acp_wire_shapes.md` §6 which lists `uri, name, mimeType, title`; placing `Name` between `URI` and `Title` matches that ordering). Add doc comment: "Name is the human-readable file/resource name (wire field `name`, required by ACP spec on resource_link). When empty, the wire encoder falls back to `path.Base(URI)` per D-04." Do not add a JSON tag.

Do NOT add `BlockKindImage` (D-15: explicitly deferred to Phase 2; the wireBlock fields for image content are added in Plan 03's wire-shape work but the canonical block kind does not land in Phase 1.1).

Run `go build ./internal/canonical/...` to confirm compilation.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/Projects/repos/local/loop24-gateway &amp;&amp; go build ./internal/canonical/... &amp;&amp; go vet ./internal/canonical/...</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/canonical/stop_reason.go` exists; contains `type StopReason int` and `StopUnknown StopReason = iota` (so zero-value is StopUnknown).
    - Source assertion: `internal/canonical/stop_reason.go` contains all six constant names: `StopUnknown`, `StopEndTurn`, `StopMaxTokens`, `StopMaxTurnRequests`, `StopRefusal`, `StopCancelled`.
    - Source assertion: `internal/canonical/model.go` contains `type ModelInfo struct` with fields `ID string` and `Name string`.
    - Source assertion: `internal/canonical/capabilities.go` contains `type PromptCapabilities struct` with fields `Image bool`, `Audio bool`, `EmbeddedContext bool`.
    - Source assertion: `internal/canonical/chunk.go` `ResourceLinkBlock` struct now contains `Name string`.
    - Build: `go build ./internal/canonical/...` exits 0.
    - Vet: `go vet ./internal/canonical/...` exits 0.
    - Import-discipline: `grep -RE '^[[:space:]]*"loop24-gateway/internal/' internal/canonical/ | grep -v '_test.go'` returns zero matches (canonical imports nothing under internal/, per Phase 1 D-04).
    - JSON-tag discipline: `grep -E '\`json:' internal/canonical/stop_reason.go internal/canonical/model.go internal/canonical/capabilities.go` returns zero matches.
  </acceptance_criteria>
  <done>The four files compile, vet is clean, no internal/ imports, no JSON tags. All six StopReason constants present with StopUnknown as the zero value.</done>
</task>

<task type="auto">
  <name>Task 2: Add a presence/zero-value test for the new canonical types</name>
  <files>internal/canonical/types_test.go</files>
  <read_first>
    - internal/canonical/chunk.go (final file from Task 1 — read to confirm field names match the test)
    - internal/canonical/stop_reason.go (from Task 1)
    - internal/canonical/model.go (from Task 1)
    - internal/canonical/capabilities.go (from Task 1)
    - .planning/phases/01.1-acp-wire-alignment/01.1-CONTEXT.md D-02 (StopUnknown is the zero value — explicit assertion)
    - .planning/phases/01-foundations/01-CONTEXT.md D-18 (whitebox unit tests live in `package <pkg>`; this is the canonical package so `package canonical`)
  </read_first>
  <action>
Create `internal/canonical/types_test.go` with `package canonical`. Add a single test `TestCanonicalTypes_PresenceAndZeroValues(t *testing.T)`. It performs four checks:

(1) Assert `var s StopReason` evaluates to `StopUnknown` via `if s != StopUnknown { t.Errorf(...) }`. Then assert all six constants are distinct by comparing them pairwise (or simpler: confirm `StopEndTurn != StopUnknown && StopMaxTokens != StopEndTurn && ...`; a small table with `[]StopReason{StopUnknown, StopEndTurn, StopMaxTokens, StopMaxTurnRequests, StopRefusal, StopCancelled}` and a uniqueness check via a `map[StopReason]bool` is cleaner).

(2) Assert `var m ModelInfo` zero value has empty `ID` and `Name`; populate `ModelInfo{ID: "claude-sonnet-4-7", Name: "Claude Sonnet 4.7"}` and verify field round-trip.

(3) Assert `var p PromptCapabilities` zero value has all three flags `false`; populate `PromptCapabilities{Image: true, Audio: false, EmbeddedContext: true}` and verify field round-trip.

(4) Assert `var b ResourceLinkBlock` allows `b.Name = "foo.txt"` and round-trips it (i.e., the field exists and is settable).

Do not use `t.Parallel()` for this single-purpose presence test (consistent with the simplest Phase 1 test in the package; the variance tests in Plan 04 will use parallel).

Imports: only `"testing"`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/Projects/repos/local/loop24-gateway &amp;&amp; go test ./internal/canonical/... -run TestCanonicalTypes_PresenceAndZeroValues -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `go test ./internal/canonical/... -run TestCanonicalTypes_PresenceAndZeroValues -count=1` exits 0.
    - Source assertion: `internal/canonical/types_test.go` declares `package canonical`.
    - Behavior assertion: The test fails if `StopUnknown` is not the zero value of `StopReason` (verified by the test body using `var s StopReason; if s != StopUnknown { t.Errorf(...) }`).
    - Build: `go vet ./internal/canonical/...` exits 0.
  </acceptance_criteria>
  <done>The presence test compiles, passes, and asserts StopUnknown is the zero value plus the new struct fields are settable.</done>
</task>

</tasks>

<verification>
- `go build ./...` exits 0 (whole-repo build still green after the additive change).
- `go vet ./...` exits 0.
- `go test ./internal/canonical/... -count=1` exits 0.
- `grep -RE '^[[:space:]]*"loop24-gateway/internal/' internal/canonical/ | grep -v '_test.go'` returns zero (leaf-package invariant preserved).
</verification>

<success_criteria>
- Wave 1 outputs land: StopReason, ModelInfo, PromptCapabilities exist as exported canonical types; ResourceLinkBlock has a Name field.
- StopReason zero value is StopUnknown (D-02 forward-compat semantics).
- Plans 02, 03, and 04 can import these types from `loop24-gateway/internal/canonical` without compile error.
- Maps to ROADMAP success criteria #3 (resource_link required `name` per D-04) and is a prerequisite for criteria #1 (PromptCapabilities surfaced) and #3 (StopReason surfaced).
</success_criteria>

<output>
Create `.planning/phases/01.1-acp-wire-alignment/01.1-01-SUMMARY.md` when done. Record: files added, the chosen file-layout (split — `stop_reason.go` + `model.go` + `capabilities.go`), and confirmation that the leaf-package invariant (`internal/canonical` imports nothing under `internal/`) is preserved.
</output>
