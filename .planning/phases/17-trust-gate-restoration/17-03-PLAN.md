---
phase: 17-trust-gate-restoration
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/server.go
  - internal/pool/regression_rel_pool_01_test.go
  - internal/pool/regression_rel_pool_02_test.go
  - cmd/otto-tray/tray.go
  - internal/pool/pool.go
autonomous: true
requirements:
  - REL-FMT-GOFMT
  - REL-FMT-GOFUMPT
  - REL-LINT-G301
  - REL-LINT-G306
  - REL-LINT-UNUSED

must_haves:
  truths:
    - "gofmt -l . reports zero files (fmt-check stage of make ci passes)"
    - "gofumpt -l . reports zero files (lint stage's gofumpt sub-check passes)"
    - "golangci-lint run ./... reports zero G301 findings (gosec mkdir permission tightened to ≤0o750)"
    - "golangci-lint run ./... reports zero G306 findings (gosec writefile permission tightened to ≤0o600)"
    - "golangci-lint run ./... reports zero unused-function findings (Pool.removeSlot dead code removed)"
    - "All existing pool/session/adapter tests continue to pass under -race after the dead-code removal"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Reformatted struct-literal column alignment for the Server constructor (lines ~198-210); gofmt clean"
      contains: "shutdownCh"
    - path: "internal/pool/regression_rel_pool_01_test.go"
      provides: "Removed gofumpt-noisy leading blank line after the function-opening brace (line ~57 area)"
      contains: "fc0 := &fakeClient{}"
    - path: "internal/pool/regression_rel_pool_02_test.go"
      provides: "Removed gofumpt-noisy leading blank line after the function-opening brace (line ~62 area)"
      contains: "defer goleak.VerifyNone(t)"
    - path: "cmd/otto-tray/tray.go"
      provides: "MkdirAll permission tightened to 0o750 and WriteFile permission tightened to 0o600 in the support-bundle staging path; gosec G301 + G306 clean"
      contains: "0o750"
    - path: "internal/pool/pool.go"
      provides: "Pool.removeSlot dead function removed; stale comment references at lines 274, 695, 747 either updated or removed for hygiene"
      contains: "respawnSlot"
  key_links:
    - from: "Makefile fmt-check target"
      to: "internal/server/server.go"
      via: "gofmt -l finds zero files after the column-alignment fix"
      pattern: "shutdownCh:\\s+make\\(chan struct\\{\\}\\)"
    - from: "Makefile lint target (gosec)"
      to: "cmd/otto-tray/tray.go"
      via: "Permission constants 0o750 (MkdirAll) and 0o600 (WriteFile) satisfy G301/G306"
      pattern: "0o(750|600)"
---

<objective>
Close five mechanical trust-gate items in a single batched plan (per D-17-02 —
"five mechanical fixes in one commit; each fix is one-to-three lines;
reverting the batch as a unit is acceptable"). All five items were surfaced
by `make ci` at v1.9 milestone close and are listed in the 17-CONTEXT.md
driver table:

| # | Gate | Site | Fix |
|---|------|------|-----|
| 1 | fmt-check (gofmt) | `internal/server/server.go` ~lines 198-210 (struct field column alignment in NewWithCommit) | Run `gofmt -w internal/server/server.go` (or hand-fix the alignment); produces a small whitespace diff |
| 2 | fmt-check (gofumpt) | `internal/pool/regression_rel_pool_01_test.go` ~line 57 — leading blank line after function-opening brace | Delete the offending blank line |
| 3 | fmt-check (gofumpt) | `internal/pool/regression_rel_pool_02_test.go` ~line 62 — leading blank line after function-opening brace | Delete the offending blank line |
| 4 | lint (gosec G301) | `cmd/otto-tray/tray.go:367` — `os.MkdirAll(logDir, 0o755)` | Tighten to `0o750` |
| 5 | lint (gosec G306) | `cmd/otto-tray/tray.go:372` — `os.WriteFile(logPath, content, 0o644)` | Tighten to `0o600` |
| 6 | lint (unused) | `internal/pool/pool.go:342` — `(*Pool).removeSlot` is dead (zero non-comment callers; confirmed by repo-wide grep) | Delete the function; update or remove stale doc-comment references at lines 274, 695, 747 |

(Item 6 is the dead-code removal; items 4 and 5 are physically adjacent in
tray.go and share one commit. Items 2 and 3 are physically the same kind of
gofumpt fix in two test files. Item 1 is the gofmt fix in server.go. Per
D-17-02, all six edits ship in one batched commit.)

Per the D-17-05 scope guard, this plan does NOT touch the 5 Info-level
findings deferred from 16-REVIEW (escapeApplescript, tooltipForState
duplication, server.go indentation drift OUTSIDE the gofmt area on lines
206-208 — which IS covered by item 1 once gofmt is allowed to rewrite —,
forceCloseCh contract doc, tailLines O(n²)) nor the 12 Low reliability
findings deferred from v1.9. If any item's fix would require a behavioral
change beyond the mechanical edit described above (e.g., dead-code removal
breaks an unexpected consumer found at compile time), STOP and surface to
the operator.

Per the D-17-05 Windows risk-mitigation row: the 0o600 WriteFile tightening
on tray.go's support-bundle log file is acceptable under the project's
single-user laptop posture — no cross-user reader exists for this file. The
commit message must document this rationale so future operators understand
why the file is owner-readable only.

Purpose: closes all five trust-gate items in the cheapest possible batch.
Each fix is mechanical; the only risk is the dead-code removal cascading
(low probability — repo-wide grep confirms zero callers).
Output: A single atomic commit (or two — gofmt+gofumpt+dead-code in one,
gosec G301/G306 in a second, if the operator prefers smaller commits)
producing `gofmt -l .` and `gofumpt -l .` clean and `golangci-lint run ./...`
clean for these five rules.
</objective>

<execution_context>
@/Users/coreyellis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coreyellis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-trust-gate-restoration/17-CONTEXT.md
@.planning/phases/16-fix-mediums/16-04-SUMMARY.md
@internal/server/server.go
@cmd/otto-tray/tray.go
@internal/pool/pool.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: gofmt fix — internal/server/server.go column alignment in NewWithCommit</name>
  <files>internal/server/server.go</files>
  <read_first>
    - internal/server/server.go:198-210 — the NewWithCommit constructor body; gofmt wants the addr/shutdownCh/forceCloseCh fields aligned to the same column as cfg/logger/version/commit/start (currently the bottom three are over-indented with extra spaces)
    - .planning/phases/17-trust-gate-restoration/17-CONTEXT.md — driver table row 1 (gofmt fmt-check)
  </read_first>
  <action>
    Confirm gofmt drift exists:
    ```
    gofmt -l internal/server/server.go
    ```
    Expected: the file is listed in the output (gofmt wants to reformat it).

    Apply the gofmt fix:
    ```
    gofmt -w internal/server/server.go
    ```

    The likely change at lines 206-208 is removing two extra spaces from the field-value alignment column:
    Before (current — gofmt-drift):
    ```
        addr:         cfg.HTTPAddr,
        shutdownCh:   make(chan struct{}),
        forceCloseCh: make(chan struct{}),
    ```
    After (gofmt-clean — aligned with cfg/logger/version/commit/start above):
    ```
        addr:       cfg.HTTPAddr,
        shutdownCh: make(chan struct{}),
        forceCloseCh: make(chan struct{}),
    ```
    (Exact spacing depends on the longest field name; gofmt does the math. The point is to defer to gofmt's output, not hand-craft.)

    Verify clean:
    ```
    gofmt -l internal/server/server.go
    ```
    Expected: empty output (file is gofmt-clean).

    Verify no unrelated edits crept in (defensive):
    ```
    git diff --stat internal/server/server.go
    ```
    Expected: small diff (a handful of whitespace changes around lines 206-208), not a full-file rewrite. If gofmt rewrites large stretches, STOP — there may be deeper drift that warrants a separate plan or operator review.
  </action>
  <verify>
    <automated>gofmt -l internal/server/server.go | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>gofmt -l internal/server/server.go produces empty output. git diff is small and limited to whitespace changes in the NewWithCommit struct literal area.</done>
</task>

<task type="auto">
  <name>Task 2: gofumpt fix — strip leading blank lines in regression_rel_pool_0{1,2}_test.go</name>
  <files>internal/pool/regression_rel_pool_01_test.go, internal/pool/regression_rel_pool_02_test.go</files>
  <read_first>
    - internal/pool/regression_rel_pool_01_test.go:56-60 — the function `TestRegression_REL_POOL_01_PoolShrinksToZero` opens at line 56 with a brace followed by a blank line at line 57 before `defer goleak.VerifyNone(t)` — gofumpt forbids the leading blank line
    - internal/pool/regression_rel_pool_02_test.go:61-64 — same shape: `TestRegression_REL_POOL_02_CtrlCOrphansChildren` opens at line 61, blank line at line 62, then `defer goleak.VerifyNone(t)` at line 63
    - .planning/phases/17-trust-gate-restoration/17-CONTEXT.md — driver table row 2 (gofumpt fmt-check)
  </read_first>
  <action>
    Confirm gofumpt drift exists:
    ```
    gofumpt -l internal/pool/regression_rel_pool_01_test.go internal/pool/regression_rel_pool_02_test.go
    ```
    Expected: both files listed.

    Apply the gofumpt fix in batch:
    ```
    gofumpt -w internal/pool/regression_rel_pool_01_test.go internal/pool/regression_rel_pool_02_test.go
    ```

    Expected diff: deletion of one blank line in each file — the line immediately following the test function's opening brace.

    Verify clean:
    ```
    gofumpt -l internal/pool/regression_rel_pool_01_test.go internal/pool/regression_rel_pool_02_test.go
    ```
    Expected: empty output.

    Caveat: if Plan 17-02 is dispatched in parallel and lands on `regression_rel_pool_02_test.go` BEFORE this task runs, gofumpt's blank-line stripping must happen on top of 17-02's resultWg edits. Re-running `gofumpt -w` after 17-02's edits is safe — gofumpt is idempotent. The wave-1 parallel dispatch should be checked at plan-coordination time: if execute-phase auto-degrades to sequential, the 17-CONTEXT-recommended order (17-03 → 17-01 → 17-02) means this plan runs FIRST and 17-02 then has to re-add resultWg into the already-gofumpt-clean file.
  </action>
  <verify>
    <automated>gofumpt -l internal/pool/regression_rel_pool_01_test.go internal/pool/regression_rel_pool_02_test.go | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>gofumpt -l on both files produces empty output. Diff shows exactly one blank line deletion in each file.</done>
</task>

<task type="auto">
  <name>Task 3: gosec G301 + G306 — tighten support-bundle staging permissions in cmd/otto-tray/tray.go</name>
  <files>cmd/otto-tray/tray.go</files>
  <read_first>
    - cmd/otto-tray/tray.go:355-378 — the support-bundle error-handling block; lines 366-374 are the MkdirAll + WriteFile sites
    - cmd/otto-tray/tray.go:367 — `os.MkdirAll(logDir, 0o755)` — gosec G301 wants ≤ 0o750
    - cmd/otto-tray/tray.go:372 — `os.WriteFile(logPath, []byte(content), 0o644)` — gosec G306 wants ≤ 0o600
    - .planning/phases/16-fix-mediums/16-04-SUMMARY.md — context on the original Phase 16 T-7 work that introduced these sites (the v1.9 milestone-close gosec scan added G301/G306 rules that this code was written before)
    - .planning/phases/17-trust-gate-restoration/17-CONTEXT.md — driver table rows 3+4 (gosec G301 + G306); D-17-05 risk-mitigation row 4 (Windows single-user posture means 0o600 is safe)
  </read_first>
  <action>
    Edit `cmd/otto-tray/tray.go`:

    Step 1: at line 367, change `os.MkdirAll(logDir, 0o755)` to `os.MkdirAll(logDir, 0o750)`. Rationale: the support-bundle staging directory only needs to be readable + writable by the owning user (the operator running otto-gateway); group-readable is fine but world-readable is unnecessary for a per-user staging dir under installRoot.

    Step 2: at line 372, change `os.WriteFile(logPath, []byte(content), 0o644)` to `os.WriteFile(logPath, []byte(content), 0o600)`. Rationale: the support-bundle log file may contain stderr from kiro-cli (potentially including model output, tool-call args, or environment-variable echoes). Per the D-17-05 risk-mitigation row, this is acceptable under the project's single-user laptop posture documented in `docs/operating.md` `### v1 no-auth posture`. No cross-user reader exists.

    Verify gosec is now clean for these sites:
    ```
    golangci-lint run --enable=gosec ./cmd/otto-tray/... 2>&1 | grep -E "G301|G306" | wc -l
    ```
    Expected: 0.

    Verify the file still compiles:
    ```
    go build ./cmd/otto-tray/...
    ```

    Caveat: cmd/otto-tray has platform-specific build tags. Confirm the file in question (tray.go) does NOT have a `//go:build` tag that excludes it from the default build — if it does, the gosec check must also exercise that GOOS. Quick check:
    ```
    head -3 cmd/otto-tray/tray.go
    ```
    If the first line is a build tag like `//go:build !linux`, also run the gosec check under the relevant GOOS env (`GOOS=darwin golangci-lint run ...` etc.). Note: the v1.9 milestone gosec run that surfaced these findings ran on a particular GOOS; reproducing the same gate requires the same GOOS env.
  </action>
  <verify>
    <automated>go build ./cmd/otto-tray/... && golangci-lint run --enable=gosec ./cmd/otto-tray/... 2>&1 | grep -cE "G301|G306" | grep -q "^0$"</automated>
  </verify>
  <done>tray.go:367 reads 0o750 and tray.go:372 reads 0o600. golangci-lint with gosec enabled reports zero G301/G306 findings in cmd/otto-tray. go build ./cmd/otto-tray/... exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: dead-code removal — delete Pool.removeSlot and update stale comments</name>
  <files>internal/pool/pool.go</files>
  <read_first>
    - internal/pool/pool.go:338-351 — the removeSlot function body (zero non-comment callers in production; confirmed by repo-wide grep)
    - internal/pool/pool.go:274 — stale comment in respawnSlot doc: "the caller is expected to call removeSlot to drop the dead slot from p.all (D-03)"
    - internal/pool/pool.go:695 — stale inline comment: "landed in removeSlot. The slot is still dead (we haven't ..."
    - internal/pool/pool.go:747 — stale inline comment: "calling removeSlot. This preserves the pool's effective size so"
    - internal/pool/regression_rel_pool_01_test.go:38,46,50,102 — test-file references in comments (these stay; they're historical notes on the pre-fix behavior)
    - internal/pool/respawn_ctx_cancel_test.go:119 — test-file comment reference (stays; historical)
    - .planning/phases/17-trust-gate-restoration/17-CONTEXT.md — driver table row 5 (lint unused); origin "Phase 5 D-03 leftover, pre-v1.9 debt"
  </read_first>
  <action>
    Step 1: confirm dead code with a fresh repo-wide grep:
    ```
    grep -rn "removeSlot" internal/ cmd/ --include="*.go" | grep -v "_test.go" | grep -v "^[^:]*:[^:]*:\s*//"
    ```
    Expected: only the function definition at internal/pool/pool.go:342 and the comment lines at :274, :695, :747 — confirming zero non-comment callers in production. Test files and comments don't count as callers.

    If any non-comment caller appears: STOP and surface to the operator — the function is not actually dead and the lint warning is a false positive (or someone added a caller since the lint run).

    Step 2: delete the removeSlot function (lines 338-351 in pool.go — adjust by actual line numbers at task time):
    ```go
    // removeSlot drops slot from p.all so the pool effective size shrinks
    // (Phase 5 D-03 — respawn failure path). Held under p.mu briefly.
    // The slot is NOT returned to p.slots; subsequent NewSession callers
    // will compete for the remaining alive slots.
    func (p *Pool) removeSlot(slot *Slot) {
        p.mu.Lock()
        defer p.mu.Unlock()
        for i, s := range p.all {
            if s == slot {
                p.all = append(p.all[:i], p.all[i+1:]...)
                return
            }
        }
    }
    ```

    Step 3: update the stale comment in respawnSlot's doc at line 274. Current text: `// to call removeSlot to drop the dead slot from p.all (D-03).` Update to: `// to re-queue the slot via the same release path as the ctx-cancel branch (D-03; the removeSlot path was removed as dead code in Phase 17).`

    Step 4: update or remove the inline comments at lines 695 and 747. They reference removeSlot in the context of the slot lifecycle; brief edits to either (a) remove the removeSlot mention or (b) note "removed in Phase 17" are both acceptable. Operator preference: minimize the diff — keep the surrounding logic-explanation prose intact, just delete or rephrase the half-sentence that names removeSlot.

    Step 5: verify:
    ```
    go build ./...
    go vet ./...
    golangci-lint run --enable=unused ./internal/pool/... 2>&1 | grep -E "removeSlot" | wc -l
    # Expected: 0 (no more unused findings on removeSlot)

    go test -race -count=1 ./internal/pool/...
    # Expected: PASS (no test broke from the removal)
    ```

    Step 6: spot-check the regression tests:
    ```
    grep -n "removeSlot" internal/pool/regression_rel_pool_01_test.go internal/pool/respawn_ctx_cancel_test.go
    ```
    These references are in test-file comments describing pre-fix behavior — they stay as historical context (the test names assert the pre-fix bug, the comments explain the bug, the fix is in pool.go). Do NOT edit the test files in this task.
  </action>
  <verify>
    <automated>go build ./... && go test -race -count=1 ./internal/pool/... && grep -c "func (p \*Pool) removeSlot" internal/pool/pool.go | grep -q "^0$"</automated>
  </verify>
  <done>removeSlot function is gone from pool.go. go build ./... + go vet ./... + go test -race ./internal/pool/... all exit 0. golangci-lint --enable=unused reports zero removeSlot findings. Stale comments at lines 274, 695, 747 are updated or have the removeSlot reference removed.</done>
</task>

<task type="auto">
  <name>Task 5: Batched commit + full make ci sweep</name>
  <files>(no new edits — stages the prior four tasks' edits, runs make ci, commits)</files>
  <read_first>
    - .planning/phases/17-trust-gate-restoration/17-CONTEXT.md — Verification section criteria 1-4 (make ci 0; arch-lint notices: 0 is owned by Plan 17-01; REL-POOL-02 20/20 is owned by Plan 17-02; Plan 17-03 owns fmt + gosec + unused)
    - Makefile — the ci target's stage order (fmt-check → vet → build → lint → test-race → arch-lint → examples → govulncheck)
  </read_first>
  <action>
    This task assumes Tasks 1-4 have committed (or staged) their edits to the working tree.

    Step 1: stage all edits from Tasks 1-4. The full file list:
    - `internal/server/server.go` (Task 1 gofmt)
    - `internal/pool/regression_rel_pool_01_test.go` (Task 2 gofumpt)
    - `internal/pool/regression_rel_pool_02_test.go` (Task 2 gofumpt)
    - `cmd/otto-tray/tray.go` (Task 3 gosec G301+G306)
    - `internal/pool/pool.go` (Task 4 dead-code removal)

    Step 2: confirm with a focused git status:
    ```
    git status --short internal/server/server.go internal/pool/regression_rel_pool_01_test.go internal/pool/regression_rel_pool_02_test.go cmd/otto-tray/tray.go internal/pool/pool.go
    ```

    Step 3: choose commit granularity. Per D-17-02, batching is fine: "five mechanical fixes in one commit; each fix is one-to-three lines; reverting the batch as a unit is acceptable." Default to a single commit:
    ```
    fix(trust-gate): mechanical batch — gofmt + gofumpt + gosec G301/G306 + dead-code (Phase 17, D-17-02)
    ```
    Commit body should note:
    - `internal/server/server.go` — gofmt-clean struct literal alignment in NewWithCommit
    - `internal/pool/regression_rel_pool_0{1,2}_test.go` — gofumpt-clean leading blank lines
    - `cmd/otto-tray/tray.go` — gosec G301: MkdirAll 0o755→0o750; G306: WriteFile 0o644→0o600 (single-user laptop posture per D-17-05 risk row 4)
    - `internal/pool/pool.go` — Pool.removeSlot dead-code removal (Phase 5 D-03 leftover; zero callers in production)

    Step 4: run the full make ci sweep BEFORE pushing/concluding. Plan 17-03's task is to confirm all five mechanical items close, but the broader make ci stages (arch-lint and test-race for REL-POOL-02) are owned by 17-01 and 17-02. If Plans 17-01 and 17-02 have already merged (or are being executed in parallel and are also green), `make ci` should now exit 0 end-to-end:
    ```
    make ci
    ```
    If Plans 17-01 / 17-02 have NOT landed yet, `make ci` will still fail on arch-lint and/or test-race. That's expected — capture the gate-state evidence:
    ```
    make ci 2>&1 | tee /tmp/17-03-makeci.log
    echo "exit=$?"
    grep -E "fmt-check|gofumpt|G301|G306|unused.*removeSlot" /tmp/17-03-makeci.log
    ```
    Expected: the four lint/fmt items this plan closes (fmt-check, gofumpt, G301, G306, unused removeSlot) produce NO failure lines. Any remaining failures are from arch-lint (owned by 17-01) or REL-POOL-02 (owned by 17-02) — those are NOT this plan's responsibility.

    Step 5: capture per-gate evidence for the SUMMARY.md:
    ```
    gofmt -l . 2>&1 | grep -v '^vendor/' | wc -l   # expect 0
    gofumpt -l . 2>&1 | grep -v '^vendor/' | wc -l # expect 0
    golangci-lint run ./... 2>&1 | grep -cE "G301|G306|removeSlot" # expect 0
    ```
  </action>
  <verify>
    <automated>gofmt -l . 2>&1 | grep -v '^vendor/' | wc -l | grep -q "^0$" && gofumpt -l . 2>&1 | grep -v '^vendor/' | wc -l | grep -q "^0$" && test "$(golangci-lint run ./... 2>&1 | grep -cE 'G301|G306|removeSlot')" -eq 0</automated>
  </verify>
  <done>One commit (or two, operator preference) staged and landed. gofmt -l . clean. gofumpt -l . clean. golangci-lint G301 / G306 / unused-removeSlot all report zero findings. /tmp/17-03-makeci.log captured for SUMMARY.md evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd/otto-tray/tray.go → filesystem | The support-bundle log file is written by the otto-gateway operator's user-level process; permission tightening shrinks the read surface to the owning user only. |
| internal/pool/pool.go API | Removing removeSlot reduces the package's exported method count; the function was unexported (lowercase r) so no external API change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | tray.go support-bundle log file (0o644 → 0o600) | mitigate | Tightening the permission to 0o600 ensures the file (which may contain kiro-cli stderr including model output or env-var echoes) is readable only by the owning user. The D-17-05 risk-mitigation row 4 confirms this is acceptable under the project's single-user laptop posture. Commit message documents the rationale. |
| T-17-03-02 | Tampering | tray.go support-bundle staging dir (0o755 → 0o750) | mitigate | Tightening MkdirAll to 0o750 prevents world-write surface on the staging directory. Group-readable retained for any group-based tooling on Linux dev boxes; world-readable removed. |
| T-17-03-03 | Denial of Service | Removing Pool.removeSlot unmasks an unexpected caller at runtime | mitigate | Repo-wide grep in Task 4 Step 1 confirms zero non-comment callers in production code. go build ./... in Task 4 Step 5 verifies compile-time. go test -race ./internal/pool/... in Task 4 Step 5 verifies runtime — any caller in test code would surface as a compile error before the runtime check. |
| T-17-03-04 | Repudiation | server.go gofmt edits accidentally rewrite unrelated code | mitigate | Task 1 Step 4 includes a `git diff --stat` check that flags any unexpectedly large rewrite. Operator stop-and-review if drift extends beyond the lines 198-210 struct literal. |
| T-17-03-SC | Tampering | npm/pip/cargo installs | accept | None — pure in-tree Go edits, zero new dependencies. The package-legitimacy gate is not engaged. |
</threat_model>

<verification>
## Phase-level checks for Plan 17-03

```bash
# Item 1: gofmt clean
gofmt -l . 2>&1 | grep -v '^vendor/'
# Expected: empty

# Item 2+3: gofumpt clean
gofumpt -l . 2>&1 | grep -v '^vendor/'
# Expected: empty

# Item 4: G301 closed
golangci-lint run --enable=gosec ./cmd/otto-tray/... 2>&1 | grep -c "G301"
# Expected: 0
grep -n "0o750" cmd/otto-tray/tray.go
# Expected: at least one match around line 367

# Item 5: G306 closed
golangci-lint run --enable=gosec ./cmd/otto-tray/... 2>&1 | grep -c "G306"
# Expected: 0
grep -n "0o600" cmd/otto-tray/tray.go
# Expected: at least one match around line 372

# Item 6: dead code gone
grep -c "func (p \*Pool) removeSlot" internal/pool/pool.go
# Expected: 0
golangci-lint run --enable=unused ./internal/pool/... 2>&1 | grep -c "removeSlot"
# Expected: 0

# No new test failures
go test -race -count=1 ./internal/pool/... ./internal/server/... ./cmd/otto-tray/...
# Expected: PASS

# Full lint suite passes for the five rules this plan owns
golangci-lint run ./... 2>&1 | grep -cE "G301|G306|removeSlot"
# Expected: 0
```
</verification>

<success_criteria>
1. `gofmt -l .` produces no non-vendor output.
2. `gofumpt -l .` produces no non-vendor output.
3. `golangci-lint run --enable=gosec ./cmd/otto-tray/...` reports zero G301 and zero G306 findings.
4. `golangci-lint run --enable=unused ./internal/pool/...` reports zero findings for `removeSlot`.
5. `cmd/otto-tray/tray.go` line ~367 reads `os.MkdirAll(logDir, 0o750)`; line ~372 reads `os.WriteFile(logPath, []byte(content), 0o600)`.
6. `internal/pool/pool.go` no longer contains a `removeSlot` function definition; the three stale comments at the former lines 274, 695, 747 are updated to remove the removeSlot mention (or note "removed in Phase 17").
7. `go build ./...` + `go vet ./...` + `go test -race -count=1 ./...` exit 0.
8. One or two atomic commits on the main branch with `fix(trust-gate): mechanical batch — ...` (or split per operator preference per D-17-02).
9. Combined with Plans 17-01 (arch-lint) and 17-02 (REL-POOL-02 deflake) landing, `make ci` exits 0 end-to-end — the phase-close criterion #1 from 17-CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/17-trust-gate-restoration/17-03-SUMMARY.md` when done. The summary must record:
- The exact 5 mechanical edits applied with file:line references
- The `make ci` post-batch exit code (if Plans 17-01 / 17-02 have already landed, capture the green output; otherwise note which stages still depend on the other two plans)
- Confirmation that the D-17-05 single-user laptop posture rationale was recorded in the commit message for the 0o600 WriteFile tightening
</output>
