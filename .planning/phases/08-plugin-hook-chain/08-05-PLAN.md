---
phase: 08-plugin-hook-chain
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - internal/config/config.go
  - internal/config/plugin_config_test.go
  - internal/plugin/chain.go
  - internal/plugin/chain_filter_test.go
  - internal/server/server.go
  - internal/server/hooks_handler.go
  - internal/server/hooks_handler_test.go
  - internal/adapter/ollama/handlers.go
  - internal/adapter/ollama/handlers_test.go
  - internal/adapter/openai/handlers.go
  - internal/adapter/openai/handlers_test.go
  - internal/adapter/anthropic/handlers.go
  - internal/adapter/anthropic/handlers_test.go
  - cmd/otto-gateway/main.go
  - tests/e2e/plugin_chain_test.go
autonomous: false
requirements: [PLUG-05, OBSV-03, OBSV-04]
tags: [plugin, config, chain-filter, health-hooks, main, e2e, migration]
user_setup: []
must_haves:
  truths:
    - "D-01 (hardcoded slice): cmd/otto-gateway/main.go constructs a single literal `plugin.Chain{Pre: []engine.PreHook{...}, Post: []engine.PostHook{...}}` containing the four hooks (RequestIDHook → AuthHook → PIIRedactionHook → LoggingHook on Pre; LoggingHook only on Post per D-04). No Register(name, factory) indirection. Adding a 5th hook = one line in main.go."
    - "D-02 + SC5 (ENABLED_HOOKS allowlist; registration order preserved; typo = boot error): main.go calls `chain.Filter(cfg.EnabledHooks)`; the returned chain preserves REGISTRATION order (NOT allowlist order — test asserts `ENABLED_HOOKS=LoggingHook,RequestIDHook` produces order `[RequestIDHook, LoggingHook]`); unknown name returns error containing `unknown hook`; boot fails with non-zero exit."
    - "D-05 (PII_HASH_KEY required when mode=hash) + RESEARCH Pitfall 6: config.Load() returns error containing `PII_HASH_KEY` when `PII_REDACTION_MODE=hash` AND `PII_HASH_KEY` is empty/unset. Gateway refuses to start (no silent unkeyed-HMAC fallback)."
    - "SC1 (PreHook short-circuit through chain → adapter renders): a bad bearer token causes AuthHook (slice 2) to return a non-nil `*canonical.ChatResponse`; engine.Collect respects Codex H-4 short-circuit; adapter renders the canonical envelope as per-surface error (OpenAI `{error:{...}}` / Ollama `{error:\"...\"}` / Anthropic `{type:\"error\", error:{...}}`). E2E test verifies all three surfaces."
    - "SC7 + OBSV-04 + TRST-04: `GET /health/hooks` is auth-exempt (mounted on outer router alongside `/health` and `/health/agents` per `internal/server/server.go:196-200` precedent); returns JSON `{hooks: [{name, kind, enabled, config}]}` in registration order; mutating verbs (POST/PUT/DELETE) return 405; consumer-defined `HooksDescriptionSource` interface declared in server/ (NOT imported from plugin/) so adapter-over-canonical boundary holds; `server.HookDescription` redeclared structurally (mirror of `server.AgentSlot` pattern at agents.go:40-45)."
    - "T-8-LEAK: /health/hooks JSON response contains NO secrets — no `Tokens`, no `PIIHashKey`, no `Pattern.String()` regex sources. Handler test enforces; e2e test scans the response body and fails if any literal secret value or regex fragment appears."
    - "Migration boundary closure (08-PATTERNS Pattern F): if slice 2's SUMMARY documented `auth.Bearer` chi middleware as REMAINING in place, this slice REMOVES `auth.Bearer` from `internal/server/server.go` once AuthHook is wired into the chain and per-surface adapters stamp the credential onto ctx. `auth.IPAllowlist` stays at chi (surface-agnostic IP gate). Migration sequence: (1) AuthHook in chain, (2) adapters stamp ctx, (3) verify e2e 401 still works, (4) remove `auth.Bearer` middleware in same commit. If slice 2 already removed it, this task verifies the removal and adds a regression test."
    - "OBSV-03 / SC2 production path (Task 4b): all three adapter handlers (Ollama, OpenAI, Anthropic) read `r.Header.Get(\"X-Request-Id\")`, mint a ULID via `plugin.NewRequestID()` when absent, and stamp the ctx via `plugin.WithRequestID(ctx, id)` BEFORE invoking `engine.Run`. This is the symmetric parallel of slice 2's `canonical.WithBearerToken` adapter stamp. Without this stamp, slice 3's LoggingHook sync.Map keyed by request_id and slice 4's PII summary stash collide across concurrent requests because RequestIDFromContext returns \"\" — Task 4b closes that production gap. E2E coverage in Task 6 asserts an inbound `X-Request-Id` round-trips through every slog record carrying that exact id."
  artifacts:
    - path: "internal/config/config.go"
      provides: "EnabledHooks []string + PIIRedactionEnabled bool + PIIEnabledEntities []string + PIIRedactionMode string + PIIHashKey string Config fields + Load() validation (mode-enum + hash-mode-requires-key + entities-typo-fail-fast)"
      contains: "EnabledHooks"
    - path: "internal/config/plugin_config_test.go"
      provides: "Boot validation tests: ENABLED_HOOKS typo, PII_REDACTION_MODE=hash without PII_HASH_KEY, unknown PII_REDACTION_MODE, unknown PII_ENABLED_ENTITIES"
      contains: "TestLoad_PIIHashModeRequiresKey"
    - path: "internal/plugin/chain.go"
      provides: "Filter([]string) (Chain, error) — full impl per 08-PATTERNS chain.go block (slice 1 declared the type + skeleton; slice 5 finalizes Filter behavior covered by chain_filter_test.go)"
      contains: "func (c Chain) Filter"
    - path: "internal/plugin/chain_filter_test.go"
      provides: "Filter contract tests: empty allowlist passthrough, known-name filter (registration order preserved), unknown name error, mixed-case unknown error"
      contains: "TestChainFilter_"
    - path: "internal/server/hooks_handler.go"
      provides: "hooksHandler — GET /health/hooks JSON envelope + 405 on mutating verbs + secret-omission audit"
      contains: "func (s *Server) hooksHandler"
    - path: "internal/server/server.go"
      provides: "HookDescription struct (structurally redeclared mirror of plugin.HookDescription per agents.go:40-45 pattern) + HooksDescriptionSource interface + Config.Hooks field + s.router.Get /health/hooks registration"
      contains: "/health/hooks"
    - path: "internal/server/hooks_handler_test.go"
      provides: "Handler tests: empty chain, four-hook chain, registration-order assertion, secret-omission audit, 405-on-mutate, auth-exempt"
      contains: "TestHooksHandler_"
    - path: "cmd/otto-gateway/main.go"
      provides: "Hardcoded plugin.Chain literal + chain.Filter(cfg.EnabledHooks) + engine.Config.PreHooks/PostHooks assignment + filterRecognizers(pii.Recognizers, cfg.PIIEnabledEntities) + hooksDescriptionAdapter (consumer-defined-interface satisfaction) + auth.Bearer middleware removal (if slice 2 left it)"
      contains: "plugin.Chain{"
    - path: "tests/e2e/plugin_chain_test.go"
      provides: "Real-binary e2e — ENABLED_HOOKS gating; PII end-to-end redaction; /health/hooks shape + auth-exempt + 405 + secret-omission; three-surface bad-bearer short-circuit; hash-mode-no-key boot error; unknown-hook boot error"
      contains: "TestE2E_"
  key_links:
    - from: "cmd/otto-gateway/main.go"
      to: "internal/plugin (Chain construction)"
      via: "literal slice of engine.PreHook + engine.PostHook"
      pattern: "plugin\\.Chain\\{"
    - from: "cmd/otto-gateway/main.go"
      to: "internal/plugin (chain.Filter)"
      via: "ENABLED_HOOKS allowlist application"
      pattern: "\\.Filter\\(cfg\\.EnabledHooks\\)"
    - from: "internal/server/hooks_handler.go"
      to: "internal/server.HooksDescriptionSource (consumer-defined interface)"
      via: "Describe() (pre, post []HookDescription)"
      pattern: "HooksDescriptionSource"
    - from: "cmd/otto-gateway/main.go"
      to: "internal/server.HooksDescriptionSource"
      via: "hooksDescriptionAdapter wraps plugin.Chain — TRST-04 discipline"
      pattern: "hooksDescriptionAdapter"
    - from: "tests/e2e/plugin_chain_test.go"
      to: "/health/hooks endpoint (real binary)"
      via: "http.Get against bootGateway harness"
      pattern: "/health/hooks"
---

<objective>
Slice 5 — integration vertical slice (the Phase 8 acceptance bar). Wire
all four hooks (slices 1-4) together in main.go as a hardcoded
plugin.Chain literal (D-01); add the five Phase 8 env keys + boot
validation (D-05 + Pitfall 6 + Pitfall 7) to internal/config; ship
chain.Filter's ENABLED_HOOKS allowlist semantics + typo-fail-fast (D-02);
expose the registered chain via `GET /health/hooks` (OBSV-04 / SC7);
verify end-to-end via tests/e2e/plugin_chain_test.go that boots the real
binary under multiple env configurations and asserts per-surface
short-circuit envelopes + PII redaction + boot-error refusals.

Purpose: Without slice 5, slices 1-4 produce libraries; with slice 5,
they produce a runnable gateway whose architecture cashes out the "one
place to enforce policy" core value of PROJECT.md. Auth lives at the
canonical layer (one hook serving three surfaces); PII redaction
applies uniformly; observability via X-Request-Id correlation works
across all spans; the operator's introspection knob is GET /health/hooks.

Output: Production-ready Phase 8 closeout — env config complete, main.go
wired, /health/hooks endpoint ships, e2e suite covers SC1+SC5+SC7
acceptance bars + T-8-CFG and T-8-LEAK threat mitigations. Followed by
a blocking human-verify checkpoint that validates the full operator
workflow against the real running binary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-plugin-hook-chain/08-CONTEXT.md
@.planning/phases/08-plugin-hook-chain/08-RESEARCH.md
@.planning/phases/08-plugin-hook-chain/08-PATTERNS.md
@.planning/phases/08-plugin-hook-chain/08-VALIDATION.md
@.planning/phases/08-plugin-hook-chain/08-01-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-02-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-03-SUMMARY.md
@.planning/phases/08-plugin-hook-chain/08-04-SUMMARY.md
@internal/config/config.go
@internal/config/config_test.go
@internal/plugin/chain.go
@internal/plugin/request_id.go
@internal/plugin/auth.go
@internal/plugin/logging.go
@internal/plugin/pii/pii.go
@internal/plugin/pii/recognizers.go
@internal/server/server.go
@internal/server/agents.go
@internal/server/agents_test.go
@cmd/otto-gateway/main.go
@tests/e2e/e2e_test.go
@.go-arch-lint.yml
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (untrusted by intent — typos common) → config.Load | All five new env keys (`ENABLED_HOOKS`, `PII_REDACTION_ENABLED`, `PII_ENABLED_ENTITIES`, `PII_REDACTION_MODE`, `PII_HASH_KEY`) parsed and validated at boot |
| chain.Filter → engine.Config | Filtered chain passed into engine via PreHooks/PostHooks slices |
| external client → /health/hooks (auth-exempt) | Public introspection — must expose NO secrets |
| boot process → operator (stderr) | Boot-error messages must be CLEAR enough for the operator to fix without source-diving |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-CFG | Spoofing / Tampering (typo silently disables PII or other hooks) | `cmd/otto-gateway/main.go` ENABLED_HOOKS filter + `internal/config/config.go` PII mode/entity validation | mitigate | (a) `chain.Filter` returns error on unknown name (D-02 + RESEARCH Pitfall 7); main.go propagates the error → non-zero exit + stderr containing `unknown hook`. (b) `config.Load` rejects unknown `PII_REDACTION_MODE` values + unknown `PII_ENABLED_ENTITIES` names. (c) E2E test boots binary with `ENABLED_HOOKS=BogusHook` and asserts non-zero exit + stderr-contains `unknown hook`. (d) E2E test boots binary with `PII_ENABLED_ENTITIES=BadEntity` and asserts non-zero exit. |
| T-8-LEAK | Information Disclosure (`/health/hooks` exposes regex source / hash key / token values) | `internal/server/hooks_handler.go` + each hook's `Describe()` | mitigate | (a) Hook Describe() implementations expose ONLY safe-to-publish fields (slice 1: RequestIDHook `{format: ulid}`; slice 2: AuthHook `{token_count: N}`; slice 3: LoggingHook `{level: INFO}`; slice 4: PIIRedactionHook `{enabled, mode, entities}`) — never `Tokens`, `HashKey`, `Pattern.String()`. (b) Handler-level test (`hooks_handler_test.go`) constructs a chain with non-zero secret values and asserts the JSON response body does NOT contain those literal secret strings. (c) E2E test executes `curl /health/hooks` against a binary booted with `AUTH_TOKEN=topsecret` + `PII_HASH_KEY=ALSOSECRET` and greps the response body for both literal strings — both must be absent. (d) Source-audit grep on Describe() implementations enforced in CI. |
| T-8-HASH-BOOT | Cryptographic weakness (mode=hash with empty key = fixed-key HMAC = rainbow-table-trivial) | `internal/config/config.go` Load() validation | mitigate | Boot validation per RESEARCH Pitfall 6: if `PIIRedactionMode == "hash"` AND `len(PIIHashKey) == 0`, return error containing `PII_HASH_KEY`. Tested via `TestLoad_PIIHashModeRequiresKey` (unit) AND via e2e test (real-binary boot fails with stderr-contains `PII_HASH_KEY`). |
| T-8-AUTH-BYPASS | Spoofing / EoP (slice 2 left auth.Bearer middleware in place; if slice 5 removes it but AuthHook is filtered out via ENABLED_HOOKS, auth disappears entirely) | `cmd/otto-gateway/main.go` + chain Filter interaction | mitigate | (a) E2E test boots binary with `ENABLED_HOOKS=RequestIDHook,LoggingHook` (deliberately excludes AuthHook) AND `AUTH_TOKEN=secret`; asserts auth IS bypassed (200 returned with no bearer header). (b) Document this as accepted behavior: ENABLED_HOOKS=… without AuthHook means auth is DISABLED — operator's explicit choice. (c) Cross-reference docs/operating.md update via the human-verify checkpoint. |
| T-8-SC | Tampering (npm/pip/cargo — N/A; no new deps in slice 5) | n/a | n/a | Slice 5 introduces no new external dependencies — only stdlib + project-internal packages. Package legitimacy gate not re-fired (RESEARCH §Package Legitimacy Audit covered slice 1's oklog/ulid/v2). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — `internal/config/plugin_config_test.go` + `internal/plugin/chain_filter_test.go` + `internal/server/hooks_handler_test.go` scaffolds</name>
  <files>
    internal/config/plugin_config_test.go,
    internal/plugin/chain_filter_test.go,
    internal/server/hooks_handler_test.go
  </files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/config/plugin_config_test.go` block (lines 854-871) — t.Setenv + Load() boot-error layout
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/server/hooks_handler_test.go` block (lines 876-899) — fake-source pattern + httptest
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Pitfall 6 + Pitfall 7 (lines 704-734) — boot-error contracts
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Example 6 (/health/hooks JSON envelope, lines 1050-1104) — expected response shape
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md §health-hooks + config-validation rows (lines 52-53)
    - internal/config/config_test.go:41-104 — TestLoadEnvOverrides table layout precedent + t.Setenv usage
    - internal/server/agents_test.go:1-118 — fake-source + httptest + secret-omission precedent
    - internal/plugin/chain.go (slice 1) — Chain.Filter signature + slice 1 chain_test.go covers basic passthrough; slice 5 covers full contract
  </read_first>
  <behavior>
    **plugin_config_test.go**:
    - Test 1: `TestLoad_EnabledHooks_Parsing` — `t.Setenv("ENABLED_HOOKS", "RequestIDHook,LoggingHook")`; `cfg, err := config.Load()`; assert `err == nil`; assert `cfg.EnabledHooks` deep-equal `[]string{"RequestIDHook", "LoggingHook"}` (order preserved). Test variant: `ENABLED_HOOKS=""` → `cfg.EnabledHooks` is nil or empty slice (default-permissive).
    - Test 2: `TestLoad_PIIRedactionEnabled_Default` — without env set, `cfg.PIIRedactionEnabled == false` (operator must opt in per D-02 composition rule).
    - Test 3: `TestLoad_PIIRedactionEnabled_TrueValue` — `t.Setenv("PII_REDACTION_ENABLED", "true")`; assert `cfg.PIIRedactionEnabled == true`. Variant: `"false"`, `""`, `"yes"` (planner picks accepted truthy values; document).
    - Test 4: `TestLoad_PIIEnabledEntities_Parsing` — `t.Setenv("PII_ENABLED_ENTITIES", "Email,SSN")`; assert `cfg.PIIEnabledEntities` deep-equal `[]string{"Email", "SSN"}`.
    - Test 5: `TestLoad_PIIEnabledEntities_UnknownNameError` — `t.Setenv("PII_ENABLED_ENTITIES", "Email,BadEntity")`; `_, err := config.Load()`; assert `err != nil` AND `err.Error()` contains `PII_ENABLED_ENTITIES` AND `BadEntity`.
    - Test 6: `TestLoad_PIIRedactionMode_Default` — without env set, `cfg.PIIRedactionMode == "replace"`.
    - Test 7: `TestLoad_PIIRedactionMode_ValidModes` — for each of `["replace", "mask", "hash", "drop"]`: `t.Setenv("PII_REDACTION_MODE", mode)` + `t.Setenv("PII_HASH_KEY", "x")` (to bypass mode=hash check); assert no error and `cfg.PIIRedactionMode == mode`.
    - Test 8: `TestLoad_PIIRedactionMode_UnknownModeError` — `t.Setenv("PII_REDACTION_MODE", "bogus")`; `_, err := config.Load()`; assert `err != nil` AND `err.Error()` contains `PII_REDACTION_MODE` AND `bogus`.
    - Test 9: `TestLoad_PIIHashModeRequiresKey` — `t.Setenv("PII_REDACTION_MODE", "hash")`; PII_HASH_KEY unset; assert `err != nil` AND `err.Error()` contains `PII_HASH_KEY` (Pitfall 6).
    - Test 10: `TestLoad_PIIHashModeWithKey_NoError` — `t.Setenv("PII_REDACTION_MODE", "hash")` + `t.Setenv("PII_HASH_KEY", "test-key-32-bytes-padding-here!!")`; assert `err == nil`.
    - Test 11: `TestLoad_NonHashModeWithoutKey_NoError` — `t.Setenv("PII_REDACTION_MODE", "replace")`; PII_HASH_KEY unset; assert `err == nil` (keyless modes don't need a key — Pitfall 6 last paragraph).

    **chain_filter_test.go**:
    - Test 12: `TestChainFilter_EmptyAllowlist_Passthrough` — already partially in slice 1's chain_test.go; this slice's test confirms it again with the FULL four-hook chain. Construct chain = `plugin.Chain{Pre: []engine.PreHook{&plugin.RequestIDHook{}, &plugin.AuthHook{}, &pii.PIIRedactionHook{Enabled: true, Recognizers: pii.Recognizers, Mode: "replace"}, &plugin.LoggingHook{}}, Post: []engine.PostHook{&plugin.LoggingHook{}}}`; `filtered, err := chain.Filter(nil)`; assert `err == nil`; assert `len(filtered.Pre) == 4` and `len(filtered.Post) == 1`.
    - Test 13: `TestChainFilter_KnownAllowlist_PreservesRegistrationOrder` — `filtered, err := chain.Filter([]string{"LoggingHook", "RequestIDHook"})` (DELIBERATE allowlist-order != registration-order); assert `err == nil`; assert `filtered.Pre[0].(*plugin.RequestIDHook) != nil` AND `filtered.Pre[1].(*plugin.LoggingHook) != nil` (registration order preserved per D-02 + SC5).
    - Test 14: `TestChainFilter_UnknownAllowlist_ErrorContainsUnknownHook` — `_, err := chain.Filter([]string{"RequestIDHook", "BogusHook"})`; assert `err != nil` AND `err.Error()` contains `unknown hook` AND `BogusHook`.
    - Test 15: `TestChainFilter_SingleHookAllowlist` — `filtered, err := chain.Filter([]string{"PIIRedactionHook"})`; assert `err == nil`; assert `len(filtered.Pre) == 1` AND `filtered.Pre[0].Name() == "PIIRedactionHook"`; assert `len(filtered.Post) == 0` (PII is not in the Post chain — Pre-only per RESEARCH OQ-3-like disposition for PII).
    - Test 16: `TestChainFilter_LoggingHookInBothPreAndPost_AllowlistKeepsBoth` — `filtered, err := chain.Filter([]string{"LoggingHook"})`; assert `len(filtered.Pre) == 1` (LoggingHook last on Pre) AND `len(filtered.Post) == 1` (LoggingHook on Post). LoggingHook's dual presence is the Pre+Post pattern; one allowlist entry covers both placements.

    **hooks_handler_test.go**:
    - Test 17: `TestHooksHandler_EmptyChain` — `srv := newServerWithHooksSource(t, fakeHooksSource{})` (a `nil`-ish source); GET /health/hooks; decode body; assert `{"hooks": []}` shape (empty array, not omitted).
    - Test 18: `TestHooksHandler_FourHookChain_JSONEnvelope` — fake source returns the four expected HookDescription entries in registration order; GET; decode; assert `len(body["hooks"]) == 4`; assert entries[0].Name=="RequestIDHook", entries[1].Name=="AuthHook", entries[2].Name=="PIIRedactionHook", entries[3].Name=="LoggingHook" (registration order — D-02 + SC5).
    - Test 19: `TestHooksHandler_EntryShape` — assert each entry contains keys `name` (string), `kind` (string in `["Pre", "Post", "Pre,Post"]`), `enabled` (bool), `config` (object, possibly empty).
    - Test 20: `TestHooksHandler_SecretOmissionAudit` — fake source includes AuthHook description with `config={"token_count": 3}` AND PIIRedactionHook with `config={"enabled": true, "mode": "hash", "entities": [...]}`. **Inject sentinel secret values via the test fixture**: AuthHook `Tokens = []string{"TOPSECRET_AUTH_TOKEN_001"}`; PIIRedactionHook `HashKey = []byte("TOPSECRET_HASH_KEY_002")`. GET /health/hooks; capture response body; assert `!strings.Contains(string(body), "TOPSECRET_AUTH_TOKEN_001")` AND `!strings.Contains(string(body), "TOPSECRET_HASH_KEY_002")`. Also assert response body does NOT contain `Pattern.String()` for any recognizer (grep for `regexp` / `(?i)` / `\\b[A-Z`).
    - Test 21: `TestHooksHandler_POST_Returns405` — `req := httptest.NewRequest(http.MethodPost, "/health/hooks", nil)`; `srv.ServeHTTP(rec, req)`; assert `rec.Code == http.StatusMethodNotAllowed`. Same for PUT and DELETE.
    - Test 22: `TestHooksHandler_AuthExempt` — boot the SAME test server with `AUTH_TOKEN` set; `GET /health/hooks` WITHOUT `Authorization` header; assert `rec.Code == http.StatusOK` (mirror of `agents_test.go:253-265 TestAgentsHandler_NoAuthRequired` pattern).
    - Test 23: `TestHooksHandler_RegistrationOrder_NotAllowlistOrder` — fake source simulates `ENABLED_HOOKS=LoggingHook,RequestIDHook` (allowlist-order in env != registration-order). Fixture returns hooks in REGISTRATION order. Assert response order matches registration (RequestIDHook first), NOT allowlist order. (Belt-and-suspenders against future regression in main.go where order might be mis-derived.)

    All 23 tests fail in RED state before Tasks 2-5.
  </behavior>
  <action>
    Create `internal/config/plugin_config_test.go` (package `config_test` or `config` — match `config_test.go` precedent at lines 1-30). Implement the 11 boot-validation tests using `t.Setenv` per `internal/config/config_test.go:41-73` (note `t.Setenv` precludes `t.Parallel()`).

    Create `internal/plugin/chain_filter_test.go` (package `plugin_test` so it depends on the production package's exported symbols including `pii.PIIRedactionHook`). Implement the 5 Filter contract tests. Use real hook constructors (RequestIDHook, AuthHook, PIIRedactionHook, LoggingHook from slices 1-4) — NOT fakes — so the test verifies the production-shape Filter end-to-end.

    Create `internal/server/hooks_handler_test.go` (package `server` whitebox to access unexported `hooksHandler`). Mirror `internal/server/agents_test.go` 1:1: define `fakeHooksSource` per 08-PATTERNS lines 884-887; build a small `newServerWithHooksSource(t, source)` helper that constructs a `server.Server` with just the hooks source wired (mirror of how agents_test.go constructs the test server). For Test 22 auth-exempt, follow `agents_test.go:253-265` precedent.

    Run `go test ./internal/{config,plugin,server}/... -count=1 -race`. Expect:
    - `undefined: cfg.EnabledHooks` (or similar) for plugin_config_test.go (Task 2 unimplemented).
    - chain_filter_test.go may PASS if slice 1's Filter implementation is complete (the existing implementation should already satisfy these tests — they're regression coverage). If they fail, that's a slice-1 contract gap to address in Task 3 OR a more thorough Filter impl is required (Task 3 finalizes Filter behavior).
    - hooks_handler_test.go fails with `undefined: hooksHandler` / `undefined: HooksDescriptionSource` (Tasks 4-5 unimplemented).

    Commit subject: `test(08-05): scaffold Wave 0 — config validation + chain.Filter contract + /health/hooks handler (23 RED)`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test ./internal/config/... ./internal/plugin/... ./internal/server/... -count=1 -race 2>&amp;1 | grep -cE 'undefined:|FAIL' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/config/plugin_config_test.go` defines 11 tests with `TestLoad_` prefix. Verify: `grep -cE '^func TestLoad_' internal/config/plugin_config_test.go` returns `11`.
    - Source assertion: `internal/plugin/chain_filter_test.go` defines 5 tests with `TestChainFilter_` prefix. Verify: `grep -cE '^func TestChainFilter_' internal/plugin/chain_filter_test.go` returns `5`.
    - Source assertion: `internal/server/hooks_handler_test.go` defines 7 tests with `TestHooksHandler_` prefix. Verify: `grep -cE '^func TestHooksHandler_' internal/server/hooks_handler_test.go` returns `7`.
    - Source assertion: Test 20 contains the sentinel secret literal so the secret-omission audit cannot regress to a tautology. Verify: `grep -F 'TOPSECRET_AUTH_TOKEN_001' internal/server/hooks_handler_test.go` exits 0.
    - Behavior assertion: Tests FAIL in expected RED state (config + handler undefined; chain.Filter contract may pass if slice 1 covered it). Verify: `go test ./internal/{config,plugin,server}/... 2>&1 | grep -cE 'undefined:|FAIL'` returns at least 5.
    - Test-command assertion: `go test ./internal/{config,plugin,server}/... -count=1 -race` exits non-zero.
  </acceptance_criteria>
  <done>23 failing tests committed under `test(08-05): ...`; production code not yet touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `internal/config/config.go` with five env keys + Load() validation</name>
  <files>internal/config/config.go</files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/config/config.go (modify — add 5 env keys)` block (lines 505-583) — verbatim implementation template
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Pitfall 6 (hash mode silent unkeyed fallback, lines 704-718) — Load() boot-error contract
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §Pattern B (boot-error accumulation, lines 977-996) — errors.Join precedent
    - internal/config/config.go (existing) — `EnabledSurfaces`, `parseCIDRs`, `validateEnabledSurfaces` precedents for typo-fail-fast
    - internal/plugin/pii/recognizers.go (slice 4) — `pii.SourceAuditNames()` returns the six entity names for `validatePIIEntities`
    - internal/config/plugin_config_test.go (Task 1) — tests this implementation must satisfy
  </read_first>
  <action>
    Modify `internal/config/config.go` per 08-PATTERNS lines 505-583:

    Add five new fields to the `Config` struct (preserve struct ordering — group near `EnabledSurfaces`):
    - `EnabledHooks []string` — comma-split allowlist (ENABLED_HOOKS env var).
    - `PIIRedactionEnabled bool` — opt-in flag (PII_REDACTION_ENABLED env var).
    - `PIIEnabledEntities []string` — comma-split entity allowlist (PII_ENABLED_ENTITIES env var; empty = all six).
    - `PIIRedactionMode string` — one of `replace`|`mask`|`hash`|`drop` (PII_REDACTION_MODE env var; default `replace`).
    - `PIIHashKey string` — required when Mode=="hash" (PII_HASH_KEY env var).

    Add doc-comments per 08-PATTERNS lines 510-540 referencing CONTEXT.md D-02 / D-05 + RESEARCH Pitfall 6 / Pitfall 7.

    In `Load()`:
    1. After existing env reads, add:
       - `enabledHooks := getEnvStrSliceComma("ENABLED_HOOKS", nil)`.
       - `piiEnabled, _ := getEnvBool("PII_REDACTION_ENABLED", false)` — use the project's existing bool-parsing helper; if no `getEnvBool`, implement it inline per accepted truthy values ("1", "true", "yes" — planner picks; document).
       - `piiEntities := getEnvStrSliceComma("PII_ENABLED_ENTITIES", nil)`.
       - `piiMode := getEnvStr("PII_REDACTION_MODE", "replace")`.
       - `piiHashKey := getEnvStr("PII_HASH_KEY", "")`.

    2. Add validation helpers:
       - `func validatePIIMode(m string) error` — accept set `{"replace","mask","hash","drop"}`; reject others with `fmt.Errorf("unknown mode %q (allowed: replace, mask, hash, drop)", m)` per 08-PATTERNS lines 567-575.
       - `func validatePIIEntities(names []string) error` — accept set from `pii.SourceAuditNames()` (the six entity names from recognizers.go). If empty input, return nil (default = all). For each name in input, if not in accepted set, return `fmt.Errorf("unknown entity %q (allowed: %s)", name, strings.Join(allowed, ", "))`.

    3. In Load()'s errs accumulation per 08-PATTERNS Pattern B (lines 977-996):
       - `if err := validatePIIMode(piiMode); err != nil { errs = append(errs, fmt.Errorf("PII_REDACTION_MODE: %w", err)) }`.
       - `if err := validatePIIEntities(piiEntities); err != nil { errs = append(errs, fmt.Errorf("PII_ENABLED_ENTITIES: %w", err)) }`.
       - `if piiMode == "hash" && piiHashKey == "" { errs = append(errs, fmt.Errorf("PII_REDACTION_MODE=hash requires PII_HASH_KEY")) }` (Pitfall 6).
       - Assign fields onto Config struct.

    **Note**: `ENABLED_HOOKS` typo validation does NOT happen in config.Load() (since config doesn't know the chain shape). It happens in `chain.Filter` (Task 3 enforces) and main.go propagates the error. config.Load() only validates SHAPE (mode enum + entity allowlist + hash-key-present-when-mode-hash). Per 08-PATTERNS lines 578-583 explicit note.

    Run `go test ./internal/config/... -count=1 -race -run TestLoad_` — all 11 plugin_config_test.go tests must PASS (plus existing config tests still pass).

    Commit subject: `feat(08-05): config.Load — 5 env keys + PII mode/entities/hash-key validation`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test ./internal/config/... -count=1 -race 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/config/config.go` declares 5 new Config fields: `EnabledHooks`, `PIIRedactionEnabled`, `PIIEnabledEntities`, `PIIRedactionMode`, `PIIHashKey`. Verify: `grep -cE '\b(EnabledHooks|PIIRedactionEnabled|PIIEnabledEntities|PIIRedactionMode|PIIHashKey)\b' internal/config/config.go` returns at least `10` (5 field decls + 5 assignments in Load).
    - Source assertion: `validatePIIMode` and `validatePIIEntities` defined. Verify: `grep -cE '^func validatePII(Mode|Entities)' internal/config/config.go` returns `2`.
    - Source assertion: Mode=hash + empty-key boot error present. Verify: `grep -F 'PII_REDACTION_MODE=hash requires PII_HASH_KEY' internal/config/config.go` exits 0.
    - Behavior assertion: `TestLoad_PIIHashModeRequiresKey` passes — boot error contains `PII_HASH_KEY`.
    - Behavior assertion: `TestLoad_PIIRedactionMode_UnknownModeError` passes — boot error contains `PII_REDACTION_MODE` and `bogus`.
    - Behavior assertion: `TestLoad_PIIEnabledEntities_UnknownNameError` passes — boot error contains `BadEntity`.
    - Test-command assertion: `go test ./internal/config/... -count=1 -race` exits 0 (all old + new config tests pass).
    - Test-command assertion: `go build ./...` exits 0 (no compile regressions).
  </acceptance_criteria>
  <done>Five env keys loaded and validated; 11 boot-validation tests green; existing config tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize `chain.Filter` semantics + `Describe` consumer interface in `internal/plugin/chain.go`</name>
  <files>internal/plugin/chain.go</files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/plugin/chain.go` block (lines 61-130) — Filter algorithm verbatim
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Pitfall 7 (lines 720-734) — typo-fail-fast contract
    - .planning/phases/08-plugin-hook-chain/08-01-SUMMARY.md — slice 1's Filter implementation (what's already shipped; this task fills any gaps the chain_filter_test.go uncovers)
    - internal/plugin/chain.go (slice 1 existing) — current Filter impl
    - internal/plugin/chain_filter_test.go (Task 1) — 5 tests this implementation must satisfy
  </read_first>
  <action>
    **Do NOT recreate `internal/plugin/chain.go`** — slice 1 owns the type declaration + skeleton Filter. This task finalizes Filter behavior to satisfy the slice-5 contract tests (Test 12-16 from Task 1).

    Read slice 1's existing `chain.go`. Verify:
    1. `Chain.Filter(allowlist []string) (Chain, error)` exists.
    2. Empty/nil allowlist returns chain unchanged (default-permissive).
    3. Unknown allowlist name returns error containing `unknown hook`.
    4. Registration order preserved (NOT allowlist order — D-02 + SC5).
    5. LoggingHook present in BOTH Pre and Post is matched ONCE on its Name (the `LoggingHook` allowlist entry covers both placements per Test 16).

    If slice 1's Filter ALREADY satisfies all of (1)-(5), this task is read-only — just run the tests to confirm. If gaps exist (e.g., (5) was not implemented because slice 1 only had a 1-hook chain to test against), this task fills them.

    **If gaps exist**, modify `Chain.Filter` per 08-PATTERNS lines 90-109:
    - Build `allow := map[string]struct{}` from allowlist.
    - Walk `c.Pre` and `c.Post` separately; for each hook, get name via `hookName(h)` (the helper slice 1 declared). If `name in allow`, append to filteredPre/filteredPost AND `matched[name] = true`.
    - After both walks, collect unknown names: `for name := range allow { if !matched[name] { unknown = append(unknown, fmt.Errorf("unknown hook in ENABLED_HOOKS: %q", name)) } }`.
    - Return `errors.Join(unknown...)` on non-empty unknowns; otherwise return the filtered Chain.

    Verify `Chain.Describe()` returns Pre and Post HookDescription slices in registration order — used by hooksHandler in Task 4.

    Run `go test ./internal/plugin/... -count=1 -race -run 'TestChainFilter_'` — all 5 tests must PASS.
    Also run full `go test ./internal/plugin/... -count=1 -race` to confirm no regression against slices 1, 3, 4.

    Commit subject: `feat(08-05): finalize chain.Filter for four-hook chain + Describe contract`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test ./internal/plugin/... -count=1 -race 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/plugin/chain.go` Filter implementation iterates BOTH `c.Pre` AND `c.Post` (for LoggingHook dual-placement coverage per Test 16). Verify: `grep -cE 'for .* range (c\.Pre|c\.Post)' internal/plugin/chain.go` returns at least `2`.
    - Source assertion: Filter returns error containing literal substring `unknown hook` when given bogus name. Verified by Test 14.
    - Behavior assertion: `TestChainFilter_KnownAllowlist_PreservesRegistrationOrder` passes — allowlist `[LoggingHook, RequestIDHook]` yields filtered `[RequestIDHook, LoggingHook]` (registration order preserved per D-02 + SC5).
    - Behavior assertion: `TestChainFilter_LoggingHookInBothPreAndPost_AllowlistKeepsBoth` passes — single allowlist entry `LoggingHook` keeps it on both Pre and Post.
    - Behavior assertion: `TestChainFilter_UnknownAllowlist_ErrorContainsUnknownHook` passes — error message references both `unknown hook` AND `BogusHook`.
    - Test-command assertion: `go test ./internal/plugin/... -count=1 -race` exits 0 (all Phase 8 unit tests, slices 1+3+4+5 chain_filter_test.go pass).
  </acceptance_criteria>
  <done>chain.Filter satisfies four-hook contract; 5 Filter tests + all slice 1/3/4 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement `internal/server/hooks_handler.go` + `internal/server/server.go` `/health/hooks` registration + `HooksDescriptionSource` interface + `HookDescription` struct</name>
  <files>
    internal/server/hooks_handler.go,
    internal/server/server.go
  </files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/server/hooks_handler.go` block (lines 417-453) — handler verbatim template
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`internal/server/server.go (modify — register /health/hooks)` block (lines 457-501) — registration pattern
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §Pattern A (consumer-defined interface + cmd-level adapter, lines 961-975) — TRST-04 discipline
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Example 6 (JSON envelope, lines 1050-1104) — wire shape
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §Pitfall 9 (no regex / secret leak, lines 753-768) — Describe whitelist contract
    - internal/server/agents.go — entire file is the template (AgentSlot redeclared structurally; agentsHandler shape; PoolDetailSource consumer-defined interface)
    - internal/server/agents_test.go — fake source + httptest precedent
    - internal/server/server.go:196-200 — /health/agents auth-exempt registration pattern
    - internal/plugin/chain.go (slice 1+Task 3) — `Chain.Describe() (pre, post []HookDescription)` is the data plane this handler exposes
    - internal/server/hooks_handler_test.go (Task 1) — 7 tests this implementation must satisfy
  </read_first>
  <action>
    **(A) `internal/server/server.go` modifications** per 08-PATTERNS lines 457-501:

    1. After the existing `AgentSlot` struct (around `agents.go:40-45` precedent — but for hooks, declared in `server.go` itself), declare:
       ```
       type HookDescription struct { Name, Kind string; Enabled bool; Config map[string]any }
       ```
       with `json:` tags exactly matching plugin.HookDescription. **Redeclare structurally** (do NOT import `internal/plugin`) — mirror of `server.AgentSlot` redeclaration pattern. The cmd/otto-gateway adapter (Task 5) does the field-copy. **CRITICAL**: do NOT modify `.go-arch-lint.yml` to add `plugin` to `server.mayDependOn` — the structural redeclaration is exactly to avoid that boundary breakage.

    2. Declare the consumer-defined interface (mirror of `PoolDetailSource` in `server.go:25-42`):
       ```
       type HooksDescriptionSource interface { Describe() (pre, post []HookDescription) }
       ```

    3. Add field to `Config` struct: `Hooks HooksDescriptionSource` (may be nil; handler renders empty shape if nil per 08-PATTERNS line 484).

    4. Add field to `Server` struct: `hooks HooksDescriptionSource`.

    5. In `NewFromConfig` (the existing `Server` constructor): assign `s.hooks = cfg.Hooks`.

    6. Register the route on the OUTER router (auth-exempt) per 08-PATTERNS lines 472-477. **Critical**: register adjacent to `s.router.Get("/health/agents", s.agentsHandler)` at the precedent line:
       ```
       s.router.Get("/health/hooks", s.hooksHandler)
       ```
       Add comment referencing OBSV-04 + SC7 ("no runtime mutate path in v1; restart to change config").

    **(B) `internal/server/hooks_handler.go` new file** per 08-PATTERNS lines 417-453:

    Package banner: `// Phase 8 OBSV-04 — GET /health/hooks JSON envelope handler. Read-only chain introspection. Auth-exempt (mounted on outer router alongside /health and /health/agents per server.go:196-200 precedent). Per SC7: no runtime mutate path; restart to change config. T-8-LEAK per security_enforcement — config map MUST NOT contain secrets (RESEARCH Pitfall 9: no regex sources, no token values, no hash key bytes).`

    Imports: `encoding/json`, `net/http`.

    Type:
    ```
    type HooksResponse struct {
        Hooks []HookDescription `json:"hooks"`
    }
    ```
    (Top-level key `hooks` per RESEARCH Example 6 line 1054.)

    Handler:
    ```
    func (s *Server) hooksHandler(w http.ResponseWriter, r *http.Request) {
        // 405 on mutating verbs (NB: chi router with .Get() already restricts to GET; this is defense-in-depth).
        if r.Method != http.MethodGet {
            w.Header().Set("Allow", "GET")
            w.WriteHeader(http.StatusMethodNotAllowed)
            return
        }
        resp := HooksResponse{Hooks: []HookDescription{}}
        if s.hooks != nil {
            pre, post := s.hooks.Describe()
            resp.Hooks = append(resp.Hooks, pre...)
            resp.Hooks = append(resp.Hooks, post...)
        }
        w.Header().Set("Content-Type", "application/json")
        w.WriteHeader(http.StatusOK)
        if err := json.NewEncoder(w).Encode(resp); err != nil {
            LoggerFromCtx(r.Context(), s.logger).Error("hooks encode", "err", err)
        }
    }
    ```

    **Important shape note**: the response is `{"hooks": [...]}` (single flat array per RESEARCH Example 6). Pre hooks listed first (registration order), Post hooks appended (registration order). LoggingHook appears TWICE in the response if registered both Pre and Post — once with `kind:"Pre,Post"` from Pre slice OR once each with `kind:"Pre"` and `kind:"Post"`. **Planner decides**: prefer the SINGLE-ENTRY-WITH-COMBINED-KIND form per RESEARCH Example 6 line 1059 (`"kind": "Pre,Post"`). If the hook is in both slices, dedupe in the handler by name and combine kinds. Document the dedupe strategy in code comment.

    Run `go test ./internal/server/... -count=1 -race -run TestHooksHandler_` — all 7 hooks_handler_test.go tests must PASS.

    Commit subject: `feat(08-05): GET /health/hooks handler + HooksDescriptionSource consumer interface (OBSV-04)`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test ./internal/server/... -count=1 -race 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `internal/server/server.go` declares `type HookDescription struct` AND `type HooksDescriptionSource interface`. Verify: `grep -cE '^type (HookDescription struct|HooksDescriptionSource interface)' internal/server/server.go` returns `2`.
    - Source assertion: `/health/hooks` route registered on outer router. Verify: `grep -F 's.router.Get("/health/hooks"' internal/server/server.go` exits 0.
    - Source assertion: NO `internal/plugin` import in server.go (structural HookDescription preserves boundary). Verify: `grep -E 'otto-gateway/internal/plugin' internal/server/server.go` returns nothing.
    - Source assertion: `internal/server/hooks_handler.go` exists and defines `hooksHandler`. Verify: `grep -cE '^func \(s \*Server\) hooksHandler' internal/server/hooks_handler.go` returns `1`.
    - Source assertion: handler returns 405 on non-GET. Verify: `grep -F 'MethodNotAllowed' internal/server/hooks_handler.go` exits 0.
    - Behavior assertion: `TestHooksHandler_SecretOmissionAudit` passes — response body does NOT contain `TOPSECRET_AUTH_TOKEN_001` or `TOPSECRET_HASH_KEY_002` literal strings (T-8-LEAK mitigated).
    - Behavior assertion: `TestHooksHandler_POST_Returns405` passes.
    - Behavior assertion: `TestHooksHandler_AuthExempt` passes — GET succeeds without bearer header.
    - Test-command assertion: `go test ./internal/server/... -count=1 -race` exits 0 (all old + 7 new tests pass).
    - Test-command assertion: `go-arch-lint check` exits 0 (server does NOT import plugin; structural HookDescription preserves boundary).
    - Test-command assertion: `go build ./...` exits 0.
  </acceptance_criteria>
  <done>/health/hooks endpoint shipped; 7 handler tests green; arch-lint clean; structural redeclaration preserves TRST-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4b: Stamp `X-Request-Id` on ctx in adapter handlers (OBSV-03/SC2 production path)</name>
  <files>internal/adapter/ollama/handlers.go, internal/adapter/openai/handlers.go, internal/adapter/anthropic/handlers.go, internal/adapter/ollama/handlers_test.go, internal/adapter/openai/handlers_test.go, internal/adapter/anthropic/handlers_test.go</files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-RESEARCH.md §"Open Questions (RESOLVED)" Q2 + Q3 — adapter-side ctx-stamp is the ratified production path (mirror of slice 2's `canonical.WithBearerToken` stamp)
    - .planning/phases/08-plugin-hook-chain/08-01-PLAN.md §Task 4 action note (final paragraph) — slice 1 explicitly defers adapter stamping to slice 5; this task closes the deferral
    - .planning/phases/08-plugin-hook-chain/08-02-PLAN.md §Task 2/3/4 — the slice-2 pattern this task mirrors: read header → stamp ctx → call engine.Run
    - internal/plugin/request_id.go (slice 1) — `WithRequestID(ctx, id) context.Context` + `NewRequestID() string` ULID generator; the consumers slice 3 LoggingHook (sync.Map keyed by request_id) + slice 4 PIIRedactionHook summary stash + RequestIDHook itself ALL depend on the adapter stamping this BEFORE `engine.Run` is invoked
    - internal/adapter/ollama/handlers.go — current entry-point handler structure + the `engine.Run` invocation site (the call this task adds the ctx wrap immediately before)
    - internal/adapter/openai/handlers.go — same
    - internal/adapter/anthropic/handlers.go — same; note this adapter ALSO has the dual-header `x-api-key` / `Authorization` path (Phase 3.1 D-15) — X-Request-Id is a single header (`X-Request-Id`), so dual-header logic does NOT apply here; one header read suffices
    - internal/adapter/ollama/handlers_test.go (and the openai / anthropic counterparts) — existing test scaffold for handler invocation; mirror to add round-trip ctx-stamp assertions
  </read_first>
  <action>
    **(A) Adapter handlers — read header, generate-if-absent, stamp ctx.** For each of `internal/adapter/{ollama,openai,anthropic}/handlers.go`:

    1. Locate the handler function that calls `engine.Run(ctx, ...)` (chat-completions endpoint for OpenAI, `/api/chat` for Ollama, `/v1/messages` for Anthropic).
    2. BEFORE the `engine.Run` invocation, add this sequence:
       - Read `headerValue := r.Header.Get("X-Request-Id")` from the inbound `*http.Request`.
       - If `headerValue == ""`, call `headerValue = plugin.NewRequestID()` to mint a fresh ULID (slice 1 shipped `plugin.NewRequestID`).
       - Wrap ctx: `ctx = plugin.WithRequestID(ctx, headerValue)`.
       - (Optional response-header echo) — at response render time, write `w.Header().Set("X-Request-Id", plugin.RequestIDFromContext(ctx))` so clients can correlate. Delegated to the existing per-surface render path; if the render path is shared across handlers, the stamp can be moved into the shared writer. Implementer's call; the SOURCE assertion below only enforces the inbound stamp.

    **(B) Symmetry with slice 2 `WithBearerToken`.** This task is the X-Request-Id parallel of slice 2's bearer-token adapter stamp. The pattern is identical: header read → ctx stamp → `engine.Run`. Use the exact ordering "(1) request_id stamp, (2) bearer token stamp, (3) `engine.Run`" so the chain's RequestIDHook + AuthHook both have their ctx prerequisites present before chain.Pre runs.

    **(C) Round-trip test per adapter.** In each `internal/adapter/<surface>/handlers_test.go`:

    - Add a test `TestHandler_StampsRequestIDOnContext_FromHeader`: send a request with `X-Request-Id: 01HKQ8...` (a literal ULID); intercept the ctx passed to a fake engine (the existing engine-stub pattern in each adapter's test file is the analog); assert `plugin.RequestIDFromContext(ctx) == "01HKQ8..."`.
    - Add a test `TestHandler_StampsRequestIDOnContext_GeneratedWhenAbsent`: send a request with no `X-Request-Id` header; assert `plugin.RequestIDFromContext(ctx)` returns a non-empty ULID-shaped string (26 chars, Crockford Base32 alphabet).

    Three adapters × 2 tests = 6 new tests total. Re-use the existing engine-stub harness in each adapter's test file (do not invent a new test infrastructure).

    **(D) Property invariant assertion.** The combination of (A) + slice 1's RequestIDHook (which honors an inbound ctx-stamped id) + slice 3's LoggingHook sync.Map keyed by `RequestIDFromContext(ctx)` + slice 4's PII summary stash by request_id MUST satisfy:

    *"After this task, every code path that reaches `engine.Run` carries a non-empty `plugin.RequestIDFromContext(ctx)`. Test coverage for the three adapters above is sufficient — chain runner does not need a guard."*

    Commit subject: `feat(08-05): stamp X-Request-Id on ctx in adapter handlers (close OBSV-03/SC2)`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go test ./internal/adapter/... -count=1 -race 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `plugin.WithRequestID` call appears in all three adapter handlers. Verify: `grep -l 'plugin.WithRequestID' internal/adapter/ollama/handlers.go internal/adapter/openai/handlers.go internal/adapter/anthropic/handlers.go | wc -l | tr -d ' '` returns `3`.
    - Source assertion: header read for `X-Request-Id` appears alongside the stamp. Verify: `grep -F 'r.Header.Get("X-Request-Id")' internal/adapter/ollama/handlers.go internal/adapter/openai/handlers.go internal/adapter/anthropic/handlers.go | wc -l | tr -d ' '` returns `3`.
    - Source assertion: generation-on-absence path uses `plugin.NewRequestID()`. Verify: `grep -F 'plugin.NewRequestID()' internal/adapter/ollama/handlers.go internal/adapter/openai/handlers.go internal/adapter/anthropic/handlers.go | wc -l | tr -d ' '` returns `3`.
    - Behavior assertion: `TestHandler_StampsRequestIDOnContext_FromHeader` passes for all three surfaces (3 tests, one per adapter).
    - Behavior assertion: `TestHandler_StampsRequestIDOnContext_GeneratedWhenAbsent` passes for all three surfaces (3 tests).
    - Test-command assertion: `go test ./internal/adapter/... -count=1 -race` exits 0 (full adapter suite green; new 6 tests + existing).
    - Test-command assertion: `go-arch-lint check` exits 0 (adapter packages already may depend on `plugin` per slice 1's arch-lint update + slice 2's adapter→canonical pattern — verify the existing rule covers `plugin` direct import; if not, this task ALSO updates `.go-arch-lint.yml` to allow `adapter/*` → `plugin` since the stamping helpers live in `plugin`; do this BEFORE adding the calls so arch-lint runs clean on each file edit).
  </acceptance_criteria>
  <done>All three adapter handlers stamp `X-Request-Id` on ctx before `engine.Run`; 6 new tests pass; production OBSV-03/SC2 path closed (chain hooks can rely on a non-empty request_id in ctx — sync.Map collision risk eliminated).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Wire `cmd/otto-gateway/main.go` — Chain literal + Filter + engine injection + hooksDescriptionAdapter + filterRecognizers + migration boundary cleanup</name>
  <files>cmd/otto-gateway/main.go</files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`cmd/otto-gateway/main.go (modify — construct + filter + inject chain)` block (lines 587-656) — verbatim wiring template
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §Pattern F (AuthHook migration boundary, lines 1030-1041) — sequencing rule
    - .planning/phases/08-plugin-hook-chain/08-CONTEXT.md §D-01 (lines 102-135) — hardcoded slice pattern
    - .planning/phases/08-plugin-hook-chain/08-CONTEXT.md §D-02 + §D-04 + §D-05 — chain composition + order + hash-key
    - .planning/phases/08-plugin-hook-chain/08-02-SUMMARY.md — slice 2's auth migration state: whether `auth.Bearer` middleware was already removed or remains
    - cmd/otto-gateway/main.go — existing structure: engine.New site + server.NewFromConfig site + adapter patterns
    - internal/plugin/chain.go (slices 1+3) — Chain type + Filter
    - internal/plugin/request_id.go (slice 1) — RequestIDHook
    - internal/plugin/auth.go (slice 2) — AuthHook
    - internal/plugin/logging.go (slice 3) — LoggingHook
    - internal/plugin/pii/pii.go (slice 4) — PIIRedactionHook
    - internal/plugin/pii/recognizers.go (slice 4) — Recognizers + SourceAuditNames
    - internal/server/server.go (Task 4 just-created) — HooksDescriptionSource + HookDescription
    - internal/config/config.go (Task 2 just-created) — EnabledHooks + PII* fields
  </read_first>
  <action>
    **(A) Construct the hardcoded Chain literal** per 08-PATTERNS lines 591-616 + D-01:

    AFTER `cfg, err := config.Load()` succeeds AND AFTER the pool warmup (so logger is ready), BEFORE `engine.New(...)`:

    1. Build `filteredRecognizers := filterRecognizers(pii.Recognizers, cfg.PIIEnabledEntities)` — a small helper defined in main.go that returns the subset of recognizers whose Name is in `cfg.PIIEnabledEntities`; if `cfg.PIIEnabledEntities` is empty, returns `pii.Recognizers` unchanged. Compile-time wiring; no runtime overhead per request.

    2. Construct the literal:
       ```
       chain := plugin.Chain{
           Pre: []engine.PreHook{
               &plugin.RequestIDHook{Logger: logger},
               &plugin.AuthHook{Tokens: cfg.AuthToken},
               &pii.PIIRedactionHook{
                   Recognizers:     filteredRecognizers,
                   Enabled:         cfg.PIIRedactionEnabled,
                   Mode:            cfg.PIIRedactionMode,
                   HashKey:         []byte(cfg.PIIHashKey),
                   EnabledEntities: cfg.PIIEnabledEntities,
               },
               &plugin.LoggingHook{Logger: logger},
           },
           Post: []engine.PostHook{
               &plugin.LoggingHook{Logger: logger}, // SAME LOGIC, reads start time from the per-request map keyed by request_id (slice 3 SUMMARY documents the mechanism)
           },
       }
       ```
       **Note**: D-04 order — RequestID → Auth → PII → Logging on Pre. Logging is BOTH last on Pre (sees post-PII content) AND on Post (timing + summary emission).

    3. Apply ENABLED_HOOKS allowlist filter (D-02 typo-fail-fast):
       ```
       chain, err = chain.Filter(cfg.EnabledHooks)
       if err != nil {
           cleanup()
           return nil, func() {}, fmt.Errorf("chain filter: %w", err)
       }
       ```
       (Or whatever error propagation pattern main.go uses; mirror pool warmup error handling at `main.go:171-174` per 08-PATTERNS lines 578-583.)

    **(B) Inject chain into engine.New** per 08-PATTERNS lines 619-627:

    Modify the existing `engine.New(engine.Config{...})` call to set:
    - `PreHooks: chain.Pre`
    - `PostHooks: chain.Post`

    Also apply to any per-session engine instances (08-PATTERNS line 626: "Plus the same Pre/Post on every per-session engine constructed at lines 233-253"). Verify session-registry engine construction site and inject.

    **(C) Wire `/health/hooks` via the bridge adapter** per 08-PATTERNS lines 632-654:

    Add a small adapter struct in main.go:
    ```
    type hooksDescriptionAdapter struct {
        chain plugin.Chain
    }
    func (h hooksDescriptionAdapter) Describe() (pre, post []server.HookDescription) {
        p, q := h.chain.Describe()
        return convertHookDescriptions(p), convertHookDescriptions(q)
    }
    func convertHookDescriptions(in []plugin.HookDescription) []server.HookDescription {
        out := make([]server.HookDescription, len(in))
        for i, x := range in {
            out[i] = server.HookDescription{Name: x.Name, Kind: x.Kind, Enabled: x.Enabled, Config: x.Config}
        }
        return out
    }
    ```
    (This is the TRST-04 adapter — server doesn't import plugin; main.go bridges via field-copy. Mirror of `poolStatsAdapter` at `main.go:428-435` per 08-PATTERNS lines 643-654.)

    Modify the existing `server.NewFromConfig(server.Config{...})` call to add:
    - `Hooks: hooksDescriptionAdapter{chain: chain}`

    **(D) AuthHook migration boundary cleanup** per 08-PATTERNS Pattern F lines 1030-1041:

    **Read 08-02-SUMMARY.md first**: if slice 2's SUMMARY documented that `auth.Bearer` chi middleware was LEFT IN PLACE (slice 2 declined to remove it), this task removes it now:
    - Locate `auth.Bearer(...)` middleware registration in `internal/server/server.go:230` (or wherever Bearer-Auth chi middleware is mounted on the protected sub-tree).
    - **Delete the `r.Use(auth.Bearer(cfg.AuthCfg))` call** (or equivalent).
    - **DO NOT delete `auth.Bearer` function itself** — per RESEARCH §Runtime State Inventory lines 608-612, the function stays shipped (used by tests and as the reference implementation).
    - **DO NOT delete `auth.IPAllowlist`** — IP allowlist stays at chi (surface-agnostic; runs before engine).

    If slice 2's SUMMARY documented that `auth.Bearer` was ALREADY removed in slice 2: this task verifies the removal and adds a regression test that the binary STILL 401s with a bad bearer token (because AuthHook in the chain now owns auth).

    **(E) Build + smoke test**:

    Run `go build ./cmd/otto-gateway`. Boot the binary locally with default config (no ENABLED_HOOKS set, no PII vars set) — `bin/otto-gateway`. `curl http://localhost:11434/health/hooks` should return JSON with the four hooks in registration order.

    Run `go test ./... -count=1 -race -timeout=120s` (full project suite). All existing tests must still pass.

    Commit subject: `feat(08-05): wire plugin.Chain in main.go + /health/hooks + auth migration cleanup`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; go build ./cmd/otto-gateway &amp;&amp; go test ./... -count=1 -race -timeout=120s 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `cmd/otto-gateway/main.go` constructs `plugin.Chain{Pre: [...], Post: [...]}` literal. Verify: `grep -F 'plugin.Chain{' cmd/otto-gateway/main.go` exits 0.
    - Source assertion: Chain contains all four hook constructors. Verify: `grep -cE '&(plugin\.RequestIDHook|plugin\.AuthHook|pii\.PIIRedactionHook|plugin\.LoggingHook)' cmd/otto-gateway/main.go` returns at least `5` (LoggingHook appears twice — Pre + Post).
    - Source assertion: Filter applied with cfg.EnabledHooks. Verify: `grep -F 'chain.Filter(cfg.EnabledHooks)' cmd/otto-gateway/main.go` exits 0.
    - Source assertion: hooksDescriptionAdapter present + wired into server.Config. Verify: `grep -F 'hooksDescriptionAdapter' cmd/otto-gateway/main.go` exits 0.
    - Source assertion: `chain.Pre` and `chain.Post` assigned to engine.Config. Verify: `grep -cE 'PreHooks:\s+chain\.Pre|PostHooks:\s+chain\.Post' cmd/otto-gateway/main.go` returns `2`.
    - Source assertion: filterRecognizers helper defined. Verify: `grep -F 'filterRecognizers' cmd/otto-gateway/main.go` exits 0.
    - Source assertion: AuthHook migration complete — `auth.Bearer` is no longer mounted as chi middleware in server.go (only in main.go indirectly via AuthHook constructor). Verify: `grep -E 'r\.Use\(auth\.Bearer' internal/server/server.go` returns nothing. (`auth.IPAllowlist` MAY remain — that's correct per Pattern F.)
    - Behavior assertion: `go build ./cmd/otto-gateway` exits 0.
    - Test-command assertion: `go test ./... -count=1 -race -timeout=120s` exits 0 (full project test suite green).
    - Test-command assertion: `go-arch-lint check` exits 0 (TRST-04 preserved; server still doesn't import plugin; main is the bridge).
  </acceptance_criteria>
  <done>main.go wired; chain construction + Filter + engine injection + /health/hooks adapter all in place; auth migration boundary closed; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: `tests/e2e/plugin_chain_test.go` — real-binary e2e for SC1 + SC5 + SC7 + T-8-CFG + T-8-LEAK + T-8-HASH-BOOT</name>
  <files>tests/e2e/plugin_chain_test.go</files>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §`tests/e2e/plugin_chain_test.go` block (lines 902-955) — real-binary harness template
    - .planning/phases/08-plugin-hook-chain/08-PATTERNS.md §Pattern G (E2E real-binary harness + gate, lines 1042-1046)
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md §Wave 0 Requirements line 74 + §Per-Task Verification Map row health-hooks (line 53)
    - tests/e2e/e2e_test.go — `gateOrSkip`, `bootGateway(t, extraEnv map[string]string)`, `freePort`, `healthOK` helpers (the reusable harness)
    - tests/e2e/anthropic_e2e_test.go OR openai_e2e_test.go — existing e2e suite layout for three-surface coverage
    - tests/e2e/tools_testmain_test.go — `TestMain` build pattern (if extension required)
  </read_first>
  <action>
    Create `tests/e2e/plugin_chain_test.go` with `//go:build e2e` build tag + `package e2e_test`.

    Implement the following e2e scenarios using the `bootGateway(t, extraEnv)` harness from `tests/e2e/e2e_test.go`. Each scenario is a separate `TestE2E_*` function so they can run independently and be filtered.

    **Scenario 1: `TestE2E_HealthHooks_DefaultChain`**
    - `bootGateway(t, nil)` — no env overrides; default chain (all four hooks enabled).
    - GET `http://<baseURL>/health/hooks`; assert 200 + Content-Type JSON.
    - Decode `{Hooks []HookDescription}`; assert `len(Hooks) >= 4` (Pre + Post — LoggingHook may appear once with `kind:"Pre,Post"` or twice).
    - Assert order: first hook is RequestIDHook, last hook (Pre side) is LoggingHook.
    - Assert each entry has `name`, `kind`, `enabled`, `config` keys.

    **Scenario 2: `TestE2E_HealthHooks_AuthExempt`**
    - `bootGateway(t, map[string]string{"AUTH_TOKEN": "secret"})`.
    - GET `/health/hooks` WITHOUT `Authorization` header; assert 200 (auth-exempt per SC7).
    - For belt-and-suspenders: GET `/api/chat` WITHOUT auth; assert 401-equivalent (AuthHook short-circuits).

    **Scenario 3: `TestE2E_HealthHooks_NoSecretLeak`**
    - `bootGateway(t, map[string]string{"AUTH_TOKEN": "TOPSECRET_AUTH_E2E", "PII_HASH_KEY": "TOPSECRET_HASH_E2E", "PII_REDACTION_MODE": "hash"})`.
    - GET `/health/hooks`; capture response body as string.
    - Assert `!strings.Contains(body, "TOPSECRET_AUTH_E2E")` AND `!strings.Contains(body, "TOPSECRET_HASH_E2E")`.
    - Also assert body does NOT contain regex fragments: `assert !strings.Contains(body, "(?i)\\b[A-Z0-9")` (Email regex source); `assert !strings.Contains(body, "\\b(?:[0-9]{1,3}\\.")` (IPv4 regex source).

    **Scenario 4: `TestE2E_HealthHooks_POST_Returns405`**
    - bootGateway default; POST `/health/hooks` with empty body; assert 405.
    - Same for PUT and DELETE.

    **Scenario 5: `TestE2E_PIIRedaction_Email`**
    - `bootGateway(t, map[string]string{"PII_REDACTION_ENABLED": "true", "PII_REDACTION_MODE": "replace"})`.
    - POST `/v1/chat/completions` (or `/api/chat`) with body containing user content `"please email corey@cmetech.io about the report"`.
    - **The kiro-cli subprocess is the upstream; depending on the e2e harness convention, either (a) a fake-kiro stub answers and the test asserts the request kiro-cli SAW contained `<EMAIL>` (NOT `corey@cmetech.io`), or (b) the harness logs the inbound canonical request and the test reads the log. Read `tests/e2e/e2e_test.go` to determine which.** If neither: skip this scenario behind a `t.Skip("requires PII e2e harness instrumentation")` and document in SUMMARY that this scenario is satisfied only by the unit + handler tests from slice 4.

    **Scenario 6: `TestE2E_BadBearer_AllThreeSurfaces`**
    - `bootGateway(t, map[string]string{"AUTH_TOKEN": "validtoken"})`.
    - For each surface, send a request with `Authorization: Bearer wrongtoken`:
      - Ollama: POST `/api/chat`; expect Ollama error envelope shape `{"error": "..."}` per CONTEXT.md.
      - OpenAI: POST `/v1/chat/completions`; expect OpenAI error envelope shape `{"error": {"message": "...", "type": "..."}}`.
      - Anthropic: POST `/v1/messages` with `anthropic-version: 2023-06-01`; expect Anthropic error envelope `{"type": "error", "error": {"type": "authentication_error", "message": "..."}}`.
    - Each surface MUST return its native shape (per CONTEXT.md D-04 short-circuit-via-adapter contract).
    - This is the **SC1 acceptance bar** verified end-to-end.

    **Scenario 7: `TestE2E_BootError_UnknownHook`**
    - Call `bootGateway(t, map[string]string{"ENABLED_HOOKS": "BogusHook"})`; expect the harness to FAIL to bring up `/health` (binary exits before warmup completes).
    - **The bootGateway harness reports this via `t.Skipf("gateway exited before warmup …")` or a `t.Fatal` per 08-PATTERNS line 950-953.** Capture the binary's stderr; assert it contains `unknown hook` AND `BogusHook`.
    - **If bootGateway doesn't surface stderr, extend it OR use `os/exec.Command` directly for this scenario**, capturing stderr via a `bytes.Buffer`. Document the chosen approach in SUMMARY.

    **Scenario 8: `TestE2E_BootError_HashModeNoKey`**
    - Call `bootGateway(t, map[string]string{"PII_REDACTION_MODE": "hash"})` (PII_HASH_KEY unset).
    - Assert: binary exits non-zero before `/health` accepts; stderr contains `PII_HASH_KEY` (Pitfall 6 mitigation; T-8-HASH-BOOT mitigation).

    **Scenario 9: `TestE2E_EnabledHooks_Filter_PreservesOrder`**
    - `bootGateway(t, map[string]string{"ENABLED_HOOKS": "LoggingHook,RequestIDHook"})` (deliberate allowlist-order != registration-order).
    - GET `/health/hooks`; decode; assert response order matches REGISTRATION order: `Hooks[0].Name == "RequestIDHook"`, `Hooks[1].Name == "LoggingHook"` (D-02 + SC5 verified end-to-end against the real binary).

    Run `go test -tags e2e ./tests/e2e/... -count=1 -race -timeout=180s -run TestE2E_` — the e2e suite must pass under the `OTTO_E2E=1` gate (or whatever the project convention is — read `tests/e2e/e2e_test.go` for the gate var name).

    Commit subject: `test(08-05): real-binary e2e for plugin chain (SC1+SC5+SC7 + T-8-CFG+T-8-LEAK+T-8-HASH-BOOT)`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp; OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -count=1 -race -timeout=180s -run TestE2E_ 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/e2e/plugin_chain_test.go` has `//go:build e2e` tag. Verify: `head -3 tests/e2e/plugin_chain_test.go | grep -F '//go:build e2e'` exits 0.
    - Source assertion: Defines 9 `TestE2E_` functions. Verify: `grep -cE '^func TestE2E_' tests/e2e/plugin_chain_test.go` returns at least `9`.
    - Source assertion: Scenario 3 includes literal `TOPSECRET_AUTH_E2E` sentinel and the assertion negates its presence in the response body. Verify: `grep -F 'TOPSECRET_AUTH_E2E' tests/e2e/plugin_chain_test.go` exits 0.
    - Behavior assertion: Scenarios 1-4 + 6-9 pass under the e2e gate. Scenario 5 may skip if harness lacks instrumentation; document.
    - Behavior assertion: Scenario 6 verifies three distinct error envelope shapes (one per surface) end-to-end through the real binary — SC1 acceptance bar.
    - Behavior assertion: Scenarios 7 + 8 verify boot-error refusal — T-8-CFG and T-8-HASH-BOOT mitigations confirmed against the running binary.
    - Test-command assertion: `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -count=1 -race -timeout=180s -run TestE2E_` exits 0.
  </acceptance_criteria>
  <done>9-scenario real-binary e2e suite green; SC1 + SC5 + SC7 verified end-to-end; T-8-CFG + T-8-LEAK + T-8-HASH-BOOT verified end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 7: Blocking human-verify — operator workflow against the real running binary</name>
  <read_first>
    - .planning/phases/08-plugin-hook-chain/08-VALIDATION.md §Manual-Only Verifications (lines 82-86) — operator workflow checks
    - .planning/phases/08-plugin-hook-chain/08-CONTEXT.md §specifics line 581 — `/health/hooks` operator visibility cash-out
    - .planning/phases/08-plugin-hook-chain/08-CONTEXT.md §specifics line 586 — hash-key key-rotation as a feature
    - docs/operating.md (if exists; if not, the human-verify step requires creating one in this checkpoint's follow-up)
  </read_first>
  <what-built>
    All four Phase 8 hooks are wired into the gateway via cmd/otto-gateway/main.go. The chain runs on every request through the canonical engine. `/health/hooks` exposes the registered chain. Five env knobs (ENABLED_HOOKS, PII_REDACTION_ENABLED, PII_ENABLED_ENTITIES, PII_REDACTION_MODE, PII_HASH_KEY) control behavior. Boot-fail-fast for unknown ENABLED_HOOKS names and mode=hash-without-key.
  </what-built>
  <how-to-verify>
    1. From the project root, run `make build` (or `go build ./cmd/otto-gateway -o bin/otto-gateway`).
    2. Boot the binary with default config:
       ```
       AUTH_TOKEN=test123 bin/otto-gateway
       ```
       Wait for "listening on :11434" or equivalent log line.
    3. **SC7 check — view-only introspection works:**
       ```
       curl -s http://localhost:11434/health/hooks | jq
       ```
       Confirm:
       - JSON response with `hooks` array
       - Four entries present: RequestIDHook, AuthHook, PIIRedactionHook, LoggingHook (in that order on Pre; LoggingHook also appears on Post or has `kind:"Pre,Post"`)
       - AuthHook entry has `config.token_count: 1` (NOT `tokens: ["test123"]`)
       - PIIRedactionHook entry shows `config.enabled: false` (default), `config.mode: "replace"`, `config.entities: ["Email", "IPv4", "IPv6", "SSN", "CreditCard", "USPhone"]`
       - **NO regex sources anywhere in the response.**
       - **NO `test123` (the AUTH_TOKEN value) anywhere in the response.**
    4. **SC1 check — short-circuit through chain to adapter render:**
       ```
       curl -i -X POST http://localhost:11434/api/chat \
         -H 'Authorization: Bearer wrongtoken' \
         -H 'Content-Type: application/json' \
         -d '{"model":"auto","messages":[{"role":"user","content":"hi"}]}'
       ```
       Confirm: response is a 4xx with Ollama error envelope shape (`{"error": "..."}`).
       Repeat with `/v1/chat/completions` for OpenAI shape `{"error": {"message", "type"}}`.
       Repeat with `/v1/messages` + `-H 'anthropic-version: 2023-06-01'` for Anthropic shape `{"type": "error", "error": {"type", "message"}}`.
    5. **SC5 check — ENABLED_HOOKS allowlist:**
       - Stop the binary. Restart with: `ENABLED_HOOKS=RequestIDHook,LoggingHook AUTH_TOKEN=test123 bin/otto-gateway`.
       - `curl /health/hooks` — confirm only the two hooks appear in the response, in REGISTRATION order (RequestIDHook first, LoggingHook last).
       - `curl /api/chat` WITHOUT bearer header — confirm 200 (AuthHook is filtered out; auth is disabled per accepted T-8-AUTH-BYPASS behavior).
       - Stop. Restart with `ENABLED_HOOKS=BogusHook`. Confirm: gateway exits immediately with stderr containing `unknown hook` AND `BogusHook`.
    6. **SC6 check — PII redaction works:**
       - Stop. Restart with `PII_REDACTION_ENABLED=true PII_REDACTION_MODE=replace bin/otto-gateway`.
       - Send a request with content containing `corey@cmetech.io`.
       - Observe gateway logs (or upstream kiro-cli logs if accessible): the canonical request sent downstream should contain `<EMAIL>` or `<EMAIL_1>`, NOT `corey@cmetech.io`.
       - Inspect the structured slog `plugin.before` log line — should reference `redacted={Email:1}` field.
    7. **D-05 check — hash mode key rotation invalidates correlation:**
       - Restart with `PII_REDACTION_ENABLED=true PII_REDACTION_MODE=hash PII_HASH_KEY=test-key-32-bytes-padding-here!!`. Send a request containing `corey@cmetech.io`. Note the tag in logs (e.g., `<EMAIL:h-7c1aa3f9>`).
       - Restart with a DIFFERENT `PII_HASH_KEY=different-key-32-bytes-padding-x`. Send the same email. Note the tag (should be different from the prior tag — confirms key rotation invalidates correlation per D-05).
    8. **T-8-HASH-BOOT check — mode=hash without key refuses to start:**
       - Restart with `PII_REDACTION_MODE=hash` and PII_HASH_KEY UNSET. Confirm: gateway exits non-zero with stderr containing `PII_HASH_KEY`.
    9. **`docs/operating.md` update:**
       - Confirm `docs/operating.md` (or wherever operator env-var docs live) has been updated to document:
         - The five new env vars (ENABLED_HOOKS, PII_REDACTION_ENABLED, PII_ENABLED_ENTITIES, PII_REDACTION_MODE, PII_HASH_KEY)
         - The two-knob composition (ENABLED_HOOKS = presence; PII_REDACTION_ENABLED = work-doing) per CONTEXT.md §specifics line 572-575
         - The restart-to-apply rule (SC7; no runtime mutate path)
         - The PII_HASH_KEY rotation workflow (D-05 + CONTEXT.md §specifics line 586)
         - The boot-error conditions (unknown ENABLED_HOOKS name; mode=hash without key)
       - If `docs/operating.md` was NOT updated, **note this in the resume signal** — the executor must add it before phase close.
    10. Reply with one of:
       - `approved` — Phase 8 acceptance closed; ready for `/gsd-verify-work`.
       - `approved with notes: <details>` — accepted with non-blocking observations recorded in SUMMARY.
       - `block: <reason>` — phase cannot close until reason is addressed.
  </how-to-verify>
  <resume-signal>Type `approved`, `approved with notes: <details>`, or `block: <reason>`.</resume-signal>
  <acceptance_criteria>
    - Source assertion: this checkpoint task is marked `gate="blocking-human"` — auto-advance prohibited.
    - Behavior assertion: operator response captured in the plan SUMMARY's "Operator sign-off" section.
    - Test-command assertion: N/A (human-only; the SC1/SC5/SC7 + T-8-CFG/T-8-LEAK/T-8-HASH-BOOT automated checks were satisfied in Tasks 1-6).
  </acceptance_criteria>
  <done>Operator confirms the end-to-end Phase 8 closeout via the running binary; docs/operating.md updated; Phase 8 is shippable.</done>
</task>

</tasks>

<verification>
- `go test ./... -count=1 -race -timeout=120s` exits 0 (full project test suite green; Phase 8 unit + handler + e2e all pass).
- `OTTO_E2E=1 go test -tags e2e ./tests/e2e/... -count=1 -race -timeout=180s -run TestE2E_` exits 0 (real-binary 9-scenario e2e green).
- `gosec ./... -severity high -confidence high` exits 0 (no weak-crypto or subprocess findings).
- `go-arch-lint check` exits 0 (TRST-04 preserved across all five Phase 8 slices).
- `go build ./...` exits 0.
- `make lint` (golangci-lint) exits 0.
- `make ci` exits 0 if it composes the above gates.
- Operator human-verify checkpoint (Task 7) approved.
</verification>

<success_criteria>
- **SC1 satisfied**: PreHook short-circuit through engine to adapter render verified across three surfaces (Ollama, OpenAI, Anthropic) via e2e Scenario 6.
- **SC5 satisfied**: ENABLED_HOOKS allowlist + registration-order preservation + typo-fail-fast verified end-to-end via e2e Scenarios 7 + 9 plus unit Tests 13-15 in chain_filter_test.go.
- **SC6 satisfied**: PIIRedactionHook env knobs work; six recognizers active; hash mode requires key (e2e Scenario 8); operator-confirmed via human-verify Task 7 step 6.
- **SC7 satisfied**: `/health/hooks` view-only endpoint ships; auth-exempt; returns registration order; no secrets leak; no runtime mutate path (POST/PUT/DELETE → 405); e2e Scenarios 1-4 + handler tests.
- **OBSV-04 satisfied**: `/health/hooks` JSON envelope renders chain introspection per RESEARCH Example 6.
- **PLUG-05 satisfied**: ENABLED_HOOKS env var enables/disables hooks at boot; boot-error on typo; unit + e2e coverage.
- **D-01 satisfied**: hardcoded plugin.Chain literal in main.go is the single source of truth; no Register(name, factory) indirection.
- **D-02 satisfied**: registration order preserved by Filter (test asserts); typo-fail-fast (e2e Scenario 7); composition with PII_REDACTION_ENABLED documented.
- **D-04 satisfied**: chain order RequestID → Auth → PII → Logging on Pre verified by the construction in main.go AND by slice 3's LoggingHook seeing redacted content end-to-end.
- **D-05 satisfied**: PII_HASH_KEY required for hash mode (unit + e2e); key rotation invalidates correlation (operator-verified).
- **T-8-CFG mitigated**: typo at ENABLED_HOOKS or PII_REDACTION_MODE or PII_ENABLED_ENTITIES → boot error.
- **T-8-LEAK mitigated**: `/health/hooks` exposes no secrets; e2e + handler tests enforce.
- **T-8-HASH-BOOT mitigated**: mode=hash without PII_HASH_KEY → boot error; e2e + unit tests enforce.
- **Migration boundary closed**: `auth.Bearer` chi middleware removed; AuthHook in chain owns auth; auth.IPAllowlist stays at chi.
- **TRST-04 preserved**: server doesn't import plugin; main.go bridges via hooksDescriptionAdapter + structural HookDescription redeclaration.
</success_criteria>

<output>
Create `.planning/phases/08-plugin-hook-chain/08-05-SUMMARY.md` when done. Include:
- Operator sign-off recorded from Task 7 (`approved` / `approved with notes` / `block`).
- Whether `docs/operating.md` was updated in this slice (yes / no — if no, list the doc deltas that remain).
- The exact registration order of hooks as observed in `/health/hooks` against the running binary — slice 5's verification baseline.
- Whether LoggingHook appears as ONE entry with `kind:"Pre,Post"` OR as TWO entries (`Pre` + `Post`) in the response — pin the chosen convention for future docs.
- Final disposition of `auth.Bearer` middleware: removed in slice 5 OR was already removed in slice 2 (per 08-02-SUMMARY.md).
- Any test scenarios that had to skip due to e2e harness limitations (Scenario 5 PII redaction visibility was the documented risk).
- Full-suite test counts before and after slice 5 (Phase 8 total test deltas).
- Any architectural deviations from the planned design (e.g., if the LoggingHook dedupe-in-handler approach was not viable and the response shows two entries instead of one).
</output>
</content>
</invoke>