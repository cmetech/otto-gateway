---
phase: 05-pool-stateful-sessions
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - internal/session/entry_acp.go
  - internal/session/registry.go
  - internal/session/registry_test.go
  - internal/session/entry_acp_test.go
  - tests/e2e/pool_sessions_e2e_test.go
  - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
requirements_addressed:
  - SESS-01
  - SESS-02
  - SESS-03
tags:
  - session
  - acp
  - kiro-cli
  - wire-protocol
  - root-cause
  - gap-closure
  - sc3
  - sc4

must_haves:
  truths:
    - "Stateful /api/chat -H 'X-Session-Id: <sid>' returns 200 with an assistant message body against the real kiro-cli — 100% reliability across multiple sids and fresh boots (SC3 closure)."
    - "Stateless /api/chat (no X-Session-Id) continues to return 200 — no regression of the working pool path."
    - "OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions/SessionIDAffinity' ./tests/e2e/... -count=1 -timeout=60s exits 0."
    - "OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions/(IdleReap_RealTime|DeleteSession_OK|DeleteSession_CancelsInFlight)' ./tests/e2e/... -count=1 -timeout=120s exits 0 (SC4 closure)."
    - "DeleteSession_CancelsInFlight asserts that AT LEAST ONE chunk was produced before DELETE — strengthened so it cannot pass for the wrong reason (i.e., a Prompt that returned 500 immediately)."
    - "A new wire-protocol diagnostic produces a captured JSON-RPC transcript of (a) the working pool path and (b) the broken session path against the SAME kiro-cli binary; the diff identifies the protocol-level cause and is committed to .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md as the load-bearing root-cause artifact (referenced from the fix-site source comment per D-deep_work_rules)."
    - "The code fix is paired with a unit test using an ACP fake (fakeacp_test.go or equivalent in internal/acp / internal/session) that asserts the corrected RPC sequence — guarding against regression without requiring the live e2e suite."
    - "go test ./... -count=1 -race -timeout=180s stays green across all 12 packages; go vet ./... clean; go build ./... clean."
    - "No change to D-15 / D-16 wire shape (locked by /health/agents external observers); no change to the pool path code unless the root cause demands it."
  artifacts:
    - path: ".planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md"
      provides: "Wire-protocol diff transcript + 1-paragraph root cause + remediation summary (deliverable from Task 1+2+3)."
      contains: "## Working Pool Path"
    - path: "internal/session/entry_acp.go"
      provides: "Corrected Entry.NewSession / Entry.Prompt RPC sequence per root cause."
      contains: "func (e *Entry) NewSession"
    - path: "internal/session/entry_acp_test.go"
      provides: "Unit test exercising the corrected protocol sequence via an ACP fake — guards against regression."
      contains: "func TestEntry_Prompt"
    - path: "tests/e2e/pool_sessions_e2e_test.go"
      provides: "Strengthened DeleteSession_CancelsInFlight that asserts ≥1 streamed chunk before DELETE."
      contains: "chunk"
  key_links:
    - from: "internal/session/entry_acp.go::Entry.NewSession or Entry.Prompt"
      to: ".planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md"
      via: "source comment cites the WIRE-DIFF file by relative path so the executor can re-discover the root cause"
      pattern: "05-04-WIRE-DIFF"
    - from: "tests/e2e/pool_sessions_e2e_test.go::SessionIDAffinity"
      to: "internal/session/entry_acp.go::Entry.Prompt"
      via: "live HTTP /api/chat → adapter → engine.Run → Entry.Prompt → kiro-cli session/prompt"
      pattern: "e2e-sid-1"
---

<objective>
Close the Phase 5 BLOCKER gap: stateful `/api/chat -H 'X-Session-Id: <sid>'` requests reproducibly return HTTP 500 with `engine: prompt: session: prompt: acp: prompt: rpc error -32603: Internal error` against the real kiro-cli, while the stateless path on the same gateway works correctly. The session registry creates entries (visible in `/health/agents`), but kiro-cli rejects every subsequent `session/prompt` against the cached session id.

Purpose: SESS-01 / SESS-02 / SESS-03 are unit-green but integration-broken. Phase 5's load-bearing property ("stateful sessions keyed by X-Session-Id via a registry") is observably unmet. This plan diagnoses the protocol-level difference between the working pool path and the broken session path, encodes the fix, and lifts SC3 + SC4 to FULLY SATISFIED.

Output:
- `05-04-WIRE-DIFF.md` — captured JSON-RPC transcripts + diff + root-cause note (load-bearing artifact, referenced from the fix-site comment).
- Code fix in `internal/session/entry_acp.go` (and/or `internal/session/registry.go::createEntry` depending on root cause).
- Unit test using an ACP fake that asserts the corrected protocol sequence.
- Strengthened `DeleteSession_CancelsInFlight` e2e subtest.
- All three previously-failing e2e subtests green against live kiro-cli.

Constraints (from CLAUDE.md + planning_context):
- Go 1.23+, stdlib + chi, race-detector ALWAYS on, no cgo.
- Do NOT change the D-15/D-16 wire shape (`/health/agents` is externally observed).
- Do NOT modify the pool path code unless the root cause demands it (regression risk is high and pool is verified green).
- Do NOT re-plan CR-01..04 or WR-01..07 (already fixed); do NOT add WR-02 (deferred by D-18 design conflict).
- The diagnostic tasks find the root cause; only after the diff is captured may the code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pool-stateful-sessions/05-CONTEXT.md
@.planning/phases/05-pool-stateful-sessions/05-RESEARCH.md
@.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
@.planning/phases/05-pool-stateful-sessions/05-02-PLAN.md
@.planning/phases/05-pool-stateful-sessions/05-02-SUMMARY.md
@.planning/phases/05-pool-stateful-sessions/05-03-SUMMARY.md
@docs/reference/acp_wire_shapes.md

# Source files the executor MUST read (root-cause investigation):
@internal/session/entry_acp.go
@internal/session/registry.go
@internal/pool/pool.go
@internal/acp/client.go
@internal/engine/engine.go
@tests/e2e/pool_sessions_e2e_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture wire-level JSON-RPC transcript of the WORKING pool path</name>
  <files>
    - tools/kiro-shim/main.go
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md
  </files>
  <read_first>
    - internal/pool/pool.go (NewSession line ~408, Warmup line ~114, Prompt line ~467) — observe the per-request fresh-NewSession-then-Prompt sequence
    - internal/acp/client.go (Initialize, NewSession, SetModel, Prompt, Cancel, the rpcRequest/rpcResponse types, write/read paths)
    - internal/engine/engine.go (Run line ~146) — confirms engine calls ACPClient.NewSession → optional SetModel → Prompt in that order
    - tests/e2e/pool_sessions_e2e_test.go (SessionIDAffinity line ~205) — confirms test invocation shape
    - docs/reference/acp_wire_shapes.md — the authoritative wire spec for comparison
  </read_first>
  <action>
    Build a JSON-RPC stdio recorder that wraps the kiro-cli subprocess and writes every outbound frame (gateway→kiro) and every inbound frame (kiro→gateway) to a transcript file. Two viable approaches — pick ONE based on which is faster to ship cleanly:

    Option A (shim binary, preferred): Create `tools/kiro-shim/main.go` (a tiny ~80-LOC Go program) that:
    1. Spawns the real kiro-cli via `os/exec` with the same args/env it received,
    2. Pipes stdin → kiro.stdin while teeing to `/tmp/kiro-wire-{pid}.out`,
    3. Pipes kiro.stdout → stdout while teeing to `/tmp/kiro-wire-{pid}.in`,
    4. Prefixes each line in the tee with `[OUT] ` / `[IN] ` + nanosecond timestamp.
    Build it with `go build -o /tmp/kiro-shim ./tools/kiro-shim`.

    Option B (Go-level wire log): Add a DEBUG-gated `tee` in `internal/acp/framer.go` (the writeFrame / readFrame entry points) that emits every JSON-RPC frame to `slog` at DEBUG level when env `OTTO_ACP_WIRE_LOG=1` is set. Capture via `OTTO_ACP_WIRE_LOG=1 ... 2>&1 | grep '^level=DEBUG msg=acp.wire'`.

    Then:
    1. Pick the shim approach unless framer instrumentation is trivially smaller; the shim survives without modifying production code.
    2. Boot the gateway with the shim as the kiro binary: `KIRO_CMD=/tmp/kiro-shim KIRO_ARGS="$(which kiro-cli) acp" POOL_SIZE=2 ./bin/otto-gateway`.
    3. Fire ONE stateless `/api/chat` request: `curl -X POST http://127.0.0.1:11434/api/chat -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'` → expect HTTP 200.
    4. Capture the resulting `/tmp/kiro-wire-*.{out,in}` files and merge them by timestamp into `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl` (one line per frame, prefixed with direction + ts).
    5. Copy the transcript into the `## Working Pool Path` section of `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` (create the file with that single section for now; later tasks fill in the broken-path section + diff + root cause).

    Concrete RPC fields to capture per frame: `jsonrpc`, `id`, `method`, `params` (full body), `result` (full body) for responses. Notifications (no id) are captured verbatim. Timestamps in nanoseconds since process start.

    Output file MUST be deterministic enough to diff: redact only the kiro-cli process PID and any timestamp inside response bodies (e.g., usage.duration_ns) — the framing timestamps are fine. Use a single jq-style normalisation pass if needed: `jq -c '. | del(.params.timestamp?)' < raw.jsonl > normalized.jsonl`.

    Important: DO NOT commit the shim binary or temp files. The shim source under `tools/kiro-shim/` is fine to commit (it's useful for future debugging); transcripts live in `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-*.jsonl`. Add `tools/kiro-shim/` to `.go-arch-lint.yml` as an isolated component (no internal imports) so the arch-lint gate stays clean.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl && test -s .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl && grep -c '"method"' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl | awk '{exit !($1 >= 3)}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl` exists and is non-empty.
    - Transcript contains at least: `initialize` (request + response), `session/new` (request + response with a real session id), `session/prompt` (request + at least one `session/update` notification + final response).
    - Transcript also contains the per-request `session/new` that engine.Run issues (i.e., the working pool path DOES call NewSession per request — that is the pool's stateless-engine contract).
    - File `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` exists with a `## Working Pool Path` section containing or @-referencing the transcript.
    - The live `/api/chat` request returned HTTP 200 (recorded as a sanity line at the top of the transcript or in a sibling `.out` file).
  </acceptance_criteria>
  <done>Wire transcript captured for the working stateless path. Ready to capture the broken stateful path with the same shim against the same kiro-cli binary in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture wire-level JSON-RPC transcript of the BROKEN session path</name>
  <files>
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md
  </files>
  <read_first>
    - internal/session/registry.go (createEntry line ~221, Get line ~169) — confirms the registry's NewSession-then-cache lifecycle
    - internal/session/entry_acp.go (NewSession line ~24, Prompt line ~48) — confirms Entry.NewSession is a pure accessor; Entry.Prompt forwards to Client.Prompt with the cached SessionID
    - internal/adapter/ollama/handlers.go (resolveEngine line ~102, handleChat line ~49) — confirms the X-Session-Id branch sequence
    - The Task 1 shim and its transcript layout
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl (Task 1 output) — the comparison baseline
  </read_first>
  <action>
    Reuse the Task 1 shim binary and capture infrastructure. Boot a fresh gateway instance (kill any prior one to start with a clean transcript) and fire a stateful request:

    1. `KIRO_CMD=/tmp/kiro-shim KIRO_ARGS="$(which kiro-cli) acp" POOL_SIZE=2 ./bin/otto-gateway &` — fresh boot.
    2. Wait for warmup to settle (`/health` returns pool size 2).
    3. Fire ONE stateful request: `curl -i -X POST -H 'X-Session-Id: smoke' http://127.0.0.1:11434/api/chat -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'` — expect HTTP 500 with `acp: prompt: rpc error -32603: Internal error`.
    4. Capture the resulting per-pid transcripts. Identify the dedicated-session subprocess (NOT the pool slots) — match by `cwd` argument passed at spawn time, or by the appearance of a `session/new` whose result.sessionId is later referenced by the failing `session/prompt`.
    5. Merge into `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl`.
    6. Append a `## Broken Session Path` section to `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` linking to the new transcript, plus the curl response body and the gateway log line containing `acp: prompt: rpc error -32603`.

    DO capture: every frame the registry's kiro-cli subprocess saw, including any `initialize` retry, the `session/new` that succeeded, any `session/set_model` (or absence thereof), the failing `session/prompt`, and the kiro-cli error response body (params/error/data fields). The kiro-cli -32603 error message body is the key forensic artifact — capture the FULL `error.data` field if present.

    Concrete normalization: use the same jq pass as Task 1 so timestamps inside response bodies do not pollute the diff.

    DO NOT yet attempt to fix anything; that is Tasks 3+4. The deliverable here is purely the transcript.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl && test -s .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl && grep -c '"method"\s*:\s*"session/prompt"' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl | awk '{exit !($1 >= 1)}' && grep -q -i 'rpc error\|-32603\|Internal error' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl` exists and is non-empty.
    - Transcript contains at least: `initialize` + `session/new` (that succeeded), followed by `session/prompt` that received an error response carrying code `-32603` (the JSON-RPC error envelope is captured verbatim — `code`, `message`, `data` fields).
    - WIRE-DIFF.md has a `## Broken Session Path` section linking the transcript and quoting the kiro-cli error body verbatim.
    - The curl response observed during capture was HTTP 500 with the expected error string — recorded at the top of the transcript or in a sibling `.out` file.
  </acceptance_criteria>
  <done>Both transcripts captured against the same kiro-cli binary, in the same environment, with deterministic normalisation. Ready for diff + root-cause analysis in Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Diff transcripts and identify the protocol-level root cause</name>
  <files>
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md
  </files>
  <read_first>
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl (Task 1)
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl (Task 2)
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md (Tasks 1+2 partial output)
    - docs/reference/acp_wire_shapes.md (authoritative spec for session lifecycle)
    - internal/engine/engine.go::Run (engine sequence ground truth)
    - internal/pool/pool.go::Warmup + Pool.NewSession + Pool.Prompt (pool sequence ground truth — note: pool calls NewSession PER REQUEST via engine.Run; the warmup-time NewSession is followed by Cancel(sid) so it does not stick)
    - internal/session/registry.go::createEntry (session sequence — calls NewSession ONCE at entry creation)
    - internal/session/entry_acp.go::NewSession (returns cached SessionID — engine.Run reuses it across requests)
    - internal/session/entry_acp.go::Prompt (forwards to Client.Prompt with the cached SessionID)
  </read_first>
  <action>
    Compute a structural diff of the two transcripts focused on the JSON-RPC method sequence (ignore frame-level ids and timestamps). Use `jq -c '{method, params: (.params // null | del(.timestamp?, .id?))}' < transcript.jsonl > sequence.txt` to produce a methods-only summary, then `diff -u sequence-pool.txt sequence-session.txt` for the comparison.

    Hypothesis-ordered analysis (from VERIFICATION.md gap 1):
    - **H-A**: kiro-cli rejects reuse of a cached session id across multiple `session/prompt` calls. If the session-path transcript shows ONE `session/new` followed by a `session/prompt` that errors, and the pool-path transcript shows `session/new` IMMEDIATELY BEFORE EVERY `session/prompt`, then kiro-cli's session lifecycle is "session/new → ONE session/prompt → terminate". Evidence: pool.Warmup line 148 explicitly calls `slot.Client.Cancel(sid)` immediately after the warmup-time NewSession because "engine.Run creates a fresh session per request per D-05 / slot-stateless semantics" (pool.go comment line 145-147). The registry path defeats this by caching the SessionID. This is the MOST LIKELY root cause given the evidence in the existing code.
    - **H-B**: cwd handshake differs. The pool's warmup uses `cfg.KiroCWD`; subsequent engine.Run calls pass a pickCwd-derived cwd via `slot.Client.NewSession(ctx, cwd)`. Entry.NewSession ignores its cwd argument and returns the cached SessionID — if the SessionID is cwd-bound on kiro-cli's side, prompts from a different cwd context fail. Check whether cwd was different between Task 1 and Task 2 captures; if not, H-B is not exclusively the root cause.
    - **H-C**: implicit SetModel sequence. The pool sets the model per-request when req.Model is non-empty and not "auto"; the registry's diff-skip means a model change between requests calls SetModel ONCE then caches. With `model: "auto"`, neither path calls SetModel — so H-C is not the root cause for the curl reproduction.
    - **H-D**: prompt block construction differs. Both paths funnel through `buildBlocks` in engine.Run — same canonical → ACP conversion. So H-D is not a root cause unless the diff shows different block shapes (which would itself be a Phase 2 bug).

    Confirm the root cause by reading the actual diff. Write a single-paragraph (3-6 sentence) root-cause note into the `## Root Cause` section of `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md`. The note MUST:
    - Name the specific JSON-RPC field or sequence step that differs.
    - Cite the line numbers in pool.go vs entry_acp.go / registry.go where the divergence is encoded.
    - State the remediation in one sentence (e.g., "Entry.NewSession must call Client.NewSession per request and discard the cached SessionID" OR "createEntry must NOT call NewSession; instead defer it to Entry.NewSession on first Prompt").

    Also write a `## Remediation Plan` section enumerating:
    - Files that will change in Task 4 (paths + 1-line rationale per file).
    - Whether the fix is in `entry_acp.go` (most likely) or also `registry.go::createEntry` (possible if NewSession must move out of createEntry).
    - Whether the SessionID caching strategy changes (likely: yes, per-prompt fresh session) or stays (only if root cause is something else).
    - Any new test scaffolding needed in `entry_acp_test.go` to guard the fix.

    Commit `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` together with both `.jsonl` transcripts at this checkpoint so subsequent tasks have a stable artifact to reference.
  </action>
  <verify>
    <automated>grep -q '^## Root Cause' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md && grep -q '^## Remediation Plan' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md && grep -E 'entry_acp\.go|registry\.go' .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md | head -1 | wc -l | awk '{exit !($1 >= 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - WIRE-DIFF.md contains a `## Root Cause` section naming a specific JSON-RPC field, method, or sequence difference (NOT "the session path is broken" — must be at the wire level).
    - WIRE-DIFF.md contains a `## Remediation Plan` section enumerating affected files and the test scaffolding for Task 5.
    - The root-cause note cites at least one source-file path with a line-number range (e.g., `internal/session/entry_acp.go:24-26`).
    - The remediation plan identifies whether the fix is local to `entry_acp.go` or requires changes in `registry.go::createEntry` as well.
  </acceptance_criteria>
  <done>Root cause identified and documented as a load-bearing artifact (05-04-WIRE-DIFF.md). Remediation plan is concrete and bounded. Code change in Task 4 follows the plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Encode the fix in internal/session and prove it via an ACP fake</name>
  <files>
    - internal/session/entry_acp.go
    - internal/session/registry.go
    - internal/session/entry_acp_test.go
  </files>
  <read_first>
    - .planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md (Task 3 — the canonical root cause + remediation plan)
    - internal/session/entry_acp.go (current implementation — NewSession is a pure accessor; Prompt forwards to Client.Prompt with cached SessionID)
    - internal/session/registry.go::createEntry (currently calls Client.NewSession during creation and caches the SessionID)
    - internal/session/registry.go::Entry (current field set — Mu, Client, SessionID, LastUsed, LastModel, Dead, creating, ready, readyOnce)
    - internal/session/registry_test.go (existing fake-based tests — fakeClientFactory + fakeACPClient patterns)
    - internal/acp/fakeacp_test.go (the ACP fake server used by integration_test.go — patterns for asserting wire sequences)
    - internal/pool/pool.go::Prompt + Pool.NewSession (the working analog — note the per-request NewSession; this is what entry_acp.go must emulate)
    - internal/engine/engine.go::Run (sequence ground truth — engine calls ACPClient.NewSession → optional SetModel → Prompt)
  </read_first>
  <behavior>
    The behavior expected of the fix is determined by Task 3's root cause; encode the tests FIRST (RED), then implement (GREEN). Likely behavior given the H-A hypothesis (most probable):

    - Test 1 (`TestEntry_Prompt_CallsNewSessionPerRequest`): Given an `Entry` wired to a fake PoolClient that records every NewSession + Prompt call, calling `Entry.NewSession(ctx, cwd)` followed by `Entry.Prompt(ctx, sid, blocks)` once MUST produce one Client.NewSession call AND one Client.Prompt call against THAT freshly-issued sid (NOT against a cached older sid). Two consecutive Entry.NewSession + Entry.Prompt cycles MUST produce two NewSession calls.
    - Test 2 (`TestEntry_NewSession_DoesNotReuseCached`): Calling `Entry.NewSession(ctx, cwd)` twice in sequence returns two DIFFERENT session ids (because the fake increments a counter); Entry.SessionID is no longer the load-bearing cache for engine.Run.
    - Test 3 (`TestEntry_Prompt_PropagatesNewSessionError`): When the fake's NewSession returns an error, Entry.NewSession surfaces it wrapped; Entry.Prompt is never called.
    - Test 4 (regression — guards H-B if cwd was the root cause): Each Entry.NewSession passes the cwd argument through to Client.NewSession verbatim.
    - Test 5 (regression — keep the per-session subprocess affinity intact): The fake counts spawn calls; two NewSession calls on the same Entry MUST NOT spawn a second subprocess (Entry still owns ONE Client.Done()-watched kiro-cli child).

    If Task 3 identified a DIFFERENT root cause (H-B / something else), replace the test set above with tests targeting that root cause directly. The behavior block is binding once Task 3 lands — encode whatever Task 3 prescribed.

    Refactor notes for the GREEN step:
    - If the fix is "call NewSession per request": Entry.NewSession changes from `return e.SessionID, nil` to `return e.Client.NewSession(ctx, cwd)`. Entry.SessionID becomes either a derived per-call value (passed to Cancel/SetModel via the standard engine.Run flow) or is removed entirely.
    - `registry.go::createEntry` may need to STOP calling `client.NewSession` during creation — defer it to the first Entry.NewSession call. Or keep the createEntry call as a "warmup" but call Cancel(sid) on it immediately (pool.Warmup pattern at pool.go:148).
    - The `/health/agents` D-16 session row shape MUST NOT change. SessionDetail.ID is the client-supplied X-Session-Id, not the kiro-cli session id — confirm this in `internal/session/stats.go` before refactoring (it already is per the verification report).
    - Per-entry Mu serialization (D-07) STAYS. Surface handlers still Lock the entry around the engine.Run call.
    - LastModel caching (D-09) STAYS as a per-entry property; the diff-skip is at the Entry.SetModel level — but if NewSession is now per-request, SetModel must run for every request where the model was set on a prior cycle... investigate during the GREEN step whether D-09's diff-skip still makes sense given a fresh kiro-cli session per request. Most likely: the diff-skip is no longer correct (each new kiro-cli session starts with no model set), so SetModel always runs when req.Model is non-empty and not "auto". Update the behavior in entry_acp.go and remove or invert the diff-skip cache as Task 3's remediation plan prescribes.
    - The source comment at the modified function MUST cite `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` so future readers can re-discover the root cause.
  </behavior>
  <action>
    RED → GREEN → REFACTOR cycle:

    1. RED: Create `internal/session/entry_acp_test.go` with the test set in `<behavior>` (and any Task-3-specific cases). Use a custom `recordingClient` (PoolClient) that records ordered method calls + their arguments; reuse the patterns from `registry_test.go`'s fakeClientFactory if practical, but the new tests live in a sibling file to avoid coupling. Run `go test ./internal/session/... -count=1 -run TestEntry_Prompt` and confirm ALL the new tests FAIL with the current `entry_acp.go` (cached-SessionID accessor) — this proves the tests actually exercise the broken behavior.

    2. GREEN: Apply the fix prescribed by Task 3's `## Remediation Plan`. Most likely changes:
       - `internal/session/entry_acp.go::NewSession` changes from `return e.SessionID, nil` to `return e.Client.NewSession(ctx, cwd)`.
       - `internal/session/registry.go::createEntry` may stop calling `client.NewSession` (defer to Entry.NewSession on first use) OR keep it but Cancel the warmup session immediately (pool.Warmup parity).
       - The `Entry.SessionID` field may become unused; if so, REMOVE it (and update `/health/agents` SessionDetail rendering to use the client-supplied sid which it already does per stats.go).
       - Re-evaluate D-09 SetModel diff-skip: if every request now has a fresh kiro-cli session id, the diff-skip cache (LastModel) is stale across requests. Either delete the field + force SetModel-when-non-auto, or scope the cache to the current session id (LastSetSessionID + LastModel) — the simpler fix is to delete the cache and always issue SetModel.
       - Add a 5-7 line comment at the top of the modified function citing `05-04-WIRE-DIFF.md` so future readers know why this changed.
       - Run `go test ./internal/session/... -count=1 -race -timeout=60s` — the new tests now PASS, and ALL existing registry/reaper tests STAY GREEN.

    3. REFACTOR: If the GREEN step left dead fields/parameters (e.g., the cwd argument on Entry.NewSession that used to be ignored), clean them up. Run `go vet ./...` + `golangci-lint run` to confirm no new findings.

    4. Run the full unit suite: `go test ./... -count=1 -race -timeout=180s` — ALL 12 packages stay green.

    5. Commit at this checkpoint with a message like `fix(05): SC3 root cause — per-request session/new instead of cached SessionID (see 05-04-WIRE-DIFF.md)`.

    DO NOT touch pool path code. DO NOT change the D-15/D-16 wire shape. DO NOT remove the per-entry Mu (D-07 STAYS — the mutex serializes prompts per X-Session-Id, which is a separate concern from the kiro-cli session id lifecycle).
  </action>
  <verify>
    <automated>go test ./internal/session/... -count=1 -race -timeout=60s -run TestEntry_Prompt && go test ./... -count=1 -race -timeout=180s && go vet ./... && grep -q "05-04-WIRE-DIFF" internal/session/entry_acp.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/session/entry_acp_test.go` exists with at least 3 new tests covering Task-3-identified behavior.
    - `go test ./internal/session/... -count=1 -race -run TestEntry_Prompt` exits 0.
    - Full unit suite (`go test ./... -count=1 -race -timeout=180s`) exits 0 across all 12 packages.
    - `go vet ./...` clean; `golangci-lint run` no new findings.
    - The modified function in `entry_acp.go` (or `registry.go`) carries a comment citing `05-04-WIRE-DIFF.md`.
    - `git grep -c "05-04-WIRE-DIFF"` in source files returns ≥ 1.
  </acceptance_criteria>
  <done>Fix is encoded and unit-test-locked. Live integration verification follows in Task 6.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Strengthen DeleteSession_CancelsInFlight to assert ≥1 chunk before DELETE</name>
  <files>
    - tests/e2e/pool_sessions_e2e_test.go
  </files>
  <read_first>
    - tests/e2e/pool_sessions_e2e_test.go (DeleteSession_CancelsInFlight line ~342) — current weak assertion
    - tests/e2e/pool_sessions_e2e_test.go (chatBody helper, doJSON helper, bootGateway helper) — patterns used by sibling subtests
    - Verification gap text in 05-VERIFICATION.md noting this subtest "passes for the wrong reason"
  </read_first>
  <behavior>
    The current assertion only verifies that the streaming response BODY closes within 5 seconds. Under the pre-fix conditions, the stream closes immediately because Entry.Prompt returns a 500. Post-fix, an in-flight stream MUST actually be producing chunks when DELETE arrives.

    Strengthened behavior:
    - Test invocation streams `/api/chat` with `X-Session-Id: e2e-del-cancel` and `stream: true`.
    - The drain goroutine counts the number of bytes received AND the number of NDJSON lines observed BEFORE the DELETE call is issued.
    - After issuing DELETE, the stream must terminate within 5s (existing assertion).
    - NEW: assert that at least ONE complete NDJSON line was received BEFORE the DELETE was issued (proves the stream was producing real content from kiro-cli, not bailing out on a Prompt error).
    - NEW: the bytes-received counter MUST exceed 0 before DELETE — this is the load-bearing strengthening from VERIFICATION.md gap 2.

    Edge case: if kiro-cli streams its first chunk in ≥ 250ms, the existing `time.Sleep(250ms)` between stream start and DELETE may be too short. Either:
    - Wait for the chunk counter to reach 1 (with a bounded poll, max ~3s), THEN issue DELETE; OR
    - Increase the pre-DELETE sleep to 500ms - 1s based on observed timing in Task 4 reproduction.
  </behavior>
  <action>
    Modify `tests/e2e/pool_sessions_e2e_test.go::DeleteSession_CancelsInFlight`:

    1. Replace the simple `io.Copy(io.Discard, streamResp.Body)` with a chunk-counting reader (e.g., a `bufio.Scanner` that increments a counter per `Scan()` call, recording the count and total bytes under a `sync.Mutex`).
    2. Replace the fixed `time.Sleep(250ms)` with a bounded poll loop (≤ 3s) that waits for the chunk counter to reach 1, then issues DELETE. On timeout (no chunk in 3s), `t.Fatalf("stream produced no chunks within 3s — fix did not land")`.
    3. After the stream-done channel closes, assert the recorded chunk count is ≥ 1 with a clear failure message: `t.Errorf("stream produced %d chunks before DELETE; want ≥1 (DeleteSession_CancelsInFlight cannot pass for the wrong reason)", count)`.
    4. Keep the existing 5s bounded termination assertion intact.
    5. Run `OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions/DeleteSession_CancelsInFlight' ./tests/e2e/... -count=1 -timeout=60s` — confirm it passes once Task 4's fix is in place.

    Use only stdlib + the existing test helpers; no new dependencies.
  </action>
  <verify>
    <automated>OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions/DeleteSession_CancelsInFlight' ./tests/e2e/... -count=1 -timeout=60s</automated>
  </verify>
  <acceptance_criteria>
    - The DeleteSession_CancelsInFlight subtest now asserts ≥1 chunk was produced before DELETE (verified by reading the test source: `git grep -c 'chunks before DELETE' tests/e2e/pool_sessions_e2e_test.go` returns ≥ 1).
    - The subtest passes against the LIVE kiro-cli after Task 4's fix.
    - The subtest does NOT pass when run against the PRE-fix code path (verified locally by stashing Task 4's changes temporarily; not committed).
  </acceptance_criteria>
  <done>The cancel-in-flight subtest can no longer pass for the wrong reason. SC4's cancel semantics are integration-verified, not just unit-verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Run full e2e suite — SC3 + SC4 closure verification</name>
  <files>
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
  </files>
  <read_first>
    - tests/e2e/pool_sessions_e2e_test.go (all 10 subtests)
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md (the gap inventory we are closing)
    - The fix-site source comment (entry_acp.go) referencing 05-04-WIRE-DIFF.md
  </read_first>
  <action>
    Final integration sweep:

    1. `make build` — produce a fresh binary at `bin/otto-gateway`.
    2. `go test ./... -count=1 -race -timeout=180s` — full unit suite, race-detector on. Expect: ALL 12 packages green.
    3. `OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions' ./tests/e2e/... -count=1 -timeout=180s` — full e2e suite. Expect: all subtests pass (or AllDeadRespawnFails SKIP by design). The previously-failing trio (SessionIDAffinity, IdleReap_RealTime, DeleteSession_OK) MUST now pass; DeleteSession_CancelsInFlight (strengthened in Task 5) MUST also pass.
    4. Smoke curl: `KIRO_CMD=$(which kiro-cli) POOL_SIZE=2 ./bin/otto-gateway &` (fresh boot). Then:
       - `curl -X POST -H 'X-Session-Id: smoke-1' http://127.0.0.1:11434/api/chat -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'` — expect HTTP 200 with an assistant body containing real content.
       - Re-fire the same curl with the same sid (X-Session-Id: smoke-1) — also HTTP 200, same kiro-cli child PID still alive (`pgrep -P <gw-pid> kiro-cli` shows POOL_SIZE pool slots + 1 registry child).
       - Stateless curl (no X-Session-Id) — HTTP 200 (no regression).
       - `curl -X DELETE http://127.0.0.1:11434/v1/sessions/smoke-1` — HTTP 200 `{"deleted":"smoke-1"}`.
       - `curl http://127.0.0.1:11434/health/agents` — shows the smoke-1 session GONE.
    5. Update `.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` frontmatter `status` to `verified` IF AND ONLY IF all the above pass; leave it as-is otherwise and surface the failing item.
    6. Append a short `## Re-verification 2026-NN-NN` section to 05-VERIFICATION.md noting: which subtests passed, the curl reproduction outcomes, and the fix commit hash. DO NOT delete the original `gaps:` block — it stays as historical record.
    7. Kill the gateway process; verify no orphaned kiro-cli children with `pgrep kiro-cli` (expect zero matches).

    On any failure, STOP — do not proceed to Plan 05-05. The remaining gaps are recoverable; bring them back to a fresh planning loop with `/gsd-plan-phase 5 --gaps` if Task 4's fix did not actually land the integration green.
  </action>
  <verify>
    <automated>go test ./... -count=1 -race -timeout=180s && OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions' ./tests/e2e/... -count=1 -timeout=180s</automated>
  </verify>
  <acceptance_criteria>
    - Full unit suite green (all 12 packages, race-clean).
    - Full e2e suite green: SessionIDAffinity PASS, IdleReap_RealTime PASS, DeleteSession_OK PASS, DeleteSession_CancelsInFlight PASS (with strengthened ≥1-chunk assertion), DeleteSession_Unknown PASS (no regression), HealthAgentsShape PASS, WarmupBeforeListen PASS, SaturationBlocking PASS, DeadSlotLazyRespawn PASS, AllDeadRespawnFails SKIP.
    - Smoke curl reproducibly returns HTTP 200 for stateful requests with the same sid; DELETE returns 200 with `{"deleted":"smoke-1"}`.
    - No orphaned kiro-cli children after shutdown (`pgrep kiro-cli` exits 1 / matches none).
    - 05-VERIFICATION.md has a re-verification section dated to today recording closure of SC3 + SC4.
  </acceptance_criteria>
  <done>Phase 5 SC3 + SC4 are FULLY SATISFIED end-to-end against live kiro-cli. SESS-01 + SESS-02 + SESS-03 lift from PARTIAL → SATISFIED. Plan 05-05 (perf report) is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway ↔ kiro-cli stdio | JSON-RPC frames flow over a child stdio pipe. The transcript shim in Task 1 sits IN this boundary and writes the captured frames to a local file. |
| gateway HTTP ↔ caller | curl / e2e tests issue HTTP requests; no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information disclosure | Wire transcripts (`.jsonl` files) | accept | Transcripts contain prompt content ("hi") — synthetic input only; no PII; no credentials (AUTH_TOKEN not in prompt body); files live under `.planning/phases/...` which is already a project-internal directory. Operator who runs the diagnostic owns the file. |
| T-05-04-02 | Tampering | `tools/kiro-shim/main.go` | mitigate | Shim source is committed and reviewable; its only job is tee-style forwarding. arch-lint enforces it imports no internal packages so it cannot inject into the runtime. |
| T-05-04-03 | Denial of service | Fix to Entry.Prompt | mitigate | Per-request NewSession adds 1 RPC round-trip per stateful request (~10ms latency overhead). Acceptable per CLAUDE.md non-functional bound (p99(Go) ≤ p99(Node) + 10%). Quantified in Plan 05-05 perf report. |
| T-05-04-04 | Repudiation | Source comment citing 05-04-WIRE-DIFF.md | accept | The artifact is committed to the repo; the comment-to-artifact link is auditable via git history. |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
After all tasks complete, the gap-closure plan is verified by:

1. **Wire diff exists and is reviewable.** `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` contains both transcripts (or refs to the `.jsonl` files), a one-paragraph root cause citing source line numbers, and a remediation plan that matches the actual code change.

2. **Unit tests guard the fix.** `internal/session/entry_acp_test.go` exercises the corrected protocol sequence via an ACP fake; running `go test ./internal/session/... -count=1 -race -run TestEntry_Prompt` exits 0 against the fix and would fail against the pre-fix code.

3. **Race-detector clean across full suite.** `go test ./... -count=1 -race -timeout=180s` exits 0; no new findings from `go vet ./...` or `golangci-lint run`.

4. **Live integration green.** `OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions' ./tests/e2e/... -count=1 -timeout=180s` exits 0 with the previously-failing trio (SessionIDAffinity / IdleReap_RealTime / DeleteSession_OK) now passing AND DeleteSession_CancelsInFlight passing with the strengthened ≥1-chunk assertion.

5. **No regression of working paths.** Stateless `/api/chat`, `/health`, `/health/agents`, `/api/version`, all OpenAI/Anthropic chat endpoints, and the pool warmup invariant (WarmupBeforeListen) continue to behave exactly as before.

6. **VERIFICATION.md re-verification stamp.** A re-verification section dated to fix-day records the closure of gap 1 + gap 2 and the commit hashes of Tasks 4-6.
</verification>

<success_criteria>
Gap closure complete when:

- [ ] `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-POOL.jsonl` exists (Task 1)
- [ ] `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-SESSION.jsonl` exists with -32603 captured (Task 2)
- [ ] `.planning/phases/05-pool-stateful-sessions/05-04-WIRE-DIFF.md` has Working Pool Path, Broken Session Path, Root Cause, and Remediation Plan sections (Tasks 1-3)
- [ ] `internal/session/entry_acp.go` fix applied with source comment citing 05-04-WIRE-DIFF.md (Task 4)
- [ ] `internal/session/entry_acp_test.go` exists with ≥3 new tests guarding the fix (Task 4)
- [ ] `tests/e2e/pool_sessions_e2e_test.go::DeleteSession_CancelsInFlight` asserts ≥1 chunk before DELETE (Task 5)
- [ ] `go test ./... -count=1 -race -timeout=180s` exits 0
- [ ] `OTTO_E2E=1 go test -tags=e2e -run 'TestE2E_PoolSessions' ./tests/e2e/... -count=1 -timeout=180s` exits 0 across the full subtest set
- [ ] Smoke curl: stateful `/api/chat -H 'X-Session-Id: smoke-1'` returns HTTP 200 with assistant body
- [ ] `/v1/sessions/smoke-1` DELETE returns 200 `{"deleted":"smoke-1"}` end-to-end
- [ ] 05-VERIFICATION.md updated with re-verification stamp
- [ ] No orphaned kiro-cli children after shutdown
</success_criteria>

<output>
Create `.planning/phases/05-pool-stateful-sessions/05-04-SUMMARY.md` when done with:
- Tasks executed (count, duration)
- Root cause (one paragraph, citing WIRE-DIFF.md)
- Files changed (paths + 1-line rationale each)
- Test deltas (new tests added; existing tests modified)
- Before/after curl evidence (HTTP status + truncated body)
- Re-verification result (e2e subtest pass/fail summary)
- Requirements lifted: SESS-01 PARTIAL → SATISFIED; SESS-02 PARTIAL → SATISFIED; SESS-03 PARTIAL → SATISFIED
</output>
