---
phase: 05-pool-stateful-sessions
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - internal/server/server.go
  - internal/server/health.go
  - internal/server/agents.go
  - internal/server/agents_test.go
  - internal/server/sessions_delete.go
  - internal/server/sessions_delete_test.go
  - internal/adapter/ollama/handlers.go
  - internal/adapter/ollama/handlers_test.go
  - internal/adapter/openai/handlers.go
  - internal/adapter/openai/handlers_test.go
  - internal/adapter/anthropic/handlers.go
  - internal/adapter/anthropic/handlers_test.go
  - cmd/otto-gateway/main.go
  - tests/e2e/pool_sessions_e2e_test.go
  - tests/e2e/reports/PHASE5-PERF.md
autonomous: false
requirements:
  - OBSV-02
  - SESS-01
  - SESS-03
  - POOL-04
requirements_addressed:
  - OBSV-02
  - SESS-01
  - SESS-03
  - POOL-04
tags:
  - server
  - health-agents
  - delete-session
  - surface-routing
  - main-wiring

must_haves:
  truths:
    - "GET /health/agents returns 200 with body containing {pool: {size, alive, busy, slots: []}, sessions: []} — D-14, D-15, D-16."
    - "/health/agents is auth-exempt — added to the outer-router exempt list in server.NewFromConfig alongside /health (D-18)."
    - "Per-slot row exactly matches D-15: {label, alive, busy, current_session_id (nullable)}."
    - "Per-session row exactly matches D-16: {id, alive, busy, last_used (RFC 3339), model (nullable)}."
    - "Full session ids verbatim — no redaction (D-17)."
    - "DELETE /v1/sessions/:id is auth-PROTECTED (mounted on the /v1 prefix's auth-wrapped sub-router); returns 200 {deleted: id} on success or 404 on unknown sid (D-08)."
    - "DELETE handler cancels in-flight prompt then closes the subprocess (delegates to registry.Delete which already does this per Codex M-3 in plan 05-02)."
    - "Each surface handler (ollama, openai, anthropic) reads X-Session-Id; empty → engine.Run(pool); non-empty → registry.Get + Mu.Lock + engine.Run(entry) + defer Mu.Unlock + defer entry.MarkUsed (D-05, D-07, D-11)."
    - "ErrSessionMaxExceeded from registry.Get is rendered as 503 in each surface adapter's native error envelope (D-06)."
    - "cmd/otto-gateway/main.go constructs the registry alongside the pool; reaper started via Registry.Start; shutdown order is registry.Close before pool.Close (Pitfall 5 bounded-time)."
    - "Phase 4 D-06 watchdog applies to stateful sessions — disconnect-ctx cancel still fires session/cancel because both paths go through engine.Run."
  artifacts:
    - path: "internal/server/agents.go"
      provides: "AgentsResponse + agentsHandler — D-14/D-15/D-16 JSON wire shape"
      contains: "func (s *Server) agentsHandler"
    - path: "internal/server/sessions_delete.go"
      provides: "SessionsRouter (RouteRegistrar) + handleDelete for DELETE /v1/sessions/:id"
      contains: "DELETE"
    - path: "internal/server/server.go"
      provides: "RegistryStatsSource interface + cfg.Registry wiring + /health/agents exempt route"
      contains: "RegistryStatsSource"
    - path: "internal/adapter/ollama/handlers.go"
      provides: "X-Session-Id branch in handleChat + handleGenerate"
      contains: "X-Session-Id"
    - path: "internal/adapter/openai/handlers.go"
      provides: "X-Session-Id branch in handleChatCompletions + handleCompletions"
      contains: "X-Session-Id"
    - path: "internal/adapter/anthropic/handlers.go"
      provides: "X-Session-Id branch in handleMessages"
      contains: "X-Session-Id"
    - path: "cmd/otto-gateway/main.go"
      provides: "registry construction + Start + shutdown ordering + registryStatsAdapter + SessionsRouter mount on /v1"
      contains: "session.New"
  key_links:
    - from: "internal/server/server.go:NewFromConfig"
      to: "internal/server/agents.go:agentsHandler"
      via: "s.router.Get(\"/health/agents\", s.agentsHandler) outside the auth.Route block"
      pattern: "/health/agents"
    - from: "internal/server/sessions_delete.go:SessionsRouter.RegisterRoutes"
      to: "internal/session/Registry.Delete"
      via: "registry.Delete(sid) inside DELETE /sessions/{id} handler"
      pattern: "Registry\\.Delete|registry\\.Delete"
    - from: "internal/adapter/{ollama,openai,anthropic}/handlers.go"
      to: "internal/session/Registry.Get"
      via: "registry.Get(ctx, sid, cwd) when X-Session-Id header is non-empty"
      pattern: "r\\.Header\\.Get\\(\"X-Session-Id\"\\)"
    - from: "cmd/otto-gateway/main.go:newApp"
      to: "internal/session/Registry.Close"
      via: "cleanup closure: registry.Close BEFORE pool.Close"
      pattern: "registry\\.Close|a\\.registry\\.Close"
---

<objective>
Wire the Slice-B registry (plan 05-02) and Slice-A pool detail (plan 05-01) into the gateway's HTTP surface and runtime. This is Slice C: the user-visible endpoints (`GET /health/agents`, `DELETE /v1/sessions/:id`) plus the surface-handler routing that makes `X-Session-Id` route stateful requests to the registry instead of the warm pool. Closes OBSV-02 (the auth-exempt detail endpoint), the HTTP-route side of SESS-03 (DELETE), and SESS-01's surface-handler integration. Also adds the cmd/otto-gateway/main.go wiring (registry construction, reaper start, ordered shutdown).

Purpose: After this plan, an operator can `curl /health/agents`, watch a session appear after the first chat with X-Session-Id, watch it reap after 30 min idle (or DELETE it on demand), and see the dead-slot lazy re-spawn from plan 05-01 reflected in the per-slot detail. Phase 5 acceptance criteria SC1..SC5 all become testable end-to-end.

Output: `agents.go` + `sessions_delete.go` + their tests in internal/server; X-Session-Id branches in all three adapter handlers; cmd/otto-gateway/main.go wiring; the RegistryStatsSource interface mirror of PoolStatsSource.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pool-stateful-sessions/05-CONTEXT.md
@.planning/phases/05-pool-stateful-sessions/05-RESEARCH.md
@.planning/phases/05-pool-stateful-sessions/05-PATTERNS.md
@.planning/phases/05-pool-stateful-sessions/05-VALIDATION.md
@.planning/phases/05-pool-stateful-sessions/05-01-PLAN.md
@.planning/phases/05-pool-stateful-sessions/05-02-PLAN.md
@.planning/phases/04-streaming/04-CONTEXT.md
@.planning/phases/02-ollama-end-to-end/02-CONTEXT.md
@./CLAUDE.md

# Source files this plan modifies (executor MUST read before editing)
@internal/server/server.go
@internal/server/health.go
@internal/server/middleware.go
@internal/adapter/ollama/handlers.go
@internal/adapter/openai/handlers.go
@internal/adapter/anthropic/handlers.go
@cmd/otto-gateway/main.go

# Plan 05-01 + 05-02 contracts the executor depends on
@internal/pool/detail.go
@internal/session/registry.go
@internal/session/stats.go
@internal/session/entry_acp.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server-side wire — RegistryStatsSource interface + agentsHandler + /health/agents exempt route + /health populates Sessions.Active</name>
  <files>internal/server/server.go, internal/server/health.go, internal/server/agents.go, internal/server/agents_test.go</files>
  <read_first>
    - internal/server/server.go (full — particularly PoolStatsSource at lines 25-31 and exempt routes at lines 164-172 + Config struct at lines 75-87)
    - internal/server/health.go (full — healthHandler at lines 57-75 is the literal template for agentsHandler; HealthResponse + PoolStats + SessionStats + EmbeddingStats type declarations)
    - internal/server/middleware.go (full — LoggerFromCtx at lines 50-55 is reused inside agentsHandler)
    - internal/pool/detail.go (plan 05-01 producer side — AgentSlot type + Pool.Detail() method)
    - internal/session/stats.go (plan 05-02 producer side — SessionDetail + Registry.Detail() + Stats())
    - .planning/phases/05-pool-stateful-sessions/05-PATTERNS.md (§"internal/server/server.go (MODIFY)" + §"internal/server/health.go (MODIFY)" + §"internal/server/agents.go (NEW)" — verbatim shapes including JSON-tag discipline; §"Pattern 7: /health/agents handler")
    - .planning/phases/05-pool-stateful-sessions/05-RESEARCH.md (§"Code Examples Block 4: agentsHandler rendering" — full file body; §"Pattern 7" — wire shape recommendation; §"Architectural Responsibility Map" — server owns rendering, pool+session own data)
    - .planning/phases/05-pool-stateful-sessions/05-CONTEXT.md (D-14/D-15/D-16/D-17/D-18 verbatim)
  </read_first>
  <behavior>
    - Test 1: TestAgentsHandler_EmptyPoolAndRegistry — GET /health/agents with nil pool + nil registry returns 200 with body `{"pool":{"size":0,"alive":0,"busy":0,"slots":null},"sessions":null}` (or `[]` if normalized — pick one and document; pool.size = 0 reflects no pool wired).
    - Test 2: TestAgentsHandler_PopulatedPool — wire fake pool source returning 4 slots; assert response.Pool.Size == 4, response.Pool.Slots has 4 rows each with label "slot-N", alive true, busy false, current_session_id null.
    - Test 3: TestAgentsHandler_PopulatedSessions — wire fake registry source returning 2 sessions; assert response.Sessions has 2 rows with correct id/last_used/model.
    - Test 4: TestAgentsHandler_RowShapeMatchesD15D16 — assert via JSON decode that response has the exact key names: pool.{size,alive,busy,slots}, slot.{label,alive,busy,current_session_id}, session.{id,alive,busy,last_used,model}.
    - Test 5: TestAgentsHandler_NoAuthRequired (D-18) — make request WITHOUT bearer token to a NewFromConfig-built server with AuthTokens=["secret"]; assert 200 (auth-exempt).
    - Test 6: TestHealthHandler_PopulatesSessionsActive — wire fake registry source returning Stats{Active: 3}; GET /health response body's sessions.active == 3.
    - Test 7: TestAgentsHandler_LastUsedIsRFC3339 — populate one session with LastUsed = time.Now(); assert response's sessions[0].last_used parses cleanly via time.Parse(time.RFC3339Nano, value) — Go's default time.Time.MarshalJSON emits RFC 3339 with sub-second precision.
  </behavior>
  <action>
    In `internal/server/server.go` per 05-PATTERNS.md §"internal/server/server.go (MODIFY)":
    - Add `type RegistryStatsSource interface { Stats() SessionStats; Detail() []AgentSession }` next to the existing PoolStatsSource at lines 25-31. (`SessionStats` already exists in health.go from Phase 1; `AgentSession` is defined in agents.go below.)
    - Add `PoolDetail PoolDetailSource` and `Registry RegistryStatsSource` fields to `Config` struct (lines 75-87). PoolDetailSource is a new interface `interface { Detail() []AgentSlot }` declared in agents.go (since it returns AgentSlot — the type lives in this package or is mirrored — see below). Two-interface choice per 05-RESEARCH §"Standard Stack alternatives Decision: two interfaces" (mirrors the Phase 2 pattern).
    - Add `poolDetail PoolDetailSource` and `registry RegistryStatsSource` fields to `Server` struct (lines 89-99).
    - In `NewFromConfig` (lines 150-212), copy `cfg.PoolDetail` and `cfg.Registry` into `s.poolDetail` and `s.registry`.
    - In `NewFromConfig`'s exempt-route block (lines 164-172), add: `s.router.Get("/health/agents", s.agentsHandler)` AFTER `s.router.Get("/health", s.healthHandler)`. This is D-18 — auth-exempt.

    Create `internal/server/agents.go` per 05-PATTERNS.md §"internal/server/agents.go (NEW)" + 05-RESEARCH §"Code Examples Block 4" verbatim:
    - Type `AgentsResponse struct { Pool AgentsPool `json:"pool"`; Sessions []AgentSession `json:"sessions"` }`.
    - Type `AgentsPool struct { Size int `json:"size"`; Alive int `json:"alive"`; Busy int `json:"busy"`; Slots []AgentSlot `json:"slots"` }`.
    - Type `AgentSlot struct { Label string `json:"label"`; Alive bool `json:"alive"`; Busy bool `json:"busy"`; CurrentSessionID *string `json:"current_session_id"` }` — D-15 row shape verbatim. (Note: plan 05-01 defined a `pool.AgentSlot` with the same JSON shape. To avoid cross-package coupling at the type level — and per 05-PATTERNS.md `PoolDetailSource interface { Detail() []AgentSlot }` — there are two valid choices: (a) `internal/server` re-declares its OWN AgentSlot type and a small `cmd/otto-gateway/main.go` adapter converts pool's AgentSlot → server's AgentSlot field-by-field; (b) `internal/server` defines `AgentSlot` and `internal/pool/detail.go` is updated to return `server.AgentSlot` directly. Choose (a) — keeps the engine boundary discipline ("adapter does the mapping"). Document the choice in the agents.go doc-comment.) — The server-local AgentSlot lives in agents.go.
    - Type `AgentSession struct { ID string `json:"id"`; Alive bool `json:"alive"`; Busy bool `json:"busy"`; LastUsed time.Time `json:"last_used"`; Model *string `json:"model"` }` — D-16 row shape verbatim.
    - Type `PoolDetailSource interface { Detail() []AgentSlot }` — symmetric to RegistryStatsSource but for the pool side.
    - Function `func (s *Server) agentsHandler(w http.ResponseWriter, r *http.Request)`:
        - `resp := AgentsResponse{}`.
        - `if s.poolDetail != nil { slots := s.poolDetail.Detail(); resp.Pool.Slots = slots; resp.Pool.Size = len(slots); for _, sl := range slots { if sl.Alive { resp.Pool.Alive++ }; if sl.Busy { resp.Pool.Busy++ } } }`.
        - `if s.registry != nil { resp.Sessions = s.registry.Detail() }`.
        - Set Content-Type, WriteHeader(200), encode JSON. Mirror the healthHandler error-logging pattern via LoggerFromCtx at lines 73 of health.go: `if err := json.NewEncoder(w).Encode(resp); err != nil { LoggerFromCtx(r.Context(), s.logger).Error("agents encode", "err", err) }`.
    - Add a small helper `stringPtr(s string) *string` if not already in this package — needed by the cmd/otto-gateway adapter wiring in Task 4.

    In `internal/server/health.go` per 05-PATTERNS.md §"internal/server/health.go (MODIFY)":
    - In `healthHandler` (lines 57-75), add nil-safe registry-stats lookup: `var ss SessionStats; if s.registry != nil { ss = s.registry.Stats() }`. Mirror the existing `if s.pool != nil { ps = s.pool.Stats() }` pattern.
    - Populate `resp.Sessions = ss` in the HealthResponse construction. (`SessionStats.Active` field already exists from Phase 1 — populated from registry.Stats().Active.)
    - Note: the `RegistryStatsSource.Stats()` method returns `server.SessionStats` (Phase 1 type, already in health.go). cmd/otto-gateway/main.go's adapter in Task 4 maps `session.Stats{Active: N}` → `server.SessionStats{Active: N}`.

    Create `internal/server/agents_test.go` with the seven tests in `<behavior>`. Use the existing test-helper patterns from `internal/server/server_test.go` (chi.NewRouter + httptest.NewRequest + httptest.ResponseRecorder + JSON decode). Build a `fakePoolDetailSource` and `fakeRegistryStatsSource` struct that satisfy the interfaces; inject via Config when calling NewFromConfig.

    For Test 5 (TestAgentsHandler_NoAuthRequired): build NewFromConfig with `AuthTokens: []string{"secret"}` AND `Surfaces: []SurfaceMount{...one mount at /v1...}`. Make the request with NO `Authorization` header. Assert 200 (exempt route).

    Verify in agents_test.go that JSON encoding round-trips cleanly — decode the response and assert specific field values; do not just string-match the encoded JSON.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestAgentsHandler_|TestHealthHandler_PopulatesSessionsActive' -count=1 -timeout=30s -race</automated>
    <automated>go test ./internal/server/... -count=1 -timeout=60s -race</automated>
    <automated>go vet ./internal/server/...</automated>
    <automated>grep -E 'func \(s \*Server\) agentsHandler' internal/server/agents.go</automated>
    <automated>grep -E 'type AgentsResponse struct' internal/server/agents.go</automated>
    <automated>grep -E 'type AgentSlot struct' internal/server/agents.go</automated>
    <automated>grep -E 'type AgentSession struct' internal/server/agents.go</automated>
    <automated>grep -E 'type RegistryStatsSource interface' internal/server/server.go</automated>
    <automated>grep -E 's\.router\.Get\("/health/agents", s\.agentsHandler\)' internal/server/server.go</automated>
    <automated>grep -E '`json:"current_session_id"`' internal/server/agents.go</automated>
    <automated>grep -E '`json:"last_used"`' internal/server/agents.go</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/server/server.go contains "type RegistryStatsSource interface"`
    - Source: `internal/server/server.go contains "s.router.Get(\"/health/agents\", s.agentsHandler)"` inside NewFromConfig BEFORE the per-prefix Route block (i.e., on the outer router, auth-exempt)
    - Source: `internal/server/agents.go contains "type AgentsResponse struct"`, `"type AgentsPool struct"`, `"type AgentSlot struct"`, `"type AgentSession struct"`, `"type PoolDetailSource interface"`, `"func (s *Server) agentsHandler"`
    - Source: AgentSlot has four JSON tags `label`, `alive`, `busy`, `current_session_id`
    - Source: AgentSession has five JSON tags `id`, `alive`, `busy`, `last_used`, `model`
    - Source: `internal/server/health.go` healthHandler populates `Sessions` via `s.registry.Stats()` with nil-safety
    - Test: `go test -run TestAgentsHandler_NoAuthRequired ./internal/server/... -count=1 -timeout=30s -race exits 0` (D-18 enforced)
    - Test: `go test -run TestAgentsHandler_RowShapeMatchesD15D16 ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestHealthHandler_PopulatesSessionsActive ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Behavior: GET /health/agents returns 200 with body containing fields `pool` (object) and `sessions` (array)
    - Behavior: every existing /health test continues to pass (no regression on HealthResponse shape — Sessions.Active is the only added field-population, the field already existed)
  </acceptance_criteria>
  <done>
    /health/agents serves D-14/D-15/D-16 wire shape; /health populates sessions.active from the registry; both are auth-exempt; the RegistryStatsSource and PoolDetailSource interfaces are declared; all tests pass with -race.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DELETE /v1/sessions/:id via SessionsRouter (RouteRegistrar) — auth-protected, 200/404 semantics, in-flight cancel</name>
  <files>internal/server/sessions_delete.go, internal/server/sessions_delete_test.go</files>
  <read_first>
    - internal/server/server.go (RouteRegistrar interface at lines 38-40; SurfaceMount type at lines 46-52; the per-prefix Route block at lines 191-209 is where each surface's RegisterRoutes is called inside the auth-wrapped sub-router)
    - internal/adapter/ollama/handlers.go (lines 304-319 — writeError pattern; this is the canonical error-writing helper to mirror; alternatively define a small server-local writeError in sessions_delete.go)
    - internal/adapter/anthropic/handlers.go (lines 35-122 — handler style + error envelope rendering as DOCUMENTATION; sessions_delete.go uses a simpler generic JSON error since the DELETE response is operator-only, not surface-shaped)
    - internal/session/registry.go (Registry.Delete signature from plan 05-02: returns `error`; sentinel `ErrSessionNotFound`)
    - .planning/phases/05-pool-stateful-sessions/05-PATTERNS.md (§"internal/server/sessions_delete.go (NEW)" — verbatim shape; SessionDeleter interface — narrow surface for testability)
    - .planning/phases/05-pool-stateful-sessions/05-RESEARCH.md (§"Pattern 5: DELETE /v1/sessions/:id" — full body; §"Common Pitfalls Pitfall 1: DELETE-during-stream race" — registry.Delete already handles this via Codex M-3 in plan 05-02)
    - .planning/phases/05-pool-stateful-sessions/05-CONTEXT.md (D-08 verbatim: 200 {deleted: id} on success, 404 on unknown sid)
  </read_first>
  <behavior>
    - Test 1: TestSessionsRouter_Delete_KnownSid_Returns200WithDeleted — wire fake SessionDeleter that returns nil; DELETE /v1/sessions/abc-123 returns 200 with body `{"deleted":"abc-123"}` (assert via JSON decode).
    - Test 2: TestSessionsRouter_Delete_UnknownSid_Returns404 — fake SessionDeleter returns session.ErrSessionNotFound; assert response status == 404 with error body.
    - Test 3: TestSessionsRouter_Delete_InternalError_Returns500 — fake SessionDeleter returns a generic error; assert 500 with error body; assert logger.Error was called with sid and err.
    - Test 4: TestSessionsRouter_Delete_MissingSid_Returns400 — chi routes DELETE /v1/sessions/{id} so an empty {id} returns 404 from chi BEFORE our handler runs. Confirm behavior matches; document. (If chi handles this entirely, this test asserts a chi-level 404; if our handler can receive empty {id}, assert our 400.) — Use the actual chi behavior; do not contort the handler.
    - Test 5: TestSessionsRouter_Delete_RequiresAuth — full NewFromConfig server with AuthTokens=["secret"] and the SessionsRouter mounted at /v1; DELETE WITHOUT bearer token returns 401 (the auth.Bearer middleware in the /v1 prefix Route block guards this route — proves it is NOT auth-exempt).
    - Test 6: TestSessionsRouter_Delete_AcceptsValidAuth — same setup; DELETE WITH `Authorization: Bearer secret` returns 200 (sid mocked).
    - Test 7: TestSessionsRouter_Delete_CallsRegistryDelete — fake SessionDeleter records calls; DELETE /v1/sessions/sess-X causes deleter to be called with "sess-X" exactly once.
  </behavior>
  <action>
    Create `internal/server/sessions_delete.go` per 05-PATTERNS.md §"internal/server/sessions_delete.go (NEW)" + 05-RESEARCH §"Pattern 5":
    - Package `server`.
    - Type `SessionDeleter interface { Delete(sid string) error }` — narrow interface so tests can inject a fake without depending on internal/session.
    - Type `SessionsRouter struct { Registry SessionDeleter; Logger *slog.Logger }` — satisfies server.RouteRegistrar via RegisterRoutes.
    - Method `func (sr *SessionsRouter) RegisterRoutes(r chi.Router)`: `r.Delete("/sessions/{id}", sr.handleDelete)`. Per Pattern 5 + the server's existing chi pattern. This is registered inside the auth-wrapped /v1 prefix Route block (wiring lives in Task 4 / cmd/otto-gateway/main.go).
    - Method `func (sr *SessionsRouter) handleDelete(w http.ResponseWriter, r *http.Request)`:
        - `sid := chi.URLParam(r, "id")`. If `sid == ""` write 400 with generic error body (in practice chi routes this case to a separate 404; defensive guard).
        - `if err := sr.Registry.Delete(sid); err != nil { ... }`:
            - `errors.Is(err, session.ErrSessionNotFound)` → write 404 with `{"error":"unknown session"}`.
            - Otherwise: log via `LoggerFromCtx(r.Context(), sr.Logger).Error("session: delete failed", "sid", sid, "err", err)`; write 500 with `{"error":"delete failed"}`.
            - Return early.
        - Success path: `w.Header().Set("Content-Type", "application/json"); w.WriteHeader(http.StatusOK); _ = json.NewEncoder(w).Encode(map[string]string{"deleted": sid})`.

    Add a small writeError helper LOCAL to internal/server (or, if preferred, copy the existing pattern from internal/adapter/ollama/handlers.go:315-319 — both work; the simpler path is a local helper in agents.go or sessions_delete.go):
    - `func writeJSONError(w http.ResponseWriter, status int, msg string) { w.Header().Set("Content-Type", "application/json"); w.WriteHeader(status); _ = json.NewEncoder(w).Encode(map[string]string{"error": msg}) }`. Place in a small server-local file or inline at the top of sessions_delete.go.

    Create `internal/server/sessions_delete_test.go` with the seven tests in `<behavior>`:
    - Test fixture: `type fakeSessionDeleter struct { mu sync.Mutex; calls []string; returnErr error }` with a `Delete(sid string) error` method that records the sid and returns returnErr.
    - For Tests 5+6 (auth gating), build a full NewFromConfig server with `Surfaces: []SurfaceMount{{Prefix: "/v1", Router: &SessionsRouter{Registry: fakeDeleter, Logger: testutil.Logger(t)}}}` so the auth middleware runs. For Tests 1-4 + 7, construct a chi router directly with just `r.Route("/v1", func(r chi.Router) { (&SessionsRouter{Registry: fakeDeleter, Logger: testLogger}).RegisterRoutes(r) })` — skips auth for unit-test purity.

    Do NOT change the existing RouteRegistrar interface, SurfaceMount type, or NewFromConfig grouping logic. The SessionsRouter satisfies the existing pattern verbatim.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run 'TestSessionsRouter_' -count=1 -timeout=30s -race</automated>
    <automated>go test ./internal/server/... -count=1 -timeout=60s -race</automated>
    <automated>go vet ./internal/server/...</automated>
    <automated>grep -E 'type SessionsRouter struct' internal/server/sessions_delete.go</automated>
    <automated>grep -E 'r\.Delete\("/sessions/\{id\}"' internal/server/sessions_delete.go</automated>
    <automated>grep -E 'errors\.Is\(err, session\.ErrSessionNotFound\)' internal/server/sessions_delete.go</automated>
    <automated>grep -E '"deleted": sid' internal/server/sessions_delete.go</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/server/sessions_delete.go contains "type SessionsRouter struct"`
    - Source: `internal/server/sessions_delete.go contains "type SessionDeleter interface"` with method `Delete(sid string) error`
    - Source: `internal/server/sessions_delete.go contains "r.Delete(\"/sessions/{id}\""` (chi route registration)
    - Source: `internal/server/sessions_delete.go contains "errors.Is(err, session.ErrSessionNotFound)"`
    - Source: `internal/server/sessions_delete.go contains "{\"deleted\":` or constructs the deleted-id JSON via `map[string]string{"deleted": sid}`
    - Test: `go test -run TestSessionsRouter_Delete_KnownSid_Returns200WithDeleted ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestSessionsRouter_Delete_UnknownSid_Returns404 ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestSessionsRouter_Delete_RequiresAuth ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestSessionsRouter_Delete_AcceptsValidAuth ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestSessionsRouter_Delete_CallsRegistryDelete ./internal/server/... -count=1 -timeout=30s -race exits 0`
    - Behavior: DELETE /v1/sessions/{unknown_id} returns 404
    - Behavior: DELETE /v1/sessions/{known_id} returns 200 with body containing `"deleted"` and the sid value
    - Behavior: DELETE without bearer token (auth required) returns 401
  </acceptance_criteria>
  <done>
    SessionsRouter is a RouteRegistrar that mounts DELETE /v1/sessions/{id}; behind auth; returns 200/404/500 with the D-08 wire shape; calls into the SessionDeleter interface (registry.Delete) per the Codex M-3 discipline already implemented in plan 05-02.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: X-Session-Id branch in all three surface handlers (Ollama + OpenAI + Anthropic) — engine.Run dispatch with per-entry mutex + MarkUsed defer</name>
  <files>internal/adapter/ollama/handlers.go, internal/adapter/ollama/handlers_test.go, internal/adapter/openai/handlers.go, internal/adapter/openai/handlers_test.go, internal/adapter/anthropic/handlers.go, internal/adapter/anthropic/handlers_test.go</files>
  <read_first>
    - internal/adapter/ollama/handlers.go (full — handleChat at lines 24-73, handleGenerate, writeError pattern at 304-319)
    - internal/adapter/openai/handlers.go (full — handleChatCompletions at lines 26-86)
    - internal/adapter/anthropic/handlers.go (full — handleMessages at lines 35-122, error envelope rendering)
    - internal/engine/engine.go (Engine.Run signature + ACPClient interface at lines 34-50 — the entry point both pool and *session.Entry call into)
    - internal/session/registry.go (Registry.Get + ErrSessionMaxExceeded sentinels from plan 05-02)
    - internal/session/entry_acp.go (*Entry satisfies engine.ACPClient — compile-time assertion proves this; Entry.MarkUsed + Mu fields are public)
    - .planning/phases/05-pool-stateful-sessions/05-PATTERNS.md (§"Surface handlers — ...handlers.go" — each surface's analog line numbers and verbatim X-Session-Id branch shape; §"engine boundary" anti-pattern: do not add Detail() to engine.ACPClient)
    - .planning/phases/05-pool-stateful-sessions/05-RESEARCH.md (§"Pattern 6: Surface-handler routing on X-Session-Id" — full body; §"Common Pitfalls Pitfall 3" — TTL is a lower bound; §"Code Examples Block 3" — Entry implementing engine.ACPClient)
    - .planning/phases/05-pool-stateful-sessions/05-CONTEXT.md (D-05/D-06/D-07/D-09/D-11 verbatim)
  </read_first>
  <behavior>
    For each of three surfaces (Ollama, OpenAI, Anthropic) implement matching behavior on its chat-style handler(s). Test names below are illustrative; mirror the per-surface naming convention.
    - Test 1: TestOllamaHandleChat_NoXSessionId_RoutesToPool — request with empty X-Session-Id header; assert pool's NewSession called once; registry not consulted.
    - Test 2: TestOllamaHandleChat_WithXSessionId_RoutesToRegistry — request with X-Session-Id="sid-X"; assert registry.Get called with "sid-X"; pool's NewSession NOT called; engine.Run called with the entry returned by Get.
    - Test 3: TestOllamaHandleChat_SessionMaxExceeded_Returns503 — fake registry returns ErrSessionMaxExceeded; assert 503 response with Ollama-shaped error body.
    - Test 4: TestOllamaHandleChat_TakesEntryMutex — after handler returns, entry.MarkUsed has been called once and entry.Mu is unlocked (assert via TryLock).
    - Test 5: TestOllamaHandleChat_MarkUsedFiresAfterStreamCompletes (D-11) — instrument fake entry to record the time MarkUsed is called; assert MarkUsed timestamp > stream-Result timestamp.
    - Test 6: TestOllamaHandleChat_GenericRegistryError_Returns500 — registry.Get returns a non-ErrSessionMaxExceeded error; assert 500.
    Repeat the test shape (5-6 tests per surface) for OpenAI (handleChatCompletions) and Anthropic (handleMessages). For OpenAI the error envelope is `{"error":{...}}`; for Anthropic the error envelope is `{"type":"error","error":{"type":"...","message":"..."}}` (use the existing render-error helpers in each adapter).
  </behavior>
  <action>
    Plan-level decision (LOCKED per checker revision — supersedes the earlier "SessionHandle" alternative):

    Each adapter's Config struct gains a `Registry *session.Registry` field directly. Adapters import `internal/session` at the type level. This is permitted by .go-arch-lint.yml (the engine-boundary rule applies to `internal/engine`, not `internal/session`). No SessionHandle release-closure pattern, no narrow per-adapter SessionLookup interface — the per-adapter `Registry *session.Registry` field is the single, locked routing mechanism.

    Deferred: SessionHandle release-closure pattern — not needed because `Entry.Mu.Lock()` paired with `defer entry.Mu.Unlock()` and `defer entry.MarkUsed()` in the surface handler already provides the required serialization. The lifecycle is short enough and the handler-local defer pattern is clearer than a release-closure indirection.

    For each adapter (`internal/adapter/{ollama,openai,anthropic}`):
    - Add `Registry *session.Registry` field to the adapter's Config struct (alongside the existing Engine field). This is the locked routing mechanism — DO NOT introduce a SessionLookup interface or SessionHandle wrapper.
    - Add `KiroCWD string` field if not already present (the adapter must pass cwd to registry.Get; lookup from cfg.KiroCWD; default empty string).
    - Add `EngineForACP func(client engine.ACPClient) *engine.Engine` field to the adapter's Config struct alongside the existing Engine field. This is the LOCKED engine-integration seam (see "Engine integration mechanics" below). Wired unconditionally by Task 4 in cmd/otto-gateway/main.go regardless of whether the request takes the pool path or the registry path.

    In each handler that handles chat-style traffic (`handleChat` + `handleGenerate` for Ollama; `handleChatCompletions` + `handleCompletions` for OpenAI; `handleMessages` for Anthropic), insert the X-Session-Id branch per 05-RESEARCH §"Pattern 6" + 05-PATTERNS.md §"Pattern — Anthropic stream branch":

    The branch lives AFTER request decoding and BEFORE the existing `runHandle, err := engine.Run(ctx, req)` call. Pseudocode:
    1. `sid := r.Header.Get("X-Session-Id")`.
    2. If `sid == ""` OR `a.cfg.Registry == nil`: existing pool path unchanged. Use the existing engine (which wraps the pool).
    3. Else: `entry, err := a.cfg.Registry.Get(r.Context(), sid, a.cfg.KiroCWD)`.
        - If `errors.Is(err, session.ErrSessionMaxExceeded)`: render 503 in the adapter's native error envelope (Ollama: writeError pattern; OpenAI: write OpenAI `{"error":{...}}` envelope; Anthropic: write `{"type":"error","error":{"type":"overloaded_error","message":"session capacity exceeded"}}` and set status 503).
        - If other error: log + render 500 in native error envelope.
    4. `entry.Mu.Lock()`.
    5. `defer entry.Mu.Unlock()`.
    6. `defer entry.MarkUsed()` — D-11: MUST come AFTER Mu.Unlock in defer order (deferred functions run LIFO, so this defer registers FIRST = runs LAST; but actually D-11 says MarkUsed updates LastUsed after the stream's Result returns. Since both defers fire after the handler returns, and the stream's Result already returned by then, both orderings are correct. To make D-11's "AFTER Result returns" property crystal clear in code, place `defer entry.MarkUsed()` AFTER `defer entry.Mu.Unlock()` so it runs FIRST in the defer stack — closer to when Result returned. The behavior is identical because Mu is the entry's mutex, not anything Result interacts with).
    7. Build the per-request engine via the LOCKED factory closure: `eng := a.cfg.EngineForACP(entry)` (registry path) or `eng := a.cfg.EngineForACP(a.cfg.Pool)` (pool path). Both branches go through the same `EngineForACP` factory field on the adapter's Config — wired by Task 4. Then call `eng.Run(ctx, req)` to get the RunHandle. Do NOT modify `internal/engine/engine.go` — the existing `Engine.Run` + `ACPClient` interface is sufficient; the factory closure is the integration seam.
    8. Continue down the existing engine.Run / runSSEEmitter / writeNonStream path UNCHANGED. The watchdog from Phase 4 D-06 fires session/cancel on ctx termination — works the same for stateful sessions because both paths go through engine.Run.

    Engine integration mechanics (LOCKED — supersedes the earlier "Alternative: extend engine.Run to take ACPClient as a parameter" hedge): each adapter's Config has a `EngineForACP func(client engine.ACPClient) *engine.Engine` factory field. The adapter calls `eng := a.cfg.EngineForACP(client)` before `eng.Run(...)`. cmd/otto-gateway/main.go (Task 4) wires `EngineForACP` to a closure that calls `engine.New(engine.Config{ACP: client, ...})` per request — the engine constructor is cheap (no goroutines spawned until Run). This eliminates the need for any RunWithACP variant on engine.Engine and keeps `internal/engine/engine.go` UNCHANGED in Phase 5.

    Each surface's handlers_test.go: add the 5-6 tests listed in `<behavior>` per surface. Use httptest + a small fakeSessionRegistry struct that records `Get(ctx, sid, cwd)` calls and returns a pre-built `*session.Entry`. Construct the Entry via the LOCKED canonical seam from plan 05-02 Task 0: `session.NewEntryForTest(client, sid)` — do NOT stand up a real `session.Registry` + fakeClientFactory just to obtain an Entry. The fakeSessionRegistry implementation satisfies `*session.Registry`-compatible call sites by exposing the same `Get(ctx, sid, cwd) (*session.Entry, error)` method shape. For Test 4's TryLock assertion on Entry.Mu — Mu is exported, so the test directly TryLocks the mutex after the handler returns.

    Do NOT change any other adapter behavior (decode/wire/render). Do NOT add Detail() to engine.ACPClient (anti-pattern per 05-RESEARCH §Anti-Patterns).

    Check that engine.Run's existing Phase 4 D-06 watchdog (the AfterFunc that fires session/cancel on ctx-done) still applies. Phase 4 D-06 is built into engine.Run itself — it fires for ANY client (pool or *session.Entry). Confirm via reading engine.go line 196 area. No changes needed to engine.go.
  </action>
  <verify>
    <automated>go test ./internal/adapter/ollama/... -run 'TestOllamaHandleChat_|TestOllamaHandleGenerate_' -count=1 -timeout=60s -race</automated>
    <automated>go test ./internal/adapter/openai/... -run 'TestOpenAIHandleChatCompletions_|TestOpenAIHandleCompletions_' -count=1 -timeout=60s -race</automated>
    <automated>go test ./internal/adapter/anthropic/... -run 'TestAnthropicHandleMessages_' -count=1 -timeout=60s -race</automated>
    <automated>go test ./internal/adapter/... -count=1 -timeout=120s -race</automated>
    <automated>go vet ./internal/adapter/...</automated>
    <automated>grep -rn 'r\.Header\.Get("X-Session-Id")' internal/adapter/</automated>
    <automated>grep -rn 'a\.cfg\.Registry\.Get' internal/adapter/ || true</automated>
    <automated>grep -rn 'entry\.Mu\.Lock\(\)' internal/adapter/</automated>
    <automated>grep -rn 'defer entry\.MarkUsed\(\)' internal/adapter/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/adapter/ollama/handlers.go contains "X-Session-Id"`
    - Source: `internal/adapter/openai/handlers.go contains "X-Session-Id"`
    - Source: `internal/adapter/anthropic/handlers.go contains "X-Session-Id"`
    - Source: each surface's handler file contains `entry.Mu.Lock()` and `defer entry.MarkUsed()` patterns
    - Source: each surface's handler file branches on `errors.Is(err, session.ErrSessionMaxExceeded)` and renders 503 in the native error envelope
    - Source: each surface's adapter Config struct contains `EngineForACP func(engine.ACPClient) *engine.Engine` (i.e., `internal/adapter/ollama/config.go` or equivalent contains "EngineForACP func(engine.ACPClient) *engine.Engine"; same for openai and anthropic)
    - Source: `internal/engine/engine.go` is NOT modified by this plan (the factory closure is the integration seam; engine.go stays unchanged in Phase 5)
    - Source: `internal/adapter/ollama/handlers_session_test.go` (or the equivalent test file for at least one surface) contains "session.NewEntryForTest(" — the canonical Entry-construction seam from plan 05-02
    - Test: `go test -run TestOllamaHandleChat_NoXSessionId_RoutesToPool ./internal/adapter/ollama/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestOllamaHandleChat_WithXSessionId_RoutesToRegistry ./internal/adapter/ollama/... -count=1 -timeout=30s -race exits 0`
    - Test: `go test -run TestOllamaHandleChat_SessionMaxExceeded_Returns503 ./internal/adapter/ollama/... -count=1 -timeout=30s -race exits 0`
    - Test: same three test outcomes for OpenAI's TestOpenAIHandleChatCompletions_* and Anthropic's TestAnthropicHandleMessages_*
    - Race: `go test -race ./internal/adapter/... -count=1 -timeout=180s exits 0`
    - Architecture: `.go-arch-lint.yml` still passes — adapters may import internal/session (this is permitted; the rule is "adapters MUST NOT import internal/engine"; internal/session is not engine).
    - No regression: all existing Phase 2/3/3.1/4 adapter tests continue to pass — the X-Session-Id branch is ADDITIVE.
  </acceptance_criteria>
  <done>
    Each surface handler routes stateless requests to the pool (unchanged) and stateful requests (X-Session-Id present) to the registry. Per-entry mutex serializes per-sid; MarkUsed updates LastUsed on response complete. SESSION_MAX overflow renders 503 in each surface's native error envelope. All adapter tests pass with -race.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: cmd/otto-gateway/main.go wiring — Registry construction + Start + adapters + SessionsRouter mount + ordered shutdown</name>
  <files>cmd/otto-gateway/main.go</files>
  <read_first>
    - cmd/otto-gateway/main.go (full — newApp at lines 117-302, cleanup closure at 122-128, poolStatsAdapter at 309-316, the existing surface wiring 287-299, ollamaEngineAdapter / openaiEngineAdapter / anthropicEngineAdapter at lines 327-486)
    - internal/server/server.go (Config fields after Task 1 — Registry RegistryStatsSource + PoolDetail PoolDetailSource; Surfaces []SurfaceMount unchanged)
    - internal/server/agents.go (after Task 1 — AgentSlot / AgentSession types this main.go's adapters must produce)
    - internal/server/sessions_delete.go (after Task 2 — SessionsRouter type and its Registry SessionDeleter field)
    - internal/session/registry.go (after plan 05-02 — Registry exposes Stats() session.Stats, Detail() []session.SessionDetail, Delete(sid) error)
    - internal/pool/detail.go (after plan 05-01 — Pool.Detail() []pool.AgentSlot)
    - internal/config/config.go (after plan 05-02 — Config.SessionTTL + Config.SessionMax)
    - internal/adapter/{ollama,openai,anthropic}/adapter.go (after Task 3 — each adapter's Config has a `Registry *session.Registry` field AND an `EngineForACP func(engine.ACPClient) *engine.Engine` factory field — LOCKED by Task 3)
    - .planning/phases/05-pool-stateful-sessions/05-PATTERNS.md (§"cmd/otto-gateway/main.go (MODIFY)" — insertions 1-5 verbatim)
    - .planning/phases/05-pool-stateful-sessions/05-RESEARCH.md (§"Pitfall 5: Reaper-vs-Close goroutine leak on shutdown" — bounded shutdown ordering: registry first, pool second)
    - .planning/phases/05-pool-stateful-sessions/05-CONTEXT.md (Claude's Discretion: registry close before pool close + bounded time)
  </read_first>
  <behavior>
    This task is wiring; the integration test is a real-binary smoke + a goleak gate. Behaviors:
    - The gateway binary builds: `go build ./cmd/otto-gateway/...` exits 0.
    - The `app` struct has a `registry *session.Registry` field.
    - `newApp` (or its equivalent constructor) calls `session.New(session.Config{...})` AFTER the pool is constructed and warmed.
    - `Registry.Start(ctx)` is called once after construction.
    - `registryStatsAdapter` (or equivalent) implements `server.RegistryStatsSource` by wrapping the *session.Registry; converts session.Stats → server.SessionStats and session.SessionDetail → server.AgentSession.
    - `poolDetailAdapter` (or equivalent) implements `server.PoolDetailSource` by wrapping the *pool.Pool; converts pool.AgentSlot → server.AgentSlot.
    - `server.NewFromConfig(server.Config{..., Pool: poolStatsAdapter, PoolDetail: poolDetailAdapter, Registry: registryStatsAdapter, Surfaces: [..., SessionsRouter mounted at OpenAIPathPrefix (/v1)] })`.
    - The SessionsRouter SurfaceMount is added to the existing surfaces slice at the OpenAIPathPrefix (default /v1) so it gets the auth.Bearer + auth.IPAllowlist middleware from the per-prefix Route block.
    - Each adapter's Config gets the registry passed in (per Task 3's chosen mechanism — Registry *session.Registry field or interface alias).
    - Cleanup closure (the existing cleanup at lines 122-128) is restructured to: (1) registry.Close() FIRST, (2) pool.Close() SECOND. Both are nil-safe.
    - The reaper goroutine teardown completes in bounded time per Pitfall 5 — main.go does not add a timeout because the wg.Wait in Registry.Close (plan 05-02) is already bounded by TickInterval.
    - All existing config wiring stays: KiroCmd, KiroArgs, KiroCWD, PingInterval, AuthTokens, AllowedPrefixes flow into both pool.Config AND session.Config (the env-vars are shared per Backward-Compat contract).

    Goleak / build / smoke tests:
    - Test 1: TestMain_BuildsCleanly — `go build ./cmd/otto-gateway/...` exits 0. Compile-time gate (run as part of CI; no separate Go test needed).
    - Test 2: TestNewApp_RegistryAndPoolConstructed — exported test-only constructor or hook that runs newApp with mocked Factory; assert app.registry != nil and app.pool != nil.
    - Test 3: TestNewApp_CleanupOrdersRegistryBeforePool — instrument fake registry + fake pool Close methods to record the order; assert registry.Close was called before pool.Close.
    - Test 4: TestNewApp_RegistryCloseIsBoundedTime — call cleanup; assert returns within 2s with TickInterval=100ms (real-time, no mocks).

    (Note: cmd/otto-gateway is a main package; testing it directly requires either a `cmd/otto-gateway/main_test.go` or relocating helpers to a sub-package. The simpler path: introduce a small `cmd/otto-gateway/app.go` with the `app` struct + `newApp` extracted out of `main.go`'s `main()` function — the existing code may already do this. Tests live in `cmd/otto-gateway/app_test.go`. If the codebase already extracts an `app` package, tests live there.)
  </behavior>
  <action>
    Read `cmd/otto-gateway/main.go` first to confirm the current `newApp` / `app` shape. Apply the five insertions per 05-PATTERNS.md §"cmd/otto-gateway/main.go (MODIFY)":

    Insertion 1 — Registry construction (after the existing pool construction at lines 130-155):
    - Construct `a.registry = session.New(session.Config{ Logger: logger, TTL: cfg.SessionTTL, TickInterval: 60 * time.Second, MaxSessions: cfg.SessionMax, KiroCmd: cfg.KiroCmd, KiroArgs: cfg.KiroArgs, KiroCWD: cfg.KiroCWD, PingInterval: cfg.PingInterval })`. Only do this when `cfg.KiroCmd != ""` (mirrors the existing pool guard).
    - Call `a.registry.Start(context.Background())` to spawn the reaper. (Or wire to the existing newApp ctx if there is one; the registry's Start signature takes a ctx but currently uses it only for closing — see plan 05-02 Task 0/2 for the exact body.)

    Insertion 2 — App struct gains the registry field (lines 99-105):
    - Add `registry *session.Registry` field to the `app` struct.

    Insertion 3 — Cleanup ordering (lines 122-128):
    - Restructure the cleanup closure to call `a.registry.Close()` BEFORE `a.pool.Close()`. Both nil-safe. Log errors via slog without aborting the rest of the closure (mirror the existing pool-close error-logging shape).

    Insertion 4 — Stats adapters mirroring `poolStatsAdapter` at lines 309-316:
    - `type registryStatsAdapter struct { reg *session.Registry }` with two methods:
        - `func (r registryStatsAdapter) Stats() server.SessionStats { s := r.reg.Stats(); return server.SessionStats{Active: s.Active} }`.
        - `func (r registryStatsAdapter) Detail() []server.AgentSession { details := r.reg.Detail(); out := make([]server.AgentSession, 0, len(details)); for _, d := range details { out = append(out, server.AgentSession{ID: d.ID, Alive: d.Alive, Busy: d.Busy, LastUsed: d.LastUsed, Model: d.Model}) }; return out }`.
    - `type poolDetailAdapter struct { pool *pool.Pool }` with one method:
        - `func (p poolDetailAdapter) Detail() []server.AgentSlot { slots := p.pool.Detail(); out := make([]server.AgentSlot, 0, len(slots)); for _, sl := range slots { out = append(out, server.AgentSlot{Label: sl.Label, Alive: sl.Alive, Busy: sl.Busy, CurrentSessionID: sl.CurrentSessionID}) }; return out }`.

    Insertion 5 — server.NewFromConfig wires the registry + SessionsRouter (lines 287-299):
    - Build `var registryForServer server.RegistryStatsSource; if a.registry != nil { registryForServer = registryStatsAdapter{reg: a.registry} }`.
    - Build `var poolDetailForServer server.PoolDetailSource; if a.pool != nil { poolDetailForServer = poolDetailAdapter{pool: a.pool} }`.
    - Append SessionsRouter to the surfaces slice: `if a.registry != nil { surfaces = append(surfaces, server.SurfaceMount{ Prefix: cfg.OpenAIPathPrefix, Router: &server.SessionsRouter{ Registry: a.registry, Logger: logger } }) }`. Note: SessionsRouter satisfies SessionDeleter via session.Registry.Delete; the Prefix uses cfg.OpenAIPathPrefix (default /v1 per SURF-02 / SURF-08).
    - In the server.NewFromConfig call, add `PoolDetail: poolDetailForServer, Registry: registryForServer`.

    Per-adapter wiring (each surface's Config object) — Task 3 LOCKED the per-adapter `Registry *session.Registry` field + `EngineForACP` factory closure; wire BOTH unconditionally:
    - Pass `a.registry` into each adapter Config (e.g., `OllamaCfg.Registry = a.registry`, `OpenAICfg.Registry = a.registry`, `AnthropicCfg.Registry = a.registry`).
    - Pass `KiroCWD: cfg.KiroCWD` into each adapter Config (the registry needs cwd at Get time).
    - Wire `EngineForACP` on each adapter Config to a per-request engine factory closure: `OllamaCfg.EngineForACP = func(c engine.ACPClient) *engine.Engine { return engine.New(engine.Config{ACP: c, Logger: logger, ...same fields as the existing per-adapter engine construction... }) }`. Same closure shape for OpenAI and Anthropic. This is the integration seam between Task 3's locked routing and the engine — `internal/engine/engine.go` is NOT modified.

    Add tests in `cmd/otto-gateway/app_test.go` (or `main_test.go` if the codebase uses that pattern):
    - TestNewApp_RegistryAndPoolConstructed: inject a fake ClientFactory; call newApp (or the constructor); assert a.registry != nil + a.pool != nil.
    - TestNewApp_CleanupOrdersRegistryBeforePool: instrument fake registry + pool Close methods to record call order; assert order via a shared []string log.
    - TestNewApp_RegistryCloseIsBoundedTime: use real session.Registry with TickInterval=100ms; call cleanup; assert returns within 2s. (Pitfall 5)

    Confirm the .go-arch-lint.yml: cmd/otto-gateway is already permitted to import internal/session (it's the wiring layer). No change needed.

    Do NOT change Engine construction beyond what's needed to thread the registry into each adapter. The engine itself stays bound to the pool for stateless requests; stateful requests use the per-request engine pattern from Task 3.
  </action>
  <verify>
    <automated>go build ./cmd/otto-gateway/...</automated>
    <automated>go vet ./cmd/otto-gateway/...</automated>
    <automated>go test ./cmd/otto-gateway/... -count=1 -timeout=30s -race</automated>
    <automated>go test ./... -count=1 -timeout=180s -race</automated>
    <automated>grep -E 'session\.New\(session\.Config' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'a\.registry\.Start' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'a\.registry\.Close\(\)' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'a\.pool\.Close\(\)' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'registryStatsAdapter' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'poolDetailAdapter' cmd/otto-gateway/main.go</automated>
    <automated>grep -E 'server\.SessionsRouter' cmd/otto-gateway/main.go</automated>
  </verify>
  <acceptance_criteria>
    - Build: `go build ./cmd/otto-gateway/...` exits 0
    - Source: `cmd/otto-gateway/main.go contains "session.New(session.Config"`
    - Source: `cmd/otto-gateway/main.go contains "a.registry.Start"`
    - Source: `cmd/otto-gateway/main.go contains "a.registry.Close()"` AND it appears BEFORE `"a.pool.Close()"` in the cleanup closure (assert by line-number search or by inspection during review; or write a small lint test that parses the source)
    - Source: `cmd/otto-gateway/main.go contains "registryStatsAdapter"` and `"poolDetailAdapter"`
    - Source: `cmd/otto-gateway/main.go contains "server.SessionsRouter"` constructed and appended to the surfaces slice
    - Test: `go test -run TestNewApp_CleanupOrdersRegistryBeforePool ./cmd/otto-gateway/... -count=1 -timeout=10s -race exits 0`
    - Test: `go test -run TestNewApp_RegistryCloseIsBoundedTime ./cmd/otto-gateway/... -count=1 -timeout=10s -race exits 0`
    - Full-suite green: `go test ./... -count=1 -timeout=180s -race exits 0`
    - Architecture: `.go-arch-lint.yml` still passes (cmd/otto-gateway is allowed to import internal/session, internal/pool, internal/server)
    - No regression: all Phase 2/3/3.1/4 integration tests continue to pass — wiring is additive.
  </acceptance_criteria>
  <done>
    cmd/otto-gateway constructs registry + pool; reaper started; cleanup teardown is registry-first / pool-second and bounded; /health/agents + DELETE /v1/sessions/:id endpoints are wired; all three surfaces have registry access. Build green; full test suite green with -race; goleak clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: E2E test coverage for Phase 5 success criteria (tests/e2e/pool_sessions_e2e_test.go)</name>
  <files>tests/e2e/pool_sessions_e2e_test.go</files>
  <read_first>
    - tests/e2e/README.md (HUMAN-UAT mapping convention; how Phase 3.1 + Phase 4 converted manual UAT steps into automated subtests)
    - tests/e2e/e2e_test.go (shared helpers — `gateOrSkip`, `resolveKiro`, `freePort`, `bootGateway`, `TestMain`, `moduleRoot`, `builtBinary` — DO NOT redefine in the new file)
    - tests/e2e/ollama_e2e_test.go (closest analog — same `//go:build e2e` tag, same `package e2e_test`, NDJSON / per-subtest organization pattern)
    - tests/e2e/anthropic_e2e_test.go (SSE assertion patterns; helpful for the in-flight DELETE-cancel subtest)
    - tests/e2e/openai_e2e_test.go (per-subtest structure pattern)
    - .planning/phases/05-pool-stateful-sessions/05-CONTEXT.md (SC1..SC5 contract + D-15/D-16 wire shape under test)
    - .planning/phases/05-pool-stateful-sessions/05-VALIDATION.md §"Acceptance Thresholds" (per-SC thresholds — translate to assertions)
    - internal/server/agents.go (NEW after Task 1 — the wire shape under test; AgentSlot + AgentSession JSON tags must match)
  </read_first>
  <action>
    Create `tests/e2e/pool_sessions_e2e_test.go` — a new e2e test file (build tag `e2e`, package `e2e_test`) that automates SC1..SC5 against a live gateway boot. This file MUST reuse the shared helpers in `tests/e2e/e2e_test.go` (`gateOrSkip(t)`, `bootGateway`, `freePort`, `resolveKiro`) and MUST NOT redefine `TestMain`, `moduleRoot`, `builtBinary`, or any shared helper.

    File header:
    - `//go:build e2e` build tag (first non-blank line)
    - `package e2e_test`
    - Imports: stdlib `bytes`, `context`, `encoding/json`, `fmt`, `io`, `net/http`, `os`, `os/exec`, `path/filepath`, `strconv`, `sync`, `syscall`, `testing`, `time`. NO third-party deps.

    Top-level test: `func TestE2E_PoolSessions(t *testing.T)`. The function calls `gateOrSkip(t)` first (so the suite is skipped unless `OTTO_E2E=1`), then runs each SC as a subtest via `t.Run("<Subtest>", func(t *testing.T) { ... })`. Each subtest boots a fresh gateway via `bootGateway(t, env...)` so subtests are independent — boot/teardown cost is acceptable for e2e.

    Subtests (ten total, all under `TestE2E_PoolSessions`):

    1. `WarmupBeforeListen` — SC1. Boot gateway with `POOL_SIZE=4` and a small `KIRO_CMD` warmup window. Fire 10 sequential `POST /api/chat` requests (non-streaming, short payload). Time each request (wall-clock around the HTTP call). Assert: request #2 latency is within ±25% of request #10 latency. (±25% absorbs CI noise while still catching warmup-tax regressions; the Acceptance Threshold for SC1 is ±10% in 05-VALIDATION but that tighter bound is the manual perf-comparison job in Task 6 — the e2e gate is the looser noise-tolerant bound.)

    2. `SaturationBlocking` — SC2. Boot with `POOL_SIZE=4`. Launch 8 concurrent goroutines, each firing `/api/chat` with no `X-Session-Id` (stateless → pool path). Assertions: (a) at peak — captured via a parallel `GET /health/agents` poll loop — `pool.busy == 4` and 4 slots show `busy: true`; (b) all 8 requests complete with HTTP 200; (c) no goroutine deadlock under `-race` (the test runs as part of `go test -race -tags=e2e ...`).

    3. `SessionIDAffinity` — SC3. Boot. Send two `/api/chat` requests with the same `X-Session-Id: e2e-sid-1` header (sequential). Then `GET /health/agents`. Assert: exactly one entry in `sessions[]` with `id == "e2e-sid-1"`. Then send a third request WITHOUT the header. Re-query `/health/agents`. Assert: `sessions[]` still has exactly one entry (the original sid); the stateless request did NOT create a new session entry.

    4. `IdleReap_RealTime` — SC4 (reaper side). Boot with env `SESSION_TTL_MS=500` and `SESSION_TICK_INTERVAL_MS=100` (these env vars MUST be exposed by Task 4's main.go wiring — the test-injectable knobs override `TickInterval: 60 * time.Second` from the production default). Create a session via `/api/chat` with `X-Session-Id: e2e-reap-1`. Confirm presence via `/health/agents`. Wait 1.5 seconds. Re-poll `/health/agents`. Assert: the session entry no longer appears in `sessions[]` (reaped).

    5. `DeleteSession_OK` — SC4 (DELETE side). Boot. Create session via `/api/chat` with `X-Session-Id: e2e-del-ok`. Send `DELETE /v1/sessions/e2e-del-ok` (with the AUTH_TOKEN bearer set during boot). Assert: HTTP 200 with body decoded to `{"deleted":"e2e-del-ok"}` (use `json.NewDecoder(...).Decode(&map[string]string{})`). Re-poll `/health/agents`. Assert: `sessions[]` does not contain the sid.

    6. `DeleteSession_Unknown` — SC4 (unknown sid). Boot. Send `DELETE /v1/sessions/does-not-exist` directly (no prior create). Assert: HTTP 404.

    7. `DeleteSession_CancelsInFlight` — SC4 + D-08. Boot. Start a streaming `/api/chat` request with `X-Session-Id: e2e-del-cancel` and `stream: true` in a background goroutine, reading the response body. Wait briefly (~250ms) for the stream to start producing tokens. From the test goroutine, issue `DELETE /v1/sessions/e2e-del-cancel`. Assert: (a) DELETE returns 200; (b) the streaming response goroutine's `Read` returns within a bounded timeout (≤3s) with either io.EOF or a connection-closed error — the streaming response MUST terminate cleanly, not hang indefinitely.

    8. `HealthAgentsShape` — SC5 (wire shape). Boot. With no auth header at all (D-18 — auth-exempt), `GET /health/agents`. Assert: HTTP 200. Decode body into a struct with the exact D-15/D-16 keys: `pool` object has `size`, `alive`, `busy`, `slots[]` (each slot has `label`, `alive`, `busy`, `current_session_id`); `sessions[]` (each session has `id`, `alive`, `busy`, `last_used`, `model`). Use `json.RawMessage` or a strict decode against a typed struct that exactly mirrors agents.go.

    9. `DeadSlotLazyRespawn` — SC5 (dead-slot detection + lazy respawn). Boot with `POOL_SIZE=4`. Discover the kiro-cli child PID for one slot — preferred approach: use `pgrep -P <gateway-pid> kiro-cli` via `exec.Command`. Alternative: if gateway-pid discovery via `bootGateway` is awkward, fall back to `exec.Command("pkill", "-P", strconv.Itoa(gatewayPid), "-n", "kiro-cli")` to kill one child. The bootGateway helper already records the gateway process — extend / use whatever PID accessor it exposes. Kill one child via `syscall.Kill(pid, syscall.SIGKILL)` (or `pkill` shell-out). Wait ~500ms. Poll `/health/agents`; assert one slot shows `alive: false`. Fire a fresh `/api/chat` (no `X-Session-Id` — pool path). Assert: request succeeds (HTTP 200) — Pool.NewSession lazy-respawned a new slot synchronously. Re-poll `/health/agents`; assert: all 4 slots show `alive: true` again (and `pool.slots[]` has 4 entries — no shrink in the healthy-respawn path).

    10. `AllDeadRespawnFails` — SC5 (fault path D-03). Boot with `KIRO_CMD` pointing at a small shell stub that the test writes at startup. The stub lives under `tests/e2e/cmd/stub-kiro-fails/` — the test writes the file to a temp dir at runtime, `os.Chmod(0o755)` it, and passes its absolute path via `KIRO_CMD`. Stub body: `#!/bin/sh
exit 1` (exits immediately with non-zero — Pool.respawn(slot) will return an error per D-03). Boot the gateway with `POOL_SIZE=4` and this failing stub. Because the stub exits immediately at warmup, all 4 slots will already be dead — Pool warmup will either fail to start OR start with a shrunk pool. Verify the all-dead respawn-fails behavior by: (a) firing one `/api/chat` and asserting HTTP 503; (b) `GET /health/agents` and asserting `pool.slots[]` length is less than 4 (the pool shrunk per D-03 `removeSlot` path in plan 05-01).

    HTTP helpers: define small private helpers `doJSON(t, method, url, headers, body)` and `pollHealthAgents(t, url, auth)` at file scope (NOT `package e2e` — these are file-local) to avoid duplicating the boilerplate across subtests. AUTH_TOKEN is set during `bootGateway` (the helper already wires `AUTH_TOKEN` env-var → `Authorization: Bearer ...` flow per how ollama_e2e_test.go and anthropic_e2e_test.go do it).

    The test file MUST NOT introduce any new top-level `TestMain` (TestMain is owned by `tests/e2e/e2e_test.go`). It MUST NOT redefine `gateOrSkip`, `resolveKiro`, `freePort`, `bootGateway`, `moduleRoot`, or `builtBinary` — those are shared helpers.

    Total: 10 subtests under `TestE2E_PoolSessions`. Each subtest is independent (own bootGateway). Total runtime budget under `-timeout=180s` with 10 subtests × ~10s boot is ~100s — well within budget.
  </action>
  <verify>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions ./tests/e2e/... -count=1 -timeout=180s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/WarmupBeforeListen ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/SaturationBlocking ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/SessionIDAffinity ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/IdleReap_RealTime ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/DeleteSession_OK ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/DeleteSession_Unknown ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/DeleteSession_CancelsInFlight ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/HealthAgentsShape ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/DeadSlotLazyRespawn ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions/AllDeadRespawnFails ./tests/e2e/... -count=1 -timeout=60s</automated>
    <automated>go test ./tests/e2e/... -count=1 -timeout=30s</automated>
    <automated>go build -tags=e2e ./tests/e2e/...</automated>
    <automated>go vet -tags=e2e ./tests/e2e/...</automated>
    <automated>grep -E '^//go:build e2e' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>grep -E '^package e2e_test' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>grep -E 'func TestE2E_PoolSessions\(' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>grep -c 't\.Run\(' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>grep -E 'gateOrSkip\(t\)' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>grep -E 'bootGateway' tests/e2e/pool_sessions_e2e_test.go</automated>
    <automated>! grep -E 'func TestMain' tests/e2e/pool_sessions_e2e_test.go</automated>
  </verify>
  <acceptance_criteria>
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "//go:build e2e"`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "package e2e_test"`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "func TestE2E_PoolSessions("`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"WarmupBeforeListen\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"SaturationBlocking\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"SessionIDAffinity\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"IdleReap_RealTime\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"DeleteSession_OK\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"DeleteSession_Unknown\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"DeleteSession_CancelsInFlight\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"HealthAgentsShape\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"DeadSlotLazyRespawn\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "t.Run(\"AllDeadRespawnFails\""`
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "gateOrSkip(t)"` (suite is opt-in via `OTTO_E2E=1`)
    - File: `tests/e2e/pool_sessions_e2e_test.go contains "bootGateway"` (reuses shared helper — does NOT redefine)
    - File: `tests/e2e/pool_sessions_e2e_test.go does NOT contain "func TestMain"` (TestMain lives in e2e_test.go)
    - Build: `go build -tags=e2e ./tests/e2e/...` exits 0
    - Vet: `go vet -tags=e2e ./tests/e2e/...` exits 0
    - Suite skip (kiro absent or OTTO_E2E unset): `go test ./tests/e2e/... -count=1 exits 0` — without `-tags=e2e`, this file is excluded from the build set (opt-in via build tag); without `OTTO_E2E=1`, `gateOrSkip(t)` skips at runtime.
    - Suite green (kiro authenticated + OTTO_E2E=1): `OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions ./tests/e2e/... -count=1 -timeout=180s exits 0`.
  </acceptance_criteria>
  <threat_model_refs>
    - T-05-01 (SESSION_MAX cap surfaces as 503) — implicitly exercised by SessionIDAffinity (session creation through registry).
    - T-05-06 (goroutine leak under repeated boot/teardown) — bootGateway helper handles teardown; each subtest's boot/teardown is the goroutine-leak exposure surface. Existing e2e suites (ollama/openai/anthropic) prove the helper is leak-clean.
    - T-05-07 (respawn CPU saturation under all-dead) — `AllDeadRespawnFails` directly exercises this fault path; the stub-kiro-fails binary forces respawn to fail every time.
  </threat_model_refs>
  <done>
    `tests/e2e/pool_sessions_e2e_test.go` exists with 10 subtests under `TestE2E_PoolSessions`. Each SC1..SC5 has at least one automated subtest. The file uses shared helpers from `tests/e2e/e2e_test.go` (no redefinition). `go build -tags=e2e ./tests/e2e/...` succeeds; the suite is opt-in via `OTTO_E2E=1` + `-tags=e2e`. Manual residual work moves to Task 6.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual residual verification — perf-vs-Node delta + SESSION_MAX RSS sanity</name>
  <files>tests/e2e/reports/PHASE5-PERF.md</files>
  <action>
    Tasks 1-5 cover SC1..SC5 functional acceptance via automated unit, integration, and e2e tests. Task 6 covers ONLY the genuinely-manual items that resist hermetic automation: (a) the perf-vs-Node delta required by CLAUDE.md's "must not be slower than Node under concurrent load. Tail latency should improve." constraint, and (b) the SESSION_MAX RSS sanity check from 05-VALIDATION.md "Manual-Only Verifications" (per-session RSS is platform- and binary-version-dependent, not deterministic in unit tests).

    AFTER Tasks 1-5 complete with the full automated suite green (`go test ./... -count=1 -race -timeout=180s` exits 0; `make ci` exits 0; `OTTO_E2E=1 go test -tags=e2e -run TestE2E_PoolSessions ./tests/e2e/... -count=1 -timeout=180s` exits 0), present this BLOCKING checkpoint to the developer. The checkpoint is BLOCKING (NOT auto-approvable per workflow.auto_advance) because both manual residuals are cross-runtime / cross-platform comparisons that are not hermetic unit-test concerns.

    Run the two verification steps in `<how-to-verify>` against a fresh `bin/otto-gateway` boot with real kiro-cli on PATH. Record both outcomes in `tests/e2e/reports/PHASE5-PERF.md` (create the file if it does not exist; the `tests/e2e/reports/` directory already exists from earlier phases).
  </action>
  <verify>
    <human-check>Both verification steps in `<how-to-verify>` pass AND the report `tests/e2e/reports/PHASE5-PERF.md` exists with both result sections populated.</human-check>
  </verify>
  <what-built>
    Phase 5 ships: warm pool with dead-slot lazy re-spawn (plan 05-01), session registry + reaper + per-entry mutex (plan 05-02), DELETE /v1/sessions/:id + /health/agents + surface routing + main.go wiring (plan 05-03). After tasks 1-5 of this plan complete with the full automated suite (including e2e) green, run the gateway against real kiro-cli and side-by-side against the Node implementation to confirm the two manual residual constraints: (1) Go is not slower than Node under concurrent load, (2) per-session RSS is consistent and SESSION_MAX=32 default stays well within typical dev/prod RAM budgets.
  </what-built>
  <how-to-verify>
    Prereqs:
    - `kiro-cli` 2.4.x installed and on PATH.
    - `make build` produces `bin/otto-gateway`.
    - The Node implementation referenced in CLAUDE.md (`../gitlab.rosetta.ericssondevops.com/loop_24/acp_server`) is checked out and runnable.
    - `ab` (Apache Bench) or `wrk` available locally (`brew install wrk` on macOS).
    - `tests/e2e/reports/` directory exists.

    Step 1 — Perf delta vs Node implementation (CLAUDE.md constraint):

      a. Boot the Node implementation at `:11434` with `POOL_SIZE=4`. Wait for warmup to complete.
      b. Run a concurrent load test: `wrk -t4 -c8 -d30s -s <script-with-POST-/api/chat-body> --latency http://localhost:11434/api/chat`. Capture p50, p99, and tail (p99.9 if available). Record in `PHASE5-PERF.md` under "Node baseline".
      c. Stop the Node implementation. Boot `bin/otto-gateway` with the SAME `POOL_SIZE=4` and the same KIRO_CMD it would use in production. Wait for warmup.
      d. Run the IDENTICAL wrk command against the Go gateway. Capture p50, p99, tail. Record in `PHASE5-PERF.md` under "Go gateway".
      e. PASS criterion: p99(Go) ≤ p99(Node) + 10% (i.e., Go is no worse than Node by more than 10% at the tail). Document the exact numbers in the report.

    Step 2 — SESSION_MAX RSS sanity (05-VALIDATION.md "Manual-Only Verifications"):

      a. Stop any running gateway. Boot `bin/otto-gateway` with env `POOL_SIZE=4 SESSION_MAX=8 AUTH_TOKEN=hunter2`.
      b. Spawn 8 sessions via curl loop:
         ```
         for i in 1 2 3 4 5 6 7 8; do
           curl -sH "Authorization: Bearer hunter2"                 -H "X-Session-Id: rss-test-$i"                 -X POST http://localhost:11434/api/chat                 -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}' > /dev/null
         done
         ```
      c. `curl -sH 'Authorization: Bearer hunter2' http://localhost:11434/health/agents | jq '.sessions | length'`. Expected: 8.
      d. Capture per-session RSS via `ps -o pid,rss,command -p $(pgrep -P $(pgrep otto-gateway) kiro-cli | tr '\n' ',')` (or `ps aux | grep kiro-cli`). Record each kiro-cli child's RSS in MB in `PHASE5-PERF.md`.
      e. PASS criterion: per-session RSS is consistent (no obvious leak — values within ±20% of each other) AND `32 × (average per-session RSS) ≤ 25% of 8 GB` (i.e., the SESSION_MAX=32 default would consume ≤ 2 GB on a typical 8 GB machine).

    Report file layout (`tests/e2e/reports/PHASE5-PERF.md`):

      ```markdown
      # Phase 5 — Manual Perf + RSS Report

      Run date: <YYYY-MM-DD>
      Host: <`uname -a` output>
      kiro-cli version: <`kiro-cli --version`>

      ## Step 1 — Perf delta vs Node implementation

      ### Node baseline (wrk t4 c8 d30s)
      - p50: <ms>
      - p99: <ms>
      - tail: <ms>

      ### Go gateway (wrk t4 c8 d30s)
      - p50: <ms>
      - p99: <ms>
      - tail: <ms>

      Verdict: <PASS / FAIL>  (p99 Go ≤ p99 Node + 10%)

      ## Step 2 — SESSION_MAX RSS sanity

      | Session ID | kiro-cli PID | RSS (MB) |
      |------------|--------------|----------|
      | rss-test-1 | <pid>        | <rss>    |
      | ... (8 rows total) |     |          |

      Average per-session RSS: <MB>
      Projected 32-session total: <MB>
      Verdict: <PASS / FAIL>  (per-session RSS consistent AND 32 × avg ≤ 2 GB)
      ```

    Stop the gateway with Ctrl-C. Observe shutdown logs are bounded.

    Record both verdicts in the report. If either fails, do NOT approve — capture the failing measurement set and surface it for plan revision.
  </how-to-verify>
  <acceptance_criteria>
    - File: `tests/e2e/reports/PHASE5-PERF.md exists`
    - Source: `tests/e2e/reports/PHASE5-PERF.md contains "p99 Go"` (Step 1 perf section populated)
    - Source: `tests/e2e/reports/PHASE5-PERF.md contains "per-session RSS"` (Step 2 RSS section populated)
    - Human gate: developer confirms both PASS criteria are met by reading the report.
  </acceptance_criteria>
  <resume-signal>Type "approved" if both Step 1 (p99 Go ≤ p99 Node + 10%) and Step 2 (per-session RSS consistent AND 32 × avg ≤ 2 GB) PASS, and `tests/e2e/reports/PHASE5-PERF.md` contains both result sections. If either fails, paste the failing step's number + the measured numbers + the report path so the plans can be revised.</resume-signal>
  <done>
    Manual residual gate cleared: Go gateway not slower than Node implementation under concurrent load (p99 within +10% tolerance), per-session RSS consistent and SESSION_MAX=32 default stays within typical RAM budget. Report committed at `tests/e2e/reports/PHASE5-PERF.md`. Phase 5 is ready for the verify-work pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client → /health/agents | Operator-facing read endpoint; auth-exempt per D-18. Exposes session IDs verbatim per D-17. |
| External client → DELETE /v1/sessions/:id | Auth-protected; deletes any sid owned by the AUTH_TOKEN holder. Single-tenant assumption inherited from Phase 1/2. |
| External client → /api/chat + /v1/chat/completions + /v1/messages with X-Session-Id | Surface handlers extract sid from header and route to registry; client-supplied unguessable id (no gateway-side generation). |
| internal/adapter → internal/session | Adapters import internal/session at the type level for *session.Registry + *session.Entry; permitted by .go-arch-lint (rule is "MUST NOT import internal/engine", not internal/session). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure (session-id exposure on auth-exempt /health/agents) | internal/server/agents.go | accept | D-17 + D-18 locked: full session ids verbatim, auth-exempt. Operators in multi-tenant deployments reverse-proxy to gate. Document in docs/operating.md. Severity LOW in single-tenant assumption. |
| T-05-04-MT | Information Disclosure (session-hijacking in multi-tenant deployment without AUTH_TOKEN) | internal/adapter/*/handlers.go | mitigate | Documented: in any multi-tenant deployment, operators MUST set AUTH_TOKEN — surface routes /api and /v1 are auth-gated by Phase 2 OBSV-01 + AUTH-01..03. Anti-shoulder-surf: full sid in /health/agents response body unredacted (D-17 + Deferred Items). |
| T-05-05 | Tampering / IDOR (DELETE any sid) | internal/server/sessions_delete.go | accept | Single-tenant assumption: any AUTH_TOKEN holder may DELETE any sid. Documented. Severity LOW. |
| T-05-02 | DoS (per-entry mutex deadlock vs reaper) | internal/adapter/*/handlers.go | mitigate | Adapters Lock entry.Mu before Prompt; defer Unlock + MarkUsed AFTER stream completes (D-07 + D-11). Reaper uses TryLock-skip-in-flight (D-12 — implemented in plan 05-02, tested there). Severity LOW with mitigation in place. |
| T-05-04-CHILDREN | Tampering (subprocess control via env-var injection) | cmd/otto-gateway/main.go session.New | accept | KIRO_CMD / KIRO_ARGS / KIRO_CWD are operator-controlled env vars (Phase 1 trust boundary). Subprocess spawn uses os/exec.CommandContext; gosec G204 already flags tainted-input regressions. Not a Phase 5 surface change. |
| T-OBSV-DOS | DoS (large /health/agents response) | internal/server/agents.go | accept | Response size scales with pool size + SESSION_MAX. SESSION_MAX=32 default; per-row overhead ~150 bytes; 32 sessions + 4 slots ~6KB. No DoS concern at these magnitudes. |
| T-05-06-SHUTDOWN | DoS (slow shutdown if registry hangs) | cmd/otto-gateway/main.go cleanup | mitigate | Registry.Close blocks at most TickInterval (60s prod, much less in test). Pitfall 5 mitigation per plan 05-02. Cleanup closure order is registry FIRST, pool SECOND. TestNewApp_RegistryCloseIsBoundedTime asserts <2s with TickInterval=100ms. |
</threat_model>

<verification>
- `make ci` (golangci-lint + go test -race + govulncheck) green for `./internal/server/...`, `./internal/adapter/...`, `./cmd/otto-gateway/...`.
- `go build ./cmd/otto-gateway/...` produces a working binary; SC1..SC5 verified against real kiro-cli in Task 5 human-verify.
- `.go-arch-lint.yml` passes — internal/adapter may import internal/session (rule applies only to internal/engine boundary).
- All Phase 2/3/3.1/4 regression tests continue to pass — Phase 5 additions are additive at every layer.
- /health continues to return its existing flat shape (LB-friendly summary) with sessions.active now populated from the registry.
- /health/agents emits the D-14/D-15/D-16 verbose detail shape with auth-exempt access (D-18).
- DELETE /v1/sessions/:id is auth-protected and returns the D-08 200/404 shape.
- Each surface handler routes correctly per X-Session-Id presence.
</verification>

<success_criteria>
- OBSV-02: GET /health/agents returns the D-14/D-15/D-16 wire shape with auth-exempt access (D-18). Verified by TestAgentsHandler_* + manual curl in Task 5.
- SESS-01 surface side: each surface handler reads X-Session-Id, routes to registry on non-empty, locks entry.Mu before Prompt, defers Unlock + MarkUsed (D-05/D-07/D-09/D-11). Verified by TestOllamaHandleChat_WithXSessionId_RoutesToRegistry + per-surface equivalents.
- SESS-03 HTTP side: DELETE /v1/sessions/:id returns 200 {deleted: id} or 404; auth-protected (D-08 + D-18). Verified by TestSessionsRouter_Delete_* + Task 5 manual curl.
- POOL-04 visible: per-slot Detail() rows reflect dead-slot state until lazy re-spawn (Pitfall 6). Verified in Task 5 step 15-19.
- D-18 invariant: /health/agents is in the exempt-routes list; DELETE /v1/sessions/:id is in the auth-protected /v1 prefix. Verified by TestAgentsHandler_NoAuthRequired + TestSessionsRouter_Delete_RequiresAuth.
- Shutdown ordering: registry.Close → pool.Close, bounded time. Verified by TestNewApp_CleanupOrdersRegistryBeforePool + TestNewApp_RegistryCloseIsBoundedTime + manual Task 5 shutdown observation.
- Phase 4 D-06 watchdog preserved: disconnect-cancel still fires session/cancel for stateful sessions (both paths go through engine.Run unchanged). Verified by manual curl-then-^C in Task 5.
- `make ci` green; `.go-arch-lint.yml` passes.
- ALL eight Phase 5 requirements (POOL-01..04, SESS-01..03, OBSV-02) closed across plans 05-01 + 05-02 + 05-03.
</success_criteria>

<output>
Create `.planning/phases/05-pool-stateful-sessions/05-03-SUMMARY.md` when done. Summary must include:
- Files added (agents.go, sessions_delete.go, their tests) and modified (server.go, health.go, three adapter handler files + tests, main.go).
- The RegistryStatsSource / PoolDetailSource interface choice (two interfaces vs combined — note the actual choice and why).
- The X-Session-Id routing mechanism (LOCKED per revision: per-adapter `Registry *session.Registry` field + `EngineForACP func(engine.ACPClient) *engine.Engine` factory closure; NO SessionLookup interface, NO SessionHandle wrapper, NO engine.go modification).
- The /health/agents wire shape sample (one slot row + one session row, raw JSON).
- Task 5 human-verification outcomes — paste the 21 PASS/FAIL records.
- Any deviations from 05-PATTERNS.md or 05-RESEARCH.md.
- A pointer to the next phase: `/gsd-verify-work 5` should be run after this plan completes to walk the eight REQ-IDs.
</output>
