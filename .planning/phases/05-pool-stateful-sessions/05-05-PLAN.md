---
phase: 05-pool-stateful-sessions
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on:
  - 05-04
files_modified:
  - tests/e2e/reports/PHASE5-PERF.md
  - tests/e2e/wrk/post.lua
  - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
autonomous: false
requirements: []
requirements_addressed: []
tags:
  - perf
  - rss
  - human-verification
  - claude-md-non-functional
  - gap-closure

# Revision: addresses 05-REVIEWS.md HIGH-3, LOW-2

must_haves:
  truths:
    - "`tests/e2e/reports/PHASE5-PERF.md` exists on disk after this plan runs (the SUMMARY-claimed placeholder was never created — gap 3 from 05-VERIFICATION.md)."
    - "The report contains: measurement protocol, expected acceptance thresholds, placeholder p99 latency table, placeholder per-session RSS table, sign-off section."
    - "Each manual measurement task is explicitly marked `AWAITING MANUAL MEASUREMENT` until the human operator fills it in — Claude does NOT auto-populate numbers."
    - "Acceptance thresholds are encoded verbatim from CLAUDE.md non-functional constraint and 05-VERIFICATION.md gap 3: p99(Go) ≤ p99(Node) + 10% under 4×8×30s wrk; per-session RSS within ±20% of mean; 32 × avg RSS ≤ 2 GB on an 8 GB host."
    - "The manual measurement steps cite exact commands (wrk invocation, ps -o pid,rss command) so a future operator can reproduce them without re-reading 05-VERIFICATION.md."
    - "After human sign-off, 05-VERIFICATION.md is updated with a re-verification line confirming the manual gate is satisfied."
    - "Plan 05-05 cannot be marked complete (and Phase 5 cannot be re-stamped `verified`) while the PHASE5-PERF.md report contains ANY placeholder tokens: `TBD`, `AWAITING MANUAL MEASUREMENT`, `BLOCKED_ON_05-04`, `NODE_IMPL_UNAVAILABLE`, `placeholder`, `pending` (case-insensitive)."  # Revision: addresses 05-REVIEWS.md HIGH-3
    - "The Manual Perf + RSS Gate has exactly TWO valid closure states: `Satisfied` (full measurement: Node p99, Go p99, per-session RSS all present and meeting thresholds) OR `Accepted-with-Notes` (human operator explicitly signs off with rationale ≥ 1 paragraph and sign-off line containing operator name + ISO date). Any other state means the gate is NOT closed."  # Revision: addresses 05-REVIEWS.md HIGH-3
    - "The global phase `status:` field in 05-VERIFICATION.md frontmatter flips to `verified` IFF (1) SC3 and SC4 are absent from `gaps:` (closed by Plan 05-04), (2) PHASE5-PERF.md is `Satisfied` or `Accepted-with-Notes`, AND (3) no placeholder tokens remain in PHASE5-PERF.md."  # Revision: addresses 05-REVIEWS.md LOW-2 + HIGH-3
  artifacts:
    - path: "tests/e2e/reports/PHASE5-PERF.md"
      provides: "Phase 5 perf+RSS report skeleton — auto-generated; numbers filled in by human operator at human-verify checkpoints. Must contain NO placeholder tokens at closure time."
      contains: "Acceptance Thresholds"
    - path: ".planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md"
      provides: "Re-verification entry recording satisfaction of human_verification gate (perf + RSS), AND the global `status:` flip to `verified` after all closure conditions are met."
      contains: "Manual Perf + RSS Gate"
  key_links:
    - from: "tests/e2e/reports/PHASE5-PERF.md::Acceptance Thresholds"
      to: "CLAUDE.md::Performance constraint"
      via: "Threshold values cite CLAUDE.md as their source"
      pattern: "CLAUDE\\.md|Node implementation"
    - from: ".planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md::Manual Perf + RSS Gate"
      to: "tests/e2e/reports/PHASE5-PERF.md"
      via: "VERIFICATION re-stamp cross-references the report path; both files must be free of placeholder tokens at closure"
      pattern: "tests/e2e/reports/PHASE5-PERF\\.md"
---

<objective>
Close Phase 5 gap 3 from 05-VERIFICATION.md: `tests/e2e/reports/PHASE5-PERF.md` was claimed as an auto-approved placeholder in the 05-03 SUMMARY, but the file does not exist on disk. The CLAUDE.md non-functional constraint ("must not be slower than the Node implementation") is unmeasured.

This plan ships in two halves:

1. **Autonomous (Task 1):** Create the report skeleton with measurement protocol, acceptance thresholds, placeholder tables, and clear `AWAITING MANUAL MEASUREMENT` markers — so the file exists on disk and the structure is locked.

2. **Manual gates (Tasks 2 + 3 + 4):** Human-action checkpoints where the operator runs the measurements side-by-side (Node ↔ Go) and populates the tables. These cannot be automated: the Node implementation lives at `../gitlab.rosetta.ericssondevops.com/loop_24/acp_server` (separate repo), wrk/ab needs to be installed, RSS measurements are platform-dependent.

Purpose: Phase 5's load-bearing non-functional constraint becomes measured, not claimed. The report becomes the authoritative artifact for future regressions ("did Phase 6 slow us down vs this baseline?").

**Closure semantics (binding):**

The Manual Perf + RSS Gate has exactly TWO valid closure states:

- **`Manual Perf + RSS Gate — Satisfied`** — full measurement: Node p99, Go p99, per-session RSS all present and meeting thresholds.
- **`Manual Perf + RSS Gate — Accepted-with-Notes (<operator>, <YYYY-MM-DD>)`** — human operator explicitly signs off with rationale; the rationale must be ≥ 1 paragraph (in PHASE5-PERF.md's `## Sign-Off ## Notes`) and the sign-off line in 05-VERIFICATION.md must include the operator's name + ISO date in the exact form above.

Any other state (TBD / pending / BLOCKED_ON_05-04 / NODE_IMPL_UNAVAILABLE / placeholder / AWAITING MANUAL MEASUREMENT) means the gate is NOT closed. Plan 05-05 cannot be marked complete (and Phase 5 cannot be re-stamped `verified` globally) while ANY of those tokens appear in PHASE5-PERF.md.

Output:
- `tests/e2e/reports/PHASE5-PERF.md` populated by human at the three checkpoints — with NO placeholder tokens at closure.
- `tests/e2e/wrk/post.lua` committed (the wrk script referenced by the protocol).
- `.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` re-stamped with the manual gate marked satisfied AND the global `status:` field flipped to `verified` (only when all three closure conditions hold).

Constraints:
- The reports directory is gitignored (`tests/e2e/reports/` is in `.gitignore`); the FILE is regenerated per measurement run. Do not attempt to commit it to git — the existence-on-disk gate is what closes the verification gap.
- This plan introduces NO source code changes; it is documentation + measurement.
- No REQ-ID maps to this plan because it is a CLAUDE.md non-functional constraint, not a v1 requirement (per planning_context guidance: "no REQ-ID — non-functional CLAUDE.md constraint; record under must_haves/truths not requirements_addressed").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
@.planning/phases/05-pool-stateful-sessions/05-03-SUMMARY.md
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate PHASE5-PERF.md skeleton</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
    - tests/e2e/wrk/post.lua
  </files>
  <read_first>
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md (human_verification block — exact thresholds + measurement commands)
    - .gitignore (confirm tests/e2e/reports/ is gitignored at line 43)
    - CLAUDE.md (the "must not be slower than the Node implementation" non-functional constraint, around line 30)
  </read_first>
  <action>
    Create the perf report skeleton file at `tests/e2e/reports/PHASE5-PERF.md`. The parent directory already exists from previous reports and is gitignored; confirm with `ls tests/e2e/reports/`. Use `Write` (not heredoc) to create the file.

    The skeleton document MUST contain exactly these top-level sections in this order: (1) `# Phase 5 Performance & RSS Report` heading with Phase / Generated / Status / Source gap metadata; (2) `## Acceptance Thresholds` table with three rows (Latency p99, Per-session RSS spread, Total RSS budget) sourcing CLAUDE.md and 05-VERIFICATION.md; (3) `## Test Environment` table with rows for host OS/arch, CPU model, Physical RAM, Go version, kiro-cli version, OTTO Gateway commit, Node impl path, Node impl commit, wrk version — each cell shows `<fill in: ...>` with the recipe command; (4) `## Measurement Protocol — Latency` with the wrk-based 4×8×30s protocol from 05-VERIFICATION.md gap 3 verbatim, including the post.lua dependency, the two-target side-by-side boot, the p50/p90/p99/p99.9/max capture, and the `p99_delta_pct` formula; (5) `### Latency Results` table with rows for Node (port 11435), Go OTTO (port 11434), Delta (Go - Node), each cell `TBD`, followed by a "Threshold check" line marked `PENDING / PASS / FAIL → AWAITING MANUAL MEASUREMENT`; (6) `## Measurement Protocol — Per-Session RSS` with the curl-loop + ps-based protocol, the 8-session population, the per-child RSS capture, the spread formula `spread_pct = (max - min) / mean * 100`, and the projection `projected_32 = 32 × avg_rss_kb / 1024 / 1024 GB`; (7) `### Per-Session RSS Results` 8-row per-session table (`rss-1` through `rss-8`, PID / RSS-KB / RSS-MB each TBD) followed by a stats table (Mean / Min / Max / Spread% / Projected 32× GB, each TBD) followed by two threshold check lines for ≤20% spread and ≤2 GB total; (8) `## Sign-Off` table with rows Measured by, Date, Latency threshold, RSS spread threshold, Total RSS budget threshold, Overall verdict, Notes / anomalies — all `TBD` or `PENDING`.

    Each measurement-result row MUST use `TBD` as the cell value. Each threshold-check line MUST contain the literal string `AWAITING MANUAL MEASUREMENT`. The acceptance thresholds (10% p99 delta; 20% spread; 32 × avg ≤ 2 GB) MUST be quoted verbatim from CLAUDE.md and 05-VERIFICATION.md so the source is traceable.

    Also create `tests/e2e/wrk/post.lua` if it does not already exist. It is a 4-line wrk Lua script that sets the POST method, sets Content-Type to application/json, reads the request body from `/tmp/req.json`, and assigns it to `wrk.body`. This file is NOT in `.gitignore` and is committed to source for future runs. The exact identifiers it needs are: `wrk.method = "POST"`, `wrk.headers["Content-Type"] = "application/json"`, an `io.open("/tmp/req.json", "rb")` call, and `wrk.body = f:read("*all")` (with nil-guard on the file handle).

    The file is gitignored, so writing it does not add it to git history. The acceptance criterion is "exists on disk after the gap-closure plan runs" per 05-VERIFICATION.md gap 3 — write the file and verify with `wc -l` (expect ≥ 80 lines for the full skeleton).
  </action>
  <verify>
    <automated>test -f tests/e2e/reports/PHASE5-PERF.md && wc -l tests/e2e/reports/PHASE5-PERF.md | awk '{exit !($1 >= 80)}' && grep -q "AWAITING MANUAL MEASUREMENT" tests/e2e/reports/PHASE5-PERF.md && grep -q "p99(Go)" tests/e2e/reports/PHASE5-PERF.md && test -f tests/e2e/wrk/post.lua</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/reports/PHASE5-PERF.md` exists on disk with ≥ 80 lines and all eight required sections.
    - All measurement-result tables show `TBD` cells with `AWAITING MANUAL MEASUREMENT` markers.
    - `tests/e2e/wrk/post.lua` exists and is committable (not in .gitignore).
    - Acceptance thresholds quoted verbatim from CLAUDE.md and 05-VERIFICATION.md.
  </acceptance_criteria>
  <done>Skeleton in place with placeholder tokens. Manual gates (Tasks 2-4) MUST replace all placeholders before closure.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual latency measurement (Node ↔ Go side-by-side)</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Task 1 output — the protocol the operator follows)
    - 05-VERIFICATION.md human_verification block (exact thresholds)
  </read_first>
  <action>
    Human operator runs the latency measurement protocol — Claude cannot automate this because (a) the Node implementation lives in a sibling repo at `../gitlab.rosetta.ericssondevops.com/loop_24/acp_server` which may not be runnable from this Claude session, (b) `wrk` is not guaranteed to be on PATH, (c) the measurement is platform-dependent and requires two long-running server processes the operator owns.

    Operator steps:
    1. Confirm the Node impl is runnable: `cd ../gitlab.rosetta.ericssondevops.com/loop_24/acp_server && ls acp-ollama-server.js`. If the file is absent on this host, the operator must consciously choose between (a) deferring the gate (do not signal `latency-measured`; do not populate the table) or (b) recording an explicit Accepted-with-Notes path (see Task 4) — but do NOT leave `NODE_IMPL_UNAVAILABLE` as the final cell value. Placeholder tokens in PHASE5-PERF.md block closure.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->
    2. Confirm `wrk` is installed: `command -v wrk`. If absent, install via the platform's package manager (`brew install wrk` on macOS / `apt install wrk` on Debian-derived Linux). Do NOT record `pending` or `placeholder` as final cell values — those tokens block closure.
    3. Follow the `## Measurement Protocol — Latency` section in `tests/e2e/reports/PHASE5-PERF.md` exactly: boot both implementations on distinct ports, warmup with `wrk -t4 -c8 -d10s`, then capture 30s with `wrk -t4 -c8 -d30s --latency -s tests/e2e/wrk/post.lua http://<port>/api/chat`.
    4. Populate the `### Latency Results` table with measured p50 / p90 / p99 / p99.9 / max for BOTH targets (Node + Go OTTO). Compute `p99_delta_pct = (p99_go - p99_node) / p99_node * 100` and write the result in the Delta row.
    5. Update the threshold check line at the bottom of the section to PASS (if `p99_delta_pct ≤ 10`) / MISSED (if 10 < `p99_delta_pct` < 25) / SEVERE (if `p99_delta_pct ≥ 25`) — recording the actual delta value. Do NOT leave `AWAITING MANUAL MEASUREMENT` in this line at closure.
    6. If Node impl is unavailable AND the operator chooses to proceed with Go-only data, the operator MUST record concrete Go-side numbers AND a `Notes:` line in the Latency section explaining the Node skip rationale. This data only closes the gate via the Accepted-with-Notes path in Task 4. If the operator does NOT have Go-side data either, STOP — do not signal `latency-measured`; rerun the measurement.
    7. Save the updated `PHASE5-PERF.md` to disk (the file is gitignored, so this is an on-disk write only).
    8. Resume Claude with signal `latency-measured <delta-percent>` OR `latency-deferred <reason>`. The signal `latency-skipped` is no longer accepted as a closure path — skipped measurements leave the gap open and Plan 05-05 cannot complete.
  </action>
  <verify>
    <automated>grep -E "p99_delta_pct|PASS|MISSED|SEVERE" tests/e2e/reports/PHASE5-PERF.md | head -3 | wc -l | awk '{exit !($1 >= 1)}'</automated>
    <human-check>The operator confirms via resume signal that Latency Results table is populated with concrete numbers (or signals `latency-deferred` and acknowledges Plan 05-05 stays incomplete). PHASE5-PERF.md has at least one non-TBD numeric cell in the Latency Results table.</human-check>
  </verify>
  <acceptance_criteria>
    - `### Latency Results` table populated with measured numeric values (Node + Go OR Go-only with documented Notes rationale).
    - Threshold check line updated to PASS / MISSED / SEVERE (NOT `AWAITING MANUAL MEASUREMENT`, NOT `BLOCKED_ON_05-04`, NOT `NODE_IMPL_UNAVAILABLE`).
    - Operator has resumed Claude with `latency-measured` (proceed) or `latency-deferred` (Plan 05-05 stays incomplete).
  </acceptance_criteria>
  <done>Latency measurements populated with concrete numbers OR the operator has accepted that the plan stays open via `latency-deferred`. No placeholder tokens remain in the Latency Results section.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual SESSION_MAX RSS sanity (8 sessions, per-child RSS)</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Task 1 output)
    - 05-VERIFICATION.md human_verification[2] (RSS thresholds)
  </read_first>
  <action>
    Human operator runs the per-session RSS measurement protocol — Claude cannot automate this because RSS values are platform- and kiro-cli-version-dependent and require a long-running gateway process the operator owns.

    Pre-requisite: Plan 05-04 is GREEN (stateful sessions actually work end-to-end against real kiro-cli, including the StatefulContinuity_TwoTurns subtest). If 05-04 is not green, this checkpoint cannot be completed because sessions cannot be populated — the operator must signal `rss-deferred BLOCKED_ON_05-04` and accept that Plan 05-05 stays incomplete. Do NOT write `BLOCKED_ON_05-04` as a cell value in PHASE5-PERF.md — that token blocks closure.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->

    Operator steps:
    1. Boot the gateway with `POOL_SIZE=4 SESSION_MAX=8 AUTH_TOKEN=hunter2 KIRO_CMD=$(which kiro-cli) ./bin/otto-gateway &`. Record the gateway PID.
    2. Spawn 8 sessions via curl loop with X-Session-Id values `rss-1` through `rss-8`. Confirm via `curl http://localhost:11434/health/agents | jq '.sessions | length'` returns 8.
    3. Capture per-child RSS using `ps -o pid,rss -p $(pgrep -P $GATEWAY_PID kiro-cli | tr '\n' ',' | sed 's/,$//')`. Distinguish the 4 pool-slot children from the 8 registry-session children by matching PIDs against `/health/agents` `current_session_id` field (registry children serve a `rss-N` session; pool children are not bound to any sid at idle).
    4. Populate the `### Per-Session RSS Results` per-session table with the 8 registry children's PID + RSS-KB + RSS-MB.
    5. Compute mean / min / max RSS across the 8 children. Compute `spread_pct = (max - min) / mean * 100` and `projected_32 = 32 × avg_rss_kb / 1024 / 1024 GB`. Populate the stats table.
    6. Update the two threshold check lines: Spread ≤ 20% → PASS / MISSED; Projected ≤ 2 GB → PASS / MISSED. Do NOT leave `AWAITING MANUAL MEASUREMENT` or `BLOCKED_ON_05-04` in these lines at closure.
    7. Save the updated `PHASE5-PERF.md` to disk (gitignored — on-disk write only).
    8. Resume Claude with signal `rss-measured <mean-mb> <spread-pct>` OR `rss-deferred <reason>`. The signal `rss-skipped` is no longer accepted as a closure path — skipped measurements leave the gap open and Plan 05-05 cannot complete.
  </action>
  <verify>
    <automated>grep -E "Mean RSS|Spread \(%\)|Projected 32" tests/e2e/reports/PHASE5-PERF.md | head -3 | wc -l | awk '{exit !($1 >= 3)}'</automated>
    <human-check>The operator confirms via resume signal that Per-Session RSS Results + stats tables are populated with concrete numbers (or signals `rss-deferred` and acknowledges Plan 05-05 stays incomplete). PHASE5-PERF.md has non-TBD numeric values in the Mean RSS, Spread, and Projected 32× rows.</human-check>
  </verify>
  <acceptance_criteria>
    - `### Per-Session RSS Results` per-session table populated with 8 PID + RSS rows (concrete numbers).
    - Stats table (Mean / Min / Max / Spread / Projected 32×) populated with concrete numbers.
    - Both threshold check lines updated to PASS / MISSED (NOT `AWAITING MANUAL MEASUREMENT`, NOT `BLOCKED_ON_05-04`).
    - Operator has resumed Claude with `rss-measured` (proceed) or `rss-deferred` (Plan 05-05 stays incomplete).
  </acceptance_criteria>
  <done>Per-session RSS measurements populated with concrete numbers OR the operator has accepted that the plan stays open via `rss-deferred`. No placeholder tokens remain in the RSS Results section.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Sign-off + 05-VERIFICATION.md re-stamp + global status flip (only when all closure conditions hold)</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Tasks 2-3 output)
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md (where the re-stamp goes; also the source of truth for the global `status:` field and the `gaps:` block)
  </read_first>
  <action>
    Human operator finalizes the perf gate by signing off the report, updating the verification record, and (only if all closure conditions hold) flipping the global phase status.

    Operator steps:

    1. Open `tests/e2e/reports/PHASE5-PERF.md` and populate the `## Sign-Off` table with concrete values (NOT TBD / pending / placeholder):
       - Measured by: operator name or handle (e.g., `corey@cmetech.io`)
       - Date: ISO date (YYYY-MM-DD)
       - Latency threshold: PASS / MISSED / SEVERE (from Task 2)
       - RSS spread threshold: PASS / MISSED (from Task 3)
       - Total RSS budget threshold: PASS / MISSED (from Task 3)
       - Overall verdict: ACCEPTED (all thresholds PASS) / ACCEPTED-WITH-NOTES (some thresholds MISSED but operator accepts; rationale required) / REJECTED (gap stays open)
       - Notes / anomalies: For ACCEPTED-WITH-NOTES, this field MUST be ≥ 1 paragraph (3+ sentences) explaining (a) which threshold was missed, (b) why the deviation is acceptable for this phase, (c) what follow-up (if any) is needed in a future phase. For ACCEPTED, a 1-2 sentence note is sufficient. For REJECTED, document what was wrong and what needs to happen before retry.

    <!-- Revision: addresses 05-REVIEWS.md HIGH-3 (placeholder token rejection + two-state closure gate) -->
    2. Run the placeholder-token audit on PHASE5-PERF.md before proceeding. The audit MUST reject closure if ANY of the following tokens appear in the file (case-sensitive; embed in the verify automation):
       - `TBD`
       - `AWAITING MANUAL MEASUREMENT`
       - `BLOCKED_ON_05-04`
       - `NODE_IMPL_UNAVAILABLE`
       - `pending`
       - `placeholder`

       Run: `! grep -E '(TBD|AWAITING MANUAL MEASUREMENT|BLOCKED_ON_05-04|NODE_IMPL_UNAVAILABLE|placeholder|pending)' tests/e2e/reports/PHASE5-PERF.md`. If ANY token is found, STOP — close the report by returning to Tasks 2-3 (or signal `signed-off rejected` and accept that Plan 05-05 stays incomplete). The gate cannot be closed while these tokens remain.

    3. Open `.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` and append a new heading section after any prior re-verification section (or at end-of-file). The section heading MUST be EXACTLY one of these two forms (no other strings close the gate):

       - Form A (full measurement): `## Manual Perf + RSS Gate — Satisfied`
       - Form B (operator-acknowledged deviation): `## Manual Perf + RSS Gate — Accepted-with-Notes (<operator>, <YYYY-MM-DD>)` — the parenthetical MUST contain operator name + ISO date, e.g., `## Manual Perf + RSS Gate — Accepted-with-Notes (corey@cmetech.io, 2026-05-26)`.

       Under that heading record: a reference to `tests/e2e/reports/PHASE5-PERF.md`, the three threshold outcomes (latency / spread / total), the Overall verdict, the Measured by handle, and a Notes line (≥ 1 paragraph for Accepted-with-Notes). End with: "Gap 3 from the original verification report is now <closed / closed-with-notes / NOT-closed>." matching the Overall verdict.

    4. Verdict mapping:
       - Overall verdict ACCEPTED → use Form A heading → status `closed`.
       - Overall verdict ACCEPTED-WITH-NOTES → use Form B heading → status `closed-with-notes` (caveats recorded but gap is closed for phase purposes).
       - Overall verdict REJECTED → DO NOT add either heading → status `NOT-closed`. STOP. The gap stays open until a passing measurement is recorded; open a follow-up plan to investigate the regression.

    5. **Global phase status flip (Plan 05-05's exclusive responsibility — Plan 05-04 did NOT touch this).** After the verification re-stamp section is written, check whether ALL THREE closure conditions hold:
       (a) The frontmatter `gaps:` block in 05-VERIFICATION.md does NOT contain SC3 or SC4 entries (Plan 05-04 removed them).
       (b) PHASE5-PERF.md is `Satisfied` (Form A) OR `Accepted-with-Notes` (Form B).
       (c) PHASE5-PERF.md contains NO placeholder tokens (the audit from step 2 passed).

       If ALL THREE hold: update the frontmatter top-level `status:` field in 05-VERIFICATION.md to `verified`. If ANY condition fails: leave the `status:` field as-is and surface which condition failed. Plan 05-05 cannot be marked complete if the status flip cannot happen because of placeholder tokens.

    6. Resume Claude with signal `signed-off accepted` / `signed-off accepted-with-notes` / `signed-off rejected`. Verdict drives whether Plan 05-05 completes successfully (rejected = Plan 05-05 fails).
  </action>
  <verify>
    <automated>! grep -E '(TBD|AWAITING MANUAL MEASUREMENT|BLOCKED_ON_05-04|NODE_IMPL_UNAVAILABLE|placeholder|pending)' tests/e2e/reports/PHASE5-PERF.md && ( grep -q '^## Manual Perf + RSS Gate — Satisfied' .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md || grep -qE '^## Manual Perf \+ RSS Gate — Accepted-with-Notes \(.+, [0-9]{4}-[0-9]{2}-[0-9]{2}\)$' .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md ) && grep -q "Overall verdict" tests/e2e/reports/PHASE5-PERF.md</automated>
    <human-check>VERIFICATION.md contains either the `## Manual Perf + RSS Gate — Satisfied` heading OR the `## Manual Perf + RSS Gate — Accepted-with-Notes (<operator>, <YYYY-MM-DD>)` heading (exact form, with operator + date in parentheses for the latter); PHASE5-PERF.md Sign-Off table has all fields populated (no TBD / pending / placeholder remaining); operator has resumed Claude with `signed-off <verdict>` and the verdict is not REJECTED. If all three closure conditions hold (SC3/SC4 absent from gaps:, PHASE5-PERF.md Satisfied or Accepted-with-Notes, no placeholder tokens), the global `status:` field in 05-VERIFICATION.md frontmatter is flipped to `verified`.</human-check>
  </verify>
  <acceptance_criteria>
    - PHASE5-PERF.md Sign-Off table fully populated with concrete values (no TBD / pending / placeholder remaining).
    - PHASE5-PERF.md passes the placeholder-token audit: `! grep -E '(TBD|AWAITING MANUAL MEASUREMENT|BLOCKED_ON_05-04|NODE_IMPL_UNAVAILABLE|placeholder|pending)' tests/e2e/reports/PHASE5-PERF.md` exits 0.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->
    - 05-VERIFICATION.md contains EXACTLY one of the two closure headings: `## Manual Perf + RSS Gate — Satisfied` OR `## Manual Perf + RSS Gate — Accepted-with-Notes (<operator>, <YYYY-MM-DD>)`. No other heading closes the gate.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->
    - For Accepted-with-Notes: the Notes section in PHASE5-PERF.md is ≥ 1 paragraph (3+ sentences) AND the 05-VERIFICATION.md heading contains the operator's name + ISO date in the parenthetical.
    - Overall verdict is ACCEPTED or ACCEPTED-WITH-NOTES (not REJECTED).
    - Operator has resumed Claude with `signed-off accepted` or `signed-off accepted-with-notes`.
    - If ALL three closure conditions hold (SC3/SC4 absent from `gaps:`, closure heading present, no placeholder tokens), the global `status:` field in 05-VERIFICATION.md frontmatter is flipped to `verified`. Otherwise it remains UNCHANGED and the failing condition is documented.  <!-- Revision: addresses 05-REVIEWS.md LOW-2 + HIGH-3 -->
  </acceptance_criteria>
  <done>The Manual Perf + RSS Gate is closed via either `Satisfied` or `Accepted-with-Notes` (with operator name + ISO date). PHASE5-PERF.md contains no placeholder tokens. The global phase status flip to `verified` is gated on all three closure conditions holding.</done>
</task>

</tasks>

<verification>
Plan 05-05 closure is verified by:

1. **Skeleton exists.** `test -f tests/e2e/reports/PHASE5-PERF.md && wc -l tests/e2e/reports/PHASE5-PERF.md` returns a path + line count ≥ 80.
2. **Skeleton contains the right scaffolding (only valid before Tasks 2-4 run).** After Task 1 only: `grep -c "AWAITING MANUAL MEASUREMENT"` returns > 0. After Tasks 2-4 complete and the gate closes: this count is exactly 0.
3. **wrk Lua script committed.** `test -f tests/e2e/wrk/post.lua`.
4. **Sign-off populated.** After Task 4, the Sign-Off table contains NO `TBD` entries for "Measured by" / "Date" / "Overall verdict".
5. **No placeholder tokens at closure.** `! grep -E '(TBD|AWAITING MANUAL MEASUREMENT|BLOCKED_ON_05-04|NODE_IMPL_UNAVAILABLE|placeholder|pending)' tests/e2e/reports/PHASE5-PERF.md` exits 0.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->
6. **VERIFICATION.md re-stamped via one of exactly two valid closure headings.** Either `grep -q '^## Manual Perf + RSS Gate — Satisfied'` exits 0 OR `grep -qE '^## Manual Perf \+ RSS Gate — Accepted-with-Notes \(.+, [0-9]{4}-[0-9]{2}-[0-9]{2}\)$'` exits 0. Any other heading does NOT close the gate.  <!-- Revision: addresses 05-REVIEWS.md HIGH-3 -->
7. **Overall verdict is not REJECTED.** If the operator records REJECTED, Plan 05-05 FAILS — gap 3 remains open and a follow-up plan is required.
8. **Global phase status flip is conditional.** The frontmatter top-level `status:` field in 05-VERIFICATION.md flips to `verified` IFF all three conditions hold: (a) SC3/SC4 absent from `gaps:`, (b) one of the two closure headings present, (c) no placeholder tokens in PHASE5-PERF.md. Otherwise status remains UNCHANGED.  <!-- Revision: addresses 05-REVIEWS.md LOW-2 + HIGH-3 -->
</verification>

<success_criteria>
Plan 05-05 complete when:

- [ ] `tests/e2e/reports/PHASE5-PERF.md` exists on disk with skeleton structure (Task 1)
- [ ] `tests/e2e/wrk/post.lua` exists and is committable (Task 1)
- [ ] Latency table populated by human operator with concrete numeric values (Task 2)
- [ ] Per-session RSS table populated by human operator with concrete numeric values (Task 3)
- [ ] Sign-Off table populated with verdict (Task 4)
- [ ] PHASE5-PERF.md contains NO placeholder tokens: `TBD`, `AWAITING MANUAL MEASUREMENT`, `BLOCKED_ON_05-04`, `NODE_IMPL_UNAVAILABLE`, `placeholder`, `pending` (Task 4)
- [ ] 05-VERIFICATION.md has EXACTLY ONE of: `## Manual Perf + RSS Gate — Satisfied` OR `## Manual Perf + RSS Gate — Accepted-with-Notes (<operator>, <YYYY-MM-DD>)` (Task 4)
- [ ] For Accepted-with-Notes path: Notes in PHASE5-PERF.md Sign-Off section is ≥ 1 paragraph (3+ sentences)
- [ ] Overall verdict is ACCEPTED or ACCEPTED-WITH-NOTES (not REJECTED)
- [ ] If SC3/SC4 absent from `gaps:` AND closure heading present AND no placeholder tokens: global `status:` flipped to `verified` (Task 4)
</success_criteria>

<output>
Create `.planning/phases/05-pool-stateful-sessions/05-05-SUMMARY.md` when done with:
- Skeleton creation status (file path + line count)
- Latency measurement summary (delta % — concrete number, not skip reason)
- RSS measurement summary (mean MB / spread % / projected GB — concrete numbers, not skip reason)
- Overall sign-off verdict (ACCEPTED / ACCEPTED-WITH-NOTES)
- Placeholder-token audit result (must be PASS)
- Closure heading form used (Satisfied vs Accepted-with-Notes with operator + date)
- VERIFICATION.md re-stamp evidence
- Global `status:` field flip result (verified / unchanged + reason)
- Gap 3 closure status (closed / closed-with-notes / NOT-closed)
</output>
