---
phase: 05-pool-stateful-sessions
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on:
  - 05-04
files_modified:
  - tests/e2e/reports/PHASE5-PERF.md
  - tests/e2e/wrk/post.lua
  - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
autonomous: false
requirements: []
requirements_addressed: []
tags:
  - perf
  - rss
  - human-verification
  - claude-md-non-functional
  - gap-closure

must_haves:
  truths:
    - "`tests/e2e/reports/PHASE5-PERF.md` exists on disk after this plan runs (the SUMMARY-claimed placeholder was never created — gap 3 from 05-VERIFICATION.md)."
    - "The report contains: measurement protocol, expected acceptance thresholds, placeholder p99 latency table, placeholder per-session RSS table, sign-off section."
    - "Each manual measurement task is explicitly marked `AWAITING MANUAL MEASUREMENT` until the human operator fills it in — Claude does NOT auto-populate numbers."
    - "Acceptance thresholds are encoded verbatim from CLAUDE.md non-functional constraint and 05-VERIFICATION.md gap 3: p99(Go) ≤ p99(Node) + 10% under 4×8×30s wrk; per-session RSS within ±20% of mean; 32 × avg RSS ≤ 2 GB on an 8 GB host."
    - "The manual measurement steps cite exact commands (wrk invocation, ps -o pid,rss command) so a future operator can reproduce them without re-reading 05-VERIFICATION.md."
    - "After human sign-off, 05-VERIFICATION.md is updated with a re-verification line confirming the manual gate is satisfied."
  artifacts:
    - path: "tests/e2e/reports/PHASE5-PERF.md"
      provides: "Phase 5 perf+RSS report skeleton — auto-generated; numbers filled in by human operator at human-verify checkpoints."
      contains: "Acceptance Thresholds"
    - path: ".planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md"
      provides: "Re-verification entry recording satisfaction of human_verification gate (perf + RSS)."
      contains: "Re-verification"
  key_links:
    - from: "tests/e2e/reports/PHASE5-PERF.md::Acceptance Thresholds"
      to: "CLAUDE.md::Performance constraint"
      via: "Threshold values cite CLAUDE.md as their source"
      pattern: "CLAUDE\\.md|Node implementation"
    - from: ".planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md::human_verification"
      to: "tests/e2e/reports/PHASE5-PERF.md"
      via: "VERIFICATION re-stamp cross-references the report path"
      pattern: "tests/e2e/reports/PHASE5-PERF\\.md"
---

<objective>
Close Phase 5 gap 3 from 05-VERIFICATION.md: `tests/e2e/reports/PHASE5-PERF.md` was claimed as an auto-approved placeholder in the 05-03 SUMMARY, but the file does not exist on disk. The CLAUDE.md non-functional constraint ("must not be slower than the Node implementation") is unmeasured.

This plan ships in two halves:

1. **Autonomous (Task 1):** Create the report skeleton with measurement protocol, acceptance thresholds, placeholder tables, and clear `AWAITING MANUAL MEASUREMENT` markers — so the file exists on disk and the structure is locked.

2. **Manual gates (Tasks 2 + 3 + 4):** Human-action checkpoints where the operator runs the measurements side-by-side (Node ↔ Go) and populates the tables. These cannot be automated: the Node implementation lives at `../gitlab.rosetta.ericssondevops.com/loop_24/acp_server` (separate repo), wrk/ab needs to be installed, RSS measurements are platform-dependent.

Purpose: Phase 5's load-bearing non-functional constraint becomes measured, not claimed. The report becomes the authoritative artifact for future regressions ("did Phase 6 slow us down vs this baseline?").

Output:
- `tests/e2e/reports/PHASE5-PERF.md` populated by human at the three checkpoints.
- `tests/e2e/wrk/post.lua` committed (the wrk script referenced by the protocol).
- `.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` re-stamped with the manual gate marked satisfied.

Constraints:
- The reports directory is gitignored (`tests/e2e/reports/` is in `.gitignore`); the FILE is regenerated per measurement run. Do not attempt to commit it to git — the existence-on-disk gate is what closes the verification gap.
- This plan introduces NO source code changes; it is documentation + measurement.
- No REQ-ID maps to this plan because it is a CLAUDE.md non-functional constraint, not a v1 requirement (per planning_context guidance: "no REQ-ID — non-functional CLAUDE.md constraint; record under must_haves/truths not requirements_addressed").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
@.planning/phases/05-pool-stateful-sessions/05-03-SUMMARY.md
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate PHASE5-PERF.md skeleton</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
    - tests/e2e/wrk/post.lua
  </files>
  <read_first>
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md (human_verification block — exact thresholds + measurement commands)
    - .gitignore (confirm tests/e2e/reports/ is gitignored at line 43)
    - CLAUDE.md (the "must not be slower than the Node implementation" non-functional constraint, around line 30)
  </read_first>
  <action>
    Create the perf report skeleton file at `tests/e2e/reports/PHASE5-PERF.md`. The parent directory already exists from previous reports and is gitignored; confirm with `ls tests/e2e/reports/`. Use `Write` (not heredoc) to create the file.

    The skeleton document MUST contain exactly these top-level sections in this order: (1) `# Phase 5 Performance & RSS Report` heading with Phase / Generated / Status / Source gap metadata; (2) `## Acceptance Thresholds` table with three rows (Latency p99, Per-session RSS spread, Total RSS budget) sourcing CLAUDE.md and 05-VERIFICATION.md; (3) `## Test Environment` table with rows for host OS/arch, CPU model, Physical RAM, Go version, kiro-cli version, OTTO Gateway commit, Node impl path, Node impl commit, wrk version — each cell shows `<fill in: ...>` with the recipe command; (4) `## Measurement Protocol — Latency` with the wrk-based 4×8×30s protocol from 05-VERIFICATION.md gap 3 verbatim, including the post.lua dependency, the two-target side-by-side boot, the p50/p90/p99/p99.9/max capture, and the `p99_delta_pct` formula; (5) `### Latency Results` table with rows for Node (port 11435), Go OTTO (port 11434), Delta (Go - Node), each cell `TBD`, followed by a "Threshold check" line marked `PENDING / PASS / FAIL → AWAITING MANUAL MEASUREMENT`; (6) `## Measurement Protocol — Per-Session RSS` with the curl-loop + ps-based protocol, the 8-session population, the per-child RSS capture, the spread formula `spread_pct = (max - min) / mean * 100`, and the projection `projected_32 = 32 × avg_rss_kb / 1024 / 1024 GB`; (7) `### Per-Session RSS Results` 8-row per-session table (`rss-1` through `rss-8`, PID / RSS-KB / RSS-MB each TBD) followed by a stats table (Mean / Min / Max / Spread% / Projected 32× GB, each TBD) followed by two threshold check lines for ≤20% spread and ≤2 GB total; (8) `## Sign-Off` table with rows Measured by, Date, Latency threshold, RSS spread threshold, Total RSS budget threshold, Overall verdict, Notes / anomalies — all `TBD` or `PENDING`.

    Each measurement-result row MUST use `TBD` as the cell value. Each threshold-check line MUST contain the literal string `AWAITING MANUAL MEASUREMENT`. The acceptance thresholds (10% p99 delta; 20% spread; 32 × avg ≤ 2 GB) MUST be quoted verbatim from CLAUDE.md and 05-VERIFICATION.md so the source is traceable.

    Also create `tests/e2e/wrk/post.lua` if it does not already exist. It is a 4-line wrk Lua script that sets the POST method, sets Content-Type to application/json, reads the request body from `/tmp/req.json`, and assigns it to `wrk.body`. This file is NOT in `.gitignore` and is committed to source for future runs. The exact identifiers it needs are: `wrk.method = "POST"`, `wrk.headers["Content-Type"] = "application/json"`, an `io.open("/tmp/req.json", "rb")` call, and `wrk.body = f:read("*all")` (with nil-guard on the file handle).

    The file is gitignored, so writing it does not add it to git history. The acceptance criterion is "exists on disk after the gap-closure plan runs" per 05-VERIFICATION.md gap 3 — write the file and verify with `wc -l` (expect ≥ 80 lines for the full skeleton).
  </action>
  <verify>
    <automated>test -f tests/e2e/reports/PHASE5-PERF.md && wc -l tests/e2e/reports/PHASE5-PERF.md | awk '{exit !($1 >= 80)}' && grep -q "AWAITING MANUAL MEASUREMENT" tests/e2e/reports/PHASE5-PERF.md && grep -q "p99(Go)" tests/e2e/reports/PHASE5-PERF.md && test -f tests/e2e/wrk/post.lua</automated>
  </verify>
  <done>
    - `tests/e2e/reports/PHASE5-PERF.md` exists on disk with ≥ 80 lines and all eight required sections.
    - All measurement-result tables show `TBD` cells with `AWAITING MANUAL MEASUREMENT` markers.
    - `tests/e2e/wrk/post.lua` exists and is committable (not in .gitignore).
    - Acceptance thresholds quoted verbatim from CLAUDE.md and 05-VERIFICATION.md.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual latency measurement (Node ↔ Go side-by-side)</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Task 1 output — the protocol the operator follows)
    - 05-VERIFICATION.md human_verification block (exact thresholds)
  </read_first>
  <action>
    Human operator runs the latency measurement protocol — Claude cannot automate this because (a) the Node implementation lives in a sibling repo at `../gitlab.rosetta.ericssondevops.com/loop_24/acp_server` which may not be runnable from this Claude session, (b) `wrk` is not guaranteed to be on PATH, (c) the measurement is platform-dependent and requires two long-running server processes the operator owns.

    Operator steps:
    1. Confirm the Node impl is runnable: `cd ../gitlab.rosetta.ericssondevops.com/loop_24/acp_server && ls acp-ollama-server.js`. If the file is absent on this host, skip this checkpoint and record `NODE_IMPL_UNAVAILABLE` in the threshold-check line — Go-side numbers can still be captured as a baseline.
    2. Confirm `wrk` is installed: `command -v wrk`. If absent, install via the platform's package manager (`brew install wrk` on macOS / `apt install wrk` on Debian-derived Linux) or record blocked.
    3. Follow the `## Measurement Protocol — Latency` section in `tests/e2e/reports/PHASE5-PERF.md` exactly: boot both implementations on distinct ports, warmup with `wrk -t4 -c8 -d10s`, then capture 30s with `wrk -t4 -c8 -d30s --latency -s tests/e2e/wrk/post.lua http://<port>/api/chat`.
    4. Populate the `### Latency Results` table with measured p50 / p90 / p99 / p99.9 / max for BOTH targets (Node + Go OTTO). Compute `p99_delta_pct = (p99_go - p99_node) / p99_node * 100` and write the result in the Delta row.
    5. Update the threshold check line at the bottom of the section to PASS (if `p99_delta_pct ≤ 10`) / MISSED (if 10 < `p99_delta_pct` < 25) / SEVERE (if `p99_delta_pct ≥ 25`) — recording the actual delta value.
    6. If Node impl is unavailable, populate the Go-side row only and record `BASELINE_ONLY (Node unavailable)` in the threshold-check line.
    7. Save the updated `PHASE5-PERF.md` to disk (the file is gitignored, so this is an on-disk write only).
    8. Resume Claude with signal `latency-measured <delta-percent>` OR `latency-skipped <reason>`.
  </action>
  <verify>
    <automated>grep -E "p99_delta_pct|BASELINE_ONLY|NODE_IMPL_UNAVAILABLE|PASS|MISSED|SEVERE" tests/e2e/reports/PHASE5-PERF.md | head -5 | wc -l | awk '{exit !($1 >= 1)}'</automated>
    <human-check>The operator confirms via resume signal that Latency Results table is populated (or the skip reason is recorded). PHASE5-PERF.md has at least one non-TBD cell in the Latency Results table.</human-check>
  </verify>
  <done>
    - `### Latency Results` table populated with measured values OR skip reason recorded.
    - Threshold check line updated to PASS / MISSED / SEVERE / BASELINE_ONLY / NODE_IMPL_UNAVAILABLE.
    - Operator has resumed Claude with `latency-measured` or `latency-skipped` signal.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual SESSION_MAX RSS sanity (8 sessions, per-child RSS)</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Task 1 output)
    - 05-VERIFICATION.md human_verification[2] (RSS thresholds)
  </read_first>
  <action>
    Human operator runs the per-session RSS measurement protocol — Claude cannot automate this because RSS values are platform- and kiro-cli-version-dependent and require a long-running gateway process the operator owns.

    Pre-requisite: Plan 05-04 is GREEN (stateful sessions actually work end-to-end against real kiro-cli). If 05-04 is not green, this checkpoint cannot be completed because sessions cannot be populated — operator records `BLOCKED_ON_05-04` and signals `rss-skipped`.

    Operator steps:
    1. Boot the gateway with `POOL_SIZE=4 SESSION_MAX=8 AUTH_TOKEN=hunter2 KIRO_CMD=$(which kiro-cli) ./bin/otto-gateway &`. Record the gateway PID.
    2. Spawn 8 sessions via curl loop with X-Session-Id values `rss-1` through `rss-8`. Confirm via `curl http://localhost:11434/health/agents | jq '.sessions | length'` returns 8.
    3. Capture per-child RSS using `ps -o pid,rss -p $(pgrep -P $GATEWAY_PID kiro-cli | tr '\n' ',' | sed 's/,$//')`. Distinguish the 4 pool-slot children from the 8 registry-session children by matching PIDs against `/health/agents` `current_session_id` field (registry children serve a `rss-N` session; pool children are not bound to any sid at idle).
    4. Populate the `### Per-Session RSS Results` per-session table with the 8 registry children's PID + RSS-KB + RSS-MB.
    5. Compute mean / min / max RSS across the 8 children. Compute `spread_pct = (max - min) / mean * 100` and `projected_32 = 32 × avg_rss_kb / 1024 / 1024 GB`. Populate the stats table.
    6. Update the two threshold check lines: Spread ≤ 20% → PASS / MISSED; Projected ≤ 2 GB → PASS / MISSED.
    7. Save the updated `PHASE5-PERF.md` to disk (gitignored — on-disk write only).
    8. Resume Claude with signal `rss-measured <mean-mb> <spread-pct>` OR `rss-skipped <reason>`.
  </action>
  <verify>
    <automated>grep -E "Mean RSS|Spread \(%\)|Projected 32" tests/e2e/reports/PHASE5-PERF.md | head -3 | wc -l | awk '{exit !($1 >= 3)}'</automated>
    <human-check>The operator confirms via resume signal that Per-Session RSS Results + stats tables are populated (or the skip reason is recorded). PHASE5-PERF.md has non-TBD values in the Mean RSS, Spread, and Projected 32× rows.</human-check>
  </verify>
  <done>
    - `### Per-Session RSS Results` per-session table populated with 8 PID + RSS rows OR skip reason recorded.
    - Stats table (Mean / Min / Max / Spread / Projected 32×) populated.
    - Both threshold check lines updated to PASS / MISSED.
    - Operator has resumed Claude with `rss-measured` or `rss-skipped` signal.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Sign-off + 05-VERIFICATION.md re-stamp</name>
  <files>
    - tests/e2e/reports/PHASE5-PERF.md
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md
  </files>
  <read_first>
    - tests/e2e/reports/PHASE5-PERF.md (Tasks 2-3 output)
    - .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md (where the re-stamp goes)
  </read_first>
  <action>
    Human operator finalizes the perf gate by signing off the report and updating the verification record.

    Operator steps:
    1. Open `tests/e2e/reports/PHASE5-PERF.md` and populate the `## Sign-Off` table:
       - Measured by: operator name or handle
       - Date: ISO date (YYYY-MM-DD)
       - Latency threshold: PASS / MISSED / SKIPPED (from Task 2)
       - RSS spread threshold: PASS / MISSED / BLOCKED_ON_05-04 (from Task 3)
       - Total RSS budget threshold: PASS / MISSED / BLOCKED_ON_05-04 (from Task 3)
       - Overall verdict: ACCEPTED / ACCEPTED-WITH-NOTES / REJECTED
       - Notes / anomalies: 1-3 sentences of free text

    2. Open `.planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` and append a new heading section after any prior re-verification section (or at end-of-file). The section heading is `## Manual Perf + RSS Gate — Satisfied <ISO date>`. Under that heading record: a reference to `tests/e2e/reports/PHASE5-PERF.md`, the three threshold outcomes (latency / spread / total), the Overall verdict, the Measured by handle, and a Notes line. End with: "Gap 3 from the original verification report is now <closed / closed-with-notes / NOT-closed>." matching the Overall verdict.

    3. Verdict mapping:
       - Overall verdict ACCEPTED → "closed".
       - Overall verdict ACCEPTED-WITH-NOTES → "closed-with-notes" (caveats recorded but gap is closed for phase purposes).
       - Overall verdict REJECTED → "NOT-closed" — STOP, do NOT mark the manual gate satisfied. The gap stays open until a passing measurement is recorded; open a follow-up plan to investigate the regression.

    4. Resume Claude with signal `signed-off accepted` / `signed-off accepted-with-notes` / `signed-off rejected`. Verdict drives whether Plan 05-05 completes successfully (rejected = Plan 05-05 fails).
  </action>
  <verify>
    <automated>grep -q "Manual Perf + RSS Gate — Satisfied" .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md && grep -q "Overall verdict" tests/e2e/reports/PHASE5-PERF.md</automated>
    <human-check>VERIFICATION.md contains the re-stamp section dated to today; PHASE5-PERF.md Sign-Off table has all fields populated (no TBD remaining); operator has resumed Claude with `signed-off <verdict>` and the verdict is not REJECTED (REJECTED reopens the gap and fails this plan).</human-check>
  </verify>
  <done>
    - PHASE5-PERF.md Sign-Off table fully populated (no TBD in that section).
    - 05-VERIFICATION.md contains a `## Manual Perf + RSS Gate — Satisfied <date>` section linking to the report.
    - Overall verdict is ACCEPTED or ACCEPTED-WITH-NOTES (not REJECTED).
    - Operator has resumed Claude with `signed-off <verdict>`.
  </done>
</task>

</tasks>

<verification>
Plan 05-05 closure is verified by:

1. **Skeleton exists.** `test -f tests/e2e/reports/PHASE5-PERF.md && wc -l tests/e2e/reports/PHASE5-PERF.md` returns a path + line count ≥ 80.
2. **Skeleton contains the right scaffolding.** `grep -c "AWAITING MANUAL MEASUREMENT"` returns > 0; `grep -q "p99(Go)"` returns 0.
3. **wrk Lua script committed.** `test -f tests/e2e/wrk/post.lua`.
4. **Sign-off populated.** After Task 4, the Sign-Off table contains NO `TBD` entries for "Measured by" / "Date" / "Overall verdict".
5. **VERIFICATION.md re-stamped.** `grep -q "Manual Perf + RSS Gate — Satisfied" .planning/phases/05-pool-stateful-sessions/05-VERIFICATION.md` returns 0 (success).
6. **Overall verdict is not REJECTED.** If the operator records REJECTED, Plan 05-05 FAILS — gap 3 remains open and a follow-up plan is required.
</verification>

<success_criteria>
Plan 05-05 complete when:

- [ ] `tests/e2e/reports/PHASE5-PERF.md` exists on disk with skeleton structure (Task 1)
- [ ] `tests/e2e/wrk/post.lua` exists and is committable (Task 1)
- [ ] Latency table populated by human operator (Task 2) — or SKIPPED with recorded reason
- [ ] Per-session RSS table populated by human operator (Task 3) — or SKIPPED if 05-04 is not green
- [ ] Sign-Off table populated with verdict (Task 4)
- [ ] 05-VERIFICATION.md has a `## Manual Perf + RSS Gate — Satisfied <date>` section (Task 4)
- [ ] Overall verdict is ACCEPTED or ACCEPTED-WITH-NOTES (not REJECTED — REJECTED reopens the gap)
</success_criteria>

<output>
Create `.planning/phases/05-pool-stateful-sessions/05-05-SUMMARY.md` when done with:
- Skeleton creation status (file path + line count)
- Latency measurement summary (delta % or SKIP reason)
- RSS measurement summary (mean MB / spread % / projected GB or SKIP reason)
- Overall sign-off verdict
- VERIFICATION.md re-stamp evidence
- Gap 3 closure status (closed / closed-with-notes / NOT-closed)
</output>
