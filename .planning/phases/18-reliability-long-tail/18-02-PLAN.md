---
phase: 18-reliability-long-tail
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/acp/client.go
  - internal/acp/regression_rel_obsv_03_test.go
  - internal/pool/pool.go
  - internal/pool/pool_test.go
  - internal/pool/exit_watcher.go
  - internal/pool/regression_rel_obsv_02_test.go
  - internal/pool/regression_rel_http_07_test.go
  - internal/engine/engine.go
  - internal/engine/regression_rel_http_07_test.go
  - internal/admin/tail.go
  - internal/admin/regression_rel_http_07_test.go
  - internal/admin/regression_rel_obsv_04_test.go
  - internal/adapter/ollama/handlers.go
  - internal/adapter/ollama/regression_rel_http_06_test.go
  - internal/config/config.go
  - cmd/otto-gateway/main.go
autonomous: true
requirements: [REL-HTTP-06, REL-HTTP-07, REL-OBSV-02, REL-OBSV-03, REL-OBSV-04]
requirements_addressed: [REL-HTTP-06, REL-HTTP-07, REL-OBSV-02, REL-OBSV-03, REL-OBSV-04]
tags: [go, observability, slog, goroutine-recovery, reliability]
threat_model_summary:
  - "T-18-02-T: log injection via kiro-cli stderr — mitigated by slog string-field JSON escape (D-18-04)"
  - "T-18-02-D: scanner overflow / goroutine leak — mitigated by 1MB byte-cap + bufio.Reader.ReadString + goleak.VerifyNone (D-18-04)"
  - "T-18-02-I: panic stack-trace info disclosure — mitigated by existing engine.go panic-recover template parity (D-18-07)"
  - "T-18-02-S: divergent log-tail path resolution — mitigated by Config.AdminTailPath single source of truth (D-18-08)"

must_haves:
  truths:
    - "RESOLVED OQ-1 (engine watchdog identity): D-18-07 covers ALL 4 sites — (a) admin tailer goroutine in internal/admin/tail.go run() body, (b) pool ctx-watcher goroutine in internal/pool/pool.go ~line 859, (c) pool exit-watcher goroutine in internal/pool/exit_watcher.go ~line 32, AND (d) the context.AfterFunc CALLBACK BODY at internal/engine/engine.go:255-265. All four get the same defer-recover wrapper."
    - "RESOLVED OQ-3 (D-18-05 success reason): byte-exact string `lazy-respawn-success`"
    - "RESOLVED OQ-4 (D-18-04 overflow): use bufio.Reader.ReadString('\\n') mirroring internal/admin/tail.go, NOT bufio.Scanner. bufio.Scanner STOPS on bufio.ErrTooLong; ReadString continues and the per-line byte-cap is applied AFTER read by trimming the returned string."
    - "kiro-cli stderr is captured via per-line ReadString goroutine; slog.Warn fields: worker_pid + line. slot_id is OMITTED for v1.10.3 (CONTEXT.md permits)."
    - "Worker recovery slog.Info from inside Pool.respawnSlot success path; field key for slot is `label` (mirrors exit_watcher.go:42 death log per RESEARCH.md Pattern 3 / Pitfall 5 recommendation)."
    - "Ollama streaming eng.Run failure emits slog.Warn BEFORE the error frame write; field set mirrors REL-HTTP-03: worker_pid=0, bytes_streamed=0, session_id, request_id, err, kiro_exit_code (only when errors.As exec.ExitError)."
    - "Panic-recover at all 4 sites logs at slog.Error with fields site, panic, stack."
    - "Config.AdminTailPath (NEW string field) is populated in config.Load via deriveChatTraceFile; writer + tailer both read from it; tailer open failure logs at WARN (not Debug)."
    - "Phase-close criterion 4: grep -rn `cmd.Stderr = os.Stderr` internal/ cmd/ returns NO production-code hits after this plan lands."
  artifacts:
    - path: internal/acp/regression_rel_obsv_03_test.go
      provides: REL-OBSV-03 stderr scanner + goleak regression
      contains: "TestRegression_REL_OBSV_03"
    - path: internal/pool/regression_rel_obsv_02_test.go
      provides: REL-OBSV-02 worker recovery INFO regression
      contains: "TestRegression_REL_OBSV_02"
    - path: internal/pool/regression_rel_http_07_test.go
      provides: REL-HTTP-07 pool ctx-watcher + exit-watcher panic-recover
      contains: "TestRegression_REL_HTTP_07_PoolCtxWatcher,TestRegression_REL_HTTP_07_PoolExitWatcher"
    - path: internal/admin/regression_rel_http_07_test.go
      provides: REL-HTTP-07 admin tailer panic-recover
      contains: "TestRegression_REL_HTTP_07_AdminTailer"
    - path: internal/engine/regression_rel_http_07_test.go
      provides: REL-HTTP-07 engine AfterFunc callback panic-recover
      contains: "TestRegression_REL_HTTP_07_EngineAfterFunc"
    - path: internal/admin/regression_rel_obsv_04_test.go
      provides: REL-OBSV-04 single source path + WARN on missing
      contains: "TestRegression_REL_OBSV_04"
    - path: internal/adapter/ollama/regression_rel_http_06_test.go
      provides: REL-HTTP-06 Ollama eng.Run WARN regression
      contains: "TestRegression_REL_HTTP_06"
    - path: internal/config/config.go
      provides: Config.AdminTailPath NEW field populated in Load
    - path: cmd/otto-gateway/main.go
      provides: tailer wiring reads cfg.AdminTailPath single source
  key_links:
    - from: internal/acp/client.go
      to: cfg.Logger.Warn("kiro-cli stderr", ...)
      via: bufio.Reader.ReadString
      pattern: "kiro-cli stderr"
    - from: internal/pool/pool.go respawnSlot success
      to: p.cfg.Logger.Info("pool: slot recovered", ...)
      via: slog.Info after slot.dead = false + p.mu.Unlock
      pattern: "pool: slot recovered"
    - from: internal/adapter/ollama/handlers.go
      to: a.cfg.Logger.Warn("ollama: streaming eng.Run failed", ...)
      via: slog.Warn BEFORE writeError
      pattern: "ollama: streaming eng.Run failed"
    - from: 4 panic sites
      to: logger.Error("goroutine panic recovered", ...)
      via: defer recover
      pattern: "goroutine panic recovered"
    - from: internal/config/config.go Config.AdminTailPath
      to: cmd/otto-gateway/main.go tailer
      via: cfg.AdminTailPath
      pattern: "AdminTailPath"
---

<objective>
Close REL-HTTP-06, REL-HTTP-07, REL-OBSV-02, REL-OBSV-03, REL-OBSV-04 — five
observability-symmetry items removing the gateway's asymmetric logs (worker
death without recovery, kiro-cli stderr leaking to bare os.Stderr, Ollama
streaming eng.Run failures silently dropped, four panic sites that would
crash the process) and the writer-vs-tailer split-brain on the log-tail path.

Purpose: After this plan lands, an operator tailing the structured log gets
a complete causal picture of worker lifecycle (death → recovery), kiro-cli
warnings inline with gateway events, every adapter error path surfaced
symmetrically, and a contract that no background goroutine can take down
the gateway silently.

Output:
  - 5 RED→GREEN test cycles (grouped into 3 implementation tasks for
    sensible commit granularity)
  - Config.AdminTailPath NEW field coordinated with 18-01 (soft sequence —
    see Pre-coordination note below)
  - make ci exit 0
  - grep -rn "cmd.Stderr = os.Stderr" internal/ cmd/ returns no hits
  - go test -race ./internal/... clean including goleak.VerifyNone gates
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-reliability-long-tail/18-CONTEXT.md
@.planning/phases/18-reliability-long-tail/18-RESEARCH.md
@.planning/phases/18-reliability-long-tail/18-VALIDATION.md
@internal/acp/client.go
@internal/pool/pool.go
@internal/pool/exit_watcher.go
@internal/engine/engine.go
@internal/admin/tail.go
@internal/adapter/ollama/handlers.go
@internal/adapter/ollama/ndjson.go
@internal/config/config.go
@cmd/otto-gateway/main.go
</context>

<artifacts_this_phase_produces>
NEW symbols (exclude these from drift checks):

- Config.AdminTailPath (NEW string field on internal/config/Config)
- acp.Client.Pid() int (NEW exported method on *acp.Client)
- pool.PoolClient interface gains Pid() int (NEW interface method)
- Seven NEW test files:
  - internal/acp/regression_rel_obsv_03_test.go
  - internal/pool/regression_rel_obsv_02_test.go
  - internal/pool/regression_rel_http_07_test.go
  - internal/admin/regression_rel_http_07_test.go
  - internal/engine/regression_rel_http_07_test.go
  - internal/admin/regression_rel_obsv_04_test.go
  - internal/adapter/ollama/regression_rel_http_06_test.go
- New slog message strings (byte-exact):
  - "kiro-cli stderr" (Warn)
  - "pool: slot recovered" (Info)
  - "ollama: streaming eng.Run failed" (Warn)
  - "goroutine panic recovered" (Error)
- Test-only seam variables (panic probes) added to support panic-injection — gated by package-private vars, default nil/no-op in production
</artifacts_this_phase_produces>

<pre_coordination>
Soft sequence with 18-01 (Plan 1 of Phase 18): Plan 18-01 modifies
internal/config/config.go for D-18-01/02/03. This plan adds Config.AdminTailPath
(D-18-08) to the same struct. CONTEXT.md asserts "Wave 1 parallel-safe" — both
plans are wave 1 — but to avoid a trivial textual conflict on the Config struct
declaration block, run 18-01 FIRST. The conflict (if any) is at the struct
field list, not in behavior; resolution is mechanical (both fields are additive).
If 18-01 has not landed yet when 18-02 starts, add Config.AdminTailPath in this
plan's Task 3 (D-18-08) without removing or moving any 18-01 fields.
</pre_coordination>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED to GREEN — REL-OBSV-03 stderr scanner + REL-HTTP-06 Ollama eng.Run WARN</name>
  <files>internal/acp/regression_rel_obsv_03_test.go, internal/acp/client.go, internal/adapter/ollama/regression_rel_http_06_test.go, internal/adapter/ollama/handlers.go</files>

  <read_first>
    - internal/acp/client.go (lines 280–340): current `cmd.Stderr = os.Stderr` at line 289, `cmd.Start()` at line 327, internal `c.cmd.Process.Pid` usage at line 1188
    - internal/admin/tail.go: search for `ReadString('\n')` — this is the precedent pattern OQ-4 mandates (do NOT use bufio.Scanner)
    - internal/adapter/ollama/handlers.go (lines 220–340): the eng.Run error path at ~lines 240–252 where REL-HTTP-06 attaches
    - internal/adapter/ollama/ndjson.go (lines 550–610): the REL-HTTP-03 WARN field set this site mirrors VERBATIM
    - 18-CONTEXT.md §D-18-04, §D-18-06
    - 18-RESEARCH.md §"Pattern 6: REL-HTTP-03 WARN log field set" + §"Pitfall 2: cmd.StderrPipe() must be called BEFORE cmd.Start()"
  </read_first>

  <behavior>
    REL-OBSV-03 stderr scanner cases:
    - A1 (single line): fake kiro writes "test-stderr-line-18-02" to stderr then exits. Expect exactly one slog.Warn record, msg="kiro-cli stderr", worker_pid==fake PID, line=="test-stderr-line-18-02".
    - A2 (three lines): fake kiro writes three \n-separated lines. Expect three records.
    - A3 (goleak): after Close + Done, goleak.VerifyNone(t) clean.
    - A4 (line-too-long): fake kiro writes a 2MB line (no newline) followed by "\ntail-line\n". The reader does NOT silently stop; "tail-line" still gets logged. The 2MB line is byte-capped at 1MB before passing to Warn.

    REL-HTTP-06 Ollama eng.Run WARN cases:
    - B1 (generic error): fake engine.Run returns errors.New("synthetic"). Expect exactly one slog.Warn BEFORE the response frame, msg="ollama: streaming eng.Run failed", fields per behavior list.
    - B2 (exit error): fake engine.Run returns a real *exec.ExitError. Expect kiro_exit_code field present with ExitCode().
    - B3 (non-exit error): fake engine.Run returns errors.New(...). Expect kiro_exit_code field NOT present.

    GREEN: client.go uses cmd.StderrPipe BEFORE cmd.Start, launches a goroutine using bufio.NewReader + ReadString('\n') loop. handlers.go inserts the WARN log BEFORE writeError.
  </behavior>

  <action>
    PART A — REL-OBSV-03

    RED phase:
    Create internal/acp/regression_rel_obsv_03_test.go in package acp (or acp_test — match surrounding _test.go files via `grep -l "^package acp_test" internal/acp/*_test.go`). Use a fake kiro-cli implemented via os/exec:
    - Linux/darwin path: `exec.Command("sh", "-c", "echo 'test-stderr-line-18-02' 1>&2")` for A1; `printf 'l1\\nl2\\nl3\\n' 1>&2` for A2; for A4 use Go's `os.Stderr.WriteString` via `go run` inline if python is not guaranteed in CI.
    - Windows path: gated `//go:build windows` companion file using `cmd /c`, OR skip Windows for v1.10.3 (precedent in existing internal/acp/*_test.go).

    Capture slog via a buffer-backed `slog.New(slog.NewJSONHandler(buf, ...))` and inject it as cfg.Logger. Construct an acp.Client pointing at the fake command (mirror existing constructors from client_test.go). Start it; wait for subprocess exit; call client.Close(); wait for client.Done(). Decode slog records and assert per A1/A2.

    For A3: add `goleak.VerifyNone(t)` AT THE END after Close + Done per RESEARCH.md Pitfall 3.

    For A4: use a Go subprocess via `exec.Command("go", "run", "-")` with stdin piping the source, OR use `exec.Command("dd", "if=/dev/zero", "bs=1024", "count=2048")` piped to stderr via shell. Assert "tail-line" appears in records.

    Run `go test -race ./internal/acp/ -run TestRegression_REL_OBSV_03` — confirm FAIL. Commit: `test(18-02): RED — REL-OBSV-03 kiro-cli stderr -> structured slog`.

    GREEN phase:
    Open internal/acp/client.go. Around line 289, REMOVE `cmd.Stderr = os.Stderr`. Then:
    - BEFORE the existing `cmd.Start()` at line 327, insert: `stderrPipe, perr := cmd.StderrPipe()`. On perr != nil, return `fmt.Errorf("acp: StderrPipe: %w", perr)`.
    - AFTER `cmd.Start()` succeeds, launch a goroutine. The goroutine body must:
      1. `defer stderrPipe.Close()` at the top.
      2. Build `reader := bufio.NewReader(stderrPipe)`.
      3. Loop: `line, rerr := reader.ReadString('\n')`. After read:
         - Trim trailing CR/LF: `t := strings.TrimRight(strings.TrimSpace(line), "\r")`.
         - If non-empty: byte-cap to 1MB via `if len(t) > 1024*1024 { t = t[:1024*1024] }`, then emit `c.cfg.Logger.Warn("kiro-cli stderr", "worker_pid", cmd.Process.Pid, "line", t)`. Guard with `if c.cfg.Logger != nil` (mirrors exit_watcher.go:41 pattern).
         - If rerr != nil: return (cleanly exits the goroutine on io.EOF or pipe error).

    New imports needed for client.go: `bufio`, `io` (if not already), `strings` (if not already). Verify with `grep -n "^import" -A30 internal/acp/client.go`.

    Re-run `go test -race ./internal/acp/ -run TestRegression_REL_OBSV_03` — confirm PASS including goleak. Commit: `feat(18-02): GREEN — REL-OBSV-03 kiro-cli stderr -> structured slog (D-18-04)`.

    PART B — REL-HTTP-06

    RED phase:
    Create internal/adapter/ollama/regression_rel_http_06_test.go. Construct a fake engine.Engine whose Run returns a fixed error. If REL-HTTP-03 test exists at internal/adapter/ollama/regression_rel_http_03_test.go, copy its fake-engine setup verbatim.

    Test TestRegression_REL_HTTP_06 with subtests B1/B2/B3 per behavior section. Each subtest:
    1. Builds a buffer-backed slog handler and injects via adapter Config Logger.
    2. POSTs a streaming chat request to a test server.
    3. Decodes captured slog records.
    4. Asserts per behavior.

    For B2 — build a real *exec.ExitError via `cmd := exec.Command("false"); _ = cmd.Run(); var ee *exec.ExitError; errors.As(cmd.Err, &ee)` OR construct one through a small Go subprocess; assert ExitCode() returns 1 or similar.

    Run `go test -race ./internal/adapter/ollama/ -run TestRegression_REL_HTTP_06` — confirm FAIL. Commit: `test(18-02): RED — REL-HTTP-06 Ollama streaming eng.Run WARN`.

    GREEN phase:
    Open internal/adapter/ollama/handlers.go. At the eng.Run error path (~lines 240–252), BEFORE the existing `writeError(...)` call, insert:
    - Build `logArgs := []any{"session_id", "", "worker_pid", 0, "bytes_streamed", 0, "request_id", plugin.RequestIDFromContext(ctx), "err", err}`.
    - If `var exitErr *exec.ExitError; errors.As(err, &exitErr)` then `logArgs = append(logArgs, "kiro_exit_code", exitErr.ExitCode())`.
    - Emit `a.cfg.Logger.Warn("ollama: streaming eng.Run failed", logArgs...)` guarded by `if a.cfg.Logger != nil`.

    New imports: `os/exec`, `errors`. `plugin` likely already present (used elsewhere for RequestIDFromContext).

    Re-run — confirm PASS. Commit: `feat(18-02): GREEN — REL-HTTP-06 Ollama streaming eng.Run WARN (D-18-06)`.
  </action>

  <verify>
    <automated>go test -race ./internal/acp/ -run TestRegression_REL_OBSV_03 && go test -race ./internal/adapter/ollama/ -run TestRegression_REL_HTTP_06</automated>
  </verify>

  <acceptance_criteria>
    - internal/acp/regression_rel_obsv_03_test.go exists; cases A1–A4 pass under `-race` AND goleak.VerifyNone.
    - `grep -rn 'cmd.Stderr = os.Stderr' internal/ cmd/ --include='*.go'` returns ZERO production-code hits (exclude _test.go from grep if any test asserts the string is gone).
    - The stderr reader uses bufio.NewReader + ReadString('\n'). `grep -n 'bufio.NewScanner' internal/acp/client.go` returns ZERO hits.
    - internal/adapter/ollama/regression_rel_http_06_test.go exists; cases B1–B3 pass.
    - The new slog.Warn msg is byte-exact: "ollama: streaming eng.Run failed".
    - Field key order matches ndjson.go:567-577 — session_id, worker_pid, bytes_streamed, plus request_id, err, conditional kiro_exit_code.
    - worker_pid: 0 and bytes_streamed: 0 placeholders accepted per RESEARCH.md A4 (do NOT extend RunHandle for this plan).
    - go test -race ./internal/acp/ ./internal/adapter/ollama/ clean.
  </acceptance_criteria>

  <done>
    REL-OBSV-03 + REL-HTTP-06 RED→GREEN cycles complete; kiro-cli stderr is structured; Ollama streaming eng.Run failures emit symmetric WARN logs.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED to GREEN — REL-OBSV-02 worker recovery INFO + REL-HTTP-07 panic recovery at 4 sites</name>
  <files>internal/pool/regression_rel_obsv_02_test.go, internal/pool/regression_rel_http_07_test.go, internal/admin/regression_rel_http_07_test.go, internal/engine/regression_rel_http_07_test.go, internal/pool/pool.go, internal/pool/pool_test.go, internal/pool/exit_watcher.go, internal/engine/engine.go, internal/admin/tail.go, internal/acp/client.go</files>

  <read_first>
    - internal/pool/pool.go (lines 258–339, 840–870): respawnSlot success site and per-session ctx-watcher goroutine
    - internal/pool/exit_watcher.go (lines 1–51): death log at line 42 (gives the `label` field-name precedent); goroutine at line 32 needs panic-recover
    - internal/engine/engine.go (lines 255–340): line 255-265 is the context.AfterFunc CALLBACK that calls Cancel(sid) — wrap callback BODY in defer-recover. Lines 317–329 is the canonical callPreHookSafe template to mirror.
    - internal/admin/tail.go (line 217 + run body at ~285): goroutine launch + body where defer-recover lands at the top
    - internal/pool/regression_rel_pool_01_test.go (lines 59–90): fakeClient + fireDone scaffold for driving warmup→fireDone→respawn flow
    - internal/pool/pool_test.go (lines 48–167): fakeClient definition; needs to grow Pid() int method
    - 18-CONTEXT.md §D-18-05, §D-18-07
    - 18-RESEARCH.md §"Pattern 1: Panic-recover (D-18-07 template)", §"Pattern 3: Existing exit-watcher slog field set (D-18-05 mirror)", §"Pattern 5: fakeClient with Pid accessor", §"Pitfall 5"
  </read_first>

  <behavior>
    Part A — REL-OBSV-02 worker recovery INFO:
    - A1: Drive warmup → fireDone → respawn via fakeClient. Configure fake Pid() to return distinct values (e.g., 1001 OLD, 1002 NEW). Trigger worker death; wait for respawnSlot success path. Decode pool logger records; assert one record msg="pool: slot recovered", level=Info, label==<slot label>, worker_pid==1002, previous_pid==1001, reason=="lazy-respawn-success".

    Part B — REL-HTTP-07 panic recovery at 4 sites:
    - B1 (admin tailer): inject panic via test-only hook variable; assert msg="goroutine panic recovered", level=Error, site=="admin-tailer".
    - B2 (pool ctx-watcher): inject panic via test-only hook; assert site=="pool-ctx-watcher".
    - B3 (pool exit-watcher): inject panic via test-only hook; assert site=="pool-exit-watcher".
    - B4 (engine AfterFunc callback): inject panic via test-only hook; assert site=="engine-after-func".

    Each B case also asserts the goroutine exits cleanly (no restart) and the gateway is still alive (test continues to execute).
  </behavior>

  <action>
    PRE-STEP — fakeClient Pid accessor + interface extension:
    Open internal/pool/pool_test.go. Add a `Pid() int` method to fakeClient mirroring existing methods at lines 48–167. Add a `pid int` field to fakeClient that tests can set.
    Open internal/pool/pool.go. Find `type PoolClient interface` and add `Pid() int` to the interface.
    Open internal/acp/client.go. Add an exported `func (c *Client) Pid() int` method: returns `c.cmd.Process.Pid` when `c.cmd != nil && c.cmd.Process != nil`, else 0. Use the same accessor pattern already in use at line 1188 per RESEARCH.md.
    Verify `go build ./internal/pool/... ./internal/acp/...` succeeds. (This lands in the REL-OBSV-02 GREEN commit — it's a coupled prerequisite, not its own commit.)

    PART A — REL-OBSV-02

    RED phase:
    Create internal/pool/regression_rel_obsv_02_test.go. Reuse warmup/fireDone scaffold from regression_rel_pool_01_test.go:59-90. Set up Pool with two fakeClient instances configured with distinct Pid() values (1001 then 1002). Inject a buffer-backed slog handler as p.cfg.Logger. Drive the death → respawn flow. Decode records; assert per A1.

    Run `go test -race ./internal/pool/ -run TestRegression_REL_OBSV_02` — confirm FAIL. Commit: `test(18-02): RED — REL-OBSV-02 worker recovery INFO`.

    GREEN phase:
    Open internal/pool/pool.go. In respawnSlot:
    - At step 1 (closing the old client, ~line 322), BEFORE the close call, capture: `previousPid := slot.Client.Pid()`.
    - After `slot.dead = false` (~line 333) AND after `p.mu.Unlock()` (~line 337) — emit AFTER unlock to keep critical section narrow per RESEARCH.md §"D-18-05 INFO emission ordering" — insert: `if p.cfg.Logger != nil { p.cfg.Logger.Info("pool: slot recovered", "label", slot.Label, "worker_pid", newClient.Pid(), "previous_pid", previousPid, "reason", "lazy-respawn-success") }`.

    Field key for slot label is `label` (matches death log at exit_watcher.go:42 per RESEARCH.md Pattern 3 — resolves Pitfall 5 in favor of symmetry with existing death log).

    Re-run — confirm PASS. Commit: `feat(18-02): GREEN — REL-OBSV-02 worker recovery INFO + acp.Client.Pid accessor (D-18-05)`.

    PART B — REL-HTTP-07 panic recovery at 4 sites

    RED phase:
    Add test-only seam variables (each guarded by a comment "Test-only: leave nil in production"):
    - internal/admin/tail.go: add package-private `var adminTailerPanicProbe func()`. The tailer run() body calls `if adminTailerPanicProbe != nil { adminTailerPanicProbe() }` ONCE near the top of the loop (after the defer-recover that will be added in GREEN).
    - internal/pool/pool.go: add `var ctxWatcherPanicProbe func()`. The per-session ctx-watcher goroutine body calls it on each iteration (or once before the select).
    - internal/pool/exit_watcher.go: add `var exitWatcherPanicProbe func()`. Exit-watcher goroutine body calls it before the select.
    - internal/engine/engine.go: add `var afterFuncPanicProbe func()`. The AfterFunc callback body calls it before Cancel.

    Important caveat: the probe variables must be added in the GREEN commit ALONGSIDE the defer-recover wrappers — RED commits typically don't add seams. Resolve this by landing the seam variables in the RED commit as no-op probes (default nil → no behavior change) but no defer-recover yet, so the panic propagates and the tests fail because the gateway crashes the test goroutine. Alternative: use `t.Cleanup(func() { ... = nil })` to restore; tests set the probe to `func() { panic("test-18-02") }`.

    Create:
    - internal/admin/regression_rel_http_07_test.go: TestRegression_REL_HTTP_07_AdminTailer. Set adminTailerPanicProbe to func() { panic("test-18-02") }. Start the tailer. Wait for the goroutine to run the probe. Capture slog; assert msg="goroutine panic recovered", site=="admin-tailer".
    - internal/pool/regression_rel_http_07_test.go: TestRegression_REL_HTTP_07_PoolCtxWatcher (sets ctxWatcherPanicProbe) and TestRegression_REL_HTTP_07_PoolExitWatcher (sets exitWatcherPanicProbe). Each spins up the relevant goroutine and asserts the panic-recovered log.
    - internal/engine/regression_rel_http_07_test.go: TestRegression_REL_HTTP_07_EngineAfterFunc. Sets afterFuncPanicProbe. Triggers the AfterFunc callback by creating a session and cancelling its context. Asserts the panic-recovered log.

    Run package tests — confirm FAIL. Commit: `test(18-02): RED — REL-HTTP-07 panic recovery at 4 sites + test seams`.

    GREEN phase:
    Add defer-recover at each site. The template (from RESEARCH.md §Pattern 1) is:
    ```
    defer func() {
        if r := recover(); r != nil {
            logger.Error("goroutine panic recovered",
                "site", "<site>",
                "panic", fmt.Sprintf("%v", r),
                "stack", string(debug.Stack()))
        }
    }()
    ```

    Site (a) — internal/admin/tail.go run() body (~line 285): add at the TOP of run, BEFORE any variable declarations. Use `t.logger` for the Error call. Site name: "admin-tailer". Add `runtime/debug` import if not present (fmt already there per RESEARCH.md).

    Site (b) — internal/pool/pool.go ctx-watcher (~line 859): capture `logger := p.cfg.Logger` BEFORE `go func()` (logger may not be in goroutine closure). Add defer-recover at top of goroutine body. Site name: "pool-ctx-watcher". Guard with `if logger != nil`. Add `runtime/debug`, `fmt` if not present.

    Site (c) — internal/pool/exit_watcher.go (~line 32 goroutine): same shape; capture logger before goroutine; site name: "pool-exit-watcher".

    Site (d) — internal/engine/engine.go AfterFunc callback (lines 255–265): wrap the CALLBACK BODY (the closure passed to context.AfterFunc) in defer-recover. NOT a goroutine, but the runtime task can still panic. Site name: "engine-after-func". engine.go already imports runtime/debug per RESEARCH.md Pattern 1.

    Re-run all 4 tests — confirm PASS. Commit: `feat(18-02): GREEN — REL-HTTP-07 panic recovery at 4 sites (D-18-07)`.
  </action>

  <verify>
    <automated>go test -race ./internal/pool/ -run TestRegression_REL_OBSV_02 ./internal/pool/ -run TestRegression_REL_HTTP_07 ./internal/admin/ -run TestRegression_REL_HTTP_07_AdminTailer ./internal/engine/ -run TestRegression_REL_HTTP_07_EngineAfterFunc</automated>
  </verify>

  <acceptance_criteria>
    - acp.Client.Pid() int exists; returns 0 when c.cmd or c.cmd.Process is nil; never panics.
    - pool.PoolClient interface has Pid() int; fakeClient implements it with a settable pid field.
    - internal/pool/regression_rel_obsv_02_test.go exists; case A1 passes.
    - The pool recovery slog.Info uses msg byte-exact "pool: slot recovered"; field keys are label, worker_pid, previous_pid, reason; reason value is "lazy-respawn-success" byte-exact.
    - All 4 panic-recover tests pass; each goroutine exits cleanly (no restart) after panic.
    - 4 NEW slog.Error sites use msg byte-exact "goroutine panic recovered"; field keys site, panic, stack.
    - Site names are byte-exact: "admin-tailer", "pool-ctx-watcher", "pool-exit-watcher", "engine-after-func".
    - go test -race ./internal/pool/ ./internal/admin/ ./internal/engine/ clean.
    - go test -race ./... tree-wide clean (no other tests regressed).
  </acceptance_criteria>

  <done>
    REL-OBSV-02 + REL-HTTP-07 RED→GREEN cycles complete; worker recovery is symmetric with death; 4 panic sites have defense-in-depth recovery; no goroutine can crash the gateway silently.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RED to GREEN — REL-OBSV-04 Config.AdminTailPath single source + tailer WARN on missing</name>
  <files>internal/config/config.go, internal/admin/tail.go, cmd/otto-gateway/main.go, internal/admin/regression_rel_obsv_04_test.go</files>

  <read_first>
    - internal/config/config.go: lines 267-271 (Config struct ChatTraceFile field); lines 542-543 (deriveChatTraceFile + ChatTraceFile load); the deriveChatTraceFile helper at ~line 665
    - cmd/otto-gateway/main.go: lines 295-310 (chat-trace rotator writer construction at line 302), lines 615-690 (admin handler + tailer wiring at line 624+)
    - internal/admin/tail.go: lines 298-305 (reopen() open-fail Debug log — promote to Warn) + lines 1-50 to confirm logger field
    - 18-CONTEXT.md §D-18-08
    - 18-RESEARCH.md §"D-18-08 REL-OBSV-04" codebase-map row + §"Within-task TOCTOU and lifecycle concerns" — AdminTailPath readability
  </read_first>

  <behavior>
    Cases:
    - C1: With both LOG_FILE and CHAT_TRACE_FILE unset, after config.Load: cfg.AdminTailPath equals deriveChatTraceFile(logFileForDerive)'s default output. Same string the writer would resolve.
    - C2: With CHAT_TRACE_FILE=/tmp/test-tail.log set, cfg.AdminTailPath equals "/tmp/test-tail.log".
    - C3 (writer-tailer parity): The path the chat-trace writer would open (at main.go:302) equals cfg.AdminTailPath.
    - C4 (admin WARN on missing): With cfg.AdminTailPath set to a non-existent path, the tailer's reopen() emits slog.Warn (NOT Debug) with the path field. Capture slog from t.logger.

    GREEN: Config struct gains AdminTailPath string field. config.Load populates it via the SAME deriveChatTraceFile call used for ChatTraceFile. main.go reads cfg.AdminTailPath when wiring the tailer (no independent derivation). tail.go reopen() promotes the Debug log to Warn with the resolved path.
  </behavior>

  <action>
    PRE-COORDINATION: if Plan 18-01 has not landed yet, ensure no merge conflict on the Config struct field block — add AdminTailPath at the END of the struct's string fields (or in any unused area) so positional diff is minimal.

    RED phase:
    Create internal/admin/regression_rel_obsv_04_test.go. Test TestRegression_REL_OBSV_04 with subtests:
    - C1: t.Setenv all env vars to defaults (clear LOG_FILE + CHAT_TRACE_FILE + AUTH_TOKEN + ALLOWED_IPS + KIRO_CWD; set KIRO_CMD=go + HTTP_ADDR=127.0.0.1:0 to silence 18-01 branches). Call config.Load. Assert cfg.AdminTailPath != "" AND cfg.AdminTailPath == cfg.ChatTraceFile (both derive from the same helper).
    - C2: t.Setenv("CHAT_TRACE_FILE", "/tmp/test-18-02-tail.log"). Assert cfg.AdminTailPath == "/tmp/test-18-02-tail.log".
    - C3: Read cfg.AdminTailPath and the path that cmd/otto-gateway/main.go would use to construct the chat-trace rotator at line 302. Assert they are identical strings. (Practical implementation: just assert cfg.AdminTailPath == cfg.ChatTraceFile — both must resolve to the same value via the same helper.)
    - C4: Set cfg.AdminTailPath to a path under t.TempDir() that does NOT exist. Build a Tailer with that path and a buffer-backed slog. Drive reopen(). Decode records; assert one record at slog.Warn (NOT Debug) with the path field present.

    Run `go test -race ./internal/admin/ -run TestRegression_REL_OBSV_04` — confirm FAIL. Commit: `test(18-02): RED — REL-OBSV-04 single-source path + WARN on missing`.

    GREEN phase:
    1. Open internal/config/config.go. Add `AdminTailPath string` field to the Config struct (find via `grep -n "ChatTraceFile" internal/config/config.go`; place AdminTailPath immediately below). In config.Load(), AFTER the existing `chatTraceFile := getEnvStr("CHAT_TRACE_FILE", deriveChatTraceFile(logFileForDerive))` at ~line 543, set `cfg.AdminTailPath = chatTraceFile` (or wherever cfg is assembled at the bottom of Load — read the function to confirm assignment site).

    2. Open cmd/otto-gateway/main.go. At the tailer wiring at ~line 624, change any independent path derivation to read `cfg.AdminTailPath`. If currently the tailer takes a string derived from some other config field, replace with `cfg.AdminTailPath`. Verify the chat-trace rotator at line 302 still uses `cfg.ChatTraceFile` — both fields hold the same string by construction in step 1, but the API surface is unchanged for the rotator.

    3. Open internal/admin/tail.go reopen() at ~lines 298-305. The current `t.logger.Debug(...)` on open-failure becomes `t.logger.Warn(...)`. Ensure the `path` field carries `t.path` (already in scope per RESEARCH.md "already in scope").

    Re-run — confirm PASS. Commit: `feat(18-02): GREEN — REL-OBSV-04 Config.AdminTailPath single source + WARN on missing (D-18-08)`.
  </action>

  <verify>
    <automated>go test -race ./internal/admin/ -run TestRegression_REL_OBSV_04 && go build ./...</automated>
  </verify>

  <acceptance_criteria>
    - Config struct has NEW field `AdminTailPath string` (verify via `grep -n 'AdminTailPath' internal/config/config.go` — at least 2 hits: struct decl + assignment in Load).
    - cfg.AdminTailPath equals cfg.ChatTraceFile after Load (both derived from the SAME deriveChatTraceFile call).
    - cmd/otto-gateway/main.go reads cfg.AdminTailPath when constructing the tailer (verify via `grep -n 'AdminTailPath' cmd/otto-gateway/main.go`).
    - The Tailer struct's reopen() open-fail log is at slog.Warn level (NOT Debug); the path field is included.
    - `grep -n 'logger.Debug' internal/admin/tail.go | grep -i 'open'` returns ZERO hits for the open-fail case.
    - Existing /admin Log Tail panel manual smoke (per RESEARCH.md risk): manual verification noted in SUMMARY — visit /admin after boot and confirm log tail still renders.
    - go test -race ./internal/admin/ ./internal/config/ clean; go build ./... clean.
  </acceptance_criteria>

  <done>
    REL-OBSV-04 RED→GREEN cycle complete; writer and tailer read from a single Config.AdminTailPath field; tailer open-failure surfaces as WARN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiro-cli subprocess stderr → gateway slog | Untrusted subprocess output crosses here; could attempt log-record injection. |
| Background goroutines → gateway process | A panic in any background goroutine would take down the process without recover. |
| operator env → log-tail path resolution | Writer + reader formerly resolved paths independently; risk of split-brain reading a different file than the writer wrote. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02-T | Tampering (log injection) | acp.Client stderr reader → slog.Warn | low | mitigate | D-18-04: kiro-cli stderr line is logged as the value of a STRING field `"line"`; slog JSON handler escapes it. A malicious kiro-cli output `"\",\"level\":\"ERROR\",\"msg\":\"FORGED"` cannot forge a separate log record. Verified in Task 1 by injecting a forge string and asserting it arrives as a single record literal payload. ASVS V7.1. |
| T-18-02-D | Denial of Service (goroutine leak / runaway memory) | acp.Client stderr reader goroutine + 4 panic sites | low | mitigate | D-18-04: 1MB byte-cap on logged line; bufio.Reader.ReadString does not block forever (returns on EOF/error). goleak.VerifyNone gates leak. D-18-07: panic-recover exits goroutine cleanly; no spin loop or restart. ASVS V7.4. |
| T-18-02-I | Information Disclosure (stack trace) | 4 panic sites logging debug.Stack | low | accept | Existing engine.callPreHookSafe template logs debug.Stack at Error; this is precedent from v1.5. Stacks may contain frame addresses but no bearer tokens or PII (Phase 18 does not introduce any new context-carrying request data into these goroutines). ASVS V7.4 / V8.3 minor. |
| T-18-02-S | Spoofing (writer-reader divergence) | log-tail writer vs tailer | low | mitigate | D-18-08: single Config.AdminTailPath field populated once in Load. Both writer and reader consume this field. Eliminates split-brain. ASVS V10 (Architecture). |

ASVS L1 coverage: V7 (Logging and Error Handling) primary; V10 (Architecture) secondary.
</threat_model>

<verification>
## Phase-close criteria covered by this plan

1. make ci exit 0 — verified after all three tasks land.
2. Each REL-* has a regression test:
   - REL-OBSV-03 → TestRegression_REL_OBSV_03
   - REL-HTTP-06 → TestRegression_REL_HTTP_06
   - REL-OBSV-02 → TestRegression_REL_OBSV_02
   - REL-HTTP-07 → TestRegression_REL_HTTP_07_{AdminTailer,PoolCtxWatcher,PoolExitWatcher,EngineAfterFunc}
   - REL-OBSV-04 → TestRegression_REL_OBSV_04
3. go test -race ./... clean tree-wide.
4. `grep -rn "cmd.Stderr = os.Stderr" internal/ cmd/ --include='*.go'` returns ZERO production-code hits.

Phase-wide criteria 5 (main.go:115-120 INFO line preserved) and 6 (no StateConfigError) are not in this plan's scope.

## Negative-grep gates

- `grep -n 'bufio.NewScanner' internal/acp/client.go` returns ZERO hits (OQ-4 resolution).
- `grep -n 'bufio.NewReader' internal/acp/client.go` returns ≥ 1 hit (the new reader).
- `grep -c 'goroutine panic recovered' internal/admin/tail.go internal/pool/pool.go internal/pool/exit_watcher.go internal/engine/engine.go` returns 4 distinct hits (one per site).
- `grep -c 'pool: slot recovered' internal/pool/pool.go` returns 1 hit.
- `grep -c 'ollama: streaming eng.Run failed' internal/adapter/ollama/handlers.go` returns 1 hit.
- `grep -c 'kiro-cli stderr' internal/acp/client.go` returns 1 hit.
- `grep -n 'AdminTailPath' internal/config/config.go cmd/otto-gateway/main.go` returns ≥ 3 hits (struct decl + Load assignment + main.go consumer).
</verification>

<success_criteria>
- All RED→GREEN test commits land before their implementation commits (TDD ordering visible in git log).
- go test -race ./... exits 0.
- go vet ./... exits 0.
- go build ./... exits 0 tree-wide.
- make ci exit 0 (v1.10.2 baseline preserved).
- No new go.mod entries.
- The 4 site names ("admin-tailer", "pool-ctx-watcher", "pool-exit-watcher", "engine-after-func") are byte-exact.
- The success-path reason ("lazy-respawn-success") is byte-exact.
- worker_pid: 0 and bytes_streamed: 0 placeholders accepted for REL-HTTP-06.
</success_criteria>

<output>
Create `.planning/phases/18-reliability-long-tail/18-02-SUMMARY.md` when done. Required sections:
- REQ-ID closeout (REL-HTTP-06, REL-HTTP-07, REL-OBSV-02, REL-OBSV-03, REL-OBSV-04 each with the regression test path)
- 4 panic sites documented with site name and goroutine identity
- Commit hashes (RED + GREEN per REQ-ID — expect 6–10 commits total depending on RED grouping)
- make ci output line confirming exit 0
- `grep -rn 'cmd.Stderr = os.Stderr' internal/ cmd/` output line confirming zero hits
- Any open follow-ups (e.g. slot_id plumbing punted to v1.10.4 per CONTEXT.md)
</output>
