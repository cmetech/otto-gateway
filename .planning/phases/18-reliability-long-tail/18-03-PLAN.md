---
phase: 18-reliability-long-tail
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/otto-tray/tray.go
  - cmd/otto-tray/fsm.go
  - cmd/otto-tray/poller.go
  - cmd/otto-tray/regression_rel_tray_08_test.go
  - cmd/otto-tray/regression_rel_tray_09_test.go
  - scripts/otto-gw
  - scripts/otto-gw.ps1
  - tests/wrappers/otto-gw-support-rows_test.bats
  - tests/reliability/manual/REL-TRAY-02-repro.ps1
autonomous: true
requirements: [REL-TRAY-08, REL-TRAY-09]
requirements_addressed: [REL-TRAY-08, REL-TRAY-09]
tags: [go, tray, bash, powershell, wrapper, bundle, reliability]
threat_model_summary:
  - "T-18-03-T: TOCTOU on sentinel file write/read — mitigated by atomic rename + ≤3s flap window acceptance (D-18-09)"
  - "T-18-03-I: sentinel file leaking partial env contents — mitigated by 0o600 mode + one-line trim + 200-byte truncation (D-18-09)"
  - "T-18-03-T: path traversal via $HOME/.otto-gw/ — mitigated by os.UserHomeDir + filepath.Join (D-18-09)"
  - "T-18-03-I: support-bundle PII leakage via removed rows — out of scope (rows removed, not added)"

must_haves:
  truths:
    - "RESOLVED OQ-2 (D-18-09 probeFunc signature): extend stateInput with a NEW field `ConfigError string` and short-circuit `computeState` at the TOP — if ConfigError != \"\" return stateOutput{State: StateError, Detail: \"config error: \" + in.ConfigError}. probeFunc signature itself is UNCHANGED; the poller synthesizes the new stateInput field by reading the sentinel file every tick."
    - "REL-TRAY-08 reuses StateError (NO new StateConfigError state). The tray FSM state list is UNCHANGED — verified by phase-close criterion 6."
    - "REL-TRAY-08 sentinel path: $HOME/.otto-gw/.config-error (mode 0o600); parent dir created with MkdirAll(dir, 0o750). Bash + PowerShell wrappers both implement write-on-fail / delete-on-success."
    - "REL-TRAY-08 sentinel content is trimmed to ONE LINE and truncated to 200 bytes max before being assigned to stateOutput.Detail (PII / secret minimization)."
    - "REL-TRAY-09 removes BOTH broken macOS rows from scripts/otto-gw (the bash bundle generator): the autostart probe at lines ~1951-1962 (wrong plist name `com.otto.tray.plist` — actual is `io.cmetech.otto-tray.plist`) AND the tray-state.txt row at lines ~1928-1935 (reads a file the tray has never written)."
    - "REL-TRAY-09 leaves the Windows bundle path UNCHANGED. scripts/otto-gw.ps1 lines 1647-1676 (Windows autostart probe via Run-key) remain untouched."
    - "REL-TRAY-09 manual repro test tests/reliability/manual/REL-TRAY-02-repro.ps1 has been read and confirmed to assert ONLY on Windows-side fields (health.json unreachable sentinel + bundle stdout path); per RESEARCH.md A3, no PS1 update is required. The plan still includes a no-op verification task that confirms the PS1 test runs unchanged."
  artifacts:
    - path: cmd/otto-tray/regression_rel_tray_08_test.go
      provides: REL-TRAY-08 sentinel detection → StateError regression
      contains: "TestRegression_REL_TRAY_08"
    - path: cmd/otto-tray/regression_rel_tray_09_test.go
      provides: REL-TRAY-09 bundle row absence assertion (Go-side coverage of the macOS row removal)
      contains: "TestRegression_REL_TRAY_09"
    - path: tests/wrappers/otto-gw-support-rows_test.bats
      provides: Bash-level negative-grep on the bundle output to assert removed rows are absent
      contains: "@test \"REL-TRAY-09:"
    - path: cmd/otto-tray/fsm.go
      provides: stateInput gains NEW field ConfigError string; computeState short-circuits at top
    - path: cmd/otto-tray/poller.go
      provides: poller reads sentinel file each tick and populates stateInput.ConfigError
    - path: scripts/otto-gw
      provides: load_env_file wrapper writes/deletes sentinel + macOS bundle rows removed
    - path: scripts/otto-gw.ps1
      provides: PowerShell mirror of sentinel write/delete
  key_links:
    - from: scripts/otto-gw load_env_file
      to: $HOME/.otto-gw/.config-error sentinel file
      via: echo / rm -f
      pattern: "\\.config-error"
    - from: cmd/otto-tray/poller.go
      to: stateInput.ConfigError
      via: os.ReadFile($HOME/.otto-gw/.config-error)
      pattern: "ConfigError"
    - from: cmd/otto-tray/fsm.go computeState
      to: stateOutput{State: StateError, Detail: "config error: ..."}
      via: top-of-function short-circuit
      pattern: "config error:"
---

<objective>
Close REL-TRAY-08 and REL-TRAY-09 — two tray-honesty items. REL-TRAY-08 makes
malformed dotenv files visible: the wrapper writes a sentinel on parse failure
and the tray surfaces it via StateError instead of polling the wrong port and
showing "stopped". REL-TRAY-09 removes two macOS support-bundle rows that have
always lied: the autostart probe checks the wrong plist name, and the
tray-state.txt row reads a file the tray has never written.

Purpose: After this plan lands, an operator with a malformed `.env` sees a
tray error state with the parse error inline, not a confusing "stopped" icon.
The macOS support bundle stops emitting two rows of fiction.

Output:
  - 2 RED→GREEN test cycles (REL-TRAY-08 in Go; REL-TRAY-09 in bash + Go)
  - Wrapper changes in scripts/otto-gw + scripts/otto-gw.ps1 for sentinel write
  - Tray changes in cmd/otto-tray/{poller,fsm}.go for sentinel read + stateInput extension
  - Bundle row removal in scripts/otto-gw (macOS-side only)
  - make ci exit 0
  - go test -race ./cmd/otto-tray/ clean
  - bash -n + shellcheck scripts/otto-gw clean
  - PowerShell static review (pwsh smoke deferred per quick 260531-oax precedent)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-reliability-long-tail/18-CONTEXT.md
@.planning/phases/18-reliability-long-tail/18-RESEARCH.md
@.planning/phases/18-reliability-long-tail/18-VALIDATION.md
@cmd/otto-tray/tray.go
@cmd/otto-tray/fsm.go
@cmd/otto-tray/poller.go
@cmd/otto-tray/autostart_darwin.go
@scripts/otto-gw
@scripts/otto-gw.ps1
@tests/reliability/manual/REL-TRAY-02-repro.ps1
</context>

<artifacts_this_phase_produces>
NEW symbols (exclude these from drift checks):

- stateInput.ConfigError string (NEW field on internal stateInput struct in cmd/otto-tray/fsm.go)
- Top-of-function short-circuit branch in computeState
- Sentinel file path: $HOME/.otto-gw/.config-error (NEW operator-visible path; mode 0o600)
- TestRegression_REL_TRAY_08 (cmd/otto-tray/regression_rel_tray_08_test.go)
- TestRegression_REL_TRAY_09 (cmd/otto-tray/regression_rel_tray_09_test.go)
- tests/wrappers/otto-gw-support-rows_test.bats (NEW bats file)
- scripts/otto-gw additions: sentinel write/delete in load_env_file; bundle rows REMOVED (negative artifact)
- scripts/otto-gw.ps1 additions: sentinel mirror in Set-Content

NO new tray FSM states. NO new go.mod entries. NO new env vars.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED to GREEN — REL-TRAY-08 sentinel-driven StateError</name>
  <files>cmd/otto-tray/regression_rel_tray_08_test.go, cmd/otto-tray/fsm.go, cmd/otto-tray/poller.go, cmd/otto-tray/tray.go, scripts/otto-gw, scripts/otto-gw.ps1</files>

  <read_first>
    - cmd/otto-tray/fsm.go (lines 1-75): existing computeState function + stateInput / stateOutput struct definitions; StateError constant at ~line 14
    - cmd/otto-tray/poller.go: the poller loop that builds stateInput each tick and calls computeState
    - cmd/otto-tray/tray.go (lines 140-179, 366-376): makeProbe + existing sentinel-file write precedent at last-error.log (this is the file-permission analog to mirror)
    - scripts/otto-gw (lines 200-225): load_env_file function — current parse-tolerant loader; this is where sentinel write/delete is wired
    - scripts/otto-gw (call sites at lines ~466, 475, 686): where load_env_file is invoked
    - scripts/otto-gw.ps1: search for `function Load-EnvFile` (or the equivalent dot-source pattern) to find the PowerShell mirror
    - 18-CONTEXT.md §D-18-09
    - 18-RESEARCH.md §"D-18-09 REL-TRAY-08 (wrapper sentinel write)", §"Pattern 4: Sentinel file conventions", §"Pitfall 6: Sentinel-file race", §"Open Questions §2"
  </read_first>

  <behavior>
    Cases:
    - A1 (sentinel present → StateError): t.Setenv("HOME", t.TempDir()); create $HOME/.otto-gw/.config-error with content "syntax error on line 3: missing quote". Build a Tailer/Poller that reads the sentinel each tick. Drive one poll cycle. Assert the resulting state is StateError AND Detail contains "config error:" AND Detail contains "syntax error on line 3: missing quote".
    - A2 (sentinel absent → normal state): No sentinel file present. Drive one poll cycle. Assert state is NOT StateError (whatever state the pidAlive/healthOK probes resolve to, which in tests is typically StateStopped or StateRunning).
    - A3 (sentinel content truncation): Create sentinel with a 500-byte single line. Assert Detail is truncated to ≤ 200 bytes (per CONTEXT.md / RESEARCH.md PII minimization).
    - A4 (sentinel multi-line trimmed to first line): Create sentinel with "line1\nline2\nline3". Assert Detail contains "line1" but NOT "line2" or "line3".
    - A5 (computeState short-circuit precedence): Set sentinel AND set pidAlive=true + healthOK=true (would normally → StateRunning). Assert computeState returns StateError (sentinel wins over PID/health probes per OQ-2 resolution).

    GREEN:
    - fsm.go: stateInput grows `ConfigError string` field; computeState short-circuits at the top: `if in.ConfigError != "" { return stateOutput{State: StateError, Detail: "config error: " + in.ConfigError} }`.
    - poller.go: reads $HOME/.otto-gw/.config-error each tick; populates stateInput.ConfigError; trims to first line + truncates to 200 bytes.
    - Wrappers (scripts/otto-gw + .ps1): load_env_file writes sentinel on parse failure, deletes on success.
  </behavior>

  <action>
    PART A — Go-side (tray)

    RED phase:
    Create cmd/otto-tray/regression_rel_tray_08_test.go in package main (mirror surrounding test files in cmd/otto-tray/). Helper: a small testSentinelHome(t) function that uses t.TempDir() + t.Setenv("HOME", tempDir) on POSIX, t.Setenv("USERPROFILE", tempDir) on Windows. Test scaffolding:
    - Build stateInput with PIDAlive/HealthOK/Snapshot values + the NEW ConfigError field.
    - Call computeState directly for cases A1, A3, A4, A5 (no poller needed — pure computeState behavior).
    - For A2: same but ConfigError="" → assert state != StateError.

    Run `go test -race ./cmd/otto-tray/ -run TestRegression_REL_TRAY_08` and confirm FAIL (stateInput does not have ConfigError field; computeState does not short-circuit). Commit: `test(18-03): RED — REL-TRAY-08 sentinel-driven StateError`.

    GREEN phase:
    1. Open cmd/otto-tray/fsm.go. Add a NEW field `ConfigError string` to the stateInput struct (find via `grep -n "type stateInput" cmd/otto-tray/fsm.go`). At the TOP of computeState (before the existing PIDAlive check), insert:
       ```
       if in.ConfigError != "" {
           return stateOutput{State: StateError, Detail: "config error: " + in.ConfigError}
       }
       ```
       (Shown as literal source.)

    2. Open cmd/otto-tray/poller.go. In the poller tick loop, BEFORE building stateInput, read the sentinel:
       - Build path: `home, _ := os.UserHomeDir(); sentinelPath := filepath.Join(home, ".otto-gw", ".config-error")`.
       - `data, rerr := os.ReadFile(sentinelPath)`.
       - If rerr == nil: process data — trim to first line via `strings.SplitN(string(data), "\n", 2)[0]`, then trim whitespace, then truncate to 200 bytes via `if len(s) > 200 { s = s[:200] }`. Assign to stateInput.ConfigError.
       - If rerr != nil (most commonly os.IsNotExist): leave stateInput.ConfigError == "".

    3. Verify imports in poller.go: `os`, `path/filepath`, `strings`. Add as needed.

    Re-run `go test -race ./cmd/otto-tray/ -run TestRegression_REL_TRAY_08` — confirm PASS. Commit: `feat(18-03): GREEN — REL-TRAY-08 sentinel-driven StateError via stateInput.ConfigError (D-18-09)`.

    PART B — Wrappers (bash + PowerShell)

    scripts/otto-gw load_env_file modification:
    1. Open scripts/otto-gw. Locate load_env_file (~lines 200-225). The loader currently silently skips malformed lines. Modify it to:
       - Track whether any line failed parsing (e.g., a local `parse_errors` accumulator variable).
       - On any parse failure (the existing skip path), append `"$line_number:$line_content"` (or similar) to parse_errors.
       - After the loader loop:
         * If parse_errors is non-empty: `mkdir -p "$HOME/.otto-gw"`, then `echo "$parse_errors" | head -c 200 > "$HOME/.otto-gw/.config-error"`, then `chmod 600 "$HOME/.otto-gw/.config-error"`. Also `echo "config parse error: $parse_errors" 1>&2` (loud terminal signal per CONTEXT.md D-18-09 step 1).
         * If parse_errors is empty: `rm -f "$HOME/.otto-gw/.config-error"` (delete-on-success).
    2. Wire this for ALL three call sites at ~lines 466, 475, 686.

    Run `bash -n scripts/otto-gw` and `shellcheck scripts/otto-gw` — both clean.

    scripts/otto-gw.ps1 mirror:
    Open scripts/otto-gw.ps1. Mirror the same logic in PowerShell using `Set-Content -Path` + `Remove-Item -Force -ErrorAction SilentlyContinue`. Sentinel path: `$env:USERPROFILE\.otto-gw\.config-error` (Windows) — but for cross-platform parity with bash, prefer `$env:HOME` first then fall back to `$env:USERPROFILE`. New-Item -ItemType Directory -Force for the parent dir.

    Static review only on darwin per quick 260531-oax precedent (pwsh not on dev box).

    No commit for Part B yet — fold into the GREEN commit if test scaffolding for bash exists (next step), OR commit separately as `feat(18-03): wrapper sentinel write/delete on dotenv parse (D-18-09)`.
  </action>

  <verify>
    <automated>go test -race ./cmd/otto-tray/ -run TestRegression_REL_TRAY_08 && bash -n scripts/otto-gw && shellcheck scripts/otto-gw</automated>
  </verify>

  <acceptance_criteria>
    - cmd/otto-tray/regression_rel_tray_08_test.go exists; all 5 cases (A1–A5) pass.
    - stateInput struct has NEW field `ConfigError string` (verify via `grep -n 'ConfigError' cmd/otto-tray/fsm.go`).
    - computeState short-circuits at the TOP — the new `if in.ConfigError != "" { return ... }` block is FIRST inside the function body. Verify via reading fsm.go.
    - Returned Detail string starts with `"config error: "` byte-exact.
    - poller.go reads $HOME/.otto-gw/.config-error each tick via os.ReadFile + filepath.Join + os.UserHomeDir.
    - Multi-line sentinel content is trimmed to the first line.
    - Sentinel content is truncated to ≤ 200 bytes after trim.
    - StateError + Detail wins over PIDAlive/HealthOK probes (case A5).
    - scripts/otto-gw: parse failure → `$HOME/.otto-gw/.config-error` written with mode 0o600; parse success → file removed via `rm -f`.
    - scripts/otto-gw `bash -n` clean; `shellcheck` clean.
    - scripts/otto-gw.ps1 mirror present (static review only — pwsh smoke deferred).
    - NO new tray FSM state added. `grep -n 'State[A-Z]' cmd/otto-tray/fsm.go` shows the SAME state constants as before the plan (StateRunning, StateStopped, StateDegraded, StateError, etc. — exact set unchanged). Phase-close criterion 6 satisfied.
    - go test -race ./cmd/otto-tray/... clean.
  </acceptance_criteria>

  <done>
    REL-TRAY-08 RED→GREEN cycle complete; sentinel write/read pipeline surfaces config errors via StateError without expanding the FSM state list.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED to GREEN — REL-TRAY-09 macOS bundle row removal (both broken rows)</name>
  <files>tests/wrappers/otto-gw-support-rows_test.bats, cmd/otto-tray/regression_rel_tray_09_test.go, scripts/otto-gw, tests/reliability/manual/REL-TRAY-02-repro.ps1</files>

  <read_first>
    - scripts/otto-gw (lines 1920-1962): support-bundle generator for macOS — both broken rows live here. Specifically:
      * Lines ~1928-1935: tray-state.txt block (reads `$OTTO_INSTALL_ROOT/.otto/tray/state` — never written by tray)
      * Lines ~1951-1962: autostart probe block (checks `com.otto.tray.plist` — wrong name; actual is `io.cmetech.otto-tray.plist`)
    - cmd/otto-tray/autostart_darwin.go (line 15): confirms actual plist label is `io.cmetech.otto-tray`
    - tests/reliability/manual/REL-TRAY-02-repro.ps1: Windows-only test; per RESEARCH.md A3, no update needed — but the planner / executor should READ it once to confirm
    - scripts/otto-gw.ps1 (lines 1647-1676): Windows autostart probe — UNCHANGED by this task (probes the Run-key correctly per RESEARCH.md)
    - 18-CONTEXT.md §D-18-10
    - 18-RESEARCH.md §"D-18-10 REL-TRAY-09" + §"Pitfall 7: Bundle row removal breaks bash strict-mode"
  </read_first>

  <behavior>
    Cases:
    - B1 (bash bundle row absence — bats): Run `scripts/otto-gw support` on macOS in a tmpdir; the produced bundle directory does NOT contain a file matching `tray/tray-state.txt` AND does NOT contain a file matching `tray/autostart.txt` (or whatever the autostart probe wrote — check the current code for the exact filename). The bundle stays well-formed (other macOS rows still present: pidfile.txt, etc).
    - B2 (Go-side smoke — regression_rel_tray_09_test.go): If `cmd/otto-tray/` has a support-bundle generator (it may not — bundle generation is bash-side), this test asserts the Go bundle code does not produce the broken rows. If bundle is bash-only on macOS, this test is a TRIVIAL smoke (e.g., assert a specific path is empty / absent in any Go-emitted bundle structure), OR skip per RESEARCH.md A3 because bundle is wholly bash-side. **Default: skip this Go-side test if no Go bundle code touches macOS rows; document the skip in the test file.**
    - B3 (Windows path unchanged): Run scripts/otto-gw.ps1 support flow (static check only on darwin) — `grep -c "Run-key" scripts/otto-gw.ps1` returns the SAME count as before the plan. No Windows bundle changes.
    - B4 (existing manual repro test unchanged): tests/reliability/manual/REL-TRAY-02-repro.ps1 does NOT assert on the removed rows; per RESEARCH.md A3, no update is required. Verify by reading the file.

    GREEN: Both row-emitting code blocks in scripts/otto-gw are removed; bats test passes; existing Windows manual repro is untouched.
  </behavior>

  <action>
    RED phase:
    Create tests/wrappers/otto-gw-support-rows_test.bats. Bats test scaffolding:
    - `@test "REL-TRAY-09: macOS bundle does NOT contain tray/tray-state.txt"`: set up a tmpdir with OTTO_INSTALL_ROOT pointing there, set HOME to tmpdir, run `bash scripts/otto-gw support 2>&1` (or whatever the support subcommand invocation is — read `usage()` in otto-gw to confirm). Inspect the produced bundle directory; assert `! [ -f "$bundle_dir/tray/tray-state.txt" ]`. Skip the test if not on darwin: `[[ "$OSTYPE" == "darwin"* ]] || skip "macOS-only"`.
    - `@test "REL-TRAY-09: macOS bundle does NOT contain tray/autostart.txt"`: similar shape, asserting `! [ -f "$bundle_dir/tray/autostart.txt" ]` (use the actual filename the current code emits — read scripts/otto-gw lines ~1951-1962 to identify it).
    - `@test "REL-TRAY-09: macOS bundle still contains tray/pidfile.txt"`: positive control — ensure removal doesn't kill the entire section. Assert `[ -f "$bundle_dir/tray/pidfile.txt" ]`.

    Create cmd/otto-tray/regression_rel_tray_09_test.go with a single test TestRegression_REL_TRAY_09 that is `t.Skip("bundle row removal is bash-side; covered by tests/wrappers/otto-gw-support-rows_test.bats")`. This is a sentinel test so the Wave 0 inventory in 18-VALIDATION.md is satisfied and future maintainers find the link.

    Run `bats tests/wrappers/otto-gw-support-rows_test.bats` — confirm FAIL on the two negative-assertion tests (the rows still exist in the bundle). Commit: `test(18-03): RED — REL-TRAY-09 macOS bundle row absence`.

    GREEN phase:
    Open scripts/otto-gw. Locate the two row-emitting blocks:
    1. Lines ~1928-1935 (tray-state.txt block): REMOVE the entire `if [ -f "$OTTO_INSTALL_ROOT/.otto/tray/state" ] ... fi` block.
    2. Lines ~1951-1962 (autostart probe block): REMOVE the entire `if [ -f "$HOME/Library/LaunchAgents/com.otto.tray.plist" ] ... fi` block (use exact greppable header to confirm the boundary).

    Add a single-line comment where each block was removed:
    ```
    # tray-state.txt row removed in v1.10.3 per REL-TRAY-09 (read file the tray never wrote)
    # autostart probe row removed in v1.10.3 per REL-TRAY-09 (checked wrong plist; actual is io.cmetech.otto-tray)
    ```

    Re-run `bats tests/wrappers/otto-gw-support-rows_test.bats` — confirm PASS. Also run `bash -n scripts/otto-gw` + `shellcheck scripts/otto-gw` — both clean.

    Verify the existing manual repro test tests/reliability/manual/REL-TRAY-02-repro.ps1 still runs unchanged (read the file once; confirm zero references to "tray-state" or "com.otto.tray.plist" or "autostart.txt"; if any reference exists, the planner missed something and the test needs updating in the same commit per D-18-10 side-effect — but per RESEARCH.md A3 no update is expected).

    Commit: `feat(18-03): GREEN — REL-TRAY-09 remove broken macOS bundle rows (D-18-10)`.
  </action>

  <verify>
    <automated>bats tests/wrappers/otto-gw-support-rows_test.bats && bash -n scripts/otto-gw && shellcheck scripts/otto-gw && go test -race ./cmd/otto-tray/ -run TestRegression_REL_TRAY_09</automated>
  </verify>

  <acceptance_criteria>
    - tests/wrappers/otto-gw-support-rows_test.bats exists with the 3 cases (2 negative + 1 positive control).
    - bats tests pass on darwin (skip cleanly on Linux/Windows).
    - scripts/otto-gw no longer contains the tray-state.txt block. `grep -c 'tray-state.txt' scripts/otto-gw` returns 0 (or only the historical-removal comment line) — verify via `grep -v '^\s*#' scripts/otto-gw | grep -c 'tray-state.txt' == 0`.
    - scripts/otto-gw no longer contains the autostart probe block. `grep -v '^\s*#' scripts/otto-gw | grep -c 'com.otto.tray.plist' == 0`.
    - One-line removal comment present at each former site (for future maintainers per RESEARCH.md Pitfall 7).
    - `bash -n scripts/otto-gw` clean.
    - `shellcheck scripts/otto-gw` clean (no new findings introduced).
    - scripts/otto-gw.ps1 is BYTE-IDENTICAL to pre-plan for the Windows-bundle section. `git diff scripts/otto-gw.ps1` for this task scopes only to the Task 1 sentinel mirror, NOT the Windows bundle section.
    - tests/reliability/manual/REL-TRAY-02-repro.ps1 is BYTE-IDENTICAL to pre-plan (RESEARCH.md A3 confirmed no update needed).
    - cmd/otto-tray/regression_rel_tray_09_test.go exists as a Skip-stub linking to the bats test.
  </acceptance_criteria>

  <done>
    REL-TRAY-09 RED→GREEN cycle complete; both broken macOS rows removed from the bundle; existing repro tests untouched; Windows bundle path unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper script (bash/pwsh) → tray (Go binary) | The wrapper writes a file the tray reads — TOCTOU race window between write and read. |
| $HOME/.otto-gw/.config-error → sentinel-read code | Operator-controlled file (the wrapper writes it). Path traversal via $HOME manipulation possible if HOME is unexpectedly relative. |
| scripts/otto-gw support → tar archive consumer | Bundle is operator-only diagnostic; not transmitted externally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03-T | Tampering / TOCTOU | $HOME/.otto-gw/.config-error sentinel write+read | low | accept | D-18-09: wrapper writes sentinel BEFORE attempting gateway launch; tray polls every ~3-6s. Flap window ≤ 3s per RESEARCH.md Pitfall 6 + CONTEXT.md "Risks" row. The operator-visible impact is brief incorrect state during config reload — acceptable. Documented in task acceptance + sentinel-read code comment. |
| T-18-03-I | Information Disclosure | sentinel file contents may include partial env var values from parse error | low | mitigate | Mode 0o600 on the file; bash `chmod 600` and Go-side os.OpenFile is not used (read-only). Single-user laptop posture per quick 260531-ra6 / Phase 17 D-17-05 — no cross-user reader exists. Content trimmed to one line and capped at 200 bytes before being passed to Detail. |
| T-18-03-T | Path Traversal | $HOME manipulation in tray sentinel-read | low | mitigate | os.UserHomeDir + filepath.Join clean `..` segments. If HOME is unset or empty, os.UserHomeDir returns an error path; sentinel-read returns IsNotExist; stateInput.ConfigError stays empty — safe degraded mode. |
| T-18-03-I | Information Disclosure | support-bundle macOS rows previously contained tray-state.txt + autostart probe | low | mitigate | Out of scope for new disclosure: D-18-10 REMOVES the rows. Negative artifact — disclosure surface SHRINKS. No mitigation required; the threat is eliminated by row removal. |

ASVS L1 coverage: V5 (Input Validation — sentinel content trim/cap), V7 (Logging — minimal disclosure), V10 (Architecture — sentinel-file contract documented).
</threat_model>

<verification>
## Phase-close criteria covered by this plan

1. make ci exit 0 — verified after both tasks land.
2. Each REL-* has a regression test:
   - REL-TRAY-08 → TestRegression_REL_TRAY_08 (Go) — 5 cases (A1–A5)
   - REL-TRAY-09 → tests/wrappers/otto-gw-support-rows_test.bats (bash) + TestRegression_REL_TRAY_09 (Go skip-stub)
3. go test -race ./cmd/otto-tray/... clean.
6. Tray FSM state list UNCHANGED — `grep -n 'State[A-Z][a-zA-Z]*\s*=' cmd/otto-tray/fsm.go` produces the SAME line count pre vs post. NO StateConfigError introduced. Phase-close criterion 6 explicit.

Phase-wide criteria 4 (cmd.Stderr grep) and 5 (main.go INFO line) not in this plan's scope.

## Negative-grep gates

- `grep -v '^\s*#' scripts/otto-gw | grep -c 'tray-state.txt' == 0` (production lines, comments excluded).
- `grep -v '^\s*#' scripts/otto-gw | grep -c 'com.otto.tray.plist' == 0`.
- `grep -n '\.config-error' scripts/otto-gw scripts/otto-gw.ps1` returns ≥ 4 hits (write + delete in both wrappers).
- `grep -n '\.config-error' cmd/otto-tray/poller.go` returns ≥ 1 hit.
- `grep -n 'ConfigError' cmd/otto-tray/fsm.go cmd/otto-tray/poller.go` returns ≥ 3 hits (struct field decl + read in poller + use in computeState).
- `grep -c 'StateConfigError' cmd/otto-tray/` returns 0 (D-18-09 explicit no-new-state).
</verification>

<success_criteria>
- Both RED→GREEN cycles complete; test commits land BEFORE implementation commits.
- go test -race ./cmd/otto-tray/ clean.
- bats tests/wrappers/otto-gw-support-rows_test.bats passes on darwin.
- bash -n scripts/otto-gw clean.
- shellcheck scripts/otto-gw clean.
- make ci exit 0 end-to-end (v1.10.2 baseline preserved).
- Tray FSM state list unchanged (phase-close criterion 6).
- scripts/otto-gw.ps1 Windows path unchanged (only the sentinel-mirror addition in Task 1 modifies this file).
- tests/reliability/manual/REL-TRAY-02-repro.ps1 untouched.
- No new go.mod entries.
- PowerShell smoke deferred per quick 260531-oax precedent (no pwsh on darwin dev box); static review only.
</success_criteria>

<output>
Create `.planning/phases/18-reliability-long-tail/18-03-SUMMARY.md` when done. Required sections:
- REQ-ID closeout (REL-TRAY-08, REL-TRAY-09 each with regression test paths)
- Sentinel-file contract documentation snippet (path, mode, write/delete semantics)
- Removed bash blocks documented (which lines pre-edit, what they were)
- Commit hashes (4 expected: 2 RED + 2 GREEN)
- make ci output line confirming exit 0
- bats test output line confirming pass on darwin
- Note on PowerShell smoke deferral
- Any open follow-ups (e.g. macOS launch agent re-add if/when shipped — out of scope per D-18-10)
</output>
