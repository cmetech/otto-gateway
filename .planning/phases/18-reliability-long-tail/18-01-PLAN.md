---
phase: 18-reliability-long-tail
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/regression_rel_cfg_05_test.go
  - internal/config/regression_rel_cfg_06_test.go
  - internal/config/regression_rel_cfg_07_test.go
autonomous: true
requirements: [REL-CFG-05, REL-CFG-06, REL-CFG-07]
requirements_addressed: [REL-CFG-05, REL-CFG-06, REL-CFG-07]
tags: [go, config-validation, slog, reliability]
threat_model_summary:
  - "T-18-01: env tampering (ALLOWED_IPS/AUTH_TOKEN/KIRO_CMD/KIRO_CWD) — mitigated by Warn + treat-as-unset (D-18-01) and named error chain (D-18-02)"
  - "T-18-02: tilde-path traversal via KIRO_CWD — mitigated by os.UserHomeDir + filepath.Join (D-18-02)"
  - "T-18-03: HTTP_ADDR bind-DoS pre-bound by another process — mitigated by bind-then-close probe surfacing config-named error (D-18-03)"

must_haves:
  truths:
    - "ALLOWED_IPS / AUTH_TOKEN containing only whitespace/commas produces a slog.Warn record (from inside config.Load via slog.Default) AND is treated as unset (no allowlist / no auth) — per D-18-01"
    - "KIRO_CMD that does not resolve via exec.LookPath surfaces an errors.Join'd error of the form `config: KIRO_CMD (\"<value>\"): not found in PATH or unreadable` — per D-18-02"
    - "KIRO_CWD that is missing or not a directory surfaces a config-named error containing the variable name AND the offending value — per D-18-02"
    - "KIRO_CWD prefixed with `~/` (or exactly `~`) is expanded via os.UserHomeDir + filepath.Join during config.Load; the expanded path is what is stored in Config.KiroCWD — per D-18-02"
    - "HTTP_ADDR that is already bound surfaces a config-named error of the form `config: HTTP_ADDR (\"<addr>\"): bind probe failed: <wrapped syscall err>` from config.Load (not after pool warmup) — per D-18-03"
    - "The boot-time auth-state log line at cmd/otto-gateway/main.go:115-120 (`auth mode ... enabled=... ip_allowlist=...`) remains at INFO level — D-18-01 explicitly does not escalate it"
    - "Open Question §1 (research) — NOT in scope for this plan; engine-watchdog resolution belongs to 18-02"
    - "Open Question §2 (research) — NOT in scope for this plan; probeFunc extension belongs to 18-03"
    - "Open Question §3 (research) — NOT in scope for this plan; lazy-respawn-success reason belongs to 18-02"
    - "Open Question §4 (research) — NOT in scope for this plan; bufio.Reader.ReadString stderr scanner belongs to 18-02"
  artifacts:
    - path: internal/config/regression_rel_cfg_05_test.go
      provides: REL-CFG-05 regression test (Warn + treat-as-unset)
      contains: "TestRegression_REL_CFG_05"
    - path: internal/config/regression_rel_cfg_06_test.go
      provides: REL-CFG-06 regression test (named KIRO_CMD/KIRO_CWD errors + ~ expansion)
      contains: "TestRegression_REL_CFG_06"
    - path: internal/config/regression_rel_cfg_07_test.go
      provides: REL-CFG-07 regression test (bind-then-close probe surfaces named error)
      contains: "TestRegression_REL_CFG_07"
    - path: internal/config/config.go
      provides: Validation branches for D-18-01/02/03 — degenerate-env Warn lines, named KIRO_CMD/KIRO_CWD errors, ~/$HOME expansion, bind-then-close probe
  key_links:
    - from: internal/config/config.go (degenerate-env branch)
      to: slog.Default()
      via: slog.Default().Warn(...)
      pattern: "slog\\.Default\\(\\)\\.Warn"
    - from: internal/config/config.go (named errors)
      to: errs accumulator → errors.Join at end of Load()
      via: errs = append(errs, fmt.Errorf("config: KIRO_..."))
      pattern: "config: KIRO_(CMD|CWD)"
    - from: internal/config/config.go (port probe)
      to: net.Listen("tcp", httpAddr)
      via: "ln, lerr := net.Listen ..."
      pattern: "net\\.Listen\\(\"tcp\", httpAddr\\)"
---

<objective>
Close REL-CFG-05, REL-CFG-06, REL-CFG-07 — three config-load hardening items
that remove operator footguns introduced by degenerate env values, low-level
exec/stat errors, and post-warmup port-bind failures.

Purpose: After this plan lands, an operator with a malformed `.env` (or a
second instance of the gateway already bound to HTTP_ADDR) gets a loud,
named, actionable error in the FIRST seconds of boot — not five-to-ten
seconds later after pool warmup, and not silently with auth disabled.

Output:
  - 3 RED→GREEN test commits (one per REQ-ID)
  - 1 implementation commit per REQ-ID (3 total) adding the validation
    branches to internal/config/config.go
  - All three regression tests passing under `go test -race`
  - `make ci` exit 0 (no regression to v1.10.2 baseline)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-reliability-long-tail/18-CONTEXT.md
@.planning/phases/18-reliability-long-tail/18-RESEARCH.md
@.planning/phases/18-reliability-long-tail/18-VALIDATION.md
@internal/config/config.go
@internal/config/regression_rel_cfg_03_test.go
@internal/config/regression_rel_cfg_01_test.go
@cmd/otto-gateway/main.go
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (exclude these from drift checks):

- `TestRegression_REL_CFG_05` — internal/config/regression_rel_cfg_05_test.go
- `TestRegression_REL_CFG_06` — internal/config/regression_rel_cfg_06_test.go
- `TestRegression_REL_CFG_07` — internal/config/regression_rel_cfg_07_test.go
- Three new files under internal/config/:
  - regression_rel_cfg_05_test.go
  - regression_rel_cfg_06_test.go
  - regression_rel_cfg_07_test.go
- Three new error strings emitted from config.Load():
  - `config: KIRO_CMD ("<value>"): not found in PATH or unreadable`
  - `config: KIRO_CWD ("<value>"): directory does not exist`
  - `config: KIRO_CWD ("<value>"): not a directory`
  - `config: HTTP_ADDR ("<addr>"): bind probe failed: <wrapped syscall err>`
- Two new slog.Warn lines emitted from config.Load() via slog.Default():
  - `AUTH_TOKEN looks degenerate (no entries after trim+CSV split); treating as unset`
  - `ALLOWED_IPS looks degenerate (no entries after trim+CSV split); treating as unset`

No new exported types, no new env vars, no new public functions.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — REL-CFG-05 degenerate ALLOWED_IPS / AUTH_TOKEN</name>
  <files>internal/config/regression_rel_cfg_05_test.go, internal/config/config.go</files>

  <read_first>
    - internal/config/config.go — focus on lines 295–330 (env load block) and the existing REL-CFG-03 emission shape at lines 576–580 (this is the slog.Default().Warn precedent the new lines mirror)
    - internal/config/regression_rel_cfg_03_test.go — lines 31–93: the `captureSlogDefault` helper + `decodeLogRecords` helper that the new test reuses (same package, same file scope)
    - internal/config/regression_rel_cfg_01_test.go — lines 32–87 for table-driven test scaffolding precedent
    - .planning/phases/18-reliability-long-tail/18-CONTEXT.md §D-18-01
    - .planning/phases/18-reliability-long-tail/18-RESEARCH.md §"Pattern 2: slog.Default capture" and §"D-18-01 REL-CFG-05" code example
  </read_first>

  <behavior>
    RED test cases (write FIRST, expect to fail against unmodified config.go):
    - Case A: `t.Setenv("AUTH_TOKEN", " , ")` → captureSlogDefault buffer contains a record with msg="AUTH_TOKEN looks degenerate (no entries after trim+CSV split); treating as unset" at level=WARN; cfg.AuthTokens is empty.
    - Case B: `t.Setenv("ALLOWED_IPS", ",,  ,")` → captureSlogDefault buffer contains a record with msg="ALLOWED_IPS looks degenerate (no entries after trim+CSV split); treating as unset" at level=WARN; cfg.AllowedIPs is empty.
    - Case C: `t.Setenv("AUTH_TOKEN", "real-token")` → NO degenerate Warn record; cfg.AuthTokens equals []string{"real-token"}.
    - Case D: `t.Setenv("ALLOWED_IPS", "127.0.0.1/32")` → NO degenerate Warn record; cfg.AllowedIPs has one entry.
    - Case E: `os.Unsetenv("AUTH_TOKEN")` AND `os.Unsetenv("ALLOWED_IPS")` → NO degenerate Warn records (both unset is the normal case, not degenerate).
    - Case F (whitespace-only AUTH_TOKEN): `t.Setenv("AUTH_TOKEN", "   ")` → WARN emitted; treat as unset.

    GREEN: config.go emits the Warn lines via slog.Default() and AuthTokens/AllowedIPs are returned empty in the degenerate cases.
  </behavior>

  <action>
    Phase A (RED):
    Create `internal/config/regression_rel_cfg_05_test.go` in package `config` (mirroring `regression_rel_cfg_03_test.go` package declaration). Reuse `captureSlogDefault(t)` and `decodeLogRecords(t, buf)` from `regression_rel_cfg_03_test.go` (same package — no import needed; just call the helpers). Write a table-driven test `TestRegression_REL_CFG_05` covering Cases A–F above. Each subtest:
      1. Calls `captureSlogDefault(t)` to install the buffer-handler.
      2. Calls `t.Setenv(...)` / `os.Unsetenv(...)` for the relevant env vars (and `t.Setenv("KIRO_CMD", "go")` so KIRO_CMD validation doesn't poison the test — Task 2 will land KIRO_CMD validation in the same package, so use a binary guaranteed to be in PATH on dev + CI machines).
      3. Calls `config.Load()` (or whichever exported loader name applies).
      4. Asserts via `decodeLogRecords` that the expected Warn record IS or IS NOT present in the buffer (depending on case).
      5. Asserts cfg.AuthTokens / cfg.AllowedIPs match expected shape (empty vs populated).
    Run `go test -race ./internal/config/ -run TestRegression_REL_CFG_05` and confirm it FAILS (no Warn records emitted from current code). Commit as `test(18-01): RED — REL-CFG-05 degenerate AUTH_TOKEN/ALLOWED_IPS Warn assertion`.

    Phase B (GREEN):
    Open `internal/config/config.go`. At the AUTH_TOKEN load site (currently ~line 317 — `authTokens := getEnvStrSliceComma("AUTH_TOKEN", nil)`), insert BEFORE the load:
        `rawAuth := strings.TrimSpace(os.Getenv("AUTH_TOKEN"))`
    AFTER the load, insert:
        `if rawAuth != "" && len(authTokens) == 0 { slog.Default().Warn("AUTH_TOKEN looks degenerate (no entries after trim+CSV split); treating as unset", "raw", rawAuth) }`

    Same shape at the ALLOWED_IPS load site (currently ~lines 319–323):
        `rawAllowed := strings.TrimSpace(os.Getenv("ALLOWED_IPS"))`
    After the existing load + `parseCIDRs(allowedIPEntries)` call:
        `if rawAllowed != "" && len(allowedIPs) == 0 && err == nil { slog.Default().Warn("ALLOWED_IPS looks degenerate (no entries after trim+CSV split); treating as unset", "raw", rawAllowed) }`
    (The `&& err == nil` guard prevents the Warn from firing when parseCIDRs already returned a real error — that error path is non-degenerate and should land in errs / errors.Join, not Warn.)

    Confirm `strings` is already imported in config.go; `log/slog` is already imported per RESEARCH.md §"Pattern 2" precedent. No new imports.

    Re-run `go test -race ./internal/config/ -run TestRegression_REL_CFG_05` and confirm PASS. Commit as `feat(18-01): GREEN — REL-CFG-05 Warn on degenerate AUTH_TOKEN/ALLOWED_IPS (D-18-01)`.
  </action>

  <verify>
    <automated>go test -race ./internal/config/ -run TestRegression_REL_CFG_05</automated>
  </verify>

  <acceptance_criteria>
    - File `internal/config/regression_rel_cfg_05_test.go` exists with `TestRegression_REL_CFG_05` table-driven test covering all 6 cases above.
    - Each subtest is independent (uses `t.Setenv` for env, not `os.Setenv`).
    - `go test -race ./internal/config/ -run TestRegression_REL_CFG_05` is RED before the config.go edit and GREEN after.
    - config.go emits exactly TWO new Warn calls via `slog.Default().Warn(...)`. The msg strings are byte-exact:
        - `AUTH_TOKEN looks degenerate (no entries after trim+CSV split); treating as unset`
        - `ALLOWED_IPS looks degenerate (no entries after trim+CSV split); treating as unset`
    - Field set on both Warn calls: a single `"raw"` field carrying the trimmed input.
    - Both Warns fire AFTER the existing env load and BEFORE the existing errors.Join return.
    - `grep -v '^\\s*//' internal/config/config.go | grep -c 'slog.Default().Warn'` ≥ existing-count + 2.
    - `cmd/otto-gateway/main.go:115-120` `logger.Info("auth mode", ...)` line is BYTE-IDENTICAL pre vs post (D-18-01 explicitly preserves it). `git diff cmd/otto-gateway/main.go` shows zero changes in this plan.
    - `go test -race ./internal/config/...` passes wholesale (no other tests regressed).
  </acceptance_criteria>

  <done>
    REL-CFG-05 RED→GREEN cycle complete: degenerate AUTH_TOKEN/ALLOWED_IPS values emit a Warn record from config.Load via slog.Default and are returned as empty slices (auth off, allowlist off — matching the documented "no auth if env unset" posture). Boot-time INFO auth-state line at main.go:115-120 unchanged.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED→GREEN — REL-CFG-06 named KIRO_CMD / KIRO_CWD errors + ~ expansion</name>
  <files>internal/config/regression_rel_cfg_06_test.go, internal/config/config.go</files>

  <read_first>
    - internal/config/config.go — focus on lines 295–298 (KIRO_CMD + KIRO_CWD load) and the existing errs accumulator + errors.Join return (~line 583)
    - internal/config/regression_rel_cfg_01_test.go — table-driven scaffold precedent for named-error assertions (lines 32–87)
    - internal/acp/client.go — search for `exec.LookPath` / `exec.CommandContext` to confirm KIRO_CMD usage (no behavioral change required here; just understand the downstream contract)
    - .planning/phases/18-reliability-long-tail/18-CONTEXT.md §D-18-02
    - .planning/phases/18-reliability-long-tail/18-RESEARCH.md §"D-18-02 REL-CFG-06" codebase-map rows
  </read_first>

  <behavior>
    RED test cases (write FIRST, expect to fail against unmodified config.go):
    - Case A: `t.Setenv("KIRO_CMD", "definitely-not-a-real-binary-zzz")` → config.Load returns an error matching `config: KIRO_CMD ("definitely-not-a-real-binary-zzz"): not found in PATH or unreadable`. Assert via `strings.Contains(err.Error(), ...)` so wrapping order doesn't matter.
    - Case B: `t.Setenv("KIRO_CMD", "go")` (binary guaranteed in PATH on dev + CI) → config.Load returns no KIRO_CMD-related error.
    - Case C: `t.Setenv("KIRO_CWD", "/nonexistent/path/zzz-18-01")` → error matches `config: KIRO_CWD ("/nonexistent/path/zzz-18-01"): directory does not exist`.
    - Case D: `t.Setenv("KIRO_CWD", "<path-to-a-file>")` where the path is created via `t.TempDir() + "/file"` and `os.WriteFile` — expect error matches `config: KIRO_CWD ("<path>"): not a directory`.
    - Case E: `t.Setenv("HOME", t.TempDir())` + `t.Setenv("KIRO_CWD", "~/sub")`; create `<HOME>/sub` directory; call config.Load; assert NO error AND `cfg.KiroCWD` equals `filepath.Join(<HOME>, "sub")` (expansion applied).
    - Case F: `os.Unsetenv("KIRO_CWD")` AND `t.Setenv("KIRO_CMD", "go")` → no error related to KIRO_CWD (empty is the default — Cwd is optional).
    - Case G: `t.Setenv("KIRO_CWD", "~")` alone (no slash) → expands to HOME exactly.

    GREEN: config.go performs LookPath on KIRO_CMD, stat+IsDir on KIRO_CWD, tilde expansion when KIRO_CWD starts with `~/` or equals `~`. Errors append to existing errs slice via errors.Join idiom.
  </behavior>

  <action>
    Phase A (RED):
    Create `internal/config/regression_rel_cfg_06_test.go` in package `config`. Table-driven test `TestRegression_REL_CFG_06` covering Cases A–G. Each subtest:
      1. Resets all env vars used by config.Load via `t.Setenv` (defaults + the case-under-test override) so previous subtests don't leak. Critical: `t.Setenv("AUTH_TOKEN", "")` and `t.Setenv("ALLOWED_IPS", "")` so REL-CFG-05 doesn't interfere; `t.Setenv("HTTP_ADDR", "127.0.0.1:0")` to avoid REL-CFG-07 bind-probe interference once Task 3 lands.
      2. Calls `config.Load()`.
      3. For error cases: `require/assert.Contains(err.Error(), <expected substring>)`.
      4. For success cases: assert `err == nil` (or that the returned error does NOT contain the KIRO_CMD/KIRO_CWD substrings).
      5. For Case E/G: also assert `cfg.KiroCWD == filepath.Join(home, "sub")` / `cfg.KiroCWD == home`.
    Run `go test -race ./internal/config/ -run TestRegression_REL_CFG_06` and confirm FAIL. Commit as `test(18-01): RED — REL-CFG-06 named KIRO_CMD/KIRO_CWD errors + ~ expansion`.

    Phase B (GREEN):
    Open `internal/config/config.go`. After the KIRO_CMD load (~line 296 `kiroCmd := getEnvStr("KIRO_CMD", "kiro-cli")`), insert:
        ```
        if _, lookErr := exec.LookPath(kiroCmd); lookErr != nil {
            errs = append(errs, fmt.Errorf("config: KIRO_CMD (%q): not found in PATH or unreadable", kiroCmd))
        }
        ```
    (Do NOT inline a code fence in the action text — this is shown here only as the literal source to place into config.go.)

    After the KIRO_CWD load (~line 298 `kiroCWD := getEnvStr("KIRO_CWD", "")`), insert (with tilde expansion FIRST):
        - If `strings.HasPrefix(kiroCWD, "~/")` then `home, herr := os.UserHomeDir(); if herr == nil { kiroCWD = filepath.Join(home, kiroCWD[2:]) }`.
        - Else if `kiroCWD == "~"` then `home, herr := os.UserHomeDir(); if herr == nil { kiroCWD = home }`.
        - Then `if kiroCWD != "" { stat, sErr := os.Stat(kiroCWD); switch { case sErr != nil: errs = append(errs, fmt.Errorf("config: KIRO_CWD (%q): directory does not exist", kiroCWD)); case !stat.IsDir(): errs = append(errs, fmt.Errorf("config: KIRO_CWD (%q): not a directory", kiroCWD)) }`.
    Skip BOTH checks when `kiroCWD == ""` (default — Cwd is optional per CONTEXT.md).

    Store the (possibly expanded) value in `Config.KiroCWD` exactly as RESEARCH.md §Codebase-Map row mandates.

    Required imports for config.go (verify with `goimports` / `go build`): `os/exec`, `path/filepath`, `strings` (already present per Task 1). `os` and `fmt` already present.

    Re-run `go test -race ./internal/config/ -run TestRegression_REL_CFG_06` and confirm PASS. Commit as `feat(18-01): GREEN — REL-CFG-06 named KIRO_CMD/KIRO_CWD errors + ~ expansion (D-18-02)`.
  </action>

  <verify>
    <automated>go test -race ./internal/config/ -run TestRegression_REL_CFG_06</automated>
  </verify>

  <acceptance_criteria>
    - File `internal/config/regression_rel_cfg_06_test.go` exists; Cases A–G all assert as described above.
    - The KIRO_CMD error string is byte-exact: `config: KIRO_CMD ("<value>"): not found in PATH or unreadable`. `%q` formatting on the value (matches RESEARCH.md recommendation).
    - The KIRO_CWD errors are byte-exact:
        - `config: KIRO_CWD ("<value>"): directory does not exist`
        - `config: KIRO_CWD ("<value>"): not a directory`
    - `cfg.KiroCWD` carries the tilde-expanded path when input started with `~/` or equaled `~`.
    - When `KIRO_CWD == ""` (unset), neither check fires and `cfg.KiroCWD == ""` exactly.
    - `go test -race ./internal/config/...` passes wholesale; existing REL-CFG-01/03 tests still green.
    - No new dependency added to `go.mod` (only stdlib).
    - Down-stream usage in `internal/acp/client.go` not modified (the expanded path flows transparently because Cwd consumption was already path-based).
  </acceptance_criteria>

  <done>
    REL-CFG-06 RED→GREEN cycle complete: KIRO_CMD/KIRO_CWD misconfiguration surfaces a config-named, actionable error from config.Load; tilde expansion works on both POSIX and Windows via os.UserHomeDir.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RED→GREEN — REL-CFG-07 bind-then-close HTTP_ADDR port probe</name>
  <files>internal/config/regression_rel_cfg_07_test.go, internal/config/config.go</files>

  <read_first>
    - internal/config/config.go — line 295 (`httpAddr := getEnvStr("HTTP_ADDR", "127.0.0.1:18080")`) and the errs accumulator pattern downstream
    - internal/server/server.go — search for `ListenAndServe` to confirm the real bind site exists at ~line 443 (no edit here; just understanding)
    - .planning/phases/18-reliability-long-tail/18-CONTEXT.md §D-18-03
    - .planning/phases/18-reliability-long-tail/18-RESEARCH.md §"D-18-03 REL-CFG-07" code example
  </read_first>

  <behavior>
    RED test cases (write FIRST, expect to fail against unmodified config.go):
    - Case A: Test pre-binds a listener on `127.0.0.1:0` (kernel-assigned port), reads its `Addr().String()`, then `t.Setenv("HTTP_ADDR", <that addr>)`. Calls config.Load. Asserts error contains the substring `HTTP_ADDR (` and the substring `bind probe failed`. Closes the pre-bound listener in `t.Cleanup`.
    - Case B: `t.Setenv("HTTP_ADDR", "127.0.0.1:0")` (kernel-assigned — always bindable) → config.Load returns no HTTP_ADDR-related error.
    - Case C: `os.Unsetenv("HTTP_ADDR")` — default `127.0.0.1:18080` is used. The probe still runs (per CONTEXT.md "Probe runs unconditionally"). If 18080 is bound on the dev/CI box, the test must NOT depend on its free state — use the env explicitly in this case via `t.Setenv("HTTP_ADDR", "127.0.0.1:0")` for the success path. **Replace Case C with**: `t.Setenv("HTTP_ADDR", "127.0.0.1:0")` → ensure probe-success path also handles the kernel-assigned-port case cleanly (the bind succeeds, the close succeeds, no error appended).

    GREEN: config.go performs `net.Listen("tcp", httpAddr)` + immediate `Close()`. On error, appends a wrapped, config-named error to errs.
  </behavior>

  <action>
    Phase A (RED):
    Create `internal/config/regression_rel_cfg_07_test.go` in package `config`. Table-driven `TestRegression_REL_CFG_07` covering Cases A and B (drop C — kernel-assigned-0 + a pre-bound port deterministically cover both branches).

    Case A scaffold (port-in-use):
      1. `t.Setenv("KIRO_CMD", "go")` + `os.Unsetenv("AUTH_TOKEN")` + `os.Unsetenv("ALLOWED_IPS")` + `os.Unsetenv("KIRO_CWD")` (silence Tasks 1–2 branches).
      2. `ln, err := net.Listen("tcp", "127.0.0.1:0")`. `t.Cleanup(func() { ln.Close() })`.
      3. `addr := ln.Addr().String()`. `t.Setenv("HTTP_ADDR", addr)`.
      4. `_, loadErr := config.Load()`. Assert loadErr != nil. Assert `strings.Contains(loadErr.Error(), "HTTP_ADDR (")` AND `strings.Contains(loadErr.Error(), "bind probe failed")` AND `strings.Contains(loadErr.Error(), addr)`.

    Case B scaffold (free port):
      1. Same env silencing as A.
      2. `t.Setenv("HTTP_ADDR", "127.0.0.1:0")` (kernel will assign + close).
      3. `_, loadErr := config.Load()`. Assert that even if loadErr != nil, `!strings.Contains(loadErr.Error(), "HTTP_ADDR")` (no HTTP_ADDR-related error message).

    Run `go test -race ./internal/config/ -run TestRegression_REL_CFG_07` and confirm FAIL on Case A (no error today). Commit as `test(18-01): RED — REL-CFG-07 bind-then-close HTTP_ADDR port probe`.

    Phase B (GREEN):
    Open `internal/config/config.go`. After all other env loads (the existing pattern is to accumulate everything into errs then call `errors.Join` at the end — match that), and crucially BEFORE the existing errors.Join return at ~line 583, insert:
        ```
        if ln, lerr := net.Listen("tcp", httpAddr); lerr != nil {
            errs = append(errs, fmt.Errorf("config: HTTP_ADDR (%q): bind probe failed: %w", httpAddr, lerr))
        } else {
            _ = ln.Close()
        }
        ```
    (Shown here only as the literal placement source.)

    Place the probe AFTER httpAddr is loaded but at the END of the validation block so all other errors still accumulate together (one errors.Join surface).

    Verify imports: `net` is new to config.go — add to the import block. `fmt` already present.

    Re-run `go test -race ./internal/config/ -run TestRegression_REL_CFG_07` and confirm PASS. Commit as `feat(18-01): GREEN — REL-CFG-07 bind-then-close HTTP_ADDR probe (D-18-03)`.
  </action>

  <verify>
    <automated>go test -race ./internal/config/ -run TestRegression_REL_CFG_07</automated>
  </verify>

  <acceptance_criteria>
    - File `internal/config/regression_rel_cfg_07_test.go` exists; Cases A + B assert as described.
    - The HTTP_ADDR error contains the substring `config: HTTP_ADDR (` AND `bind probe failed`. Wrapped error chain preserves the underlying syscall error via `%w` so `errors.Unwrap` returns a net.OpError or net.AddrError variant.
    - Probe runs unconditionally — even when HTTP_ADDR is the default value (per CONTEXT.md).
    - Probe runs AFTER other env loads so all config errors surface in a single errors.Join.
    - Probe uses `net.Listen("tcp", addr)` + immediate `Close()`. NO retry, NO timeout, NO lsof / netstat parsing (per CONTEXT.md TOCTOU acceptance).
    - `go test -race ./internal/config/...` passes wholesale.
    - `go vet ./internal/config/...` clean.
    - `go build ./...` succeeds — confirming the new `net` import didn't conflict.
  </acceptance_criteria>

  <done>
    REL-CFG-07 RED→GREEN cycle complete: port-in-use is discovered in config.Load (within milliseconds), not after a 5–10s pool warmup. The bind-then-close TOCTOU window is documented and accepted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-controlled env → config.Load | Tainted env input (ALLOWED_IPS / AUTH_TOKEN / KIRO_CMD / KIRO_CWD / HTTP_ADDR) crosses here. Same trust boundary as the Node version. |
| KIRO_CWD `~/` literal → filepath.Join | Tilde-expanded path is then handed to acp.Client as Cwd. Operator-controlled, not request-time. |
| net.Listen probe → real ListenAndServe | TOCTOU window (microseconds) between probe close and real bind. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01-S | Spoofing / Tampering | env loader for AUTH_TOKEN / ALLOWED_IPS | low | mitigate | D-18-01: detect degenerate input via `strings.TrimSpace(os.Getenv(...))` + parsed-empty comparison; emit slog.Warn from inside config.Load() so the operator sees the silent-disable in logs. ASVS V2 (Authentication) + V4 (Access Control) UX hardening. Verified by Task 1 cases A/B/F. |
| T-18-01-T | Tampering | env loader for KIRO_CMD / KIRO_CWD | low | mitigate | D-18-02: exec.LookPath check on KIRO_CMD; os.Stat + IsDir check on KIRO_CWD; named errors surface the offending variable + value. Path-traversal via `~/..` is bounded by filepath.Join which cleans `..` segments. ASVS V5 (Input Validation). Verified by Task 2 cases A/C/D. |
| T-18-01-D | Denial of Service | HTTP_ADDR bind | low | mitigate | D-18-03: bind-then-close probe surfaces port-in-use pre-warmup, preventing the operator from paying the 5–10s warmup cost before failing. TOCTOU window (microseconds) accepted per CONTEXT.md — the probe is best-effort, not authoritative. Verified by Task 3 case A. |
| T-18-01-I | Information Disclosure | config-named error strings include the offending env value | low | accept | The values are operator-controlled paths/binary names; surfacing them in logs is the documented behavior per `%q`-formatted Errorf. AUTH_TOKEN value is NOT logged (only the "raw" CSV-trimmed string in the degenerate-Warn case, which by definition is whitespace/commas). No PII or secret leaks expected. |

ASVS L1 coverage: V2 (Authentication UX), V4 (Access Control UX), V5 (Input Validation), V7 (Error Handling and Logging — structured slog).
</threat_model>

<verification>
## Phase-close criteria covered by this plan

1. `make ci` exit 0 — verified by running `make ci` after all three tasks land.
2. Each REL-* has a regression test:
   - REL-CFG-05 → `TestRegression_REL_CFG_05`
   - REL-CFG-06 → `TestRegression_REL_CFG_06`
   - REL-CFG-07 → `TestRegression_REL_CFG_07`
3. `go test -race ./internal/config/...` clean.
5. Boot-time auth-state line at `cmd/otto-gateway/main.go:115-120` UNCHANGED — `git diff cmd/otto-gateway/main.go` shows zero edits in this plan.

Phase-wide criteria 4 (`grep -rn "cmd.Stderr = os.Stderr"`) and 6 (no `StateConfigError`) are NOT in this plan's scope — Plan 18-02 and Plan 18-03 own them respectively.

## Negative-grep gates

- `grep -v '^\s*//' internal/config/config.go | grep -c 'slog.Default().Warn'` must increase by 2 (new degenerate-env Warns).
- `grep -c 'config: KIRO_CMD' internal/config/config.go` must equal 1.
- `grep -c 'config: KIRO_CWD' internal/config/config.go` must equal 2 (two distinct error strings).
- `grep -c 'config: HTTP_ADDR' internal/config/config.go` must equal 1.
- `git diff cmd/otto-gateway/main.go` must show zero hunks for this plan (D-18-01 INFO line preservation).
</verification>

<success_criteria>
- All three RED→GREEN cycles complete with the test commits landing BEFORE the implementation commits (TDD ordering visible in `git log`).
- `go test -race ./internal/config/...` exits 0.
- `go vet ./internal/config/...` exits 0.
- `go build ./...` succeeds tree-wide.
- `make ci` exit 0 end-to-end (v1.10.2 baseline preserved).
- No new go.mod entries.
- `cmd/otto-gateway/main.go` byte-identical to pre-plan baseline (D-18-01 explicit preservation).
- `Config.KiroCWD` field type unchanged (still `string`); only its assigned value differs when tilde-prefixed.
</success_criteria>

<output>
Create `.planning/phases/18-reliability-long-tail/18-01-SUMMARY.md` when done. Required sections:
- REQ-ID closeout (REL-CFG-05, REL-CFG-06, REL-CFG-07 each with the regression test path)
- Commit hashes (3 RED + 3 GREEN commits expected, or 6 commits batched into RED→GREEN pairs)
- `make ci` output line confirming exit 0
- Note any open follow-ups (none expected — all three D-IDs lock cleanly)
</output>
