---
phase: 03-openai-surface
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - cmd/otto-gateway/main.go
  - internal/adapter/openai/integration_test.go
autonomous: false
requirements: [SURF-02, SURF-04, SURF-06]
must_haves:
  truths:
    - "cmd/otto-gateway/main.go constructs the OpenAI adapter only when ENABLED_SURFACES contains openai, builds the []server.SurfaceMount list (Ollama→/api, Anthropic→prefix, OpenAI→/v1), and passes it to server.NewFromConfig"
    - "ENABLED_SURFACES=ollama (subset omitting openai) leaves /v1/chat/completions unmounted; the default ollama,anthropic,openai mounts all three"
    - "A real-kiro round-trip through the mounted OpenAI adapter (stream and non-stream) returns a valid OpenAI response sourced from a live kiro-cli subprocess, with no goroutine leak"
    - "A Pi-SDK CLI configured with baseUrl=http://localhost:11434/v1 + bearer completes a streamed chat round-trip against the running gateway with zero SDK modification (SC2 HUMAN-UAT)"
    - "make ci is green including arch-lint with the adapter_openai boundary, all three adapters importing only canonical (+plugin), none importing engine (SC5)"
  artifacts:
    - path: "cmd/otto-gateway/main.go"
      provides: "openai surface gating + openaiEngineAdapter/openaiRunHandleAdapter bridge + SurfaceMount list wiring + boot-log openai_mounted"
      contains: "openaiEngineAdapter"
    - path: "internal/adapter/openai/integration_test.go"
      provides: "real-kiro round-trip (stream + non-stream) gated on KIRO_CMD"
      contains: "Integration"
  key_links:
    - from: "cmd/otto-gateway/main.go"
      to: "server.NewFromConfig"
      via: "Surfaces []server.SurfaceMount list (replaces parallel ProtectedRouter fields)"
      pattern: "SurfaceMount"
    - from: "cmd/otto-gateway/main.go openai branch"
      to: "openai.New(openai.Config{Engine, ModelCatalog})"
      via: "slices.Contains(cfg.EnabledSurfaces, \"openai\") gate; pool as ModelCatalog"
      pattern: "slices.Contains.*openai|EnabledSurfaces"
---

<objective>
Wire the OpenAI surface into the running binary and prove the full phase acceptance:
the composition root (`cmd/otto-gateway/main.go`) gates the OpenAI adapter on
ENABLED_SURFACES, bridges the concrete `*engine.Engine` to the adapter's local
interface, builds the `[]server.SurfaceMount` list (the D-01 contract from Plan 01),
and passes it to `server.NewFromConfig`. This replaces the parallel
`OllamaProtectedRouter`/`AnthropicProtectedRouter` Config fields with the generic list.

This is the final vertical-slice step: after this plan a real Pi-SDK client hits the
running gateway over real HTTP and gets a streamed kiro-cli-sourced OpenAI response —
the SC2 acceptance bar — and the architectural boundary check (SC5) confirms all
three adapters stay engine-free.

Purpose: SC1–SC5 all pass against the real binary; Pi HUMAN-UAT confirms the
end-to-end round-trip with zero SDK modification.
Output: main.go wiring (gating + bridge + SurfaceMount list + boot log), the
real-kiro integration round-trip, and a blocking HUMAN-UAT checkpoint for Pi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-openai-surface/03-CONTEXT.md
@.planning/phases/03-openai-surface/03-RESEARCH.md
@.planning/phases/03-openai-surface/03-PATTERNS.md
@.planning/phases/03-openai-surface/03-VALIDATION.md
@.planning/phases/03-openai-surface/03-01-SUMMARY.md
@.planning/phases/03-openai-surface/03-02-SUMMARY.md
@.planning/phases/03-openai-surface/03-03-SUMMARY.md

<interfaces>
<!-- From Plans 01-03 -->
server.Config now has `Surfaces []SurfaceMount` (Prefix string, Router RouteRegistrar w/ RegisterRoutes(chi.Router)); OllamaVersionHandler + its prefix remain as outer-router fields (Plan 01).
openai.New(openai.Config{Logger, Engine, ModelCatalog}) *openai.Adapter // Adapter implements RegisterRoutes (RouteRegistrar)
openai.Engine = {Collect(ctx,*canonical.ChatRequest)(*canonical.ChatResponse,error); Run(ctx,*canonical.ChatRequest)(RunHandle,error)}
a.pool already satisfies Models() []canonical.ModelInfo (used at main.go:171-174 as catalogForAdapter) — pass the SAME pool as openai ModelCatalog.

<!-- Pi UAT config (RESEARCH Pi SDK Contract — VERIFIED, baseUrl key + stream:true hard-coded) -->
~/.gsd/agent/models.json provider: {baseUrl:"http://localhost:11434/v1", api:"openai-completions", apiKey:"<AUTH_TOKEN or any>", models:[{id:"auto"}]}
Pi hard-codes stream:true → exercises the SSE path. Official openai npm SDK sends Authorization: Bearer <apiKey>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.go wiring — gating + engine bridge + SurfaceMount list + boot log</name>
  <read_first>
    - cmd/otto-gateway/main.go (the file being modified: anthropic gating block lines 187-206; the catalogForAdapter usage lines 171-174; anthropicEngineAdapter/anthropicRunHandleAdapter bridge lines 292-352; the server.NewFromConfig call with parallel ProtectedRouter fields lines 259-262; the boot-log line lines 243-248)
    - internal/server/server.go (Plan 01 refactored Config — the Surfaces []SurfaceMount field + RouteRegistrar this wiring must satisfy)
    - .planning/phases/03-openai-surface/03-PATTERNS.md § Modified-File Edit Map → cmd/otto-gateway/main.go + § Shared Patterns → Engine bridge
  </read_first>
  <files>cmd/otto-gateway/main.go</files>
  <action>
    In `newApp` (after the anthropic block at lines 187-206), add an `if slices.Contains(cfg.EnabledSurfaces, "openai")` branch constructing `openai.New(openai.Config{Logger: …, Engine: openaiEngineAdapter{eng}, ModelCatalog: <the same pool/catalogForAdapter used for ollama at main.go:171-174>})`. Add `openaiEngineAdapter`/`openaiRunHandleAdapter` types by copying the anthropic bridge (main.go:292-352) verbatim and renaming — Go return-type invariance requires the explicit wrapper; `Run` returns the local RunHandle interface, `Collect` passes through.
    Replace the parallel `OllamaProtectedRouter`/`AnthropicProtectedRouter` fields in the `server.NewFromConfig` call (lines 259-262) with a `Surfaces: []server.SurfaceMount{...}` list: append `{Prefix: cfg.OllamaPathPrefix (or "/api"), Router: <ollama adapter>}`, `{Prefix: cfg.AnthropicPathPrefix, Router: <anthropic adapter>}` (only when anthropic enabled), `{Prefix: cfg.OpenAIPathPrefix, Router: openaiAdapter}` (only when openai enabled). Each adapter must expose RegisterRoutes (RouteRegistrar) — Ollama and Anthropic adapters were exposing ProtectedRouter(); per D-01 planner's discretion you MUST either add a RegisterRoutes method to them or adapt the SurfaceMount.Router type to accept their existing router. Choose the RegisterRoutes approach for consistency (RESEARCH recommendation) — if the Ollama/Anthropic adapters only expose ProtectedRouter(), add a thin RegisterRoutes that registers their routes onto the passed chi.Router (mirror the openai adapter), or have the server wrap a chi.Router into a RouteRegistrar. Keep the OllamaVersionHandler + its prefix on the outer-router fields (unchanged — auth-exempt /api/version).
    Update the boot-log line (lines 243-248) to include an `openai_mounted` key reflecting whether the openai surface was added.
    NOTE: this task changes the Ollama/Anthropic adapter route-exposure mechanism if they don't already have RegisterRoutes — that is in-scope here (the D-01 refactor is not complete until main.go compiles against the new server.Config). Do not break the existing Ollama/Anthropic routes; the existing adapter integration tests must still pass.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/server/... ./internal/adapter/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds (the whole binary compiles against the refactored server.Config).
    - `go test ./internal/server/... ./internal/adapter/...` passes (Ollama + Anthropic + OpenAI adapter + server tests all green — no regression from the route-exposure change).
    - Source assertion: `grep -q 'openaiEngineAdapter' cmd/otto-gateway/main.go` and `grep -q 'SurfaceMount' cmd/otto-gateway/main.go`.
    - Source assertion: `grep -q 'slices.Contains(cfg.EnabledSurfaces, "openai")' cmd/otto-gateway/main.go` (gating).
    - Source assertion: `grep -q 'openai_mounted' cmd/otto-gateway/main.go` (boot-log observability for the widened default — Runtime State Inventory).
    - Behavior: with ENABLED_SURFACES omitting openai, no openai SurfaceMount is appended (gating proven by reading the branch; a config-level test or boot smoke can confirm).
  </acceptance_criteria>
  <done>main.go gates the OpenAI adapter on ENABLED_SURFACES, bridges the engine, builds the SurfaceMount list (all three surfaces), logs openai_mounted, and the whole binary compiles with all adapter+server tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Real-kiro integration round-trip (stream + non-stream) + make ci gate</name>
  <read_first>
    - internal/adapter/anthropic/integration_test.go (analog: resolveKiroCLI lines 32-45, kiroSetup lines 57-101, realEngineAdapter lines 107-…, the real-kiro round-trip pattern + the KIRO_CMD skip gate; SSE assertion lines 232-244; bufio.Scanner frame loop lines 274-279)
    - internal/adapter/openai/integration_test.go (Plan 02 — extend the fake-engine round-trip with the real-kiro path if not already present)
    - .planning/phases/03-openai-surface/03-VALIDATION.md § Sampling Rate (phase gate: make ci green incl. arch-lint, then Pi HUMAN-UAT)
  </read_first>
  <files>internal/adapter/openai/integration_test.go</files>
  <action>
    Extend `internal/adapter/openai/integration_test.go` with a real-kiro round-trip test gated on `KIRO_CMD` (skip when unset — copy the anthropic resolveKiroCLI/kiroSetup gate, lines 32-45/57-101). The test wires the real pool/engine via the test-local realEngineAdapter (copy anthropic's lines 107-…), mounts the openai adapter under `r.Route("/v1", …)` with the real auth-less test router, then: (1) POSTs a stream:false chat → asserts 200 + a chat.completion envelope with non-empty assistant content sourced from live kiro-cli; (2) POSTs a stream:true chat → asserts 200 + Content-Type text/event-stream + at least one content delta frame + a finish_reason frame + `data: [DONE]` (bufio.Scanner). `goleak` (the package TestMain from Plan 01) must report no leak. Keep the test fast and resilient (short prompt "hi"; reasonable timeout) — mirror the anthropic integration test's structure exactly.
    After the test exists, the executor runs `make ci` to confirm the phase gate: lint + go test -race ./... + govulncheck + arch-lint (with the new adapter_openai boundary — SC5) all green.
  </action>
  <verify>
    <automated>go test ./internal/adapter/openai/... && make arch-lint</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/adapter/openai/...` passes (real-kiro test skips cleanly when KIRO_CMD unset; runs and passes when set).
    - `make arch-lint` passes — adapter_openai imports only canonical (+plugin), none import engine (SC5; VALIDATION.md TRST-04 row).
    - `make ci` is green (full phase gate: lint + race + govulncheck + arch-lint). [Executor runs this; record the result in the summary.]
    - Behavior (when KIRO_CMD set): real-kiro stream + non-stream round-trips return valid OpenAI responses with non-empty content; goleak reports no leak.
    - Source assertion: `grep -q 'resolveKiroCLI\|KIRO_CMD\|Skip' internal/adapter/openai/integration_test.go` (real-kiro gated, not always-run).
  </acceptance_criteria>
  <done>The real-kiro integration test round-trips both stream and non-stream against live kiro-cli (gated on KIRO_CMD), goleak-clean, and make ci passes the full trust-gate suite including the adapter_openai arch boundary (SC5).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pi-SDK HUMAN-UAT round-trip (SC2 / SURF-06 acceptance bar)</name>
  <files>cmd/otto-gateway/main.go, internal/adapter/openai/, ~/.gsd/agent/models.json</files>
  <what-built>
    A fully wired OpenAI surface on the running OTTO Gateway: `POST /v1/chat/completions`
    (stream + non-stream), `GET /v1/models`, `POST /v1/completions`, gated by
    ENABLED_SURFACES, co-mounted with Anthropic on `/v1` via the D-01 SurfaceMount
    grouping, auth via the existing dual-header bearer middleware. Pi hard-codes
    stream:true, so this verifies the SSE path end-to-end against live kiro-cli.
  </what-built>
  <how-to-verify>
    1. Build and start the gateway with a known token and all surfaces:
       `AUTH_TOKEN=testtoken KIRO_CMD=kiro-cli ENABLED_SURFACES=ollama,anthropic,openai make build && ./bin/otto-gateway`
       (confirm the boot log shows `openai_mounted` true and the gateway is listening on :11434).
    2. Configure Pi (loop24-client / @loop24/pi-ai) in `~/.gsd/agent/models.json` with a provider block:
       `{"providers":{"otto":{"baseUrl":"http://localhost:11434/v1","api":"openai-completions","apiKey":"testtoken","models":[{"id":"auto"}]}}}`
       (the apiKey must equal AUTH_TOKEN so the bearer auth passes; Pi sends it as Authorization: Bearer).
    3. Run a chat through Pi against the `otto` provider with a simple prompt (e.g. "say hi in one word").
    4. Confirm: Pi receives a streamed assistant reply (tokens arrive incrementally, not one blob), the reply is non-empty and sourced from kiro-cli, and Pi did NOT need any code modification.
    5. (Optional secondary checks) `curl -s http://localhost:11434/v1/models -H 'Authorization: Bearer testtoken'` lists "auto" + kiro models matching `curl /api/tags`; `curl -X POST .../v1/chat/completions -H 'Authorization: Bearer testtoken' -d '{"model":"auto","messages":[{"role":"user","content":"hi"}],"stream":false}'` returns a chat.completion JSON envelope (SC1).
  </how-to-verify>
  <action>Operator-driven verification (no code change). Start the gateway with all three surfaces enabled and a known AUTH_TOKEN, configure Pi per the how-to-verify steps, and drive a streamed chat through Pi. Claude has already automated everything automatable (wiring in Task 1, real-kiro round-trip in Task 2); this step confirms the dominant SURF-06 client works against the live binary with zero SDK modification.</action>
  <verify>Pi receives an incrementally-streamed, non-empty assistant reply sourced from kiro-cli; the optional curl checks return a chat.completion envelope (SC1) and a /v1/models list matching /api/tags (SC3).</verify>
  <done>Operator reports "approved" — Pi completed the streamed round-trip with zero SDK modification (SC2 / SURF-06 closed). Any failure becomes a gap-closure trigger.</done>
  <resume-signal>Type "approved" once Pi completes the streamed round-trip with zero SDK modification, or describe what failed (e.g. stream not incremental, auth 401, mis-shaped frames).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ENABLED_SURFACES | Gating decision controls whether /v1/chat/completions is reachable at all |
| Pi client → running gateway | Real bearer-authenticated HTTP + live SSE crosses into the wired adapter and on to the kiro-cli subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-30 | EoP | ENABLED_SURFACES gating in main.go | mitigate | The openai SurfaceMount is appended ONLY under `slices.Contains(cfg.EnabledSurfaces, "openai")`; a subset omitting openai leaves the surface unmounted (no route, no handler). Verified by reading the gate + the widened-default boot log. |
| T-03-31 | Spoofing | Pi bearer auth on /v1 | mitigate | The /v1 SurfaceMount prefix block applies auth.Bearer (dual-header) once (Plan 01 / D-01); Pi's apiKey flows as Authorization: Bearer and is validated against AUTH_TOKEN. HUMAN-UAT exercises the authenticated path. |
| T-03-32 | DoS | SSE goroutine under a real client | mitigate | Real-kiro integration test + goleak prove the streaming handler is leak-free under a live round-trip; ctx-propagation unwinds the ACP prompt on disconnect (3.1 D-06). |
| T-03-33 | Info Disclosure | TRST-04 boundary across all three adapters | mitigate | `make arch-lint` (SC5) blocks any adapter importing internal/engine; run in make ci as the phase gate. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs ZERO new packages (RESEARCH § Package Legitimacy Audit — N/A). No install task exists; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `go build ./...` green (binary compiles against the refactored server.Config).
- `go test ./internal/server/... ./internal/adapter/...` green (no regression in Ollama/Anthropic routes after the SurfaceMount/RegisterRoutes change).
- `make arch-lint` green — all three adapters canonical-only, none import engine (SC5).
- `make ci` green (full trust-gate phase gate).
- Pi HUMAN-UAT: streamed round-trip succeeds with zero SDK modification (SC2 / SURF-06).
</verification>

<success_criteria>
- SC1: stream:false curl returns a chat.completion JSON envelope from the running gateway.
- SC2: Pi-SDK with baseUrl=…/v1 + bearer completes a streamed chat round-trip, zero SDK modification (HUMAN-UAT).
- SC3: /v1/models and /api/tags reflect the same set (curl cross-check + Plan 03 construction).
- SC4: ENABLED_SURFACES gates the openai surface on/off without code changes; default mounts all three (main.go gating + Plan 01 config).
- SC5: arch-lint passes — adapter_openai/ollama/anthropic import only canonical (+plugin), none import engine.
</success_criteria>

<output>
Create `.planning/phases/03-openai-surface/03-04-SUMMARY.md` when done
</output>
