---
phase: 08.3.2
plan: 02
type: execute
wave: 2
depends_on: [08.3.2-01]
files_modified:
  - scripts/test-pii.ps1
autonomous: true
requirements: [TEST-01]
tags: [smoke-test, powershell, fake-kiro, pii, windows]
must_haves:
  truths:
    - "scripts/test-pii.ps1 accepts a new -Mode parameter with ValidateSet('live','fake') and default 'fake'."
    - "In -Mode fake the script writes an NDJSON notifications file containing one agent_message_chunk frame per expected plaintext value, using no-BOM UTF-8 encoding."
    - "In -Mode fake the script checks (before running probes) that the gateway's currently-configured KIRO_CMD resolves to a fake binary path; if not, it exits 2 with an operator-actionable message."
    - "In -Mode live the script emits a deprecation banner pointing at Phase 08.3.2 and runs today's path unchanged (back-compat for operators verifying live kiro-cli)."
    - "The script's existing PII probe + assertion logic (Invoke-PIIProbe, the [PII:...] regex check, the $script:PIIExpected loop) is preserved byte-for-byte."
    - "On the Windows splunk box with bin/fake-kiro-cli.exe + gateway started with KIRO_CMD pointing at it, `.\\scripts\\test-pii.ps1 pii` exits 0 with '0 check(s) failed'."
  artifacts:
    - path: "scripts/test-pii.ps1"
      provides: "Operator smoke test for PII round-trip with -Mode fake default and -Mode live legacy fallback"
      contains: "ValidateSet('live', 'fake')"
  key_links:
    - from: "scripts/test-pii.ps1 (-Mode fake)"
      to: "bin/fake-kiro-cli.exe (built by Plan 01's make build-fake-kiro)"
      via: "OTTO_FAKE_KIRO_NOTIFICATIONS_FILE env var read by the fake at tests/e2e/cmd/fake-kiro-cli/main.go:35,180"
      pattern: "OTTO_FAKE_KIRO_NOTIFICATIONS_FILE"
    - from: "scripts/test-pii.ps1 notification frames"
      to: "tests/e2e/cmd/fake-kiro-cli/main.go:114 (hard-coded sessionId)"
      via: "params.sessionId == 'e2e-session-1' literal"
      pattern: "e2e-session-1"
    - from: "gateway PIIRedactionHook Post"
      to: "$script:PIIExpected[] values in Invoke-PIIProbe"
      via: "decrypt of [PII:Email:...] tokens back to plaintext; the fake's echo of plaintext appears alongside"
      pattern: "\\[PII:[A-Za-z]+:[A-Za-z0-9_-]+\\]"
---

<objective>
Add an operator-mode switch to `scripts/test-pii.ps1` so the `pii` scenario can verify PII round-trip against a deterministic fake-kiro-cli worker (option 2a from RESEARCH) instead of depending on the live LLM's willingness to echo PII-shaped data verbatim. This is the load-bearing vertical slice for Phase 08.3.2: after this plan ships, an operator on the Windows splunk box can run `.\scripts\test-pii.ps1 pii` against a `PII_REDACTION_MODE=encrypt` gateway with `KIRO_CMD=<absolute path to bin\fake-kiro-cli.exe>` and observe `0 check(s) failed` end-to-end through `/v1/messages`, `/v1/chat/completions`, and `/api/chat`.

Purpose: closes the LLM-cooperation dependency that broke the v1.9.3 smoke run on 2026-06-03. Default `-Mode fake` makes the script pass out of the box on a fresh operator install; `-Mode live` stays as the documented legacy path with a known-failure banner for operators verifying the real `kiro-cli` end-to-end.

Output: a modified `scripts/test-pii.ps1` that operators can run today on the Windows box with Plan 01's `bin/fake-kiro-cli.exe` in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-VALIDATION.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add -Mode parameter, comment-based help update, and live/fake dispatch in Invoke-Scenario-Pii</name>
  <files>scripts/test-pii.ps1</files>
  <read_first>
    - scripts/test-pii.ps1 lines 1-83 (comment-based help block + param() declaration + -h/-Help handling — this is where the new `.PARAMETER Mode` block and new `-Mode` switch go)
    - scripts/test-pii.ps1 lines 290-322 (PII scenario header + $script:PIIPrompt + $script:PIIExpected definitions — the central state that the new dispatch reads)
    - scripts/test-pii.ps1 lines 409-431 (Invoke-Scenario-Pii dispatch — the function the new dispatch wraps)
    - scripts/test-pii.ps1 lines 436-460 (top-level switch + final exit code accounting — preserve unchanged)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md section `scripts/test-pii.ps1 (MODIFY — add -Mode parameter; default fake)`
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md §Validation Architecture (Option 2a verification procedure) and §Open Questions Q2 recommendation (default fake)
    - tests/e2e/cmd/fake-kiro-cli/main.go (lines 113-122 for hard-coded sessionID, lines 128-137 for prompt handler emission order, lines 35-37 for env-var contract)
  </read_first>
  <action>
    Five-part edit to `scripts/test-pii.ps1`. All edits respect the project's PowerShell 5.1+ baseline and the ASCII-only constraint from commit f7ccd40 (no Unicode in script literals — escape via `\uXXXX` if absolutely needed; the existing prompt is already ASCII).

    Part A — Comment-based help block (lines 21-50 area): insert a new `.PARAMETER Mode` block alongside the existing `.PARAMETER Scenario` / `.PARAMETER Surface` blocks. Wording must explain: `live` is the legacy LLM-cooperation path (default before Phase 08.3.2; now opt-in with a known-failure banner) and `fake` is the deterministic-worker path that requires the operator to have run `make build-fake-kiro` and set `KIRO_CMD` to the absolute path of `bin\fake-kiro-cli.exe` in `.otto-gw.overrides.env` before starting the gateway. Add one `.EXAMPLE` block: `.\test-pii.ps1 pii -Mode fake` (the default, written explicitly for clarity). Add a `.NOTES`-level reference: "Phase 08.3.2 — see .planning/phases/08.3.2-pii-smoke-test-methodology-fix/ for the methodology rationale."

    Part B — param() block (lines 58-78): insert `[ValidateSet('live', 'fake')] [string]$Mode = 'fake'` between the `$Surface` declaration and the `$Base` declaration. Default value `'fake'` per RESEARCH Open Question Q2 recommendation and PATTERNS shared pattern "Operator-mode flag default `fake` (not `live`)." Preserve the existing `$Help` / `[Alias('h')]` block and the `Get-Help -Full` short-circuit at lines 80-83.

    Part C — Build a new helper function `New-NotifFrame` placed in the helpers region around line 290 (immediately before `$script:PIIPrompt`). Exact shape per PATTERNS analog 2 / RESEARCH §Code Examples:

    - Function signature: `function New-NotifFrame { param([string]$SessionId, [string]$Text) ... }`
    - Returns a single compact-JSON line via `ConvertTo-Json -Compress -Depth 10`
    - Frame structure matches the JSON-RPC `session/update` shape verified at `tests/e2e/tools_fixtures_test.go:212`: `{"jsonrpc":"2.0","method":"session/update","params":{"sessionId":"<id>","update":{"sessionUpdate":"agent_message_chunk","content":{"text":"<text>"}}}}`
    - SessionId MUST be `e2e-session-1` literally when called from this script (the fake hard-codes that value at main.go:114; using a different value would trigger Phase 08.3.1's demux-dropped path)

    Part D — Refactor `Invoke-Scenario-Pii` (around line 409) so it branches on `$Mode`:

    - Add helper `Invoke-Scenario-Pii-Fake` that runs BEFORE the existing probes:
      (i) Determine notifications file path: `$notifsPath = Join-Path $env:TEMP 'otto-pii-notifs.ndjson'` (per RESEARCH §Code Examples)
      (ii) Build NDJSON content from `$script:PIIExpected` — one frame per expected plaintext via `New-NotifFrame -SessionId 'e2e-session-1' -Text "<value>"`. Each frame on its own line, terminated with `\n`. Join the array with `"`n"`.
      (iii) Write the file using `[System.IO.File]::WriteAllText($notifsPath, $body, [System.Text.UTF8Encoding]::new($false))` — the no-BOM ctor. This is mandatory per Pitfall 1 (AP-1) and the f7ccd40 prior incident; DO NOT use `Out-File`, `Set-Content`, or `>` redirection here.
      (iv) Pre-flight validation: pass each line of the file through `ConvertFrom-Json` in a try/catch loop; if any line fails to parse, `Write-Fail "notifications file frame N is invalid JSON: <err>"` and `return` (Pitfall 2 — the fake silently skips malformed lines, so catching them at script side is the only way the operator gets a useful error).
      (v) Precondition: `GET /admin/api/snapshot` (or `/health/hooks`) — read the gateway's currently-active `KiroCmd` field. If it does not contain the substring `fake` (case-insensitive match), `Write-Fail "gateway KIRO_CMD does not resolve to a fake worker — operator must set KIRO_CMD=<absolute path to bin\fake-kiro-cli.exe> in .otto-gw.overrides.env and restart"` and exit code 2 (mirroring the existing `/health` precondition at lines 183-193). This is T2 (gateway-pointed-at-fake-in-production) defense in depth.
      (vi) Inform the operator how the notifications file is wired: write a `Write-Info` line stating `"NOTE: the gateway process must have OTTO_FAKE_KIRO_NOTIFICATIONS_FILE=$notifsPath set in its environment. If it doesn't, restart the gateway with that env var set."`. The script does NOT manipulate the gateway env — that's the operator's job per RESEARCH §Architecture Diagram precondition.
      (vii) Call the existing `Invoke-PIIProbe` per surface — exactly as the current `Invoke-Scenario-Pii` does at lines 419-430. NO changes to the probe function itself.

    - Add helper `Invoke-Scenario-Pii-Live` that wraps today's existing logic at lines 411-430 unchanged, BUT prepends a deprecation banner: `Write-Section "DEPRECATED: -Mode live"` then `Write-Info "The live path depends on the LLM (Claude via kiro-cli) verbatim-echoing PII-shaped data, which current model releases refuse on safety grounds. Expect 'round-trip decrypt: NOT in response' failures. See Phase 08.3.2 for the deterministic-worker alternative."`. Then run today's path.

    - The top-level `Invoke-Scenario-Pii` becomes a 4-line dispatch:
      ```
      function Invoke-Scenario-Pii {
          if ($Mode -eq 'fake') { Invoke-Scenario-Pii-Fake } else { Invoke-Scenario-Pii-Live }
      }
      ```

    Part E — Do NOT modify: `Invoke-PIIProbe` (lines 372-407), `Get-ResponseText` (lines 324-370), the top-level dispatch switch (lines 436-445), the final exit-code accounting (lines 447-460), the `diag`/`wire` scenarios, the `-Help` / `Get-Help` shim, or any color/IO helper. Surgical edits only.

    Anti-pattern guardrails (from PATTERNS.md §Anti-Patterns):
    - Do NOT default `$Mode` to `'live'` for "back-compat" — the live path is known-broken, so default-on means default-fail. The default IS the fix.
    - Do NOT inline the fake binary path or call `go build` from this script — Plan 01's `make build-fake-kiro` owns binary production; this script only reads `$env:TEMP` and the gateway-reported `KiroCmd`.
    - Do NOT use `Out-File` anywhere in Part C/D — Pitfall 1 (AP-1) BOM corruption is the load-bearing risk.
    - Do NOT inline the notification frame as a here-string template — use `ConvertTo-Json` so the escaping is correct for embedded special characters in `$script:PIIExpected` values (Pitfall 2 / AP-1 sibling).
    - Do NOT have `Invoke-Scenario-Pii-Fake` start/stop the gateway. Operator owns gateway lifecycle (RESEARCH §Architecture Diagram precondition; matches the existing `Invoke-Scenario-Pii` contract).
  </action>
  <verify>
    <automated>grep -E "^\s*\[ValidateSet\('live', 'fake'\)\]" scripts/test-pii.ps1 | grep -v '^#' | grep -c . | grep -q '^1$' &amp;&amp; grep -E "^\s*\[string\]\$Mode = 'fake'" scripts/test-pii.ps1 | grep -v '^#' | grep -c . | grep -q '^1$' &amp;&amp; grep -c 'function New-NotifFrame' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; grep -c 'function Invoke-Scenario-Pii-Fake' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; grep -c 'function Invoke-Scenario-Pii-Live' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; grep -c 'e2e-session-1' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; grep -c 'UTF8Encoding.*false' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; ! awk '/^function Invoke-Scenario-Pii-Fake/,/^}/' scripts/test-pii.ps1 | grep -E 'Out-File|Set-Content' &amp;&amp; grep -c 'OTTO_FAKE_KIRO_NOTIFICATIONS_FILE' scripts/test-pii.ps1 | grep -q '^1$' &amp;&amp; grep -c 'Phase 08.3.2' scripts/test-pii.ps1 | grep -Eq '^[1-9]' &amp;&amp; make vet</automated>
    <human-check>Operator runs `.\scripts\test-pii.ps1 pii` (default `-Mode fake`) on the Windows splunk box with bin/fake-kiro-cli.exe built and gateway started with KIRO_CMD pointing at it + OTTO_FAKE_KIRO_NOTIFICATIONS_FILE set. Expected: `0 check(s) failed` exit 0. Operator separately runs `.\scripts\test-pii.ps1 pii -Mode live` — expected: deprecation banner visible, today's known-failure assertions surface (this is the documented legacy path, not a regression).</human-check>
  </verify>
  <done>
    Script accepts and validates the new `-Mode` parameter. New helper functions (`New-NotifFrame`, `Invoke-Scenario-Pii-Fake`, `Invoke-Scenario-Pii-Live`) exist and use the prescribed primitives (no-BOM UTF-8 write, ConvertTo-Json frame builder, sessionId literal `e2e-session-1`). Existing `Invoke-PIIProbe`, `Get-ResponseText`, and the top-level dispatch switch are byte-identical to before. On the Windows box with the precondition satisfied, the script exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> test-pii.ps1 | operator-trusted invocation; operator chose to swap KIRO_CMD; script does not escalate privilege |
| test-pii.ps1 -> $env:TEMP (notifications NDJSON) | script writes a temp file the gateway-spawned fake reads; content is operator-supplied plaintext fixtures |
| gateway -> fake-kiro-cli (operator-configured) | gateway spawns fake via KIRO_CMD; same trust posture as live kiro-cli (operator chose the binary path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3.2-02-T2 | Tampering | operator forgets to revert KIRO_CMD; production gateway serves fake responses to real clients | mitigate | The script's `Invoke-Scenario-Pii-Fake` precondition checks the gateway's reported `KiroCmd` field; if it points at fake, the script proceeds. If the operator later forgets to restore `KIRO_CMD=kiro-cli`, the same `KiroCmd` field on `/admin/about` makes that visible. Out of scope for this plan: a gateway-side WARN at startup when `KIRO_CMD` resolves to a binary named `*fake*` — tracked as a future hardening item; documented in the HUMAN-UAT addendum in Plan 03. |
| T-08.3.2-02-T3 | Tampering | operator-supplied PII fixture contains crafted JSON-RPC frames that exploit the fake | accept | The fake binary runs in operator-trusted context (operator chose to swap it in). Plain text values in `$script:PIIExpected` are wrapped via `ConvertTo-Json` which handles escaping correctly. The notification frames the fake replays are JSON-RPC notifications, not requests, so the fake's response surface is bounded to `session/update` emissions. No privilege escalation path. |
| T-08.3.2-02-AP1 | Tampering | UTF-8 BOM corrupts the fake's first-line JSON parse | mitigate | `[System.IO.File]::WriteAllText(..., [System.Text.UTF8Encoding]::new($false))` ctor is the only write path. The verify gate `grep -c 'UTF8Encoding.*false'` enforces it. Anti-pattern guardrail in action: explicit prohibition on `Out-File`/`Set-Content` in the action section. |
| T-08.3.2-02-AP2 | Tampering | malformed JSON in notifications file fails silently | mitigate | Pre-flight `ConvertFrom-Json` validation per line in `Invoke-Scenario-Pii-Fake` step (iv). Catches the failure mode Pitfall 2 documents (fake at main.go:202 silently skips malformed lines). |

No `high`-severity threats. The script is operator-tool only, never reached by external HTTP requests, never executes during `make ci`.
</threat_model>

<verification>
- `grep` for `[ValidateSet('live', 'fake')]`, `$Mode = 'fake'`, `function New-NotifFrame`, `function Invoke-Scenario-Pii-Fake`, `function Invoke-Scenario-Pii-Live`, `e2e-session-1`, `UTF8Encoding.*false`, `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE`, `Phase 08.3.2` (each must appear at least once)
- Negative check: `Out-File.*notifs` or `Out-File` inside `Invoke-Scenario-Pii-Fake` must return nothing
- Behavioral (operator-manual, Windows box, splunk per VALIDATION.md): `.\scripts\test-pii.ps1 pii` exits 0 with `0 check(s) failed` when gateway is properly configured with `KIRO_CMD=<absolute path to bin\fake-kiro-cli.exe>` and `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE=<temp path>` set in the gateway process env
- Behavioral (operator-manual): `.\scripts\test-pii.ps1 pii -Mode live` shows the deprecation banner and runs today's path
- `make ci` remains green — the script is not in any CI target
- `Invoke-PIIProbe` body (lines 372-407 today) is byte-identical after the edit
</verification>

<success_criteria>
On the Windows splunk box: operator runs `.\scripts\test-pii.ps1 pii` (default `-Mode fake`) against a v1.9.4 gateway started with KIRO_CMD pointing at `bin\fake-kiro-cli.exe` + OTTO_FAKE_KIRO_NOTIFICATIONS_FILE pointing at the script's temp file, and observes exit 0 with `0 check(s) failed`. The legacy `-Mode live` path remains available with a clear deprecation banner. The PII probe + assertion logic is preserved unchanged so the "no ciphertext leak" correctness signal (which has been passing) continues to pass. TEST-01 is satisfied for the PowerShell surface; Plan 03 mirrors the change to the POSIX sibling.
</success_criteria>

<output>
Create `.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-02-SUMMARY.md` documenting: the exact line-ranges edited, the final shape of the `-Mode` parameter validation, the New-NotifFrame function body, the Invoke-Scenario-Pii-Fake precondition-check + notifications-write sequence, the Invoke-Scenario-Pii-Live deprecation-banner wording, the Windows operator-manual sign-off (paste the script's `pii` output showing `0 check(s) failed`), and any deviation from the planned approach with justification.
</output>
</content>
</invoke>