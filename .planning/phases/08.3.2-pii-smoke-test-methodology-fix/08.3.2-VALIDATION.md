---
phase: 08.3.2
slug: pii-smoke-test-methodology-fix
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-04
updated: 2026-06-04
---

# Phase 08.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.23+) with `-race`; PowerShell 5.1+ for the Windows operator smoke run; `bash` 3.2+ for the POSIX smoke run sibling |
| **Config file** | `internal/acp/testmain_test.go` (existing `goleak.VerifyTestMain`); new `Makefile` target `build-fake-kiro` produces `bin/fake-kiro-cli[.exe]` |
| **Quick run command** | `make build-fake-kiro && make vet` |
| **Full suite command** | `make ci` (gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`) |
| **Estimated runtime** | ~30 s for quick run; ~90 s for `make ci`; ~10 s for the operator-only `test-pii.ps1 pii` run against fake worker |

---

## Sampling Rate

- **After every task commit:** Run the per-task `<automated>` block from the plan (each cited in the Per-Task Verification Map below)
- **After every plan wave:** Run `make ci`
- **Before `/gsd-verify-work`:** `make ci` green + `test-pii.ps1 pii` exits 0 against fake worker on Windows AND `test-pii.sh pii` exits 0 against fake worker on POSIX
- **Max feedback latency:** ~30 s (quick) / ~90 s (full)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.3.2-01-01 | 01 | 1 | TEST-01 (introducer) | T-REQ | New `TEST-01` row + Traceability `Pending` row added in REQUIREMENTS.md; ROADMAP entry typo `TRST-01, TRST-02` corrected to `TEST-01` in same commit | doc/source | `grep -c '^- \[ \] \*\*TEST-01\*\*:' REQUIREMENTS.md` returns 1; ROADMAP Requirements line equals `TEST-01`; TRST-01/02 typo removed from Phase 08.3.2 entry | ✅ scaffold | ✅ green |
| 08.3.2-01-02 | 01 | 1 | TEST-01 | T1 (binary shadows PATH); T4 (release-bundle exclusion) | New `make build-fake-kiro` target builds `bin/fake-kiro-cli[.exe]` via `CGO_ENABLED=0 go build -o`; target is NOT a dependency of `ci`/`all`/`package`/`build`/`stage_unix` | build | `make build-fake-kiro && test -x bin/fake-kiro-cli$(go env GOEXE)` + negative grep against the release/CI targets in Makefile | ✅ scaffold | ✅ green |
| 08.3.2-02-01 | 02 | 2 | TEST-01 | T2 (gateway-pointed-at-fake); T3 (NDJSON content); AP-1 (UTF-8 BOM); AP-2 (absolute KIRO_CMD) | New `-Mode {live,fake}` parameter on `scripts/test-pii.ps1` with default `fake`; `Invoke-Scenario-Pii-Fake` writes notifications NDJSON via no-BOM UTF-8 only (no `Out-File`/`Set-Content` in function body); `-Mode live` prints deprecation banner pointing at this phase | shell + integration (operator) | Long `grep` chain validating ValidateSet, default value, function presence, e2e-session-1 literal, UTF-8 encoding, NDJSON env var, AP-1 negative awk-extract check + `make vet` | ✅ exists | ⬜ pending Windows operator |
| 08.3.2-03-01 | 03 | 3 | TEST-01 | T2; AP-1 (heredoc no-BOM); AP-3 (default-fake parity) | POSIX sibling `scripts/test-pii.sh` exposes `--mode {live,fake}` with default `fake`; same notification frames, same `e2e-session-1` literal, same `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE` contract as PowerShell sibling | shell + integration (operator) | Long `grep` chain validating case branches, default value, function presence, e2e-session-1 literal, NDJSON env var + `sh -n` + optional `shellcheck` + `make vet` | ✅ exists | ⬜ pending POSIX operator |
| 08.3.2-03-02 | 03 | 3 | TEST-01 (close-out) | T-REQ (precondition gate) | `08.3-HUMAN-UAT.md` gains `## Follow-up: Phase 08.3.2` section documenting the methodology change; `REQUIREMENTS.md` TEST-01 row flipped `[ ]` → `[x]`; Traceability row flipped `Pending` → `Complete`. **PRECONDITION:** flip is blocked unless both `08.3.2-02-SUMMARY.md` AND `08.3.2-03-SUMMARY.md` contain `0 check(s) failed` (or `all checks passed`) — orchestrator-enforced by `<automated>` precondition grep | doc | Precondition checks on Plan 02 + Plan 03 SUMMARY.md sign-offs, then UAT addendum + REQUIREMENTS.md flip verification | ✅ exists | ⬜ pending Plans 02 + 03 sign-offs |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `Makefile` — `build-fake-kiro` target → **Plan 01 Task 2**
- [x] `REQUIREMENTS.md` — `TEST-01: smoke tests must not depend on LLM cooperation for correctness` → **Plan 01 Task 1**
- [x] `tests/e2e/cmd/fake-kiro-cli/main.go` — confirmed ready as-is (no source changes needed per RESEARCH §Findings 1) → cited in `read_first` of Plans 02 + 03
- [x] Packaging confirmation — `bin/fake-kiro-cli[.exe]` NOT included in release zip → **Plan 01 Task 2** threat model T4 mitigation (target not a dependency of `package`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `test-pii.ps1 pii -Mode fake` returns exit 0 on the Windows splunk box (same box as Phase 8.3 HUMAN-UAT) | TEST-01 | Operator-only by design; tests the gateway against a deterministic worker on the production OS | 1. Pull v1.9.4 on the Windows box 2. `make build-fake-kiro` (or unzip release if shipped) 3. Set `KIRO_CMD=C:\otto-gateway\bin\fake-kiro-cli.exe` in `.otto-gw.overrides.env` 4. Start gateway: `.\scripts\otto-gw.ps1 start` 5. Run `.\scripts\test-pii.ps1 pii` 6. Verify `0 check(s) failed` |
| `test-pii.sh pii --mode fake` returns exit 0 on macOS / Linux dev box | TEST-01 | Operator-only by design; POSIX sibling of the PowerShell smoke test | 1. From repo root: `make build-fake-kiro` 2. `KIRO_CMD=$(pwd)/bin/fake-kiro-cli ./scripts/otto-gw.sh start` 3. `./scripts/test-pii.sh pii` 4. Verify `0 check(s) failed` |
| `test-pii.ps1 pii -Mode live` reports documented known-failure mode against current Claude | (regression doc) | Live LLM behavior is non-deterministic; documents the legacy path's known break | Operator runs `.\scripts\test-pii.ps1 pii -Mode live` against live `kiro-cli`; expects `round-trip decrypt: ... NOT in response` failures AND a banner pointing at this phase's findings. NOT a green requirement — documents the deprecated path |
| Sibling byte-equivalence check (W5 from plan-checker) | TEST-01 | Catches f7ccd40-class sibling drift | Operator runs both scripts against the same fixture (set `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE` to a path; the script writes its NDJSON output there) and compares the byte stream with `sha256sum` (POSIX) vs `Get-FileHash -Algorithm SHA256` (Windows). Hashes MUST match |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has either `<automated>` or — for the operator-manual tasks — explicit operator instructions backed by `<human-check>`)
- [x] Wave 0 covers all MISSING references (Makefile target, REQUIREMENTS.md TEST-01, fake-kiro-cli pattern reuse cited in read_first, packaging confirmation via threat T4 mitigation)
- [x] No watch-mode flags
- [x] Feedback latency < 30 s (quick) / < 90 s (full)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-04
