---
phase: 08.3.2
slug: pii-smoke-test-methodology-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 08.3.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.23+) with `-race` for the new Go-side unit / integration tests; PowerShell 5.1+ for the operator smoke run; `bash` for the POSIX smoke run sibling |
| **Config file** | `internal/acp/testmain_test.go` (existing `goleak.VerifyTestMain`); new `.otto-gw.overrides.smoke.env` lives alongside `.otto-gw.overrides.env` |
| **Quick run command** | `go test -race ./tests/e2e/... -run TestSmokePIIWithFakeKiroCli -timeout 60s` |
| **Full suite command** | `make ci` (gofumpt → vet → build → golangci-lint → govulncheck → `go test -race ./...`) + `make build-fake-kiro` |
| **Estimated runtime** | ~30 s for quick run; ~90 s for `make ci`; ~10 s for the operator-only `test-pii.ps1 pii` run against fake worker |

---

## Sampling Rate

- **After every task commit:** Run `go test -race ./tests/e2e/...` (covers fake-kiro-cli + new integration test)
- **After every plan wave:** Run `make ci`
- **Before `/gsd-verify-work`:** `make ci` green + `test-pii.ps1 pii` exits 0 against fake worker on at least one Windows AND one POSIX run
- **Max feedback latency:** ~30 s (quick) / ~90 s (full)

---

## Per-Task Verification Map

> Populated by the planner — one row per task with explicit `<automated>` verify command. Operator-only smoke runs are tracked as manual checks.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.3.2-XX-XX | XX | 1 | TEST-01 | — | (planner to fill) | unit / integration / shell | `go test -race -run <Name> ./tests/e2e/...` OR `pwsh -File scripts/test-pii.ps1 pii -Mode fake` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Makefile` — add `build-fake-kiro` target producing `bin/fake-kiro-cli[.exe]` cross-compiled per host platform (mirrors `make build` shape)
- [ ] `REQUIREMENTS.md` — introduce `TEST-01: smoke tests must not depend on LLM cooperation for correctness` under the existing `TRST-*` neighbourhood OR a new `TEST-*` category (planner's call)
- [ ] `tests/e2e/` — confirm `tools_*_test.go` patterns (researcher cited these as battle-tested invocation examples for `fake-kiro-cli`); plan should reuse rather than reinvent
- [ ] `scripts/install.sh` / packaging — confirm whether `bin/fake-kiro-cli[.exe]` is shipped alongside `otto-gateway` in the release tarball (researcher Q1)

*If none of the above need work: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `test-pii.ps1 pii -Mode fake` returns exit 0 on the Windows splunk box (same box as Phase 8.3 HUMAN-UAT) | TEST-01 | Operator-only by design; tests the gateway against a deterministic worker on the production OS | 1. Pull v1.9.4 (this phase) on the Windows box 2. `make build` + `make build-fake-kiro` (or unzip the release artifact that includes both) 3. Set `KIRO_CMD=C:\otto-gateway\bin\fake-kiro-cli.exe` in `.otto-gw.overrides.env` 4. Start gateway: `.\scripts\otto-gw.ps1 start` 5. Run `.\scripts\test-pii.ps1 pii` (mode defaults to `fake`) 6. Verify `0 check(s) failed` |
| `test-pii.sh pii --mode fake` returns exit 0 on macOS / Linux dev box | TEST-01 | Operator-only by design; POSIX sibling of the PowerShell smoke test | 1. From repo root: `make build-fake-kiro` 2. `KIRO_CMD=$(pwd)/bin/fake-kiro-cli ./scripts/otto-gw.sh start` 3. `./scripts/test-pii.sh pii` 4. Verify `0 check(s) failed` |
| `test-pii.ps1 pii -Mode live` reports the documented known-failure mode against current Claude | (regression doc) | Live LLM behavior is non-deterministic; this is documenting the legacy path's known break, not asserting correctness | Operator runs `.\scripts\test-pii.ps1 pii -Mode live` against live `kiro-cli`; expects `round-trip decrypt: ... NOT in response` failures AND a banner pointing at this phase's findings. NOT a green requirement — documents the deprecated path |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Makefile target, REQUIREMENTS.md entry, tests/e2e/ pattern reuse, packaging confirmation)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30 s (quick) / < 90 s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
