---
phase: 08.3.2
plan: 03
type: execute
wave: 3
depends_on: [08.3.2-02]
files_modified:
  - scripts/test-pii.sh
  - .planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TEST-01]
tags: [smoke-test, posix, shell, fake-kiro, pii, human-uat]
must_haves:
  truths:
    - "scripts/test-pii.sh accepts a new --mode flag with validator restricting to {live,fake} and default 'fake'."
    - "test-pii.sh in --mode fake builds a notifications NDJSON file in $TMPDIR_ containing one agent_message_chunk frame per expected plaintext, using sessionId 'e2e-session-1' literally."
    - "test-pii.sh and test-pii.ps1 stay in sync on parameter shape and notification format — both default to 'fake', both honor the same gateway-side env-var contract (OTTO_FAKE_KIRO_NOTIFICATIONS_FILE)."
    - "test-pii.sh --mode live emits a deprecation banner and runs today's PII path unchanged."
    - "08.3-HUMAN-UAT.md is updated with a follow-up entry documenting the methodology change, the operator-verification steps for both Windows and POSIX, and the historical evidence of why -Mode live is now deprecated."
    - "REQUIREMENTS.md TEST-01 checkbox flips from [ ] to [x] and Traceability status flips from 'Pending' to 'Complete' after operator HUMAN-UAT sign-off."
    - "make ci remains green."
  artifacts:
    - path: "scripts/test-pii.sh"
      provides: "POSIX sibling smoke test mirroring test-pii.ps1's -Mode behavior"
      contains: "--mode"
    - path: ".planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md"
      provides: "Phase 08.3.2 follow-up methodology change record"
      contains: "08.3.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-01 status flipped to Complete on HUMAN-UAT pass"
      contains: "[x] **TEST-01**"
  key_links:
    - from: "scripts/test-pii.sh (--mode fake)"
      to: "bin/fake-kiro-cli (built by Plan 01 on POSIX host)"
      via: "operator sets KIRO_CMD=<absolute path> + OTTO_FAKE_KIRO_NOTIFICATIONS_FILE=<TMPDIR_ path>"
      pattern: "OTTO_FAKE_KIRO_NOTIFICATIONS_FILE"
    - from: "scripts/test-pii.sh and scripts/test-pii.ps1"
      to: "tests/e2e/cmd/fake-kiro-cli/main.go:114"
      via: "sessionId 'e2e-session-1' literal — used by both siblings to avoid Phase 08.3.1 demux-drop"
      pattern: "e2e-session-1"
---

<objective>
Mirror Plan 02's PowerShell `-Mode` switch to the POSIX sibling `scripts/test-pii.sh` so the two scripts stay in sync (sibling drift is a known bitrot vector per the f7ccd40 prior incident); document the methodology change in `08.3-HUMAN-UAT.md` so a future operator reading the Phase 8.3 verification record understands why the smoke test's behavior changed in v1.9.4; and flip the TEST-01 checkbox + Traceability status in `REQUIREMENTS.md` from Pending to Complete on HUMAN-UAT sign-off.

Purpose: completes the vertical slice — Windows operator path (Plan 02) is the load-bearing one, but POSIX must mirror it or the next sibling drift will reintroduce bugs (commit f7ccd40 is the prior cautionary tale). The HUMAN-UAT addendum preserves the 2026-06-03 splunk-box pass as historical evidence and records the new operator-verification steps in one place. The REQUIREMENTS.md flip is the formal close-out.

Output: a `scripts/test-pii.sh` that mirrors test-pii.ps1, an updated 08.3-HUMAN-UAT.md, and a checked-off TEST-01 row in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-VALIDATION.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-01-SUMMARY.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-02-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Mirror -Mode parameter and Invoke-Scenario-Pii split to scripts/test-pii.sh</name>
  <files>scripts/test-pii.sh</files>
  <read_first>
    - scripts/test-pii.sh lines 1-37 (header comment block + usage flags table — the new `--mode` flag must appear in the Flags subsection so `usage()` at line 60 renders it via the existing `sed` extraction)
    - scripts/test-pii.sh lines 60-95 (usage() function + defaults + arg parser — the new `--mode` / `--mode=*` cases go in the existing while-loop, and the SCENARIO-style validator block at lines 91-94 is the analog for the new MODE validator)
    - scripts/test-pii.sh lines 71-72 (TMPDIR_ + trap — the notifications file lives at $TMPDIR_/notifications.ndjson and is auto-cleaned)
    - scripts/test-pii.sh lines 247-365 (PII scenario header + PII_PROMPT + PII_EXPECT_* + pii_probe function + scenario_pii dispatch)
    - scripts/test-pii.sh lines 354-365 (top-level case dispatch + final exit accounting — preserve unchanged)
    - scripts/test-pii.ps1 (the just-landed reference — adopt the same default value 'fake', same sessionId literal 'e2e-session-1', same notification frame shape; the executor MUST cross-check sibling parity by reading the PS variant before writing the shell variant)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md section `scripts/test-pii.sh (MODIFY — add --mode parameter, mirror PS changes)`
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md §Open Questions Q3 (decision: yes, apply to POSIX sibling in this phase to prevent sibling drift)
    - tests/e2e/cmd/fake-kiro-cli/main.go (lines 113-122 hard-coded sessionId, lines 195-207 silent malformed-line skip — Pitfall 2 applies on POSIX too even though there's no BOM concern)
  </read_first>
  <action>
    Five-part edit to `scripts/test-pii.sh`. Must remain POSIX `/bin/sh` compatible — no bash-only syntax (no arrays, no `[[ ]]`, no `local`, no process substitution). The script's `set -eu` at line 38 is preserved.

    Part A — Header comment block (lines 25-36 area): add a new line `  --mode NAME     live | fake   (default: fake — Phase 08.3.2 methodology fix; live is the legacy LLM-cooperation path with known failures)`. Match the indentation of the existing `--surface NAME ...` line so `usage()`'s `sed` at line 60 picks it up.

    Part B — Defaults + arg parser (lines 65-94):
    - Add `MODE="fake"` to the defaults block (between `SURFACE="all"` and `BASE=...`).
    - In the `while [ $# -gt 0 ]; do case` loop, add two cases (mirror the `--surface` / `--surface=*` pattern at lines 77-78):
      `--mode) MODE="$2"; shift 2 ;;`
      `--mode=*) MODE="${1#*=}"; shift ;;`
    - Add a validator block after the existing SURFACE validator at lines 91-94:
      ```
      case "$MODE" in
          live|fake) ;;
          *) printf '--mode must be one of: live, fake\n' >&amp;2; exit 2 ;;
      esac
      ```

    Part C — Define a new helper function `write_notifs_file` placed in the PII scenario region around line 246 (before PII_PROMPT). Body:

    ```
    write_notifs_file() {
        # write_notifs_file PATH VAL1 [VAL2 ...]
        # Emits one JSON-RPC session/update notification per value to PATH.
        # sessionId is the literal 'e2e-session-1' to match
        # tests/e2e/cmd/fake-kiro-cli/main.go:114 (and avoid Phase 08.3.1
        # demux-drop). Caller is responsible for ensuring each value
        # contains no double-quote or backslash characters; the current
        # fixtures (corey@cmetech.io, 192.168.1.42) satisfy that.
        notifs_path="$1"; shift
        : > "$notifs_path"
        for v in "$@"; do
            printf '{"jsonrpc":"2.0","method":"session/update","params":{"sessionId":"e2e-session-1","update":{"sessionUpdate":"agent_message_chunk","content":{"text":"%s"}}}}\n' "$v" >> "$notifs_path"
        done
    }
    ```

    Why no jq: the values are ASCII fixtures pinned in this script; mirroring the existing `HAS_JQ` branching from line 104 would complicate the contract without adding safety for the current value shape. If a future fixture needs escaping, the function gets a jq branch (out of scope here).

    Part D — Refactor `scenario_pii` (around line 337) into two helpers `scenario_pii_fake` and `scenario_pii_live`, dispatched by `$MODE`. Mirror Plan 02's structure:

    `scenario_pii_fake()` body:
    (i) `notifs_path="$TMPDIR_/notifications.ndjson"` — reuse the existing TMPDIR_ + trap (no manual cleanup needed).
    (ii) Call `write_notifs_file "$notifs_path" "$PII_EXPECT_EMAIL" "$PII_EXPECT_IPV4"`.
    (iii) Pre-flight validation: loop `while IFS= read -r line; do printf '%s' "$line" | (jq . >/dev/null 2>&1 || python3 -c 'import json,sys; json.loads(sys.stdin.read())' 2>/dev/null); done < "$notifs_path"` — if ANY line fails to parse, `fail "notifications file frame invalid JSON"` and `return`. If neither jq nor python3 is available, skip the pre-flight with a `warn` line ("skipping notifications NDJSON validation; jq or python3 recommended").
    (iv) Precondition check: `curl_auth GET "$BASE/admin/api/snapshot"` (or `/health/hooks` — pick whichever exposes the `KiroCmd` field; check by grepping admin templates if uncertain). Parse the JSON for `KiroCmd`; if it does not contain the substring `fake` (case-insensitive via `grep -i`), `fail "gateway KIRO_CMD does not resolve to a fake worker — set KIRO_CMD=$(pwd)/bin/fake-kiro-cli in .otto-gw.overrides.env and restart"` and exit 2. Mirrors the existing `/health` precondition at lines 117-122.
    (v) `info "NOTE: the gateway process must have OTTO_FAKE_KIRO_NOTIFICATIONS_FILE=$notifs_path set in its environment. If it doesn't, restart the gateway with that env var set."`
    (vi) Call the existing `pii_probe` per surface — exactly as `scenario_pii` does today at lines 341-350. NO changes to `pii_probe` itself.

    `scenario_pii_live()` body:
    - Print deprecation banner: `section "DEPRECATED: --mode live"` then `info "The live path depends on the LLM (Claude via kiro-cli) verbatim-echoing PII-shaped data, which current model releases refuse on safety grounds. Expect 'round-trip decrypt: NOT in response' failures. See Phase 08.3.2 for the deterministic-worker alternative."`
    - Run today's existing logic at lines 337-351 unchanged.

    Replace the existing `scenario_pii()` body with a 3-line dispatch:
    ```
    scenario_pii() {
        if [ "$MODE" = "fake" ]; then scenario_pii_fake; else scenario_pii_live; fi
    }
    ```

    Part E — Do NOT modify: `pii_probe` (lines 288-335), `extract_response_text` (lines 257-286), the top-level `case "$SCENARIO"` dispatch (lines 354-365), the exit-code accounting at the end, the `diag`/`wire` scenarios, the `usage()` function (the new `--mode` line is picked up automatically by its `sed`), and the `set -eu` directive.

    POSIX-shell guardrails:
    - No `bash`-isms: no arrays for the variadic `write_notifs_file` — use `shift` + positional params. No `[[ ... ]]`. No `local` (helpers can use shell-local convention — variables are global by default in `/bin/sh`).
    - The `printf` format string in `write_notifs_file` is single-quoted so the `"%s"` placeholder is preserved literally; `$v` interpolation happens only in the argument list.
    - No BOM concerns (POSIX `printf` produces clean bytes).
    - No `read -r` flag dependency in the validation loop is portable; if portability is questionable, document it (most modern `/bin/sh` implementations accept it; if not, fall back to plain `read`).
  </action>
  <verify>
    <automated>grep -cE "^[[:space:]]*--mode\)" scripts/test-pii.sh | grep -q '^1$' &amp;&amp; grep -cE "^[[:space:]]*--mode=\*\)" scripts/test-pii.sh | grep -q '^1$' &amp;&amp; grep -c 'MODE="fake"' scripts/test-pii.sh | grep -q '^1$' &amp;&amp; grep -c 'live|fake' scripts/test-pii.sh | grep -Eq '^[1-9]' &amp;&amp; grep -c 'write_notifs_file' scripts/test-pii.sh | grep -Eq '^[2-9]' &amp;&amp; grep -c 'scenario_pii_fake' scripts/test-pii.sh | grep -Eq '^[2-9]' &amp;&amp; grep -c 'scenario_pii_live' scripts/test-pii.sh | grep -Eq '^[2-9]' &amp;&amp; grep -c 'e2e-session-1' scripts/test-pii.sh | grep -Eq '^[1-9]' &amp;&amp; grep -c 'OTTO_FAKE_KIRO_NOTIFICATIONS_FILE' scripts/test-pii.sh | grep -Eq '^[1-9]' &amp;&amp; grep -c 'Phase 08.3.2' scripts/test-pii.sh | grep -Eq '^[1-9]' &amp;&amp; sh -n scripts/test-pii.sh &amp;&amp; (command -v shellcheck >/dev/null 2>&amp;1 &amp;&amp; shellcheck scripts/test-pii.sh || true) &amp;&amp; make vet</automated>
    <human-check>Operator runs `./scripts/test-pii.sh pii` (default `--mode fake`) on a POSIX dev box (macOS or Linux) with `make build-fake-kiro` having produced bin/fake-kiro-cli + gateway started with KIRO_CMD pointing at it + OTTO_FAKE_KIRO_NOTIFICATIONS_FILE set. Expected: exit 0 with "all checks passed" (or "0 check(s) failed" wording). Operator also runs `./scripts/test-pii.sh pii --mode live` — expected: deprecation banner visible. **Sibling-parity check (W5 from plan-checker):** Operator runs both scripts against the same fixture (set `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE` to a path; the script writes its NDJSON output there) and compares the byte stream with `sha256sum` (POSIX) vs `Get-FileHash -Algorithm SHA256` (Windows). Hashes MUST match exactly — any divergence indicates f7ccd40-style sibling drift.</human-check>
  </verify>
  <done>
    `scripts/test-pii.sh` exposes the new `--mode` flag with the same default and validation surface as `scripts/test-pii.ps1`. The two siblings produce equivalent notification frames (verified by comparing the on-disk output bytes when both run with the same fixtures). On a POSIX dev box with the preconditions met, `./scripts/test-pii.sh pii` exits 0. `sh -n` (and `shellcheck` if available) passes.
  </done>
</task>

<task type="execute">
  <name>Task 2: Update 08.3-HUMAN-UAT.md follow-up entry and flip REQUIREMENTS.md TEST-01 to Complete</name>
  <files>.planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md (full file — find the `## Gaps` section the ROADMAP entry references; the follow-up entry goes immediately after that gap record so the linkage from "the gap was found" to "the gap was closed in 08.3.2" is one document)
    - .planning/REQUIREMENTS.md (the TEST-01 bullet and Traceability row landed by Plan 01 Task 1, plus the Coverage block; this task only toggles checkbox state and Traceability status)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-01-SUMMARY.md (line ranges where TEST-01 was inserted)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-02-SUMMARY.md (Windows operator sign-off output — paste relevant lines into the HUMAN-UAT addendum)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-VALIDATION.md §Manual-Only Verifications (gives the operator's verification steps for both Windows and POSIX — copy verbatim into the HUMAN-UAT addendum)
  </read_first>
  <action>
    Part A — Append a new section to `.planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md` placed immediately after the existing `## Gaps` section (or at the end of file if `## Gaps` is the last section). Header: `## Follow-up: Phase 08.3.2 — PII Smoke Test Methodology Fix (2026-06-04)`. Body sections:

    - **Methodology change summary** (3-5 lines): explain that v1.9.3 HUMAN-UAT confirmed the encrypt/decrypt pipeline is correct (the "no ciphertext leak" assertion has been passing all along) but the "round-trip decrypt: '...' present in response" assertion failed because Claude (via kiro-cli) refuses to verbatim-echo PII-shaped data on safety grounds. Phase 08.3.2 replaces the LLM-cooperation dependency with a deterministic fake-kiro-cli worker accessed via the existing `KIRO_CMD` env var, eliminating the bitrot class.
    - **Operator verification steps (Windows)**: paste the steps from VALIDATION.md §Manual-Only Verifications row 1 (the splunk-box procedure: pull v1.9.4 + `make build-fake-kiro` + set KIRO_CMD in `.otto-gw.overrides.env` + start gateway + run `.\scripts\test-pii.ps1 pii` + verify `0 check(s) failed`).
    - **Operator verification steps (POSIX)**: paste the steps from VALIDATION.md §Manual-Only Verifications row 2 (the macOS/Linux dev-box procedure).
    - **Historical evidence preserved**: state that the 2026-06-03 splunk-box pass for items 1-3 (Phase 8.3 success criteria) remains valid as historical evidence of Phase 8.3's deadlock-removal correctness. The PII smoke run from that session is preserved as the discovery moment for the methodology gap.
    - **Live mode known-failure note**: brief paragraph documenting that `-Mode live` / `--mode live` remains supported but will fail today's `round-trip decrypt: '<value>' NOT in response` assertion against current Claude releases. The script emits a deprecation banner on invocation. Operators who want to verify a real `kiro-cli` end-to-end should use the standard Phase 8.3 smoke check (engine.prompt.sent + engine.prompt.completed log markers + a non-PII chat round-trip) rather than relying on PII verbatim-echo.
    - **Linked artifacts**: bullet list pointing at `.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-{RESEARCH,PATTERNS,VALIDATION,01-SUMMARY,02-SUMMARY,03-SUMMARY}.md`.

    Do NOT delete or rewrite the existing `## Gaps` section or any prior HUMAN-UAT content — append only. The historical record of "this gap was found" must remain intact next to the new record of "this gap was closed."

    Part B — Flip TEST-01 status in `.planning/REQUIREMENTS.md`:
    - Change the TEST-01 bullet's checkbox from `- [ ] **TEST-01**:` to `- [x] **TEST-01**:`. Do NOT change the requirement wording.
    - Change the Traceability row from `| TEST-01 | Phase 08.3.2 | Pending |` to `| TEST-01 | Phase 08.3.2 | Complete |`.
    - Update the `Last updated` footer line (around line 226) to append: `2026-06-04 — TEST-01 verified via operator HUMAN-UAT in Phase 08.3.2.` Do NOT modify the `Requirements defined` line.

    Part C — Pre-flip gate: this task MUST NOT flip TEST-01 to `[x]` / Complete until operator HUMAN-UAT for BOTH Plan 02 (Windows) AND Task 1 of this plan (POSIX) has been recorded with successful exit-0 runs. If at execution time either run has not been completed (check by reading 08.3.2-02-SUMMARY.md and the current task's pre-flip notes), STOP and emit `BLOCKED: TEST-01 flip requires both Windows + POSIX HUMAN-UAT sign-offs (see VALIDATION.md §Manual-Only Verifications)`. Operator runs the smoke tests and pastes results into the HUMAN-UAT addendum BEFORE this task completes its flip step.
  </action>
  <verify>
    <automated>test -f .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-02-SUMMARY.md &amp;&amp; test -f .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-03-SUMMARY.md &amp;&amp; (grep -Eq '0 check\(s\) failed|all checks passed' .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-02-SUMMARY.md || { echo 'BLOCKED: Plan 02 (Windows) operator sign-off missing 0 check(s) failed marker; refusing to flip TEST-01 to Complete'; exit 1; }) &amp;&amp; (grep -Eq '0 check\(s\) failed|all checks passed' .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-03-SUMMARY.md || { echo 'BLOCKED: Plan 03 (POSIX) operator sign-off missing 0 check(s) failed marker; refusing to flip TEST-01 to Complete'; exit 1; }) &amp;&amp; grep -c '## Follow-up: Phase 08.3.2' .planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md | grep -q '^1$' &amp;&amp; grep -c '08.3.2-RESEARCH.md\|08.3.2-PATTERNS.md\|08.3.2-VALIDATION.md' .planning/phases/08.3-acp-prompt-non-blocking-refactor/08.3-HUMAN-UAT.md | grep -Eq '^[1-9]' &amp;&amp; grep -c '^- \[x\] \*\*TEST-01\*\*:' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; grep -c '^| TEST-01 | Phase 08.3.2 | Complete |' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; ! grep -q '^| TEST-01 | Phase 08.3.2 | Pending |' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q '^- \[ \] \*\*TEST-01\*\*:' .planning/REQUIREMENTS.md &amp;&amp; grep -q '2026-06-04 — TEST-01 verified' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    `08.3-HUMAN-UAT.md` has a new `## Follow-up: Phase 08.3.2` section documenting the methodology change, both operator-verification procedures, and links to the phase artifacts. The existing `## Gaps` section is preserved unchanged. `REQUIREMENTS.md` TEST-01 row is checked `[x]`, Traceability is `Complete`, and the footer notes the verification date.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> test-pii.sh | operator-trusted invocation; same posture as PowerShell sibling |
| test-pii.sh -> $TMPDIR_ | script writes a tmpfile auto-cleaned by trap at EXIT/INT/TERM |
| HUMAN-UAT addendum | append-only documentation; no code surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3.2-03-T2 | Tampering | sibling drift between test-pii.ps1 and test-pii.sh (re-introduces bugs the other sibling fixed) | mitigate | This plan's Task 1 explicitly cross-checks the PS variant before writing the shell variant, uses the same default (`fake`), the same sessionId literal (`e2e-session-1`), the same env-var contract (`OTTO_FAKE_KIRO_NOTIFICATIONS_FILE`), and the same banner wording. The verify gate asserts both scripts contain `e2e-session-1` and `OTTO_FAKE_KIRO_NOTIFICATIONS_FILE`. Anti-pattern guardrail tied to commit f7ccd40 (the prior sibling-drift incident on this same file pair). |
| T-08.3.2-03-AP1 | Tampering | POSIX BOM concern (does not apply on POSIX but cited defensively) | accept | POSIX `printf` produces clean UTF-8 bytes by default; no BOM risk. PowerShell sibling owns the BOM mitigation. |
| T-08.3.2-03-AP3 | Repudiation | TEST-01 flipped to Complete before operator actually ran the smoke test | mitigate | Task 2's Part C is a pre-flip gate: the task MUST not flip the checkbox until both Plan 02 (Windows) and Task 1 (POSIX) HUMAN-UAT sign-offs are recorded. If either is missing the task emits BLOCKED and waits. Documented in the action section. |

No `high`-severity threats. All mitigations are documentation + ordering discipline.
</threat_model>

<verification>
- `grep -E "^[[:space:]]*--mode\)" scripts/test-pii.sh` returns 1
- `grep 'MODE="fake"' scripts/test-pii.sh` returns 1
- `grep 'scenario_pii_fake' scripts/test-pii.sh` returns ≥ 2 (definition + dispatch)
- `grep 'e2e-session-1' scripts/test-pii.sh` returns ≥ 1
- `sh -n scripts/test-pii.sh` exits 0
- `shellcheck scripts/test-pii.sh` is clean (if shellcheck installed)
- `08.3-HUMAN-UAT.md` contains `## Follow-up: Phase 08.3.2` heading
- REQUIREMENTS.md TEST-01 row is `[x]` with Traceability `Complete` and footer dated 2026-06-04
- Behavioral (operator-manual, POSIX): `./scripts/test-pii.sh pii` exits 0 with "all checks passed" when gateway is properly configured
- `make ci` remains green
</verification>

<success_criteria>
Both sibling scripts (`test-pii.ps1` and `test-pii.sh`) expose equivalent `-Mode`/`--mode` parameters with the same default (`fake`) and the same notification-file contract. The HUMAN-UAT record for Phase 8.3 carries a follow-up section documenting the Phase 08.3.2 methodology change and preserving the 2026-06-03 splunk-box evidence. REQUIREMENTS.md TEST-01 is checked Complete after operator verification on both Windows AND POSIX. Phase 08.3.2 close-out is ready.
</success_criteria>

<output>
Create `.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-03-SUMMARY.md` documenting: the exact line-ranges edited in test-pii.sh, the final shape of the --mode validator, the write_notifs_file helper body, the POSIX operator-manual sign-off (paste `./scripts/test-pii.sh pii` output showing "all checks passed"), the lines appended to 08.3-HUMAN-UAT.md, and the REQUIREMENTS.md flip details. If the pre-flip gate caught a missing HUMAN-UAT step, document the BLOCKED state and the operator action that resolved it.
</output>
</content>
</invoke>