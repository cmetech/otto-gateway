---
phase: 08.3.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - Makefile
autonomous: true
requirements: [TEST-01]
tags: [smoke-test, fake-kiro, makefile, requirements]
must_haves:
  truths:
    - "TEST-01 exists in REQUIREMENTS.md under a new Test methodology subsection."
    - "TEST-01 has a Traceability table row pointing to Phase 08.3.2 with status Complete (set on close-out, [x])."
    - "ROADMAP entry for Phase 08.3.2 cites the canonical requirement (TEST-01), not the previous typo (TRST-01/02)."
    - "make build-fake-kiro produces bin/fake-kiro-cli (POSIX) or bin/fake-kiro-cli.exe (Windows) from tests/e2e/cmd/fake-kiro-cli, CGO disabled."
    - "make build-fake-kiro is exposed via make help and listed in .PHONY."
    - "make ci remains green (no changes to compiled Go code; new Makefile target does not enter the ci dependency graph)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-01 definition + Traceability row"
      contains: "TEST-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 08.3.2 entry with corrected Requirements field"
      contains: "TEST-01"
    - path: "Makefile"
      provides: "build-fake-kiro target compiling tests/e2e/cmd/fake-kiro-cli to bin/"
      contains: "build-fake-kiro"
  key_links:
    - from: "scripts/test-pii.ps1 (-Mode fake path, plan 02)"
      to: "bin/fake-kiro-cli[.exe]"
      via: "operator sets KIRO_CMD=<absolute path to bin/fake-kiro-cli>"
      pattern: "build-fake-kiro"
    - from: "Phase 08.3.2 close-out"
      to: "REQUIREMENTS.md TEST-01 traceability row"
      via: "checkbox flip + status Complete"
      pattern: "TEST-01.*Phase 08.3.2"
---

<objective>
Establish the foundation for the PII smoke-test methodology fix: introduce requirement TEST-01 in REQUIREMENTS.md (operator smoke tests must not depend on LLM cooperation), correct the ROADMAP 08.3.2 entry's requirement-ID typo (TRST-01/02 -> TEST-01) so the plan's `requirements: [TEST-01]` field has a valid backing requirement, and add a `build-fake-kiro` Makefile target that compiles `tests/e2e/cmd/fake-kiro-cli` into `bin/fake-kiro-cli[.exe]` so the PowerShell + shell smoke scripts (built in plans 02 and 03) have a stable, deterministic worker binary to point `KIRO_CMD` at.

Purpose: nothing downstream can claim `requirements: [TEST-01]` until the requirement exists; the scripts cannot reach a fake worker until a build target produces the binary at a known path. This plan is the strictly-sequenced prerequisite for the vertical-slice work that follows.

Output: REQUIREMENTS.md TEST-01 entry + Traceability row, ROADMAP entry corrected, Makefile `build-fake-kiro` target available for local builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md
@.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce TEST-01 in REQUIREMENTS.md and correct the ROADMAP entry typo</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — observe the section header style `### Trust gates — Lint, test, security` at lines 94-103 and the Traceability table starting at line 152; the `Coverage` block at 217-222 reports `62 total`)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md section §Assumptions Log row A5 and the final REQ-ID note (recommends option (a): introduce TEST-01)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md section `.planning/REQUIREMENTS.md (MODIFY — add TEST-01 requirement)`
    - .planning/ROADMAP.md lines 466-495 (Phase 08.3.2 entry, particularly the `**Requirements:**` line)
  </read_first>
  <action>
    Add a new subsection to REQUIREMENTS.md immediately after the existing `### Trust gates — Lint, test, security` block (after the TRST-08 bullet and before `## v2 Requirements`). Header: `### Test methodology — Smoke tests + operator verification`. Single bullet:

    - Identifier: `TEST-01`
    - Status checkbox: leave UNCHECKED (`[ ]`) in this task — Plan 03 (or the phase close-out) flips it to `[x]` after operator HUMAN-UAT verifies the round-trip works. Mirrors how PLUG-06 / OBSV-04 were toggled in Phase 08.1.
    - Body wording (use this exact phrasing for traceability with the ROADMAP entry and PATTERNS analog): "Operator smoke tests under `scripts/test-pii.*` MUST NOT depend on the LLM's willingness to echo / repeat / verbatim-reproduce test fixture data. The encrypt -> decrypt round-trip assertion uses a deterministic worker (default: `tests/e2e/cmd/fake-kiro-cli` swapped in via `KIRO_CMD`) so that future model-policy changes at `kiro-cli`'s upstream do not cause the smoke test to bitrot."

    Add one Traceability row to the existing table (after the TRST-08 row at line 215, preserving alphabetic/numeric placement consistent with the existing ordering — TEST-01 goes immediately after the trust-gate cluster):

    `| TEST-01 | Phase 08.3.2 | Pending |` (status flips to `Complete` at phase close-out; this task lands it as Pending because no plan in this phase has run yet).

    Update the `## Coverage` block (lines 217-222): bump `v1 requirements: 62 total` -> `63 total`, `Mapped to phases: 62 ✓` -> `63 ✓`. Keep the `Unmapped: 0 ✓` line. Add a one-line note to the `Last updated` footer: "TEST-01 added in Phase 08.3.2 — smoke-test methodology requirement."

    Then correct ROADMAP.md Phase 08.3.2 entry: change the `**Requirements:** TRST-01, TRST-02 (smoke-test reliability — verify against existing IDs during planning)` line to `**Requirements:** TEST-01 (introduced in this phase — smoke tests must not depend on LLM cooperation; see REQUIREMENTS.md ### Test methodology)`. This is a typo correction confirmed in RESEARCH §Assumptions Log A5 — TRST-01/02 are the existing golangci-lint and govulncheck CI gates already complete in Phase 1.

    Do NOT touch any other ROADMAP entries, requirement bullets, or coverage rows.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*TEST-01\*\*:' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; grep -c '^| TEST-01 | Phase 08.3.2 | Pending |' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; grep -q '63 total' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^\*\*Requirements:\*\* TEST-01' .planning/ROADMAP.md &amp;&amp; ! grep -A2 '### Phase 08.3.2:' .planning/ROADMAP.md | grep -q 'TRST-01, TRST-02'</automated>
  </verify>
  <done>
    REQUIREMENTS.md has exactly one `TEST-01` bullet, one Traceability row, coverage bumped to 63. ROADMAP Phase 08.3.2 entry's Requirements line cites TEST-01 only (no TRST-01/02 typo remains).
  </done>
</task>

<task type="execute">
  <name>Task 2: Add Makefile build-fake-kiro target (host-platform variant)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 19-26 (.PHONY declarations + `build` target shape)
    - Makefile lines 84-102 (`cross-darwin-arm64` / `cross-windows-amd64` recipes — `CGO_ENABLED=0` + `GOOS`/`GOARCH` pattern)
    - Makefile lines 251-252 (`help` target — `awk` over `## description` annotations)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-RESEARCH.md §Architectural Responsibility Map row "Fake-kiro binary location" (calls for a stable path) + §Open Questions Q1 (recommendation: pre-built shipped artifact OR on-demand build)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-PATTERNS.md section `Makefile (MODIFY — add build-fake-kiro target)` Variant A (host-only — chosen here)
    - .planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-VALIDATION.md Wave 0 Requirements item "Makefile — add build-fake-kiro target"
  </read_first>
  <action>
    Add a new phony target `build-fake-kiro` immediately after the `cross-windows-amd64` recipe (Makefile around line 102) and before the `Distribution packaging` comment block (line 104). Use the Variant A (host-only) shape from PATTERNS.md — operators on Windows run `make build-fake-kiro` on that host, operators on POSIX run it on theirs. The target compiles `./tests/e2e/cmd/fake-kiro-cli` to `$(BUILD_DIR)/fake-kiro-cli` on POSIX hosts and to `$(BUILD_DIR)/fake-kiro-cli.exe` on Windows hosts (detect via `go env GOEXE` or condition on `GOOS=windows`).

    Recipe template (the executor should adapt the binary-suffix detection consistently with how `package` target detects host OS at lines 119-126):

    - `.PHONY` line at the top of the file (currently lines 19-21): append `build-fake-kiro` to the existing `.PHONY` list. Place it next to the existing `build` target so the read order is `build` then `build-fake-kiro`.
    - Recipe body: `@mkdir -p $(BUILD_DIR)` then a single `CGO_ENABLED=0 go build -o $(BUILD_DIR)/fake-kiro-cli$$(go env GOEXE) ./tests/e2e/cmd/fake-kiro-cli` line. The `$$(go env GOEXE)` shell expansion yields `.exe` on Windows hosts and empty string on POSIX hosts; this gives one recipe that works on both.
    - Help annotation: `build-fake-kiro: ## Build deterministic fake-kiro-cli into bin/ for scripts/test-pii.* fake mode` — appended via the `## description` convention so `make help` lists it.

    Do NOT add `build-fake-kiro` as a dependency of `ci`, `all`, `build`, `package`, or any release-packaging target — per the threat model (T4) the fake binary must NOT be auto-included in release artifacts. It is built only when an operator explicitly runs `make build-fake-kiro`. The `stage_unix` macro at lines 148-160 stays unchanged.

    Do NOT add cross-compile variants (no `build-fake-kiro-windows-amd64`, etc.) in this plan — RESEARCH Open Question Q1's preferred path is "ship pre-built" but that's deferred to a future packaging discussion; this phase scopes a host-only build target that operators run on their own box, matching the existing `build` target's contract.
  </action>
  <verify>
    <automated>grep -E '^\.PHONY.* build-fake-kiro' Makefile &amp;&amp; grep -E '^build-fake-kiro:.*## Build deterministic fake-kiro-cli' Makefile &amp;&amp; grep -A3 '^build-fake-kiro:' Makefile | grep -q 'CGO_ENABLED=0 go build -o' &amp;&amp; grep -A3 '^build-fake-kiro:' Makefile | grep -q 'tests/e2e/cmd/fake-kiro-cli' &amp;&amp; ! grep -E '^(ci|all|package|build|stage_unix):.* build-fake-kiro' Makefile &amp;&amp; make build-fake-kiro &amp;&amp; test -x bin/fake-kiro-cli$(go env GOEXE)</automated>
  </verify>
  <done>
    `make help` lists `build-fake-kiro`. `make build-fake-kiro` produces an executable file at `bin/fake-kiro-cli` (POSIX) or `bin/fake-kiro-cli.exe` (Windows). `make ci` still passes (the new target is not in its dependency chain). `make package` does not pull the fake binary into `dist/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Makefile | operator's local Go toolchain compiles a binary the smoke test will later swap into KIRO_CMD; binary runs in operator's trust domain only |
| Makefile -> bin/ | build artifact written to repo-local `bin/`; not on PATH by default; not shipped in release packages by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3.2-01-T1 | Tampering | `bin/fake-kiro-cli[.exe]` in repo working tree shadowing PATH or being mistaken for production worker | mitigate | (a) `make build-fake-kiro` writes to `bin/` which is NOT added to PATH by `make build` or any install script — verified by `grep -r 'PATH.*bin' Makefile scripts/install.sh` returning nothing that exports `bin/`; (b) TEST-01 wording in REQUIREMENTS.md explicitly identifies the fake as a "deterministic worker" used "via `KIRO_CMD` swap" — documents intent for any operator reading the requirement; (c) Plan 02's PowerShell precondition gate verifies that `KIRO_CMD` points at the operator-chosen fake path before running probes (defense in depth) |
| T-08.3.2-01-T4 | Tampering | release artifact accidentally bundles the test fixture | mitigate | The `build-fake-kiro` target is NOT added as a dependency of `package`, `package-all`, `cross`, `ci`, or `all`. The `stage_unix` macro at Makefile:148-160 enumerates exactly which scripts/binaries land in `dist/otto_gateway/` — `fake-kiro-cli` is NOT in that list. Future-proofing: a follow-up phase (not this one) may choose to ship the fake explicitly under a `tools/` subdirectory with a separate README; that decision is out of scope here. |
| T-08.3.2-01-REQ | Repudiation | requirement ID drift between REQUIREMENTS.md and ROADMAP | mitigate | Task 1 lands both files in a single commit (REQUIREMENTS.md inserts + ROADMAP typo fix). Verify step asserts both sides match. No window in git history where ROADMAP cites a phantom requirement. |

No `high`-severity threats. ASVS L1 gate satisfied: no production code path changes; the fake binary is operator-trusted; release artifacts unaffected.
</threat_model>

<verification>
- `grep -c '^- \[ \] \*\*TEST-01\*\*:' .planning/REQUIREMENTS.md` returns `1`
- `grep -c '^| TEST-01 | Phase 08.3.2 | Pending |' .planning/REQUIREMENTS.md` returns `1`
- `grep -q '63 total' .planning/REQUIREMENTS.md` succeeds
- `grep -q '^\*\*Requirements:\*\* TEST-01' .planning/ROADMAP.md` succeeds (Phase 08.3.2 entry)
- ROADMAP Phase 08.3.2 entry no longer mentions `TRST-01, TRST-02`
- `make build-fake-kiro` produces an executable at `bin/fake-kiro-cli` on POSIX or `bin/fake-kiro-cli.exe` on Windows
- `make ci` exits 0 (no regression; new target not in `ci` chain)
- `make help` lists `build-fake-kiro` with its description
- `grep -E '^(ci|all|package|build|stage_unix):.* build-fake-kiro' Makefile` returns nothing (T4 mitigation holds)
</verification>

<success_criteria>
TEST-01 exists in REQUIREMENTS.md (unchecked, awaiting close-out flip), has a Traceability row, and is referenced by the ROADMAP Phase 08.3.2 entry. `make build-fake-kiro` is a working, host-platform target that produces a stable-path fake-kiro binary. `make ci` remains green and release packaging is untouched. Plan 02 can now safely claim `requirements: [TEST-01]` and reference `bin/fake-kiro-cli[.exe]` as a known artifact.
</success_criteria>

<output>
Create `.planning/phases/08.3.2-pii-smoke-test-methodology-fix/08.3.2-01-SUMMARY.md` documenting: which lines were edited in REQUIREMENTS.md (TEST-01 bullet, Traceability row, coverage bump), which line in ROADMAP.md was corrected, the exact Makefile target shape that landed, the result of `make build-fake-kiro` on the host platform, and `make ci` green confirmation.
</output>
</content>
</invoke>