---
phase: 14-verify-reliability-findings
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/plugin/regression_rel_hooks_01_test.go
  - internal/config/regression_rel_cfg_01_test.go
  - internal/config/regression_rel_cfg_02_test.go
  - internal/config/regression_rel_cfg_03_test.go
  - internal/pool/regression_rel_cfg_04_test.go
  - .planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-C-1.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-C-2.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-C-3.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-O-1.md
  - .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md
autonomous: true
requirements:
  - REL-VERIFY-MED
  - REL-VERIFY-GATE

must_haves:
  truths:
    - "All 5 Config/Hooks/Observability findings (G-1, C-1, C-2, C-3, O-1) have an evidence file at .planning/phases/14-verify-reliability-findings/14-FINDING-<ID>.md with the D-08 frontmatter (finding, severity, rel_id, status, target_phase, verified_at) and the 4 required sections"
    - "All 5 findings are Medium per D-02 — they could ship as code-walks only, but per CONTEXT.md `<specifics>` we ship a t.Skip()'d Go regression test for each too (the C-1 / C-2 tests are direct templates of the existing TestLoad_StreamIdleTimeoutSec_Negative pattern)"
    - "Each Go regression test follows TestRegression_REL_<KEY>_<NN>_<ShortDescription> naming and carries the verbatim D-12 t.Skip(\"REL-<KEY>-<NN> (<finding>): regression test — unskip in Phase 16 fix commit\") string as the first line of the test body — all 5 reference Phase 16 (Medium)"
    - "Ledger fragment 14-LEDGER-FRAGMENT-04.md lists 5 rows (G-1, C-1, C-2, C-3, O-1) with D-07 column order"
    - "If this plan is the last of the 4 parallel plans to complete AND plan 14-01 has not yet merged AND fragments 01/02/03 all exist, this plan executes the master-ledger merge per the D-07 + Discretion-block spec"
    - "go build ./... passes; go vet ./internal/config/... ./internal/plugin/... ./internal/pool/... clean; gofumpt -l returns empty for new test files; go test -race against affected packages passes (new tests run as SKIP)"
    - "Read-only-implementation rule: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` empty at plan close"
  artifacts:
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md"
      provides: "G-1 (REL-HOOKS-01, Medium) verdict + cite + pointer to TestRegression_REL_HOOKS_01_NonStreamingPostHookSkip"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-C-1.md"
      provides: "C-1 (REL-CFG-01, Medium) verdict + cite + pointer to TestRegression_REL_CFG_01_NegativeZeroEnvCoercion"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-C-2.md"
      provides: "C-2 (REL-CFG-02, Medium) verdict + cite + pointer to TestRegression_REL_CFG_02_PingIntervalPanic"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-C-3.md"
      provides: "C-3 (REL-CFG-03, Medium) verdict + cite + pointer to TestRegression_REL_CFG_03_EmbeddingModelDefaultUnimplemented"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-O-1.md"
      provides: "O-1 (REL-CFG-04, Medium) verdict + cite + pointer to TestRegression_REL_CFG_04_PoolExhaustionSilent"
    - path: "internal/plugin/regression_rel_hooks_01_test.go"
      provides: "TestRegression_REL_HOOKS_01_NonStreamingPostHookSkip"
      contains: "t.Skip(\"REL-HOOKS-01 (G-1): regression test"
    - path: "internal/config/regression_rel_cfg_01_test.go"
      provides: "TestRegression_REL_CFG_01_NegativeZeroEnvCoercion (subtests per env var)"
      contains: "t.Skip(\"REL-CFG-01 (C-1): regression test"
    - path: "internal/config/regression_rel_cfg_02_test.go"
      provides: "TestRegression_REL_CFG_02_PingIntervalPanic"
      contains: "t.Skip(\"REL-CFG-02 (C-2): regression test"
    - path: "internal/config/regression_rel_cfg_03_test.go"
      provides: "TestRegression_REL_CFG_03_EmbeddingModelDefaultUnimplemented"
      contains: "t.Skip(\"REL-CFG-03 (C-3): regression test"
    - path: "internal/pool/regression_rel_cfg_04_test.go"
      provides: "TestRegression_REL_CFG_04_PoolExhaustionSilent"
      contains: "t.Skip(\"REL-CFG-04 (O-1): regression test"
    - path: ".planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md"
      provides: "5-row table (G-1, C-1, C-2, C-3, O-1) with D-07 column order"
  key_links:
    - from: "internal/plugin/regression_rel_hooks_01_test.go"
      to: "internal/engine/collect.go:150-166, :167-171, :187-191 + internal/adapter/anthropic/collect.go:169-180, :182-185, :207 + internal/plugin/logging.go:186, :215 + internal/plugin/trace.go:222, :248"
      via: "drive non-streaming idle-timeout 504 / Result() error 500 path; assert PostHook ran via slog JSON capture + startTimes sync.Map empty"
      pattern: "TestRegression_REL_HOOKS_01_NonStreamingPostHookSkip"
    - from: "internal/config/regression_rel_cfg_01_test.go"
      to: "internal/config/config.go:313, 336, 343, 353, 487 + internal/pool/config.go:119-122 + internal/session/config.go:99-108 + cmd/otto-gateway/main.go:296"
      via: "t.Setenv per var (POOL_SIZE, SESSION_MAX, SESSION_TTL_MS, SESSION_TICK_INTERVAL_MS, CHAT_TRACE_MAX_AGE_DAYS) = -5 → config.Load() → assert named error (direct copy of TestLoad_StreamIdleTimeoutSec_Negative shape)"
      pattern: "TestRegression_REL_CFG_01_NegativeZeroEnvCoercion"
    - from: "internal/config/regression_rel_cfg_02_test.go"
      to: "internal/config/config.go:295, 1057-1071 + internal/acp/client.go:59-61, :505"
      via: "t.Setenv(PING_INTERVAL, '0s') → config.Load() → assert named error (post-fix); pre-fix: Load succeeds, time.NewTicker panics later"
      pattern: "TestRegression_REL_CFG_02_PingIntervalPanic"
    - from: "internal/config/regression_rel_cfg_03_test.go"
      to: "CLAUDE.md (env-var contract documentation) + internal/server/health.go:22-23, 44-47"
      via: "t.Setenv(EMBEDDING_MODEL_DEFAULT, 'qwen3-embed') + captureSlog → assert Warn record mentions the var name (post-fix); pre-fix: silently ignored"
      pattern: "TestRegression_REL_CFG_03_EmbeddingModelDefaultUnimplemented"
    - from: "internal/pool/regression_rel_cfg_04_test.go"
      to: "internal/pool/pool.go:490-505, :506, :611, :687"
      via: "fakeClient with blocking Prompt + 2 concurrent acquires under size-1 pool; assert captureSlog buffer contains exactly one Warn 'pool: waiting for free slot' record (post-fix); pre-fix: no record at default log level"
      pattern: "TestRegression_REL_CFG_04_PoolExhaustionSilent"
---

<objective>
Verify the 5 Config/Hooks/Observability reliability findings (G-1, C-1, C-2, C-3, O-1 — all Medium) from `docs/reviews/2026-06-11-reliability-review.md` against current `main` source. Produce 5 per-finding evidence files, 5 Go regression-test files following the C-1/C-2 direct-template pattern from `internal/config/config_test.go` lines 615-682, and ledger fragment 04. If this plan is the last of the 4 parallel plans to finish AND plan 14-01 has not yet merged, do the merge.

Purpose: Phase 16 fixes all 5 findings (REL-HOOKS-01, REL-CFG-01..04). Phase 14 confirms each failure path still exists in current source via Go test reproducers. Read-only-implementation rule applies — no production source edits to internal/config/, internal/plugin/, internal/engine/, internal/pool/ non-test files.

Output:
- 5 evidence files at `.planning/phases/14-verify-reliability-findings/14-FINDING-{G-1,C-1,C-2,C-3,O-1}.md`
- 5 Go test files with verbatim D-12 t.Skip strings (all reference Phase 16 because all are Medium)
- Ledger fragment at `14-LEDGER-FRAGMENT-04.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verify-reliability-findings/14-CONTEXT.md
@.planning/phases/14-verify-reliability-findings/14-PATTERNS.md
@docs/reviews/2026-06-11-reliability-review.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Verify G-1 (Medium, REL-HOOKS-01) — non-streaming PostHook skip → sync.Map leak</name>
  <files>
    internal/plugin/regression_rel_hooks_01_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the G-1 entry),
    internal/engine/collect.go (lines 150-166, 167-171, 187-191 cited),
    internal/adapter/anthropic/collect.go (lines 169-180, 182-185, 207 cited),
    internal/plugin/logging.go (lines 186, 215 cited — the startTimes sync.Map and the PostHook entry/exit),
    internal/plugin/trace.go (lines 222, 248 cited — same shape for ChatTraceHook),
    internal/plugin/logging_test.go (lines 43-65 for captureSlog + decodeRecords),
    internal/plugin/trace_test.go (lines 30-60 for readNDJSON harness)
  </read_first>
  <behavior>
    - On non-streaming error paths (idle-timeout 504 or Result() error 500), the PostHook chain is skipped
    - LoggingHook.startTimes and ChatTraceHook.startTimes sync.Maps retain stale entries — retry storm leaks memory
    - Pre-fix reproducer: drive a non-streaming error path; assert no PostHook record in slog/NDJSON buffer AND sync.Map size > 0 after
  </behavior>
  <action>
    Step 1 — Verify cite at internal/engine/collect.go:150-166, :167-171, :187-191; internal/adapter/anthropic/collect.go:169-180, :182-185, :207; internal/plugin/logging.go:186, :215; internal/plugin/trace.go:222, :248. Determine whether PostHook is invoked on the error paths today. Record current file:line.

    Step 2 — Create internal/plugin/regression_rel_hooks_01_test.go. Package `plugin_test`. Function: `TestRegression_REL_HOOKS_01_NonStreamingPostHookSkip`. First line of test body: `t.Skip("REL-HOOKS-01 (G-1): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: use captureSlog(t) per Pattern B to get a slog.Logger + *bytes.Buffer, construct a LoggingHook + ChatTraceHook with that logger, call PreHook to populate startTimes, then simulate the error path by calling the error-completion entry point directly (do NOT call PostHook). Then call decodeRecords(t, buf) and assert NO record has `event: post_chain_out`. Also assert via reflection or an exposed accessor (if any) that startTimes sync.Map is non-empty (leaked entries). Use `defer goleak.VerifyNone(t)` if internal/plugin/testmain_test.go has goleak.

    Step 3 — Write 14-FINDING-G-1.md with frontmatter (finding: G-1, severity: M, rel_id: REL-HOOKS-01, status, target_phase: 16, verified_at: 2026-06-11) and four sections per D-08. Verdict per D-11 bias.
  </action>
  <verify>
    <automated>cd internal/plugin && go test -race -run TestRegression_REL_HOOKS_01 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/plugin/... && gofumpt -l internal/plugin/regression_rel_hooks_01_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md && grep -c "REL-HOOKS-01" .planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md</automated>
  </verify>
  <done>
    Test runs as SKIP with Phase 16 reference. Evidence file complete with verdict.
  </done>
  <acceptance_criteria>
    14-FINDING-G-1.md and internal/plugin/regression_rel_hooks_01_test.go exist per D-08 + D-12 specs (Phase 16 in Skip string).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Verify C-1 (Medium, REL-CFG-01) — negative/zero env coercion (direct template)</name>
  <files>
    internal/config/regression_rel_cfg_01_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-C-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the C-1 entry),
    internal/config/config.go (lines 313, 336, 343, 353, 487 cited),
    internal/pool/config.go (lines 119-122 cited),
    internal/session/config.go (lines 99-108 cited),
    cmd/otto-gateway/main.go (line 296 cited),
    internal/config/config_test.go (lines 615-682 — TestLoad_StreamIdleTimeoutSec_Negative family — DIRECT TEMPLATE, copy exactly)
  </read_first>
  <behavior>
    - POOL_SIZE, SESSION_MAX, SESSION_TTL_MS, SESSION_TICK_INTERVAL_MS, CHAT_TRACE_MAX_AGE_DAYS = negative/zero are silently coerced instead of producing a loud boot error
    - Pre-fix reproducer: t.Setenv per var = "-5", call config.Load(), assert err == nil OR err does not mention the var name (pre-fix observable)
    - Post-fix: each var produces err mentioning the var name + "must be >= 0" (matching STREAM_IDLE_TIMEOUT_SEC posture)
  </behavior>
  <action>
    Step 1 — Verify cite at internal/config/config.go:313, 336, 343, 353, 487; internal/pool/config.go:119-122; internal/session/config.go:99-108; cmd/otto-gateway/main.go:296. For each env var, determine whether negative/zero is currently rejected or coerced. Record per-var verdict.

    Step 2 — Create internal/config/regression_rel_cfg_01_test.go. Package `config_test`. Function: `TestRegression_REL_CFG_01_NegativeZeroEnvCoercion`. First line of test body: `t.Skip("REL-CFG-01 (C-1): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: 5 t.Run() subtests, one per env var (POOL_SIZE, SESSION_MAX, SESSION_TTL_MS, SESSION_TICK_INTERVAL_MS, CHAT_TRACE_MAX_AGE_DAYS). Each subtest is a direct copy/parameterization of TestLoad_StreamIdleTimeoutSec_Negative (config_test.go:655-667):
    ```go
    t.Setenv("POOL_SIZE", "-5")
    _, err := config.Load()
    if err == nil { t.Fatal("Load() should return an error for POOL_SIZE=-5, got nil") }
    if !strings.Contains(err.Error(), "POOL_SIZE") { t.Errorf("error should mention POOL_SIZE, got: %v", err) }
    if !strings.Contains(err.Error(), "must be >= 0") { t.Errorf("error should explain the constraint, got: %v", err) }
    ```
    The whole thing is t.Skip()'d so it doesn't run in Phase 14. Phase 16's fix commit deletes the Skip line; the subtests then exercise the new boot-error path.

    Step 3 — Write 14-FINDING-C-1.md with frontmatter (finding: C-1, severity: M, rel_id: REL-CFG-01, status, target_phase: 16, verified_at: 2026-06-11) and four sections. "Current-source check" must list per-var status (silently-coerced vs already-rejected) so the Phase 16 fix scope is per-var explicit.
  </action>
  <verify>
    <automated>cd internal/config && go test -race -run TestRegression_REL_CFG_01 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/config/... && gofumpt -l internal/config/regression_rel_cfg_01_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-C-1.md && grep -c "REL-CFG-01" .planning/phases/14-verify-reliability-findings/14-FINDING-C-1.md && grep -c "POOL_SIZE" internal/config/regression_rel_cfg_01_test.go</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete with per-var verdict matrix.
  </done>
  <acceptance_criteria>
    14-FINDING-C-1.md and internal/config/regression_rel_cfg_01_test.go exist per spec. Test file contains all 5 env var names as subtest cases. Evidence file's Current-source check section lists per-var status.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Verify C-2 (Medium, REL-CFG-02) — PING_INTERVAL <= 0 panic (direct template)</name>
  <files>
    internal/config/regression_rel_cfg_02_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-C-2.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the C-2 entry),
    internal/config/config.go (lines 295, 1057-1071 cited — the PING_INTERVAL parse),
    internal/acp/client.go (lines 59-61, 505 cited — the time.NewTicker invocation),
    internal/config/config_test.go (lines 655-667 — same direct template as C-1)
  </read_first>
  <behavior>
    - PING_INTERVAL <= 0 currently passes config.Load() unchecked; time.NewTicker(d) panics at runtime when d <= 0
    - Pre-fix reproducer: t.Setenv(PING_INTERVAL, "0s"), config.Load() succeeds (no error); ticker would panic later
    - Post-fix: config.Load() returns a named error before any ticker construction
  </behavior>
  <action>
    Step 1 — Verify cite at internal/config/config.go:295, :1057-1071 and internal/acp/client.go:59-61, :505. Determine whether PING_INTERVAL <= 0 is rejected today. Record current file:line.

    Step 2 — Create internal/config/regression_rel_cfg_02_test.go. Package `config_test`. Function: `TestRegression_REL_CFG_02_PingIntervalPanic`. First line of test body: `t.Skip("REL-CFG-02 (C-2): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: same shape as C-1 — `t.Setenv("PING_INTERVAL", "0s"); _, err := config.Load(); if err == nil || !strings.Contains(err.Error(), "PING_INTERVAL") { t.Fatalf(...) }`. Also include a subtest for "-60000ms" per the success criterion. Pre-fix observable: err is nil.

    Step 3 — Write 14-FINDING-C-2.md with frontmatter (finding: C-2, severity: M, rel_id: REL-CFG-02, status, target_phase: 16, verified_at: 2026-06-11) and four sections.
  </action>
  <verify>
    <automated>cd internal/config && go test -race -run TestRegression_REL_CFG_02 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/config/... && gofumpt -l internal/config/regression_rel_cfg_02_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-C-2.md && grep -c "REL-CFG-02" .planning/phases/14-verify-reliability-findings/14-FINDING-C-2.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-C-2.md and internal/config/regression_rel_cfg_02_test.go exist per spec.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Verify C-3 (Medium, REL-CFG-03) — EMBEDDING_MODEL_DEFAULT documented but never read</name>
  <files>
    internal/config/regression_rel_cfg_03_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-C-3.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the C-3 entry),
    CLAUDE.md (the env-var contract — search for EMBEDDING_MODEL_DEFAULT to confirm it's documented),
    internal/server/health.go (lines 22-23, 44-47 cited),
    internal/config/config_test.go (env-set pattern around lines 655-667),
    internal/plugin/logging_test.go (captureSlog pattern lines 43-65)
  </read_first>
  <behavior>
    - EMBEDDING_MODEL_DEFAULT is in CLAUDE.md's env-var contract but the config loader and runtime never read it
    - Pre-fix reproducer: t.Setenv("EMBEDDING_MODEL_DEFAULT", "qwen3-embed"), boot via config.Load(), capture slog; assert NO Warn record mentions the var name (pre-fix observable: silently ignored)
    - Post-fix: config.Load emits a startup Warn naming the var (D-decision per CONTEXT.md success criterion 13)
  </behavior>
  <action>
    Step 1 — Verify cite. Grep CLAUDE.md for EMBEDDING_MODEL_DEFAULT to confirm documentation present. Read internal/server/health.go:22-23, :44-47 + any other read site for EMBEDDING_MODEL_DEFAULT (grep `internal/` for the literal). Determine whether the var is read anywhere today. Record findings.

    Step 2 — Create internal/config/regression_rel_cfg_03_test.go. Package `config_test`. Function: `TestRegression_REL_CFG_03_EmbeddingModelDefaultUnimplemented`. First line of test body: `t.Skip("REL-CFG-03 (C-3): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: define captureSlog helper inline (per Pattern B) or import from internal/plugin/logging_test.go if accessible (likely not — package-private; redefine inline). `t.Setenv("EMBEDDING_MODEL_DEFAULT", "qwen3-embed")`, call config.Load() with the captured slog logger as the Load's logger argument (if Load takes a logger; if not, use slog.SetDefault temporarily). Then decode records and assert NO record matches `level: WARN AND msg contains "EMBEDDING_MODEL_DEFAULT"` (pre-fix observable).

    Step 3 — Write 14-FINDING-C-3.md with frontmatter (finding: C-3, severity: M, rel_id: REL-CFG-03, status, target_phase: 16, verified_at: 2026-06-11) and four sections. "Current-source check" must explicitly confirm grep across internal/ for the var literal — if it's read NOWHERE, that's the pre-fix `confirmed` evidence.
  </action>
  <verify>
    <automated>cd internal/config && go test -race -run TestRegression_REL_CFG_03 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/config/... && gofumpt -l internal/config/regression_rel_cfg_03_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-C-3.md && grep -c "REL-CFG-03" .planning/phases/14-verify-reliability-findings/14-FINDING-C-3.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete with grep verdict.
  </done>
  <acceptance_criteria>
    14-FINDING-C-3.md and internal/config/regression_rel_cfg_03_test.go exist per spec. Evidence file's Current-source check explicitly cites the grep result for EMBEDDING_MODEL_DEFAULT across internal/.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: Verify O-1 (Medium, REL-CFG-04) — pool exhaustion silent at default log level</name>
  <files>
    internal/pool/regression_rel_cfg_04_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-O-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the O-1 entry),
    internal/pool/pool.go (lines 490-505, 506, 611, 687 cited — the slot-acquire wait point),
    internal/pool/pool_test.go (fakeClient with promptFn gate pattern),
    internal/plugin/logging_test.go (captureSlog + decodeRecords pattern)
  </read_first>
  <behavior>
    - When the pool is fully saturated and a request parks waiting for a slot, no log line is emitted at the default log level
    - Operators see "the gateway silently stopped answering" with no signal in the log
    - Pre-fix reproducer: size-1 pool with a fakeClient whose Prompt blocks, fire a second concurrent acquire, assert captureSlog buffer contains ZERO Warn records mentioning "waiting for free slot" (pre-fix observable)
  </behavior>
  <action>
    Step 1 — Verify cite at internal/pool/pool.go:490-505, :506, :611, :687. Determine whether any Warn-level log is emitted at the slot-acquire wait point today. Record current file:line.

    Step 2 — Create internal/pool/regression_rel_cfg_04_test.go. Package `pool_test`. Function: `TestRegression_REL_CFG_04_PoolExhaustionSilent`. First line of test body: `t.Skip("REL-CFG-04 (O-1): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: define captureSlog helper inline (per Pattern B — internal/plugin/logging_test.go's helper is package-private). Construct pool.Config with the captured logger and Size:1, build a fakeClient whose Prompt blocks on an unsignaled channel (per pool_test.go's fakeClient.promptFn injection point), Warmup, fire a goroutine doing Acquire+Prompt (parks), then fire a second Acquire from the test goroutine. Wait a short window (e.g. 100ms via time.Sleep — fine for this test since the assertion is the absence of a log line). decodeRecords(buf); assert no record has level WARN and msg containing "pool: waiting for free slot" (pre-fix observable). Use `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-O-1.md with frontmatter (finding: O-1, severity: M, rel_id: REL-CFG-04, status, target_phase: 16, verified_at: 2026-06-11) and four sections.
  </action>
  <verify>
    <automated>cd internal/pool && go test -race -run TestRegression_REL_CFG_04 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/pool/... && gofumpt -l internal/pool/regression_rel_cfg_04_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-O-1.md && grep -c "REL-CFG-04" .planning/phases/14-verify-reliability-findings/14-FINDING-O-1.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-O-1.md and internal/pool/regression_rel_cfg_04_test.go exist per spec. Note: this is the ONE file outside the plan's primary subsystem (config/plugin) — it lives in internal/pool/ because the failure path is at the pool-acquire site. Confirm there's no file-ownership conflict with plan 14-01 (14-01's pool files are regression_rel_pool_01..04, NOT regression_rel_cfg_04 — distinct filenames so no conflict).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write ledger fragment 14-LEDGER-FRAGMENT-04.md + (conditional) master ledger merge</name>
  <files>
    .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md,
    .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (re-read each G-1/C-1/C-2/C-3/O-1 entry for exact cites),
    .planning/phases/14-verify-reliability-findings/14-FINDING-G-1.md, 14-FINDING-C-1.md, 14-FINDING-C-2.md, 14-FINDING-C-3.md, 14-FINDING-O-1.md (verdicts from tasks 1-5),
    .planning/REQUIREMENTS.md §Traceability
  </read_first>
  <action>
    Step 1 — Write 14-LEDGER-FRAGMENT-04.md. Single markdown table with exactly 5 data rows (G-1, C-1, C-2, C-3, O-1) + header per D-07: `| Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase |`. Target phase: 16 for all 5 (all Medium), or `drop` for any false-positive.

    Step 2 — Check fragments 01, 02, 03 existence and master ledger existence. If 14-VERIFICATION-LEDGER.md already exists, STOP (someone else merged). If all 3 sibling fragments exist AND master is absent, this plan is last-to-finish: proceed to Step 3.

    Step 3 (merge — only if conditions in Step 2 met) — Same merge procedure as plan 14-01 Task 7 Step 3:
    - Write `14-VERIFICATION-LEDGER.md` with `# Phase 14 Verification Ledger\n\n**Generated:** 2026-06-11\n\n## Summary\n` and per-severity confirmed/false-positive/needs-investigation counts
    - 23 in-scope rows in canonical order: P-1..P-6 (from FRAGMENT-01), H-1..H-5 (FRAGMENT-02), T-1..T-7 (FRAGMENT-03), G-1, C-1..C-3, O-1 (FRAGMENT-04)
    - 12 Low placeholder rows below marked `Status: n/a (deferred to v1.10)`, file:line and target_phase as `—`, Evidence `—`
    - Annotate Summary with `needs_investigation_count` and flag D-10 close gate (count == 0)

    Step 4 (merge — only if conditions in Step 2 met) — Update .planning/REQUIREMENTS.md §Traceability: for each `false-positive` REL-*, change row to `status: dropped (FP — see 14-FINDING-<ID>.md)`.

    Step 5 — Read-only-implementation check: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` MUST return empty.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md && grep -cE "^\| (G-1|C-1|C-2|C-3|O-1) " .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md | grep -q "^5$" && grep -E "^\| Finding ID \| Sev \| REL-\* ID \| Status \| File:line \| Evidence \| Target phase \|" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-04.md && git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/' | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    14-LEDGER-FRAGMENT-04.md exists with 5 rows. Master ledger and REQUIREMENTS.md update only if conditions met. Read-only diff empty.
  </done>
  <acceptance_criteria>
    14-LEDGER-FRAGMENT-04.md exists with the D-07 spec. Read-only-implementation rule holds.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 5 Config/Hooks/Obs evidence files exist with D-08 frontmatter + 4 required sections + verdict per D-09/D-10/D-11
- All 5 Go test files exist with verbatim D-12 t.Skip strings (all reference Phase 16 because all are Medium)
- Ledger fragment 04 exists with D-07 column order and 5 rows
- Trust gates: gofumpt clean on new files, go vet clean for internal/config/, internal/plugin/, internal/pool/, go test -race against those packages passes (new tests SKIP)
- Read-only-implementation rule: production-source diff empty
</verification>

<success_criteria>
- 5 evidence files, 5 Go test files, 1 ledger fragment land on disk
- Every new test runs as SKIP (none PASS, none FAIL)
- D-11 bias respected; D-09 false-positive bar respected; D-10 escape hatch respected
- This plan contributes 5 rows toward the Phase 14 close gate (needs_investigation_count == 0)
</success_criteria>

<output>
Create `.planning/phases/14-verify-reliability-findings/14-04-SUMMARY.md` when done. Summary should:
- List the 5 verdicts by ID and severity
- Report confirmed / false-positive / needs-investigation counts
- Confirm read-only-implementation diff is empty
- Note whether this plan executed the master-ledger merge (Task 6 Step 2 conditions) or just wrote the fragment
- Surface any false-positive REL-* IDs that should drop from Phase 16 scope (with backlink)
</output>
