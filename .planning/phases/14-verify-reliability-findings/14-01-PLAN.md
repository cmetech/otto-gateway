---
phase: 14-verify-reliability-findings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/pool/regression_rel_pool_01_test.go
  - internal/pool/regression_rel_pool_02_test.go
  - internal/pool/regression_rel_pool_03_test.go
  - internal/pool/regression_rel_pool_04_test.go
  - internal/session/regression_rel_pool_05_test.go
  - internal/acp/regression_rel_pool_06_test.go
  - tests/reliability/manual/REL-POOL-06-repro.go
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-2.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-3.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-4.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-5.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-P-6.md
  - .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md
  - .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REL-VERIFY-CRIT
  - REL-VERIFY-HIGH
  - REL-VERIFY-MED
  - REL-VERIFY-GATE

must_haves:
  truths:
    - "All 6 Pool/ACP findings (P-1..P-6) have a per-finding evidence file at .planning/phases/14-verify-reliability-findings/14-FINDING-<ID>.md with frontmatter (finding, severity, rel_id, status, target_phase, verified_at), Review-citation, Current-source-check, Evidence, and Verdict sections per D-08"
    - "Per D-01 (mixed-by-severity rigor): P-1, P-2, P-3 (Critical+High) each have a Go regression test in internal/pool/ following TestRegression_REL_POOL_<NN>_<ShortDescription> naming, with t.Skip(\"REL-POOL-<NN> (<finding>): regression test — unskip in Phase 15 fix commit\") as the first line of the test body and a reproducer body BELOW the Skip per D-12. P-4/P-5/P-6 (Medium) use code-walk only per D-01 (lighter evidence bar)."
    - "P-4 (Medium) has a Go regression test in internal/pool/ (chunk-consumer-starvation reproducer) with the same t.Skip pattern — Phase 16 fix flips the Skip"
    - "P-5 (Medium, LastUsed race) has a Go regression test in internal/session/ that surfaces the data race under go test -race ./... once unskipped — Phase 16 fix flips the Skip and restores the -race trust gate"
    - "P-6 (Medium, Windows pgid) has BOTH a skipped Go stub at internal/acp/regression_rel_pool_06_test.go pointing at the manual reproducer AND a runnable manual reproducer at tests/reliability/manual/REL-POOL-06-repro.go with the header convention from PATTERNS.md Pattern F (finding ID, REL-* ID, target phase, target OS, expected pre-fix behavior, expected post-fix behavior, step-by-step run instructions)"
    - "Ledger fragment 14-LEDGER-FRAGMENT-01.md lists 6 rows (P-1..P-6) with column order: Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase per D-07"
    - "If this plan is the last of the 4 parallel plans to complete its fragment, the executor MERGES all 4 fragments (14-LEDGER-FRAGMENT-{01..04}.md) into .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md as a single markdown table with header row + Summary section counting confirmed/false-positive/needs-investigation by severity, AND updates .planning/REQUIREMENTS.md traceability table marking any false-positive REL-* rows as `dropped (FP — see 14-FINDING-<ID>.md)`. Plan 14-01 is the designated merge-owner if all 4 finish simultaneously."
    - "needs_investigation_count == 0 across all 23 ledger rows at phase close (D-10 gate)"
    - "go build ./... passes; go vet ./... passes; gofumpt -l -d . returns empty for new test files; go test -race ./internal/pool/ ./internal/session/ ./internal/acp/ passes (all new TestRegression_REL_POOL_* tests are skipped — none run, none flip red)"
    - "Read-only-implementation rule: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` returns empty at plan close"
  artifacts:
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md"
      provides: "P-1 (REL-POOL-01, Critical) verdict + cite + pointer to TestRegression_REL_POOL_01_PoolShrinksToZero"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-2.md"
      provides: "P-2 (REL-POOL-02, High) verdict + cite + pointer to TestRegression_REL_POOL_02_CtrlCOrphansChildren"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-3.md"
      provides: "P-3 (REL-POOL-03, High) verdict + cite + pointer to TestRegression_REL_POOL_03_StaleActiveStreamClobber"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-4.md"
      provides: "P-4 (REL-POOL-04, Medium) verdict + cite + pointer to TestRegression_REL_POOL_04_ConsumerBlockedReadLoop"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-5.md"
      provides: "P-5 (REL-POOL-05, Medium) verdict + cite + pointer to TestRegression_REL_POOL_05_LastUsedRace"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-P-6.md"
      provides: "P-6 (REL-POOL-06, Medium) verdict + cite + pointer to tests/reliability/manual/REL-POOL-06-repro.go"
    - path: "internal/pool/regression_rel_pool_01_test.go"
      provides: "TestRegression_REL_POOL_01_PoolShrinksToZero (t.Skip, reproducer body below)"
      contains: "t.Skip(\"REL-POOL-01 (P-1): regression test"
    - path: "internal/pool/regression_rel_pool_02_test.go"
      provides: "TestRegression_REL_POOL_02_CtrlCOrphansChildren"
      contains: "t.Skip(\"REL-POOL-02 (P-2): regression test"
    - path: "internal/pool/regression_rel_pool_03_test.go"
      provides: "TestRegression_REL_POOL_03_StaleActiveStreamClobber"
      contains: "t.Skip(\"REL-POOL-03 (P-3): regression test"
    - path: "internal/pool/regression_rel_pool_04_test.go"
      provides: "TestRegression_REL_POOL_04_ConsumerBlockedReadLoop"
      contains: "t.Skip(\"REL-POOL-04 (P-4): regression test"
    - path: "internal/session/regression_rel_pool_05_test.go"
      provides: "TestRegression_REL_POOL_05_LastUsedRace"
      contains: "t.Skip(\"REL-POOL-05 (P-5): regression test"
    - path: "internal/acp/regression_rel_pool_06_test.go"
      provides: "TestRegression_REL_POOL_06_WindowsPgidNoop stub pointing at manual script"
      contains: "tests/reliability/manual/REL-POOL-06-repro.go"
    - path: "tests/reliability/manual/REL-POOL-06-repro.go"
      provides: "Runnable Windows pgid kill-group reproducer with PATTERNS.md Pattern F header"
    - path: ".planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md"
      provides: "Markdown table with 6 rows (P-1..P-6), columns: Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase"
  key_links:
    - from: "internal/pool/regression_rel_pool_01_test.go"
      to: "internal/pool/pool.go:534, :491-505, :297-306"
      via: "reproducer body asserts pool.Stats().Size shrink on transient acquire error (mirror of respawn_ctx_cancel_test.go shape — invert the assertion to demonstrate pre-fix bug)"
      pattern: "TestRegression_REL_POOL_01_PoolShrinksToZero"
    - from: "internal/pool/regression_rel_pool_02_test.go"
      to: "cmd/otto-gateway/main.go:131, :127 + internal/server/server.go:377-381"
      via: "fakeClient.cancelCallList() asserts deferred cleanup ran on simulated SIGINT-equivalent Close() path"
      pattern: "TestRegression_REL_POOL_02_CtrlCOrphansChildren"
    - from: "internal/pool/regression_rel_pool_03_test.go"
      to: "internal/acp/client.go:868-870, :894-896 + internal/pool/pool.go:618-635"
      via: "two-goroutine race on activeStream pointer reuse asserted via fakeClient.promptFn injection"
      pattern: "TestRegression_REL_POOL_03_StaleActiveStreamClobber"
    - from: "internal/pool/regression_rel_pool_04_test.go"
      to: "internal/acp/stream.go:105-122 + internal/acp/client.go:503-526, :1085"
      via: "stalled drainChunks consumer asserts readLoop liveness escalation to SIGKILL"
      pattern: "TestRegression_REL_POOL_04_ConsumerBlockedReadLoop"
    - from: "internal/session/regression_rel_pool_05_test.go"
      to: "internal/session/registry.go:206, :358 + internal/session/entry_acp.go:77-79"
      via: "concurrent Registry.Get + MarkUsed under -race surfaces DATA RACE"
      pattern: "TestRegression_REL_POOL_05_LastUsedRace"
    - from: "tests/reliability/manual/REL-POOL-06-repro.go"
      to: "internal/acp/pool_pgid_windows.go:15, :21"
      via: "standalone main spawns kiro-cli with grandchildren on Windows, asserts cmd.Cancel() leaves orphans pre-fix"
      pattern: "package main"
---

<objective>
Verify the 6 Pool/ACP reliability findings (P-1 Critical, P-2 High, P-3 High, P-4 Medium, P-5 Medium, P-6 Medium) from `docs/reviews/2026-06-11-reliability-review.md` against current `main` source. Produce 6 per-finding evidence files, 6 Go regression-test reproducer files (5 in-tree + 1 stub pointing at a manual script), 1 manual Windows pgid reproducer script, and ledger fragment 01. If this plan is the last of the 4 parallel plans to finish, also merge the 4 fragments into the master verification ledger and update REQUIREMENTS.md traceability for any false-positives.

Purpose: Phase 15 fixes P-1/P-2/P-3 (REL-POOL-01/02/03 — Critical + High) and Phase 16 fixes P-4/P-5/P-6 (REL-POOL-04/05/06 — Medium). Phase 14 produces the gating evidence ledger that drives which REL-POOL-* requirements enter each downstream phase. Read-only-implementation rule applies — no production source edits.

Output:
- 6 evidence files at `.planning/phases/14-verify-reliability-findings/14-FINDING-{P-1,P-2,P-3,P-4,P-5,P-6}.md`
- 6 Go test files (5 reproducers + 1 manual-pointer stub) following `TestRegression_REL_POOL_<NN>_<ShortDescription>` naming with `t.Skip("REL-POOL-<NN> (<finding>): regression test — unskip in Phase 15 fix commit")` per D-12
- 1 manual reproducer at `tests/reliability/manual/REL-POOL-06-repro.go` with Pattern F header
- Ledger fragment at `.planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md`
- (If last to finish) merged master ledger at `14-VERIFICATION-LEDGER.md` + REQUIREMENTS.md traceability updates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verify-reliability-findings/14-CONTEXT.md
@.planning/phases/14-verify-reliability-findings/14-PATTERNS.md
@docs/reviews/2026-06-11-reliability-review.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Verify P-1 (Critical, REL-POOL-01) — pool shrinks to zero on transient acquire error</name>
  <files>
    internal/pool/regression_rel_pool_01_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-1 entry — read the full failure scenario, not just the cite),
    internal/pool/pool.go (specifically lines 534, 491-505, 297-306 cited by the finding — read the surrounding ~30 lines for context),
    internal/pool/pool_test.go (lines 48-192 for fakeClient + fakeClientFactory + ctxBlockingFactory harness),
    internal/pool/respawn_ctx_cancel_test.go (whole file — the exact analog per PATTERNS.md)
  </read_first>
  <behavior>
    - The current pool acquire path on transient error (line 534 cite area) decrements slot count without re-warming
    - After warmup → kill → transient acquire error sequence, pool.Stats().Size shrinks toward 0
    - Pre-fix reproducer body asserts the shrink behavior (pool.Stats().Size == 0 after N transient errors)
    - Test is t.Skip()'d at top of function body so go test passes; reproducer body is present, compiles cleanly, and will run once Phase 15 deletes the t.Skip line
  </behavior>
  <action>
    Step 1 — Verify cite against current source. Open internal/pool/pool.go at lines 534, 491-505, 297-306. Read the actual code at each citation. Determine whether the failure path (transient acquire error not triggering re-warm) still exists in current main. Record the current file:line in case the code has moved since the review.

    Step 2 — Create internal/pool/regression_rel_pool_01_test.go. Package `pool_test`. First line of test body: `t.Skip("REL-POOL-01 (P-1): regression test — unskip in Phase 15 fix commit")` — verbatim string per D-12. Function name: `TestRegression_REL_POOL_01_PoolShrinksToZero`. BELOW the Skip line, write the actual reproducer body adapting `internal/pool/respawn_ctx_cancel_test.go`'s `ctxBlockingFactory` harness: construct a size-1 pool with a fakeClientFactory whose Spawn returns a transient error after the first client, warm up, kill the warmup client (fc0.fireDone()), drive an acquire that hits the transient error path, assert `p.Stats().Size == 0` (pre-fix assertion demonstrating the bug). Add `defer goleak.VerifyNone(t)` per Pattern A. Do NOT add a new TestMain — the package already has goleak.VerifyTestMain via testmain_test.go.

    Step 3 — Write .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md with frontmatter (finding: P-1, severity: C, rel_id: REL-POOL-01, status: confirmed | false-positive | needs-investigation per Step 1 verdict, target_phase: 15 (or drop if false-positive), verified_at: 2026-06-11) and four sections per D-08:
      - "## Review citation" — quote or cite the P-1 entry in docs/reviews/2026-06-11-reliability-review.md verbatim including file:line
      - "## Current-source check" — fresh file:line cite from Step 1 + 3-5 sentence description of what the code does today and whether the failure path is intact
      - "## Evidence" — pointer to internal/pool/regression_rel_pool_01_test.go::TestRegression_REL_POOL_01_PoolShrinksToZero with the exact t.Skip() string
      - "## Verdict" — confirmed (default per D-11 bias) unless cite is gone (false-positive per D-09) or genuinely ambiguous (needs-investigation per D-10 — must propose what would flip it)

    Per D-11 bias: when cite is intact and no concrete added guard is citable, the verdict is `confirmed`, not "probably-doesn't-trigger".

    NOTE on Phase 14 TDD adaptation: the RED→GREEN→REFACTOR cycle is deferred to Phase 15. For this phase the "GREEN" gate is "test compiles + t.Skip() line present + reproducer body present + go test -run TestRegression_REL_POOL_01 shows --- SKIP". Do NOT attempt to make the test pass red→green here — Phase 15's fix commit does that by deleting the t.Skip line in the same atomic commit as the source fix.
  </action>
  <verify>
    <automated>cd internal/pool && go test -race -run TestRegression_REL_POOL_01 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/pool/... && gofumpt -l internal/pool/regression_rel_pool_01_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md && grep -c "REL-POOL-01" .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md</automated>
  </verify>
  <done>
    internal/pool/regression_rel_pool_01_test.go exists, compiles, runs as SKIP (not FAIL), contains the exact t.Skip string from D-12 and a reproducer body following the respawn_ctx_cancel_test.go shape. 14-FINDING-P-1.md exists with frontmatter + 4 required sections + verdict per D-09/10/11. go vet ./internal/pool/... is clean. gofumpt produces no diff on the new test file.
  </done>
  <acceptance_criteria>
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md is produced with the per-D-08 frontmatter (finding, severity, rel_id, status, target_phase, verified_at) and four sections (Review citation, Current-source-check, Evidence, Verdict). internal/pool/regression_rel_pool_01_test.go is produced with `TestRegression_REL_POOL_01_PoolShrinksToZero`, the verbatim D-12 t.Skip string as the first line of the test body, and a non-empty reproducer body below it.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Verify P-2 (High, REL-POOL-02) — Ctrl-C orphans kiro-cli children</name>
  <files>
    internal/pool/regression_rel_pool_02_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-2.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-2 entry),
    cmd/otto-gateway/main.go (lines 131, 127 cited by the finding — read the full shutdown handler block ~80 lines),
    internal/server/server.go (lines 377-381 cited by the finding),
    internal/pool/pool_test.go (lines 162-168 for fakeClient.cancelCallList() helper)
  </read_first>
  <behavior>
    - Pool.Close() must call Cancel() on every in-flight session before returning so deferred cleanup actually fires on all SIGINT exit paths
    - Pre-fix reproducer asserts that one or more in-flight sessions did NOT receive Cancel() when shutdown ran via a non-graceful exit path
  </behavior>
  <action>
    Step 1 — Verify cite. Open cmd/otto-gateway/main.go at lines 131, 127 and internal/server/server.go at 377-381. Read the surrounding shutdown handler logic. Determine whether the deferred-cleanup-on-every-exit-path fix is present in current main. Record the current file:line.

    Step 2 — Create internal/pool/regression_rel_pool_02_test.go. Package `pool_test`. Function: `TestRegression_REL_POOL_02_CtrlCOrphansChildren`. First line: `t.Skip("REL-POOL-02 (P-2): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: build a size-2 pool with fakeClientFactory, fire two in-flight Prompt calls (use fakeClient.promptFn returning a blocking stream), call pool.Close() from a goroutine simulating a SIGINT-equivalent path, then assert that fakeClient.cancelCallList() returns both session IDs (post-fix expectation). The reproducer is in-the-shape of the pre-fix bug: the assertion that NO sessions were cancelled (the pre-fix observable) is what flips green→red→green across Phase 15. Add `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-P-2.md with frontmatter (finding: P-2, severity: H, rel_id: REL-POOL-02, status, target_phase: 15, verified_at: 2026-06-11) and the four sections per D-08. The "Current-source check" section MUST explicitly note whether `cmd/otto-gateway/main.go` defers cleanup unconditionally or only on the graceful-shutdown branch (the finding's specific failure path). Verdict per D-11 bias.
  </action>
  <verify>
    <automated>cd internal/pool && go test -race -run TestRegression_REL_POOL_02 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/pool/... && gofumpt -l internal/pool/regression_rel_pool_02_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-2.md && grep -c "REL-POOL-02" .planning/phases/14-verify-reliability-findings/14-FINDING-P-2.md</automated>
  </verify>
  <done>
    Test file exists, runs as SKIP, contains exact D-12 t.Skip string. Evidence file has frontmatter + 4 sections + explicit verdict on whether cmd/otto-gateway/main.go's deferred cleanup covers all SIGINT exit paths. go vet clean, gofumpt clean.
  </done>
  <acceptance_criteria>
    14-FINDING-P-2.md and internal/pool/regression_rel_pool_02_test.go exist per the D-08 + D-12 specs above. Test runs as SKIP (not FAIL, not PASS — Skip is the expected status throughout Phase 14).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Verify P-3 (High, REL-POOL-03) — stale activeStream clobber</name>
  <files>
    internal/pool/regression_rel_pool_03_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-3.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-3 entry),
    internal/acp/client.go (lines 868-870, 894-896 cited by the finding — read the activeStream lifecycle code ~50 lines),
    internal/pool/pool.go (lines 618-635 cited by the finding),
    internal/acp/stream_race_test.go (whole file — race-narrowing pattern),
    internal/pool/pool_test.go (lines 128-136 for fakeClient.promptFn injection point)
  </read_first>
  <behavior>
    - Two-goroutine race where goroutine A holds a stale awaitPromptResult reference and goroutine B acquires the recycled slot
    - Pre-fix reproducer body demonstrates the race produces a silent empty 200 (final result is well-formed but content-empty)
    - Test compiles + skips; Phase 15 fix removes Skip to flip the assertion
  </behavior>
  <action>
    Step 1 — Verify cite. Open internal/acp/client.go at lines 868-870 and 894-896, and internal/pool/pool.go at 618-635. Determine whether a CAS-guarded activeStream clear is present in current main (D-09 would call that a false-positive). Record current file:line.

    Step 2 — Create internal/pool/regression_rel_pool_03_test.go. Package `pool_test`. Function: `TestRegression_REL_POOL_03_StaleActiveStreamClobber`. First line: `t.Skip("REL-POOL-03 (P-3): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: use sync.WaitGroup + a tight loop driving Prompt concurrently against a size-1 pool, with fakeClient.promptFn returning a fakeStream that races a stale awaitPromptResult against a new acquire. Assert that the next Prompt returns a non-nil, non-empty result (the post-fix invariant). Use `-race`-friendly goroutine choreography. Add `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-P-3.md with frontmatter (finding: P-3, severity: H, rel_id: REL-POOL-03, status, target_phase: 15, verified_at: 2026-06-11) and the four sections. The "Current-source check" must explicitly evaluate whether a CAS guard exists at the cited site (per D-09 false-positive bar). Verdict per D-11.
  </action>
  <verify>
    <automated>cd internal/pool && go test -race -run TestRegression_REL_POOL_03 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/pool/... && gofumpt -l internal/pool/regression_rel_pool_03_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-3.md && grep -c "REL-POOL-03" .planning/phases/14-verify-reliability-findings/14-FINDING-P-3.md</automated>
  </verify>
  <done>
    Test file exists, runs as SKIP, exact D-12 t.Skip string. Evidence file has frontmatter + 4 sections + verdict, with explicit CAS-guard evaluation per D-09. go vet clean, gofumpt clean.
  </done>
  <acceptance_criteria>
    14-FINDING-P-3.md and internal/pool/regression_rel_pool_03_test.go exist with the required structures. Evidence file's "Current-source check" section explicitly addresses the D-09 false-positive bar (cite gone OR concrete guard at file:line).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Verify P-4 (Medium, REL-POOL-04) — consumer-blocked readLoop SIGKILL escalation</name>
  <files>
    internal/pool/regression_rel_pool_04_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-4.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-4 entry),
    internal/acp/stream.go (lines 105-122 cited),
    internal/acp/client.go (lines 503-526, 1085 cited),
    internal/pool/pool_test.go (lines 35-39 for drainChunks helper)
  </read_first>
  <behavior>
    - Stalled chunk consumer (no drain on the chunk channel) causes the worker's ping to escalate to SIGKILL even when kiro-cli is healthy
    - Reproducer stalls the consumer and asserts SIGKILL fires pre-fix
  </behavior>
  <action>
    Step 1 — Verify cite at internal/acp/stream.go:105-122 and internal/acp/client.go:503-526, :1085. Determine whether readLoop liveness is currently coupled to consumer drain. Record current file:line.

    Step 2 — Create internal/pool/regression_rel_pool_04_test.go. Function: `TestRegression_REL_POOL_04_ConsumerBlockedReadLoop`. First line: `t.Skip("REL-POOL-04 (P-4): regression test — unskip in Phase 16 fix commit")` — note this references **Phase 16** (Medium), not Phase 15. Reproducer body: build a pool with a fakeClient whose stream sends chunks faster than the consumer drains, leave the consumer parked (do NOT call drainChunks), assert via a fakeClient counter or slog capture that SIGKILL was issued. Add `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-P-4.md with frontmatter (finding: P-4, severity: M, rel_id: REL-POOL-04, status, target_phase: 16, verified_at: 2026-06-11) and the four sections.

    NOTE: P-4 is Medium per D-02 (code-walk-only would suffice), but it is still in the C+H reproducer-test naming scheme in PATTERNS.md and CONTEXT.md `<specifics>` — so we ship a t.Skip()'d reproducer test for it too. The Skip reason references Phase 16 (the Medium fix phase) instead of Phase 15.
  </action>
  <verify>
    <automated>cd internal/pool && go test -race -run TestRegression_REL_POOL_04 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/pool/... && gofumpt -l internal/pool/regression_rel_pool_04_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-4.md && grep -c "REL-POOL-04" .planning/phases/14-verify-reliability-findings/14-FINDING-P-4.md</automated>
  </verify>
  <done>
    Test runs as SKIP, exact D-12 t.Skip string (with Phase 16 reference). Evidence file complete with verdict.
  </done>
  <acceptance_criteria>
    14-FINDING-P-4.md and internal/pool/regression_rel_pool_04_test.go exist per spec. Skip string explicitly references Phase 16 (not Phase 15) because P-4 is Medium.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: Verify P-5 (Medium, REL-POOL-05) — Entry.LastUsed data race</name>
  <files>
    internal/session/regression_rel_pool_05_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-5.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-5 entry),
    internal/session/registry.go (lines 206, 358 cited),
    internal/session/entry_acp.go (lines 77-79 cited),
    internal/session/registry_test.go (lines 23-101 for fakeClient + concurrent patterns),
    internal/session/reaper_test.go (lines 34-47 for Registry construction template)
  </read_first>
  <behavior>
    - Concurrent Registry.Get + MarkUsed on the same entry races on Entry.LastUsed
    - Reproducer triggers the race; assertion is "test passes under -race after fix; pre-fix it produces a DATA RACE report"
  </behavior>
  <action>
    Step 1 — Verify cite at internal/session/registry.go:206, :358 and internal/session/entry_acp.go:77-79. Determine whether Entry.LastUsed access is now atomic/mutex-guarded. Record current file:line.

    Step 2 — Create internal/session/regression_rel_pool_05_test.go. Package `session_test`. Function: `TestRegression_REL_POOL_05_LastUsedRace`. First line: `t.Skip("REL-POOL-05 (P-5): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: construct a session Registry via the reaper_test.go template (TTL: 200ms, TickInterval: 50ms, MaxSessions: 32), acquire one entry, then fire N goroutines (e.g. 64) tight-looping Get + MarkUsed against the same session ID via sync.WaitGroup. Under -race, this surfaces the race. Add `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-P-5.md with frontmatter (finding: P-5, severity: M, rel_id: REL-POOL-05, status, target_phase: 16, verified_at: 2026-06-11) and the four sections. The "Current-source check" must explicitly note whether Entry.LastUsed access uses atomic.Int64 or sync.Mutex today (D-09 false-positive bar).
  </action>
  <verify>
    <automated>cd internal/session && go test -race -run TestRegression_REL_POOL_05 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/session/... && gofumpt -l internal/session/regression_rel_pool_05_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-5.md && grep -c "REL-POOL-05" .planning/phases/14-verify-reliability-findings/14-FINDING-P-5.md</automated>
  </verify>
  <done>
    Test runs as SKIP under -race. Evidence file complete with explicit guard-presence evaluation.
  </done>
  <acceptance_criteria>
    14-FINDING-P-5.md and internal/session/regression_rel_pool_05_test.go exist per spec. Test must pass with t.Skip even under -race (Skip happens before the race code).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Verify P-6 (Medium, REL-POOL-06) — Windows pgid no-op (manual reproducer)</name>
  <files>
    internal/acp/regression_rel_pool_06_test.go,
    tests/reliability/manual/REL-POOL-06-repro.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-6.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the P-6 entry),
    internal/acp/pool_pgid_windows.go (lines 15, 21 cited — the entire file is short, read it all),
    internal/acp/pool_pgid_unix.go (the unix sibling — for contrast),
    tests/scripts/test-support-bundle.sh (lines 14-40 for manual script header convention shape; this is the closest in-tree analog)
  </read_first>
  <behavior>
    - Windows pgid kill-group is a no-op; cmd.Cancel() leaves grandchild kiro-cli processes orphaned
    - Failure mode is Windows-only and requires a real process tree — cannot be exercised in go test on Linux CI
    - Manual reproducer ships as a standalone main package under tests/reliability/manual/
    - Go test stub points at the script
  </behavior>
  <action>
    Step 1 — Verify cite at internal/acp/pool_pgid_windows.go:15, :21. Confirm whether the file still contains a no-op or stub kill-group implementation. Record current file:line.

    Step 2 — Create tests/reliability/manual/ directory (it does not exist — confirmed empty `tests/` only contains e2e, install, scripts). Create tests/reliability/manual/REL-POOL-06-repro.go as a standalone `package main` Go program with the Pattern F header comment block at top:
    ```
    // Finding ID: P-6
    // REL-* ID: REL-POOL-06
    // Target phase: 14 (verify) / 16 (fix)
    // Target OS: Windows
    // Expected pre-fix behavior: grandchild kiro-cli processes survive cmd.Cancel()
    // Expected post-fix behavior: full process group dies within 2s of cmd.Cancel()
    // Run instructions: GOOS=windows go run tests/reliability/manual/REL-POOL-06-repro.go (on a Windows host with kiro-cli on PATH)
    ```
    Body: spawn a kiro-cli child that itself spawns grandchildren (e.g. cmd /c with subcommands), wait for ready, then call cmd.Cancel() and poll Task Manager (via os/exec invoking tasklist.exe) to detect orphans. Exit code 0 = orphans found (pre-fix observable) or 1 = clean tree death (post-fix). This is a `package main` — NOT a _test.go file — so it cannot collide with the regular go test build.

    Step 3 — Create internal/acp/regression_rel_pool_06_test.go. Function: `TestRegression_REL_POOL_06_WindowsPgidNoop`. First line: `t.Skip("REL-POOL-06 (P-6): manual reproducer — see tests/reliability/manual/REL-POOL-06-repro.go for Windows-only reproducer")`. Body: empty (the Skip is sufficient — this is a discoverability stub per D-03/D-12).

    Step 4 — Write 14-FINDING-P-6.md with frontmatter (finding: P-6, severity: M, rel_id: REL-POOL-06, status, target_phase: 16, verified_at: 2026-06-11) and the four sections. "Evidence" section MUST point at BOTH the manual script path AND the Go stub. Verdict per D-11 (code-walk on the no-op windows file is sufficient to call it `confirmed`).
  </action>
  <verify>
    <automated>cd internal/acp && go test -race -run TestRegression_REL_POOL_06 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/acp/... && go build ./tests/reliability/manual/... 2>&1 || (cd tests/reliability/manual && go vet ./...) && gofumpt -l internal/acp/regression_rel_pool_06_test.go tests/reliability/manual/REL-POOL-06-repro.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-P-6.md && grep -c "REL-POOL-06" .planning/phases/14-verify-reliability-findings/14-FINDING-P-6.md && grep -c "Target OS: Windows" tests/reliability/manual/REL-POOL-06-repro.go</automated>
  </verify>
  <done>
    Stub test runs as SKIP. Manual reproducer go file has Pattern F header, compiles via go vet, can be built (cross-compile may not work on Linux CI but `go vet` and a parse check are enough — verify with `gofumpt -l`). Evidence file complete with pointers to both files.
  </done>
  <acceptance_criteria>
    14-FINDING-P-6.md, internal/acp/regression_rel_pool_06_test.go, AND tests/reliability/manual/REL-POOL-06-repro.go all exist. The manual script's header comment block contains all 7 fields from Pattern F (finding ID, REL-* ID, target phase, target OS, expected pre-fix behavior, expected post-fix behavior, run instructions).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Write ledger fragment 14-LEDGER-FRAGMENT-01.md + (if last to finish) merge master ledger</name>
  <files>
    .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md,
    .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (re-read each P-1..P-6 entry to extract exact file:line cites for the table),
    .planning/phases/14-verify-reliability-findings/14-FINDING-P-1.md through 14-FINDING-P-6.md (the verdicts written in tasks 1-6 — these populate the Status column),
    .planning/REQUIREMENTS.md (specifically the §Traceability table — read the REL-POOL-* rows to confirm column shape before updating)
  </read_first>
  <action>
    Step 1 — Write .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md. Single markdown table with exactly 6 data rows (P-1..P-6) and header. Column order per D-07 (NO deviation):
    | Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase |
    Status is read from each 14-FINDING-<ID>.md's frontmatter `status` field (confirmed / false-positive / needs-investigation). File:line is the **current-source** cite from each evidence file's "Current-source check" section, NOT the original review cite. Evidence is the relative path `14-FINDING-<ID>.md`. Target phase is `15` (P-1, P-2, P-3), `16` (P-4, P-5, P-6), or `drop` (if any false-positive).

    Step 2 — Check whether fragments 02, 03, 04 already exist on disk (`ls .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-{02,03,04}.md`). If ALL THREE exist, this plan is the last-to-finish: proceed to Step 3 (merge). If any fragment is missing, STOP HERE — the plan that finishes last will do the merge. Per plan-level must_haves, plan 14-01 is the designated merge-owner if multiple plans finish simultaneously; the executor handles this by checking fragment existence atomically.

    Step 3 (merge — only if last to finish) — Write .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md:
    - Top: `# Phase 14 Verification Ledger\n\n**Generated:** 2026-06-11\n\n## Summary\n` followed by counts by severity × status (e.g. "Critical: 1 confirmed / 0 false-positive / 0 needs-investigation; High: X / Y / Z; Medium: X / Y / Z").
    - Below: single master table with 23 rows (P-1..P-6, H-1..H-5, T-1..T-7, G-1, C-1..C-3, O-1) in this exact order. Same 7-column header as the fragments. Concatenate the row content from all 4 fragments, then add a final block for the 12 Lows per D-deferred convention: rows with `Status: n/a (deferred to v1.10)` for completeness (file:line and target_phase columns may be `—`). Total = 23 in-scope rows + 12 placeholder Low rows = 35 rows matching the review's count.

    Step 4 (merge — only if last to finish) — Update .planning/REQUIREMENTS.md §Traceability table. For each finding flagged `false-positive` in the master ledger, change that REL-*'s row to `status: dropped (FP — see 14-FINDING-<ID>.md)`. If all 23 findings are `confirmed`, no updates are needed beyond marking REL-VERIFY-* rows as `complete` once Phase 14 closes (but that flip belongs to the phase-close commit, not this task — confirm via D-discretion "REQUIREMENTS.md traceability table updates" block).

    Step 5 — Run the read-only-implementation check: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` MUST return empty. If it returns anything, find and revert the production-source edit (this plan is forbidden from touching internal/ or cmd/ non-test files).

    Step 6 (merge — only if last to finish) — Run the CLAUDE.md trust-gate sequence at WHOLE-TREE scope as the final phase-close gate, AFTER all 4 plans' worktrees are merged into the phase branch. This is the per-CLAUDE.md non-negotiable verification that catches any signature drift introduced by the 19 new `regression_rel_*_test.go` files across packages (especially relevant because plans 14-01 and 14-04 both add files under `internal/pool/`):

    ```
    gofumpt -l . && go vet ./... && go build ./... && go test -race ./...
    ```

    All 4 commands MUST exit zero. The `go test -race ./...` run is where all 19 in-tree TestRegression_REL_* tests show as `--- SKIP` (zero `--- FAIL`, zero `--- PASS` — the t.Skip ensures none flip). If any test compiles dirty (e.g. a signature drift in a fakeclient seam), this step catches it. golangci-lint and govulncheck are NOT required at Phase 14 close (the t.Skip'd test bodies don't ship to production), but the run-time trio above is mandatory.

    GATE: Phase 14 close requires `needs_investigation_count == 0` per D-10. If any 14-FINDING-<ID>.md ended up with `status: needs-investigation`, the merged ledger blocks the gate and Phase 15/16 cannot plan until those rows are resolved. Surface this in the ledger Summary section explicitly.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md && grep -c "^| P-" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md | grep -q "^6$" && grep -E "^\| Finding ID \| Sev \| REL-\* ID \| Status \| File:line \| Evidence \| Target phase \|" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-01.md && (! ls .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-0{2,3,4}.md >/dev/null 2>&1 || (test -f .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md && grep -c "^| [PHTGCO]-" .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md | awk '$1 >= 23 {exit 0} {exit 1}')) && git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/' | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    14-LEDGER-FRAGMENT-01.md exists with exactly 6 data rows and the D-07 column order. If 14-02/03/04 fragments all exist, 14-VERIFICATION-LEDGER.md exists with the 23 in-scope rows + 12 Low placeholders, a Summary section with by-severity counts, and a needs_investigation_count gate annotation. REQUIREMENTS.md traceability is updated for any false-positives. read-only-implementation diff is empty. If last to finish: whole-tree trust-gate trio (`gofumpt -l . && go vet ./... && go build ./... && go test -race ./...`) all exit zero, with all 19 TestRegression_REL_* tests reported as `--- SKIP`.
  </done>
  <acceptance_criteria>
    14-LEDGER-FRAGMENT-01.md exists per D-07 column spec with all 6 P-* rows. The plan-level read-only-implementation rule holds: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` is empty.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 6 Pool/ACP evidence files exist with D-08 frontmatter + 4 required sections + a final verdict per D-09/D-10/D-11
- All 6 Go test files exist with the verbatim D-12 t.Skip string and the TestRegression_REL_POOL_<NN>_<ShortDescription> naming
- The manual reproducer at tests/reliability/manual/REL-POOL-06-repro.go exists with the Pattern F header block
- Ledger fragment 14-LEDGER-FRAGMENT-01.md exists with D-07 column order and 6 rows
- (If last-to-finish) Master 14-VERIFICATION-LEDGER.md exists with 23 in-scope rows, 12 Low placeholders, Summary section with by-severity counts, and explicit needs_investigation_count annotation
- Per-plan trust gates pass: `gofumpt -l internal/pool internal/session internal/acp` returns empty for new files; `go vet ./internal/pool/... ./internal/session/... ./internal/acp/...` clean; `go build ./...` succeeds; `go test -race ./internal/pool/... ./internal/session/... ./internal/acp/...` passes (new TestRegression_REL_POOL_* tests run as SKIP, none FAIL)
- (If last-to-finish) Whole-tree trust gate per CLAUDE.md non-negotiable contract: `gofumpt -l . && go vet ./... && go build ./... && go test -race ./...` all exit zero. All 19 TestRegression_REL_* tests in-tree report `--- SKIP`; zero `--- FAIL`, zero `--- PASS`. This is the phase-close gate that catches cross-plan signature drift (especially under `internal/pool/` where 14-01 and 14-04 both add files).
- Read-only-implementation rule: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` is empty
</verification>

<success_criteria>
- 6 evidence files, 6 Go test files, 1 manual reproducer, 1 ledger fragment land on disk
- Every new test runs as SKIP (none PASS, none FAIL)
- Every evidence file's frontmatter `status` is one of: confirmed | false-positive | needs-investigation
- D-11 bias respected: ambiguous-but-cite-intact verdicts default to `confirmed`, not "probably-doesn't-trigger"
- D-09 false-positive bar respected: any `false-positive` verdict cites a concrete added guard or a missing original cite
- D-10 escape hatch respected: any `needs-investigation` verdict carries an explicit "what would flip this" note and a target follow-up
- Phase 14 close gate: needs_investigation_count == 0 (this plan contributes 6 rows toward that gate)
</success_criteria>

<output>
Create `.planning/phases/14-verify-reliability-findings/14-01-SUMMARY.md` when done. Summary should:
- List the 6 verdicts (one per finding) by ID and severity
- Report counts of confirmed / false-positive / needs-investigation across the 6 Pool/ACP findings
- Confirm the read-only-implementation diff is empty
- Note whether this plan was the last-to-finish (and therefore wrote the master ledger) or wrote only the fragment
- Surface any false-positive REL-* IDs that should be dropped from Phase 15/16 scope (with backlink to the evidence file)
</output>
