---
phase: 14-verify-reliability-findings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/regression_rel_http_01_test.go
  - internal/adapter/openai/regression_rel_http_02_test.go
  - internal/adapter/openai/regression_rel_http_03_test.go
  - internal/adapter/ollama/regression_rel_http_03_test.go
  - internal/server/regression_rel_http_04_test.go
  - internal/admin/regression_rel_http_05_test.go
  - .planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-H-2.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-H-3.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-H-4.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-H-5.md
  - .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md
autonomous: true
requirements:
  - REL-VERIFY-HIGH
  - REL-VERIFY-MED
  - REL-VERIFY-GATE

must_haves:
  truths:
    - "All 5 HTTP-surface findings (H-1..H-5) have an evidence file at .planning/phases/14-verify-reliability-findings/14-FINDING-<ID>.md with the D-08 frontmatter (finding, severity, rel_id, status, target_phase, verified_at) and the 4 required sections (Review citation, Current-source check, Evidence, Verdict)"
    - "H-1, H-2, H-3 (High) each have a Go regression test following TestRegression_REL_HTTP_<NN>_<ShortDescription> naming with the verbatim D-12 t.Skip(\"REL-HTTP-<NN> (<finding>): regression test — unskip in Phase 15 fix commit\") string as the first line of the test body and a reproducer body below it"
    - "H-3 specifically produces TWO reproducer files — one in internal/adapter/openai/ and one in internal/adapter/ollama/ — because the High finding spans both surfaces"
    - "H-4 and H-5 (Medium) each have a Go regression test with the same shape, but the t.Skip string references Phase 16 instead of Phase 15"
    - "Ledger fragment 14-LEDGER-FRAGMENT-02.md lists 5 rows (H-1..H-5) with D-07 column order: Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase"
    - "If this plan is the last of the 4 parallel plans to complete its fragment, the executor MERGES all 4 fragments into 14-VERIFICATION-LEDGER.md and updates REQUIREMENTS.md traceability for any false-positives. Plan 14-01 is the designated primary merge-owner; this plan executes the merge ONLY if 14-01 has not yet completed and fragments 01/03/04 all exist on disk."
    - "go build ./... passes; go vet ./internal/server/... ./internal/adapter/openai/... ./internal/adapter/ollama/... ./internal/admin/... clean; gofumpt -l returns empty for new test files; go test -race against affected packages passes (all new tests run as SKIP)"
    - "Read-only-implementation rule: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` empty at plan close"
  artifacts:
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md"
      provides: "H-1 (REL-HTTP-01, High) verdict + cite + pointer to TestRegression_REL_HTTP_01_ShutdownBlocksOnAdminSSE"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-H-2.md"
      provides: "H-2 (REL-HTTP-02, High) verdict + cite + pointer to TestRegression_REL_HTTP_02_IdleTimeoutReturnsHungWorker"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-H-3.md"
      provides: "H-3 (REL-HTTP-03, High) verdict + cite + pointers to both TestRegression_REL_HTTP_03_* files"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-H-4.md"
      provides: "H-4 (REL-HTTP-04, Medium) verdict + cite + pointer to TestRegression_REL_HTTP_04_BodyReadDeadlineMissing"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-H-5.md"
      provides: "H-5 (REL-HTTP-05, Medium) verdict + cite + pointer to TestRegression_REL_HTTP_05_AdminTailerLineCapBypass"
    - path: "internal/server/regression_rel_http_01_test.go"
      provides: "TestRegression_REL_HTTP_01_ShutdownBlocksOnAdminSSE"
      contains: "t.Skip(\"REL-HTTP-01 (H-1): regression test"
    - path: "internal/adapter/openai/regression_rel_http_02_test.go"
      provides: "TestRegression_REL_HTTP_02_IdleTimeoutReturnsHungWorker"
      contains: "t.Skip(\"REL-HTTP-02 (H-2): regression test"
    - path: "internal/adapter/openai/regression_rel_http_03_test.go"
      provides: "TestRegression_REL_HTTP_03_MidStreamTruncationIsSilent (OpenAI variant)"
      contains: "t.Skip(\"REL-HTTP-03 (H-3): regression test"
    - path: "internal/adapter/ollama/regression_rel_http_03_test.go"
      provides: "TestRegression_REL_HTTP_03_MidStreamTruncationIsSilent (Ollama variant)"
      contains: "t.Skip(\"REL-HTTP-03 (H-3): regression test"
    - path: "internal/server/regression_rel_http_04_test.go"
      provides: "TestRegression_REL_HTTP_04_BodyReadDeadlineMissing"
      contains: "t.Skip(\"REL-HTTP-04 (H-4): regression test"
    - path: "internal/admin/regression_rel_http_05_test.go"
      provides: "TestRegression_REL_HTTP_05_AdminTailerLineCapBypass"
      contains: "t.Skip(\"REL-HTTP-05 (H-5): regression test"
    - path: ".planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md"
      provides: "5-row table (H-1..H-5) with D-07 column order"
  key_links:
    - from: "internal/server/regression_rel_http_01_test.go"
      to: "internal/server/server.go:346-383 + internal/admin/sse.go:167-203"
      via: "long-lived admin log-tail SSE held open during srv.Shutdown(ctx); assert shutdown returns < 5s"
      pattern: "TestRegression_REL_HTTP_01_ShutdownBlocksOnAdminSSE"
    - from: "internal/adapter/openai/regression_rel_http_02_test.go"
      to: "internal/adapter/openai/sse.go:460-462, :482-484"
      via: "fakeRunHandle + cancelled ctx, assert Cancel(sid) called before slot return"
      pattern: "TestRegression_REL_HTTP_02_IdleTimeoutReturnsHungWorker"
    - from: "internal/adapter/openai/regression_rel_http_03_test.go + internal/adapter/ollama/regression_rel_http_03_test.go"
      to: "internal/adapter/openai/sse.go:543-557 + internal/adapter/ollama/ndjson.go:541-549"
      via: "fakeStream.result.err = errors.New('worker died'); OpenAI asserts data:{error} frame + [DONE]; Ollama asserts done:true,done_reason:error"
      pattern: "TestRegression_REL_HTTP_03_MidStreamTruncationIsSilent"
    - from: "internal/server/regression_rel_http_04_test.go"
      to: "internal/server/server.go:347-360"
      via: "slow-write request body via stalled io.Reader; assert handler returns within bounded read deadline"
      pattern: "TestRegression_REL_HTTP_04_BodyReadDeadlineMissing"
    - from: "internal/admin/regression_rel_http_05_test.go"
      to: "internal/admin/tail.go:393-427"
      via: "multi-MB newline-terminated line via RingBuffer + Tailer; assert truncation at TailerMaxLineBytes on subscriber channel and SSE stream"
      pattern: "TestRegression_REL_HTTP_05_AdminTailerLineCapBypass"
---

<objective>
Verify the 5 HTTP-surface reliability findings (H-1 High, H-2 High, H-3 High, H-4 Medium, H-5 Medium) from `docs/reviews/2026-06-11-reliability-review.md` against current `main` source. Produce 5 per-finding evidence files, 6 Go regression-test files (H-3 ships two — OpenAI + Ollama — because the finding spans both surfaces), and ledger fragment 02. If this plan is the last of the 4 parallel plans to finish AND plan 14-01 has not yet merged, do the merge.

Purpose: Phase 15 fixes H-1/H-2/H-3 (REL-HTTP-01/02/03 — High) and Phase 16 fixes H-4/H-5 (REL-HTTP-04/05 — Medium). Phase 14 confirms each failure path still exists in current source. Read-only-implementation rule applies — no production source edits.

Output:
- 5 evidence files at `.planning/phases/14-verify-reliability-findings/14-FINDING-{H-1,H-2,H-3,H-4,H-5}.md`
- 6 Go test files (H-3 has both OpenAI and Ollama variants) with verbatim D-12 t.Skip strings and the TestRegression_REL_HTTP_<NN>_<ShortDescription> naming
- Ledger fragment at `14-LEDGER-FRAGMENT-02.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verify-reliability-findings/14-CONTEXT.md
@.planning/phases/14-verify-reliability-findings/14-PATTERNS.md
@docs/reviews/2026-06-11-reliability-review.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Verify H-1 (High, REL-HTTP-01) — shutdown blocks on admin SSE</name>
  <files>
    internal/server/regression_rel_http_01_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the H-1 entry — full failure scenario),
    internal/server/server.go (lines 346-383 cited — read the Shutdown handler block),
    internal/admin/sse.go (lines 167-203 cited — the log-tail SSE handler),
    internal/server/server_test.go (lines 22-30 for newTestServer helper),
    internal/admin/sse_test.go (whole file, especially TestServeSSE_* patterns)
  </read_first>
  <behavior>
    - srv.Shutdown(ctx) blocks for the full grace period (30s default) while a long-lived admin log-tail SSE keeps a connection open
    - Pre-fix reproducer asserts Shutdown takes > 5s; post-fix asserts < 1s
  </behavior>
  <action>
    Step 1 — Verify cite at internal/server/server.go:346-383 and internal/admin/sse.go:167-203. Read the Shutdown handler. Determine whether it currently waits for SSE drain or unwinds long-lived SSE during grace. Record current file:line.

    Step 2 — Create internal/server/regression_rel_http_01_test.go. Package `server_test`. Function: `TestRegression_REL_HTTP_01_ShutdownBlocksOnAdminSSE`. First line: `t.Skip("REL-HTTP-01 (H-1): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: construct via newTestServer(t), wrap with httptest.NewServer, open a long-lived GET to /admin/log-tail via http.Client with no read deadline, then call srv.Shutdown(ctx) with a 10s deadline from a goroutine. Measure elapsed time via time.Since(start); assert it exceeds 5s (pre-fix observable). Add `defer goleak.VerifyNone(t)` per Pattern A.

    Step 3 — Write 14-FINDING-H-1.md with frontmatter (finding: H-1, severity: H, rel_id: REL-HTTP-01, status, target_phase: 15, verified_at: 2026-06-11) and the four sections per D-08. Verdict per D-11 bias.
  </action>
  <verify>
    <automated>cd internal/server && go test -race -run TestRegression_REL_HTTP_01 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/server/... && gofumpt -l internal/server/regression_rel_http_01_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md && grep -c "REL-HTTP-01" .planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md</automated>
  </verify>
  <done>
    Test file exists, runs as SKIP, contains exact D-12 t.Skip string. Evidence file has frontmatter + 4 sections. go vet clean, gofumpt clean.
  </done>
  <acceptance_criteria>
    14-FINDING-H-1.md and internal/server/regression_rel_http_01_test.go exist per D-08 + D-12 specs.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Verify H-2 (High, REL-HTTP-02) — idle-timeout returns hung worker</name>
  <files>
    internal/adapter/openai/regression_rel_http_02_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-2.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the H-2 entry),
    internal/adapter/openai/sse.go (lines 460-462, 482-484 cited — the idle-timeout return path),
    internal/adapter/openai/sse_test.go (lines 25-54 — TestSSE_CtxCancel template; lines 1-80 for fakeRunHandle/fakeStream/fakePoolClient shapes)
  </read_first>
  <behavior>
    - On idle-timeout, slot returns to the free pool WITHOUT calling Cancel(sid) on the underlying kiro-cli session
    - Subsequent request acquires the same slot whose worker is mid-abandoned-prompt
    - Pre-fix reproducer asserts Cancel(sid) was NOT called before slot return; post-fix asserts it WAS called
  </behavior>
  <action>
    Step 1 — Verify cite at internal/adapter/openai/sse.go:460-462, :482-484. Determine whether the idle-timeout path currently calls an explicit session/cancel before slot return. Record current file:line.

    Step 2 — Create internal/adapter/openai/regression_rel_http_02_test.go. Package `openai_test`. Function: `TestRegression_REL_HTTP_02_IdleTimeoutReturnsHungWorker`. First line: `t.Skip("REL-HTTP-02 (H-2): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: adapt TestSSE_CtxCancel — fakeRunHandle with unbuffered chan canonical.Chunk (never closed), drive runSSEEmitter with a ctx that triggers the idle-timeout path (use a time.AfterFunc or a fake idle deadline), then inspect the fakePoolClient's recorded cancel calls. Pre-fix observable: cancel call list does NOT contain the session_id. Use `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-H-2.md with the D-08 frontmatter and four sections. Verdict per D-11 bias.
  </action>
  <verify>
    <automated>cd internal/adapter/openai && go test -race -run TestRegression_REL_HTTP_02 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../../.. && go vet ./internal/adapter/openai/... && gofumpt -l internal/adapter/openai/regression_rel_http_02_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-H-2.md && grep -c "REL-HTTP-02" .planning/phases/14-verify-reliability-findings/14-FINDING-H-2.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete with verdict per D-11. go vet + gofumpt clean.
  </done>
  <acceptance_criteria>
    14-FINDING-H-2.md and internal/adapter/openai/regression_rel_http_02_test.go exist per spec.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Verify H-3 (High, REL-HTTP-03) — mid-stream truncation is silent on BOTH OpenAI + Ollama</name>
  <files>
    internal/adapter/openai/regression_rel_http_03_test.go,
    internal/adapter/ollama/regression_rel_http_03_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-3.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the H-3 entry — covers BOTH surfaces),
    internal/adapter/openai/sse.go (lines 543-557 cited),
    internal/adapter/ollama/ndjson.go (lines 541-549 cited),
    internal/adapter/openai/sse_test.go (fakeStream patterns),
    internal/adapter/ollama/ndjson_test.go (lines 25-60 for fakeRunHandle shape)
  </read_first>
  <behavior>
    - kiro-cli dies mid-stream → fakeStream final result carries an error → emitter silently terminates without an error frame
    - OpenAI pre-fix: stream just ends, no `data: {"error":...}` frame, no `[DONE]` — looks like a clean half-finished answer
    - Ollama pre-fix: same — no `{"done": true, "done_reason": "error"}` envelope
    - Post-fix: explicit error frames per surface; this reproducer asserts the pre-fix observable on both
  </behavior>
  <action>
    Step 1 — Verify cite at internal/adapter/openai/sse.go:543-557 and internal/adapter/ollama/ndjson.go:541-549. Determine whether surface-native terminal error frames are emitted today. Record current file:line for BOTH surfaces.

    Step 2a — Create internal/adapter/openai/regression_rel_http_03_test.go. Package `openai_test`. Function: `TestRegression_REL_HTTP_03_MidStreamTruncationIsSilent`. First line: `t.Skip("REL-HTTP-03 (H-3): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: fakeRunHandle with a fakeStream whose final result carries `err: errors.New("worker died")`, drive runSSEEmitter via httptest.NewRecorder. Read rec.Body. Pre-fix observable: body does NOT contain `data: {"error":` and does NOT end with `data: [DONE]\n\n`. Use `defer goleak.VerifyNone(t)`.

    Step 2b — Create internal/adapter/ollama/regression_rel_http_03_test.go. Package `ollama_test`. Same function name `TestRegression_REL_HTTP_03_MidStreamTruncationIsSilent`. Same verbatim t.Skip string. Reproducer body: fakeStream with err set, drive the NDJSON emitter, read rec.Body, assert body does NOT contain `"done":true,"done_reason":"error"` (pre-fix observable). Use `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-H-3.md with the D-08 frontmatter and four sections. "Evidence" section must point at BOTH test files explicitly. Verdict per D-11 bias.

    H-3 produces TWO test files because the High finding spans two surfaces. Both have identical function names but live in different packages; they compile independently and Phase 15 unskips both in one atomic commit per surface fix.
  </action>
  <verify>
    <automated>cd internal/adapter/openai && go test -race -run TestRegression_REL_HTTP_03 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../../.. && cd internal/adapter/ollama && go test -race -run TestRegression_REL_HTTP_03 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../../.. && go vet ./internal/adapter/openai/... ./internal/adapter/ollama/... && gofumpt -l internal/adapter/openai/regression_rel_http_03_test.go internal/adapter/ollama/regression_rel_http_03_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-H-3.md && grep -c "REL-HTTP-03" .planning/phases/14-verify-reliability-findings/14-FINDING-H-3.md</automated>
  </verify>
  <done>
    Both test files exist, both run as SKIP, both contain exact D-12 t.Skip string. Evidence file points at both. go vet + gofumpt clean across both adapter packages.
  </done>
  <acceptance_criteria>
    14-FINDING-H-3.md exists and Evidence section references BOTH internal/adapter/openai/regression_rel_http_03_test.go AND internal/adapter/ollama/regression_rel_http_03_test.go.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Verify H-4 (Medium, REL-HTTP-04) — no body-read deadline</name>
  <files>
    internal/server/regression_rel_http_04_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-4.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the H-4 entry),
    internal/server/server.go (lines 347-360 cited — the http.Server config block),
    internal/server/server_test.go (newTestServer helper + httptest.NewRequestWithContext shape lines 38-41),
    internal/server/server_admin_test.go (request-shape patterns)
  </read_first>
  <behavior>
    - http.Server has no ReadTimeout or per-request body-read deadline
    - A client that stops sending the body mid-upload parks for hours via TCP keepalive
    - Pre-fix reproducer asserts the handler does NOT return within a bounded deadline
  </behavior>
  <action>
    Step 1 — Verify cite at internal/server/server.go:347-360. Read the http.Server construction. Determine whether ReadTimeout, ReadHeaderTimeout, or a per-request body-read deadline is set. Record current file:line.

    Step 2 — Create internal/server/regression_rel_http_04_test.go. Package `server_test`. Function: `TestRegression_REL_HTTP_04_BodyReadDeadlineMissing`. First line: `t.Skip("REL-HTTP-04 (H-4): regression test — unskip in Phase 16 fix commit")` verbatim (Phase 16, Medium). Reproducer body: build a stalled io.Reader (e.g. a struct whose Read blocks on an unsignaled channel), wrap as the http.Request body, drive POST /v1/chat/completions through newTestServer's ServeHTTP using httptest.NewRecorder + httptest.NewRequestWithContext, with a goroutine watchdog at 3s asserting the handler has NOT returned (pre-fix observable). Use `defer goleak.VerifyNone(t)`.

    Step 3 — Write 14-FINDING-H-4.md with frontmatter (finding: H-4, severity: M, rel_id: REL-HTTP-04, status, target_phase: 16, verified_at: 2026-06-11) and the four sections. Verdict per D-11.
  </action>
  <verify>
    <automated>cd internal/server && go test -race -run TestRegression_REL_HTTP_04 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/server/... && gofumpt -l internal/server/regression_rel_http_04_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-H-4.md && grep -c "REL-HTTP-04" .planning/phases/14-verify-reliability-findings/14-FINDING-H-4.md</automated>
  </verify>
  <done>
    Test runs as SKIP with Phase 16 reference. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-H-4.md and internal/server/regression_rel_http_04_test.go exist per spec with Phase 16 reference in the Skip string.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: Verify H-5 (Medium, REL-HTTP-05) — admin tailer line-cap bypass</name>
  <files>
    internal/admin/regression_rel_http_05_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-5.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the H-5 entry),
    internal/admin/tail.go (lines 393-427 cited — the line-handling code),
    internal/admin/tail_test.go (lines 38-54 for waitLines helper + RingBuffer/Tailer patterns),
    internal/admin/sse_test.go (subscriber → SSE flow)
  </read_first>
  <behavior>
    - A multi-MB newline-terminated single line bypasses TailerMaxLineBytes when written into the RingBuffer
    - The subscriber channel emits the full line; the SSE stream writes it unbounded
    - Memory growth is unbounded — pre-fix observable is "delivered line length > TailerMaxLineBytes"
  </behavior>
  <action>
    Step 1 — Verify cite at internal/admin/tail.go:393-427. Determine whether per-line truncation at TailerMaxLineBytes is enforced for newline-terminated input. Record current file:line.

    Step 2 — Create internal/admin/regression_rel_http_05_test.go. Package `admin_test`. Function: `TestRegression_REL_HTTP_05_AdminTailerLineCapBypass`. First line: `t.Skip("REL-HTTP-05 (H-5): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: construct a RingBuffer + Tailer per the tail_test.go patterns, subscribe via the test helper, append a single line of length 5*TailerMaxLineBytes terminated by `\n`, then call waitLines(ch, 1, 2*time.Second). Pre-fix observable: `len(received[0]) > TailerMaxLineBytes`. Use `defer goleak.VerifyNone(t)` if the package has goleak.VerifyTestMain (check internal/admin/testmain_test.go presence — if absent, skip the per-test defer).

    Step 3 — Write 14-FINDING-H-5.md with frontmatter (finding: H-5, severity: M, rel_id: REL-HTTP-05, status, target_phase: 16, verified_at: 2026-06-11) and the four sections.
  </action>
  <verify>
    <automated>cd internal/admin && go test -race -run TestRegression_REL_HTTP_05 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 && cd ../.. && go vet ./internal/admin/... && gofumpt -l internal/admin/regression_rel_http_05_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-H-5.md && grep -c "REL-HTTP-05" .planning/phases/14-verify-reliability-findings/14-FINDING-H-5.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete with verdict.
  </done>
  <acceptance_criteria>
    14-FINDING-H-5.md and internal/admin/regression_rel_http_05_test.go exist per spec.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write ledger fragment 14-LEDGER-FRAGMENT-02.md + (if 14-01 hasn't merged and this is last) merge master ledger</name>
  <files>
    .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md,
    .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (re-read each H-1..H-5 entry for exact cites),
    .planning/phases/14-verify-reliability-findings/14-FINDING-H-1.md through 14-FINDING-H-5.md (verdicts written in tasks 1-5),
    .planning/REQUIREMENTS.md §Traceability
  </read_first>
  <action>
    Step 1 — Write 14-LEDGER-FRAGMENT-02.md. Single markdown table with exactly 5 data rows (H-1..H-5) + header. Column order per D-07: `| Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase |`. Status sourced from each evidence file's frontmatter `status`. File:line is the current-source cite (not the original review cite). Evidence is the relative path `14-FINDING-<ID>.md`. Target phase: 15 for H-1/H-2/H-3, 16 for H-4/H-5 (or `drop` if any false-positive).

    Step 2 — Check fragments 01, 03, 04 existence (`ls .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-{01,03,04}.md`). If 14-VERIFICATION-LEDGER.md ALREADY EXISTS, plan 14-01 (or another plan) already merged — STOP. If all 3 sibling fragments exist AND master ledger does NOT, this plan is the last-to-finish and 14-01 has not yet executed its merge step: proceed to Step 3.

    Step 3 (merge — only if conditions in Step 2 met) — Same merge procedure as plan 14-01 Task 7 Step 3: write `14-VERIFICATION-LEDGER.md` with Summary section (by-severity confirmed/false-positive/needs-investigation counts), 23 in-scope rows in canonical order (P-1..P-6, H-1..H-5, T-1..T-7, G-1, C-1..C-3, O-1), 12 Low placeholder rows marked `n/a (deferred to v1.10)`. Annotate the Summary with `needs_investigation_count` and flag whether the D-10 close gate (count == 0) is met.

    Step 4 (merge — only if conditions in Step 2 met) — Update .planning/REQUIREMENTS.md §Traceability: for each `false-positive` REL-*, change row to `status: dropped (FP — see 14-FINDING-<ID>.md)`.

    Step 5 — Read-only-implementation check: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` MUST return empty.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md && grep -c "^| H-" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md | grep -q "^5$" && grep -E "^\| Finding ID \| Sev \| REL-\* ID \| Status \| File:line \| Evidence \| Target phase \|" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-02.md && git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/' | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    14-LEDGER-FRAGMENT-02.md exists with 5 H-* rows and D-07 column order. Master ledger and REQUIREMENTS.md update only happen if this plan is genuinely last-to-finish (sibling fragments present, master absent). Read-only-implementation diff empty.
  </done>
  <acceptance_criteria>
    14-LEDGER-FRAGMENT-02.md exists with the D-07 spec. `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` is empty.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 5 HTTP evidence files exist with D-08 frontmatter + 4 required sections + verdict per D-09/D-10/D-11
- All 6 Go test files (H-3 is two files) exist with verbatim D-12 t.Skip strings and TestRegression_REL_HTTP_<NN>_<ShortDescription> naming
- Ledger fragment 02 exists with D-07 column order and 5 rows
- Trust gates: gofumpt clean, go vet clean for internal/server/, internal/admin/, internal/adapter/openai/, internal/adapter/ollama/, go test -race against those packages passes (new tests SKIP)
- Read-only-implementation rule: production-source diff empty
</verification>

<success_criteria>
- 5 evidence files, 6 Go test files, 1 ledger fragment land on disk
- Every new test runs as SKIP (none PASS, none FAIL)
- D-11 bias respected; D-09 false-positive bar respected; D-10 escape hatch respected
- This plan contributes 5 rows toward the Phase 14 close gate (needs_investigation_count == 0)
</success_criteria>

<output>
Create `.planning/phases/14-verify-reliability-findings/14-02-SUMMARY.md` when done. Summary should:
- List the 5 verdicts by ID and severity
- Report confirmed / false-positive / needs-investigation counts
- Confirm read-only-implementation diff is empty
- Note whether this plan executed the master-ledger merge (per Task 6 Step 2 conditions) or just wrote the fragment
- Surface any false-positive REL-* IDs that should drop from Phase 15/16 scope
</output>
