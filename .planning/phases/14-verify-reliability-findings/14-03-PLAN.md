---
phase: 14-verify-reliability-findings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/otto-tray/regression_rel_tray_01_test.go
  - cmd/otto-tray/regression_rel_tray_02_test.go
  - cmd/otto-tray/regression_rel_tray_03_test.go
  - cmd/otto-tray/regression_rel_tray_04_test.go
  - cmd/otto-tray/regression_rel_tray_05_test.go
  - cmd/otto-tray/regression_rel_tray_06_test.go
  - cmd/otto-tray/regression_rel_tray_07_test.go
  - tests/reliability/manual/REL-TRAY-02-repro.ps1
  - tests/reliability/manual/REL-TRAY-03-repro.sh
  - tests/reliability/manual/REL-TRAY-06-repro.ps1
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-2.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-3.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-4.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-5.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-6.md
  - .planning/phases/14-verify-reliability-findings/14-FINDING-T-7.md
  - .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md
autonomous: true
requirements:
  - REL-VERIFY-HIGH
  - REL-VERIFY-MED
  - REL-VERIFY-GATE

must_haves:
  truths:
    - "All 7 Tray/wrapper findings (T-1..T-7) have an evidence file at .planning/phases/14-verify-reliability-findings/14-FINDING-<ID>.md with D-08 frontmatter and the 4 required sections (Review citation, Current-source check, Evidence, Verdict)"
    - "T-1 (High) has a Go regression test in cmd/otto-tray/ following TestRegression_REL_TRAY_01_<ShortDescription> naming with the verbatim D-12 t.Skip(\"REL-TRAY-01 (T-1): regression test — unskip in Phase 15 fix commit\") string"
    - "T-2 (High, Windows PowerShell): BOTH a Go stub at cmd/otto-tray/regression_rel_tray_02_test.go pointing at the manual script AND a runnable manual reproducer at tests/reliability/manual/REL-TRAY-02-repro.ps1 with Pattern F header"
    - "T-3 (High, macOS GUI): BOTH a Go stub at cmd/otto-tray/regression_rel_tray_03_test.go pointing at the manual script AND a runnable manual reproducer at tests/reliability/manual/REL-TRAY-03-repro.sh with Pattern F header"
    - "T-4 (Medium) has a Go regression test in cmd/otto-tray/ with the same D-12 shape, Skip string referencing Phase 16"
    - "T-5 (Medium) has a Go regression test in cmd/otto-tray/ — direct template from poller_test.go's TestPoller_EmitsStateOnEachTick"
    - "T-6 (Medium, Windows PowerShell): BOTH a Go stub at cmd/otto-tray/regression_rel_tray_06_test.go pointing at the manual script AND a runnable manual reproducer at tests/reliability/manual/REL-TRAY-06-repro.ps1 with Pattern F header"
    - "T-7 (Medium) has a Go regression test in cmd/otto-tray/ for support-bundle size/time bounds"
    - "All 7 Go test files carry the `//go:build darwin || windows` build tag per Pattern G — Linux CI skips tray tests by build constraint"
    - "Ledger fragment 14-LEDGER-FRAGMENT-03.md lists 7 rows (T-1..T-7) with D-07 column order"
    - "If this plan is the last of the 4 parallel plans to complete AND plan 14-01 has not yet merged AND fragments 01/02/04 all exist, this plan executes the master-ledger merge"
    - "go build ./... passes (tray build-tag protects Linux CI); on darwin/windows: gofumpt -l cmd/otto-tray returns empty for new files, go vet ./cmd/otto-tray/... clean, go test -race ./cmd/otto-tray/... passes (all TestRegression_REL_TRAY_* run as SKIP)"
    - "Read-only-implementation rule: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` empty at plan close"
  artifacts:
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md"
      provides: "T-1 (REL-TRAY-01, High) verdict + cite + pointer to TestRegression_REL_TRAY_01_PIDIdentityUnchecked"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-2.md"
      provides: "T-2 (REL-TRAY-02, High) verdict + cite + pointers to stub test AND tests/reliability/manual/REL-TRAY-02-repro.ps1"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-3.md"
      provides: "T-3 (REL-TRAY-03, High) verdict + cite + pointers to stub test AND tests/reliability/manual/REL-TRAY-03-repro.sh"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-4.md"
      provides: "T-4 (REL-TRAY-04, Medium) verdict + cite + pointer to TestRegression_REL_TRAY_04_WindowsNotifyBlocking"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-5.md"
      provides: "T-5 (REL-TRAY-05, Medium) verdict + cite + pointer to TestRegression_REL_TRAY_05_DegradedWhenPoolWedged"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-6.md"
      provides: "T-6 (REL-TRAY-06, Medium) verdict + cite + pointers to stub test AND tests/reliability/manual/REL-TRAY-06-repro.ps1"
    - path: ".planning/phases/14-verify-reliability-findings/14-FINDING-T-7.md"
      provides: "T-7 (REL-TRAY-07, Medium) verdict + cite + pointer to TestRegression_REL_TRAY_07_SupportBundleBounds"
    - path: "cmd/otto-tray/regression_rel_tray_01_test.go"
      provides: "TestRegression_REL_TRAY_01_PIDIdentityUnchecked"
      contains: "t.Skip(\"REL-TRAY-01 (T-1): regression test"
    - path: "cmd/otto-tray/regression_rel_tray_02_test.go"
      provides: "Stub for T-2 pointing at REL-TRAY-02-repro.ps1"
      contains: "tests/reliability/manual/REL-TRAY-02-repro.ps1"
    - path: "cmd/otto-tray/regression_rel_tray_03_test.go"
      provides: "Stub for T-3 pointing at REL-TRAY-03-repro.sh"
      contains: "tests/reliability/manual/REL-TRAY-03-repro.sh"
    - path: "cmd/otto-tray/regression_rel_tray_04_test.go"
      provides: "TestRegression_REL_TRAY_04_WindowsNotifyBlocking"
      contains: "t.Skip(\"REL-TRAY-04 (T-4): regression test"
    - path: "cmd/otto-tray/regression_rel_tray_05_test.go"
      provides: "TestRegression_REL_TRAY_05_DegradedWhenPoolWedged"
      contains: "t.Skip(\"REL-TRAY-05 (T-5): regression test"
    - path: "cmd/otto-tray/regression_rel_tray_06_test.go"
      provides: "Stub for T-6 pointing at REL-TRAY-06-repro.ps1"
      contains: "tests/reliability/manual/REL-TRAY-06-repro.ps1"
    - path: "cmd/otto-tray/regression_rel_tray_07_test.go"
      provides: "TestRegression_REL_TRAY_07_SupportBundleBounds"
      contains: "t.Skip(\"REL-TRAY-07 (T-7): regression test"
    - path: "tests/reliability/manual/REL-TRAY-02-repro.ps1"
      provides: "Windows PowerShell reproducer for support-bundle exit-1-on-gateway-down failure"
    - path: "tests/reliability/manual/REL-TRAY-03-repro.sh"
      provides: "macOS shell reproducer for silent gateway death + tray no-op observation"
    - path: "tests/reliability/manual/REL-TRAY-06-repro.ps1"
      provides: "Windows PowerShell reproducer for bundle-path stdout pollution"
    - path: ".planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md"
      provides: "7-row table (T-1..T-7) with D-07 column order"
  key_links:
    - from: "cmd/otto-tray/regression_rel_tray_01_test.go"
      to: "scripts/otto-gw:553-561, :776-786 + scripts/otto-gw.ps1:388-394, :560-566 + cmd/otto-tray/tray.go:144-151 + cmd/otto-tray/fsm.go:40-50"
      via: "pidfile_test.go pattern extended — write pidfile with os.Getpid(), assert PID-trust path treats test binary as 'the gateway' (pre-fix observable)"
      pattern: "TestRegression_REL_TRAY_01_PIDIdentityUnchecked"
    - from: "tests/reliability/manual/REL-TRAY-02-repro.ps1"
      to: "scripts/otto-gw.ps1:584-593, :1464"
      via: "drive `scripts/otto-gw.ps1 support` with gateway down; pre-fix: Get-GatewayStatus exit 1 aborts; post-fix: bundle completes with unreachable: sentinel"
      pattern: "Pattern F header + scripts/otto-gw.ps1 support invocation"
    - from: "tests/reliability/manual/REL-TRAY-03-repro.sh"
      to: "cmd/otto-tray/tray.go:199-201, :74-75 + cmd/otto-tray/uihelpers_darwin.go:43-58"
      via: "kill gateway out from under running tray; observe icon/tooltip in real GUI session"
      pattern: "Pattern F header + macOS GUI observation steps"
    - from: "cmd/otto-tray/regression_rel_tray_04_test.go"
      to: "cmd/otto-tray/tray.go:199-201 + cmd/otto-tray/uihelpers_windows.go:50-68"
      via: "inject fake notify() that blocks 30s; assert applyState returns < 100ms (pre-fix: blocks)"
      pattern: "TestRegression_REL_TRAY_04_WindowsNotifyBlocking"
    - from: "cmd/otto-tray/regression_rel_tray_05_test.go"
      to: "cmd/otto-tray/tray.go:153 + cmd/otto-tray/fsm.go:52-54 + internal/server/health.go:70-77"
      via: "fakeProbe with PoolAlive=0, PoolSize=4, PoolBusy=4; assert StateDegraded emitted (post-fix); pre-fix: StateRunning"
      pattern: "TestRegression_REL_TRAY_05_DegradedWhenPoolWedged (poller_test.go direct template)"
    - from: "tests/reliability/manual/REL-TRAY-06-repro.ps1"
      to: "cmd/otto-tray/tray.go:296-299 + scripts/otto-gw.ps1:321, :330, :1644"
      via: "wrapper writes 'loaded env file: ...' to stdout before bundle path; pre-fix: tray opens first stdout line; post-fix: tray parses last non-empty line"
      pattern: "Pattern F header + Windows wrapper stdout capture"
    - from: "cmd/otto-tray/regression_rel_tray_07_test.go"
      to: "scripts/otto-gw:1864-1873, :1957-1989 + scripts/otto-gw.ps1:1489-1494, :1586-1596 + cmd/otto-tray/runner.go:29-33"
      via: "drive wrapper with large fake log dir; assert bundle under --max-mb cap, completes within timeout, staging cleaned on timeout"
      pattern: "TestRegression_REL_TRAY_07_SupportBundleBounds"
---

<objective>
Verify the 7 Tray/wrapper reliability findings (T-1 High, T-2 High, T-3 High, T-4 Medium, T-5 Medium, T-6 Medium, T-7 Medium) from `docs/reviews/2026-06-11-reliability-review.md` against current `main` source. Produce 7 per-finding evidence files, 7 Go regression-test files (3 of which are skipped stubs pointing at manual scripts because the failure modes are Windows/PowerShell-only or macOS-GUI-only), 3 manual reproducer scripts, and ledger fragment 03.

Purpose: Phase 15 fixes T-1/T-2/T-3 (REL-TRAY-01/02/03 — High) and Phase 16 fixes T-4/T-5/T-6/T-7 (REL-TRAY-04/05/06/07 — Medium). Phase 14 confirms each failure path still exists in current source. Read-only-implementation rule applies — no production source edits to `cmd/otto-tray/` non-test files or `scripts/otto-gw*`.

Output:
- 7 evidence files at `.planning/phases/14-verify-reliability-findings/14-FINDING-{T-1..T-7}.md`
- 7 Go test files in `cmd/otto-tray/` with build tag `//go:build darwin || windows` per Pattern G; T-2/T-3/T-6 are skipped stubs pointing at manual scripts (D-03)
- 3 manual reproducer scripts in `tests/reliability/manual/` with Pattern F header
- Ledger fragment at `14-LEDGER-FRAGMENT-03.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verify-reliability-findings/14-CONTEXT.md
@.planning/phases/14-verify-reliability-findings/14-PATTERNS.md
@docs/reviews/2026-06-11-reliability-review.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Verify T-1 (High, REL-TRAY-01) — PID identity unchecked</name>
  <files>
    cmd/otto-tray/regression_rel_tray_01_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-1 entry),
    scripts/otto-gw (lines 553-561, 776-786 cited — bash wrapper Stop/Restart),
    scripts/otto-gw.ps1 (lines 388-394, 560-566 cited — PowerShell wrapper Stop/Restart),
    cmd/otto-tray/tray.go (lines 144-151 cited — tray probe),
    cmd/otto-tray/fsm.go (lines 40-50 cited — state classification),
    cmd/otto-tray/pidfile_test.go (whole file — PID round-trip + processAlive patterns)
  </read_first>
  <behavior>
    - Wrapper Stop/Restart and tray probe trust a PID from the pidfile without verifying the process is actually the gateway
    - A recycled PID can be killed as if it were the gateway (catastrophic)
    - Pre-fix reproducer: write pidfile with os.Getpid() (the test binary, NOT the gateway); assert PID-trust path returns "alive: yes, identity: trusted"
  </behavior>
  <action>
    Step 1 — Verify cite at scripts/otto-gw:553-561, :776-786; scripts/otto-gw.ps1:388-394, :560-566; cmd/otto-tray/tray.go:144-151; cmd/otto-tray/fsm.go:40-50. For each, determine whether a process-name or cmdline check exists today. Record current file:line.

    Step 2 — Create cmd/otto-tray/regression_rel_tray_01_test.go with the build tag header on line 1: `//go:build darwin || windows` (Pattern G). Package `main` (per cmd/otto-tray convention — check fsm_test.go and pidfile_test.go for the package name). Function: `TestRegression_REL_TRAY_01_PIDIdentityUnchecked`. First line of test body: `t.Skip("REL-TRAY-01 (T-1): regression test — unskip in Phase 15 fix commit")` verbatim. Reproducer body: write a pidfile to t.TempDir() containing strconv.Itoa(os.Getpid()) (the test binary's PID), call whatever the current trust path is (e.g. tray.readPIDFile + processAlive + the new-not-yet-existing identity check), assert that the result mis-identifies the test binary as the gateway (pre-fix observable).

    Step 3 — Write 14-FINDING-T-1.md with frontmatter (finding: T-1, severity: H, rel_id: REL-TRAY-01, status, target_phase: 15, verified_at: 2026-06-11) and four sections per D-08. Verdict per D-11.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_01 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux (build tag)") && cd ../.. && go vet ./cmd/otto-tray/... 2>&1 | grep -v "no Go files" || true && gofumpt -l cmd/otto-tray/regression_rel_tray_01_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md && grep -c "REL-TRAY-01" .planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md && head -1 cmd/otto-tray/regression_rel_tray_01_test.go | grep -E "^//go:build darwin \|\| windows"</automated>
  </verify>
  <done>
    Test file exists with Pattern G build tag, runs as SKIP on darwin (Linux skips by build constraint), contains exact D-12 t.Skip string. Evidence file has frontmatter + 4 sections. gofumpt clean.
  </done>
  <acceptance_criteria>
    14-FINDING-T-1.md and cmd/otto-tray/regression_rel_tray_01_test.go exist per spec. Build tag `//go:build darwin || windows` is the first line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify T-2 (High, REL-TRAY-02) — Windows support-bundle exit 1 (manual reproducer)</name>
  <files>
    cmd/otto-tray/regression_rel_tray_02_test.go,
    tests/reliability/manual/REL-TRAY-02-repro.ps1,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-2.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-2 entry),
    scripts/otto-gw.ps1 (lines 584-593, 1464 cited — Get-GatewayStatus + support verb invocation),
    tests/scripts/test-support-bundle.ps1 (script shape — closest in-tree convention, read whole file for header + assertion patterns)
  </read_first>
  <behavior>
    - Running `scripts/otto-gw.ps1 support` while the gateway is stopped causes Get-GatewayStatus to exit 1 mid-collection, aborting the support bundle
    - Failure mode is PowerShell-only — cannot be exercised in go test on Linux CI
    - Manual reproducer ships as a PowerShell script under tests/reliability/manual/
  </behavior>
  <action>
    Step 1 — Verify cite at scripts/otto-gw.ps1:584-593, :1464. Read Get-GatewayStatus's pidfile-absent / stale-pid branches. Determine whether they currently terminate via `exit 1` or return a structured "unreachable" sentinel. Record current file:line.

    Step 2 — Create tests/reliability/manual/REL-TRAY-02-repro.ps1 with Pattern F header block at top (PowerShell comment style — `#`):
    ```powershell
    # Finding ID: T-2
    # REL-* ID: REL-TRAY-02
    # Target phase: 14 (verify) / 15 (fix)
    # Target OS: Windows
    # Expected pre-fix behavior: scripts/otto-gw.ps1 support with gateway stopped exits 1 before creating bundle
    # Expected post-fix behavior: bundle is created on disk with health/health.json containing unreachable: sentinel
    # Run instructions: 1) Ensure gateway is stopped (scripts/otto-gw.ps1 stop). 2) Run this script. 3) Inspect $LASTEXITCODE and resulting bundle path.
    ```
    Body: invoke `scripts/otto-gw.ps1 support` (resolve path relative to script location), capture exit code + stdout + the resulting bundle path (if any), Write-Host the observed pre-fix vs post-fix verdict.

    Step 3 — Create cmd/otto-tray/regression_rel_tray_02_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_02_WindowsBundleExitOne`. First line of test body: `t.Skip("REL-TRAY-02 (T-2): manual reproducer — see tests/reliability/manual/REL-TRAY-02-repro.ps1 for Windows-only reproducer")`. Body: empty (discoverability stub per D-03).

    Step 4 — Write 14-FINDING-T-2.md with frontmatter (finding: T-2, severity: H, rel_id: REL-TRAY-02, status, target_phase: 15, verified_at: 2026-06-11) and four sections. "Evidence" section MUST point at BOTH the manual script AND the Go stub. Verdict per D-11.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_02 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_02_test.go && test -f tests/reliability/manual/REL-TRAY-02-repro.ps1 && grep -c "Target OS: Windows" tests/reliability/manual/REL-TRAY-02-repro.ps1 && grep -c "Finding ID: T-2" tests/reliability/manual/REL-TRAY-02-repro.ps1 && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-2.md && grep -c "REL-TRAY-02" .planning/phases/14-verify-reliability-findings/14-FINDING-T-2.md</automated>
  </verify>
  <done>
    Stub test runs as SKIP. Manual script has Pattern F header with all 7 fields. Evidence file points at both files.
  </done>
  <acceptance_criteria>
    14-FINDING-T-2.md, cmd/otto-tray/regression_rel_tray_02_test.go, AND tests/reliability/manual/REL-TRAY-02-repro.ps1 all exist. Pattern F header on the manual script contains all 7 fields.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify T-3 (High, REL-TRAY-03) — macOS silent gateway death (manual reproducer)</name>
  <files>
    cmd/otto-tray/regression_rel_tray_03_test.go,
    tests/reliability/manual/REL-TRAY-03-repro.sh,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-3.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-3 entry),
    cmd/otto-tray/tray.go (lines 199-201, 74-75 cited — tray death-detection path),
    cmd/otto-tray/uihelpers_darwin.go (lines 43-58 cited — the LSUIElement notification no-op),
    cmd/otto-tray/autostart_darwin_test.go (build-tag shape `//go:build darwin`)
  </read_first>
  <behavior>
    - When the gateway dies on macOS, the tray's notification path is a no-op (LSUIElement agents can't show modal dialogs)
    - User sees no signal — icon doesn't change, tooltip doesn't update
    - Failure mode requires a real macOS GUI session — cannot be exercised in CI
  </behavior>
  <action>
    Step 1 — Verify cite at cmd/otto-tray/tray.go:199-201, :74-75 and cmd/otto-tray/uihelpers_darwin.go:43-58. Read the death-detection path. Determine whether icon swap / tooltip update / non-no-op channel is implemented today. Record current file:line.

    Step 2 — Create tests/reliability/manual/REL-TRAY-03-repro.sh with Pattern F header (bash `#` comment style):
    ```bash
    #!/usr/bin/env bash
    # Finding ID: T-3
    # REL-* ID: REL-TRAY-03
    # Target phase: 14 (verify) / 15 (fix)
    # Target OS: macOS
    # Expected pre-fix behavior: killing gateway out from under running tray produces no visible signal in menu bar
    # Expected post-fix behavior: icon and/or tooltip change to a death-indicating state user can observe at a glance
    # Run instructions: 1) Start tray app via scripts/otto-gw start. 2) In a second terminal, kill -9 the gateway PID from $(cat $PIDFILE). 3) Watch the menu bar icon and tooltip for 30 seconds. 4) Report whether any visible signal appeared.
    ```
    Body: detect PIDFILE location (read from a known env var or default path used by scripts/otto-gw), `kill -9 $(cat $PIDFILE)`, then loop reading whatever observable signal the tray exposes (e.g. printing instructions for the human operator since this is a GUI-bound verification). Make it executable: `chmod +x` after write.

    Step 3 — Create cmd/otto-tray/regression_rel_tray_03_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_03_MacosSilentGatewayDeath`. First line of test body: `t.Skip("REL-TRAY-03 (T-3): manual reproducer — see tests/reliability/manual/REL-TRAY-03-repro.sh for macOS-only reproducer")`. Body: empty (discoverability stub).

    Step 4 — Write 14-FINDING-T-3.md with frontmatter (finding: T-3, severity: H, rel_id: REL-TRAY-03, status, target_phase: 15, verified_at: 2026-06-11) and four sections. "Evidence" section MUST point at BOTH files. Verdict per D-11.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_03 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_03_test.go && test -f tests/reliability/manual/REL-TRAY-03-repro.sh && test -x tests/reliability/manual/REL-TRAY-03-repro.sh && grep -c "Target OS: macOS" tests/reliability/manual/REL-TRAY-03-repro.sh && grep -c "Finding ID: T-3" tests/reliability/manual/REL-TRAY-03-repro.sh && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-3.md && grep -c "REL-TRAY-03" .planning/phases/14-verify-reliability-findings/14-FINDING-T-3.md</automated>
  </verify>
  <done>
    Stub test runs as SKIP. Manual script is executable, has Pattern F header with all 7 fields, target OS macOS. Evidence file points at both.
  </done>
  <acceptance_criteria>
    14-FINDING-T-3.md, cmd/otto-tray/regression_rel_tray_03_test.go, AND tests/reliability/manual/REL-TRAY-03-repro.sh all exist. Pattern F header on manual script contains all 7 fields and target OS is "macOS".
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: Verify T-4 (Medium, REL-TRAY-04) — Windows notify blocking</name>
  <files>
    cmd/otto-tray/regression_rel_tray_04_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-4.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-4 entry),
    cmd/otto-tray/tray.go (lines 199-201 cited),
    cmd/otto-tray/uihelpers_windows.go (lines 50-68 cited — the modal notify path),
    cmd/otto-tray/fsm_test.go (computeState table-driven pattern — closest analog)
  </read_first>
  <behavior>
    - notify() on Windows pops a foreground-stealing modal dialog that blocks the uiLoop for up to 30s
    - Pre-fix reproducer: inject a fake notify that blocks on a channel; call applyState; assert it blocks > 100ms
  </behavior>
  <action>
    Step 1 — Verify cite at cmd/otto-tray/tray.go:199-201 and cmd/otto-tray/uihelpers_windows.go:50-68. Determine whether notify() is dispatched off the uiLoop today. Record current file:line.

    Step 2 — Create cmd/otto-tray/regression_rel_tray_04_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_04_WindowsNotifyBlocking`. First line of test body: `t.Skip("REL-TRAY-04 (T-4): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: define a `blockingNotify` closure that waits on an unsignaled chan struct{}, inject it via whatever notify-hook seam exists (or use a package-level var swap if no seam exists today — read tray.go to see what's accessible from the test package), call applyState with a state-change payload that triggers notify, measure elapsed time, assert elapsed > 100ms (pre-fix observable).

    Step 3 — Write 14-FINDING-T-4.md with frontmatter (finding: T-4, severity: M, rel_id: REL-TRAY-04, status, target_phase: 16, verified_at: 2026-06-11) and four sections.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_04 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_04_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-4.md && grep -c "REL-TRAY-04" .planning/phases/14-verify-reliability-findings/14-FINDING-T-4.md && head -1 cmd/otto-tray/regression_rel_tray_04_test.go | grep -E "^//go:build darwin \|\| windows"</automated>
  </verify>
  <done>
    Test runs as SKIP with Phase 16 reference. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-T-4.md and cmd/otto-tray/regression_rel_tray_04_test.go exist per spec with Pattern G build tag.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 5: Verify T-5 (Medium, REL-TRAY-05) — tray "running" while pool wedged</name>
  <files>
    cmd/otto-tray/regression_rel_tray_05_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-5.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-5 entry),
    cmd/otto-tray/tray.go (line 153 cited),
    cmd/otto-tray/fsm.go (lines 52-54 cited),
    internal/server/health.go (lines 70-77 cited),
    cmd/otto-tray/poller_test.go (lines 11-52 — TestPoller_EmitsStateOnEachTick is the direct template)
  </read_first>
  <behavior>
    - When all pool slots are busy-but-not-serving (PoolAlive=0, PoolSize=4, PoolBusy=4), the tray reports StateRunning instead of StateDegraded
    - Pre-fix reproducer: fakeProbe returning that exact Snapshot, assert state output is StateRunning (pre-fix observable); post-fix would be StateDegraded
  </behavior>
  <action>
    Step 1 — Verify cite at cmd/otto-tray/tray.go:153, cmd/otto-tray/fsm.go:52-54, internal/server/health.go:70-77. Determine whether tray currently consumes /health/pool's PoolAlive field. Record current file:line.

    Step 2 — Create cmd/otto-tray/regression_rel_tray_05_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_05_DegradedWhenPoolWedged`. First line of test body: `t.Skip("REL-TRAY-05 (T-5): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: direct copy of TestPoller_EmitsStateOnEachTick — fakeProbe{pidAlive: true, healthOK: true, snap: Snapshot{PoolAlive: 0, PoolSize: 4, PoolBusy: 4}}, tick channel, output channel, startedAt, runPoller goroutine. Send a tick, read out, assert `out.State == StateRunning` (pre-fix observable). Use the exact harness shape from poller_test.go.

    Step 3 — Write 14-FINDING-T-5.md with frontmatter (finding: T-5, severity: M, rel_id: REL-TRAY-05, status, target_phase: 16, verified_at: 2026-06-11) and four sections.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_05 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_05_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-5.md && grep -c "REL-TRAY-05" .planning/phases/14-verify-reliability-findings/14-FINDING-T-5.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-T-5.md and cmd/otto-tray/regression_rel_tray_05_test.go exist per spec.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Verify T-6 (Medium, REL-TRAY-06) — Windows bundle path stdout pollution (manual reproducer)</name>
  <files>
    cmd/otto-tray/regression_rel_tray_06_test.go,
    tests/reliability/manual/REL-TRAY-06-repro.ps1,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-6.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-6 entry),
    cmd/otto-tray/tray.go (lines 296-299 cited — the revealBundle path-parse),
    scripts/otto-gw.ps1 (lines 321, 330, 1644 cited — wrapper stdout chatter),
    tests/scripts/test-support-bundle.ps1 (script shape)
  </read_first>
  <behavior>
    - Wrapper writes "loaded env file: ..." (or similar) to stdout before printing the bundle path
    - Tray's revealBundle takes the first stdout line as the path — opens the log message, not the archive
    - Failure mode is Windows-only (the env-loading log line is platform-specific)
  </behavior>
  <action>
    Step 1 — Verify cite at cmd/otto-tray/tray.go:296-299 and scripts/otto-gw.ps1:321, :330, :1644. Determine whether revealBundle currently parses the last non-empty line. Record current file:line.

    Step 2 — Create tests/reliability/manual/REL-TRAY-06-repro.ps1 with Pattern F header (PowerShell `#` comments):
    ```powershell
    # Finding ID: T-6
    # REL-* ID: REL-TRAY-06
    # Target phase: 14 (verify) / 16 (fix)
    # Target OS: Windows
    # Expected pre-fix behavior: tray's revealBundle opens the wrapper's first stdout line (e.g. 'loaded env file: ...') instead of the .zip archive path
    # Expected post-fix behavior: tray opens the actual .zip archive at the last non-empty stdout line
    # Run instructions: 1) Set OTTO_ENV_FILE to a real .env file path. 2) Run 'scripts/otto-gw.ps1 support' and capture stdout. 3) Inspect the first vs last non-empty lines.
    ```
    Body: set OTTO_ENV_FILE env, invoke `scripts/otto-gw.ps1 support`, capture all stdout lines, print first non-empty line vs last non-empty line, show the path discrepancy.

    Step 3 — Create cmd/otto-tray/regression_rel_tray_06_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_06_WindowsBundlePathPollution`. First line of test body: `t.Skip("REL-TRAY-06 (T-6): manual reproducer — see tests/reliability/manual/REL-TRAY-06-repro.ps1 for Windows-only reproducer")`. Body: empty.

    Step 4 — Write 14-FINDING-T-6.md with frontmatter (finding: T-6, severity: M, rel_id: REL-TRAY-06, status, target_phase: 16, verified_at: 2026-06-11) and four sections. Evidence points at both files.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_06 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_06_test.go && test -f tests/reliability/manual/REL-TRAY-06-repro.ps1 && grep -c "Target OS: Windows" tests/reliability/manual/REL-TRAY-06-repro.ps1 && grep -c "Finding ID: T-6" tests/reliability/manual/REL-TRAY-06-repro.ps1 && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-6.md && grep -c "REL-TRAY-06" .planning/phases/14-verify-reliability-findings/14-FINDING-T-6.md</automated>
  </verify>
  <done>
    Stub test runs as SKIP. Manual script has Pattern F header with all 7 fields. Evidence file points at both files.
  </done>
  <acceptance_criteria>
    14-FINDING-T-6.md, cmd/otto-tray/regression_rel_tray_06_test.go, AND tests/reliability/manual/REL-TRAY-06-repro.ps1 all exist. Pattern F header has all 7 fields.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 7: Verify T-7 (Medium, REL-TRAY-07) — support bundle size/time unbounded</name>
  <files>
    cmd/otto-tray/regression_rel_tray_07_test.go,
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-7.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (the T-7 entry),
    scripts/otto-gw (lines 1864-1873, 1957-1989 cited — bash bundle collection),
    scripts/otto-gw.ps1 (lines 1489-1494, 1586-1596 cited — PowerShell bundle collection),
    cmd/otto-tray/runner.go (lines 29-33 cited — wrapper invocation),
    tests/scripts/test-support-bundle.sh (lines 14-40 for bundle structure-assertion convention)
  </read_first>
  <behavior>
    - support-bundle has no size cap and no overall timeout
    - On a multi-GB log day, the bundle can exceed disk or stall indefinitely
    - On timeout, the staging directory is leaked rather than cleaned up
    - Pre-fix reproducer: drive wrapper with a large fake log dir; assert bundle exceeds --max-mb (pre-fix observable: no enforcement)
  </behavior>
  <action>
    Step 1 — Verify cite at scripts/otto-gw:1864-1873, :1957-1989; scripts/otto-gw.ps1:1489-1494, :1586-1596; cmd/otto-tray/runner.go:29-33. Determine whether --max-mb cap and overall timeout exist today. Record current file:line.

    Step 2 — Create cmd/otto-tray/regression_rel_tray_07_test.go with build tag `//go:build darwin || windows`. Function: `TestRegression_REL_TRAY_07_SupportBundleBounds`. First line of test body: `t.Skip("REL-TRAY-07 (T-7): regression test — unskip in Phase 16 fix commit")` verbatim. Reproducer body: build a fake log dir at t.TempDir() containing a large (e.g. 100MB) log file via io.Copy(file, io.LimitReader(rand.Reader, 100<<20)), then invoke the wrapper via os/exec (with cmd/otto-tray/runner.go's resolveWrapperPath helper if accessible) with the support verb pointing at the fake dir. Capture the resulting bundle path, stat its size, assert `bundleSize > 50 * 1024 * 1024` (pre-fix observable: no cap). Use a t.TempDir() for staging output.

    Step 3 — Write 14-FINDING-T-7.md with frontmatter (finding: T-7, severity: M, rel_id: REL-TRAY-07, status, target_phase: 16, verified_at: 2026-06-11) and four sections.
  </action>
  <verify>
    <automated>cd cmd/otto-tray && (uname | grep -qE "Darwin" && go test -race -run TestRegression_REL_TRAY_07 -v 2>&1 | grep -E "(SKIP|FAIL|PASS)" | head -5 || echo "skipped on Linux") && cd ../.. && gofumpt -l cmd/otto-tray/regression_rel_tray_07_test.go && test -f .planning/phases/14-verify-reliability-findings/14-FINDING-T-7.md && grep -c "REL-TRAY-07" .planning/phases/14-verify-reliability-findings/14-FINDING-T-7.md</automated>
  </verify>
  <done>
    Test runs as SKIP. Evidence file complete.
  </done>
  <acceptance_criteria>
    14-FINDING-T-7.md and cmd/otto-tray/regression_rel_tray_07_test.go exist per spec.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Write ledger fragment 14-LEDGER-FRAGMENT-03.md + (conditional) master ledger merge</name>
  <files>
    .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md,
    .planning/phases/14-verify-reliability-findings/14-VERIFICATION-LEDGER.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    docs/reviews/2026-06-11-reliability-review.md (re-read each T-1..T-7 entry for exact cites),
    .planning/phases/14-verify-reliability-findings/14-FINDING-T-1.md through 14-FINDING-T-7.md (verdicts written in tasks 1-7),
    .planning/REQUIREMENTS.md §Traceability
  </read_first>
  <action>
    Step 1 — Write 14-LEDGER-FRAGMENT-03.md. Single markdown table with exactly 7 data rows (T-1..T-7) + header per D-07: `| Finding ID | Sev | REL-* ID | Status | File:line | Evidence | Target phase |`. Target phase: 15 for T-1/T-2/T-3, 16 for T-4/T-5/T-6/T-7 (or `drop` if any false-positive).

    Step 2 — Check fragments 01, 02, 04 existence and master ledger existence. If 14-VERIFICATION-LEDGER.md already exists, STOP (someone else merged). If all 3 sibling fragments exist AND master is absent, proceed to Step 3.

    Step 3 (merge — only if conditions in Step 2 met) — Same merge procedure as plan 14-01 Task 7 Step 3: Summary section, 23 in-scope rows in canonical order, 12 Low placeholder rows, needs_investigation_count annotation.

    Step 4 (merge — only if conditions in Step 2 met) — Update .planning/REQUIREMENTS.md §Traceability for any false-positive REL-*.

    Step 5 — Read-only-implementation check: `git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/'` MUST return empty.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md && grep -c "^| T-" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md | grep -q "^7$" && grep -E "^\| Finding ID \| Sev \| REL-\* ID \| Status \| File:line \| Evidence \| Target phase \|" .planning/phases/14-verify-reliability-findings/14-LEDGER-FRAGMENT-03.md && git diff main...HEAD -- ':!*_test.go' ':!.planning/' ':!docs/' ':!tests/reliability/' | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    14-LEDGER-FRAGMENT-03.md exists with 7 T-* rows. Master ledger and REQUIREMENTS.md update only happen if conditions met. Read-only diff empty.
  </done>
  <acceptance_criteria>
    14-LEDGER-FRAGMENT-03.md exists with 7 rows and D-07 column order. Read-only-implementation rule holds.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 7 Tray evidence files exist with D-08 frontmatter + 4 required sections + verdict per D-09/D-10/D-11
- All 7 Go test files exist with verbatim D-12 t.Skip strings (T-2/T-3/T-6 point at manual scripts per D-03)
- All 3 manual reproducer scripts exist with Pattern F header (PowerShell or bash comment style)
- Every Go test file has `//go:build darwin || windows` as the first line per Pattern G
- REL-TRAY-03-repro.sh is executable (chmod +x)
- Ledger fragment 03 exists with D-07 column order and 7 rows
- Trust gates: gofumpt clean on new files, go vet clean for cmd/otto-tray (on darwin/windows hosts), go test -race ./cmd/otto-tray/... passes on darwin (all new tests SKIP)
- Read-only-implementation rule: production-source diff empty (NO edits to scripts/otto-gw, scripts/otto-gw.ps1, or cmd/otto-tray/ non-test files)
</verification>

<success_criteria>
- 7 evidence files, 7 Go test files, 3 manual scripts, 1 ledger fragment land on disk
- Every new test runs as SKIP on darwin (where build tag matches); Linux CI excludes by build constraint
- D-11 bias respected; D-09 false-positive bar respected; D-10 escape hatch respected
- This plan contributes 7 rows toward the Phase 14 close gate (needs_investigation_count == 0)
</success_criteria>

<output>
Create `.planning/phases/14-verify-reliability-findings/14-03-SUMMARY.md` when done. Summary should:
- List the 7 verdicts by ID and severity
- Report confirmed / false-positive / needs-investigation counts
- Confirm read-only-implementation diff is empty
- Note whether this plan executed the master-ledger merge (Task 8 Step 2 conditions) or just wrote the fragment
- Surface any false-positive REL-* IDs that should drop from Phase 15/16 scope
</output>
