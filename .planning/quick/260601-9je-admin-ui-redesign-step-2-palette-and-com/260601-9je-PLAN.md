---
phase: quick-260601-9je
plan: 01
type: execute
wave: 1
depends_on:
  - quick-260601-98c-01
files_modified:
  - internal/admin/static/css/admin.css
autonomous: true
requirements:
  - admin-ui-redesign-step-2
must_haves:
  truths:
    - "Dark theme (default) renders with the new Liberty-inspired palette: deeper near-black bg (#1A1D24), elevated card surface (#242832), OTTO yellow (#FAD22D) accent on tabs/buttons/links, and modernized semantic colors (healthy #0FC373, warning #FF8C0A, failed #FF3232)"
    - "Light theme renders a real light palette: F7F8FA bg, white cards, dark fg (#1A1D24), darker yellow link (#B8930A) for AA contrast, but the HEADER stays dark (#1E2128) in BOTH themes (Liberty-style header parity)"
    - "Theme toggle flips themes with a smooth ~150ms transition on body / .otto-card / .otto-header background+color+border"
    - "All four dashboard sections (Summary strip, Pool Slots, Sessions table, Log Tail) plus header + tab nav restyled to the new look — uppercase tracked H2 headings, pill badges, accent left-border card variants, alternating-row tables, primary/secondary button shapes"
    - "Existing admin.js continues to drive the page unchanged — all data-* selectors, class names (is-healthy/is-degraded/is-down/is-alive/is-dead/is-busy/is-paused/is-disconnected/is-debug/is-info/is-warn/is-error/is-unknown), and template fields still wire up correctly"
    - "Light-mode-only subtle card shadow (0 1px 2px rgba(0,0,0,0.04)) renders on .otto-card; dark mode has no shadow"
    - "go build ./... and go test ./internal/admin/... succeed (pre-existing go vet errors in tail_test.go / tail_timberjack_test.go are NOT addressed — they were present before step 1)"
  artifacts:
    - path: "internal/admin/static/css/admin.css"
      provides: "Refactored palette (dark + light token sets), new component blocks (.otto-card--accent-*, .otto-badge variants, .otto-btn--secondary, .otto-tab updates), typography size tokens (--otto-text-*), shadow + transition hygiene rules"
      contains: "--otto-card-hover"
  key_links:
    - from: ":root token block"
      to: "[data-theme=\"light\"] override block"
      via: "matched token names with theme-specific values; --otto-header is intentionally identical in both"
      pattern: "--otto-header"
    - from: ".otto-card / .otto-summary-strip / .otto-sessions-card / .otto-log-panel / .otto-slot-card"
      to: "--otto-bg / --otto-card / --otto-border tokens"
      via: "var(--otto-*) references — zero hardcoded hex remain for theme-able surfaces"
      pattern: "var\\(--otto-card\\)"
    - from: ".otto-tab.is-active / .otto-btn primary"
      to: "--otto-accent / --otto-accent-fg"
      via: "underline and fill use the accent token, not bare #FAD22D"
      pattern: "var\\(--otto-accent\\)"
---

<objective>
Step 2 of the admin UI redesign: replace the no-op `[data-theme="light"]` overrides from step 1 with a REAL light palette AND restyle the DARK palette + all admin components to a more modern Liberty-inspired look while keeping OTTO yellow `#FAD22D` as the brand accent. Pure CSS work — no template, handler, or JS changes.

Purpose: After step 1's structural scaffolding, the page still wears the old purple-heavy palette (`#28243D` / `#3A3A3A`). Step 2 lands the visual identity: deeper near-black dark bg, elevated card surfaces, a usable light theme (header stays dark in both themes to mirror Liberty), modernized semantic colors, pill badges, accent-bordered cards, tighter typography, smooth theme transitions. After this commit the dashboard, About placeholder, and Docs placeholder all render the new look with one click of the theme toggle to switch between dark and light.

Output: A single atomic commit modifying only `internal/admin/static/css/admin.css`. No new files; no template/handler/JS changes; no new dependencies; embed.FS unchanged (already covers admin.css).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/admin/static/css/admin.css
@internal/admin/templates/base.html.tmpl
@internal/admin/templates/dashboard.html.tmpl
@internal/admin/static/js/admin.js
@.planning/quick/260601-98c-admin-ui-redesign-step-1-layout-extracti/260601-98c-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor palette tokens and restyle components for Liberty-inspired dark + real light themes</name>
  <files>
    internal/admin/static/css/admin.css (modify in place — single file)
  </files>
  <action>
    Treat this as one cohesive CSS refresh — splitting would create a broken intermediate state where some surfaces use new tokens and others still reference old ones. Make ALL the changes below in a single edit pass, then verify with the grep gates in <verify>.

    ──────────────────────────────────────────────────────────────────────────
    (A) DARK PALETTE TOKEN REWRITE — replace the existing `:root { ... }` block
    ──────────────────────────────────────────────────────────────────────────

    The current `:root` block at the top of admin.css (lines ~8-31) declares:
      --otto-bg / --otto-card / --otto-fg / --otto-muted / --otto-border /
      --otto-accent / --otto-healthy / --otto-warning / --otto-failed /
      --otto-activity / --otto-link / spacing + radius + font tokens.

    REPLACE the color portion of `:root` with the new dark token set listed below.
    KEEP the spacing tokens (--otto-space-*), radius tokens (--otto-radius-*),
    and font stack tokens (--otto-font-ui / --otto-font-mono) exactly as they
    are — they are correct and used everywhere.

    Add new typography size tokens to `:root` as well:
      --otto-text-xs: 11px
      --otto-text-sm: 13px
      --otto-text-base: 14px
      --otto-text-lg: 16px
      --otto-text-xl: 20px

    Final `:root` color tokens (DARK theme = default):
      --otto-bg:          #1A1D24
      --otto-card:        #242832
      --otto-card-hover:  #2C3140
      --otto-header:      #1E2128
      --otto-border:      #2D3340
      --otto-fg:          #FAFAFA
      --otto-fg-muted:    #9CA3AF
      --otto-fg-dim:      #6B7280
      --otto-accent:      #FAD22D
      --otto-accent-fg:   #1A1D24
      --otto-link:        #FAD22D
      --otto-healthy:     #0FC373
      --otto-warning:     #FF8C0A
      --otto-failed:      #FF3232
      --otto-activity:    #AF78D2  (keep — purple log activity dot, no design change requested)

    NOTE on naming: step 1 used `--otto-muted` for muted foreground. The new
    palette renames to `--otto-fg-muted` for clarity (and adds `--otto-fg-dim`).
    The sweep in (C) must replace EVERY `var(--otto-muted)` reference in
    admin.css with `var(--otto-fg-muted)` (~17 occurrences). Do NOT keep
    `--otto-muted` as a back-compat alias — clean break, single source of truth.

    ──────────────────────────────────────────────────────────────────────────
    (B) LIGHT PALETTE — replace the step-1 no-op `[data-theme="light"]` block
    ──────────────────────────────────────────────────────────────────────────

    The current `[data-theme="light"]` block at lines ~727-739 re-declares the
    SAME values as `:root` (step 1's no-op proof-of-plumbing). REPLACE that
    block's contents with the real light palette below. Keep the selector and
    the surrounding comment header (update the comment body to reflect that
    step 2 has landed the real palette).

    Final `[data-theme="light"]` color tokens:
      --otto-bg:          #F7F8FA
      --otto-card:        #FFFFFF
      --otto-card-hover:  #F1F3F7
      --otto-header:      #1E2128   ← INTENTIONAL: header stays dark in BOTH themes (Liberty parity)
      --otto-border:      #E5E7EB
      --otto-fg:          #1A1D24
      --otto-fg-muted:    #4B5563
      --otto-fg-dim:      #6B7280
      --otto-accent:      #FAD22D
      --otto-accent-fg:   #1A1D24
      --otto-link:        #B8930A   ← darker yellow for AA contrast on light bg
      --otto-healthy:     #059669
      --otto-warning:     #D97706
      --otto-failed:      #DC2626
      --otto-activity:    #7C3AED   (darker purple variant for light-mode log dot contrast)

    ──────────────────────────────────────────────────────────────────────────
    (C) HARDCODED-HEX SWEEP — find every hex literal in admin.css and replace
    ──────────────────────────────────────────────────────────────────────────

    Sweep the ENTIRE admin.css for hardcoded hex values that should now be
    tokens. After the sweep, the only hex literals remaining in admin.css
    should be (1) inside the `:root` block, (2) inside the
    `[data-theme="light"]` block, and (3) the rgba() shadow rule in (G).

    Specific known offenders to replace (use grep gates in <verify> to catch
    any missed ones):

      1. `.otto-status-pill { ... color: #28243D; ... }` (line ~172)
         → `color: var(--otto-accent-fg);` — pill text needs to be readable
         on yellow accent in BOTH themes; --otto-accent-fg is #1A1D24 in both
         (same family as old #28243D, slightly darker), so behavior is preserved.

      2. `.otto-badge.is-alive { background: #1B3B2E; ... }` (line ~328)
         → keep the rgba(15,195,115,0.12) pattern used by sibling .is-healthy
         variant. Replace with: `background: rgba(15, 195, 115, 0.12);`
         (i.e., make .is-alive identical to .is-healthy for consistency).

      3. `.otto-badge.is-dead { background: #3D1E1E; ... }` (line ~333)
         → `background: rgba(255, 50, 50, 0.12);`
         (matches .is-failed sibling).

      4. `.otto-tab.is-active { border-bottom-color: #FAD22D; ... }` (line ~791)
         → `border-bottom-color: var(--otto-accent);`

      5. `.otto-btn.is-active { background: var(--otto-link); border-color: var(--otto-link); }`
         (lines ~654-657) AND `.otto-btn.is-paused { ... }` (lines ~664-667)
         → the OLD --otto-link was blue `#1174E6` (visible chrome of pause btn
         when paused). NEW --otto-link is `#FAD22D` (yellow). Functionally
         this means the pause button when paused will be YELLOW instead of
         BLUE — that is the desired step-2 outcome (yellow = OTTO accent
         everywhere; remove blue link color entirely). No change needed here,
         but be aware the visual changes.

      6. `.otto-header { ... border-bottom: 1px solid rgba(255, 255, 255, 0.08); }`
         (line ~747) → keep the rgba pattern (subtle hairline on dark header
         works in both themes since header is always dark). Optionally also
         add `background: var(--otto-header);` and `color: var(--otto-fg);`
         where the otto-fg in this context should be the DARK-theme foreground
         (#FAFAFA) regardless of theme — see (D) below for the dark-header-
         always rule.

      7. `.otto-theme-toggle { ... border: 1px solid rgba(255, 255, 255, 0.2); ... }`
         (lines ~760-768) — keep the rgba (always-dark header parity), no
         change needed.

      8. `.otto-tab-nav { ... border-bottom: 1px solid rgba(255, 255, 255, 0.08); }`
         (line ~775) — REPLACE with `border-bottom: 1px solid var(--otto-border);`
         because the tab nav appears BELOW the dark header on the page bg
         (which IS theme-able). In dark theme this is barely different; in
         light theme it becomes a proper light-theme border instead of an
         invisible white-alpha hairline.

      9. Any other bare `#FAD22D`, `#28243D`, `#3A3A3A`, `#FAFAFA`, `#0FC373`,
         `#FF8C0A`, `#FF3232`, `#A0A0A0`, `#4A4A4A`, `#1174E6`, `#AF78D2`
         literals outside the two palette blocks → token-ize. Use the grep
         gates to catch any missed.

    ──────────────────────────────────────────────────────────────────────────
    (D) HEADER STAYS DARK IN BOTH THEMES
    ──────────────────────────────────────────────────────────────────────────

    The .otto-header element renders against the always-dark `--otto-header`
    background (Liberty-style). Inside that bar, text + icons must be
    readable on dark, so they should NOT switch to dark fg in light theme.

    Update .otto-header (around line ~742-748):
      .otto-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.75rem 1.25rem;
        background: var(--otto-header);
        color: #FAFAFA;                                /* always-light fg */
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      }

    Update .otto-header-brand (line ~750-753): no change needed — inherits color.

    Update .otto-theme-toggle (line ~760-768): no change needed — uses
    `color: inherit` which now resolves to the always-light header fg.

    Update the dark-theme tab-nav appearance separately — tab nav sits BELOW
    the header on the theme-able body background, so it uses theme tokens
    normally (per (C) point 8).

    ──────────────────────────────────────────────────────────────────────────
    (E) NEW COMPONENT CLASS BLOCKS — add at the end of admin.css
    ──────────────────────────────────────────────────────────────────────────

    Append the following NEW rules at the end of admin.css (after the
    Quick 260601-98c section, in a new `Quick 260601-9je — palette + component
    restyle` section with a comment header). Do not duplicate existing
    `.otto-card` / `.otto-badge` base rules — these are ADDITIVE variants and
    new classes.

    1. Card accent-border variants (apply 4px left border keyed to semantic
       color, on top of the base .otto-card rule):

         .otto-card--accent-healthy { border-left: 4px solid var(--otto-healthy); }
         .otto-card--accent-warning { border-left: 4px solid var(--otto-warning); }
         .otto-card--accent-failed  { border-left: 4px solid var(--otto-failed); }
         .otto-card--accent-accent  { border-left: 4px solid var(--otto-accent); }

    2. Pill badge base + dot variant (additive — does NOT replace existing
       .otto-badge rules, which are inline rectangular tags. The new
       .otto-badge--pill modifier makes the badge fully rounded with an
       optional leading dot. Keep both styles available so existing JS
       continues working unchanged.):

         .otto-badge--pill {
           border-radius: var(--otto-radius-pill);
           padding: 4px 10px;
           display: inline-flex;
           align-items: center;
           gap: 6px;
           font-size: var(--otto-text-xs);
         }

         .otto-badge--pill::before {
           content: "";
           display: inline-block;
           width: 6px;
           height: 6px;
           border-radius: 50%;
           background: currentColor;
         }

    3. Secondary button variant (transparent + bordered, complements existing
       .otto-btn which already styles as filled/secondary-card-like). Also
       define a true `.otto-btn--primary` variant for the accent-filled
       primary action:

         .otto-btn--primary {
           background: var(--otto-accent);
           border-color: var(--otto-accent);
           color: var(--otto-accent-fg);
           font-weight: 600;
         }

         .otto-btn--primary:hover { opacity: 0.9; }

         .otto-btn--secondary {
           background: transparent;
           border: 1px solid var(--otto-border);
           color: var(--otto-fg);
         }

         .otto-btn--secondary:hover { background: var(--otto-card-hover); }

    4. Tab nav hover state refinement (the existing .otto-tab:hover uses
       `opacity: 0.85` — replace it with a color shift for clearer affordance
       and a smoother hover transition):

         .otto-tab {
           transition: color 150ms, border-color 150ms;
         }
         .otto-tab:hover {
           opacity: 1;
           color: var(--otto-fg-muted);
         }

       Note: the .otto-tab element is inside .otto-tab-nav which sits on the
       theme-able body bg, so var(--otto-fg-muted) flips with theme.

    5. Theme toggle hover lift:

         .otto-theme-toggle {
           transition: background 150ms;
         }
         .otto-theme-toggle:hover {
           background: rgba(255, 255, 255, 0.06);
         }

    ──────────────────────────────────────────────────────────────────────────
    (F) TYPOGRAPHY — standardize sizes on tokens
    ──────────────────────────────────────────────────────────────────────────

    Modify EXISTING typography rules to use the new size tokens AND apply the
    Liberty-style uppercase tracked headings:

      .otto-h2 {
        font-size: var(--otto-text-xl);          /* was 16px, now 20px */
        font-weight: 600;
        line-height: 1.4;
        color: var(--otto-fg);
        margin: 0 0 var(--otto-space-base) 0;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .otto-summary-label {
        font-size: var(--otto-text-xs);          /* was 12px, now 11px */
        font-weight: 400;
        color: var(--otto-fg-muted);
        text-transform: uppercase;
        letter-spacing: 0.06em;                   /* was 0.03em — tighter Liberty look */
        line-height: 1.4;
      }

      .otto-summary-value {
        font-size: var(--otto-text-xl);          /* was 20px — already correct, switch to token */
        font-weight: 600;
        color: var(--otto-fg);
        line-height: 1.3;
      }

      .otto-summary-value.text-mono {
        font-family: var(--otto-font-mono);
        font-size: var(--otto-text-base);        /* was 14px — token */
      }

    KEEP the system font stack already defined in :root — do NOT add @import.

    ──────────────────────────────────────────────────────────────────────────
    (G) VISUAL HYGIENE — light-mode card shadow + theme transitions
    ──────────────────────────────────────────────────────────────────────────

    1. Light-mode-only subtle card shadow (placed AFTER the .otto-card base
       rule, before responsiveness section):

         [data-theme="light"] .otto-card {
           box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
         }

       (Dark theme has no shadow — would be invisible against dark surfaces
       and add noise.)

    2. Smooth theme transition on body + .otto-card + .otto-header:

       Modify the existing `body` rule to add the transition declaration.
       Add the same transition to .otto-card (modify existing rule). Add to
       .otto-header (modify existing rule).

         body {
           margin: 0;
           background: var(--otto-bg);
           color: var(--otto-fg);
           font-family: var(--otto-font-ui);
           font-size: var(--otto-text-base);     /* was 14px — token */
           line-height: 1.5;
           transition: background-color 150ms, color 150ms, border-color 150ms;
         }

         .otto-card {
           background: var(--otto-card);
           border: 1px solid var(--otto-border);
           border-radius: var(--otto-radius-card);
           padding: var(--otto-space-base);
           transition: background-color 150ms, color 150ms, border-color 150ms;
         }

         .otto-header {
           /* ... existing block from (D) ... */
           transition: background-color 150ms, color 150ms, border-color 150ms;
         }

    ──────────────────────────────────────────────────────────────────────────
    (H) HTML color-scheme — keep declared `dark` (no theme-aware switch needed)
    ──────────────────────────────────────────────────────────────────────────

    The current `html { color-scheme: dark; }` rule at line ~42 controls
    browser-native widget chrome (scrollbar color, form input default look).
    Leave it as `dark` — light theme is opt-in via the toggle and we accept
    that native scrollbars stay dark in light mode for step 2 (revisit later
    if it becomes a complaint). Document this with a one-line comment.

    Alternatively, if you prefer fully theme-aware native chrome, you may
    instead write:
      html { color-scheme: dark; }
      html[data-theme="light"] { color-scheme: light; }
    Both are acceptable. Choose ONE and add a comment explaining the choice.

    ──────────────────────────────────────────────────────────────────────────
    HARD CONSTRAINTS (recap from task_scope)
    ──────────────────────────────────────────────────────────────────────────

    - NO template, handler, or admin.js changes. CSS-only commit.
    - NO new external assets — no @import url(...), no CDN refs, no font files.
    - PRESERVE every existing class name (is-healthy / is-degraded / is-down /
      is-alive / is-dead / is-busy / is-paused / is-disconnected / is-debug /
      is-info / is-warn / is-error / is-unknown / is-yellow / is-orange /
      is-red / text-mono / etc.) — admin.js writes/reads these via classList
      and they must continue to map to visible styles.
    - PRESERVE the embed.FS hookup — admin.css is already embedded by
      `//go:embed ... static`, no Go change required.
    - SINGLE atomic commit. Do NOT split palette / sweep / new components
      across multiple commits — the intermediate states would be visually
      broken.
    - DO NOT use `git stash` (step 1 retrospective flagged this as a
      destructive_git_prohibition violation). Use `git diff` and
      `git status` only.
    - DO NOT attempt to fix the pre-existing `go vet` errors in
      `internal/admin/tail_test.go` / `tail_timberjack_test.go` (Go-1.24
      testing.Context vs module Go-1.23). They are unrelated to this work
      and were present before step 1.
  </action>
  <verify>
    <automated>
      cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway &amp;&amp;
      # 1. Build + test (admin package) — embed.FS picks up the modified CSS.
      go build ./... &amp;&amp;
      go test ./internal/admin/... &amp;&amp;
      # 2. New dark-theme tokens present in :root (spot-check 6 of 14).
      grep -q "^\s*--otto-card-hover:\s*#2C3140" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-header:\s*#1E2128" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-border:\s*#2D3340" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-fg-muted:\s*#9CA3AF" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-fg-dim:\s*#6B7280" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-accent-fg:\s*#1A1D24" internal/admin/static/css/admin.css &amp;&amp;
      # 3. Old dark bg (#1A1D24) IS present (it's the new bg) but old #28243D and #3A3A3A MUST be gone (excluding comments).
      test "$(grep -v '^\s*\*\|^\s*//' internal/admin/static/css/admin.css | grep -c '#28243D')" = "0" &amp;&amp;
      test "$(grep -v '^\s*\*\|^\s*//' internal/admin/static/css/admin.css | grep -c '#3A3A3A')" = "0" &amp;&amp;
      # 4. Light theme block contains the real palette values (spot-check 4).
      grep -q "^\s*--otto-bg:\s*#F7F8FA" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-card:\s*#FFFFFF" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-link:\s*#B8930A" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-fg-muted:\s*#4B5563" internal/admin/static/css/admin.css &amp;&amp;
      # 5. Bare #FAD22D MUST NOT remain outside the two palette blocks.
      # Count #FAD22D occurrences in non-comment lines; only 2 are allowed
      # (one in :root, one in [data-theme="light"]).
      test "$(grep -v '^\s*\*\|^\s*//\|^\s*/\*' internal/admin/static/css/admin.css | grep -c '#FAD22D')" -le "2" &amp;&amp;
      # 6. New component classes exist.
      grep -q "\.otto-card--accent-healthy" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-card--accent-warning" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-card--accent-failed" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-card--accent-accent" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-badge--pill" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-btn--primary" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "\.otto-btn--secondary" internal/admin/static/css/admin.css &amp;&amp;
      # 7. Typography size tokens declared.
      grep -q "^\s*--otto-text-xs:\s*11px" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "^\s*--otto-text-xl:\s*20px" internal/admin/static/css/admin.css &amp;&amp;
      # 8. Light-mode card shadow rule present.
      grep -q "\[data-theme=\"light\"\] \.otto-card" internal/admin/static/css/admin.css &amp;&amp;
      grep -q "rgba(0, 0, 0, 0.04)" internal/admin/static/css/admin.css &amp;&amp;
      # 9. Transition rules present on body, .otto-card, .otto-header.
      # (Single shorthand `transition: ... 150ms ...` is enough; we grep for the 150ms keyword.)
      grep -c "transition:.*150ms" internal/admin/static/css/admin.css | awk '$1 >= 3 { exit 0 } { exit 1 }' &amp;&amp;
      # 10. --otto-muted alias is GONE — all callers migrated to --otto-fg-muted.
      # (Token must not appear in any non-comment line.)
      test "$(grep -v '^\s*\*\|^\s*//\|^\s*/\*' internal/admin/static/css/admin.css | grep -c -- '--otto-muted')" = "0" &amp;&amp;
      # 11. Header always-dark fg literal present.
      grep -q "background:\s*var(--otto-header)" internal/admin/static/css/admin.css &amp;&amp;
      # 12. Confirm admin.js was NOT touched (sanity — should diff clean).
      test "$(git diff --name-only -- internal/admin/static/js/admin.js)" = ""
    </automated>
  </verify>
  <done>
    - `go build ./...` and `go test ./internal/admin/...` pass.
    - `internal/admin/static/css/admin.css` `:root` block contains all 14 new dark-theme tokens (incl. --otto-card-hover, --otto-header, --otto-fg-muted, --otto-fg-dim, --otto-accent-fg) and the 5 typography size tokens.
    - `[data-theme="light"]` block contains the 14 light-theme tokens (with --otto-header = #1E2128 intentionally dark).
    - No bare `#28243D` or `#3A3A3A` literals remain in non-comment lines.
    - No more than 2 occurrences of `#FAD22D` in non-comment lines (one in :root, one in light block).
    - No `--otto-muted` references remain — fully migrated to `--otto-fg-muted`.
    - New component class blocks exist: `.otto-card--accent-healthy`, `.otto-card--accent-warning`, `.otto-card--accent-failed`, `.otto-card--accent-accent`, `.otto-badge--pill`, `.otto-btn--primary`, `.otto-btn--secondary`.
    - `body`, `.otto-card`, `.otto-header` each carry a `transition: ... 150ms ...` declaration (3+ matches in grep).
    - `[data-theme="light"] .otto-card` has a `box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);` rule.
    - `internal/admin/templates/*.tmpl` and `internal/admin/static/js/admin.js` are untouched (git diff clean for those paths).
    - Single atomic git commit lands the entire change.
  </done>
</task>

</tasks>

<verification>
Full automated verification (run from repo root):

```
go build ./...
go test ./internal/admin/...
```

(Pre-existing `go vet` errors in tail_test.go / tail_timberjack_test.go are NOT in scope and should not block this work — they were present at the baseline commit before step 1.)

Optional manual smoke (not required, but useful to confirm visual outcome):

1. `go run ./cmd/otto-gateway` then open `http://localhost:11434/admin/`.
2. Confirm DARK theme renders the new Liberty-inspired palette:
   - Near-black page bg (#1A1D24).
   - Slightly elevated card surfaces (#242832).
   - OTTO yellow underline under the active "Dashboard" tab.
   - Section headings ("Pool Slots", "Active Sessions", "Log Tail") in UPPERCASE with letter-spacing.
   - Status pill, badges, log-level chips all use the new semantic colors.
3. Click the theme toggle once → page transitions smoothly (~150ms) to the LIGHT theme:
   - Light gray page bg, white cards, dark text.
   - Header bar stays DARK (intentional Liberty parity).
   - Active tab underline is still OTTO yellow.
   - Cards have a subtle drop shadow (light-mode only).
4. Click toggle again → smoothly transitions back to dark; localStorage persists choice.
5. Visit `/admin/about` and `/admin/docs` → "Coming soon" placeholders pick up the new palette automatically (they extend the same base layout).
</verification>

<success_criteria>
- All `must_haves.truths` observable in a running build.
- All `must_haves.artifacts` exist with the documented content.
- All grep gates in `<verify><automated>` pass.
- `go build ./...` and `go test ./internal/admin/...` are green.
- Single atomic commit modifies ONLY `internal/admin/static/css/admin.css`.
- Step-1 deviation (git stash misuse) is NOT repeated.
</success_criteria>

<output>
Create `.planning/quick/260601-9je-admin-ui-redesign-step-2-palette-and-com/260601-9je-SUMMARY.md` when done.
</output>
