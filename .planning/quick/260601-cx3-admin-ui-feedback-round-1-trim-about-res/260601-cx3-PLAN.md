---
phase: 260601-cx3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/admin/templates/about.html.tmpl
  - internal/admin/templates/docs.html.tmpl
  - internal/admin/static/css/admin.css
  - internal/admin/admin.go
autonomous: true
requirements:
  - CX3-01  # About: drop Endpoints + project-links footer, keep 4 cards single-row
  - CX3-02  # Docs: drop project-links footer
  - CX3-03  # Docs CLI: rebuild around otto-gw wrapper, dual-platform table
  - CX3-04  # Docs Files & paths: surface install root + env file locations
must_haves:
  truths:
    - "/admin/about renders without an Endpoints card"
    - "/admin/about renders without a project-links footer block"
    - "/admin/about still renders all 4 remaining cards (Build info, Runtime status, Feature flags, Upstream worker)"
    - "/admin/about lays the 4 cards in a single row at desktop width and falls back to a min-280px auto-fit grid below"
    - "/admin/docs renders without a project-links footer block; page body terminates after Troubleshooting"
    - "/admin/docs CLI card shows two code blocks (POSIX + Windows/PowerShell) using only otto-gw wrapper invocations — no bare otto-gateway binary commands"
    - "/admin/docs CLI card has a 3-column table mapping POSIX flag ↔ PowerShell switch ↔ description, with — (em dash) for asymmetric rows"
    - "/admin/docs Files & paths card surfaces the install root ($HOME/.otto-gw and $env:USERPROFILE\\.otto-gw) AND the OTTO_HOME override AND the .otto-gw.env user-env file location ABOVE the existing card content"
    - "go build ./... succeeds"
    - "go test ./internal/admin/... succeeds"
  artifacts:
    - path: "internal/admin/templates/about.html.tmpl"
      provides: "Trimmed About page (4 cards, no Endpoints, no project-links footer)"
    - path: "internal/admin/templates/docs.html.tmpl"
      provides: "Restructured Docs page (otto-gw-centric CLI + install-root callout, no project-links footer)"
    - path: "internal/admin/static/css/admin.css"
      provides: "Grid rule pinning the trimmed About card row to 4 columns at desktop width"
    - path: "internal/admin/admin.go"
      provides: "aboutHandler / aboutData with unused-by-template fields removed"
  key_links:
    - from: "internal/admin/templates/about.html.tmpl"
      to: "internal/admin/admin.go (aboutData)"
      via: "template field references (.BuildInfo, .Deps, .FeatureFlags, .UpstreamWorker — exact field names depend on existing aboutData shape; executor must inspect)"
      pattern: "aboutData"
    - from: "internal/admin/templates/docs.html.tmpl"
      to: "scripts/otto-gw + scripts/otto-gw.ps1"
      via: "documented wrapper subcommand + flag surface (DOCS-ONLY — no runtime coupling)"
      pattern: "otto-gw"
---

<objective>
Round-1 admin UI feedback after v1.7.0 ship: trim the About page (drop Endpoints + project-links footer, keep 4 cards single-row), drop the Docs project-links footer, rebuild the Docs CLI section around the otto-gw wrapper (POSIX + PowerShell, dual-platform table), and surface the install root + env file locations in the Files & paths card. Plus a tiny aboutHandler / aboutData field cleanup for any fields the trimmed template no longer references.

Purpose: Operators interact with the gateway through the otto-gw wrapper, not the bare otto-gateway binary. The current docs page reflects the binary surface — this drift is the largest source of operator confusion in the round-1 feedback. The About page trim reduces visual clutter and removes the duplicate project-links block (Docs already had one too).

Output: Trimmed About template, restructured Docs template, single CSS grid rule for the About row, and an aboutHandler/aboutData with dead fields removed. Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260601-aix-admin-ui-redesign-step-4-docs-page-conte/260601-aix-SUMMARY.md
@.planning/quick/260601-aix-admin-ui-redesign-step-4-docs-page-conte/260601-aix-PLAN.md
@internal/admin/templates/about.html.tmpl
@internal/admin/templates/docs.html.tmpl
@internal/admin/admin.go
@internal/admin/static/css/admin.css
@scripts/otto-gw
@scripts/otto-gw.ps1
@scripts/install.sh
@scripts/install.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim About + restructure Docs CLI + Files & paths around the otto-gw wrapper, then clean aboutData</name>
  <files>internal/admin/templates/about.html.tmpl, internal/admin/templates/docs.html.tmpl, internal/admin/static/css/admin.css, internal/admin/admin.go</files>
  <action>
    USE REPO-RELATIVE PATHS ONLY. Resolve repo root inside the worktree with `$(git rev-parse --show-toplevel)` for verification commands; never embed absolute paths from a different working copy.

    ---

    **Pre-flight reads (single pass each — extract all needed details on the first read):**

    1. Read `internal/admin/templates/about.html.tmpl` end-to-end. Identify the Endpoints card and the project-links footer block. Identify which template field references appear ONLY inside those two regions (those become deletion candidates for aboutData).
    2. Read `internal/admin/templates/docs.html.tmpl` end-to-end. Identify (a) the project-links footer block, (b) the CLI / startup flags card region (look for the existing flag table and any startup invocation examples), (c) the Files & paths card region.
    3. Read `internal/admin/admin.go` and locate the `aboutHandler` + the type backing About data (likely a struct named `aboutData` or similar, distinct from `Deps`). DO NOT modify the `Deps` type or `docsHandler` — Docs page still uses Deps fields (per CX3 task scope, out of scope to touch them).
    4. Read `scripts/otto-gw` and extract the ACTUAL subcommand list (search for the `case "$cmd" in` / `case "$1" in` dispatch in the main body — likely `start | stop | restart | status | logs | run | env | init | upgrade-env | migrate-to-overrides | version`). Extract the ACTUAL global flag list from `parse_flags()` (e.g. `--pii`, `--hash-key`, `--entities`, `--hooks`, `--auth`, `--idle-timeout`, `--env-file`, `--overrides-file`, `--template`, `--dest`, `--overrides-dest`, `--dry-run`, `--yes`, `--show-secrets`, `--debug`, `--trace`).
    5. Read `scripts/otto-gw.ps1` and extract the ACTUAL PowerShell switch surface (PascalCase, e.g. `-Trace`, `-Debug`, `-Pii`, `-HashKey`, `-EnvFile`, `-OverridesFile`, `-DryRun`, `-Yes`). DO NOT GUESS — derive each row of the table from the actual switch definitions in this file.
    6. Read `scripts/install.sh` line 15-ish to confirm `OTTO_HOME="${OTTO_HOME:-$HOME/.otto-gw}"`.
    7. Read `scripts/install.ps1` line 18-ish to confirm the `$env:USERPROFILE\.otto-gw` default for `OTTO_HOME`.

    ---

    **Change 1 — About page (`internal/admin/templates/about.html.tmpl`):**

    - DELETE the Endpoints card block in its entirety (heading + grid item).
    - DELETE the project-links footer block in its entirety.
    - The 4 remaining cards MUST be Build info, Runtime status, Feature flags, and Upstream worker. Leave their internal content untouched.
    - Wrap (or confirm wrapping) the 4 cards in a container that uses the new CSS class `.otto-card-grid-4` (defined in Change 3 below). If the existing wrapper already uses `.otto-card` items inside an `.otto-card-grid` or similar parent, ADD `.otto-card-grid-4` as an additional class on that parent rather than replacing the existing class — the existing class likely carries shared styling beyond the column count.

    ---

    **Change 2 — Docs page (`internal/admin/templates/docs.html.tmpl`):**

    a) **Drop the project-links footer block in its entirety.** The page body terminates after Troubleshooting.

    b) **Rebuild the CLI / startup card.** Replace any existing content that invokes `otto-gateway` directly (the bare binary) with TWO code blocks using the existing `.otto-code-block` class (or the closest equivalent already used elsewhere in this template — confirm by inspection):

       - Block 1 header: `macOS / Linux (POSIX)`. Contents: example invocations using `./scripts/otto-gw <subcommand> [flags]` (one example per subcommand derived from pre-flight step 4). Use realistic flag combinations where helpful, e.g. `./scripts/otto-gw start --pii hash --hash-key <key>`. The exact subcommand list MUST come from `scripts/otto-gw`, not guessed.
       - Block 2 header: `Windows (PowerShell)`. Contents: same subcommands using `.\scripts\otto-gw.ps1 <Subcommand> [-Switch value]`. Subcommand names come from `scripts/otto-gw.ps1`. The `.bat` dispatcher just forwards args to PowerShell — same switch syntax — so do NOT add a third code block for cmd.

       Above the two code blocks, ONE short paragraph (3-4 sentences max):
       "Operators interact with the gateway through the `otto-gw` wrapper rather than the `otto-gateway` binary directly. PowerShell uses PascalCase switches (`-Trace`, `-Debug`); POSIX uses kebab-case (`--trace`, `--debug`). The `.bat` dispatcher just forwards args to PowerShell — same switch syntax."

       Below the code blocks, REPLACE any existing flat flag table with a 3-column HTML table: `POSIX flag | PowerShell switch | Description`. Populate rows by reading both wrappers (pre-flight steps 4 and 5). For each row:
       - If a flag exists on BOTH sides: list both forms.
       - If a flag exists ONLY in POSIX: PowerShell column = `—` (em dash, U+2014).
       - If a switch exists ONLY in PowerShell: POSIX column = `—`.
       - Description column: concise (one sentence) — derive from the header comment in `scripts/otto-gw` (lines 11-19 enumerate the documented config flags) where possible; for plumbing flags (`--env-file`, `--dest`, etc.) write a brief description from context.

       DO NOT leave any bare `otto-gateway <flags>` examples in the card. The word `otto-gateway` may still appear inside env-var defaults (e.g. `KIRO_CMD=kiro`, `LOG_FILE=...otto-gateway.log`) — that is fine; only standalone command invocations are forbidden.

    c) **Update the Files & paths card.** ADD a new callout BLOCK at the TOP of the card, ABOVE the existing content (chat trace file, retention, embedded-assets, log destination, etc — leave those alone). The callout uses the same heading style as other in-card sub-sections in this template (confirm by inspection); contents:

       - Sub-heading: `Install root`
         - POSIX: `$HOME/.otto-gw/` (wrapped in `.otto-code-block` or `<code>`, whichever this template uses for inline paths)
         - Windows: `$env:USERPROFILE\.otto-gw\`
         - Note: "Override either default by setting `OTTO_HOME`."
       - Sub-heading: `Env files` (loaded by the wrapper on every invocation)
         - User env: `$HOME/.otto-gw.env` (POSIX) / `$env:USERPROFILE\.otto-gw.env` (Windows)
         - Project env: `$OTTO_HOME/.env.otto-gw`
         - Project overrides: `$OTTO_HOME/.otto-gw.overrides.env`

       Existing card content (chat trace, retention, log destination) is unchanged and stays below the new callout.

    ---

    **Change 3 — CSS (`internal/admin/static/css/admin.css`):**

    Add a new rule. Place it adjacent to the existing `.otto-card` / grid-related rules (executor finds the section by grepping for `.otto-card` first to keep the rule co-located):

    ```
    .otto-card-grid-4 {
      display: grid;
      gap: 1rem;
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
    @media (max-width: 1100px) {
      .otto-card-grid-4 {
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      }
    }
    ```

    (Adjust the breakpoint to whatever the existing admin.css already uses for similar fallbacks if there is a precedent — grep for `@media` in admin.css and reuse a consistent breakpoint to avoid stylistic drift.)

    ---

    **Change 4 — Handler / data cleanup (`internal/admin/admin.go`):**

    For each template field deleted from `about.html.tmpl` in Change 1, check whether the corresponding field on `aboutData` (or whatever the About struct is named) is referenced ONLY from `about.html.tmpl`. If yes, delete the struct field AND the assignment site in `aboutHandler`. If a field is also used by Docs / Dashboard / any other template — LEAVE IT ALONE.

    Specifically (subject to executor verification):
    - Endpoint URL fields that were rendered only by the deleted Endpoints card may now be dead on aboutData. Likely candidates: `OllamaPathPrefix`, `OpenAIPathPrefix`, `AnthropicPathPrefix` IF they are mirrored from `Deps` onto aboutData. DO NOT delete them from the `Deps` type — the Docs page still references the Deps copy (per CX3 task scope).
    - Repo URL string field if any existed only for the project-links footer.

    Strictly forbidden: touching `Deps`, `docsHandler`, `dashboardHandler`, `admin.js`, or any pre-existing `go vet` warnings in `tail_test.go` / `tail_timberjack_test.go`.

    ---

    **Build & test:**

    - Run `go build ./...` from the repo root (resolve via `$(git rev-parse --show-toplevel)` inside the worktree); zero errors.
    - Run `go test ./internal/admin/...`; zero failures. Do NOT run the broader `./...` test suite — the pre-existing vet issues in `tail_*` are out of scope.

    ---

    **Single commit:** All four files in one atomic commit. NO `git stash`. NO partial commits.
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      go build ./... && \
      go test ./internal/admin/... && \
      # About page assertions
      ! grep -qi 'Endpoints' internal/admin/templates/about.html.tmpl && \
      ! grep -qiE 'project-links|project_links|projectLinks' internal/admin/templates/about.html.tmpl && \
      grep -qi 'Build info' internal/admin/templates/about.html.tmpl && \
      grep -qi 'Runtime status' internal/admin/templates/about.html.tmpl && \
      grep -qi 'Feature flags' internal/admin/templates/about.html.tmpl && \
      grep -qi 'Upstream worker' internal/admin/templates/about.html.tmpl && \
      # Docs page assertions
      ! grep -qiE 'project-links|project_links|projectLinks' internal/admin/templates/docs.html.tmpl && \
      grep -q 'PowerShell' internal/admin/templates/docs.html.tmpl && \
      grep -qE 'POSIX|macOS / Linux' internal/admin/templates/docs.html.tmpl && \
      grep -q -- '--trace' internal/admin/templates/docs.html.tmpl && \
      grep -q -- '-Trace' internal/admin/templates/docs.html.tmpl && \
      grep -qF '$HOME/.otto-gw' internal/admin/templates/docs.html.tmpl && \
      grep -qF '$env:USERPROFILE\.otto-gw' internal/admin/templates/docs.html.tmpl && \
      grep -qF 'OTTO_HOME' internal/admin/templates/docs.html.tmpl && \
      grep -qF '.otto-gw.env' internal/admin/templates/docs.html.tmpl && \
      # No bare otto-gateway invocation lines in Docs CLI section
      # (strip comments + skip env-var default lines that legitimately contain "otto-gateway")
      ! grep -nE '(^|[^a-zA-Z_/.-])otto-gateway[[:space:]]+(start|stop|--|-[a-zA-Z])' internal/admin/templates/docs.html.tmpl && \
      # CSS grid rule present
      grep -q 'otto-card-grid-4' internal/admin/static/css/admin.css && \
      grep -qE 'grid-template-columns:[[:space:]]*repeat\(4' internal/admin/static/css/admin.css && \
      # Templates compile via /admin/about + /admin/docs route registration smoke (build already covers parse)
      echo "verify ok"
    </automated>
  </verify>
  <done>
    - `internal/admin/templates/about.html.tmpl` no longer contains the Endpoints card or the project-links footer; still contains Build info, Runtime status, Feature flags, Upstream worker headings; the 4 cards are wrapped in a container carrying `.otto-card-grid-4`.
    - `internal/admin/templates/docs.html.tmpl` no longer contains a project-links footer; CLI card has POSIX + PowerShell code blocks and the 3-column flag/switch/description table; Files & paths card has the install-root + env-files callout at the top, with existing content (chat trace, retention, log destination) still present below it.
    - `internal/admin/static/css/admin.css` defines `.otto-card-grid-4` with a 4-column desktop grid and a narrower-viewport fallback.
    - `internal/admin/admin.go`: any About-only fields deleted from the template are also removed from the About data struct + assignment site, with no other handlers, Deps, dashboardHandler, or admin.js touched.
    - `go build ./...` clean; `go test ./internal/admin/...` clean.
    - Single atomic git commit covering all 4 files. No `git stash` used.
  </done>
</task>

</tasks>

<verification>
**Smoke-level manual confirmation (operator at end of execute, optional — automated verify above is the gate):**

1. Start the gateway in a dev shell, hit `/admin/about` — confirm 4 cards in a row at desktop width, no Endpoints card, no project-links footer.
2. Hit `/admin/docs` — confirm CLI card has two code blocks (POSIX + PowerShell) using only `otto-gw` wrapper invocations; flag table has 3 columns; Files & paths card opens with the install-root + env-files callout; no project-links footer at the page bottom.
</verification>

<success_criteria>
- All must_haves.truths above are observable in rendered HTML for `/admin/about` and `/admin/docs`.
- `go build ./...` exits 0.
- `go test ./internal/admin/...` exits 0.
- Single atomic commit, repo-relative paths used throughout.
</success_criteria>

<output>
Create `.planning/quick/260601-cx3-admin-ui-feedback-round-1-trim-about-res/260601-cx3-SUMMARY.md` when done.
</output>
