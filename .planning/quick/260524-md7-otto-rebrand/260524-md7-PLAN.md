---
phase: quick-260524-md7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/otto-gateway/main.go
  - cmd/otto-gateway/main_test.go
  - internal/**/*.go
  - Makefile
  - internal/config/config.go
  - .golangci.yml
  - .pre-commit-config.yaml
  - .gitignore
  - .gitattributes
  - scripts/otto
  - scripts/otto.ps1
  - scripts/setup-dev.sh
  - scripts/setup-dev.ps1
  - README.md
  - DEVELOPERS.md
  - CLAUDE.md
  - docs/**
autonomous: true
requirements: [REBRAND-01]
must_haves:
  truths:
    # Positive outcomes
    - "go build ./... is clean and produces a binary named otto-gateway via make build"
    - "ZERO loop24 BRAND matches in .go files, excluding the external loop24-client product reference: grep -rn 'loop24' --include='*.go' . | grep -v 'loop24-client' returns ZERO"
    - "./bin/otto-gateway --help header reads 'Usage of otto-gateway'"
    - "./bin/otto-gateway --version prints version and exits 0"
    - "./scripts/otto status runs (exit code reflects stopped/running, not a crash)"
    - "go vet ./... clean, go test ./... -race -count=1 green, golangci-lint run ./... 0 issues, make arch-lint exits 0"
    # Guardrails — MUST remain true
    - "Node-parity functional env var names are byte-identical (KIRO_CMD, KIRO_ARGS, KIRO_CWD, DEBUG, HTTP_ADDR, PING_INTERVAL, AUTH_TOKEN, ALLOWED_IPS, AUTH_TRUST_XFF, POOL_SIZE, OLLAMA_PATH_PREFIX, OPENAI_PATH_PREFIX, ANTHROPIC_PATH_PREFIX, ENABLED_SURFACES, EMBEDDING_MODEL_DEFAULT)"
    - "External upstream reference ../gitlab.rosetta.ericssondevops.com/loop_24/acp_server is untouched"
    - "External product reference loop24-client (the Pi-SDK / GSD Pi chat client, a separate repo) is PRESERVED verbatim in all Go comments (internal/server/server.go, internal/adapter/anthropic/render.go, render_test.go, wire.go) and docs (README.md) — it is NOT a brand string and is NOT rebranded"
    - "Repo working directory loop24-gateway/ is NOT renamed"
    - ".planning/ historical artifacts are NOT bulk-rewritten"
  artifacts:
    - path: "go.mod"
      provides: "Go module path otto-gateway"
      contains: "module otto-gateway"
    - path: "cmd/otto-gateway/main.go"
      provides: "Binary source package renamed from cmd/loop24-gateway"
    - path: "Makefile"
      provides: "BINARY=otto-gateway, LDFLAGS -X otto-gateway/internal/version, start/stop/status call ./scripts/otto"
      contains: "otto-gateway"
    - path: "scripts/otto"
      provides: "POSIX wrapper renamed from scripts/loop24 with OTTO_* env vars"
    - path: "scripts/otto.ps1"
      provides: "PowerShell wrapper renamed from scripts/loop24.ps1 with OTTO_* env vars"
  key_links:
    - from: "Makefile LDFLAGS"
      to: "otto-gateway/internal/version.Version"
      via: "go build -ldflags -X"
      pattern: "otto-gateway/internal/version.Version"
    - from: "Makefile start/stop/status"
      to: "scripts/otto"
      via: "make target shells out to wrapper"
      pattern: "\\./scripts/otto"
    - from: "internal/config/config.go LoadArgs"
      to: "otto-gateway flag usage header"
      via: "flag.NewFlagSet"
      pattern: "NewFlagSet\\(\"otto-gateway\""
---

<objective>
Mechanical Tier-2 rebrand of the project from `loop24-gateway` to `otto-gateway`:
Go module path, binary name, wrapper command, and all brand strings move to OTTO.
The repo working directory is NOT renamed (Tier 3, deferred), Node-parity env var
names are preserved byte-identical, the external `loop_24/acp_server` upstream
reference stays untouched, and the external `loop24-client` product reference (a
separate Pi-SDK / GSD Pi chat client repo) is preserved verbatim everywhere it appears.

Purpose: Adopt the OTTO Gateway brand cleanly without breaking deployment swap-in
(env-var parity), cross-compile, or the trust-gate set — and without corrupting
references to the two external assets (`loop_24/acp_server`, `loop24-client`).

Output: Renamed module/binary/wrapper, rebranded build+lint config, rebranded prose
in code and docs, OTTO_* test-harness env vars, all trust gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Current state surveyed during planning (counts are ground truth as of plan time):
#   - 87 import refs `loop24-gateway/` across 48 .go files
#   - Binary source package dir: cmd/loop24-gateway/
#   - Module path: `module loop24-gateway` (go.mod line 1)
#   - LDFLAGS: -X loop24-gateway/internal/version.Version
#   - flag.NewFlagSet("loop24-gateway", ...) in internal/config/config.go (LoadArgs, ~line 191)
#   - Wrappers: scripts/loop24 (LOOP24_BIN/PID/LOG/ADDR), scripts/loop24.ps1 (+LOOP24_LOGERR)
#   - Wrapper ADDR default localhost:11435 — rename the VAR, keep the value
#   - .gitattributes line 1 has a `loop24-gateway` brand comment (rebrand)
#
# PRESERVE — two EXTERNAL assets that look like brand strings but are NOT ours:
#   1. External upstream `loop_24/acp_server` (UNDERSCORE) lives in docs/reference/acp_wire_shapes.md
#      line 8 and CLAUDE.md — uses loop_24 not loop24, so a `loop24` grep won't catch it; do NOT touch.
#   2. External product `loop24-client` — the Pi-SDK / GSD Pi chat client, a SEPARATE repo
#      (confirmed in CLAUDE.md and README.md:151). It appears in 4+ Go comments:
#        internal/server/server.go:179, internal/adapter/anthropic/render.go:186,
#        internal/adapter/anthropic/render_test.go:136, and wire.go:98 & :293.
#      These MUST stay verbatim. EVERY `loop24` grep gate in this plan excludes it via
#      `| grep -v 'loop24-client'`, and EVERY sed targets `loop24-gateway` / the brand word
#      `Loop24`/`loop24` — anchored so `loop24-client` is never rewritten (the `-gateway`
#      suffix and the `\b` word-boundary passes do not match `loop24-client`).
#   - Skill/command files under .claude/ and .kiro/ are tooling, not brand.
#   - docs/architecture/ has loop24_activity_flow_v3.png and
#     loop24_architecture_infographic_prompt.md (rebrand filenames + the prompt's prose)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go module path rename (mechanical bulk)</name>
  <files>go.mod, cmd/otto-gateway/main.go (renamed from cmd/loop24-gateway/), all internal/**/*.go</files>
  <action>
    Rename the Go module path `loop24-gateway` -> `otto-gateway` everywhere it appears as a
    module-prefixed import path. Steps, in order:

    1. `go mod edit -module otto-gateway` (rewrites go.mod line 1 to `module otto-gateway`).
    2. Rename the binary source package dir BEFORE rewriting imports so the rewrite is consistent:
       `git mv cmd/loop24-gateway cmd/otto-gateway`.
    3. Rewrite every import path prefix across all tracked Go files. Use a scripted in-place
       sed over the git-tracked Go file list, replacing the module path prefix only
       (the trailing slash anchors it to import paths, not bare brand words — those are Task 4;
       the `loop24-gateway/` pattern structurally cannot match `loop24-client`):
       `git ls-files '*.go' | xargs sed -i '' 's|loop24-gateway/|otto-gateway/|g'`
       (Note: macOS/BSD sed requires the empty `''` arg after -i. The harness runs on darwin.)
    4. Normalize formatting after the rewrite: run `make fmt` (gofumpt if present, else gofmt -w .).
    5. `go mod tidy` to settle go.mod/go.sum.

    Do NOT touch: the working directory name, anything matching `loop_24` (underscore — that is
    the external upstream), `loop24-client` (external product, Task 4 preserves it), or bare brand
    words like "Loop24 Gateway" in comments (Task 4 owns those).
  </action>
  <verify>
    <automated>go build ./... 2>&1 && test "$(grep -rn 'loop24-gateway/' --include='*.go' . | grep -v 'loop24-client' | wc -l | tr -d ' ')" = "0" && echo OK</automated>
    Also: `grep -c '^module otto-gateway$' go.mod` returns 1; `test -d cmd/otto-gateway` true; `test ! -d cmd/loop24-gateway` true.
  </verify>
  <done>go build ./... is clean, `module otto-gateway` in go.mod, cmd/otto-gateway/ exists, cmd/loop24-gateway/ gone, and ZERO `loop24-gateway/` import refs remain in any .go file (excluding the preserved external `loop24-client` references). Atomically committable.</done>
</task>

<task type="auto">
  <name>Task 2: Build + lint config rebrand</name>
  <files>Makefile, internal/config/config.go, .golangci.yml, .pre-commit-config.yaml, .gitignore, .gitattributes</files>
  <action>
    Depends on Task 1 (LDFLAGS references the NEW module path `otto-gateway/internal/version`).

    Makefile (exact changes):
      - Line 5: `BINARY      := loop24-gateway` -> `BINARY      := otto-gateway`
      - Line 8: `LDFLAGS     := -s -w -X loop24-gateway/internal/version.Version=$(VERSION)`
        -> `... -X otto-gateway/internal/version.Version=$(VERSION)`
      - Lines 62/65/68: `@./scripts/loop24 start|stop|status` -> `@./scripts/otto start|stop|status`
      - Line 1 header comment `# Loop24 Gateway — Makefile` -> `# OTTO Gateway — Makefile`
      - `PKG := ./cmd/$(BINARY)` and cross artifact names auto-follow $(BINARY) — leave them.

    internal/config/config.go:
      - In LoadArgs (~line 191): `flag.NewFlagSet("loop24-gateway", flag.ContinueOnError)`
        -> `flag.NewFlagSet("otto-gateway", flag.ContinueOnError)`. This is what produces the
        `Usage of otto-gateway` --help header (acceptance criterion). Do NOT change any env var
        names or the package doc comment's "Node reference implementation" wording.

    .golangci.yml, .pre-commit-config.yaml, .gitignore, .gitattributes:
      - `grep -n loop24` each; rebrand any binary-name / module-path / script-path / brand-comment
        reference to the otto equivalent (e.g. `.gitattributes` line 1 header comment
        `loop24-gateway` -> `otto-gateway`; .gitignore binary-name patterns `loop24-gateway*`
        -> `otto-gateway*` if present). Preserve EVERY env-var name byte-identical.
  </action>
  <verify>
    <automated>grep -c 'BINARY      := otto-gateway' Makefile | grep -qx 1 && grep -q 'otto-gateway/internal/version.Version' Makefile && grep -q 'NewFlagSet("otto-gateway"' internal/config/config.go && test "$(grep -rni 'loop24' Makefile internal/config/config.go .golangci.yml .pre-commit-config.yaml .gitignore .gitattributes | grep -v 'loop24-client' | wc -l | tr -d ' ')" = "0" && echo OK</automated>
    Then prove the build artifact name: `make clean && make build && test -x bin/otto-gateway && echo BUILT`.
  </verify>
  <done>make build produces bin/otto-gateway; LDFLAGS points at otto-gateway/internal/version; start/stop/status call ./scripts/otto; flag set named otto-gateway; ZERO loop24 brand strings in the six config files; all Node-parity env var names unchanged. Atomically committable.</done>
</task>

<task type="auto">
  <name>Task 3: Wrapper + setup scripts rebrand</name>
  <files>scripts/otto (renamed from scripts/loop24), scripts/otto.ps1 (renamed from scripts/loop24.ps1), scripts/setup-dev.sh, scripts/setup-dev.ps1</files>
  <action>
    1. Rename: `git mv scripts/loop24 scripts/otto` and `git mv scripts/loop24.ps1 scripts/otto.ps1`.

    2. In scripts/otto (POSIX) and scripts/otto.ps1 (PowerShell), rewrite ALL of:
       - Env var names: LOOP24_BIN -> OTTO_BIN, LOOP24_PID -> OTTO_PID, LOOP24_LOG -> OTTO_LOG,
         LOOP24_ADDR -> OTTO_ADDR, LOOP24_LOGERR -> OTTO_LOGERR (ps1 only). These are wrapper-local
         convenience vars, NOT the Node-parity functional set — renaming them is correct.
       - Binary path: `./bin/loop24-gateway` -> `./bin/otto-gateway` (POSIX);
         `.\bin\loop24-gateway.exe` -> `.\bin\otto-gateway.exe` (ps1).
       - PID/log filenames: `loop24-gateway.pid` -> `otto-gateway.pid`, `loop24-gateway.log`
         -> `otto-gateway.log`, `loop24-gateway-err.log` -> `otto-gateway-err.log`.
       - All echo/Write-Host/usage prose: `loop24-gateway` -> `otto-gateway` (e.g.
         "loop24-gateway started (PID ...)", "loop24-gateway: stopped", header comments,
         `Usage: .\scripts\loop24.ps1` -> `Usage: .\scripts\otto.ps1`).
       - KEEP the ADDR default VALUE `http://localhost:11435` exactly — only the var name changes.
       Scripted approach (sed for POSIX/ps1 text is fine):
       `sed -i '' -e 's/LOOP24_/OTTO_/g' -e 's/loop24-gateway/otto-gateway/g' -e 's|scripts/loop24|scripts/otto|g' -e 's|scripts\\loop24|scripts\\otto|g' scripts/otto scripts/otto.ps1`

    3. scripts/setup-dev.sh and scripts/setup-dev.ps1: `grep -n loop24` each; rebrand any
       binary-name / script-path / brand reference to otto equivalents. Do NOT rename functional
       env vars or touch loop_24 upstream references.
  </action>
  <verify>
    <automated>test -f scripts/otto && test -f scripts/otto.ps1 && test ! -e scripts/loop24 && test ! -e scripts/loop24.ps1 && test "$(grep -rni 'loop24' scripts/ | grep -v 'loop24-client' | wc -l | tr -d ' ')" = "0" && grep -q 'localhost:11435' scripts/otto && bash -n scripts/otto && echo OK</automated>
    Functional check: `./scripts/otto status` runs without crashing (exit 1 when stopped is expected/fine).
  </verify>
  <done>scripts/otto and scripts/otto.ps1 exist (old names gone), OTTO_* env vars throughout, bin path and PID/log filenames are otto-gateway.*, prose rebranded, ADDR default value 11435 preserved, ZERO loop24 anywhere under scripts/. Atomically committable.</done>
</task>

<task type="auto">
  <name>Task 4: Brand strings, doc comments + test-harness env vars in internal/ and cmd/</name>
  <files>cmd/otto-gateway/*.go, internal/**/*.go (files with non-import-path loop24/Loop24 occurrences), internal/acp/integration_test.go, internal/adapter/anthropic/integration_test.go, internal/adapter/ollama/integration_test.go, internal/pool/pool_test.go</files>
  <action>
    Task 1 already handled module-prefix identifiers (`otto-gateway/...` imports). This task
    sweeps the REMAINING bare brand words, doc comments, the test-harness env vars, and the
    fixture string in Go source: log messages, package doc comments, prose strings,
    LOOP24_KIRO_BIN / LOOP24_INTEGRATION override knobs, and the bogus-path fixture.

    PRESERVE THROUGHOUT: `loop24-client` (external product) must stay verbatim. It appears in
    Go comments at internal/server/server.go:179, internal/adapter/anthropic/render.go:186,
    internal/adapter/anthropic/render_test.go:136, and wire.go:98 & :293. NONE of the seds below
    match it — `loop24-gateway`, `Loop24 Gateway`, `Loop24 gateway`, and the `\bLoop24\b` /
    `\bloop24\b` word-boundary passes all exclude `loop24-client` because of its `-client`
    suffix — but eyeball the diff to confirm zero `loop24-client` lines changed.

    1. Find remaining brand occurrences (exclude the preserved external product):
       `git ls-files 'cmd/**/*.go' 'internal/**/*.go' | xargs grep -in 'loop24' | grep -v 'loop24-client'`
       (after Tasks 1-2 the only hits should be brand prose like "Loop24 Gateway" /
       "Loop24 gateway" in doc comments, log lines like "starting loop24-gateway",
       the LOOP24_* test env vars, and the pool_test fixture string).

    2. Rewrite brand prose, preserving the casing convention of the surrounding text. Include the
       lowercase `Loop24 gateway` variant (WARNING 4) and a reviewed word-boundary `\bLoop24\b` pass
       to catch standalone brand words in comments at adapter.go:2, auth.go:2, canonical/*.go,
       server.go:1:
       - `Loop24 Gateway` -> `OTTO Gateway`
       - `Loop24 gateway` -> `OTTO Gateway`
       - `loop24-gateway` (in prose/log strings, not import paths) -> `otto-gateway`
       - standalone `\bLoop24\b` -> `OTTO`, standalone `\bloop24\b` -> `otto` (word boundaries
         ensure `loop24-client` and `loop24-gateway`/`loop24_*` already-handled forms are not
         re-matched by these standalone passes).
       Scripted starting point, then eyeball each diff for casing AND confirm no `loop24-client`
       line is touched:
       `git ls-files 'cmd/**/*.go' 'internal/**/*.go' | xargs grep -l 'loop24\|Loop24' | xargs sed -i '' -e 's/Loop24 Gateway/OTTO Gateway/g' -e 's/Loop24 gateway/OTTO Gateway/g' -e 's/loop24-gateway/otto-gateway/g' -e 's/\bLoop24\b/OTTO/g'`

    3. TEST-HARNESS ENV VARS (D-17 PATH-override knobs, ~21 occurrences). These are test-only
       override vars, renamed by the same logic that renames LOOP24_* in scripts. Apply over the
       four affected test files:
       internal/acp/integration_test.go, internal/adapter/anthropic/integration_test.go,
       internal/adapter/ollama/integration_test.go, internal/pool/pool_test.go:
       `sed -i '' -e 's/LOOP24_KIRO_BIN/OTTO_KIRO_BIN/g' -e 's/LOOP24_INTEGRATION/OTTO_INTEGRATION/g' <those four files>`
       Also update any D-17 reference comments that name `LOOP24_KIRO_BIN` to `OTTO_KIRO_BIN`.

    4. FIXTURE STRING (BLOCKER 3): in internal/pool/pool_test.go:204 the bogus KiroCmd path
       `"/nonexistent/binary-xyz-loop24-test"` -> `"/nonexistent/binary-xyz-otto-test"` so the
       case-sensitive .go gate passes. (Covered by a `s/binary-xyz-loop24-test/binary-xyz-otto-test/g`
       or by the standalone `\bloop24\b` pass — confirm the literal is rewritten.)

    5. Do NOT change any env-var name in the Node-parity set, the "Node reference implementation"
       wording, port numbers, `loop_24` (underscore upstream), or `loop24-client` (external product).

    6. `make fmt` afterward.
  </action>
  <verify>
    <automated>test "$(grep -rni 'loop24' --include='*.go' cmd/ internal/ | grep -v 'loop24-client' | wc -l | tr -d ' ')" = "0" && grep -rq 'OTTO_KIRO_BIN' internal/ && grep -rq 'OTTO_INTEGRATION' internal/ && test "$(grep -rn 'LOOP24_KIRO_BIN\|LOOP24_INTEGRATION' --include='*.go' . | wc -l | tr -d ' ')" = "0" && grep -q 'binary-xyz-otto-test' internal/pool/pool_test.go && go build ./... && go vet ./... && echo OK</automated>
  </verify>
  <done>ZERO case-insensitive loop24 brand strings in cmd/ and internal/ .go files (loop24-client preserved); lowercase "Loop24 gateway" and standalone Loop24 comments rebranded; LOOP24_KIRO_BIN/LOOP24_INTEGRATION renamed to OTTO_* in all four test files with D-17 comments updated; pool_test.go fixture is binary-xyz-otto-test; build and vet clean; Node-parity env vars and "Node reference implementation" wording untouched. Atomically committable.</done>
</task>

<task type="auto">
  <name>Task 5: Docs rebrand + final acceptance gate</name>
  <files>README.md, DEVELOPERS.md, CLAUDE.md, docs/README.md, docs/operating.md, docs/briefs/go_port_brief.md, docs/reference/acp_wire_shapes.md, docs/architecture/loop24_architecture_infographic_prompt.md (-> otto_*), docs/architecture/loop24_activity_flow_v3.png (-> otto_*), docs/DEVELOPERS.md</files>
  <action>
    1. Rebrand brand references AND command examples (make/scripts/bin paths) across the docs set:
       README.md, DEVELOPERS.md, CLAUDE.md, docs/README.md, docs/operating.md,
       docs/briefs/go_port_brief.md.
       - `Loop24 Gateway` -> `OTTO Gateway`; `Loop24 gateway` -> `OTTO Gateway`;
         `loop24-gateway` -> `otto-gateway`; `./scripts/loop24` -> `./scripts/otto`;
         `bin/loop24-gateway` -> `bin/otto-gateway`.
       - PRESERVE `loop24-client` (external product) — it appears in README.md:151 describing the
         Pi-SDK / GSD Pi client. Use seds anchored to `loop24-gateway` / `Loop24 Gateway` /
         `Loop24 gateway` so `loop24-client` is never matched; eyeball README.md:151 to confirm.
    2. TEST-HARNESS env-var usage examples: docs/DEVELOPERS.md (and README/operating if present)
       must update any `LOOP24_INTEGRATION=1` usage examples to `OTTO_INTEGRATION=1` and any
       `LOOP24_KIRO_BIN=...` examples to `OTTO_KIRO_BIN=...`, matching the Task 4 .go rename:
       `grep -rn 'LOOP24_INTEGRATION\|LOOP24_KIRO_BIN' docs/ README.md DEVELOPERS.md` then rewrite each hit.
    3. docs/reference/acp_wire_shapes.md: rebrand any `loop24` brand strings BUT line 8's
       `../gitlab.rosetta.ericssondevops.com/loop_24/acp_server/...` is the EXTERNAL upstream
       (underscore loop_24) — DO NOT touch it. Use a sed that only targets `loop24-gateway` /
       `Loop24 Gateway` (no underscore, has `-gateway`), which structurally cannot match `loop_24`
       or `loop24-client`:
       `sed -i '' -e 's/Loop24 Gateway/OTTO Gateway/g' -e 's/loop24-gateway/otto-gateway/g' <files>`
    4. CLAUDE.md: same — rebrand brand prose (`Loop24 Gateway` -> `OTTO Gateway`); the
       `loop_24/acp_server` external path AND the `loop24-client` external product reference both stay.
       (Renaming the marketing name "Loop24 Gateway" -> "OTTO Gateway" is expected.)
    5. Rename the two brand-named architecture assets:
       `git mv docs/architecture/loop24_architecture_infographic_prompt.md docs/architecture/otto_architecture_infographic_prompt.md`
       `git mv docs/architecture/loop24_activity_flow_v3.png docs/architecture/otto_activity_flow_v3.png`
       Then rebrand brand prose inside the .md prompt file. Update any doc that links these filenames.
    6. Do NOT bulk-rewrite .planning/ artifacts, .claude/ or .kiro/ tooling skill files, or the
       repo working directory name.

    FINAL ACCEPTANCE GATE (run after docs are done — this is the whole-rebrand sign-off):
       Run every acceptance command below and confirm each passes.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./... -race -count=1 && golangci-lint run ./... && make arch-lint && make clean && make build && test -x bin/otto-gateway && ./bin/otto-gateway --version && ./bin/otto-gateway --help 2>&1 | grep -q 'Usage of otto-gateway' && test "$(grep -rn 'loop24' --include='*.go' . | grep -v 'loop24-client' | wc -l | tr -d ' ')" = "0" && echo ALL_GREEN</automated>
    Guardrail grep (the ONLY surviving loop-name refs must be: the external loop_24 upstream, the external loop24-client product, and .claude/.kiro tooling skill files):
    `grep -rni 'loop24' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=bin | grep -v 'loop24-client'` — every remaining hit must be a `loop_24` underscore upstream reference (acp_wire_shapes.md line 8, CLAUDE.md) or a .claude/.kiro tooling skill file; ZERO `loop24` (no underscore, non-client) brand hits.
    Env-parity guardrail: `grep -E 'KIRO_CMD|KIRO_ARGS|KIRO_CWD|HTTP_ADDR|PING_INTERVAL|AUTH_TOKEN|ALLOWED_IPS|AUTH_TRUST_XFF|POOL_SIZE|OLLAMA_PATH_PREFIX|OPENAI_PATH_PREFIX|ANTHROPIC_PATH_PREFIX|ENABLED_SURFACES' internal/config/config.go | wc -l` shows all names still present.
    Functional: `./scripts/otto status` runs (stopped exit is fine).
  </verify>
  <done>All trust gates green (build/vet/test-race/golangci-lint/arch-lint); make build emits bin/otto-gateway; --help reads "Usage of otto-gateway"; --version exits 0; docs LOOP24_INTEGRATION/LOOP24_KIRO_BIN examples now OTTO_*; ZERO loop24 brand strings in .go (loop24-client preserved); repo-wide loop24 grep (minus loop24-client) shows ONLY external loop_24 upstream + tooling skill files; loop24-client preserved verbatim in README.md and Go comments; all Node-parity env vars intact; working dir not renamed; .planning/ untouched. Atomically committable.</done>
</task>

</tasks>

<verification>
Whole-phase acceptance (executed in Task 5's final gate):
- `go build ./...` clean; `go vet ./...` clean
- `go test ./... -race -count=1` green
- `golangci-lint run ./...` 0 issues; `make arch-lint` exits 0
- `make clean && make build` -> `bin/otto-gateway`
- `./bin/otto-gateway --version` prints version, exit 0
- `./bin/otto-gateway --help` header reads `Usage of otto-gateway`
- `./scripts/otto status` runs (stopped is fine)
- `grep -rn 'loop24' --include='*.go' . | grep -v 'loop24-client'` -> ZERO (external loop24-client product reference excluded)
- `grep -rni 'loop24' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=bin | grep -v 'loop24-client'`
  -> ONLY the external `loop_24/acp_server` upstream references (+ .claude/.kiro tooling skill files);
  loop24-client (external product) is preserved verbatim
</verification>

<success_criteria>
The project is rebranded to OTTO Gateway / otto-gateway across module path, binary,
wrapper command, build+lint config, Go source prose, test-harness env vars, and docs —
with all trust gates green — while preserving the two external assets and project invariants:
(1) Node-parity functional env var names stay byte-identical, (2) the external
`loop_24/acp_server` upstream reference is untouched, (3) the external `loop24-client`
product reference (Pi-SDK / GSD Pi chat client) is preserved verbatim in all Go comments
and docs, (4) the repo working directory is not renamed, and (5) .planning/ history is
not rewritten.
</success_criteria>

<output>
Create `.planning/quick/260524-md7-otto-rebrand/260524-md7-SUMMARY.md` when done.
</output>
