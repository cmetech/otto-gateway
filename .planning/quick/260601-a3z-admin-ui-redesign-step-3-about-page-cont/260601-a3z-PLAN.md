---
phase: quick-260601-a3z
plan: 01
type: execute
wave: 1
depends_on: [quick-260601-98c, quick-260601-9je]
files_modified:
  - internal/admin/admin.go
  - cmd/otto-gateway/main.go
  - internal/admin/templates/base.html.tmpl
  - internal/admin/templates/about.html.tmpl
  - internal/admin/static/css/admin.css
autonomous: true
requirements: []
tags: [admin-ui, templates, theming, runtime-cfg-surfacing]

must_haves:
  truths:
    - "GET /admin/about renders six real cards (Identity, Build, Runtime, Feature flags, Upstream worker, Endpoints) populated from server-side cfg + runtime, not 'Coming soon' placeholder."
    - "Theme toggle button in the header is icon-only (sun + moon SVGs); only the moon shows in light mode and only the sun shows in dark mode."
    - "Theme toggle preserves the existing JS click handler — clicking still flips data-theme and persists to localStorage."
    - "About page reflects actual cfg values at render time (pool size, http addr, kiro cmd, path prefixes, feature flags)."
    - "ChatTrace=true renders a visible SENSITIVE indicator on the Feature Flags card."
    - "go build ./... and go test ./internal/admin/... remain clean."
  artifacts:
    - path: internal/admin/admin.go
      provides: "Deps struct extended with 12 new runtime/cfg fields; aboutHandler populates AboutData and renders via WR-05 buffer-then-write."
      contains: "AboutData"
    - path: cmd/otto-gateway/main.go
      provides: "admin.Deps construction wires all 12 new fields from cfg.* values."
      contains: "HTTPAddr:"
    - path: internal/admin/templates/about.html.tmpl
      provides: "Six cards rendered from AboutData using existing .otto-card + new About-page grid class."
      contains: "OTTO Gateway"
    - path: internal/admin/templates/base.html.tmpl
      provides: "Theme toggle button contains inline sun + moon SVGs (no text node)."
      contains: "icon-sun"
    - path: internal/admin/static/css/admin.css
      provides: ".otto-theme-toggle SVG show/hide rules keyed on [data-theme]; .otto-about-grid layout."
      contains: "icon-sun"
  key_links:
    - from: cmd/otto-gateway/main.go
      to: internal/admin/admin.go
      via: "admin.Handler(admin.Deps{...}) call passes cfg-derived fields"
      pattern: "HTTPAddr:.*cfg\\.HTTPAddr"
    - from: internal/admin/templates/about.html.tmpl
      to: internal/admin/admin.go
      via: "template {{.field}} expansion of AboutData struct rendered by aboutHandler"
      pattern: "\\{\\{\\.HTTPAddr\\}\\}|\\{\\{\\.PoolSize\\}\\}"
    - from: internal/admin/templates/base.html.tmpl
      to: internal/admin/static/css/admin.css
      via: "[data-theme=...] selectors toggle icon-sun/icon-moon visibility"
      pattern: "\\[data-theme=\"(dark|light)\"\\] \\.otto-theme-toggle \\.icon-(sun|moon)"
---

<objective>
Step 3 of the admin UI redesign: replace the About-page placeholder with real server-side content (6 cards populated from cfg + runtime), and swap the header's text-based theme toggle for an icon-only sun/moon button. Two tightly-coupled changes — both touch base.html.tmpl/admin.css — bundled into one plan with two atomic-commit tasks split along the Go/UI boundary so the Go-side change (Deps + handler + main.go) is reviewable independently of the template/CSS change.

Purpose: Make /admin/about useful (operators can see actual runtime config without grepping env vars) and make the theme toggle visually unobtrusive (icon, not text label). Closes the only remaining "Coming soon" stub from quick-260601-98c on the About route.

Output: Six populated cards on /admin/about driven by AboutData; icon-only theme toggle in the header that responds to [data-theme] without breaking the existing JS click handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260601-98c-admin-ui-redesign-step-1-layout-extracti/260601-98c-SUMMARY.md
@.planning/quick/260601-9je-admin-ui-redesign-step-2-palette-and-com/260601-9je-SUMMARY.md
@internal/admin/admin.go
@internal/admin/templates/base.html.tmpl
@internal/admin/templates/about.html.tmpl
@internal/admin/static/css/admin.css
@internal/admin/static/js/admin.js
@internal/config/config.go
@cmd/otto-gateway/main.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend admin.Deps + aboutHandler + main.go wiring (Go side)</name>
  <files>internal/admin/admin.go, cmd/otto-gateway/main.go</files>
  <behavior>
    - After this task: `admin.Deps` has 12 new fields (HTTPAddr, PoolSize, SessionTTL, StreamIdleTimeoutSec, AuthEnabled, IPAllowlistEnabled, KiroCmd, KiroArgs, KiroCwd, OllamaPathPrefix, OpenAIPathPrefix, AnthropicPathPrefix) in addition to the existing 10.
    - `aboutHandler` constructs an `AboutData` struct that includes all 12 new Deps fields PLUS Version, Commit, GoVersion (runtime.Version()), GOOS, GOARCH, Debug, ChatTrace, Start (formatted), and TabActive/PageTitle.
    - `cmd/otto-gateway/main.go` at the existing `admin.Handler(admin.Deps{...})` call site passes the 12 new fields sourced from `cfg.*`. Executor MUST first verify exact cfg field names from internal/config/config.go — known names from the read pass: HTTPAddr, PoolSize, SessionTTL, StreamIdleTimeoutSec, AuthToken (slice — AuthEnabled = len > 0), AllowedIPs (slice — IPAllowlistEnabled = len > 0), KiroCmd, KiroArgs, KiroCWD (note: Go field is KiroCWD not KiroCwd), OllamaPathPrefix, OpenAIPathPrefix, AnthropicPathPrefix.
    - `go build ./...` passes.
    - `go test ./internal/admin/...` passes.
    - TRST-04 preserved: the ONLY new import in internal/admin/admin.go is stdlib `runtime` (already allowed under TRST-04 — stdlib is whitelisted). No new imports anywhere else in the package.
  </behavior>
  <action>
    Step 1 — Extend internal/admin/admin.go:

    (a) Add 12 fields to the `Deps` struct (place after the existing ChatTrace field, before the closing brace). Use these exact Go field names and types:

        HTTPAddr             string
        PoolSize             int
        SessionTTL           time.Duration
        StreamIdleTimeoutSec int
        AuthEnabled          bool
        IPAllowlistEnabled   bool
        KiroCmd              string
        KiroArgs             []string
        KiroCwd              string
        OllamaPathPrefix     string
        OpenAIPathPrefix     string
        AnthropicPathPrefix  string

    Add a brief doc-comment block above the new field block explaining that these mirror cfg.* values surfaced on the /admin/about page (quick 260601-a3z, step 3 of admin UI redesign).

    (b) Add `"runtime"` to the import block (stdlib — TRST-04 allows it).

    (c) Rewrite `aboutHandler` to build an `AboutData` struct (declare it as an unexported package-level type OR an inline anonymous struct — executor's call; package-level type is cleaner if About-related helpers grow later). AboutData fields:

        TabActive            string  // "about"
        PageTitle            string  // "About"
        Version              string  // h.deps.Version
        Commit               string  // h.deps.Commit
        GoVersion            string  // runtime.Version()
        GOOS                 string  // runtime.GOOS
        GOARCH               string  // runtime.GOARCH
        StartedAt            string  // h.deps.Start.Format(time.RFC3339)
        HTTPAddr             string
        PoolSize             int
        SessionTTL           string  // h.deps.SessionTTL.String() — render as "30m0s" not raw ns
        StreamIdleTimeoutSec int
        StreamIdleDisplay    string  // "disabled" when StreamIdleTimeoutSec==0, else fmt.Sprintf("%ds", n)
        AuthEnabled          bool
        IPAllowlistEnabled   bool
        Debug                bool    // h.deps.Debug
        ChatTrace            bool    // h.deps.ChatTrace
        KiroCmd              string  // h.deps.KiroCmd OR "(unset — degraded mode)" when empty
        KiroArgs             string  // strings.Join(h.deps.KiroArgs, " ") OR "(none)" when empty
        KiroCwd              string  // h.deps.KiroCwd OR "(empty)" when empty
        OllamaPathPrefix     string
        OpenAIPathPrefix     string
        AnthropicPathPrefix  string

    For the empty-string fallbacks (KiroCmd/KiroArgs/KiroCwd), do the substitution in the handler (NOT in the template) so the template stays presentation-only. Add `"strings"` to imports if not already present.

    Keep the existing WR-05 buffer-then-write pattern verbatim — only the `data` struct construction changes.

    Step 2 — Wire cmd/otto-gateway/main.go:

    Locate the existing `admin.Handler(admin.Deps{ ... })` call (around line 578-589 in the read pass). Add the 12 new field assignments inside the Deps literal. Use the EXACT cfg field names verified by re-reading internal/config/config.go — the read pass shows:

        HTTPAddr:             cfg.HTTPAddr,
        PoolSize:             cfg.PoolSize,
        SessionTTL:           cfg.SessionTTL,
        StreamIdleTimeoutSec: cfg.StreamIdleTimeoutSec,
        AuthEnabled:          len(cfg.AuthToken) > 0,
        IPAllowlistEnabled:   len(cfg.AllowedIPs) > 0,
        KiroCmd:              cfg.KiroCmd,
        KiroArgs:             cfg.KiroArgs,
        KiroCwd:              cfg.KiroCWD,
        OllamaPathPrefix:     cfg.OllamaPathPrefix,
        OpenAIPathPrefix:     cfg.OpenAIPathPrefix,
        AnthropicPathPrefix:  cfg.AnthropicPathPrefix,

    Note: cfg.KiroCWD is the Go field name (all-caps CWD per Go convention in this codebase — verify before writing). admin.Deps.KiroCwd is mixed-case per the new Deps spec above.

    Step 3 — Commit as a single atomic commit on the Go-side change with subject `refactor(quick-260601-a3z): extend admin.Deps with runtime cfg fields + AboutData (step 3 part 1)`.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./... && go test ./internal/admin/... -count=1</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -c 'HTTPAddr.*cfg\.HTTPAddr' cmd/otto-gateway/main.go | grep -qE '^[1-9]'</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E 'HTTPAddr|PoolSize|SessionTTL|StreamIdleTimeoutSec|AuthEnabled|IPAllowlistEnabled|KiroCmd|KiroArgs|KiroCwd|OllamaPathPrefix|OpenAIPathPrefix|AnthropicPathPrefix' internal/admin/admin.go | grep -v '^//' | wc -l | awk '$1 >= 12 { exit 0 } { exit 1 }'</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '"runtime"' internal/admin/admin.go</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && ! grep -E 'import.*"otto-gateway/internal/(pool|session|server|engine|adapter|config|plugin|canonical)"' internal/admin/admin.go</automated>
  </verify>
  <done>
    - `Deps` has the 12 new fields with correct types.
    - `aboutHandler` builds `AboutData` populated from `h.deps.*` + runtime.* + computed display strings (StreamIdleDisplay, KiroCmd/Args/Cwd fallbacks).
    - `cmd/otto-gateway/main.go` admin.Handler call wires all 12 fields from cfg.* (with len() bool derivation for AuthEnabled / IPAllowlistEnabled).
    - go build + go test (./internal/admin/...) clean.
    - TRST-04 preserved (no new imports outside stdlib + chi + internal/version; runtime is stdlib).
    - Single atomic commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: About page template + icon theme toggle + CSS (UI side)</name>
  <files>internal/admin/templates/base.html.tmpl, internal/admin/templates/about.html.tmpl, internal/admin/static/css/admin.css</files>
  <behavior>
    - Theme toggle button in base.html.tmpl contains TWO inline SVGs (sun + moon, classes `icon-sun` and `icon-moon`) and no text node beyond the SVGs. `aria-label="Toggle theme"` and `title="Toggle theme"` preserved. The `data-theme-toggle` attribute and the existing inline JS click handler at the bottom of base.html.tmpl are preserved verbatim — that handler does NOT need to know about the icon swap, the CSS rules drive visibility.
    - In dark mode (data-theme="dark"), only the sun icon is visible (clicking switches to light).
    - In light mode (data-theme="light"), only the moon icon is visible (clicking switches to dark).
    - About page renders an Identity wordmark + tagline row, followed by a 6-card grid: Build, Runtime status, Feature flags, Upstream worker, Endpoints, Project links (links card is small footer-style — counts as the 6th tile in the grid, not a separate row).
    - Feature flags card shows AUTH / IP ALLOWLIST / DEBUG / CHAT-TRACE chips; when ChatTrace is true, a visible "SENSITIVE" indicator renders (use the existing `.otto-badge.is-warning` class for warning-orange treatment).
    - `go build ./...` passes (template is parsed via embed.FS at startup, so a parse failure surfaces immediately).
    - `go test ./internal/admin/...` passes.
  </behavior>
  <action>
    Step 1 — Edit internal/admin/templates/base.html.tmpl:

    Replace the inner content of the `<button class="otto-theme-toggle" ...>` element. Current line 14 reads:

        <button type="button" class="otto-theme-toggle" data-theme-toggle aria-label="Toggle theme">Theme</button>

    Replace with (one element, two SVG children, no text — keep the data-theme-toggle attr exactly as-is so the existing JS handler at line 23 still binds to it):

        <button type="button" class="otto-theme-toggle" data-theme-toggle aria-label="Toggle theme" title="Toggle theme"><svg class="icon-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z"/></svg><svg class="icon-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z"/></svg></button>

    Do NOT touch the inline `<script>` block at line 23 — the existing click handler queries `[data-theme-toggle]` which still matches.

    Step 2 — Replace internal/admin/templates/about.html.tmpl:

    Discard the entire current placeholder content. Write a new template that defines "content" with:

    (a) An Identity row (full-width, NOT inside the grid) — a section element with class `otto-card otto-about-identity` containing an `<h1 class="otto-about-wordmark">OTTO Gateway</h1>` and a `<p class="otto-about-tagline">LLM gateway with unified OpenAI / Ollama / Anthropic surfaces</p>`.

    (b) A grid container `<section class="otto-about-grid">` holding six `<article class="otto-card">` cards. For each card use an `<h2 class="otto-h2">` heading and a `<dl class="otto-about-dl">` definition list (dt = label, dd = value) for the row data. Empty/zero values should still render (the handler does the empty-fallback substitution).

    Card 1 — BUILD INFO:
      - Version: {{.Version}}
      - Commit: {{.Commit}}
      - Go runtime: {{.GoVersion}}
      - GOOS / GOARCH: {{.GOOS}} / {{.GOARCH}}

    Card 2 — RUNTIME STATUS:
      - Started at: {{.StartedAt}}
      - Listen address: {{.HTTPAddr}}
      - Pool size: {{.PoolSize}}
      - Session TTL: {{.SessionTTL}}
      - Stream idle timeout: {{.StreamIdleDisplay}}

    Card 3 — FEATURE FLAGS:
      - Auth: {{if .AuthEnabled}}on{{else}}off{{end}}
      - IP allowlist: {{if .IPAllowlistEnabled}}on{{else}}off{{end}}
      - Debug: {{if .Debug}}on{{else}}off{{end}}
      - Chat trace: {{if .ChatTrace}}on <span class="otto-badge is-warning">SENSITIVE</span>{{else}}off{{end}}

    Card 4 — UPSTREAM WORKER:
      - KIRO_CMD: {{.KiroCmd}}
      - KIRO_ARGS: {{.KiroArgs}}
      - KIRO_CWD: {{.KiroCwd}}

    Card 5 — ENDPOINTS:
      - Ollama prefix: {{.OllamaPathPrefix}}
      - OpenAI prefix: {{.OpenAIPathPrefix}}
      - Anthropic prefix: {{.AnthropicPathPrefix}}
      - Admin: /admin/, /admin/api/snapshot, /admin/logs/stream

    Card 6 — PROJECT LINKS:
      - Repository: <a href="https://github.com/cmetech/otto_app">github.com/cmetech/otto_app</a> <!-- TODO verify canonical URL with project owner; placeholder per source PROJECT.md reference -->
      - Documentation: <a href="/admin/docs">/admin/docs</a>

    Wrap everything in `{{define "content"}}<main class="otto-page"> ... </main>{{end}}` and end the file with `{{template "base" .}}` (matches existing dashboard/docs template structure).

    Step 3 — Append to internal/admin/static/css/admin.css (do NOT touch existing rules; append in a new commented block at the end of the file under a `Quick 260601-a3z — admin UI redesign step 3` banner):

    (a) Theme-toggle icon swap + button refresh (REPLACES the look but not the selector contract of the existing .otto-theme-toggle rules — append wins via cascade order):

        .otto-theme-toggle {
          width: 32px;
          height: 32px;
          padding: 0;
          background: transparent;
          border: none;
          border-radius: 6px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          color: inherit;
          transition: background 150ms, transform 100ms;
        }
        .otto-theme-toggle:hover {
          background: var(--otto-card-hover);
        }
        .otto-theme-toggle:active {
          transform: scale(0.95);
        }
        .otto-theme-toggle svg {
          width: 20px;
          height: 20px;
        }
        .otto-theme-toggle .icon-sun,
        .otto-theme-toggle .icon-moon {
          display: none;
        }
        [data-theme="dark"] .otto-theme-toggle .icon-sun {
          display: block;
        }
        [data-theme="light"] .otto-theme-toggle .icon-moon {
          display: block;
        }

    (b) About page grid + typography (the wordmark intentionally uses a larger font than --otto-text-xl since it's the page banner):

        .otto-about-identity {
          text-align: center;
          padding: var(--otto-space-xl);
        }
        .otto-about-wordmark {
          font-size: 32px;
          font-weight: 700;
          letter-spacing: 0.04em;
          margin: 0 0 var(--otto-space-sm) 0;
          color: var(--otto-fg);
        }
        .otto-about-tagline {
          margin: 0;
          color: var(--otto-fg-muted);
          font-size: var(--otto-text-base);
        }
        .otto-about-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
          gap: var(--otto-space-base);
        }
        .otto-about-dl {
          margin: 0;
          display: grid;
          grid-template-columns: max-content 1fr;
          gap: var(--otto-space-xs) var(--otto-space-base);
          font-size: var(--otto-text-sm);
        }
        .otto-about-dl dt {
          color: var(--otto-fg-muted);
          font-weight: 400;
          white-space: nowrap;
        }
        .otto-about-dl dd {
          margin: 0;
          color: var(--otto-fg);
          word-break: break-word;
        }
        .otto-about-dl dd a {
          color: var(--otto-link);
          text-decoration: none;
        }
        .otto-about-dl dd a:hover {
          text-decoration: underline;
        }

    Step 4 — Commit as a single atomic commit on the UI-side change with subject `feat(quick-260601-a3z): About page real content + icon theme toggle (step 3 part 2)`. NO git stash. NO --no-verify.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./... && go test ./internal/admin/... -count=1</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -c '<svg class="icon-sun"' internal/admin/templates/base.html.tmpl | grep -qE '^[1-9]'</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -c '<svg class="icon-moon"' internal/admin/templates/base.html.tmpl | grep -qE '^[1-9]'</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep 'aria-label="Toggle theme"' internal/admin/templates/base.html.tmpl</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && ! grep -E '>Theme<' internal/admin/templates/base.html.tmpl</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '\[data-theme="dark"\] \.otto-theme-toggle \.icon-sun' internal/admin/static/css/admin.css</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '\[data-theme="light"\] \.otto-theme-toggle \.icon-moon' internal/admin/static/css/admin.css</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '\.otto-about-grid' internal/admin/static/css/admin.css</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '\{\{\.HTTPAddr\}\}' internal/admin/templates/about.html.tmpl</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep -E '\{\{\.PoolSize\}\}' internal/admin/templates/about.html.tmpl</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && grep 'KIRO_CMD' internal/admin/templates/about.html.tmpl</automated>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && ! grep 'Coming soon' internal/admin/templates/about.html.tmpl</automated>
    <human-check>Run the gateway locally, navigate to http://127.0.0.1:18080/admin/about — confirm 6 cards render, values look correct, theme toggle button shows a single icon (sun in dark mode, moon in light mode), clicking flips theme + swaps icon, no text label visible on the button.</human-check>
  </verify>
  <done>
    - base.html.tmpl theme-toggle button contains inline sun + moon SVGs with classes icon-sun and icon-moon, no inner text node, aria-label preserved, data-theme-toggle attr preserved (existing JS handler still works).
    - admin.css has the show/hide rules keyed on [data-theme="dark"|"light"] + icon-only button styling (32x32, transparent, hover bg, scale-on-active).
    - about.html.tmpl renders the Identity row + 6-card grid populated from AboutData fields; ChatTrace=true case shows SENSITIVE badge; KIRO_CMD etc. labels visible.
    - admin.css has .otto-about-grid + .otto-about-dl + .otto-about-wordmark/tagline/identity rules.
    - go build + go test (./internal/admin/...) clean.
    - Single atomic commit landed.
    - Human verification confirms visual correctness (theme toggle visually icon-only with correct swap behavior; About page renders the 6 cards with real values).
  </done>
</task>

</tasks>

<verification>
End-to-end after both tasks:

1. `cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./... && go test ./internal/admin/... -count=1` — both clean.
2. Boot the gateway (e.g. `./scripts/otto-gw run` or `go run ./cmd/otto-gateway`).
3. `curl -s http://127.0.0.1:18080/admin/about` — response body MUST contain ALL of:
   - "OTTO Gateway"
   - "LLM gateway with unified"
   - "Version"
   - "Pool size"
   - "KIRO_CMD"
   - At least one numeric cfg value (e.g. `4` for default pool size, `:18080` for default HTTP_ADDR, or `30m0s` for default session TTL)
   - `<svg class="icon-sun"` AND `<svg class="icon-moon"` (via base.html.tmpl)
   - `aria-label="Toggle theme"`
4. `curl -s http://127.0.0.1:18080/admin/about | grep -E '>Theme<'` — MUST return zero matches (text label removed from the button — `title="Toggle theme"` attribute is OK because that's not a text node inside the button).
5. Open /admin/about in a browser and toggle the theme button — sun icon should show in dark mode, moon in light mode, no other text inside the button.

Constraints recap:
- NO `git stash`.
- NO new imports in internal/admin/admin.go outside stdlib + chi + internal/version. (stdlib `runtime` and `strings` are stdlib — allowed.)
- NO `go vet` fix attempts for the pre-existing tail_test.go / tail_timberjack_test.go Go-1.24/1.23 errors — out of scope.
- Two atomic commits preferred (one per task); single combined commit is acceptable but the Go/UI split is cleaner.
</verification>

<success_criteria>
1. `GET /admin/about` returns 200 with the 6 cards rendered from real cfg + runtime values (no placeholder text).
2. Header theme toggle is icon-only — sun + moon SVGs, no text node, aria-label preserved, click handler still works via existing data-theme-toggle binding.
3. CSS show/hide rules drive icon visibility off `[data-theme="dark|light"]` on `<html>`.
4. admin.Deps gains 12 new fields; main.go wires them from cfg.* (with len() bool derivation for AuthEnabled and IPAllowlistEnabled).
5. TRST-04 preserved (no forbidden imports added to internal/admin).
6. go build ./... + go test ./internal/admin/... both clean.
7. ChatTrace=true renders the visible SENSITIVE badge on the Feature Flags card.
8. KIRO_CMD empty case renders "(unset — degraded mode)" placeholder text (substitution done in handler, not template).
</success_criteria>

<output>
Create `.planning/quick/260601-a3z-admin-ui-redesign-step-3-about-page-cont/260601-a3z-SUMMARY.md` when done.
</output>
