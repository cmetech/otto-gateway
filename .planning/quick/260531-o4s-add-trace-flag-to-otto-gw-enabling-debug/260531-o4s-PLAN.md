---
phase: quick-260531-o4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/otto-gw
autonomous: true
requirements:
  - QUICK-260531-o4s
must_haves:
  truths:
    - "Running `otto-gw start --trace` exports both DEBUG=true and CHAT_TRACE=true into the gateway process environment"
    - "--trace is documented in usage() alongside --debug under 'Gateway config flags'"
    - "--trace is rejected as an init flag (not listed under 'init flags' section)"
  artifacts:
    - path: "scripts/otto-gw"
      provides: "FLAG_TRACE global + apply_cli_flags branch + parse_flags case + usage() line"
      contains: "FLAG_TRACE"
  key_links:
    - from: "parse_flags() --trace case"
      to: "FLAG_TRACE=1 global"
      via: "case statement assignment"
      pattern: "--trace\\)"
    - from: "apply_cli_flags() branch"
      to: "export CHAT_TRACE + export DEBUG"
      via: "FLAG_TRACE guard"
      pattern: "FLAG_TRACE.*-eq 1"
---

<objective>
Add a `--trace` runtime flag to scripts/otto-gw that exports both DEBUG=true and
CHAT_TRACE=true when passed, mirroring the existing `--debug` flag pattern exactly.

Purpose: Operators running start/restart/run need a single flag to enable full
observability (debug-level logging + chat-trace NDJSON hook) without manually setting
two env vars. config.go:349-350 already auto-prepends ChatTraceHook to ENABLED_HOOKS
when CHAT_TRACE=true, so exporting the two env vars is sufficient.

Output: scripts/otto-gw with three mechanical additions — one FLAG_TRACE case in
parse_flags(), one branch in apply_cli_flags(), one line in usage().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --trace flag to scripts/otto-gw (parse + apply + document)</name>
  <files>scripts/otto-gw</files>
  <action>
Three targeted edits to scripts/otto-gw. Do NOT modify the .ps1 variant, .bat
variant, config.go, or any init-flag code paths.

Edit 1 — parse_flags() case block (currently ends at `--debug) FLAG_DEBUG=1; shift ;;`
around line 210):

Add the --trace case immediately after the --debug line:

  --debug)         FLAG_DEBUG=1; shift ;;
  --trace)         FLAG_TRACE=1; shift ;;

Edit 2 — apply_cli_flags() (currently ends with the FLAG_DEBUG branch around line 185):

Add a FLAG_TRACE branch immediately after the FLAG_DEBUG branch:

  if [[ "${FLAG_DEBUG:-0}" -eq 1 ]]; then export DEBUG="true"; fi
  if [[ "${FLAG_TRACE:-0}" -eq 1 ]]; then export DEBUG="true"; export CHAT_TRACE="true"; fi

Note: --trace implies --debug (exports DEBUG=true as well), so passing --trace alone is
sufficient for full observability. An operator may pass both --debug and --trace; that is
harmless (DEBUG is exported twice to the same value).

Edit 3 — usage() (line ~911, the --debug documentation line):

Add the --trace line immediately after the --debug line:

  --debug             DEBUG=true (debug-level logging) for start | restart | run
  --trace             DEBUG=true + CHAT_TRACE=true (debug + chat-trace NDJSON) for start | restart | run

Alignment: match the column alignment of the surrounding flag lines (two-space indent,
flag left-justified, description begins at the same column as other flag descriptions).

No other changes. Do not touch init flags, .env.otto-gw.example, DEVELOPERS.md, or
any Go source files.
  </action>
  <verify>
    <automated>grep -n "FLAG_TRACE" /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway/scripts/otto-gw</automated>
  </verify>
  <done>
- grep FLAG_TRACE returns exactly 3 hits: the parse_flags case assignment, the apply_cli_flags guard, and the FLAG_TRACE variable reference inside the apply branch (export lines).
- grep "CHAT_TRACE" scripts/otto-gw returns the apply_cli_flags export line and the usage() documentation line (2 hits minimum).
- shellcheck scripts/otto-gw exits 0 (run: shellcheck scripts/otto-gw).
- The --debug line in usage() is immediately followed by the --trace line with consistent column alignment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → wrapper env | Flag value is boolean (no user-supplied string); no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o4s-01 | Information Disclosure | CHAT_TRACE=true exports raw user content to NDJSON log | accept | Operator opt-in flag; existing --chat-trace init behavior has same posture; sensitive data warning already in init flag docs |
</threat_model>

<verification>
After applying the three edits:

1. `grep -c "FLAG_TRACE" scripts/otto-gw` returns >= 3
2. `grep "CHAT_TRACE" scripts/otto-gw` shows both the export line and the usage doc line
3. `shellcheck scripts/otto-gw` exits 0
4. Manual spot-check: `bash -c 'source scripts/otto-gw; FLAG_TRACE=1; apply_cli_flags; echo "DEBUG=$DEBUG CHAT_TRACE=$CHAT_TRACE"'` prints `DEBUG=true CHAT_TRACE=true` (optional sanity check)
</verification>

<success_criteria>
- scripts/otto-gw parse_flags() recognises --trace and sets FLAG_TRACE=1
- scripts/otto-gw apply_cli_flags() exports DEBUG=true and CHAT_TRACE=true when FLAG_TRACE=1
- usage() documents --trace under "Gateway config flags (for start | restart | run | env)"
- shellcheck scripts/otto-gw exits 0
- No other files modified
</success_criteria>

<output>
Create `.planning/quick/260531-o4s-add-trace-flag-to-otto-gw-enabling-debug/260531-o4s-SUMMARY.md` when done
</output>
