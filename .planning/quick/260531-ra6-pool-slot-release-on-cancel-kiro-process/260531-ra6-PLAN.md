---
quick_id: 260531-ra6
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/pool/pool_test.go
  - internal/pool/pool.go
  - internal/acp/client.go
  - internal/acp/pool_pgid_unix.go
  - internal/acp/pool_pgid_windows.go
  - scripts/otto-gw
  - scripts/otto-gw.ps1
  - .planning/quick/260531-ra6-pool-slot-release-on-cancel-kiro-process/260531-ra6-SUMMARY.md
autonomous: true
requirements:
  - RA6-01  # Pool slot released as part of Cancel contract (watchdog-cancel path)
  - RA6-02  # kiro-cli children placed in own process group + group-signaled
  - RA6-03  # otto-gw stop wrapper reaps surviving $KIRO_CMD orphans (POSIX + PS)

must_haves:
  truths:
    - "When Pool.Cancel(sid) is called for an active session that has NOT been drained, the owning slot returns to p.slots (slot.busy effectively false) before Cancel returns."
    - "On Unix (darwin/linux), kiro-cli children spawned by *acp.Client run in their own process group; signaling that PGID kills the child + any of its grandchildren."
    - "On gateway SIGTERM, Go's exec.CommandContext SIGKILL of kiro-cli is delivered to the process group, not just the leader."
    - "After `scripts/otto-gw stop` returns, no process whose executable path equals $KIRO_CMD remains owned by the user (POSIX)."
    - "After `scripts/otto-gw.ps1 stop` returns, no Win32_Process whose Path equals $env:KIRO_CMD remains (Windows)."
  artifacts:
    - path: "internal/pool/pool_test.go"
      provides: "Regression test asserting slot release on Pool.Cancel without Result drain"
      contains: "TestPool_Cancel_ReleasesSlot_WithoutResultDrain"
    - path: "internal/acp/pool_pgid_unix.go"
      provides: "//go:build darwin || linux — SysProcAttr.Setpgid + group-kill helper"
      contains: "Setpgid"
    - path: "internal/acp/pool_pgid_windows.go"
      provides: "//go:build windows — no-op stubs preserving cross-compile"
      contains: "build windows"
  key_links:
    - from: "engine.go watchdog (context.AfterFunc → e.cfg.ACP.Cancel)"
      to: "Pool.Cancel → releaseSlotForSession → p.slots <- slot"
      via: "engine.ACPClient interface"
      pattern: "Cancel\\(sid\\)"
    - from: "acp.New (exec.CommandContext)"
      to: "cmd.SysProcAttr (Setpgid on unix, zero on windows)"
      via: "build-tagged setter applied just before cmd.Start()"
      pattern: "applyPgidAttr|SysProcAttr"
    - from: "scripts/otto-gw stop (POSIX) / Stop-Gateway (PS)"
      to: "ps/Win32_Process scan filtered by $KIRO_CMD path"
      via: "post-exit reap with 2s grace then SIGKILL / Stop-Process -Force"
      pattern: "KIRO_CMD"
---

<objective>
Land three small, related fixes for kiro-cli lifecycle hygiene as ONE quick task with three atomic commits in the order Issue 1 → Issue 2 → Issue 3. Issue 1 fixes the symptom the operator sees (Admin UI showing slot-N permanently Busy after a watchdog cancel). Issues 2 and 3 are belt-and-suspenders defenses against orphaned kiro-cli subprocesses on shutdown (process-group adoption fix + wrapper-script reap).

Purpose:
- Restore the invariant "Pool.Cancel(sid) releases sid's slot" promised by the comments on Pool.Cancel and the Codex M-3 contract. Watchdog-driven cancels currently leave the slot busy in the operator's view.
- Stop orphaning kiro-cli when the gateway exits (SIGTERM / crash).
- Give operators a clean `otto-gw stop` that does not leave stray subprocesses behind even on a hard crash.

Output:
- 1 new regression test (Issue 1 only — Issues 2/3 are hard to unit test cleanly; SUMMARY.md documents the manual reproducer).
- Updated pool.go (Issue 1 fix).
- New build-tagged files in internal/acp (Issue 2): pool_pgid_unix.go + pool_pgid_windows.go.
- Updated client.go spawn site (Issue 2 wiring).
- Updated scripts/otto-gw and scripts/otto-gw.ps1 stop logic (Issue 3).
- SUMMARY.md with manual reproducer steps for Issues 2 and 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Issue 1 — pool.Cancel slot-release fix
@internal/pool/pool.go
@internal/engine/engine.go
@internal/engine/acp_adapter.go

# Issue 2 — process group on spawn
@internal/acp/client.go

# Issue 3 — wrapper reap
@scripts/otto-gw
@scripts/otto-gw.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Pool.Cancel slot release + add regression test</name>
  <files>
    internal/pool/pool.go,
    internal/pool/pool_test.go
  </files>
  <behavior>
    Failing test FIRST (RED), then minimal fix (GREEN). Behavior under test:
    - Construct a Pool of size 1 using the existing fakeClient/fakeFactory test pattern (mirror TestPool_Cancel_ReleasesSlot at internal/pool/pool_test.go:782 and TestPool_Prompt_SessionRoutesToSlot at :405).
    - Warmup the pool, then drive: NewSession → Prompt → DO NOT range stream.Chunks() → DO NOT call stream.Result().
    - Call p.Cancel(sid) and wait briefly for the release goroutine path to run.
    - Assert via p.Stats() that Busy == 0 and Alive == 1 within a short (e.g. 250ms) bounded wait. Use existing test helpers if they exist; otherwise poll Stats with `require.Eventually`.
    - Test name: TestPool_Cancel_ReleasesSlot_WithoutResultDrain. Must be distinct from the existing TestPool_Cancel_ReleasesSlot (which presumably drains Result — keep that test untouched).

    Existing Cancel implementation in pool.go (lines 628-645) ALREADY calls releaseSlotForSession. Investigation from 260531-oox observed that the watchdog-driven cancel path does not produce a pool.release log. Possible root causes the executor must verify and fix:
      (a) Pool.Cancel's releaseSlotForSession check sees the sessionSlots entry already deleted (e.g. by a concurrent watcher) and the no-op branch hits without releasing.
      (b) The poolStreamWrapper's ctx-watcher fires releaseOnce on the request ctx Done, which sets the map-delete-first marker, and Pool.Cancel then sees `!ok` and silently returns without releasing — but the ctx-watcher's release() closure DOES send slot back to p.slots, so this would not leak. Re-derive the actual race and fix it.
      (c) The acp_adapter Cancel(sid) path: production wires Pool as engine.ACPClient (engine.go:34 + cmd/otto-gateway main), so engine watchdog SHOULD hit Pool.Cancel — confirm by reading cmd/otto-gateway/main.go (or wherever engine.Config is constructed) that the ACP field is the *Pool, NOT a NewACPClientAdapter(*acp.Client). If it is wired to the adapter, the slot is owned by the pool but Cancel is delegated to the bare acp.Client which does not know about slots — the bug is the wiring, not Cancel's body. Fix at the correct layer.

    Whichever root cause applies, the fix MUST preserve:
      - The Codex M-3 "exactly-one release" contract (sync.Once + map-delete-first).
      - The WR-04 "capture Client under p.mu" pattern.
      - No new goroutines, no new sync primitives.

    Do NOT touch: poolStreamWrapper, Prompt's ctx-watcher goroutine, releaseSlotForSession's signature, or any acp_adapter code beyond what is necessary to ensure watchdog Cancels hit the Pool's Cancel.

    Commit message format:
      fix(pool): release slot on Cancel(sid) so watchdog-cancelled sessions free their slot (260531-ra6)

      The engine watchdog (engine.go:207 context.AfterFunc) calls
      ACPClient.Cancel(sid) when a client times out at 120s. The pool's
      Cancel implementation was supposed to release the slot via
      releaseSlotForSession but [root cause]. Result: Admin UI showed
      slot-N permanently Busy after a watchdog cancel.

      Adds TestPool_Cancel_ReleasesSlot_WithoutResultDrain as the
      regression covering the Cancel-without-drain path.
  </behavior>
  <action>
    Step 1 (RED): write TestPool_Cancel_ReleasesSlot_WithoutResultDrain in internal/pool/pool_test.go matching the existing fakeClient harness shape (see TestPool_Cancel_ReleasesSlot at :782 and TestPool_Prompt_ErrorReleasesSlot at :489 for the existing patterns). Run `go test ./internal/pool/... -run TestPool_Cancel_ReleasesSlot_WithoutResultDrain -count=1` and confirm it FAILS (the slot is still owned).

    Step 2 (DIAGNOSE): read cmd/otto-gateway main.go (or wherever engine.Config.ACP is wired) and confirm whether the engine's ACPClient is the *Pool. If wired to an adapter that delegates Cancel to the bare *acp.Client without releasing the slot, the fix is at the wiring site — change main.go to pass the Pool. If wired to Pool already, root cause is inside Pool.Cancel — fix Pool.Cancel's release path so the slot is unconditionally returned (subject to the map-delete-first guard) once Cancel is invoked for a known sid.

    Step 3 (GREEN): apply the minimal fix. Re-run the regression test alone, then `go test ./internal/pool/... ./internal/engine/... -race -count=1` and confirm all existing tests still pass (the existing TestPool_Cancel_ReleasesSlot which DOES drain Result must remain green).

    Step 4: `go build ./...` clean. Commit as a single atomic commit with the message above.

    Do not touch acp_adapter Cancel, engine.go watchdog, the SSE emitter, or any logging code beyond reading.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./... && go test ./internal/pool/... ./internal/engine/... -race -count=1 -run '.*'</automated>
  </verify>
  <done>
    - New test TestPool_Cancel_ReleasesSlot_WithoutResultDrain exists and passes.
    - Existing TestPool_Cancel_ReleasesSlot continues to pass.
    - `go test ./internal/pool/... ./internal/engine/... -race` clean.
    - `go build ./...` clean.
    - One atomic commit referencing 260531-ra6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Place kiro-cli children in their own process group (Unix) so SIGTERM cascades</name>
  <files>
    internal/acp/pool_pgid_unix.go,
    internal/acp/pool_pgid_windows.go,
    internal/acp/client.go
  </files>
  <action>
    Create two build-tagged files inside internal/acp (NOT internal/pool — the only spawn site is `acp.New` at internal/acp/client.go:284, and centralizing the pgid wiring next to the spawn keeps the syscall import scoped to one package):

    1. `internal/acp/pool_pgid_unix.go` with first line `//go:build darwin || linux` followed by a blank line then `package acp`. Define:
       - `func applyPgidAttr(cmd *exec.Cmd)` — sets `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}`. Must NOT overwrite an existing SysProcAttr if the executor finds one (we do not today — single spawn site at client.go:284 — but the guard documents intent).
       - `func killProcessGroup(pid int, sig syscall.Signal) error` — sends `sig` to `-pid` via `syscall.Kill(-pid, sig)`. Returns the syscall error.

    2. `internal/acp/pool_pgid_windows.go` with first line `//go:build windows` followed by a blank line then `package acp`. Define no-op stubs with matching signatures:
       - `func applyPgidAttr(cmd *exec.Cmd) {}`
       - `func killProcessGroup(pid int, sig syscall.Signal) error { return nil }`

       The windows stub may import `syscall` only for the Signal type — if that pulls in OS-specific code, use `os.Signal` instead, or define a tiny local Signal alias to keep windows compilation clean. Goal: `GOOS=windows go build ./...` clean.

    3. In `internal/acp/client.go` immediately before `cmd.Start()` (line 299), insert `applyPgidAttr(cmd)`. This is THE ONLY spawn-site modification — no other exec.Command call exists in internal/ (grep confirms client.go:284 is the sole site).

    The killProcessGroup helper is exported via the function above for use by future shutdown paths (Phase 2 close hooks may use it); for THIS task we do not need to wire it into Close() — Go's exec.CommandContext already SIGKILLs the child on cancel, and once the child is the leader of its own pgrp, the SIGKILL is delivered to the leader, who is the kiro-cli root. Subprocess group adoption is the load-bearing change.

    Build-tag hygiene: do NOT add a `//go:build !windows` reverse tag on the unix file — use the explicit `darwin || linux` form per the existing project pattern (matches other build-tagged files if any; otherwise per the user constraints in the background).

    Commit message format:
      fix(acp): spawn kiro-cli in its own process group so SIGTERM to the gateway cascades to children (260531-ra6)

      Go's exec.Cmd places children in the parent's process group by
      default. SIGTERM to otto-gateway therefore did NOT propagate to
      kiro-cli on macOS/Linux, and on a hard exit kiro-cli was
      reparented to init.

      Adds build-tagged pool_pgid_{unix,windows}.go in internal/acp:
        - unix: SysProcAttr.Setpgid = true + killProcessGroup(-pid, sig) helper
        - windows: no-op stubs (preserves cross-compile cleanliness)

      Wired at the single spawn site (client.go:284, immediately before
      cmd.Start()). killProcessGroup is exposed for future shutdown
      paths but unused in this commit — exec.CommandContext's existing
      SIGKILL is now delivered to the kiro-cli pgrp leader.

      No new tests: subprocess-lifecycle behavior is hard to unit-test
      cleanly. Manual reproducer documented in SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && go build ./... && GOOS=windows go build ./... && go test ./internal/acp/... ./internal/pool/... -race -count=1</automated>
  </verify>
  <done>
    - Both pool_pgid_{unix,windows}.go files exist with correct build tags.
    - client.go calls applyPgidAttr(cmd) immediately before cmd.Start().
    - `go build ./...` clean on the host (macOS).
    - `GOOS=windows go build ./...` clean (cross-compile check).
    - All existing tests pass (`go test ./internal/acp/... ./internal/pool/... -race`).
    - One atomic commit referencing 260531-ra6.
  </done>
</task>

<task type="auto">
  <name>Task 3: otto-gw stop wrappers reap stray $KIRO_CMD subprocesses (POSIX + PowerShell)</name>
  <files>
    scripts/otto-gw,
    scripts/otto-gw.ps1,
    .planning/quick/260531-ra6-pool-slot-release-on-cancel-kiro-process/260531-ra6-SUMMARY.md
  </files>
  <action>
    Belt-and-suspenders cleanup for hard-crash paths (segfault, OOM, SIGKILL of the gateway) where the process-group fix from Task 2 cannot help.

    A. `scripts/otto-gw` (POSIX, bash 3.2 compatible, shellcheck clean):

       Add a new helper function `reap_kiro_orphans()` and call it from the END of `stop()` (after the existing "otto-gateway stopped" path, just before `return 0`). It is also called from the END of `stop_by_name()` after the binary kill loop completes.

       The function MUST:
         1. Resolve $KIRO_CMD by sourcing the same .env file `load_config` would (call `load_config` early in stop()? NO — stop() today does not load env. Instead: inline an `env_file="$(resolve_env_file)"; [[ -n "$env_file" ]] && load_env_file "$env_file"` at the top of reap_kiro_orphans so KIRO_CMD is in scope without changing stop()'s startup behavior. Use a subshell or a guard so the env load only happens once even if reap_kiro_orphans is called twice in a single invocation.)
         2. If $KIRO_CMD is empty, return 0 silently (no kiro configured = nothing to reap).
         3. Sleep ~2 seconds to let the OS deliver any SIGTERM that already went out.
         4. Find PIDs whose command field exactly equals the resolved $KIRO_CMD path. Use the style from the constraint: `ps -eo pid,command | awk -v cmd="$KIRO_CMD" '$2 == cmd {print $1}'`. NEVER substring-match "kiro" or "kiro-cli" by basename — the operator may run kiro-cli outside the gateway and we MUST NOT kill those.
         5. If the resulting PID list is empty, return 0 silently.
         6. Log: `echo "otto-gw: reaping stray kiro-cli orphans: <pid-list>"`. SIGTERM each pid. Wait 2 seconds. Re-scan; any survivors get SIGKILL.
         7. Final log: `echo "otto-gw: kiro-cli orphans reaped"` (only on the path where at least one orphan was killed).

       Edge cases / pitfalls:
         - $KIRO_CMD may be a bare name (e.g. "kiro-cli") rather than an absolute path. In that case the ps output's command field will likely be the absolute path the OS resolved it to. Resolve $KIRO_CMD via `command -v "$KIRO_CMD"` BEFORE the awk match — if `command -v` succeeds, match the resolved absolute path; if it fails (kiro-cli no longer on PATH), match the literal $KIRO_CMD value as-is (operator may still have processes by absolute path).
         - The `ps -eo pid,command` field on macOS BSD and Linux GNU both produce the command name as the second column; the rest of the line is the command + args. `$2 == cmd` matches the command field exactly. Good.
         - shellcheck: keep the awk one-liner double-quoted and the `-v` assignment outside the awk script body (already so in the constraint). Use `# shellcheck disable=SC2009` if the function uses ps+grep at any point (the constraint uses ps+awk so no exemption should be needed).
         - Guard against running on a system where ps lacks `-eo pid,command` (rare; both BSD and GNU support it). No fallback needed; let it fail loudly.

       Update `usage()` to add a one-line note under stop: `  stop                Stop background gateway (also reaps any stray $KIRO_CMD subprocesses)`.

    B. `scripts/otto-gw.ps1` (PowerShell 5.1+, Windows):

       Add a new function `Repair-KiroOrphans` (verb-noun PowerShell style; the verb `Repair` is approved). Mirror logic:
         1. Resolve `$env:KIRO_CMD` after `Initialize-Config` has been called. Since `Stop-Gateway` does NOT call Initialize-Config today, inline the resolution at the top of Repair-KiroOrphans: `$envFilePath = Resolve-EnvFile; if ($envFilePath) { Import-DotEnv -Path $envFilePath }` — same defensive pattern as the POSIX side.
         2. If $env:KIRO_CMD is empty, return silently.
         3. Resolve the absolute path: `$kiroPath = (Get-Command $env:KIRO_CMD -ErrorAction SilentlyContinue).Source; if (-not $kiroPath) { $kiroPath = $env:KIRO_CMD }`.
         4. Sleep 2 seconds.
         5. `$procs = Get-CimInstance Win32_Process -Filter "ExecutablePath IS NOT NULL" | Where-Object { $_.ExecutablePath -ieq $kiroPath }`. Belt-and-suspenders per the constraint: ALSO match `$_.CommandLine -like "*$kiroPath*"` as a secondary check when ExecutablePath is null but the process arglist clearly references KIRO_CMD. Union the two result sets and de-duplicate by ProcessId.
         6. If empty, return silently.
         7. Write-Host the PID list. SIGTERM via `Stop-Process -Id <pid>` (default non-Force). Sleep 2s. Survivors get `Stop-Process -Id <pid> -Force`.
         8. Final Write-Host on success.

       Call `Repair-KiroOrphans` at the end of `Stop-Gateway` AND at the end of `Stop-GatewayByName` (after the "otto-gateway stopped" message in each branch that reaches it).

       Update `Show-Usage` `stop` line to mirror the POSIX one.

    C. SUMMARY.md (.planning/quick/260531-ra6-pool-slot-release-on-cancel-kiro-process/260531-ra6-SUMMARY.md):

       Follow @$HOME/.claude/get-shit-done/templates/summary.md. Sections:

       - **What** — three lifecycle/pool-hygiene fixes shipped as three atomic commits.
       - **Why** — one paragraph per issue with the failure mode.
       - **Commits** — three SHA + message stubs (executor fills SHAs in after committing).
       - **Manual reproducer for Issue 2 (Unix process group)**:
         1. Build and start the gateway with `./scripts/otto-gw start`.
         2. Drive any chat request that spawns kiro-cli (the warm pool pre-spawns N at boot — `ps -A -o pid,pgid,command | grep -E 'kiro-cli|otto-gateway'` to see them).
         3. Note the gateway pid (call it $G) and one kiro-cli pid (call it $K). Confirm `ps -o pid,pgid -p $K` shows pgid == $K (i.e. $K is its OWN process group leader, not $G's pgrp). Pre-fix, pgid would equal $G's pgrp.
         4. `kill -TERM $G`. Confirm $K exits within a few seconds (pre-fix: $K survives and reparents to init/pid-1).
       - **Manual reproducer for Issue 3 (wrapper reap)**:
         1. Start the gateway with `./scripts/otto-gw start`.
         2. Simulate a hard crash: `kill -KILL $(cat .otto/gw/otto-gateway.pid)` (SIGKILL bypasses the process-group cascade).
         3. Confirm a stray kiro-cli is running: `pgrep -f "$KIRO_CMD"`.
         4. Run `./scripts/otto-gw stop`. Confirm:
            - "otto-gw: reaping stray kiro-cli orphans: <pid>" line appears.
            - "otto-gw: kiro-cli orphans reaped" line appears.
            - Post-stop `pgrep -f "$KIRO_CMD"` returns nothing.
         5. PowerShell mirror: same flow with `Stop-Process -Force` of the gateway then `.\scripts\otto-gw.ps1 stop`.
       - **What was NOT touched** — Anthropic adapter, PII hook, logging/trace hooks, SSE emitter, config layer beyond reading KIRO_CMD. (Honors the "no while I'm here" constraint.)
       - **Tests** — one new Go test (TestPool_Cancel_ReleasesSlot_WithoutResultDrain). Issues 2 and 3 covered by the manual reproducers above.

    Commit message format:
      fix(scripts): otto-gw stop reaps stray $KIRO_CMD subprocesses on POSIX and Windows (260531-ra6)

      Belt-and-suspenders defense for hard-crash paths (segfault, OOM,
      SIGKILL) where the Task 2 process-group fix cannot help.

      POSIX: scripts/otto-gw adds reap_kiro_orphans() — resolves $KIRO_CMD
      via the same .env loader, scans `ps -eo pid,command` for an EXACT
      command-field match (never basename), TERMs then 2s-later KILLs
      survivors. shellcheck clean. bash 3.2 compatible.

      Windows: scripts/otto-gw.ps1 adds Repair-KiroOrphans — same flow
      via Get-CimInstance Win32_Process filtered by ExecutablePath -ieq
      $env:KIRO_CMD (with a CommandLine -like fallback). Stop-Process
      then Stop-Process -Force on survivors.

      SCOPED STRICTLY to the resolved $KIRO_CMD path so operators who
      run kiro-cli outside the gateway are unaffected.

      Adds 260531-ra6-SUMMARY.md with manual reproducer steps for
      Issues 2 (process group) and 3 (wrapper reap).
  </action>
  <verify>
    <automated>cd /Users/coreyellis/code/github.com/cmetech/otto_app/otto-gateway && shellcheck scripts/otto-gw && bash -n scripts/otto-gw && go build ./...</automated>
  </verify>
  <done>
    - scripts/otto-gw `stop` and `stop_by_name` both invoke reap_kiro_orphans at the end.
    - scripts/otto-gw.ps1 Stop-Gateway and Stop-GatewayByName both invoke Repair-KiroOrphans at the end.
    - `shellcheck scripts/otto-gw` exits 0 with no findings.
    - `bash -n scripts/otto-gw` exits 0 (syntax check).
    - $KIRO_CMD match is path-exact (POSIX) or ExecutablePath-ieq (Windows); never basename.
    - 260531-ra6-SUMMARY.md exists with the sections listed above.
    - One atomic commit referencing 260531-ra6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → wrapper script | The wrapper reads $KIRO_CMD from .env or env; both are operator-controlled and not network-attacker-controlled. |
| gateway → kiro-cli subprocess | The spawn site receives Command + Args + Cwd from config; not user-controlled input. (Same boundary as the existing G204 nolint at acp/client.go:284.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ra6-01 | Tampering | wrapper reap (Task 3) | mitigate | Match $KIRO_CMD by resolved absolute path only — never basename `kiro` or `kiro-cli`. Operator-side kiro-cli processes outside the gateway are NOT killed. |
| T-ra6-02 | Denial-of-Service | wrapper reap (Task 3) | mitigate | Reap is bounded: 2s grace → TERM → 2s grace → KILL. No retry loop. Cannot wedge stop() on a recalcitrant kiro-cli. |
| T-ra6-03 | Repudiation | Task 1 fix | accept | Slot release on Cancel is a positive correctness change. Pre-existing pool.release DEBUG log already records the release event. |
| T-ra6-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies introduced. syscall is stdlib. |
</threat_model>

<verification>
After all three tasks ship and commits are recorded:

1. `go build ./...` clean on macOS host.
2. `GOOS=windows go build ./...` clean (cross-compile).
3. `go test ./internal/pool/... ./internal/engine/... ./internal/acp/... -race -count=1` clean.
4. `shellcheck scripts/otto-gw` clean.
5. `bash -n scripts/otto-gw` clean.
6. `git log --oneline -3` shows three commits, each referencing 260531-ra6, in the order Issue 1 → Issue 2 → Issue 3.
7. The new test TestPool_Cancel_ReleasesSlot_WithoutResultDrain is listed in the output of `go test ./internal/pool/... -run TestPool_Cancel_ReleasesSlot_WithoutResultDrain -v`.
</verification>

<success_criteria>
- Three atomic commits, in the required order, each referencing 260531-ra6 in the message body.
- The new regression test for Issue 1 passes; the existing TestPool_Cancel_ReleasesSlot continues to pass.
- `go build ./...` + `GOOS=windows go build ./...` + `go test ./internal/pool/... ./internal/engine/... ./internal/acp/... -race` + `shellcheck scripts/otto-gw` all clean.
- Wrapper reap is scoped strictly to the resolved $KIRO_CMD path on both POSIX and Windows.
- SUMMARY.md ships with manual reproducer steps for Issues 2 and 3.
- No "while I'm here" refactors anywhere outside the listed files_modified.
</success_criteria>

<output>
Create `.planning/quick/260531-ra6-pool-slot-release-on-cancel-kiro-process/260531-ra6-SUMMARY.md` as part of Task 3.
</output>
