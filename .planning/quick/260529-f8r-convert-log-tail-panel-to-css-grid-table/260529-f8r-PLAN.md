---
phase: quick-260529-f8r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/admin/templates/index.html.tmpl
  - internal/admin/static/js/admin.js
  - internal/admin/static/css/admin.css
autonomous: true
requirements:
  - QUICK-260529-f8r
must_haves:
  truths:
    - "Log Tail panel renders as a 4-column grid: Time | Level | Source | Message with a sticky header row"
    - "Each log line appears as one grid row; level is shown as a colored chip, not as row-wide color"
    - "Long messages wrap inside the Message column (row height varies); they do not horizontally scroll the viewport"
    - "Subprocess/non-slog raw lines that cannot be parsed for level/time/msg still appear (as a full-width fallback row spanning all 4 columns) — nothing is dropped"
    - "Regex grep filter continues to match against the original raw line (not the concatenated cell text), so existing grep patterns keep working"
    - "Log level filter, pause/resume + flush, 'N new' badge, autoscroll, connection-status text + activity dot, 1000-line DOM cap, backfill dedup all behave identically to pre-refactor"
    - "All rendering still uses textContent only — no innerHTML is introduced anywhere new (T-6.1-16)"
    - "Existing Go test suite (internal/admin/handlers_test.go, tail_test.go, sse_test.go) still passes"
  artifacts:
    - path: internal/admin/templates/index.html.tmpl
      provides: "Grid-shaped log viewport markup with sticky header row and preserved data-log-viewport / data-log-empty / role=log / aria-live attributes"
      contains: "data-log-viewport"
    - path: internal/admin/static/js/admin.js
      provides: "parseLogLine returning {level,time,msg,source,raw}; appendLine building 4-cell grid rows + full-width fallback; grep re-derive against dataset.raw"
      contains: "dataset.raw"
    - path: internal/admin/static/css/admin.css
      provides: "Grid container for .otto-log-viewport, sticky .otto-log-header-row, .otto-log-level-chip color variants, wrapping message cell, removed row-wide is-debug/is-info/is-warn/is-error color rules"
      contains: ".otto-log-level-chip"
  key_links:
    - from: "internal/admin/static/js/admin.js (appendLine)"
      to: "internal/admin/templates/index.html.tmpl (data-log-viewport)"
      via: "document.querySelector('[data-log-viewport]')"
      pattern: "data-log-viewport"
    - from: "internal/admin/static/js/admin.js (initGrepFilter / initLevelFilter re-derive)"
      to: "rendered row element"
      via: "el.dataset.raw + el.dataset.level"
      pattern: "dataset\\.(raw|level)"
    - from: "internal/admin/static/css/admin.css (.otto-log-level-chip.is-*)"
      to: "appendLine chip element"
      via: "chip span class .otto-log-level-chip.is-{level}"
      pattern: "otto-log-level-chip"
---

<objective>
Refactor the existing admin /admin Log Tail panel UI from a one-line-per-row monospace stream into a 4-column CSS-grid table (Time | Level | Source | Message) with a sticky header, colored level chips, and wrapping message cells — without changing any backend behavior, SSE wire shape, or existing functional surface (pause, grep, level filter, dedup, autoscroll, 1000-line cap, textContent-only safety).

Purpose: Improve scanability of the live log tail (visual hierarchy + sticky header + per-level chip) while preserving every regression-sensitive behavior already shipped in Phase 6.1 (T-6.1-16 textContent safety, WR-06 persisted level for re-derive, WR-03/04 backfill dedup).

Output: Updated template, JS, and CSS in `internal/admin/{templates,static/js,static/css}` with the grid layout active, grep matching against the original raw line, and the existing Go test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@internal/admin/templates/index.html.tmpl
@internal/admin/static/js/admin.js
@internal/admin/static/css/admin.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert template + CSS to 4-column grid with sticky header and level chips</name>
  <files>
    internal/admin/templates/index.html.tmpl
    internal/admin/static/css/admin.css
  </files>
  <action>
In `internal/admin/templates/index.html.tmpl` (~lines 111-115), replace the current viewport `<div class="otto-log-viewport" data-log-viewport role="log" aria-live="polite" aria-relevant="additions">` so that the viewport remains the outer scroll container and the grid lives inside it. Concretely:

- Keep `class="otto-log-viewport" data-log-viewport role="log" aria-live="polite" aria-relevant="additions"` on the OUTER element (selector compatibility with `document.querySelector('[data-log-viewport]')` in admin.js MUST be preserved).
- Inside the viewport, render a sticky header row as a child element with class `otto-log-header-row` containing four cells (spans or divs) with class `otto-log-cell otto-log-cell-{time|level|source|message}` and textContent `Time`, `Level`, `Source`, `Message`. The header row is rendered ONCE in the template, NOT injected by JS.
- Keep `<div class="otto-log-empty" data-log-empty>Waiting for log activity…</div>` as a sibling of the header row inside the viewport so the existing JS `empty.hidden = true` line in `appendLine` still works.
- Do NOT add any new server-side template variables. Header text is static.

In `internal/admin/static/css/admin.css` (~lines 506-534):

- Change `.otto-log-viewport` from a plain block to `display: grid; grid-template-columns: max-content max-content max-content 1fr; align-content: start;` while keeping `height: 480px; overflow-y: auto;` and existing font/color/background. (The grid columns size to content for time/level/source and let message fill the remainder; switch to `auto auto auto 1fr` if `max-content` causes layout glitches with long source values — prefer `minmax(0, max-content)` for the first three so very long values don't blow out the layout.)
- Add `.otto-log-header-row { display: contents; }` so its four cells participate directly in the parent grid; on each header cell apply `position: sticky; top: 0; background: var(--otto-bg); z-index: 1; padding: 4px var(--otto-space-sm); font: 600 11px/1.4 var(--otto-font-ui); color: var(--otto-muted); border-bottom: 1px solid var(--otto-border);` (use a `.otto-log-header-row .otto-log-cell` selector). The sticky background MUST match `--otto-bg` so scrolling rows are visually clipped behind it.
- Style `.otto-log-row { display: contents; }` so each appended row is also a grid contributor; cells use `.otto-log-cell { padding: 2px var(--otto-space-sm); white-space: nowrap; }` by default.
- Override `.otto-log-cell-message { white-space: pre-wrap; word-break: break-word; }` so long messages wrap inside the message column.
- Add a full-width fallback row variant: `.otto-log-row-fallback { display: contents; }` plus `.otto-log-row-fallback .otto-log-cell-fallback { grid-column: 1 / -1; padding: 2px var(--otto-space-sm); white-space: pre-wrap; word-break: break-word; color: var(--otto-muted); }`.
- Add the level chip rules: `.otto-log-level-chip { display: inline-block; padding: 0 6px; border-radius: 4px; font: 600 10px/1.6 var(--otto-font-ui); text-transform: uppercase; letter-spacing: 0.04em; }` and per-level variants — chip foreground/background pairs must remain readable on the dark theme. Use the existing palette tokens: `.is-debug` → `color: var(--otto-bg); background: var(--otto-muted);`, `.is-info` → `color: var(--otto-bg); background: var(--otto-fg);`, `.is-warn` → `color: var(--otto-bg); background: var(--otto-warning);`, `.is-error` → `color: var(--otto-bg); background: var(--otto-failed);`, plus `.is-unknown { color: var(--otto-muted); background: transparent; border: 1px solid var(--otto-border); }`.
- REMOVE the existing row-wide color rules: delete `.otto-log-line.is-debug`, `.otto-log-line.is-info`, `.otto-log-line.is-warn`, `.otto-log-line.is-error`. Color now lives on the chip, not the row. The `.otto-log-line` selector itself may be removed if no longer referenced; leaving it harmless is also acceptable.
- `.otto-log-empty` should be wrapped to span all columns when shown: add `.otto-log-empty { grid-column: 1 / -1; }` (in addition to existing styling).

Do not change `.otto-log-header` (the panel-top activity row above the controls) — only the new in-viewport header row.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('internal/admin/static/css/admin.css','utf8');const tmpl=fs.readFileSync('internal/admin/templates/index.html.tmpl','utf8');const checks=[['css has grid container',/\.otto-log-viewport\s*\{[^}]*display:\s*grid/m.test(css)],['css has sticky header cell',/\.otto-log-header-row[^{]*\.otto-log-cell[^{]*\{[^}]*position:\s*sticky/m.test(css)],['css has level-chip selector',/\.otto-log-level-chip/.test(css)],['css has chip is-warn',/\.otto-log-level-chip\.is-warn|otto-log-level-chip[\s\S]*?is-warn/.test(css)],['css has wrapping message cell',/\.otto-log-cell-message[^{]*\{[^}]*pre-wrap/.test(css)],['css has fallback cell spanning all cols',/grid-column:\s*1\s*\/\s*-1/.test(css)],['css removed row-wide is-warn color',!/\.otto-log-line\.is-warn/.test(css)],['css removed row-wide is-error color',!/\.otto-log-line\.is-error/.test(css)],['tmpl preserves data-log-viewport',/data-log-viewport/.test(tmpl)],['tmpl preserves data-log-empty',/data-log-empty/.test(tmpl)],['tmpl preserves role=log + aria-live',/role=\"log\"[\s\S]*aria-live=\"polite\"/.test(tmpl)],['tmpl has header row with Time/Level/Source/Message',/otto-log-header-row[\s\S]*Time[\s\S]*Level[\s\S]*Source[\s\S]*Message/.test(tmpl)]];let ok=true;for(const [n,p] of checks){console.log((p?'PASS':'FAIL')+': '+n);if(!p)ok=false;}process.exit(ok?0:1);"</automated>
  </verify>
  <done>
Template viewport contains a sticky 4-column header row (Time/Level/Source/Message) and preserves data-log-viewport, data-log-empty, role=log, aria-live. CSS has grid layout, sticky header cells, level chip color variants, message-cell wrapping, and full-width fallback support; row-wide is-warn/is-error color rules are gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update parseLogLine + appendLine + filter re-derive paths in admin.js</name>
  <files>
    internal/admin/static/js/admin.js
  </files>
  <action>
Modify `internal/admin/static/js/admin.js` in three regions; preserve all existing behavior (T-6.1-16 textContent-only, WR-06 persisted level, 1000-node cap, backfill dedup, pause/flush, autoscroll, connection status). Use textContent everywhere — no `innerHTML`, no `insertAdjacentHTML`.

1) `parseLogLine` (~line 442): extend the return shape from `{ level, msg }` to `{ level, time, msg, source, raw }` where `raw` is always the original line argument.

   - JSON branch (line starts with `{`): on successful parse, read `obj.time` (string), `obj.msg` (string), and `obj.logger` if present else `obj.source` (string) — document the choice in a comment ("source: prefer `logger`, fall back to `source`"). Return `{ level: lv.toLowerCase(), time: typeof obj.time === 'string' ? obj.time : '', msg: typeof obj.msg === 'string' ? obj.msg : '', source: typeof obj.logger === 'string' ? obj.logger : (typeof obj.source === 'string' ? obj.source : ''), raw: line }`. Keep the existing try/catch fallthrough behavior on parse failure.
   - slog text branch: keep `/\blevel=([A-Za-z]+)/` for level. Add `/\btime=("[^"]*"|\S+)/` to extract time (strip surrounding quotes), and `/\bmsg=("[^"]*"|\S+)/` for msg (strip surrounding quotes). Source extraction is best-effort: `/\b(logger|source)=("[^"]*"|\S+)/`. Return `{ level: match[1].toLowerCase(), time, msg, source, raw: line }`.
   - Final fallback (no level detected): return `{ level: null, time: '', msg: '', source: '', raw: line }`.

2) `appendLine` (~line 483): rewrite to build a grid row instead of a single text node.

   - Acquire the viewport (`[data-log-viewport]`) and hide `[data-log-empty]` exactly as today.
   - Decide between full row vs fallback row: if `parsed.level === null && parsed.time === '' && parsed.msg === ''`, build a fallback row — `var row = document.createElement('div'); row.className = 'otto-log-row-fallback'; row.dataset.level = 'unknown'; row.dataset.raw = parsed.raw; var cell = document.createElement('div'); cell.className = 'otto-log-cell otto-log-cell-fallback'; cell.textContent = parsed.raw; row.appendChild(cell);`. Apply the existing filter-hiding logic (see below) and append.
   - Otherwise build a 4-cell grid row: `var row = document.createElement('div'); row.className = 'otto-log-row'; row.dataset.level = parsed.level || 'unknown'; row.dataset.raw = parsed.raw;`. Then create four child cells via `document.createElement('div')` with classes `otto-log-cell otto-log-cell-time`, `otto-log-cell otto-log-cell-level`, `otto-log-cell otto-log-cell-source`, `otto-log-cell otto-log-cell-message`. Set textContent for time/source/message cells from `parsed.time`, `parsed.source`, and (`parsed.msg || parsed.raw`) respectively. For the level cell: create a child `<span class="otto-log-level-chip is-{level}">` (level = `parsed.level || 'unknown'`), set its textContent to the uppercase level string (or the literal `?` for unknown), and append the chip into the level cell. Use textContent only — never innerHTML.
   - Filter visibility: continue calling `matchesFilters(parsed, parsed.raw)` (pass the raw line so grep regex sees the same string it was matching before). If false, set `row.style.display = 'none'`. Note: with `display: contents` on the row, setting `display: none` on the row hides all its cells from the grid — confirmed behavior in modern browsers.
   - Trim DOM cap: `while (vp.children.length > 1001) { vp.removeChild(vp.firstElementChild === headerRow ? vp.children[1] : vp.firstChild); }` — but simpler and correct: change the cap walk to skip the header row. Implement as: `var firstLog = vp.querySelector('.otto-log-row, .otto-log-row-fallback'); while (vp.querySelectorAll('.otto-log-row, .otto-log-row-fallback').length > 1000) { var victim = vp.querySelector('.otto-log-row, .otto-log-row-fallback'); if (!victim) break; vp.removeChild(victim); }`. The header row and empty placeholder must never be evicted.

3) Filter re-derive paths in `initLevelFilter` (~line 610) and `initGrepFilter` (~line 628):

   - Replace `vp.querySelectorAll('.otto-log-line')` in BOTH functions with `vp.querySelectorAll('.otto-log-row, .otto-log-row-fallback')`.
   - Replace `var line = el.textContent;` in BOTH functions with `var line = el.dataset.raw || '';` so grep regex matches the original raw line, NOT the concatenated cell text. This is the load-bearing correctness change for the refactor.
   - `levelFromElement(el)` (~line 517) reads `el.dataset.level` — keep as-is; it already works for both `.otto-log-row` and `.otto-log-row-fallback`.

4) Pause-buffer flush: `initPauseButton` iterates `logNewestBuffer` and calls `appendLine(entry.line, entry.parsed)`. No change needed — the new `appendLine` accepts the same `(line, parsed)` signature; `parsed.raw` will be set because `onLogEvent` calls `parseLogLine` before buffering.

Do not change SSE wiring, `onLogEvent`, `onSSEOpen`, `onSSEError`, backfill dedup, `autoScroll`, or `updateNewestBadge`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const js=fs.readFileSync('internal/admin/static/js/admin.js','utf8');const checks=[['parseLogLine returns time field',/parseLogLine[\s\S]*?return\s*\{[^}]*time:/m.test(js)],['parseLogLine returns source field',/parseLogLine[\s\S]*?return\s*\{[^}]*source:/m.test(js)],['parseLogLine returns raw field',/parseLogLine[\s\S]*?return\s*\{[^}]*raw:/m.test(js)],['appendLine writes dataset.raw',/dataset\.raw\s*=/.test(js)],['appendLine builds otto-log-row',/otto-log-row(?![-a-z])/.test(js)],['appendLine builds level chip span',/otto-log-level-chip/.test(js)],['appendLine has fallback row class',/otto-log-row-fallback/.test(js)],['filter re-derive uses dataset.raw',/dataset\.raw\s*\|\|\s*''/.test(js)||/dataset\.raw\s*\|\|\s*\"\"/.test(js)],['no new innerHTML introduced',!/\.innerHTML\s*=/.test(js)],['no insertAdjacentHTML introduced',!/insertAdjacentHTML/.test(js)],['filter selectors updated to otto-log-row',/querySelectorAll\(\s*'\.otto-log-row,\s*\.otto-log-row-fallback'\s*\)/.test(js)||/querySelectorAll\(\s*\"\.otto-log-row,\s*\.otto-log-row-fallback\"\s*\)/.test(js)],['1000-node cap retained',/1000/.test(js)]];let ok=true;for(const [n,p] of checks){console.log((p?'PASS':'FAIL')+': '+n);if(!p)ok=false;}process.exit(ok?0:1);" && go test ./internal/admin/... -count=1</automated>
  </verify>
  <done>
parseLogLine returns `{level,time,msg,source,raw}`; appendLine constructs grid rows with a level-chip span, sets `dataset.raw` and `dataset.level`, supports the fallback full-width row for unparseable lines, and preserves the 1000-node cap. Both filter re-derive paths read `dataset.raw` instead of `el.textContent`. No `innerHTML`/`insertAdjacentHTML` introduced. The existing Go test suite under `internal/admin/...` still passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + functional smoke test in the browser</name>
  <what-built>
    Log Tail panel now renders as a 4-column CSS grid (Time | Level | Source | Message) with a sticky header, per-level colored chips, wrapping message cells, and a full-width fallback row for raw/unparseable lines. Grep filter still matches the original raw line; level filter, pause/resume + flush, "N new" badge, autoscroll, 1000-line cap, and connection status all behave as before.
  </what-built>
  <how-to-verify>
1. Build and run the gateway locally (`make run` or the project's standard local-run path) so it serves `/admin`. Confirm the kiro-cli pool is configured so log lines are flowing.
2. Open `http://localhost:<port>/admin` and scroll to the Log Tail panel.
3. Header row: confirm a single sticky row at the top of the viewport showing `Time   Level   Source   Message`. Scroll the body and confirm the header stays pinned and visually clips rows scrolling under it (background matches the panel bg).
4. Steady-state rows: confirm typical slog lines render as 4 cells, the level appears as a colored chip in the Level column (not as the row color). Verify debug=muted, info=fg-on-bg, warn=warning-orange, error=failed-red. Long JSON or long messages wrap inside the Message column without horizontally scrolling the viewport.
5. Fallback row: trigger or wait for at least one non-slog raw line (e.g., subprocess banner / panic / stderr passthrough). Confirm it appears as a single full-width row spanning all 4 columns — NOT eaten / NOT silently dropped.
6. Level filter: pick `Warn` from the dropdown — confirm only warn/error chips remain visible. Switch back to `All levels`.
7. Grep filter: type a substring you can see in the Message column (e.g., `prompt` or a specific request id). Confirm matching rows stay visible. Then type a substring that ONLY appears in the raw line but is split across cells in the rendered output (e.g., something near the slog `time=` prefix); confirm grep still matches it — this validates the dataset.raw plumbing.
8. Pause/Resume: click Pause, watch the "N new" badge increment as lines arrive, then click Resume — buffered lines flush in order, autoscroll snaps to the bottom.
9. Activity dot + status: kill or stop kiro briefly to force SSE disconnect; status text updates to "Log stream disconnected — reconnecting…" and the activity dot turns warning-orange. Reconnect: status flips back to "Connected" and the dot resumes pulse.
10. View source / DevTools: inspect a couple of rendered log rows and confirm NO `innerHTML` usage (no script/HTML appears inside cell text). Confirm `data-log-viewport` is on the outer scroll container.
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<verification>
- Build: `go build ./...` succeeds.
- Existing tests: `go test ./internal/admin/... -count=1` passes (UI markup change must not regress server-side handler/sse/tail tests).
- Static safety: grep confirms no new `innerHTML` or `insertAdjacentHTML` in `internal/admin/static/js/admin.js`.
- Visual: the human-verify checkpoint above confirms all functional regressions (level filter, grep, pause/resume, fallback row, sticky header, chip palette).
</verification>

<success_criteria>
- 4-column CSS grid renders in `/admin` Log Tail with a sticky Time/Level/Source/Message header.
- Level appears as a colored chip per row using existing palette tokens; row-wide color rules removed.
- Long messages wrap inside the Message column; viewport does not horizontally scroll.
- Unparseable / raw subprocess lines still render via a full-width fallback row.
- Grep filter matches against `dataset.raw` (the original SSE line), preserving prior grep behavior.
- Level filter, pause/resume + flush, "N new" badge, autoscroll, 1000-line DOM cap, backfill dedup, and SSE connection status all behave identically to pre-refactor.
- `textContent`-only rendering preserved (T-6.1-16); no `innerHTML` introduced.
- `go test ./internal/admin/... -count=1` green.
</success_criteria>

<output>
Create `.planning/quick/260529-f8r-convert-log-tail-panel-to-css-grid-table/260529-f8r-SUMMARY.md` when done, summarizing: template changes, JS changes (parseLogLine shape, appendLine grid construction, dataset.raw grep plumbing), CSS changes (grid container, sticky header, chip palette, fallback row, removed row-wide color rules), and visual-verification outcome.
</output>
