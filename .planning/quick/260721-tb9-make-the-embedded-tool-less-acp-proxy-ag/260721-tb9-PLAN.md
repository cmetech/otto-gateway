---
phase: quick-260721-tb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/embed/acp_proxy.json
  - internal/embed/assets.go
  - internal/embed/assets_test.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/config/loadargs_test.go
  - internal/config/regression_rel_cfg_06_test.go
  - cmd/otto-gateway/main.go
  - cmd/otto-gateway/main_test.go
  - internal/admin/admin.go
  - internal/admin/handlers_test.go
  - scripts/.env.example
autonomous: true
requirements:
  - tool-less-kiro-acp-default
must_haves:
  truths:
    - "An unconfigured gateway launches kiro-cli with argv `acp --agent acp_proxy` from a gateway-owned workspace containing `.kiro/agents/acp_proxy.json`."
    - "The shipped acp_proxy agent explicitly has no built-in tools, MCP servers, allowed tools, resources, hooks, aliases, or tool settings, and has no prompt persona."
    - "Existing KIRO_CMD, KIRO_ARGS, KIRO_CWD, --kiro-args, and --kiro-cwd overrides retain precedence."
    - "An existing acp_proxy.json is preserved byte-for-byte and a custom KIRO_CWD is never seeded."
    - "Startup logs the effective Kiro command, argv, cwd, agent path, and materialization status before pool warmup."
    - "The identity guard, deny-permission handler, and built-in-tool aliaser remain unchanged."
  artifacts:
    - path: "internal/embed/acp_proxy.json"
      provides: "Canonical tool-less Kiro custom-agent configuration."
      contains: '"tools": []'
    - path: "internal/embed/assets.go"
      provides: "Gateway-home derivation and no-clobber agent materialization."
      contains: "EnsureACPProxy"
    - path: "internal/config/config.go"
      provides: "Default acp_proxy argv/cwd with explicit-override ownership tracking."
      contains: "KiroCWDIsDefault"
    - path: "cmd/otto-gateway/main.go"
      provides: "Pre-warmup materialization and launch diagnostics."
      contains: "kiro launch configured"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/embed/assets.go"
      via: "GatewayDir supplies the default KiroCWD"
      pattern: "gatewayembed.GatewayDir"
    - from: "cmd/otto-gateway/main.go"
      to: "internal/embed/assets.go"
      via: "EnsureACPProxy runs before pool.New/Warmup"
      pattern: "gatewayembed.EnsureACPProxy"
---

<objective>
Restore the legacy loop24 launch posture by making a gateway-owned, embedded,
tool-less `acp_proxy` custom agent the default for every Kiro ACP worker while
preserving all operator overrides and runtime defense-in-depth.

The complete test-first implementation steps and exact code contracts are in
`docs/superpowers/plans/2026-07-21-tool-less-kiro-acp-agent.md`.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Embed and materialize the tool-less agent without clobbering</name>
  <files>internal/embed/acp_proxy.json, internal/embed/assets.go, internal/embed/assets_test.go</files>
  <action>
Follow Superpowers plan Task 1. Write and run failing tests before adding the
asset package. Use exclusive file creation, preserve existing regular files,
reject non-regular targets, and derive the gateway root from GW_HOME then
os.UserConfigDir()/gateway.
  </action>
  <verify>
    <automated>go test ./internal/embed -count=1</automated>
  </verify>
  <done>The exact agent JSON is embedded; initial materialization creates it; repeat/customized files are preserved; invalid targets fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply defaults and wire pre-warmup launch preparation</name>
  <files>internal/config/config.go, internal/config/config_test.go, internal/config/loadargs_test.go, internal/config/regression_rel_cfg_06_test.go, cmd/otto-gateway/main.go, cmd/otto-gateway/main_test.go</files>
  <action>
Follow Superpowers plan Tasks 2 and 3. Add failing tests for default argv/cwd,
environment and flag precedence, default ownership, materialization/logging,
preservation, custom cwd non-modification, and startup errors. Implement only
enough code to pass each red-green cycle. Call launch preparation before
pool.New and pool.Warmup.
  </action>
  <verify>
    <automated>go test ./internal/config ./cmd/otto-gateway -count=1</automated>
  </verify>
  <done>Default launches select acp_proxy from the managed workspace, explicit overrides remain authoritative, and launch state is logged before warmup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update operator docs and verify automated plus live behavior</name>
  <files>internal/admin/admin.go, internal/admin/handlers_test.go, scripts/.env.example</files>
  <action>
Follow Superpowers plan Tasks 4 and 5. Test the rendered documentation first,
update only stale Kiro defaults/examples, run full Go verification, then run all
available live Kiro, identity, tool-call, tool-execution, chat, streaming, and
three-surface checks. Report unavailable host-only checks explicitly.
  </action>
  <verify>
    <automated>go test ./... -count=1 &amp;&amp; go vet ./... &amp;&amp; go build ./cmd/otto-gateway</automated>
  </verify>
  <done>Automated gates pass, live evidence is recorded accurately, protected workarounds are unchanged, and operator docs match runtime defaults.</done>
</task>

</tasks>
