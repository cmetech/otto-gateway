---
milestone: v1.5
audited: 2026-06-04T14:10:00Z
re_audit_of: "2026-05-28T17:30:00Z (passed at 11 phases) — refreshed to cover 08.2/08.3/08.3.1/08.4"
status: tech_debt
scores:
  requirements: 63/63
  phases: 13/14 closed + 1 reverted + 1 inserted-not-executed
  integration: 10/10 (carried from prior audit — no new cross-phase wiring introduced 8.2-8.4)
  flows: 9/9 cells green (carried + PII-01 fixture added on all 3 surfaces)
phase_set:
  - 01-foundations
  - 01.1-acp-wire-alignment
  - 02-ollama-end-to-end
  - 03-openai-surface
  - 03.1-anthropic-surface
  - 04-streaming
  - 05-pool-stateful-sessions
  - 06-tool-call-path
  - 06.1-admin-observability-ui
  - 08-plugin-hook-chain
  - 08.1-close-gap-integ-01-streaming-mode-prehook-short-circuit-v1-5
  - 08.2-ollama-format-parity
  - 08.3-acp-prompt-non-blocking-refactor
  - 08.4-us-address-pii-coverage
  - 9-distribution
phases_with_dispositions:
  reverted:
    - id: "08.3.2"
      name: "PII Smoke Test Methodology Fix"
      disposition: "REVERTED 2026-06-04 (commit ff10594) — superseded by prompt-only fix in /scripts/test-pii.{ps1,sh}. Phase 8.3 (the actual ACP refactor) remained complete; the fake-kiro-cli machinery added by 08.3.2 was unwound."
  inserted_but_not_executed:
    - id: "08.3.1"
      name: "ACP Per-Session Stream Demux"
      goal: "Replace single-slot `c.activeStream *Stream` in `internal/acp/client.go:262` with per-sessionID map to close WR-04 silent cross-session leak race."
      current_state: "Roadmap entry exists; phase directory empty; `[ ] TBD (run /gsd-plan-phase 08.3.1)` checkbox. Code still uses `activeStream` single-slot (verified at `internal/acp/client.go:262, 731, 988`)."
      v1_risk_assessment: "Not exploitable in v1's pool architecture. POOL_SIZE=4 binds each kiro-cli subprocess to one worker slot (`internal/pool/pool.go`); `Client.Prompt(sessionID, …)` is serialized per slot. The WR-04 race requires the same Client instance to handle two concurrent prompts with different sessionIDs, which the pool model prevents. Multi-tenant scenarios (one shared Client serving many concurrent sessions) are out of v1 scope."
      disposition_options:
        - "DEFER to v1.6 (Recommended) — treat as concurrency-hardening for future multi-tenant ambitions."
        - "DROP from roadmap and move to backlog seed — 'multi-tenant gateway' is not a v1 use case."
        - "EXECUTE now before v1.5 close — only required if multi-tenant v1 deployment is in scope."
      recommendation: "DEFER to v1.6. v1's pool model makes the race non-exploitable. The roadmap entry should be updated to `[deferred] Deferred to v1.6 (multi-tenant concurrency hardening)` before milestone close."
verification_per_phase:
  01: passed
  01.1: passed
  02: human_needed (LangFlow zero-reconfig — operator-deferred per 2026-05-28 audit, unchanged)
  03: passed
  03.1: passed
  04: passed
  05: verified (non-standard token from older verifier; 5/5 plans + gap-closure plans 05-04/05-05 shipped)
  06: passed
  06.1: passed
  08: passed (Task 7 operator protocol operator-deferred per 2026-05-28 audit, unchanged)
  08.1: passed
  08.2: NOT_VERIFIED (shipped via /gsd-quick; SUMMARY exists; no VERIFICATION.md — quick-task path skips formal verifier)
  08.3: NOT_VERIFIED (shipped via /gsd-quick; SUMMARY exists; no VERIFICATION.md)
  08.3.1: NOT_EXECUTED (see dispositions above)
  08.4: passed (re-verified 2026-06-04T13:45 after HUMAN-UAT item 1 PASS + items 2-4 accepted_deviation)
  9-distribution: passed (carried)
post_audit_changes_since_2026-05-28:
  - phase: 08.2
    impact: "Adds LangFlow `format:\"json\"` / `format:<schema>` GEN_RULES PreHook + fence-stripper PostHook. Reuses existing PreHook/PostHook plugin chain interface — no new cross-phase wiring."
  - phase: 08.3
    impact: "Internal refactor of `acp.Client.Prompt()` (blocking → returns *Stream early, finalize via goroutine). Closes Phase 8.3 deadlock. Code review surfaced WR-04 (became Phase 08.3.1 — see dispositions). No new cross-phase wiring."
  - phase: 08.4
    impact: "Adds 3 regex recognizers (USAddress/USState/USZIP) + validator inside existing PII plugin. New requirement PII-01 added to REQUIREMENTS.md (v1 count 62 → 63). Two documented accepted_deviation: (1) USState arm-1 captured span subsumes trailing ZIP; (2) WR-01 dangerous suffix expansion reverted. v1.10.0 binary released; operator HUMAN-UAT item 1 PASS on splunk box (33/33 needle checks across all 3 surfaces); items 2-4 accepted_deviation (code-test-covered). No new cross-phase wiring."
gaps:
  requirements: []  # 63/63 marked Complete in REQUIREMENTS.md traceability; no orphans, no unsatisfied
  integration: []  # No new cross-phase wiring added post-audit; prior 10/10 carries
  flows: []        # All 9/9 cells green; PII-01 fixture exercised on all 3 surfaces (item 1 PASS)
tech_debt:
  - phase: 02
    items:
      - "HUMAN-UAT item 1 (real-kiro round-trip): pending — `LOOP24_INTEGRATION=1 go test` against operator-auth'd kiro-cli. Operator-deferred per 2026-05-28 audit."
      - "HUMAN-UAT item 2 (LangFlow zero-reconfig): pending — load-bearing per ROADMAP SC #2. Implicitly verified by Phase 08.2 (LangFlow `format` parity shipping required the base /api/chat integration to work). Formally still pending."
      - "HUMAN-UAT item 3 (auth posture smoke): pending — middleware contract is asserted in unit tests."
  - phase: 08
    items:
      - "7-step operator protocol against live binary (now including streaming bad-auth variant fixed by 08.1): pending. Operator-deferred per 2026-05-28 audit."
  - phase: 08.2
    items:
      - "Missing VERIFICATION.md (shipped via /gsd-quick which bypasses formal verifier)."
  - phase: 08.3
    items:
      - "Missing VERIFICATION.md (shipped via /gsd-quick which bypasses formal verifier)."
  - phase: 08.3.1
    items:
      - "Not executed. v1.6 deferral recommended. Roadmap checkbox should flip from `[ ] TBD` to `[deferred] Deferred to v1.6`."
  - phase: 08.4
    items:
      - "WR-NEW-03 accepted_deviation: USState arm-1 span subsumes trailing ZIP. Per-entity action routing for USZIP loses independent routing (`PII_ENTITY_ACTIONS=USZIP:…` no longer routes ZIPs). Operator audit guidance: sum USAddress + USState + USZIP."
      - "BL-NEW-01 acknowledged AP-2 residual: `\", OR 27584 stuff\"` (English-word + numeric quantity) cannot be distinguished from `\", TX 27584 ASAP\"` under RE2 (no lookahead). Documented in `recognizers_test.go:720-741`."
      - "HUMAN-UAT items 2-4 accepted_deviation (POSIX smoke, AP-2 negative-control, NER overlap-arbiter production verification). All covered by `go test -race` suite."
  - phase: cross_phase
    items:
      - "Pre-existing gofumpt drift across `cmd/` + `internal/adapter/*` (16+ files). Documented in `.planning/phases/08.1-…/deferred-items.md`."
      - "Pre-existing `internal/admin/tail_test.go` using Go 1.24 `testing.Context()` while go.mod is 1.23. Same deferred-items.md."
      - "`make ci` fails locally at `golangci-lint` step on dev box — pre-existing env gap (golangci-lint not on PATH). Passes in CI runners."
nyquist:
  compliant_phases: 3
  partial_phases: 8
  missing_phases: 5
  overall: "Accepted-trailing per 2026-05-28 audit; does not block milestone closure. Phase 08.4 ships with VALIDATION.md (verified)."
verdict_summary: "v1.5 YELLOW — ready for closure pending one decision: 08.3.1 disposition. Recommended: DEFER to v1.6 with roadmap-entry update before close. With that single edit, milestone goes GREEN."
---

# v1.5 Milestone Audit — Refresh (2026-06-04)

This document supersedes the 2026-05-28 audit which verified v1.5 at 11 phases. Since then, four additional phases shipped (08.2, 08.3, 08.4) plus one revert (08.3.2) and one insert-not-executed (08.3.1). This refresh evaluates the post-audit changes.

## What changed since the prior audit

| Date | Phase | Outcome |
|------|-------|---------|
| 2026-06-03 | 08.2 Ollama `format` Parity | shipped via /gsd-quick |
| 2026-06-03 | 08.3 ACP Prompt() Non-Blocking Refactor | shipped via /gsd-quick; v1.9.3 tag |
| 2026-06-03 | 08.3.1 ACP Per-Session Stream Demux (INSERTED) | **not executed** — see Open Decision |
| 2026-06-04 | 08.3.2 PII Smoke Test Methodology Fix | **REVERTED** in commit `ff10594` |
| 2026-06-03 → 2026-06-04 | v1.9.4 → v1.9.7 PII fidelity bug-fix series | shipped (IPv4/IPv6 letters-only bug, script-side cipher-leak, USPhone leading-paren) |
| 2026-06-04 | 08.4 US Address PII Coverage | shipped, v1.10.0 tag, HUMAN-UAT item 1 PASS on splunk box |

Cumulatively: **+1 new requirement (PII-01)**, **+5 new files** (recognizers.go growth, recognizers_test.go growth, pii_test.go growth, config.go growth, both script files grew), **0 new cross-phase wiring**.

## Cross-Phase Wiring — 10/10 WIRED (carried)

No new wiring introduced by 8.2/8.3/8.4. All three additions reuse existing layered architecture:
- 8.2: a PreHook + PostHook on the existing plugin chain (REQ-PLUGIN-01)
- 8.3: an internal refactor of `acp.Client.Prompt()` — same external interface
- 8.4: 3 recognizers inside the existing `PIIRedactionHook` (PLUG-06)

The 2026-05-28 audit's 10/10 wiring verdict carries forward unchanged.

## End-to-End Flows — 9/9 cells green

Phase 08.4 added a new fixture row (`1111 Main Street, Austin, TX 27584`) exercised on all three surfaces. Operator HUMAN-UAT item 1 PASS on splunk-box v1.10.0 binary: `/v1/messages` (200 / 8205ms), `/v1/chat/completions` (200 / 9263ms), `/api/chat` (200 / 8964ms). 33/33 needle checks pass per surface. Prior 8 entity types still green.

## Requirements Coverage — 63/63 SATISFIED

Traceability table now has 63 rows (was 62). PII-01 added under "PII — Recognizer coverage" before v2 section. v1 count footer updated 62 → 63. Zero unsatisfied, zero orphaned.

| Pre-audit | This audit | Delta |
|-----------|------------|-------|
| 62/62 satisfied | 63/63 satisfied | +PII-01 (Phase 8.4) |

## Phase Verification — 13/15 passed, 1 NOT_VERIFIED ×2, 1 NOT_EXECUTED, 1 REVERTED

See `verification_per_phase:` in YAML frontmatter for the per-phase table.

**Phases shipped via `/gsd-quick` lack VERIFICATION.md** (08.2, 08.3). This is by design — the `/gsd-quick` path is for trivial tasks and skips the formal verifier. The SUMMARY.md for each captures what shipped; ROADMAP marks them complete. For audit hygiene, recommend a `/gsd-validate-phase 08.2` and `/gsd-validate-phase 08.3` pass post-close to retroactively generate VERIFICATION.md. Non-blocking.

**Phase 08.4** re-verified 2026-06-04T13:45 after HUMAN-UAT item 1 PASS. Status flipped human_needed → passed (commit ahead of audit). Score 14/14.

## Open Decision — Phase 08.3.1 disposition

Phase 08.3.1 (ACP Per-Session Stream Demux) is the only open scope item in v1.5. Three viable paths in YAML frontmatter (`phases_with_dispositions.inserted_but_not_executed[0].disposition_options`).

**Recommendation:** DEFER to v1.6. v1's POOL_SIZE=4 model binds each kiro-cli subprocess to a worker slot; the same `acp.Client` instance is not concurrently shared across sessions, so the WR-04 race is not exploitable in v1. The roadmap entry's `[ ] TBD` checkbox should flip to `[deferred] Deferred to v1.6 (multi-tenant concurrency hardening)` before milestone close — that single edit converts this audit's YELLOW to GREEN.

## Phase 08.1 Quality Gate Cascade (carried)

The prior audit's Phase 08.1 quality-gate cascade analysis remains valid. Phase 08.1's discrete trust-gate steps in `Makefile:207` and `.github/workflows/ci.yml` are unchanged; the post-audit phases added/modified code but did not relax the trust gates.

## Residual Items (NOT blockers)

**Same as 2026-05-28 audit, plus:**

**Deferred from Phase 08.4 (documented accepted_deviation):**
- WR-NEW-03 USState span subsumes trailing ZIP — operator audit guidance: sum USAddress + USState + USZIP for plaintext-removal accounting
- BL-NEW-01 acknowledged AP-2 residual on English-word + 5-digit-quantity ambiguity (inherent RE2 limit, no lookahead)
- HUMAN-UAT items 2-4 marked accepted_deviation (POSIX smoke covered by same Go binary code path; AP-2 negative-control + NER overlap-arbiter covered by go test -race suite)

**Carried from 2026-05-28:**
- Pre-existing gofumpt drift across `cmd/` + `internal/adapter/*` (16+ files; Phase 2/3.1/8 origin)
- Pre-existing `internal/admin/tail_test.go` using Go 1.24 `testing.Context()` while go.mod is 1.23 (Phase 6.1 origin)
- Nyquist coverage: 3/11 fully compliant — accepted-trailing concern
- Phase 02 SC#2 LangFlow zero-reconfig — operator-deferred load-bearing acceptance gate
- Phase 08 Task 7 7-step operator protocol against live binary — operator-deferred

**`make ci` will fail at `fmt-check`** until a follow-up cleanup phase resolves the gofumpt drift. Same disposition as prior audit: recommend a `make fmt` tree-wide pass + go.mod 1.24 bump as the first v1.6 task.

## Verdict

**v1.5 MILESTONE: YELLOW — pending 08.3.1 disposition decision.**

- Integration: **10/10 properties WIRED** (carried — no new wiring 8.2-8.4)
- Flows: **9/9 cells green** (+PII-01 fixture across all 3 surfaces)
- Requirements: **63/63 SATISFIED** (was 62/62; +PII-01)
- Phases: **13/14 closed**, 1 reverted (08.3.2), 1 inserted-not-executed (08.3.1)
- HUMAN-UAT item 1 PASS in production against v1.10.0 binary

**With 08.3.1 explicitly deferred to v1.6, milestone goes GREEN — ready for /gsd-complete-milestone v1.5.**

Recommended next steps:
1. Decide 08.3.1 disposition (defer/drop/execute) — see Open Decision section
2. If deferring: edit `[ ] TBD` → `[deferred] Deferred to v1.6` in ROADMAP.md line for Phase 08.3.1
3. (Optional) Retroactive `/gsd-validate-phase 08.2` + `/gsd-validate-phase 08.3` to generate missing VERIFICATION.md files
4. `/gsd-complete-milestone v1.5` — archive and tag
5. `/gsd-new-milestone v1.6` — first phase could bundle gofumpt cleanup + go.mod 1.24 bump + Phase 08.3.1 multi-tenant hardening
